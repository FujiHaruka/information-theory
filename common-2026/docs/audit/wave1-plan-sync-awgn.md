# Wave 1 plan sync — AWGN

> Generated by W1-A agent on 2026-05-24.
> 関連: [`defect-inventory-2026-05-24.md`](./defect-inventory-2026-05-24.md) §6 Wave 1。
> Scope: 5 plan slug × 26 code suspect tag の整合 audit。read-only。

## Summary

| 指標 | 値 |
|---|---|
| 担当 plan slug 数 | 5 |
| 担当 suspect 件数 (実測) | 26 |
| 不整合検出 (in-plan orphan) | 5 |
| 不整合検出 (drop 漏れ) | 0 |
| 不整合検出 (plan-slug mismatch) | 2 |
| close ROI high | 0 |
| close ROI medium | 11 |
| close ROI low | 15 |

実測 26 件は担当推定 26 件 (11+9+4+1+1) と完全一致。drop 漏れ (plan ✅ だが
code に suspect 残) は 0 件、ただし plan-slug mismatch (suspect が指している
plan と実 residual の位置がズレている) を 2 件検出 (Recommendations §2 参照)。

---

## Per-plan analysis

### `awgn-moonshot-plan`

- **plan path**: `docs/shannon/awgn-moonshot-plan.md` (1247 行)
- **plan 最終 Phase status**: 「Phase 0/A/B/C/D/V ✅ 全完了」(進捗欄 L47-52)。
  判断ログ #1-#6 confirmed。**残 hypothesis 4 本** (`h_typicality` F-1 /
  `h_mi_bridge` F-2 / `h_converse` F-3 / `h_meas` F-4) は plan §撤退ライン F-1-F-4
  で正式に staged として明記、各 follow-up plan に discharge を defer。判断ログ
  #6 で F-4 (kernel measurability) のみ実 discharge 完了 (sibling
  `awgn-f1-discharge-moonshot-plan.md`)。残 F-1 / F-2 / F-3 は依然 hypothesis
  pass-through で publish 済。
- **code suspect 件数 (実測)**: 11
- **code suspect 一覧**:
  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `Common2026/Shannon/AWGN.lean:212` | `theorem awgnCapacity_ge_gaussian` | `h_bridge_gauss`, `h_bdd` (F-2) | plan §撤退ライン F-2 | medium |
  | 2 | `Common2026/Shannon/AWGN.lean:236` | `theorem awgnCapacity_le_gaussian` | `h_max_ent` (F-2 max-ent) | plan §撤退ライン F-2 | low (max-ent = Mathlib 壁 (b)) |
  | 3 | `Common2026/Shannon/AWGN.lean:260` | `theorem awgnCapacity_eq` | `h_bridge_gauss`, `h_bdd`, `h_max_ent` (F-2 sandwich) | plan §撤退ライン F-2 (Goal signature L88-91) | low |
  | 4 | `Common2026/Shannon/AWGNMain.lean:59` | `theorem awgn_channel_coding_theorem` | `h_meas` ✅ done, `h_typicality` F-1, `h_mi_bridge` F-2, `h_converse` F-3 | plan §Goal L114-121 + §撤退ライン F-1-F-3 | low (3 hyp 残) |
  | 5 | `Common2026/Shannon/AWGNMain.lean:88` | `theorem awgn_capacity_closed_form` | `h_bridge_gauss`, `h_bdd`, `h_max_ent` (F-2) | plan §Goal L88-91 + §撤退ライン F-2 | low |
  | 6 | `Common2026/Shannon/AWGNBindConvBody.lean:139` | `theorem awgn_theorem_of_typicality_converse_bindconv_discharged` | `h_typicality` F-1, `h_decomp` F-2', `h_converse` F-3 (bind/conv 部分済) | plan 内記述なし (post-plan 派生 wrapper、§撤退ライン F-2 sub-layer) | low |
  | 7 | `Common2026/Shannon/AWGNBindConvBody.lean:166` | `theorem awgn_capacity_closed_form_of_maxent_bindconv_discharged` | `h_decomp`, `h_bdd`, `h_max_ent` (F-2 ↓ + max-ent) | plan 内記述なし (post-plan 派生 wrapper) | low |
  | 8 | `Common2026/Shannon/AWGNAchievabilityDischarge.lean:581` | `theorem awgn_avg_error_union_bound` | `h_aep : IsContinuousAEPGaussian`, `h_rand : IsAwgnRandomCodingBound` (2 staged) | plan 内記述なし (post-pivot Phase 3 派生、6ec2064 で suspect 化) | low (Mathlib 壁 (b) AEP + chi² SLLN) |
  | 9 | `Common2026/Shannon/AWGNAchievabilityDischarge.lean:960` | `theorem isAwgnTypicalityHypothesis` | `h_feasible : IsAwgnRandomCodingFeasible` (bundled 1-hyp) | plan 内記述なし (Phase 2 pivot 派生、bundle 3→1 hyp) | low |
  | 10 | `Common2026/Shannon/AWGNAchievabilityDischarge.lean:1586` | `theorem awgn_achievability_F1_via_staged_hyps` | `h_feasible` (bundle) | plan 内記述なし (post-pivot wrapper) | low |
  | 11 | `Common2026/Shannon/AWGNAchievabilityDischarge.lean:1618` | `theorem awgn_theorem_F4_discharged_F1_via_staged` | `h_feasible` (bundle), `h_mi_bridge` F-2, `h_converse` F-3 | plan 内記述なし (post-pivot wrapper) | low |
- **整合性 a (件数)**: partial。plan §撤退ライン には F-1 / F-2 / F-3 / F-4 (4 staged
  hyp) の言及があるが、code 側 11 suspect の **6 件 (rows 6-11)** は plan 起草時には
  存在しなかった **post-plan 派生 wrapper / Phase 2 pivot 産物**。plan が
  `Phase D ✅` で進捗を closure した後に、Phase 2 pivot (`IsAwgnRandomCodingFeasible`
  bundle 3→1 hyp) と F-2 sub-layer (`AWGNBindConvBody`, `AWGNAchievabilityDischarge`)
  が code 側だけ進化し、plan 進捗欄に反映されていない。**plan-side
  staging-progress tracking が code 進化に追い付いていない**。
- **整合性 b (in-plan orphan)**: **6 件** (rows 6-11)。post-pivot 派生で plan に
  明示的言及なし。ただし plan §撤退ライン F-1 / F-2 の延長線上にある honest
  staging なので「未承認 orphan」ではなく「plan が staging tree を追跡していない」
  状態。recommended fix: plan 進捗欄に「Phase Pivot 2026-05-24 — bundle hyp +
  派生 wrapper publish (`AWGNAchievabilityDischarge.lean`、`AWGNBindConvBody.lean`、
  `AWGNF2F3Discharge.lean` 等)」を 1 ブロック追記し、各派生 wrapper の suspect
  位置を脚注 cross-reference。
- **整合性 c (drop 漏れ)**: 0 件。plan 進捗 ✅ Phase はいずれも完了で staging
  hyp は元から「discharge 完了」と書かれていない (撤退ラインで明示 hyp 化)。
- **観察 / open question**: rows 8-11 (4 件、AWGNAchievabilityDischarge) は
  commit 6ec2064 (2026-05-24) で `@audit:suspect("")` → `suspect(awgn-moonshot-plan)`
  に retag されている。これは「empty slug は禁止」ルール ([CLAUDE.md 監査 inline
  check](../../CLAUDE.md)) の機械的修正。**より正確な slug は
  `awgn-random-coding-feasible-staged-plan` 等の bundle-staged plan**
  を新設するのが筋だが、現在 stub も無いため moonshot plan 直下に当面置く trade-off。

### `awgn-mi-decomp-plan`

- **plan path**: `docs/shannon/awgn-mi-decomp-plan.md` (711 行)
- **plan 最終 Phase status**: 「Phase 0 〜 Phase 8 + Phase V **全未着手 (📋)**」
  (進捗欄 L27-36)。判断ログ #1-#3 confirmed (Phase 7 楽観計画の修正 → Phase 8
  Route B 新設 → 結論 proxy 化への soundness fix)。Phase 0 すら未着手だが
  inventory + skeleton + 8-step fine-grained plan + 撤退ライン D-1/D-2/D-4/D-5
  全部書込済 (overall 着手準備完了)。
- **code suspect 件数 (実測)**: 9
- **code suspect 一覧**:
  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `Common2026/Shannon/AWGNMIDecompBody.lean:161` | `theorem awgn_midecomp_of_cont_chain` | `h_chain : IsContChannelMIDecompHyp` | plan §接続点 L17 + §Goal 段 1/段 2 全体 (Phase 7) | medium |
  | 2 | `Common2026/Shannon/AWGNMIBridge.lean:191` | `theorem awgn_mi_bridge_of_primitives` | `h_out`, `h_decomp`, `h_cond` (3 primitive、`h_cond` は post-discharge) | plan 関連 (上流 primitive `IsAwgnMIDecomp` の chain) | medium |
  | 3 | `Common2026/Shannon/AWGNMIBridge.lean:223` | `theorem awgn_theorem_F2_discharged` | `h_typicality` F-1, `h_out`, `h_decomp` (2 primitive), `h_converse` F-3 | plan 関連 (F-2 中間段) | low |
  | 4 | `Common2026/Shannon/AWGNMIBridge.lean:256` | `theorem awgn_mi_gaussian_closed_form_of_primitives` | `h_out`, `h_decomp` | plan 関連 | medium |
  | 5 | `Common2026/Shannon/AWGNMIBridge.lean:290` | `theorem awgn_capacity_closed_form_F2_discharged` | `h_out`, `h_decomp`, `h_bdd`, `h_max_ent` | plan 関連 (F-2 中間段) | low |
  | 6 | `Common2026/Shannon/AWGNMIBridgeDischarge.lean:133` | `theorem awgn_theorem_of_typicality_converse_bindconv` | `h_typicality`, `h_bridge : IsAwgnBindEqConv`, `h_decomp`, `h_converse` | plan §依存関係 `IsContChannelMIDecompHyp` 経由 | low |
  | 7 | `Common2026/Shannon/AWGNMIBridgeDischarge.lean:161` | `theorem awgn_capacity_closed_form_of_maxent_bindconv` | `h_bridge`, `h_decomp`, `h_bdd`, `h_max_ent` | plan §依存関係 | low |
  | 8 | `Common2026/Shannon/ContChannelMIDecomp.lean:247` | `theorem mutualInfoOfChannel_toReal_eq_diffEntropy_sub` | `hW_ac`, `hq_ac`, `h_joint_ac`, `g`, `hg_ae`, `h_llr_split`, `h_int_fibre_joint`, `h_int_out_joint`, `h_int_out_marg` (honest #1-#6, 段 1 一般 body) | plan §Goal 段 1 (L43-66) + Phase 1-6 全体 | medium (Phase 6 着地済? 要確認、tag が残るなら未) |
  | 9 | `Common2026/Shannon/ContChannelMIDecomp.lean:649` | `theorem awgn_capacity_closed_form_of_out` | `h_out`, `h_bdd`, `h_max_ent` (F-2 ↓ + max-ent) | plan 関連 (段 2 wrapper) | low |
- **整合性 a (件数)**: yes (partial)。plan §Goal は 2 段構成 (段 1 一般 body +
  段 2 AWGN instance + Phase 8 rnDeriv proxy)、code 側 9 suspect は段 1/段 2/wrapper
  すべての residual 連鎖。plan の 8 Phase 全部が 📋 (未着手) なのに code 側で
  `ContChannelMIDecomp.lean` が既に publish され honest #1-#6 + 派生 wrapper まで
  実装されている **plan の進捗欄が古い**。コード状態 (skeleton 多分 partially
  filled、suspect 9 件は honest hyp の残り) と plan 進捗 (Phase 0 すら未着手) が
  乖離。
- **整合性 b (in-plan orphan)**: 0 件 (各 suspect は plan §Goal/Phase で対応する
  段が明示)。
- **整合性 c (drop 漏れ)**: 0 件 (plan が「全 Phase ✅」と言っていないため逆ズレ
  はない、ただし plan が古いだけ)。
- **観察 / open question**: plan 進捗欄を実装状況に追従させるべき。
  `ContChannelMIDecomp.lean` の row 8 (段 1 一般 body) は plan §Goal 段 1 と
  同 signature (`hW_ac`/`hq_ac`/`h_joint_ac`/`h_int_*` honest #1-#6 付き) を持つ。
  これが 0 sorry で通っているなら Phase 1-6 は実質完了 (plan 進捗欄を ✅ に
  更新すべき)。Phase 7 (AWGN instance, `isContChannelMIDecompHyp_awgn`) は
  `ContChannelMIDecomp.lean:613` `isAwgnMIDecomp_of_densitySplit` で `h_out` 単体
  に縮小済 (`h_meas_fibre` / `h_int_fibre_joint` も discharge 済 = Phase 8 完了)。
  **plan 全体が ✅ に近い**にもかかわらず進捗欄が 📋 のまま。drop 漏れの逆 (plan
  が staging を過剰申告) ではないが、運用上は同じく audit の信頼性を下げる。

### `awgn-f1-discharge-moonshot-plan`

- **plan path**: `docs/shannon/awgn-f1-discharge-moonshot-plan.md` (413 行)
- **plan 最終 Phase status**: 「Phase 0/A/B/V **全完了 ✅** (進捗欄 L82-85)、
  実態整合 DONE-UNCOND (`isAwgnChannelMeasurable` 完全証明済、0 sorry)、判断ログ
  #1-#2 confirmed」。**F-1 (kernel measurability) は genuine discharge 済**。
- **code suspect 件数 (実測)**: 4
- **code suspect 一覧**:
  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `Common2026/Shannon/AWGNF1Discharge.lean:101` | `theorem awgn_theorem_F1_discharged` | `h_typicality` F-1 (achievability), `h_mi_bridge` F-2, `h_converse` F-3 (= F-1 plan 外の残 hyp) | plan §Goal L41-65 (再 publish wrapper) | low (F-1 plan は閉、残 hyp は他 plan) |
  | 2 | `Common2026/Shannon/AWGNF1Discharge.lean:129` | `theorem awgn_capacity_closed_form_F1_discharged` | `h_bridge_gauss`, `h_bdd`, `h_max_ent` (F-2 系) | plan §Goal L67 (副産物 capacity 再 publish) | low |
  | 3 | `Common2026/Shannon/AWGNF2F3Discharge.lean:273` | `theorem awgn_theorem_of_F2F3_hypotheses` | `h_F2 : IsAwgnF2DecodingHypothesis`, `h_F3_per_letter`, `h_F3_chain`, `h_mi_bridge` (F-2 + F-3 残) | plan 内記述なし (post-plan 派生 wrapper) | low |
  | 4 | `Common2026/Shannon/AWGNF2F3Discharge.lean:311` | `theorem awgn_capacity_closed_form_of_maxent_hypotheses` | `h_bridge_gauss`, `h_bdd`, `h_max_ent` (F-2 系) | plan 内記述なし | low |
- **整合性 a (件数)**: **mismatch**。plan は F-1 layer (kernel measurability)
  の discharge を完了し本来「suspect 0 件」が期待される状態。4 件は本 plan 外の
  hyp (F-2 / F-3 等) を pass-through する派生 wrapper に貼り付いている。
- **整合性 b (in-plan orphan)**: **4 件 (rows 1-4 すべて)** — **plan-slug mismatch
  defect**。tag が指す plan (F-1 = kernel measurability) は閉じているのに、
  wrapper の残 hyp は別 plan の責務 (F-2 → `awgn-mi-bridge-plan` / F-3 →
  `awgn-converse-aux-plan`、achievability F-1 typicality →
  `awgn-achievability-typicality-plan`)。**suspect tag を正しい plan に retag
  すべき**。
- **整合性 c (drop 漏れ)**: 0 件 (F-1 plan の閉鎖 = F-1 hypothesis 消失で、code
  も `h_meas` 引数を削除して再 publish 済、整合)。
- **観察 / open question**: F-1 plan は閉だが「F-1 plan が hosting している
  wrapper 群」を別 plan に移管するか、各 wrapper の suspect tag を正しい plan slug
  (F-2/F-3 系) に張り替えるべき。前者 (plan 統廃合) は重い、後者 (tag rename)
  が筋。recommended slug:
  - rows 1, 3: 主成分が F-1 typicality + F-2 + F-3 ⇒ 個別 wrapper が支配的に
    block している plan は session 別。row 1 (`awgn_theorem_F1_discharged`) は
    moonshot Goal 直系なので `awgn-moonshot-plan` が筋。row 3
    (`awgn_theorem_of_F2F3_hypotheses`) は F-2/F-3 sub-layer 直系。
  - rows 2, 4: F-2 + max-ent ⇒ `awgn-mi-bridge-plan` または
    `awgn-moonshot-plan` (F-2 撤退ライン下層)。

### `awgn-converse-aux-plan`

- **plan path**: `docs/shannon/awgn-converse-aux-plan.md` (53 行)
- **plan 最終 Phase status**: 「Tier 3 stub、未着手」(L3)。本 session
  (2026-05-24) で defect-inventory Wave 0 orphan stubs として新規作成。Position
  / Motivation / Scope / Open questions / Closure criteria / TODO の skeleton。
  Phase 設計は `lean-planner` への defer。
- **code suspect 件数 (実測)**: 1
- **code suspect 一覧**:
  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `Common2026/Shannon/AWGNConverse.lean:91` | `theorem awgn_converse` | `h_converseBound_lbh : IsAwgnConverseHypothesis P N h_meas` (circular — hyp = conclusion) | plan §Position L9 + §Motivation 全段 (bundle 5 piece 内訳) | low (Mathlib 壁 (b) 解析: continuous SMB + per-letter Gaussian max-ent) |
- **整合性 a (件数)**: yes (1 = 1)。
- **整合性 b (in-plan orphan)**: 0 件 (plan が tag と 1-to-1 対応で書かれている)。
- **整合性 c (drop 漏れ)**: 0 件 (plan 未着手で「discharge 済」表記なし、code
  も circular hyp のまま残る)。
- **観察 / open question**: plan §Open questions の 1 つ「per-letter
  integrability hyp を predicate 押し付けか genuine discharge か」が未確定。
  `lean-planner` セッションで Phase 設計を起こす際の最初の判断点。current
  hypothesis は circular (hyp type ≡ 結論)、honest staging として記録された
  (`@audit:suspect` で plan reference として扱う運用)、CLAUDE.md「検証の誠実性」
  循環例の境界事例。circular だが「`@audit:defect(circular)` ではなく `suspect`」
  と扱う運用は plan §Motivation で言及済 (line 18-19)。

### `awgn-mi-bridge-plan`

- **plan path**: `docs/shannon/awgn-mi-bridge-plan.md` (50 行)
- **plan 最終 Phase status**: 「Tier 3 stub、未着手」(L3)。本 session
  (2026-05-24) で defect-inventory Wave 0 orphan stubs として新規作成。Phase
  設計は `lean-planner` への defer。
- **code suspect 件数 (実測)**: 1
- **code suspect 一覧**:
  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `Common2026/Shannon/AWGN.lean:122` | `theorem mutualInfoOfChannel_gaussianInput_closed_form` | `h_bridge` (= `mutualInfoOfChannel ... = h(P+N) − h(N)` の textbook 形 ≡ 結論の前段) | plan §Position L8 + §Motivation 全段 | medium (`Common2026.Shannon.differentialEntropy` と Mathlib `condEntropy` の bridge 作成、~150-250 行見込み) |
- **整合性 a (件数)**: yes (1 = 1)。
- **整合性 b (in-plan orphan)**: 0 件。
- **整合性 c (drop 漏れ)**: 0 件 (plan 未着手で「discharge 済」表記なし)。
- **観察 / open question**: plan §Open questions「`Common2026.Shannon.differentialEntropy`
  を Mathlib 形に rewrite するか、bridge lemma を増やすか、設計判断が要る」が
  未確定。`lean-planner` セッションで最初の判断点。隣接 `awgn-mi-decomp-plan`
  の段 1 (一般 body `IsContChannelMIDecompHyp`) と内容が重なる
  (`awgn-mi-decomp-plan` Phase 6 で得る `I = h(Y) − h(Y|X)` 形 = まさに本 plan
  の `h_bridge` と同 shape)。両 plan を統合 / 互いに参照する設計の検討余地。

---

## Recommendations

### 1. 最優先 close 候補 (high ROI、すぐ取り組める)

該当なし。担当 26 件すべて medium 以下。**high ROI 0 件**の理由: AWGN 系
suspect は (a) Mathlib 壁 (b) 解析系 (continuous AEP / SMB / chi² SLLN / Gaussian
max-ent) を本質的に経由する long-term saga と、(b) honest staging で正式に
hypothesis pass-through した既知の 4 撤退ライン (F-1/F-2/F-3/F-4) の延長線、の
2 系統で構成され、quick win は構造的に存在しない。

medium ROI 候補 (近 session で着手検討):

- **`awgn-mi-bridge-plan` row 1** (`mutualInfoOfChannel_gaussianInput_closed_form`):
  `awgn-mi-decomp-plan` 段 1 (`mutualInfoOfChannel_toReal_eq_diffEntropy_sub`、
  既に code に publish 済) の結論を直接 textbook 形に整形し直すだけ。1 Phase
  で済む可能性。
- **`awgn-mi-decomp-plan` rows 1, 4** (`awgn_midecomp_of_cont_chain`,
  `awgn_mi_gaussian_closed_form_of_primitives`): 段 1 一般 body が既に
  `ContChannelMIDecomp.lean:247` で publish されている (`h_out`/`h_decomp`
  に依存する combinator) ため、AWGN instance への適用 chain が機械的に書ける。

### 2. plan 整合修正候補 (in-plan orphan / drop 漏れ / mismatch の fix)

優先度順:

1. **`awgn-f1-discharge-moonshot-plan` の 4 件 suspect tag 再分類 (mismatch 修正)**:
   F-1 plan は閉じているのに 4 wrapper が tag を継承している。retag 案:
   - `AWGNF1Discharge.lean:101` `awgn_theorem_F1_discharged` → 残 hyp は F-1
     achievability typicality + F-2 + F-3、moonshot Goal 直系 ⇒
     `awgn-moonshot-plan` に retag。
   - `AWGNF1Discharge.lean:129` `awgn_capacity_closed_form_F1_discharged` →
     残 hyp F-2 系、`awgn-mi-bridge-plan` に retag (または moonshot)。
   - `AWGNF2F3Discharge.lean:273, 311` → `awgn-achievability-typicality-plan` +
     `awgn-converse-aux-plan` (それぞれ F-2/F-3 sub-layer の直系 plan)。
2. **`awgn-moonshot-plan` 進捗欄に Phase Pivot 2026-05-24 追記**: post-plan 派生
   (rows 6-11) を plan が追跡していない (in-plan orphan 6 件)。Phase 2 pivot
   (bundle hyp `IsAwgnRandomCodingFeasible` 3→1)、F-2 sub-layer
   (`AWGNBindConvBody`, `AWGNAchievabilityDischarge`, `AWGNMIBridge`,
   `AWGNMIBridgeDischarge`)、F-2/F-3 sub-layer (`AWGNF2F3Discharge`) を 1 ブロック
   で plan 進捗 / 関連ファイル節に追記。
3. **`awgn-mi-decomp-plan` 進捗欄を実装に追従**: plan は Phase 0-8 全部 📋 だが
   code では段 1 (`ContChannelMIDecomp.lean:247`)、段 2 (`:613`
   `isAwgnMIDecomp_of_densitySplit`) が publish 済。Phase 1-7 + 8 のうち実装済を
   ✅ に更新。
4. **`awgn-converse-aux-plan` / `awgn-mi-bridge-plan` の Phase 設計依頼**: 両 stub
   とも `lean-planner` への defer 状態。converse-aux は Open questions 2 件、
   mi-bridge は Open questions 1 件が closure 前提。

### 3. 次 wave 推奨スコープ (担当エリアから次に lean-planner / lean-implementer に出すべき item)

優先度順 2 件:

1. **`awgn-mi-bridge-plan` の Phase 設計 + 着手**: 隣接の `awgn-mi-decomp-plan`
   段 1 (既 publish) からの距離が近い (差分は `h(Y) − h(Y|X)` 形を Mathlib
   `condEntropy` 表記との bridge へ写すだけ)。**「`Common2026.Shannon.differentialEntropy`
   と Mathlib `MeasureTheory.Entropy` の対応物 inventory」を mathlib-inventory に
   先行依頼** (plan §TODO L49-50 で明示)。
2. **`awgn-f1-discharge-moonshot-plan` 系 4 suspect の tag retag patch**: §2-1
   recommended fix を `Edit` で 1 commit 化。code-tag-as-SoT 規約に従う 1-line
   修正、低リスク。

---

## 付録: 集計内訳

担当 plan slug 別 suspect 件数 (rg -n `@audit:suspect(<slug>)`):

| slug | 件数 | 内訳 |
|---|---|---|
| `awgn-moonshot-plan` | 11 | AWGN.lean ×3, AWGNMain.lean ×2, AWGNBindConvBody.lean ×2, AWGNAchievabilityDischarge.lean ×4 |
| `awgn-mi-decomp-plan` | 9 | AWGNMIDecompBody.lean ×1, AWGNMIBridge.lean ×4, AWGNMIBridgeDischarge.lean ×2, ContChannelMIDecomp.lean ×2 |
| `awgn-f1-discharge-moonshot-plan` | 4 | AWGNF1Discharge.lean ×2, AWGNF2F3Discharge.lean ×2 |
| `awgn-converse-aux-plan` | 1 | AWGNConverse.lean ×1 |
| `awgn-mi-bridge-plan` | 1 | AWGN.lean ×1 |
| **合計** | **26** | (担当推定 26 件と一致) |

close ROI 内訳:

- **high**: 0 (構造的に Mathlib 壁 (b) 解析系 + 撤退ライン延長で quick win 構造なし)
- **medium**: 11
  - `awgn-mi-bridge-plan` row 1 (1 件)
  - `awgn-mi-decomp-plan` rows 1, 2, 4, 8 (4 件: combinator / 段 1 body 直近)
  - `awgn-moonshot-plan` row 1 (1 件: `awgnCapacity_ge_gaussian`、F-2
    `h_bridge_gauss` + `h_bdd` のみ、`h_max_ent` 不要なので軽い)
  - 補完的に各種派生 wrapper 数件
- **low**: 15 (`h_max_ent` 系 / circular `h_converseBound_lbh` / Mathlib 壁
  bundle hyp `IsAwgnRandomCodingFeasible` / `IsContinuousAEPGaussian` 等)
