# Wave 1 plan sync — Channel coding (Relay / MAC / Broadcast / DMC)

> Generated by W1-C agent on 2026-05-24.
> 関連: [`defect-inventory-2026-05-24.md`](./defect-inventory-2026-05-24.md) §6 Wave 1。

## Summary

| 指標 | 値 |
|---|---|
| 担当 plan slug 数 | 7 |
| 担当 suspect 件数 (実測) | 71 |
| 不整合検出 (in-plan orphan) | 5 |
| 不整合検出 (drop 漏れ) | 0 |
| close ROI high | 8 |
| close ROI medium | 17 |
| close ROI low | 46 |

> **Note (count)**: 親 brief 推定 68 件に対し実測 71 件 (+3)。差分:
> `MultipleAccessChannel.lean` の `mac_capacity_region_outer_bound` 系
> (line 576, 648) と inner_bound 系 (line 771, 788, 821) の細分化が plan の
> 「2 主定理」表現より多く下流ヘルパに展開されているため。
> rg-verify は §Per-plan で総計 71 を確認 (33+17+11+3+3+3+1 = 71)。

---

## Per-plan analysis

### `relay-inner-bound-moonshot-plan` (33 件 — Phase 別集約)

- **plan path**: `docs/shannon/relay-inner-bound-moonshot-plan.md` (318 行)
- **plan 最終 Phase status**: 「Phase 0–V 全項目 `[ ]` のまま」だが Status 節
  (line 32) に **「PASS-THROUGH (計画通り) — 全 Phase 実装済」「plan の `[ ]` は STALE」**
  と実態整合済。撤退ライン L-RI1–L-RI4 全 4 本確定発動 (DF block Markov / DF
  sliding-window / CF Wyner-Ziv binning / CF SI decoding を `True` placeholder /
  load-bearing hyp で受ける設計)。
- **code suspect 件数 (実測)**: 33
- **Phase 別 suspect 分布**:

  | file | 件数 | 役割 | close ROI 多数派 |
  |---|---|---|---|
  | `RelayInnerBodyDischarge.lean` | 15 | L-RI1–4 witness 形 (DF/CF 各 `_of_witness` + `_min_form` / `_two_bounds` / `_log_rate` / `_consistent` / `_via_witness` の variant 群) | low (block Markov / WZ binning witness は Mathlib 壁) |
  | `RelayInnerBound.lean` | 9 | 主定理 (`relay_df_inner_bound`, `relay_cf_inner_bound`) + 4 つの form-variant (min / two-bounds / log-rate / consistent) | low |
  | `RelayCFBinningBody.lean` | 5 | CF binning body (`relay_cf_si_decoder_fail_tendsto`, `relay_cf_existence_of_witness`, `relay_cf_inner_bound_binning_discharged` 系) | low (WZ binning 自前 plumbing) |
  | `RelayDFBlockMarkovBody.lean` | 4 | DF block Markov witness 構成 (`relayDFRateWitness_of_encoder_hyp` 等) | low (block Markov ~600-1000 行) |

- **個別 suspect (主定理 + form-variant 一覧)**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `RelayInnerBound.lean:483` | `relay_df_inner_bound` | `h_ach : RelayDFAchievable` | ✓ L-RI1+L-RI2 (主定理) | low |
  | 2 | `RelayInnerBound.lean:506` | `relay_df_inner_bound_min_form` | `h_ach` + `h_min` | ✓ 主定理 form-variant | low (経由 1) |
  | 3 | `RelayInnerBound.lean:523` | `relay_df_inner_bound_two_bounds` | `h_ach` + `h₁`, `h₂` | ✓ 主定理 form-variant | low (経由 1) |
  | 4 | `RelayInnerBound.lean:538` | `relay_df_inner_bound_log_rate` | `h_ach` | ✓ Phase D log-rate wrapper | low (経由 1) |
  | 5 | `RelayInnerBound.lean:589` | `relay_cf_inner_bound` | `h_ach : RelayCFAchievable` | ✓ L-RI3+L-RI4 (主定理) | low |
  | 6 | `RelayInnerBound.lean:604` | `relay_cf_inner_bound_two_conditions` | `h_ach` + 2 cond | ✓ form-variant | low (経由 5) |
  | 7 | `RelayInnerBound.lean:616` | `relay_cf_inner_bound_log_rate` | `h_ach` | ✓ Phase D wrapper | low (経由 5) |
  | 8 | `RelayInnerBound.lean:649` | `relay_df_consistent` | `h_ach` + cut-set | ✓ Phase D two-side | low (経由 1 + cutset) |
  | 9 | `RelayInnerBound.lean:675` | `relay_cf_consistent` | `h_ach` + cut-set | ✓ Phase D two-side | low (経由 5 + cutset) |
  | 10 | `RelayInnerBodyDischarge.lean:155` | `RelayDFInnerBoundExistence_of_witness` | `IsRelayDFBlockMarkovWitness` | plan は body discharge を「別 plan に defer」明示。ここは witness API を介した修飾、対応 ✓ | low |
  | 11 | `RelayInnerBodyDischarge.lean:172` | `relay_df_body_from_witness` | `IsRelayDFBlockMarkovWitness` | ✓ | low (経由 10) |
  | 12 | `RelayInnerBodyDischarge.lean:345` | `RelayCFInnerBoundExistence_of_witness` | `IsRelayCFBinningWitness` | ✓ | low |
  | 13 | `RelayInnerBodyDischarge.lean:361` | `relay_cf_body_from_witness` | `IsRelayCFBinningWitness` | ✓ | low (経由 12) |
  | 14 | `RelayInnerBodyDischarge.lean:395` | `relay_df_inner_bound_discharged` | `IsRelayDFBlockMarkovWitness` | ✓ | low |
  | 15 | `RelayInnerBodyDischarge.lean:409` | `relay_df_inner_bound_discharged_min_form` | (経由 14) | ✓ | low |
  | 16 | `RelayInnerBodyDischarge.lean:423` | `relay_df_inner_bound_discharged_two_bounds` | (経由 14) | ✓ | low |
  | 17 | `RelayInnerBodyDischarge.lean:437` | `relay_cf_inner_bound_discharged` | `IsRelayCFBinningWitness` | ✓ | low |
  | 18 | `RelayInnerBodyDischarge.lean:451` | `relay_cf_inner_bound_discharged_two_conditions` | (経由 17) | ✓ | low |
  | 19 | `RelayInnerBodyDischarge.lean:465` | `relay_df_inner_bound_discharged_log_rate` | (経由 14) | ✓ | low |
  | 20 | `RelayInnerBodyDischarge.lean:483` | `relay_cf_inner_bound_discharged_log_rate` | (経由 17) | ✓ | low |
  | 21 | `RelayInnerBodyDischarge.lean:516` | `relay_df_consistent_discharged` | `IsRelayDFBlockMarkovWitness` + Fano + chain | ✓ | low |
  | 22 | `RelayInnerBodyDischarge.lean:541` | `relay_cf_consistent_discharged` | `IsRelayCFBinningWitness` + Fano + chain | ✓ | low |
  | 23 | `RelayInnerBodyDischarge.lean:582` | `relay_df_inner_bound_via_witness` | `IsRelayDFBlockMarkovWitness` | ✓ | low |
  | 24 | `RelayInnerBodyDischarge.lean:597` | `relay_cf_inner_bound_via_witness` | `IsRelayCFBinningWitness` | ✓ | low |
  | 25 | `RelayCFBinningBody.lean:361` | `relay_cf_si_decoder_fail_tendsto` | `h_asymp` + JT | ✓ L-RI4 (SI decode) | low |
  | 26 | `RelayCFBinningBody.lean:429` | `relay_cf_existence_of_witness` | `IsRelayCFBinningWitness` | ✓ L-RI3+4 | low |
  | 27 | `RelayCFBinningBody.lean:464` | `relay_cf_inner_bound_binning_discharged` | binning JT | ✓ L-RI3 | low |
  | 28 | `RelayCFBinningBody.lean:491` | `relay_cf_inner_bound_binning_discharged_two_conditions` | (経由 27) | ✓ | low |
  | 29 | `RelayCFBinningBody.lean:530` | `relay_cf_consistent_binning_discharged` | (27 + cutset) | ✓ | low |
  | 30 | `RelayDFBlockMarkovBody.lean:310` | `relayDFRateWitness_of_encoder_hyp` | `IsBlockMarkovEncoderHyp` | ✓ L-RI1 (block Markov encoder) | low |
  | 31 | `RelayDFBlockMarkovBody.lean:344` | `relayDFRateWitness_of_sub_hyps` | enc + dec + typ sub-hyps | ✓ L-RI1+2 | low |
  | 32 | `RelayDFBlockMarkovBody.lean:379` | `relayDFRateWitness_of_sub_hyps'` | (経由 31) | ✓ | low |
  | 33 | `RelayDFBlockMarkovBody.lean:398` | `relay_df_inner_bound_block_markov_discharged_region` | enc + region | ✓ L-RI1 | low |

- **整合性 a/b/c**:
  - **a. 件数整合**: ✓ — plan の主定理 2 本 (DF/CF) × 撤退ライン 4 本に対し、現実装は form-variant × witness API を含む 33 件展開。plan が掲げた 「L-RI1〜4 全 pass-through で publish」 と整合、ただし plan 上は witness API (`IsRelayDFBlockMarkovWitness`, `IsRelayCFBinningWitness`) や `RelayInnerBodyDischarge.lean` の存在自体に言及がなく、stale。
  - **b. 個別対応**: 33/33 が plan の L-RI1–4 撤退ラインに紐付き可。
    **In-plan orphan: `RelayInnerBodyDischarge.lean` (15 件) / `RelayCFBinningBody.lean` (5 件) / `RelayDFBlockMarkovBody.lean` (4 件) という file 自体が plan の「Phase A–V」記述に登場しない**。Status 行は file 名を `Common2026/Shannon/RelayInnerBound.lean` のみ言及。下流の body-discharge layer が暗黙で追加された痕跡。
  - **c. drop 漏れ**: ✓ なし — plan は「discharge 済」を主張していない。
- **観察 / open question**:
  - **plan stale**: 進捗マーカー `[ ]` のまま、実装は完了。Status 一文で実態整合だが、Phase A–E が `[ ]` のままなのは misleading。`[x]` への置換 + body-discharge 兄弟 file 言及を plan に追記する価値あり。
  - **連鎖 close**: #1, #5 (主定理 2 本) を close できれば form-variant 6 件 (#2–4, 6–9) と witness wrapper 15 件 (#10–24) が transitive に close 候補。事実上は **L-RI1 (block Markov) + L-RI3 (WZ binning) の本体 discharge 2 つで 33 件すべて連鎖 close**。

---

### `mac-moonshot-plan` (17 件 — Phase 別集約)

- **plan path**: `docs/shannon/mac-moonshot-plan.md` (766 行)
- **plan 最終 Phase status**: Status 行 (line 41) に「PASS-THROUGH (計画通り) — 全 Phase 実装済」「Phase 0 起草中表記は STALE」。撤退 5 本 (L-MAC1 joint typ / L-MAC2 Fano+chain / L-MAC3 inner existence / L-MAC4 outer rate bound / L-MAC5 time-sharing scope-out) 全件発動。
- **code suspect 件数 (実測)**: 17
- **Phase 別 suspect 分布**:

  | file | 件数 | 役割 | close ROI 多数派 |
  |---|---|---|---|
  | `MultipleAccessChannel.lean` | 8 | 主定理 (`mac_capacity_region_outer_bound`, `mac_capacity_region_inner_bound`) + per-user/sum cut bounds + `three_bounds` / `bundled_strict` / `consistent` variants | medium (Fano+chain は genuine 実装、joint typ はlow) |
  | `MACBodyDischarge.lean` | 2 | body discharge layer (`mac_capacity_region_inner_bound_with_body`, `_with_body_two_side`) | low |
  | `MACFanoConverseBody.lean` | 4 | per-user Fano 経由 outer bound (`mac_single_rate_bound₁/₂_with_fano`, `_with_per_user_fano`, `_of_measure`) | medium (Fano body 自前で実装済、L-MAC2 部分 discharge) |
  | `MACPerEventAEPDecay.lean` | 1 | per-event AEP decay 経由 consistent (`mac_capacity_region_consistent_of_perEvent`) | low |
  | `MACCornerPoint.lean` | 2 | pentagon/region 同値 (`_subset_pentagon`, `_is_pentagon`) | medium (time-sharing scope-out L-MAC5 を hyp 化) |

- **個別 suspect**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `MultipleAccessChannel.lean:448` | `mac_single_rate_bound₁` | `h_fano`, `h_chain`, `h_cleanup` (scalar) | ✓ Phase B (single-rate bound) | medium |
  | 2 | `MultipleAccessChannel.lean:473` | `mac_single_rate_bound₂` | (同上) | ✓ Phase B | medium |
  | 3 | `MultipleAccessChannel.lean:501` | `mac_sum_rate_bound` | (同上、sum) | ✓ Phase B | medium |
  | 4 | `MultipleAccessChannel.lean:576` | `mac_capacity_region_outer_bound` | h_fano₁/₂/joint + h_chain₁/₂/joint + h_cleanup | ✓ Phase C (outer 主定理) | medium |
  | 5 | `MultipleAccessChannel.lean:648` | `mac_capacity_region_outer_bound_three_bounds` | h₁, h₂, hs (3 ineq) | ✓ Phase C variant | medium |
  | 6 | `MultipleAccessChannel.lean:771` | `mac_capacity_region_inner_bound` | `h_jt : MACJointTypicalityAchievable` | ✓ Phase D (inner 主定理 / L-MAC1) | low |
  | 7 | `MultipleAccessChannel.lean:788` | `mac_capacity_region_inner_bound_bundled_strict` | (経由 6) | ✓ form-variant | low |
  | 8 | `MultipleAccessChannel.lean:821` | `mac_capacity_region_consistent` | outer + inner combined | ✓ Phase E two-side | low |
  | 9 | `MACBodyDischarge.lean:609` | `mac_capacity_region_inner_bound_with_body` | `h_jt` | **in-plan orphan** — plan に file/declaration 名なし。L-MAC1 partial discharge layer の hook。 | low |
  | 10 | `MACBodyDischarge.lean:650` | `mac_capacity_region_with_body_two_side` | (経由 9 + outer) | **in-plan orphan** | low |
  | 11 | `MACCornerPoint.lean:374` | `mac_capacity_region_subset_pentagon` | `IsMACTimeSharingHyp` (L-MAC5) | ✓ L-MAC5 hyp 化 (plan で scope-out 明示) | medium |
  | 12 | `MACCornerPoint.lean:388` | `mac_capacity_region_is_pentagon` | time-sharing hyp | ✓ L-MAC5 | medium |
  | 13 | `MACFanoConverseBody.lean:265` | `mac_single_rate_bound₁_with_fano` | `MACFanoEntropyData` (genuine Fano) + chain | ✓ Phase B Fano body discharged form | medium (これは partial L-MAC2 discharge) |
  | 14 | `MACFanoConverseBody.lean:280` | `mac_single_rate_bound₂_with_fano` | (同上) | ✓ | medium |
  | 15 | `MACFanoConverseBody.lean:299` | `mac_capacity_region_outer_bound_with_per_user_fano` | per-user entropy data + chain + joint Fano | ✓ Phase C with-Fano variant | medium |
  | 16 | `MACFanoConverseBody.lean:338` | `mac_capacity_region_outer_bound_of_measure` | per-user data + joint Fano + measure-theoretic packaging | ✓ Phase C measure form | medium |
  | 17 | `MACPerEventAEPDecay.lean:420` | `mac_capacity_region_consistent_of_perEvent` | per-event AEP decay + Fano + chain + cleanup | **in-plan orphan** — file 名は plan に出てこない (per-event AEP は下流の implementation detail) | low |

- **整合性 a/b/c**:
  - **a. 件数整合**: ✓ — plan の主定理 2 本 + 撤退 5 本に対し、現実装は 17 件 (主定理 form-variant + body-discharge layer)。
  - **b. 個別対応**: 14/17 が plan に明示対応。**in-plan orphan 3 件**: #9, #10 (`MACBodyDischarge.lean`), #17 (`MACPerEventAEPDecay.lean`) — file 自体が plan の Phase A-V 記述・依存関係欄に登場しない。
  - **c. drop 漏れ**: ✓ なし。
- **観察 / open question**:
  - **plan stale 同型**: Phase progress `[ ]` のまま実態完成。Status 行で実態整合済だが misleading。
  - **MACFanoConverseBody.lean (4 件) は genuine Fano body discharge**: plan の L-MAC2 を hyp 化と異なり、ここでは `MACFanoEntropyData` ベースで Fano content を実体化。**L-MAC2 partial discharge 進行済 = plan 表記より進歩**。これも plan に反映すべき。

---

### `broadcast-channel-moonshot-plan` (11 件)

- **plan path**: `docs/shannon/broadcast-channel-moonshot-plan.md` (729 行)
- **plan 最終 Phase status**: Status 行 (line 34) に「PASS-THROUGH (計画通り) — 全 Phase 実装済」「Phase 0 起草中は STALE」。撤退 5 本 (L-BC1 joint typ / L-BC2 Fano+chain / L-BC3 inner existence / L-BC4 outer rate / L-BC5 non-degraded scope-out) 全件発動。
- **code suspect 件数 (実測)**: 11
- **個別 suspect**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `BroadcastChannel.lean:442` | `bc_common_rate_bound` | `h_commonRateBound_lbh : R₂ ≤ I_u` (load-bearing) | ✓ Phase B common rate bound | medium |
  | 2 | `BroadcastChannel.lean:474` | `bc_private_rate_bound` | `h_privateRateBound_lbh` | ✓ Phase B private rate bound | medium |
  | 3 | `BroadcastChannel.lean:595` | `bc_capacity_region_outer_bound_corner_limit` | Fano₁/₂ + chain_u/xy + cleanup + h_ε ≤ 0 | ✓ Phase C corner limit | medium |
  | 4 | `BroadcastChannel.lean:745` | `bc_capacity_region_inner_bound` | `h_ach : BCSuperpositionAchievable` | ✓ Phase D inner 主定理 (L-BC1) | low |
  | 5 | `BroadcastChannel.lean:769` | `bc_capacity_region_inner_bound_bundled_strict` | (経由 4) | ✓ form-variant | low |
  | 6 | `BroadcastChannel.lean:802` | `bc_capacity_region_consistent` | outer + inner combined | ✓ Phase E two-side | low |
  | 7 | `BroadcastChannelExistenceBridgeBody.lean:313` | `bc_random_codebook_markov_of_ensemble` | `IsBCBonferroniEnsembleDecay` | **in-plan orphan** — bridge body 系は plan に file 名なし | medium |
  | 8 | `BroadcastChannelExistenceBridgeBody.lean:371` | `bc_inner_bound_with_ensemble_averaging` | (経由 7 + strict) | **in-plan orphan** | medium |
  | 9 | `BroadcastChannelExistenceBridgeBody.lean:390` | `bc_inner_bound_with_ensemble_averaging_bundled` | (経由 8) | **in-plan orphan** | medium |
  | 10 | `BroadcastChannelSuperposition.lean:546` | `bc_capacity_region_inner_bound_with_superposition_aep` | `BCSuperpositionAchievable` | ✓ 兄弟 plan で別途参照 (`BroadcastChannelSuperposition`) | low |
  | 11 | `BroadcastChannelSuperpositionBody.lean:836` | `bc_capacity_region_inner_bound_with_superposition_body` | (経由 10) | ✓ 兄弟 | low |

- **整合性 a/b/c**:
  - **a. 件数整合**: ✓ plan の主定理 2 本 + variants + 撤退 5 本に対し 11 件で妥当。
  - **b. 個別対応**: 8/11 が plan に明示対応。**in-plan orphan 3 件** (#7–9, `BroadcastChannelExistenceBridgeBody.lean`)。Plan 「依存関係」「Critical Files」欄にも記載なし。
  - **c. drop 漏れ**: ✓ なし。
- **観察 / open question**:
  - body-discharge sibling file (`BroadcastChannelSuperposition*`, `BroadcastChannelExistenceBridgeBody`, `BroadcastChannelAveraging`) が plan に未言及だが、Status 行で軽く触れている。plan に explicit pointer を追記すべき。

---

### `relay-cutset-moonshot-plan` (3 件)

- **plan path**: `docs/shannon/relay-cutset-moonshot-plan.md` (892 行)
- **plan 最終 Phase status**: Status 行 (line 32) に「PASS-THROUGH (計画通り) — 実装済」。撤退 4 本 (L-RC1 Csiszár / L-RC2 chain rule / L-RC3 composite rate / L-RC4 measurability bundle) + L-RC5 inner bound scope-out。
- **code suspect 件数 (実測)**: 3
- **個別 suspect**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `RelayCutset.lean:424` | `relay_broadcast_cut` | `RelayBcastCutFano` + `h_chain` + `h_cleanup` | ✓ Phase B `relay_broadcast_cut` | medium (Fano + chain hyp、scalar form) |
  | 2 | `RelayCutset.lean:464` | `relay_mac_cut` | `RelayMacCutFano` + `h_chain` + `h_cleanup` | ✓ Phase B `relay_mac_cut` | medium |
  | 3 | `RelayCutset.lean:535` | `relay_cutset_outer_bound` | 2 Fano + 2 chain + 2 cleanup | ✓ Phase C 主定理 | medium |

- **整合性 a/b/c**:
  - **a. 件数整合**: ✓ plan の 2 cut + 主定理に対応、3/3。
  - **b. 個別対応**: 3/3 完全 match。
  - **c. drop 漏れ**: ✓ なし。
- **観察 / open question**:
  - Plan の Status 行は `_h_csiszar : True` + `_h_chain : True` + `h_rate_bound : R ≤ ...` と書いているが、現実装では `True` placeholder ではなく **scalar Fano (`RelayBcastCutFano`, `RelayMacCutFano`) + chain rule hyp** に進化済。**plan より実装が進んだ partial L-RC1+2 discharge**。plan に反映価値あり。
  - 連鎖: relay-inner-bound の `relay_df_consistent` / `relay_cf_consistent` がこの 3 件を消費。`RelayCutset` 側を genuine discharge できれば relay-inner-bound 9 件の consistent 系も部分的に進む。

---

### `dmc-feedback-capacity-plan` (3 件)

- **plan path**: `docs/shannon/dmc-feedback-capacity-plan.md` (164 行)
- **plan 最終 Phase status**: Phase 0–D 全件 `[x]` (完了済)、後継 E-10' で per-letter bound 純粋証明済。本 plan は **MVP scope = per-letter bound を hypothesis** のまま publish。Status 行で「DONE — MVP 形だが E-10' で `feedback_per_letter_bound` を剥がし完全形 `channel_coding_feedback_converse_memoryless` 結論済」と明示。
- **code suspect 件数 (実測)**: 3
- **個別 suspect**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `ChannelCodingFeedback.lean:169` | `channel_coding_feedback_converse_chain` | `h_per_letter : ∀ i, condMI ≤ I(X_i;Y_i)` | ✓ Phase C.1 MVP form。E-10' で discharge 済 | **high** (E-10' で `feedback_per_letter_bound` 既存、call すれば close) |
  | 2 | `ChannelCodingFeedback.lean:190` | `channel_coding_feedback_converse_capacity` | `h_per_letter` + `h_capacity` | ✓ Phase C.2 MVP | **high** (同上) |
  | 3 | `ChannelCodingFeedback.lean:249` | `channel_coding_feedback_converse` | `h_per_letter` + `h_capacity` + uniform + cardinality | ✓ Phase D.1 MVP | **high** (同上) |

- **整合性 a/b/c**:
  - **a. 件数整合**: ✓ MVP scope の 3 layer (chain / capacity / Fano 合成) に対応。
  - **b. 個別対応**: 3/3 完全 match。
  - **c. drop 漏れ**: ⚠ **微弱な drop 漏れ候補** — 本 plan の Status 行で「E-10' で `feedback_per_letter_bound` (`:116`) と `channel_coding_feedback_converse_memoryless` (`:171`) が完全形を結論済」と書いており、**本 plan slug の 3 つの MVP 形 suspect は完全形が別 file (`ChannelCodingFeedbackComplete.lean`) で提供されているため、MVP の `@audit:suspect` を `@audit:retract-candidate(superseded-by-memoryless-form)` に置換可能**。
- **観察 / open question**:
  - **後継 E-10' との関係**: 完全形 `channel_coding_feedback_converse_memoryless` が無条件 publish 済なら、MVP 3 件は実質「同じ事実の別表現」=  superseded。retract-candidate 候補。

---

### `channel-coding-shannon-theorem-full-plan` (3 件)

- **plan path**: `docs/shannon/channel-coding-shannon-theorem-full-plan.md` (290 行)
- **plan 最終 Phase status**: Phase D.0–D.4 全件 `[x]`、主定理 `shannon_noisy_channel_coding_theorem_general_full` (`ChannelCodingShannonTheoremFullDischarge.lean:1588`、0 sorry) で **`hW_pos` も `h_passthrough` も無し** の完全無条件 publish 済。MVP file の `shannon_noisy_channel_coding_theorem_general` は依然 `h_passthrough` 形で残存と明記。
- **code suspect 件数 (実測)**: 3
- **個別 suspect**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `ChannelCodingConverseGeneralComplete.lean:372` | `memoryless_per_summand_bound` | `_h_memo` + `h_yother_zero` + `h_split` (derivable, hyp 形) | plan は本 file/declaration に直接言及なし — converse 系で channel-coding-converse-general-plan の方が直接担当か。**in-plan orphan (本 slug 視点)** | medium |
  | 2 | `ChannelCodingConverseGeneralComplete.lean:477` | `channel_coding_converse_general_memoryless` | hmarkov + h_memo + h_yother_zero + h_split + hMsg_uniform + hcard | plan 言及なし。**in-plan orphan (本 slug 視点)** | medium |
  | 3 | `ChannelCodingShannonTheoremFull.lean:53` | `shannon_noisy_channel_coding_theorem_general` | `h_passthrough` (MVP form) | ✓ plan Phase D.4 で「`h_passthrough` を discharge した `_full` 版が無条件最終形、MVP file は MVP として残る」と明記 → **drop 漏れではない、共存意図** | **high** (`_full` 版が無条件 publish 済、MVP は retract-candidate) |

- **整合性 a/b/c**:
  - **a. 件数整合**: 3 件中 1 件 (#3) は plan に明示対応、2 件 (#1, #2) は converse 系で本 slug の scope と微妙にずれる。slug 命名が「shannon-theorem-full」だが converse generaul complete (`channel-coding-converse-general-plan` / `channel-coding-converse-memoryless-ychain-plan` 系) と suspect 区分が曖昧。
  - **b. 個別対応**: 1/3 match。**in-plan orphan 2 件 (#1, #2)** — slug 帰属の判断ミスの可能性大。
  - **c. drop 漏れ**: 完全 discharge form (`_full`) が publish 済にもかかわらず MVP 形 #3 が suspect のまま残るのは、plan が「共存意図」と明記しているため honest defect ではないが、**`@audit:retract-candidate(superseded-by-full-discharge)` への置換候補**。
- **観察 / open question**:
  - **slug 帰属の見直しが必要**: #1, #2 (`ChannelCodingConverseGeneralComplete.lean`) の suspect は `channel-coding-converse-memoryless-ychain-plan` や `channel-coding-converse-general-plan` 系に振り直す方が plan-slug の意味的整合上望ましい。

---

### `mac-l1-discharge-moonshot-plan` (1 件)

- **plan path**: `docs/shannon/mac-l1-discharge-moonshot-plan.md` (157 行)
- **plan 最終 Phase status**: Status 行で「DONE-HONEST-HYPS (計画通り、partial discharge)」「L-MAC1-A/B/C 実証明、L-MAC1-D (4-error Bonferroni) は deferred」と明記。publish-layer hook `mac_capacity_region_inner_bound_with_joint_typ_aep` は親の `:= h_existence` pass-through (計画通り)。
- **code suspect 件数 (実測)**: 1
- **個別 suspect**:

  | # | file:line | declaration | load-bearing hyp | plan 内対応 | close ROI |
  |---|---|---|---|---|---|
  | 1 | `MACL1Discharge.lean:533` | `mac_capacity_region_inner_bound_with_joint_typ_aep` | `h_jt : MACJointTypicalityAchievable` | ✓ Section 6 publish-layer hook、L-MAC1-D deferred 明示 | low (L-MAC1-D ~500-800 行 Bonferroni body) |

- **整合性 a/b/c**:
  - **a. 件数整合**: ✓ 1/1 完全 match。
  - **b. 個別対応**: ✓ plan で「L-MAC1-D は deferred、publish-layer hook は `:= h_existence` pass-through」と明示、suspect は計画通り。
  - **c. drop 漏れ**: ✓ なし。
- **観察 / open question**:
  - 連鎖: この 1 件は `mac_capacity_region_inner_bound` (mac-moonshot-plan #6) と同じ `MACJointTypicalityAchievable` hyp に依存。共通 hyp の genuine discharge (L-MAC1-D) で両 plan の合計 2 件 (mac-moonshot #6 + mac-l1-discharge #1) を同時 close 可能。

---

## Recommendations

### 最優先 close 候補 (high ROI、すぐ取り組める)

1. **`dmc-feedback-capacity-plan` 3 件 (`channel_coding_feedback_converse_*` MVP form)** —
   後継 E-10' (`ChannelCodingFeedbackComplete.lean`) で `feedback_per_letter_bound`
   (`:116`) と `channel_coding_feedback_converse_memoryless` (`:171`) が無条件 0 sorry で
   publish 済。MVP 3 件は `@audit:suspect` → `@audit:retract-candidate(superseded-by-memoryless-form)`
   への置換、または MVP API を `_memoryless` form で internally rewrite して
   passthrough 化 (1 セッション)。

2. **`channel-coding-shannon-theorem-full-plan` #3 `shannon_noisy_channel_coding_theorem_general`** —
   `_full` 版が無条件 publish 済 (`ChannelCodingShannonTheoremFullDischarge.lean:1588`)。
   MVP の `h_passthrough` 形は共存意図ながら、**`@audit:retract-candidate(superseded-by-full-discharge)`**
   タグ追加で意図明示 (1 commit)。または MVP body を `_full` 経由に rewrite。

3. **`channel-coding-shannon-theorem-full-plan` #1, #2 (`ChannelCodingConverseGeneralComplete.lean`)
   の slug 帰属振り直し** — converse 系 plan slug (`channel-coding-converse-memoryless-ychain-plan`
   or `channel-coding-converse-general-plan`) に再分類。Wave 2 で監査再走時のノイズ削減。

### plan 整合修正候補 (in-plan orphan / drop 漏れの fix)

1. **`relay-inner-bound-moonshot-plan`**: `RelayInnerBodyDischarge.lean` / `RelayCFBinningBody.lean` /
   `RelayDFBlockMarkovBody.lean` の 3 兄弟 file を plan「依存関係」「ファイル構成」
   「Critical Files」 欄に追記。Phase progress `[ ]` → `[x]` への置換。
2. **`mac-moonshot-plan`**: `MACBodyDischarge.lean` / `MACFanoConverseBody.lean` /
   `MACPerEventAEPDecay.lean` を依存関係欄に追記。`MACFanoConverseBody.lean` は
   **plan の L-MAC2 を部分 discharge した genuine Fano body** を提供するので、
   plan 判断ログに append-only で記録すべき (計画上は撤退ライン発動と書いてあるが
   実装で partial discharge が進んだ事実)。
3. **`broadcast-channel-moonshot-plan`**: `BroadcastChannelExistenceBridgeBody.lean` を
   plan に追記 (現状 Status 行で他兄弟 file は触れるが Bridge は未言及)。
4. **`relay-cutset-moonshot-plan`**: plan 内 Goal 節の `_h_csiszar : True` placeholder
   表記を実装の `RelayBcastCutFano` / `RelayMacCutFano` scalar Fano + chain 形に
   差替 (実装が partial L-RC1/L-RC2 discharge を達成している)。

### 次 wave 推奨スコープ (lean-planner / lean-implementer に出すべき item)

1. **(plan)** `dmc-feedback-capacity-plan` の `@audit:retract-candidate` タグ付与 +
   MVP 3 件を `_memoryless` form の thin wrapper に書き換える micro-plan。1 セッション。
2. **(plan)** `relay-cutset-moonshot-plan` の Status / Goal 整合更新 plan
   (placeholder `True` 表記 → 現実装の scalar Fano hyp 表記への refresh)。0.5 セッション。

### multi-plan 連鎖 (Relay-inner-bound と Relay-cutset の共有 load-bearing hyp 等)

- **Relay-inner-bound ↔ Relay-cutset 連鎖**: relay-inner-bound の 9 件のうち
  `relay_df_consistent` (RelayInnerBound:649) / `relay_cf_consistent` (:675) /
  `relay_df_consistent_discharged` (RelayInnerBodyDischarge:516) /
  `relay_cf_consistent_discharged` (:541) /
  `relay_cf_consistent_binning_discharged` (RelayCFBinningBody:530) の **5 件が
  cut-set 側 `RelayBcastCutFano` / `RelayMacCutFano` を hyp として再消費**。
  relay-cutset-moonshot-plan 3 件 (`relay_broadcast_cut`, `relay_mac_cut`,
  `relay_cutset_outer_bound`) の genuine discharge が達成されれば、relay-inner-bound
  の consistent 系 5 件が transitive に partial 改善。
- **MAC L-MAC1 と MAC-L1-Discharge 連鎖**: mac-moonshot #6
  `mac_capacity_region_inner_bound` と mac-l1-discharge #1
  `mac_capacity_region_inner_bound_with_joint_typ_aep` は共に
  `MACJointTypicalityAchievable` を要求。L-MAC1-D Bonferroni body の単一 discharge
  で両 plan の合計 **2 件 (+ mac-moonshot #7, #8 transitive で 4 件)** を同時 close。
- **Channel-coding shannon-theorem-full ↔ converse-general 連鎖**: #3 MVP の
  `h_passthrough` は `_full` 版 (D.4) で discharge 済だが、`_full` 版自体が
  Fano + memoryless converse (`memoryless_per_summand_bound` 系) に依存。
  本 wave で W1-A / W1-D 担当の `channel-coding-converse-*` slug 群と密結合。
