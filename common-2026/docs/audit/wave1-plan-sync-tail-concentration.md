# Wave 1 plan sync — Tail/concentration (Chernoff/Hoeffding/Cramer/WynerZiv/ParallelGaussian/Shannon/その他)

> Generated by W1-E agent on 2026-05-24 (commit `8e3645f`).
> 関連: [`defect-inventory-2026-05-24.md`](./defect-inventory-2026-05-24.md) §6 Wave 1 (担当エリア
> = Tail/concentration 系)。
> SoT は code 内 `@audit:suspect(<slug>)` タグ (CLAUDE.md「検証の誠実性」)。本ファイルは plan
> 側との整合 audit であり、コードタグも plan ファイルも **編集していない** (read-only)。

## Summary

| 指標 | 値 |
|---|---|
| 担当 plan slug 数 | 17 |
| 担当 suspect 件数 (実測) | **96** (見積 ~95 / ~105 と整合) |
| 不整合検出 (in-plan orphan: plan に未記載の suspect file) | 0 (詳細 §Per-plan) |
| 不整合検出 (drop 漏れ: plan が「完了」記載なのに suspect 残置) | **2** (`wyner-ziv-discharge` / `wyner-ziv-convexity-discharge`) — どちらも plan body の Status block で「DONE-UNCOND / DONE-HONEST-HYPS」と明記済、suspect タグは long-term tracking marker として温存されているもの。実害なし、ただし「DONE 表記 + suspect タグ」併存は一般読者を惑わせる。 |
| close ROI **high** | 1 (`shannon-moonshot-plan`: 4 件のうち 3 件は ShannonHartley honest conditional pass-through を re-tag だけで close 可) |
| close ROI **medium** | 4 (`chernoff-converse-sanov-discharge-plan` / `parallel-gaussian-moonshot-plan` / `wyner-ziv-convexity-discharge-moonshot-plan` (residual のみ) / `cramer-chernoff-clt-closure-moonshot-plan`) |
| close ROI **low** | 12 (genuine Mathlib-gap な long-term 案件、本 wave で動かす対象ではない) |

**全体観察**: 担当 96 件の **大半が plan ファイルの Status block で実態整合 (2026-05-20) 注記済** (== plan 自身が現状把握済)。「plan 側が完全に把握していない suspect」は **0 件** (in-plan orphan ゼロ)。本 wave で抽出した leverage は (a) `wyner-ziv-discharge` / `wyner-ziv-convexity-discharge` の **suspect → completed re-classification** (タグ書換のみ)、(b) `shannon-moonshot-plan` の ShannonHartley conditional pass-through re-tag、(c) `chernoff-converse-sanov-discharge-plan` Phase 5 が解消すれば連鎖で 15 件 + `chernoff-converse-moonshot-plan` 4 件 + `chernoff-moonshot-plan` 1 件 + `chernoff-converse-sanov-discharge` 系 7 ファイル全部が一斉に suspect → completed 化される **multi-plan 連鎖 leverage** の 3 軸。

---

## Per-plan analysis

### `hoeffding-tradeoff-moonshot-plan` (17 件、最大スコープ)

**Plan Status** (header 注記): PASS-THROUGH (sandwich 形)。headline `hoeffding_tradeoff` (unconditional Tendsto) は未 publish。`hoeffding_tradeoff_with_hypothesis` (`HoeffdingTradeoff.lean:296`) は **achievability + converse の両方** を hypothesis として取り、本体は `tendsto_of_le_liminf_of_limsup_le` 一行。Phase C/D は L-H4 で defer。

**File 別集計** (17 件):

| file | 件数 | 内容 |
|---|---|---|
| `HoeffdingInteriorGradientBody.lean` | 6 (L238,250,263,279,295,316) | Lagrange / IVT / interior gradient → minimizer の pass-through chain |
| `HoeffdingInteriorBody.lean` | 4 (L194,275,308,343) | interior minimizer / predicate / gradient → sandwich の wrapper |
| `HoeffdingLagrangeIVTBody.lean` | 4 (L202,239,257,281) | IVT 経由 Lagrange / Tilt minimal / interior minimizer 実体構成 |
| `HoeffdingSandwichBody.lean` | 2 (L281,315) | sandwich via predicate / boundary `α ≥ kl` |
| `HoeffdingSandwichDischarge.lean` | 1 (L187) | `hoeffding_tradeoff_of_asymptotics` — DEF-FLAW (兄弟 `sandwich-plan` 経由で発見済) を honest wrapper で着地 |

**整合チェック**:
- (a) **件数**: plan は L-H4 で Phase C/D を defer したまま 17 ファイル分の suspect tag が予測される (predicate pass-through + Lagrange/IVT chain 全 file) → 一致。
- (b) **in-plan orphan**: なし。plan §「Approach 図」 + §「ファイル構成」で `HoeffdingInteriorBody` / `HoeffdingInteriorGradientBody` / `HoeffdingLagrangeIVTBody` / `HoeffdingSandwichBody` 全 file が明示。
- (c) **drop 漏れ**: なし (Phase C/D が `[ ] L-H4 defer` のまま、suspect 残置は合理)。

**close ROI**: **low**。Phase C (Sanov LDP per-Qstar + Type I 制御 + 自前 AEP) + Phase D (Stein typicality template + Pythagoras) は ~140-220 行の Mathlib 級新規実装。さらに `hoeffding-tradeoff-sandwich-plan` 側で DEF-FLAW (`steinTypeII_at_level_pmf` の定数 α 形では headline が偽) が発覚済 — **operational 量の指数 level 再定義が必須** (新サブ plan `hoeffding-exponent-level-redef-plan.md` 待ち)。本 wave スコープ外。

---

### `chernoff-converse-sanov-discharge-plan` (15 件)

**Plan Status** (header 注記): per-tilt predicate `IsBayesErrorPerTiltLowerBound` を Sanov 経由で genuine 化するための後継 plan。Phase 1-6 全 `[ ]`、`isBayesErrorPerTiltLowerBound_genuine` (Phase 4) + `chernoff_lemma_tendsto_unconditional` (Phase 5) が未 publish。

**File 別集計** (15 件):

| file | 件数 | 内容 |
|---|---|---|
| `ChernoffPerTiltSanov.lean` | 7 (L164,175,260,278,292,309,356) | `chernoff_per_tilt_via_RN` (循環 `:= h_RN`、plan §判断ログ #2 で **honesty defect** として明示記録) + 反向 pass-through + RN-base wrapper 5 本 |
| `ChernoffPerTiltDischarge.lean` | 5 (L162,182,212,277,306) | `IsBayesErrorPerTiltLowerBound` (load-bearing predicate) を入口にした下流 5 wrapper |
| `ChernoffSanovDischarge.lean` | 3 (L288,362,462) | `IsChernoffBandMassToOne` 経由の partial discharge (現 0 sorry, band-mass hypothesis でゲートあり) |

**整合チェック**:
- (a) **件数**: plan は **Phase 5 で `chernoff_per_tilt_via_RN` の循環解消 + 新規 `chernoff_lemma_tendsto_unconditional` 起票**を予告 (本 plan §Phase 5)。15 件は全て `IsBayesErrorPerTiltLowerBound` / `IsChernoffNLetterRN` / `IsChernoffPerTiltDischargeable` の predicate hyp pass-through chain。plan §依存関係 §Phase 4 で全 file 列挙済。一致。
- (b) **in-plan orphan**: なし (3 file 全て plan §依存補題 / §Phase で明示)。
- (c) **drop 漏れ**: なし (Phase 1-6 が全 `[ ]`)。

**close ROI**: **medium**。Phase 2 (step 1: tilted typical set 上の逆 Hölder per-point 下界、`min_le_rpow_mul_rpow` の逆向き、~60-90 行) + Phase 3 (Stein `steinTypicalSet_P_prob_tendsto_one` を Measure.pi 形に移植、~80-120 行) で **Mathlib 既存 (`Stein.lean` + `SanovLDPEquality.lean` + `chernoffMediatorMeasure_pi_singleton_toReal` 既架橋済) を活用した 150-300 行**。**Mathlib に MGF/Cramér primitive は存在するが Chernoff Z(λ) per-tilt 下界そのものは Mathlib 不在** (loogle 確認推奨)。1 success で **15 件 + `chernoff-converse` 4 件 + `chernoff` 1 件 = 20 件一斉 close** の連鎖あり (§multi-plan 連鎖参照)。

---

### `wyner-ziv-discharge-moonshot-plan` (13 件)

**Plan Status** (header 注記): **DONE-HONEST-HYPS (計画通り、plumbing layer 独立 publish)** — plan の「着手中」表記は STALE、実装完了。`WynerZivDischarge.lean` (0 sorry, InformationTheory.lean 編入済) に計画通りの plumbing publish 済。FLAW なし。

**File 別集計** (13 件):

| file | 件数 | 内容 |
|---|---|---|
| `WynerZivConverseChain.lean` | 6 (L129,162,452,483,532,580) | converse chain composite / block / existence / tendsto chain — n-letter Csiszár+Jensen 内訳を hypothesis pass-through 形で受ける wrapper |
| `WynerZivBinningCovering.lean` | 4 (L238,270,322,461) | binning ↔ covering+packing 等価 chain wrapper |
| `WynerZivBinningBody.lean` | 1 (L479) | random binning body (hypothesis 形) |
| `WynerZivPackingBody.lean` | 1 (L536) | packing existence (hypothesis 形) |
| `WynerZivCoveringBody.lean` | 1 (L411) | covering → asymp feed (hypothesis 形) |

**整合チェック**:
- (a) **件数**: plan §「File layout」 + §「Lemma roadmap」で 6 section 全 publish と記載、suspect 13 件は **L-WZ1/2/3 全 pass-through** の下流 chain → 一致。
- (b) **in-plan orphan**: なし。`WynerZivConverseChain` / `WynerZivBinningCovering` 等は親 `wyner-ziv-moonshot-plan` (3 ファイル分離戦略) で明示。
- (c) **drop 漏れ**: **あり (軽微)**。plan Status block が「DONE-HONEST-HYPS (計画通り)」と明記しているのに 13 件全部に `@audit:suspect` タグが残置。`suspect` は audit-tags.md 語彙では「要再検査」のニュアンス。**plan 既定の long-term residual** なら `@audit:staged(<slug>)` or `@audit:residual(<slug>)` への語彙昇格が一貫 (CLAUDE.md / `docs/audit/audit-tags.md` 参照)。**ただし実害なし** (タグ書換だけの cosmetic re-tagging、機械検証への影響ゼロ)。

**close ROI**: **low** (genuine close)。L-WZ1 (auxiliary cardinality bound) / L-WZ2 (Csiszár's sum identity) / L-WZ3 (R_WZ(D) 凸性) は別 plan で扱う方針 — `wyner-ziv-convexity-discharge-moonshot-plan` で L-WZ3 は full discharge 済 (§次項参照)、L-WZ1/L-WZ2 は **plan が明示的に scope-out** (本 plan §「撤退ライン」)。**re-tagging ROI: medium-high** (13 件まとめて `staged` 語彙へ昇格できる、~5-10 分作業)。

---

### `parallel-gaussian-moonshot-plan` (11 件)

**Plan Status** (header 注記): **PASS-THROUGH (headline)** — `parallel_gaussian_capacity_formula` (`ParallelGaussian.lean:263`) は body が `:= h_per_coord`、その `IsParallelGaussianPerCoordReduction` (`ParallelGaussian.lean:230`) は **結論そのもの** を Prop 化した hyp → `:= h_concl` retreat。0 sorry だが容量公式の実体は未証明。L-WF1 (`IsWaterFillingKKT`) は honest non-trivial Prop で `exists_waterFillingKKT_of_pos` (`ParallelGaussianKKT.lean:141`) が IVT で **実 discharge 済**。L-WF2/L-PG1 の "discharge" は定義的同一 alias/antisymmetry で実体なし。L-PG0 kernel measurability は `ParallelGaussianL_PG0Discharge.lean:98` で完全 discharge 済。

**File 別集計** (11 件):

| file | 件数 | 内容 |
|---|---|---|
| `ParallelGaussianPerCoord.lean` | 5 (L187,207,256,302,366) | per-coord 主鎖 + bridge wrapper (capacity_ge_sum, max_ent_le, 最終 `parallel_gaussian_capacity_formula`) |
| `ParallelGaussianKKT.lean` | 4 (L282,294,324,362) | KKT discharged wrapper (bundle ↔ per-coord reduction 双方向 + 主定理 KKT 版) |
| `ParallelGaussianWFStationarityBody.lean` | 1 (L143) | WFstat discharged wrapper |
| `ParallelGaussianWFCertBody.lean` | 1 (L329) | WFcert discharged wrapper |

**整合チェック**:
- (a) **件数**: plan §「進捗」で Phase A-D 全 `[ ]`、L-PG1 hypothesis pass-through 形で publish 予定とあり 11 件残置は plan 通り。
- (b) **in-plan orphan**: なし。plan §「ファイル構成」 + §「Phase B」で `ParallelGaussianKKT` / `ParallelGaussianPerCoord` / `WFStationarity` / `WFCert` 全 file 配置の予告あり。
- (c) **drop 漏れ**: なし。

**close ROI**: **medium**。`parallel_gaussian_capacity_formula` の **per-coord MI 等号 bundle** = L-PG1 (Cover-Thomas Theorem 9.4.1 本体) は ~80-150 行の AWGN per-coord chain rule + 等号成立条件。`differential-entropy` + `gaussianMutualInfo` 周辺 (Mathlib に部分あり) を完備で組めば close 可。**ただし AWGN converse の MI bridge (`mutualInfo_le_log_one_plus_snr`) が前提**で、AWGN family (`awgn-moonshot-plan` 系、W1-A 担当) に直接依存。W1-A の advance を待つ。

---

### `cramer-moonshot-plan` (8 件)

**Plan Status** (header 注記): **進捗ブロック記載どおり (この plan は正確)**。`Cramer.lean` (0 sorry) に `cramer_upper` (DONE-HONEST-HYPS: h_pos+h_cobdd) / `cramer_lower` (DONE-HONEST-HYPS: `h_tilted_lower` 残置形 L-C2) / `cramer_lower_legendre` / `cramer_tendsto` を publish。`cramer_lower` の `h_tilted_lower` 残置は L-C2 撤退どおりだが、後継チェーンで内部点 `a = deriv (cgf Y μ₀) lam` での unconditional 化を達成済 (`CramerCLTClosure.cramer_lower_at_cgfDeriv_unconditional`、residual 仮定なし)。

**File 別集計** (8 件):

| file | 件数 | 内容 |
|---|---|---|
| `Cramer.lean` | 4 (L311,451,562,596) | `cramer_upper_legendre` / `cramer_lower` (L-C2 hyp 残置) / `cramer_lower_legendre` / `cramer_tendsto` |
| `CramerPhaseDGapWorkaround.lean` | 4 (L136,160,186,259) | cylinder density 経由の Phase D 緩和 wrapper + unified Cramér+Chernoff predicate の Phase C reduction |

**整合チェック**:
- (a) **件数**: plan §進捗 Phase B/C/D 完了表記 + L-C2 撤退 + 後継 `cramer-lc2-discharge` / `cramer-lc2-ext` / `cramer-chernoff-clt-closure` に分担。`Cramer.lean` 主鎖 4 件 + `CramerPhaseDGapWorkaround.lean` 4 件 → 一致。
- (b) **in-plan orphan**: なし (`CramerPhaseDGapWorkaround.lean` も plan §進捗 Phase D で明示)。
- (c) **drop 漏れ**: なし (L-C2 残置は plan が明示)。

**close ROI**: **low** (本 slug 単体)。`cramer_lower` の `h_tilted_lower` は後継 4 plan のチェーンで内部点 unconditional 化を達成済だが、`cramer-moonshot-plan` の 8 件は **plan-side residual marker** として残置。**ROI medium** (re-tagging): 後継 closure 状態を反映して `staged` or `residual` 語彙昇格が一貫。

---

### `wyner-ziv-moonshot-plan` (6 件)

**Plan Status** (header 注記): **PASS-THROUGH (計画通り) — 全 Phase 実装済、plan の「Phase 0 起草中」表記は STALE**。3 file publish 済 (全 0 sorry)。`wyner_ziv_achievability_existence` / `wyner_ziv_converse_n_letter` / Phase D wrapper `wyner_ziv_tendsto` は全 pass-through。L-WZ1/2/3 全 pass-through (FLAW なし — 計画通り)。

**File 別集計** (6 件):

| file | 件数 | 内容 |
|---|---|---|
| `WynerZiv.lean` | 2 (L328,360) | `wynerZivRatePmf_image_bddBelow_of_objective` + headline `wyner_ziv_tendsto` (le_antisymm 形) |
| `WynerZivAchievability.lean` | 2 (L60,81) | `wyner_ziv_achievability_rate` (pass-through) + `wyner_ziv_achievability_existence` (pass-through) |
| `WynerZivConverse.lean` | 2 (L193,238) | `wyner_ziv_converse_rate` + `wyner_ziv_converse_existence` |

**整合チェック**:
- (a) **件数**: plan §進捗 で Phase B-D を `(statement-level publish via L-WP-statement-pass; 内訳は別 plan で discharge)` と明記、6 件は L-WP-statement-pass 3 file × 2 (rate + existence) → 一致。
- (b) **in-plan orphan**: なし。
- (c) **drop 漏れ**: なし (全 hypothesis pass-through、`wyner-ziv-discharge` 等の別 plan で chain discharge)。

**close ROI**: **low**。L-WZ1 cardinality bound (auxiliary alphabet `|U| ≤ |α|+1`) は別 plan に分離されたまま、L-WZ2 Csiszár's sum identity 同様。L-WZ3 凸性 は `wyner-ziv-convexity-discharge` で full discharge 済 — 連鎖反映で本 slug の `wyner_ziv_converse_*` 系 (4 件) が **partial close 可能** だが残 2 件 (achievability 系) は random-binning code construction の Mathlib gap (Wyner-Ziv 自体が Mathlib 不在)。

---

### `wyner-ziv-convexity-discharge-moonshot-plan` (5 件)

**Plan Status** (header 注記): **DONE-UNCOND (L-WZ3 凸性 full discharge 達成) — plan の「実装未着手 / Phase 1-6 全 `[ ]`」表記は STALE、実装完了**。`WynerZivCondEntDiffConvexBody.lean` (0 sorry, `True` placeholder なし)。主定理 `wynerZivCondEntDiffConvex_holds` (:354) は唯一の honest hyp `h_pmf_nn : ∀p, 0 ≤ P_XY p` の下で **無条件 (第一目標) 証明**。unconditional rate wrapper `wynerZivRateFactorizable_convex_in_D_unconditional` (:372) で `_of_condEntDiff` の `h_core` を消去。FLAW なし。InformationTheory.lean 編入済。

**File 別集計** (5 件):

| file | 件数 | 内容 |
|---|---|---|
| `WynerZivConvexityBody.lean` | 3 (L444,540,596) | `wynerZivRateFactorizable_convex` / `_of_pmf` / `_in_D` — 3 wrapper |
| `WynerZivObjectiveConvexityBody.lean` | 2 (L238,292) | `wzObjective_convex_of_condEntDiff` + `wynerZivRateFactorizable_convex_in_D_of_condEntDiff` |

**整合チェック**:
- (a) **件数**: plan の Phase 6 (unconditional rate wrapper re-publish) で **`_of_condEntDiff` の `h_core` を消す** unconditional 版を本 file で re-publish 済 → suspect 5 件は `_of_condEntDiff` 版 (conditional pass-through) を残置 + 3 wrapper の rate-level chain。
- (b) **in-plan orphan**: なし。
- (c) **drop 漏れ**: **あり (軽微)**。plan Status block で DONE-UNCOND と明記、`_unconditional` 版 (`:372`) が genuine、`_of_condEntDiff` (`:292`) は conditional 版で plan 想定通り。`suspect` タグは long-term marker としては妥当だが、**現状 unconditional 版が併存** ⇒ `_of_condEntDiff` 系のみ `suspect` 残置するか、`staged` 語彙へ昇格するかは整理余地あり。

**close ROI**: **medium** (re-tagging のみ)。実体は既に unconditional published、5 件全てが「unconditional 版があるのに `_of_condEntDiff` conditional 版を suspect 化したまま」の **categorical mismatch**。**audit-tags.md 語彙判断**で `suspect → completed` or `_of_condEntDiff` 自体を deprecate して unconditional 版へ吸収 (~10 行作業)。

---

### `shannon-moonshot-plan` (4 件、全て `ShannonHartley.lean`)

**Plan Status** (header 注記): **DONE-HONEST-HYPS** — 主定理 `shannon_converse_single_shot` は 0 sorry で publish。hypothesis は標準 measurability + uniform-message + MI 有限性のみで genuine analytic、pass-through / `Prop := True` 不在。

**File 別集計** (4 件):

| file | 件数 | 内容 |
|---|---|---|
| `ShannonHartley.lean` | 4 (L139,209,296,320) | `perSampleAwgnCapacity_eq_awgn` (SNR identity) / `shannon_hartley_formula` (`h_two_w` conditional pass-through で循環解消済、`whittaker-shannon-partial` plan で resolved) / `mk_IsBandlimitedSamplingHypothesis` (builder) / `shannon_hartley_formula_bits` |

**整合チェック**:
- (a) **件数**: plan slug `shannon-moonshot-plan` は **Phase 4-γ (single-shot Shannon converse) が達成済 (2026-05-10)**、`ShannonHartley.lean` 4 件は plan の主スコープ外。
- (b) **in-plan orphan**: **あり**。`shannon-moonshot-plan` は §Approach で `klDiv` chain rule → DPI → Fano → Shannon converse を扱い、**`ShannonHartley` は plan 本文に登場しない** — むしろ `whittaker-shannon-partial-moonshot-plan` (header 注記で RESOLVED 2026-05-20 と明記) で `shannon_hartley_formula` 等の循環解消が記録される。**suspect slug が plan と直接対応していない (orphan-ish)**。
- (c) **drop 漏れ**: **あり**。`shannon_hartley_formula` (L209) は `whittaker-shannon-partial-moonshot-plan` で 2026-05-20 honest conditional pass-through 化が記録済 (header 注記)。**slug を `whittaker-shannon-partial-moonshot-plan` に振り直す**のが整合。

**close ROI**: **high (re-tagging)**。4 件全て **slug 振り直し**で済む (ShannonHartley は `whittaker-shannon-partial` の管轄)。**genuine close は別途** — `shannon_hartley_formula` の `h_two_w` (2W DoF 恒等式) は Whittaker-Shannon sampling theorem 完成依存で long-term。

---

### `cramer-lc2-discharge-moonshot-plan` (4 件)

**Plan Status** (header 注記): **本 plan は Phase A まで (L-D3 撤退) で正確**。`CramerLC2Discharge.lean` (0 sorry) に Phase A plumbing 6 補題 publish 済。本 plan の `cramer_lower_discharged` (Phase C 完全 discharge) は未 publish のまま、後継チェーンで実質達成 (`cramer-lc2-ext` + `infinitepi-tilted` + `cramer-chernoff-clt-closure`)。

**File 別集計** (4 件、全て `CramerLC2PhaseC.lean`):

| file | 件数 | 内容 |
|---|---|---|
| `CramerLC2PhaseC.lean` | 4 (L97,145,185,218) | `tilted_lower_from_predicate` + `cramer_lower_phaseC_partial_discharge` (Phase C partial) + `cramer_lower_legendre_phaseC_partial_discharge` + `cramer_tendsto_phaseC_partial_discharge` |

**整合チェック**:
- (a) **件数**: plan §進捗 で Phase B-C は L-D3 撤退で後続 plan へ defer 明記、`CramerLC2PhaseC.lean` の partial discharge 4 wrapper を残置 → 一致。
- (b) **in-plan orphan**: なし (plan §「ファイル構成」option (ii) で `CramerLC2Discharge.lean` 新規 + `CramerLC2PhaseC.lean` の partial discharge 配置を明示)。
- (c) **drop 漏れ**: なし。

**close ROI**: **low** (本 slug 単独)。後継 `cramer-chernoff-clt-closure` で内部点 `a = deriv (cgf Y μ₀) lam` unconditional 化を達成済、本 4 件はその closure を反映して **`IsMeasureInfinitePiTiltedEq` predicate を消す** wrapper rewrite で close 可能だが、**境界 `a = boundary` は依然 hypothesis 残置必要**。Re-tagging 整理が現実的。

---

### `chernoff-converse-moonshot-plan` (4 件)

**Plan Status** (header 注記): **DONE-HONEST-HYPS (per-tilt 縮減形、L-CC2 着地) — 計画通り完了**。`ChernoffConverse.lean` (0 sorry) に 5 主定理 + Mediator 全 publish。`h_per_tilt` は honest な指数下界仮定で `:=True` / vacuous でない。完全 discharge は別 plan (`chernoff-converse-sanov-discharge`) に分離。

**File 別集計** (4 件):

| file | 件数 | 内容 |
|---|---|---|
| `ChernoffInformation.lean` | 1 (L125) | `chernoff_lemma_tendsto` (h_converse pass-through, L-Ch1) |
| `ChernoffConverse.lean` | 3 (L273,411,446) | `chernoff_converse_from_per_tilt` + `_of_per_tilt_existential` + `chernoff_lemma_tendsto_from_per_tilt` |

**整合チェック**:
- (a) **件数**: plan §進捗 で Phase 1-6 全 `[ ]` だが Status block で全完了表記、4 件は plan §Goal の 4 主定理に対応 → 一致。
- (b) **in-plan orphan**: なし。
- (c) **drop 漏れ**: なし。

**close ROI**: **medium (連鎖)**。本 4 件単独 close は `chernoff-converse-sanov-discharge` Phase 5 `chernoff_lemma_tendsto_unconditional` 完成と連動。**§multi-plan 連鎖参照** (Chernoff family 計 20 件 + ChernoffInformation 1 件)。

---

### `whittaker-shannon-partial-moonshot-plan` (2 件)

**Plan Status** (header 注記): DONE (L-WS-A scope) + FLAW-VACUOUS → **RESOLVED (2026-05-20)**。`WhittakerShannonPartial.lean` は sinc 基本性質群を実証明で publish 済 — これは genuine。`shannon_hartley_formula` の循環を honest conditional pass-through に是正済。

**File 別集計** (2 件):

| file | 件数 | 内容 |
|---|---|---|
| `WhittakerShannonFull.lean` | 2 (L372,393) | `shannon_hartley_via_full` + `shannon_hartley_via_full_bits` (bits 単位版) |

**整合チェック**:
- (a) **件数**: 2 件は `WhittakerShannonFull.lean` の `shannon_hartley_via_full` 系 — plan は `WhittakerShannonPartial.lean` ベースだが §Approach で `WhittakerShannonFull.lean` (finite-window 実証明) も整合確認、本 file 経由の hartley_formula wrapper も plan の射程内 (header 注記で言及)。
- (b) **in-plan orphan**: なし。
- (c) **drop 漏れ**: なし。

**close ROI**: **low**。Whittaker-Shannon sampling theorem の full statement (band-limited L² function complete reconstruction) は Mathlib gap。本 plan は partial で着地、long-term。

---

### `hoeffding-tradeoff-sandwich-plan` (2 件)

**Plan Status** (header 注記): 🛑 **BLOCKED — DEF-FLAW 発見 (2026-05-21)**: headline `rate → hoeffdingE2 P₁ P₂ alpha` は現 operational 定義 `steinTypeII_at_level_pmf` (定数 Type-I level `alpha`) では **数学的に偽**。**full discharge には operational 量の指数 level 再定義が必須** (= 新サブ計画 `hoeffding-exponent-level-redef-plan.md` 待ち、本計画の Phase 3/4 は無効化)。Phase 1/2/V のみ達成、honest wrapper `hoeffding_tradeoff_of_asymptotics` で着地。

**File 別集計** (2 件):

| file | 件数 | 内容 |
|---|---|---|
| `HoeffdingSandwich.lean` | 1 (L291) | `hoeffding_tradeoff_sandwich` (DEF-FLAW 経由の honest wrapper) |
| `HoeffdingTradeoff.lean` | 1 (L297) | `hoeffding_tradeoff_with_hypothesis` |

**整合チェック**:
- (a) **件数**: plan で DEF-FLAW 明示 + Phase 3/4 無効化、suspect 2 件 = `hoeffding-exponent-level-redef-plan.md` 待ちのマーカー → 一致。
- (b) **in-plan orphan**: なし。
- (c) **drop 漏れ**: なし (DEF-FLAW 明示)。

**close ROI**: **low**。DEF-FLAW 解消には `steinTypeII_at_level_pmf` 定義そのものの指数 level 化 (新 plan)、長期。

---

### `separation-theorem-moonshot-plan` (1 件)

**Plan Status** (header 注記): **DONE (achievability=DONE-UNCOND, converse=DONE-HONEST-HYPS) — plan の Phase A-D 完了表記は CODE と一致 (STALE でない)**。`SeparationTheorem.lean` (28396 B, 0 sorry)。`separation_achievability_iid` (:309) は実証明 (rate splitting + source/channel achievability 合成、honest hyp `hHC : entropy < capacity`)。`separation_converse_iid` (:445) も実証明だが channel-side rate bound (`hM_ch_bdd`, `hR_ch_le_C`) を honest hypothesis として pass-through。FLAW なし。

**File 別**: `SeparationTheorem.lean:446` (1 件, `separation_converse_iid`)

**整合チェック**: in-plan orphan なし / drop 漏れ なし。

**close ROI**: **low**。channel-side rate bound の self-discharge には `Code.averageErrorProb → maxErrorProb` 強化 + DMC converse のさらなる boilerplate ~50-100 行、現状 plan は long-term 設計。

---

### `infinitepi-tilted-rn-discharge-moonshot-plan` (1 件)

**Plan Status** (header 注記): **DONE — Phase 1-4 全達成、当初の「最低保証」を大きく超過**。進捗ブロックと判断ログが起草時のまま (Phase 0-V 全 `[ ]`、判断ログは起草 2 件のみ) で **重度に陳腐化**。`pi_tilted_sum_eq_pi_tilted` + cylinder lift + change-of-measure + residual predicate `IsTiltedWindowEventuallyLarge` 全 publish。interior ケース discharge 済、boundary ケースは後継 `cramer-chernoff-clt-closure` plan で CLT closure 済。全関連 file 0 sorry。

**File 別**: `InfinitePiTiltedChangeOfMeasure.lean:362` (1 件, `cramer_lower_phaseC_residual_discharge`)

**整合チェック**: in-plan orphan なし / drop 漏れ **あり (軽微、進捗ブロック陳腐化)** — plan body 進捗ブロックは Phase 0-V 全 `[ ]` のまま (header 注記で完了明記、本文未更新)。1 件 suspect 残置は residual predicate 経由の wrapper として妥当。

**close ROI**: **low** (本 slug 単独)。後継 `cramer-chernoff-clt-closure` 経由で実質 closure 済、本 1 件は residual predicate marker として温存も妥当。

---

### `cramer-chernoff-clt-closure-moonshot-plan` (1 件)

**Plan Status** (header 注記): **DONE-HONEST-HYPS — full closure 達成 (計画完遂)**。`CramerCLTClosure.lean` (0 sorry) に全 Phase publish 済。headline 名は実態と相違 (計画 `cramer_lower_boundary_unconditional` vs 実 `cramer_lower_at_cgfDeriv_unconditional`、:523)。内部点 `a = deriv (cgf Y μ₀) lam` で residual largeness 仮定なし。残る honest hyps は `hVar : 0 < Var[...tilted...]` + `h_coboundedBelow` のみ。**進捗 Phase 0-6 が全 `[ ]` のままだが実態は全完了**。

**File 別**: `CramerCLTClosure.lean:463` (1 件, `cramer_lower_at`)

**整合チェック**: in-plan orphan なし / drop 漏れ **あり (進捗ブロック陳腐化)** — plan body 進捗ブロック全 `[ ]` のまま。

**close ROI**: **medium**。実体 closure 済、本 1 件は `hVar` (退化処理用) + `h_coboundedBelow` (`Filter.IsCoboundedUnder`、CLAUDE.md「Mathlib 壁」の (a) 選択) の honest hyp pass-through なので **`@audit:suspect` → `@audit:completed` re-tag** が現実的 (cosmetic)。

---

### `chernoff-moonshot-plan` (1 件)

**Plan Status** (header 注記): **DONE-HONEST-HYPS (L-Ch1+L-Ch2 採用形) — 計画通り完了**。`chernoff_lemma_tendsto` は (`ChernoffInformation.lean:124`、0 sorry) に存在。`h_converse` (limsup ≤ chernoffInfo) + `h_bdd_le` を honest hypothesis として取り sandwich。`h_converse` は後継 `chernoff-converse` plan で per-tilt 形まで縮減済。`chernoff_dotEq` corollary (Phase 5) も存在。**進捗 Phase 1-5 が全 `[ ]` のままだが実態は全完了**。

**File 別**: `ChernoffInformation.lean:197` (1 件, `chernoff_dotEq_tendsto`)

**整合チェック**: in-plan orphan なし / drop 漏れ **あり (進捗ブロック陳腐化)**。

**close ROI**: **medium (連鎖)**。本 1 件は `chernoff-converse-sanov-discharge` Phase 5 連鎖で close。

---

### `birkhoff-ergodic-plan` (1 件)

**Plan Status** (header 注記): **DONE-UNCOND — headline `birkhoff_ergodic_ae` 完成済** (`BirkhoffErgodic.lean:1031`、0 sorry)。仮定は標準 typeclass のみ (`[IsProbabilityMeasure μ]` + `MeasurePreserving T μ μ` + `Ergodic T μ` + `Integrable f μ`)、pass-through なし。SMB 仮説なし形昇格も達成: 無条件 `shannon_mcmillan_breiman` は `InformationTheory/Shannon/SMBAlgoetCover.lean:2840`。

**File 別**: `BirkhoffErgodic.lean:1002` (1 件, `birkhoff_ergodic_ae_of_limit` — intermediate helper、headline `birkhoff_ergodic_ae` は :1031 で別物)

**整合チェック**: in-plan orphan なし / drop 漏れ **なし** (suspect 残置は intermediate helper `_of_limit` で、headline は `_ae` (genuine published) → plan も実態も一貫)。

**close ROI**: **low** (genuine closure)。`_of_limit` の `gInf` 仮定 (Birkhoff average の limit 存在) は backward martingale convergence で discharge 可能 (plan §3 (b) 経路)、~100 行。**ただし plan は既に `_ae` 経由で実質完了**、`_of_limit` は alternative API 用 helper として妥当。**re-tagging** で OK。

**注**: §6 wave 計画 draft で `birkhoff-ergodic-plan` を「orphan」分類していたが、実在の 198 行 plan あり (Wave 0 で確認済)。これは inventory 集計時の誤読 (inventory §6 で訂正済)。

---

## Recommendations

### 最優先 close 候補 (top-3 leverage、実行順)

1. **【high ROI / 即時実行可】re-tag rewrite (cosmetic 整理)** — DONE 表記済 plan の suspect 残置を audit-tags.md 語彙 (`staged` / `residual` / `completed` 等) で再分類。対象 (~25 件):
   - `wyner-ziv-discharge-moonshot-plan` 13 件 (DONE-HONEST-HYPS 明記)
   - `wyner-ziv-convexity-discharge-moonshot-plan` 5 件 (DONE-UNCOND 明記、`_unconditional` 併存)
   - `shannon-moonshot-plan` 4 件 → slug を `whittaker-shannon-partial-moonshot-plan` に振り直し (in-plan orphan)
   - `infinitepi-tilted-rn-discharge-moonshot-plan` / `cramer-chernoff-clt-closure-moonshot-plan` / `chernoff-moonshot-plan` 各 1 件 (進捗ブロック陳腐化)
   - **作業時間**: ~30-60 分、code edit のみ、Mathlib 進化不要。**機械検証バーへの影響ゼロ** (cosmetic) だが、wave 2 以降の「真に Mathlib-gap な suspect」絞り込み精度が一気に上がる。

2. **【medium ROI / Wave 3 候補】`chernoff-converse-sanov-discharge-plan` Phase 5 完全 discharge** — 連鎖で 20 件 (本 plan 15 + `chernoff-converse` 4 + `chernoff` 1) + `ChernoffInformation` 系を一斉 close。Phase 2 (逆 Hölder on typical set, ~60-90 行) + Phase 3 (Sanov LLN 移植 from Stein, ~80-120 行)。**Mathlib に直接の Chernoff Z(λ) per-tilt 下界は不在**だが、`SanovLDPEquality.lean` + `Stein.lean` + `ChernoffPerTiltSanov.chernoffMediatorMeasure_pi_singleton_toReal` の既存 plumbing を再利用、~150-300 行で closure 可。**plan §「設計判断: redefine しない」が integrity 高い** (`bayesErrorMinPmf` redefine の罠を明示回避)。Wave 3 (parallel ×5) で `lean-implementer` 1 seed 投入候補。

3. **【medium ROI / Wave 3 候補】`parallel-gaussian-moonshot-plan` Phase C 完全 discharge** — `parallel_gaussian_capacity_formula` の per-coord MI 等号 bundle (L-PG1) を AWGN per-coord + differential entropy chain rule で genuine 化。11 件まとめて close 可。**前提**: AWGN family (W1-A 担当の `awgn-moonshot-plan` Phase B-0 = `continuous-aep-gaussian` + `n-dim-gaussian-aep`) の advance。**W1-A の closure 進捗待ち**だが、L-PG1 自体は AWGN を黒箱 reuse で済むので AWGN 完了より早く着手可。

### plan 整合修正候補 (Wave 2 の Plan 側 sync 反映候補)

- 全 17 plan で **進捗ブロック (`- [ ] Phase N`) が body Status 注記と乖離**しているケース ~10 件。Wave 2 で planner 経由で `[x]` 化 + 判断ログ追記 (15 分/plan × 10 = ~2.5 時間)。
- `shannon-moonshot-plan` で `ShannonHartley.lean` 4 件は **slug 振り直し** (in-plan orphan、ShannonHartley は `whittaker-shannon-partial` 管轄)。
- `wyner-ziv-convexity-discharge-moonshot-plan` の `_of_condEntDiff` (conditional) vs `_unconditional` の併存整理 — unconditional 版にエイリアス化 or `_of_condEntDiff` を deprecate。

### 次 wave 推奨スコープ

- **Wave 2** (≤2 並列): 上記 recommendations 1-3 を planner で具体 Phase 設計。連鎖 leverage 最大化のため Chernoff family + ParallelGaussian + re-tag 整理を並行。
- **Wave 3** (parallel ×5): `lean-implementer` 投入。3 seeds (Chernoff Phase 5 / ParallelGaussian L-PG1 / re-tag rewrite) を最初に出し、AWGN W1-A 連動 (Phase B-0) を W1-A の closure 進捗で発火。

---

## Multi-plan 連鎖の評価

### Chernoff family (3 plan / 20 件 + ChernoffInformation 1 件 = 21 件)

- `chernoff-converse-sanov-discharge-plan` (15 件) ← parent
- `chernoff-converse-moonshot-plan` (4 件、`ChernoffInformation.lean:125` + `ChernoffConverse.lean` × 3) ← child
- `chernoff-moonshot-plan` (1 件、`ChernoffInformation.lean:197`) ← grandchild
- 連鎖性: **高い**。Sanov-discharge Phase 5 で `chernoff_lemma_tendsto_unconditional` (無条件 headline) が完成すれば、下流 ChernoffConverse / ChernoffInformation の per-tilt hyp pass-through 全てが `_unconditional` 経由で genuine 化される。**ただし plan 設計上、既存 `_via_RN` 系 lemmas は過去参照で残置 (defect 明示で docstring 更新)** — re-tag が `closed-by-successor(<slug>)` 系 audit-tags 語彙で表現できると整理が綺麗。
- ROI: **最大 leverage**。1 plan Phase 完遂で 20+ 件一斉 close。Wave 3 最有力候補。

### Cramer family (3 plan / 13 件)

- `cramer-moonshot-plan` (8 件、`Cramer.lean` 4 + `CramerPhaseDGapWorkaround.lean` 4)
- `cramer-lc2-discharge-moonshot-plan` (4 件、全 `CramerLC2PhaseC.lean`)
- `cramer-chernoff-clt-closure-moonshot-plan` (1 件、`CramerCLTClosure.lean:463`)
- 連鎖性: **中**。`cramer-chernoff-clt-closure` で内部点 `a = deriv (cgf Y μ₀) lam` の unconditional 化を達成済 — 連鎖反映で本 13 件は **plan-side residual marker** から `_unconditional` 経由 wrapper に書換可能。**ただし境界 `a = boundary` は依然 hypothesis 残置必要** (退化処理)、完全 close は long-term。
- ROI: **medium (re-tagging)**。連鎖反映の差分 commit ~50-100 行 (wrapper rewrite)。新規 Mathlib gap 攻略は不要。

### WynerZiv family (3 plan / 24 件)

- `wyner-ziv-discharge-moonshot-plan` (13 件、Discharge / ConverseChain / Binning 系)
- `wyner-ziv-moonshot-plan` (6 件、WynerZiv / WynerZivAchievability / WynerZivConverse の headline 3 file)
- `wyner-ziv-convexity-discharge-moonshot-plan` (5 件、ConvexityBody 系、L-WZ3 full discharge 済)
- 連鎖性: **低-中**。L-WZ3 凸性は full discharge 達成済、L-WZ1/L-WZ2 は別 plan に分離 (Csiszár's sum identity + auxiliary cardinality bound、Mathlib gap 案件)。**parent `wyner-ziv-moonshot-plan` の headline 6 件は L-WZ-statement-pass で plan が想定通り pass-through 維持** — 完全 close には random binning code construction の Mathlib 化 (Wyner-Ziv が Mathlib 不在、long-term)。
- ROI: **low (genuine close)** / **medium (re-tagging)** — DONE 明記済 plan の 18 件 (13 + 5) を `staged` / `completed` 語彙へ昇格すれば整理。

### その他 (cross-family 連鎖)

- **ParallelGaussian → AWGN (W1-A) 依存**: `parallel-gaussian-moonshot-plan` 11 件の L-PG1 (per-coord AWGN MI 等号 bundle) closure は W1-A の AWGN family closure と連鎖。W1-A の Phase B-0 (`continuous-aep-gaussian`) が進めば連鎖発火。
- **Hoeffding / `hoeffding-exponent-level-redef-plan.md` 待ち**: `hoeffding-tradeoff-moonshot-plan` 17 件 + `hoeffding-tradeoff-sandwich-plan` 2 件 = 19 件は DEF-FLAW (operational 量の指数 level 再定義必須) で本 wave スコープ外、`hoeffding-exponent-level-redef-plan.md` 起案待ち (新 plan)。

---

## Open questions (orchestrator / 次 wave planner 判断待ち)

1. **`shannon-moonshot-plan` の `ShannonHartley.lean` 4 件**: slug を `whittaker-shannon-partial-moonshot-plan` (header 注記で resolved 2026-05-20 と明記) に振り直すか? または `shannon-hartley-formula-plan.md` (新規) を起案するか?
2. **`wyner-ziv-discharge` / `wyner-ziv-convexity-discharge` の DONE 明記 + suspect 併存 18 件**: `audit-tags.md` 語彙でどう表現するか — `@audit:staged(<slug>)` で「load-bearing hyp 付き完成」を示すのが standard だが「passable closure を後続 plan に頼る」ケース用の語彙 (`@audit:closed-by-successor(<successor-slug>)` 等) が必要か?
3. **進捗ブロック陳腐化 ~10 件**: planner subagent で一括更新するか、orchestrator が直接 `Edit` で更新するか? 後者は ~2.5 時間の sequential 作業。
