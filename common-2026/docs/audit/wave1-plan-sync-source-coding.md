# Wave 1 plan sync — Source coding (LZ78 / Huffman)

> Generated by W1-D agent on 2026-05-24.
> 関連: [`defect-inventory-2026-05-24.md`](./defect-inventory-2026-05-24.md) §6 Wave 1。

## Summary

| 指標 | 値 |
|---|---|
| 担当 plan slug 数 | 6 |
| 担当 suspect 件数 (実測) | **59** (Huffman 30 / LZ78 29) |
| 不整合検出 (in-plan orphan, = code に対応物なし) | **0** |
| 不整合検出 (drop 漏れ, = plan が言及せず無防備の suspect) | **51** (Huffman 28 / LZ78 23) |
| close ROI **high** (single-edit / spec alignment / docstring) | **9** |
| close ROI **medium** (1-2 セッション内に Mathlib API でカバー可) | **6** |
| close ROI **low** (combinatorial / 数学コア未構築 — moonshot 同等) | **44** |

**特徴**: 6 plan のうち 5 plan が「**plan の予定 scope より実装が大幅に拡張済**」状態。
plan は publish 補題 ~7-20 件想定だが、実装は wrapper / extractor / hypothesis-chain bridge を
横展開して `@audit:suspect` が plan slug に **継承蓄積**している。コードと plan の語彙整合は
ほぼ取れているが、plan ドキュメント側の **scope 拡大の追記が遅延** している (drop 漏れ多数の主因)。
特に Huffman 系では 3 plan (huffman-moonshot / huffman-optimality-moonshot /
huffman-t1apprime-partial-moonshot) で**同じ 2 hypothesis** (`SwapNormalizationHypothesis` /
`HuffmanMergedIdentificationHypothesis`) を chain 経由で扱っており、ROI 評価には plan 間の
依存関係が必須。

## Per-plan analysis

### `huffman-optimality-moonshot-plan`

- **plan path**: `docs/shannon/huffman-optimality-moonshot-plan.md` (670 行)
- **最終 Phase status**: DONE-HONEST-HYPS — `huffmanLength_optimal_with_hypotheses` (weak form、
  2 hypothesis 引数受け取り)、`exists_sibling_min_pair` を 0 sorry publish 済。強形 (`huffmanLength_optimal`、
  hypothesis なし) は **T1-A'' へ scope-out**。判断ログ #2-#7 で sibling 条項縮退や Phase 3 pivot 履歴あり。
- **code suspect 件数**: **2** (両方 `HuffmanOptimality.lean`、`:778` + `:1028`)
- **個別不整合**:

  | line | declaration | 種別 | 詳細 |
  |---|---|---|---|
  | `:778` | `huffmanLength_optimal_aux_with_hypotheses` (private) | drop 漏れ | plan の Phase 4 (induction motor) の中身. plan は主定理 wrapper のみ言及、private aux 関数は触れず |
  | `:1028` | `huffmanLength_optimal_with_hypotheses` | drop 漏れ | 主定理 weak form. plan の Goal そのものだが、status 行 (`DONE-HONEST-HYPS`) は predicate を **「genuine analytic Prop」** と honest に明記済 |

- **整合性 a/b/c**: in-plan orphan = 0、drop 漏れ = 2、件数一致 = ✅
- **観察**: 2 件とも **plan scope に書かれた DONE 物 (= 完成想定の load-bearing hypothesis 持ち
  weak form)**。タグは「未解決の defect」ではなく「2 hypothesis を抱えた完成状態」を示す sentinel
  として機能している。**plan が status で明示済** (`:= True` 不在、genuine `∀…∃…` predicate)
  なので、**`@audit:suspect` を `@audit:staged(huffman-optimality-2hyp)` 等**に格上げするのが
  honest にも整合 (sibling 兄弟 plan の `staged/residual` 語彙と合わせる)。close ROI: **high**
  (タグ語彙変更のみ、コード本体不変)。

### `huffman-t1apprime-partial-moonshot-plan`

- **plan path**: `docs/shannon/huffman-t1apprime-partial-moonshot-plan.md` (**119 行 / 異常に短い**)
- **最終 Phase status**: DONE-HONEST-HYPS (partial) — plan は **7 補題 publish** (~200 行) を予定し、
  実装は `HuffmanT1APPrimePartial.lean` で達成済 ⇒ ただし**実装ファイルが本来 plan 想定の境界を
  大幅に超えて拡張済**。
- **code suspect 件数**: **19** (`HuffmanT1APPrimeBody.lean` 15 件 + `HuffmanT1APPrimePartial.lean` 4 件)
- **Phase 別分布** — plan が言及していない範囲が大半:

  | Phase / Section | 件数 | declaration 例 | 想定 plan scope |
  |---|---|---|---|
  | Section C (identification extractor) | 3 | `huffmanMergedIdentification_at_a` / `_at_other` / `_combined` | plan 不在 |
  | Section D (sibling dichotomy) | 1 | `huffmanMergedIdentification_dichotomy` | plan 不在 |
  | Section F (combined hypothesis wrappers) | 2 | `huffmanLength_optimal_via_partial_swap_when_eq` / `_wrapper_explicit` | plan 不在 |
  | Section H (witness extraction) | 4 | `SwapNormalizationHypothesis_apply_witness` / `_pos` / `_kraft` / `_eq` / `_expL` | plan 不在 |
  | Section J (combined hypothesis 投影) | 3 | `huffmanCombinedHypothesis_swap` / `_ident` / `huffmanLength_optimal_with_combined` | plan 不在 |
  | Section M (terminal wrapper) | 1 | `huffmanLength_optimal_terminal` | plan 不在 |
  | Wave 4-H (universe-relaxed wrapper) | 1 | `huffmanLength_optimal_with_hypotheses_at` | plan 不在 |
  | Wave 4-H (tuple wrapper) | 1 | `huffmanLength_optimal_with_combined_hypothesis` | plan 不在 |
  | Wave 4-P (PProd / contraposition wrapper) | 2 | `huffmanLength_optimal_with_pair_hypothesis` / `_with_hypotheses_contra` | plan 不在 |
  | Plan-listed (`swap_step_le_*`, `SwapNormalizationHypothesis_trivial_when_eq`) | 0 (untagged) | — | DONE 想定 |

- **整合性 a/b/c**: in-plan orphan = 0、**drop 漏れ = 19 (全部)**、件数一致 = ✅ (推定値どおり)
- **観察**:
  - plan は 119 行 / DoD は ~175 行を達成想定の小さな partial. **実装の方が plan を桁違いに
    超えた wave (Section C-M + Wave 4-H/P) で展開済**で、plan ドキュメントが完全に
    catch-up していない。
  - 19 件はすべて **`SwapNormalizationHypothesis` / `HuffmanMergedIdentificationHypothesis` 2 述語の
    extractor / wrapper / repackaging** で、本質 load-bearing は **2 hypothesis に集約済**
    (同じ source-of-truth 由来)。よって 19 件は **honest 評価上は 2 件と同等の重み**。
  - close ROI: **medium** (extractor / wrapper は機械的なので、**huffman-optimality plan 側で
    2 hypothesis を `@audit:staged` 化すれば本 plan の suspect 19 件は連動して closure 可**;
    plan ドキュメント側を Wave 4-* の追記で update も同時必要、~1 セッション)。
  - **open question**: plan の DoD は ~175 行で達成済とあるが、実装ファイル `HuffmanT1APPrimeBody.lean`
    の Wave 4-H / 4-J / 4-K / 4-P 群はどの seed で追加されたのか plan 判断ログに痕跡なし。
    plan ドキュメントへの遡及 update が必要。

### `huffman-moonshot-plan`

- **plan path**: `docs/shannon/huffman-moonshot-plan.md` (705 行)
- **最終 Phase status**: DONE-UNCOND — Phase 3 完遂 (`huffmanLength` / `_pos` / `_kraft_le_one` /
  `exists_huffman_prefix_code` の 4 件 0 sorry publish)。Phase 4-5 (sibling property + 主定理 optimality)
  は **T1-A' へ scope-out**.
- **code suspect 件数**: **9** (`HuffmanMergedIdentBody.lean` 2 / `HuffmanSwapNormalizationBody.lean` 4 /
  `HuffmanSwapStepChainBody.lean` 2 / `HuffmanStrongForm.lean` 1)
- **Phase 別分布** (全部 plan scope 外、別の vertical reduction track の suspect が
  `huffman-moonshot-plan` slug に流れ込んでいる):

  | declaration | 役割 |
  |---|---|
  | `huffmanMergedIdentification_of_aux` | primitive `MergedHuffmanAuxIdentHypothesis` → 原 `HuffmanMergedIdentificationHypothesis` の vertical reduction (`HuffmanMergedIdentBody.lean:155`) |
  | `huffmanLength_optimal_with_swap_and_aux` | identification hyp を primitive 経由で受ける wrapper (`HuffmanMergedIdentBody.lean:172`) |
  | `swapNormalizationHypothesis_of_equalizingPerm` | `EqualizingPermHypothesis` → `SwapNormalizationHypothesis` vertical reduction (`HuffmanSwapNormalizationBody.lean:133`) |
  | `equalizingPerm_of_swapTarget` | `EqualizingSwapTargetHypothesis` → `EqualizingPermHypothesis` (`HuffmanSwapNormalizationBody.lean:209`) |
  | `swapNormalizationHypothesis_of_swapTarget` | `EqualizingSwapTargetHypothesis` 経由 `SwapNormalizationHypothesis` (`HuffmanSwapNormalizationBody.lean:239`) |
  | `huffmanLength_optimal_via_equalizing_perm` | `EqualizingPermHypothesis` 経由の weak form 再公開 (`HuffmanSwapNormalizationBody.lean:252`) |
  | `huffmanLength_optimal_with_chain_combined` | 3-way hypothesis (chain) wrapper (`HuffmanSwapStepChainBody.lean:344`) |
  | `huffmanLength_optimal_via_chain_lift` | 2-way から chain 経由 wrapper (`HuffmanSwapStepChainBody.lean:360`) |
  | `huffmanLength_optimal_modulo_aux_ident` | **強形 (Hyp1 discharged、Hyp2 のみ残)** — `swap_normalization_proof` で Hyp1 完全 discharge 済、`MergedHuffmanAuxIdentHypothesis` のみ load-bearing (`HuffmanStrongForm.lean:176`) |

- **整合性 a/b/c**: **slug mis-attribution の疑い大** — plan は Phase 3 完遂 DONE で
  「`huffmanLength_optimal` は T1-A' へ scope-out」と明記。実装の 9 件はすべて **T1-A' 系の
  vertical reduction (Hyp1 discharge / strong form)** で、plan の対象外。slug は本来
  **`huffman-optimality-moonshot-plan` または新規 plan** にあるべき。
- **観察**:
  - **特筆**: `HuffmanStrongForm.lean:176` の `huffmanLength_optimal_modulo_aux_ident` は
    **`swap_normalization_proof` を呼び出し、Hyp1 を genuine discharge 済**で、残る
    open hypothesis は `MergedHuffmanAuxIdentHypothesis` の **1 つだけ**。これは
    huffman-optimality plan の status (「2 hypothesis 引数」) から **進化済の状態**。
  - plan slug `huffman-moonshot-plan` (Phase 3 まで scope) に Phase 4-5 (T1-A' scope) の suspect が
    流れ込んでいる ⇒ **slug 再付与が必要**。または新規 plan
    (`huffman-vertical-reduction-plan` 等) を立てる。
  - close ROI: `huffmanLength_optimal_modulo_aux_ident` の `MergedHuffmanAuxIdentHypothesis` 1 件
    discharge ができれば **本 plan の 9 件 + huffman-optimality 2 件 + t1apprime-partial 19 件
    のうち少なくとも 22 件が連動 closure** 可能 — ROI 評価: **high (slug 整理) + low (実証明
    discharge)**.

### `huffman-optimality-moonshot-plan` への含意 (cross-plan)

上記 3 plan で **完全に同じ 2 hypothesis** (`SwapNormalizationHypothesis` /
`HuffmanMergedIdentificationHypothesis`) を chain で扱っており、本来 1 plan + cross-link で
管理されるべき。**現状の分散管理は audit visibility の defect**。Recommendations §1 参照。

### `lz78-moonshot-plan`

- **plan path**: `docs/shannon/lz78-moonshot-plan.md` (853 行)
- **最終 Phase status**: PASS-THROUGH / FLAW-VACUOUS — `LempelZiv78.lean` 主定理 body は
  `:= h_rate_bound` の identity wrap、3 predicate (`IsZivInequalityPassthrough` /
  `IsLZ78ConversePassthrough` / `IsSMBSandwichPassthrough`) はすべて `: Prop := True`。
  撤退 5 本全発動 (L-LZ1〜L-LZ5)、Arithmetic coding + Kolmogorov complexity scope-out (L-LZ6/L-LZ7)。
- **code suspect 件数**: **16** (`LZ78AchievabilityLimsup.lean` 2 / `LZ78FinalGlue.lean` 5 /
  `LZ78SMBSandwich.lean` 3 / `LZ78DistinctEncoding.lean` 1 / `LZ78ConverseKraft.lean` 1 /
  `LZ78GreedyParsing.lean` 1 / `LZ78GreedyParsingImpl.lean` 1 / `LZ78AsEventualAchievability.lean` 2)
- **Phase 別分布** — plan の予定 Phase A-F (`LempelZiv78.lean` 内 publish のみ) を **大幅に超えた
  後継 work (lz78-achievability-converse-plan / lz78-blockrv-refactor-plan 経由)** の suspect が
  本 plan slug に集約:

  | declaration | 役割 (= 進化後の state) | line |
  |---|---|---|
  | `lz78_achievability_limsup_le₂` / `_aseventual` | base-2 SMB との合成 limsup half、`IsLZ78AchievabilityZivUpperBound` / `IsLZ78ZivAsEventual` 経由 | `LZ78AchievabilityLimsup.lean:155, :335` |
  | `lz78_two_sided_optimality_distinct_genuine` / `_aseventual` | base-2 distinct headline、2 chain-hyp 受け取り (genuine Cover-Thomas Thm 13.5.3) | `LZ78AchievabilityLimsup.lean:237`、`LZ78AsEventualAchievability.lean:417` |
  | `lz78_achievability_upper_bound_ergodic` | achievability chain hyp + SMB discharge 内蔵 | `LZ78FinalGlue.lean:167` |
  | `lz78_two_sided_optimality_ergodic` / `_of_bounds` / `_greedy_impl` / `_bdd_below_free` | S19 headline 4 variants (genuine chain hyp 受け取り) | `LZ78FinalGlue.lean:214, :258, :332, :384` |
  | `lz78_converse_le_liminf₂` | base-2 converse liminf half | `LZ78ConverseKraft.lean:170` |
  | `lz78_converse_lower_bound_ergodic` / `_of_bridge` | converse lower bound assembly (genuine + SMB discharge) | `LZ78SMBSandwich.lean:396, :418` |
  | `lz78_converse_lower_bound_ergodic_greedy` | greedy 版 converse compat | `LZ78SMBSandwich.lean:468` |
  | `lz78_two_sided_optimality_distinct_bdd_free` | distinct headline (boundedness 内部 discharge 済) | `LZ78DistinctEncoding.lean:413` |
  | `lz78_asymptotic_optimality_with_greedy_encoding` | greedy 名 instantiation | `LZ78GreedyParsing.lean:518` |
  | `lz78_asymptotic_optimality_with_greedy_impl` | greedy-impl 名 instantiation | `LZ78GreedyParsingImpl.lean:422` |
  | `lz78_two_sided_optimality_distinct_aseventual` | satisfiable a.s.-eventual hyp 経由 (FALSE core 修正版) | `LZ78AsEventualAchievability.lean:334` |

- **整合性 a/b/c**: in-plan orphan = 0、**drop 漏れ = 16 (全部)**、件数一致 = ✅
- **観察**:
  - **plan は当初の pass-through 設計 (Prop := True / `:= h_rate_bound`)**、suspect は
    **後続 plan (achievability-converse / blockrv-refactor / aseventual / distinct-encoding) で
    genuine 化された wrapper / glue 群**。両者の状態が **乖離している**。本 plan の進化先 plan は
    `参照` 節に列挙されているが、suspect tag は **本 plan slug にのみ** 付いている。
  - **特筆 honesty defect 候補**:
    - `LZ78AsEventualAchievability.lean:334` 周辺の `lz78_two_sided_optimality_distinct_aseventual` の
      docstring は plan 自身が **「FALSE core 修正版」** と明記 (旧 `IsLZ78ZivCombinatorialCore` は
      FALSE と機械確認済とのこと、`LZ78ZivTreeBridge.lean` 参照)。これは **DEFECT の honest
      アラート**であり、plan 文書化が後追いで必要。
    - `LZ78SMBSandwich.lean:362` `IsLZ78ConverseChainHyp.ofSMBBridge` は body `:= h` の
      **defeq 別名 (循環)** と `lz78-residual-discharge-plan.md` 起草時 Read で確認済。
      suspect tag は `:395, :417, :468` (3 件) で関連 lemma に貼られているが、循環 `:= h` 本人
      (`:362`) には tag なし → **defect 該当 line に **`@audit:suspect`** 追加が必要**。
  - close ROI: **low** — 16 件はすべて headline / chain-hyp variants で、各々が
    `IsLZ78ZivCombinatorialCore` / `IsLZ78AchievabilityZivUpperBound` / `IsLZ78ConverseChainHyp` /
    `IsLZ78ZivAsEventual` 等の **load-bearing combinatorial / measure-theoretic 述語** に
    依存。これらは moonshot レベル (cylinder factorization / a.s.-eventual reformulation) で、
    後続 3 plan の crux と一致。
  - **open question**: 本 plan は事実上 archive 化されていて、現実の進捗は `residual-discharge-plan`
    + `aseventual` track が source-of-truth ⇒ **本 plan の status を `SUPERSEDED-BY:
    lz78-residual-discharge-plan` 等にマーク**するのが整理として正しい。

### `lz78-ziv-inequality-discharge-moonshot-plan`

- **plan path**: `docs/shannon/lz78-ziv-inequality-discharge-moonshot-plan.md` (170 行)
- **最終 Phase status**: DONE-HONEST-HYPS (partial counting layer) + headline passthrough FLAW-VACUOUS —
  `LZ78ZivInequality.lean` の counting 補題は genuine、bridge constructor は `True.intro`。
  L-LZ1-A / L-LZ1-B 採用、L-LZ1-C / L-LZ1-D 撤退。
- **code suspect 件数**: **8** (`LZ78ZivCombinatorics.lean` 5 件 + `LZ78ZivTreeNode.lean` 3 件)
- **個別不整合**:

  | line | declaration | 役割 |
  |---|---|---|
  | `LZ78ZivCombinatorics.lean:267` | `ziv_count_mul_log_le_neg_log_blockProb` | nat-log Ziv `c·log c ≤ -log Pₙ` (`hcore : IsLZ78ZivCombinatorialCore` + `hfac : IsLZ78PerPathParsingFactorization` 経由) |
  | `LZ78ZivCombinatorics.lean:287` | `ziv_count_mul_logb_le_neg_logb_blockProb` | base-2 版 (上の log → logb 2 変換) |
  | `LZ78ZivCombinatorics.lean:412` | `lz78DistinctRate_le_blockLogAvg₂_add_slack` | per-block Phase Z4 core step |
  | `LZ78ZivCombinatorics.lean:626` | `isLZ78AchievabilityZivUpperBound_distinct` | achievability upper bound 構成 (regularity hyp 経由) |
  | `LZ78ZivCombinatorics.lean:688` | `lz78_two_sided_optimality_distinct_ziv_core_wired` | Phase V partial headline (`hcore` + `hreg` + `h_lb` で wired) |
  | `LZ78ZivTreeNode.lean:428` | `lz78DistinctRate_le_blockLogAvg₂_add_slackOverhead` | overhead-aware 版 (上記 `_add_slack` の超過 version) |
  | `LZ78ZivTreeNode.lean:651` | `isLZ78AchievabilityZivUpperBound_distinctOverhead` | overhead-aware achievability — **plan が `IsLZ78ZivCombinatorialCoreOverhead` を FALSE と機械確認済** (`@audit:suspect` で vacuous mark) |
  | `LZ78ZivTreeNode.lean:712` | `lz78_two_sided_optimality_distinct_ziv_overhead_core_wired` | overhead Phase V headline (上記 false core 経由 — **vacuously conditioned**) |

- **整合性 a/b/c**: in-plan orphan = 0、**drop 漏れ = 8**、件数一致 = ✅
- **観察**:
  - plan の予定 publish file は `LZ78ZivInequality.lean` のみ. **実装は `LZ78ZivCombinatorics.lean` +
    `LZ78ZivTreeNode.lean` の 2 新規 file で展開**、scope が plan を超えている。
  - **honesty 観点で重大**: `LZ78ZivTreeNode.lean:712` の `_overhead_core_wired` headline は
    docstring 内で **「hypothesis は FALSE で vacuously conditioned」** と self-flag 済 — これは
    **honest なアラートで、`@audit:DEFECT(suspect)` レベルだが現状は `@audit:suspect` のまま**。
    タグの語彙を `docs/audit/audit-tags.md` 規約に従って **格上げ (`@audit:residual` ないし
    `@audit:DEFECT`)** すべき候補。
  - close ROI: clean core 系 (`:267, :287, :412, :626, :688`) は **medium** (Phase Z4
    cylinder factorization を構成すれば連鎖 closure)、overhead 系 (`:428, :651, :712`) は
    **low** (Mathematical content は plan 自身が「FALSE」と判定済、再設計が必要)。

### `lz78-residual-discharge-plan`

- **plan path**: `docs/shannon/lz78-residual-discharge-plan.md` (612 行)
- **最終 Phase status**: 進行中 — converse 群 (Phase C1-C4) と achiev 群 (Phase Z1-Z6) の併行
  着手設計。distinct headline `lz78_two_sided_optimality_distinct_bdd_free` の 2 chain-hyp を
  genuine 化することで hypothesis-free `lz78_two_sided_optimality_distinct` を publish 目標。
- **code suspect 件数**: **5** (全件 `LZ78ConverseDischarge.lean`)
- **個別不整合**:

  | line | declaration | 役割 |
  |---|---|---|
  | `:198` | `lz78_converse_lower_bound_pmfBased` | Phase C2-C3 の合流 (chain-hyp + SMB lower → liminf) |
  | `:223` | `lz78_converse_lower_bound_of_pointwise` | function-level lower bound variant |
  | `:274` | `lz78_converse_lower_bound_with_chain` | parent wrapper (chain-hyp 経由) |
  | `:304` | `lz78_converse_lower_bound_discharge` | L-LZ2 full discharge wrapper (alias) |
  | `:347` | `lz78_converse_lower_bound_greedy` | greedy 版 compat |

- **整合性 a/b/c**: in-plan orphan = 0、drop 漏れ = 5、件数一致 = ✅
- **観察**:
  - **本 plan は唯一、最近 (2026-05-21) 起草で suspect 件数と plan の Phase 構成が
    最も近い** (5 件すべて Phase C 系の publish 物に対応)。
  - 5 件すべて **`IsLZ78ConverseChainHyp` を hypothesis として受ける** wrapper / variant.
    plan は Phase C4 で `isLZ78ConverseChainHyp_distinct` を genuine 構成するのが目標、
    達成時に 5 件すべて連鎖 closure 可。
  - **`lz78-residual-discharge-plan` の起草 Read 時 (判断ログ #6) に確認済**: 親
    `IsLZ78ConverseChainHyp.ofSMBBridge` (`LZ78SMBSandwich.lean:362`) は **`:= h` の循環たらい回し**。
    本 plan はこれを genuine に組み直す方針。
  - close ROI: **medium** — plan の Phase C2 (`blockKraftSum_le_one`) + Phase C3
    (`lz_per_path_ge_neg_log_blockProb`) が完了すれば 5 件全 closure。Kraft 資産あり、
    1-2 セッションで完遂可能と plan 自身が判定。

## Recommendations

### 最優先 close 候補 (leverage 順)

1. **Huffman 3-plan の cross-plan merge / slug 再整理** (ROI = **high**)
   - 現状: `huffman-moonshot-plan` (Phase 3 完遂) / `huffman-optimality-moonshot-plan`
     (weak form publish) / `huffman-t1apprime-partial-moonshot-plan` (plumbing publish) の **3 plan
     が同じ 2 hypothesis** (`SwapNormalizationHypothesis` / `HuffmanMergedIdentificationHypothesis`)
     を扱っており、code suspect 30 件のうち本質 load-bearing は **2 hypothesis のみ**。
   - 提案: (a) 2 hypothesis を `@audit:staged(huffman-2hyp)` 等の slug で**新 plan
     `huffman-2hyp-vertical-reduction-plan`** にまとめる。(b) 既存 3 plan の suspect tag は
     新 slug に **移し替え or staged 化**。code は不変、docstring + plan ドキュメント update のみ。
     => 30 件 → 2 件相当に視認性が圧縮。
2. **`HuffmanStrongForm.lean:176` `huffmanLength_optimal_modulo_aux_ident` の slug 再付与**
   (ROI = **high**)
   - 現状: slug が `huffman-moonshot-plan` だが、本 plan は Phase 3 (Kraft 充足等) のみで、
     **本 lemma は T1-A' / T1-A'' 系**。Hyp1 が `swap_normalization_proof` で genuine
     discharge 済の **進化後の状態**で、残る open hypothesis は `MergedHuffmanAuxIdentHypothesis`
     の **1 つだけ**。docstring が honest 内容を既に書いているので、`@audit:staged` 格上げで sufficient。
3. **`lz78-moonshot-plan` の SUPERSEDED 化** (ROI = **high**)
   - 現状: `lz78-moonshot-plan` は当初 pass-through 設計 (Prop := True / `:= h_rate_bound`)、
     実装は後続 3 plan (`achievability-converse` / `blockrv-refactor` / `residual-discharge`) で
     genuine 化済。suspect 16 件は後続 plan の result.
   - 提案: 本 plan の status を `SUPERSEDED-BY: lz78-residual-discharge-plan + lz78-blockrv-refactor-plan
     + lz78-achievability-converse-plan` にマーク、suspect tag を後継 plan slug に移し替え。
     16 件の slug 再分配で audit visibility が大幅向上。

### plan 整合修正候補 (visibility)

- **`huffman-t1apprime-partial-moonshot-plan.md`** に Wave 4-H / 4-J / 4-K / 4-P の追記 (= 119 行
  plan → 数百行に拡大): 既に publish 済の wrapper / extractor 19 件を plan ドキュメント側で
  記録。`@audit` SoT は code 内 tag のままだが、plan ドキュメントの存在が **honest reviewer
  への signpost** として機能。
- **`LZ78SMBSandwich.lean:362` の循環 `:= h`** に直接 `@audit:DEFECT(lz78-converse-cycle)` タグ
  追加 (現在 untagged): plan `lz78-residual-discharge-plan` の判断ログ #6 で確認済の defect が
  code tag に降りていない。

### タグ語彙の格上げ候補 (`@audit:suspect` → `@audit:staged` / `@audit:residual`)

`docs/audit/audit-tags.md` 規約参照:

- `LZ78ZivTreeNode.lean:651, :712` (overhead vacuous 系): plan 自身が「FALSE」と機械確認済 ⇒
  `@audit:residual(lz78-ziv-overhead-false)` ないし `@audit:DEFECT` 検討。
- huffman optimality 系 11 件 (`:778, :1028` + body 9 件のうち strong-form 1 件):
  honest analytic Prop hypothesis を持つ完成 state ⇒ `@audit:staged(huffman-2hyp)`。
- lz78 chain-hyp wrapper 系 16+5 件: `IsLZ78AchievabilityChainHyp` / `IsLZ78ConverseChainHyp` を
  load-bearing として受ける完成 state ⇒ `@audit:staged(lz78-2chainhyp)`。

### 次 wave 推奨スコープ

- **Wave 2 (推奨)**: huffman 3-plan merge + `HuffmanStrongForm:176` slug 再付与で **Huffman 系
  audit を 30 件 → 1 件 (`MergedHuffmanAuxIdentHypothesis`)** に圧縮 ⇒ T1-A''' 着手のためには
  predicate `Classical.choose` 非決定性 (huffmanStep の tie 破り) が真の壁 (plan 判断ログ参照)。
- **Wave 3 (推奨)**: lz78 主 plan SUPERSEDED 化 + suspect 16 件の再分配。後継 3 plan へ slug
  移管後、`lz78-residual-discharge-plan` の Phase C2-C4 完遂で 5+ 件連鎖 closure。

### multi-plan 連鎖の評価

- **Huffman 内 (chain)**: huffman → huffman-optimality → huffman-t1apprime-partial の chain は
  **本質的に 1 chain** (Phase 3 完了 → weak form → plumbing)。3 plan に分散する必然性なし。
  merge による視認性向上の余地大。30 件 suspect の本質は 2 hypothesis + 1 vertical reduction
  primitive (`MergedHuffmanAuxIdentHypothesis`) で、合計 **3 つの load-bearing source**。
- **LZ78 内 (DAG)**: lz78-moonshot (root, archive 候補) → {ziv-inequality-discharge,
  residual-discharge, achievability-converse (W1-D 担当外), blockrv-refactor (W1-D 担当外),
  aseventual-achievability (W1-D 担当外、`LZ78AsEventualAchievability.lean` で referenced)}
  の DAG. 担当 3 plan (lz78 / ziv-inequality / residual) のうち、**residual-discharge が現在
  最 active な source of truth**、他 2 plan は legacy / partial. **closure 順序**:
  ziv-inequality-discharge → residual-discharge → lz78 (SUPERSEDED) ⇒ いずれも 5-8 件単位の
  グループ closure が可能。
