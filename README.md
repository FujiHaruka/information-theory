# Shannon Information Theory in Lean 4

A Lean 4 + [Mathlib](https://github.com/leanprover-community/mathlib4) formalization of
Shannon information theory, following Cover & Thomas, *Elements of Information Theory*.

## Entirely agent-authored

The defining property of this project: **every proof was written entirely by a coding
agent** (Claude Code), with no human-written proof steps. Each proof is machine-checked by
Lean, so its correctness holds independently of who — or what — wrote it.

## Formalized results

Selected headline theorems, organized after the chapters of Cover & Thomas. Every result
listed below is **complete and machine-checked** — no `sorry`, and no axioms beyond Lean's
standard `propext` / `Classical.choice` / `Quot.sound`. Names link to their source.

Each statement is summarized from the declaration's own docstring and is trimmed to fit the
table (a trailing `…` marks where); the linked source carries the exact Lean statement with
its hypotheses.

<!-- THEOREMS:START — generated by scripts/gen_readme_table.ts from docs/readme-theorems.txt; do not edit by hand -->
Library totals, recounted on every regeneration: **4,111 theorems and lemmas**, **915 definitions** and **129,497 lines of Lean** (non-blank, comments excluded) across **421 modules**, with **0 `sorry`** and **0 custom axioms**. The 94 results below are the curated selection.

**Ch. 2 — Entropy, mutual information, DPI**

| Result | Statement |
|--------|-----------|
| [`entropy`](InformationTheory/Shannon/Bridge.lean) | Shannon entropy of a discrete random variable taking values in a finite alphabet. |
| [`mutualInfo`](InformationTheory/Shannon/MutualInfo.lean) | Mutual information via KL divergence: `I(X; Y) := KL(P_{X,Y} ‖ P_X ⊗ P_Y)`. |
| [`mutualInfo_chain_rule`](InformationTheory/Shannon/CondMutualInfo.lean) | Chain rule: `I((Z, X); Y) = I(Z; Y) + I(X; Y \| Z)`. |
| [`mutualInfo_le_of_postprocess`](InformationTheory/Shannon/DPI.lean) | **Data processing inequality**: `I(X; f(Y)) ≤ I(X; Y)` for measurable `f`. |
| [`fano_inequality_measure_theoretic`](InformationTheory/Fano/Measure.lean) | **Fano's inequality** (measure-theoretic form, deterministic decoder). |

**Ch. 3 — Asymptotic equipartition (AEP)**

| Result | Statement |
|--------|-----------|
| [`aep_ae`](InformationTheory/Shannon/AEP/Basic/Core.lean) | **Asymptotic equipartition property** (almost-sure form): for an i.i.d. discrete sequence `Xs : ℕ → Ω → α` with finite alphabet `α`, the empirical entropy estimator `(1/n) ∑ i, (−log P(Xs i ω))` converges almost surely to the entropy `H(Xs 0)`. |
| [`typicalSet`](InformationTheory/Shannon/AEP/Basic/Core.lean) | Typical set: blocks `x : Fin n → α` whose empirical entropy is within `ε` of the true entropy `H(Xs 0)`. |
| [`stronglyTypicalSet`](InformationTheory/Shannon/StrongTypicality.lean) | The strongly typical set `A^*_ε^n := { x : Fin n → α \| ∀ a, \|(typeCount x a : ℝ)/n - P(a)\| ≤ ε }`. |

**Ch. 4 — Entropy rate & the SMB theorem**

| Result | Statement |
|--------|-----------|
| [`entropyRate`](InformationTheory/Shannon/EntropyRate.lean) | Entropy rate `lim H(X_0, …, X_{n-1}) / n` (Cover-Thomas). Existence proven by `entropyRate_exists_of_stationary`. |
| [`shannon_mcmillan_breiman`](InformationTheory/Shannon/SMB/AlgoetCover/Liminf.lean) | **Shannon–McMillan–Breiman theorem**: for a stationary ergodic process with finite alphabet `α`, the per-symbol negative log-likelihood `blockLogAvg μ p n` converges almost surely to the entropy rate `entropyRate μ p`. This is the hypothesis-free capstone: the four hypotheses of `shannon_mcmillan_breiman_of_sandwich` hold unconditionally for an ergodic process. |
| [`birkhoff_ergodic_ae`](InformationTheory/Shannon/BirkhoffErgodic.lean) | The **Birkhoff individual ergodic theorem**. For a probability-preserving ergodic transformation `T : Ω → Ω` and an integrable observable `f : Ω → ℝ`, the Birkhoff time averages A_n ω := (∑_{i=0}^{n} f (T^[i] ω)) / (n + 1) converge almost everywhere to the spatial mean `∫ f dμ`. |

**Ch. 5 — Data compression**

| Result | Statement |
|--------|-----------|
| [`shannonCode_expected_length_bounds`](InformationTheory/Shannon/ShannonCode/Basic.lean) | Shannon code sandwich (Cover–Thomas): for a probability measure `P` with full support on a finite alphabet, the Shannon codeword lengths satisfy `H_D(P) ≤ E[L_Shannon] < H_D(P) + 1`. |
| [`kraftSum_le_one_of_uniquelyDecodable`](InformationTheory/Shannon/McMillanKraftBridge.lean) | **McMillan's inequality**, per-symbol form: if the codeword assignment `c : α → List β` is injective with uniquely-decodable image, then the per-symbol Kraft sum `kraftSum \|β\| (\|c ·\|) ≤ 1`. `D = Fintype.card β` is the alphabet size; the codeword lengths are `l a = (c a).length`. |
| [`huffmanLength_optimal`](InformationTheory/Shannon/Huffman/StrongForm.lean) | **Huffman's theorem** (optimality): `huffmanLength P` minimizes expected codeword length among all positive Kraft-feasible length functions. |

**Ch. 6 — Gambling & the doubling rate**

| Result | Statement |
|--------|-----------|
| [`doublingRate_le_proportional`](InformationTheory/Shannon/Gambling/Basic.lean) | Proportional (Kelly) betting maximizes the doubling rate: for any full-support bet `b` and positive odds `o`, `W(b, o, p) ≤ W(p, o, p)`. This is Cover–Thomas. |
| [`doublingRate_eq_proportional_iff`](InformationTheory/Shannon/Gambling/Basic.lean) | The doubling rate is maximized by `b` iff `b` is the proportional (Kelly) bet `b = p`. |
| [`sideInfo_doublingRate_increment_eq_mutualInfo`](InformationTheory/Shannon/Gambling/SideInformation.lean) | The increment of the optimal doubling rate obtained from the side information `Y` equals the mutual information `I(X; Y)`. Writing `W*(X \| Y)` for the optimal conditional doubling rate and `W*(X)` for the optimal doubling rate of the `X`-marginal, `W*(X \| Y) − W*(X) = I(X; Y)`. This is Cover–Thomas (gambling with side information). |
| [`condDoublingRate_le_proportional`](InformationTheory/Shannon/Gambling/SideInformation.lean) | Proportional (Kelly) betting on each observed value of the side information maximizes the conditional doubling rate: for any full-support strategy `b` and positive odds `o`, `W(b \| Y) ≤ W(pXgivenY \| Y)`. This is the conditional step towards Cover–Thomas. |
| [`seqLogWealth_div_tendsto_doublingRate`](InformationTheory/Shannon/Gambling/OperationalSequences.lean) | For an i.i.d. horse-race sequence `Xs` and a fixed bet `b` under odds `o`, the time-averaged log-wealth growth `(1/n)·log S_n` converges almost surely to the doubling rate `doublingRate b o (lawPmf μ (Xs 0))`, where `lawPmf μ (Xs 0)` is the law of a single race (Cover–Thomas). |
| [`seqLogWealth_proportional_div_tendsto`](InformationTheory/Shannon/Gambling/OperationalSequences.lean) | The sequence-level growth rate of the proportional (Kelly) bet `b = p` in closed form, `W(p, o, p) = ∑ x, p x · log (o x) − H(p)`. |
| [`seqLogWealth_proportional_asymptotically_optimal`](InformationTheory/Shannon/Gambling/OperationalSequences.lean) | The proportional (Kelly) bet `b = p` is asymptotically optimal at the sequence level (Cover–Thomas): almost surely both the arbitrary full-support bet `b` and the Kelly bet `p` have a growth rate, and the arbitrary bet does not beat the Kelly bet. |
| [`seqLogWealth_tendsto_atTop_of_pos_doublingRate`](InformationTheory/Shannon/Gambling/OperationalSequences.lean) | If the doubling rate is positive, the log-wealth `log S_n` diverges to `+∞` almost surely, i.e. wealth grows exponentially: a positive doubling rate means the gambler gets rich (Cover–Thomas). |
| [`seqLogWealth_tendsto_atBot_of_neg_doublingRate`](InformationTheory/Shannon/Gambling/OperationalSequences.lean) | If the doubling rate is negative, the log-wealth `log S_n` diverges to `−∞` almost surely, i.e. wealth decays to zero exponentially: a negative doubling rate means the gambler goes broke (Cover–Thomas). |

**Ch. 7 — Channel capacity**

| Result | Statement |
|--------|-----------|
| [`shannon_noisy_channel_coding_theorem_general`](InformationTheory/Shannon/ChannelCoding/ShannonTheoremMaxError.lean) | **Shannon's noisy channel coding theorem** (general, max-error form): for `R < capacity W` and any `ε > 0`, there exists `N` such that for all `n ≥ N` there is a code with `M ≥ ⌈exp(nR)⌉` codewords achieving max-error less than `ε`. |
| [`channel_coding_feedback_converse`](InformationTheory/Shannon/ChannelCoding/Feedback.lean) | Feedback channel coding converse, hypothesis form. Under the per-letter bound `I(Msg; Y_i \| Y^{<i}) ≤ I(X_i; Y_i)` and `I(X_i; Y_i) ≤ C` for all `i`, combined with the Fano inequality: `log \|M\| ≤ n · C.toReal + h(Pe) + Pe · log(\|M\| - 1)` where `Pe := μ {Msg ≠ decoder ∘ Y^n}`. The capacity `C` is an arbitrary `ℝ≥0∞` value; callers supply the DMC capacity bound. |
| [`shannon_converse_single_shot`](InformationTheory/Shannon/Converse.lean) | The single-shot converse to the channel coding theorem: for a uniformly distributed message `Msg` transmitted via channel output `Yo` and decoded by `decoder`, `log \|M\| ≤ I(Msg; Yo) + h(Pe) + Pe · log(\|M\| - 1)`, where `Pe = errorProb μ Msg Yo decoder = μ {Msg ≠ decoder ∘ Yo}`. Requires `mutualInfo μ Msg Yo ≠ ∞` so that `.toReal` is monotone across DPI. |
| [`channelCoding_strong_converse_asymptotic`](InformationTheory/Shannon/ChannelCoding/StrongConverseAsymptotic.lean) | **Wolfowitz strong converse (asymptotic)**: for a memoryless channel `W` over finite alphabets, if the rate `log (M n) / n` eventually exceeds `capacity W + δ` (with `δ > 0`), then the average error probability tends to `1`. |

**Ch. 8 — Differential entropy**

| Result | Statement |
|--------|-----------|
| [`differentialEntropy_gaussianReal`](InformationTheory/Shannon/DifferentialEntropy.lean) | The differential entropy of a Gaussian: `h(𝒩(m, v)) = (1/2) log (2πe v)`. |
| [`jointDifferentialEntropyPi_le_sum`](InformationTheory/Shannon/MultivariateDiffEntropy.lean) | `n`-variable differential-entropy subadditivity `h(Yⁿ) ≤ ∑ᵢ h(Yᵢ)` (the parallel-Gaussian consumer form). |

**Ch. 9 — Gaussian channel**

| Result | Statement |
|--------|-----------|
| [`awgn_capacity_closed_form_genuine`](InformationTheory/Shannon/AWGN/CapacityConverseMaxent.lean) | AWGN capacity closed form (Cover-Thomas). `awgnCapacity P N = (1/2) log(1 + P/N)`. This supersedes `awgnCapacity_eq` (`AWGN/Basic.lean`): there the converse max-entropy bound `h_max_ent` was an explicit hypothesis; here it is discharged by `awgn_per_input_mi_le_log`. |
| [`contAwgn_eq_shannonHartley`](InformationTheory/Shannon/ShannonHartley/ConverseFinal.lean) | The operational capacity of the band-limited AWGN channel equals the closed form `W·log(1 + P/(N₀·W))` — the continuous-time Shannon-Hartley formula. Both halves are proved: achievability (`≥`) by `contAwgn_ge_shannonHartley`, and the converse (`≤`) by `contAwgn_le_shannonHartley` (band-Gram ellipsoid → prolate-count water-filling → the `T → ∞`, `c₀ → 0`, `ε → 0` limits). |
| [`parallel_gaussian_capacity_formula_minimal`](InformationTheory/Shannon/ParallelGaussian/PerCoordRegularity.lean) | **Parallel Gaussian channel capacity** (water-filling), hypothesis-minimal form. The parallel Gaussian capacity equals the water-filling sum at the KKT water level `ν`. The regularity bundle is assembled internally, so the only inputs are the KKT condition `h_kkt` and the preconditions `hP` / `hN` / `h_meas` / `h_parallel_meas`. |
| [`whittaker_shannon_bandlimited`](InformationTheory/Shannon/WhittakerShannon.lean) | **Whittaker–Shannon sampling theorem**: a band-limited signal is recovered from its integer samples — for continuous integrable `f` whose Fourier transform vanishes outside `[-1/2, 1/2]`, the cardinal series `∑ n : ℤ, f n · sinc (t - n)` sums to `f t` at every real `t`. |
| [`whittaker_shannon_hasSum`](InformationTheory/Shannon/WhittakerShannon.lean) | **Whittaker–Shannon**, per-`t` `HasSum` on an L² spectrum (unconditional core). |

**Ch. 10 — Rate–distortion**

| Result | Statement |
|--------|-----------|
| [`rate_distortion_achievability`](InformationTheory/Shannon/RateDistortion/AchievabilityStrongTypicality.lean) | **Rate-distortion theorem** (achievability). The public form; the unconditional (no `hqStar_pos`) statement requires a perturbation argument. |
| [`rate_distortion_achievability_operational`](InformationTheory/Shannon/RateDistortion/AchievabilityUnconditional.lean) | **Rate-distortion theorem** (achievability, unconditional operational form). For a full-support source `P_X` with a feasible constraint set and any rate `R > R(D)`, for every `ε > 0` there is a block length `N` such that every `n ≥ N` admits a rate-`R` lossy code whose expected block distortion is at most `D + ε`. |
| [`rate_distortion_achievability_operational_general`](InformationTheory/Shannon/RateDistortion/AchievabilityGeneralSource.lean) | **Rate-distortion theorem** (achievability, general source). Removes the full-support precondition from `rate_distortion_achievability_operational`: for any source `P_X ∈ stdSimplex` with a feasible constraint set and any rate `R > R(D)`, for every `ε > 0` there is a block length `N` such that every `n ≥ N` admits a rate-`R` lossy code whose expected block distortion is at most `D + ε`. |
| [`rateDistortionFunction_convexOn`](InformationTheory/Shannon/RateDistortion/Convexity.lean) | The rate-distortion function is convex in the distortion threshold, `R(λ D₁ + (1-λ) D₂) ≤ λ R(D₁) + (1-λ) R(D₂)`. The hypothesis `h_int_witness` (integrability of `d` on every joint with `Prod.fst`-marginal `P`) is a regularity precondition, needed so that the mixture witness has well-defined feasibility (`expectedDistortion` linearity). |
| [`rate_distortion_converse_n_letter_singleLetter`](InformationTheory/Shannon/RateDistortion/ConverseNLetter.lean) | **Rate-distortion theorem** (converse, n-letter single-letterized form). Given a block lossy code, an i.i.d. source `P_X`, and a probability space `(Ω, μ)` where `Xs i : Ω → α` are i.i.d. copies of `P_X` (mutual independence `hindep` + identical marginals `hXs_law`) and `X̂ᵢ := (decoder ∘ encoder ∘ X^n)ᵢ`, the single-letter rate-distortion function satisfies … |

**Ch. 11 — Hypothesis testing & large deviations**

| Result | Statement |
|--------|-----------|
| [`stein_converse_finite_n`](InformationTheory/Shannon/Stein/Converse.lean) | **Stein's lemma** (converse, finite `n`): any measurable `ε`-level test `s` (with `Pⁿ sᶜ ≤ ε`) and `0 < n` satisfies `-(1/n) log Qⁿ s ≤ (klDiv P Q).toReal / (1−ε) + log 2 / (n(1−ε))`. |
| [`sanov_ldp_upper_bound`](InformationTheory/Shannon/Sanov/LDP.lean) | **Sanov's theorem** (LDP upper bound): `(1/n) log Q^n({x \| typeCount x ∈ E n}) ≤ -D + ε` for all large `n`, provided every `c ∈ E n` satisfies `D ≤ klDivIndex c n Q`. |
| [`cramer_lower_boundary`](InformationTheory/Shannon/CramerCltBoundaryClosure.lean) | **Cramér's theorem** (large-deviation lower bound, boundary case): for a bounded i.i.d. sequence and a tilt `λ` whose cgf derivative is `a`, `−(λ·a − cgf λ) ≤ liminf (1/n) log P(∑ᵢ Yᵢ ≥ a·n)`. |
| [`chernoff_converse`](InformationTheory/Shannon/Chernoff/Converse.lean) | The optimal Bayes error exponent cannot exceed the Chernoff information (Cover–Thomas, converse half). Proved on the interior `0 < λ* < 1` (the overlapping-support / non-degenerate case; `hlam_io` is a non-degeneracy precondition, not load-bearing). |
| [`tvNorm_le_sqrt_klDiv`](InformationTheory/Shannon/Pinsker/Basic.lean) | **Pinsker's inequality** (weak form, constant `1`): for probability measures `P ≪ Q` on a finite alphabet, `tvNorm P Q ≤ √((klDiv P Q).toReal)`. |

**Ch. 12 — Maximum entropy**

| Result | Statement |
|--------|-----------|
| [`entropy_le_log_card`](InformationTheory/Shannon/MaxEntropy/Basic.lean) | **Gibbs' inequality** (uniform bound): the Shannon entropy of a finite-alphabet random variable is at most `log \|α\|`, for an arbitrary probability measure `μ`. |
| [`expFamily_maximizes_entropy_of_KKT`](InformationTheory/Shannon/MaxEntropy/ConstrainedKKT.lean) | **Maximum entropy theorem** (upper bound, `KKTSolution` form): a constraint-feasible `P` cannot exceed the entropy of the exponential-family solution attached to a KKT witness. |

**Ch. 13 — Universal coding (LZ78)**

| Result | Statement |
|--------|-----------|
| [`lz78_asymptotic_optimality_with_greedy`](InformationTheory/Shannon/LZ78/AsymptoticOptimality/ParentBridgeAchievability.lean) | LZ78 asymptotic optimality for the concrete greedy parser `lz78GreedyEncodingLength` (Cover–Thomas). For a stationary ergodic source `p : ErgodicProcess μ α` on a finite alphabet `α`, the per-symbol output length of the longest-prefix-match greedy LZ78 parse converges almost surely to the bit entropy rate: `lim_{n → ∞} (1/n) · lz78GreedyEncodingLength(X^n) = entropyRate₂ μ p a.s.` The convergence … |
| [`arithmeticCode_expected_length_bounds`](InformationTheory/Shannon/ArithmeticCoding.lean) | **Shannon-Fano-Elias coding**: the expected-length sandwich `H₂(P) ≤ E[L] ≤ H₂(P) + 2`. |

**Ch. 14 — Kolmogorov complexity**

| Result | Statement |
|--------|-----------|
| [`kolmogorov_entropy_rate`](InformationTheory/Shannon/Kolmogorov/EntropyRate.lean) | Kolmogorov complexity converges to the entropy rate: for an i.i.d. source, the normalized expected conditional complexity of a length-`n` block tends to the bit-rebased entropy `H(X) / log 2` (CT 2nd ed.). |
| [`condComplexity_not_computable`](InformationTheory/Shannon/Kolmogorov/Noncomputable.lean) | Conditional Kolmogorov complexity is not computable, for any fixed condition `y` (Berry's paradox): a computable `C(· \| y)` would let one search for the least string of complexity at least `k`, describing it in only `natLen k` bits. |
| [`incompressible_seq_freq_tendsto_half`](InformationTheory/Shannon/Kolmogorov/Incompressible.lean) | Law-of-large-numbers corollary: along an eventually-incompressible family the frequency of ones converges to `1/2`. |
| [`universalProb_ge_two_pow_neg_prefixComplexity`](InformationTheory/Shannon/Kolmogorov/UniversalProbability.lean) | The universal probability dominates the weight of a shortest program: `2^{-K(x)} ≤ P_U(x)`. |
| [`prefixComplexity_le_two_mul_neg_logb_universalProb`](InformationTheory/Shannon/Kolmogorov/Levin.lean) | The upper half of the factor-two relation between prefix complexity and universal probability: `K(x) ≤ 2 * (-log₂ P_U(x)) + 1`. Together with `neg_logb_universalProb_le_prefixComplexity` this places `K(x)` between `-log₂ P_U(x)` and twice that value plus one. |
| [`chaitinOmega_le_one`](InformationTheory/Shannon/Kolmogorov/Omega.lean) | The halting probability is a subprobability: `Ω ≤ 1`, because the halting programs form a prefix-free set and every finite subsum is a Kraft sum. The bound is not the degenerate empty-sum reading: the halting set is nonempty (the literal echo program halts), and `chaitinOmega_pos` pins `Ω` to `(0, 1]`. |
| [`prefixComplexity_not_computable`](InformationTheory/Shannon/Kolmogorov/Omega.lean) | Prefix Kolmogorov complexity is not computable (Berry's paradox): a computable `K` would let one search for the least string of prefix complexity at least `k`, which the self-delimiting interpreter then describes in `2 · natLen k + O(1)` bits. |
| [`prefixUniversalEval_dom_not_computablePred`](InformationTheory/Shannon/Kolmogorov/PrefixComputability.lean) | The halting set of the self-delimiting machine is undecidable: a decision procedure for it would decide the halting problem of Mathlib's universal interpreter, which the interpret-mode entry `prefixInterpretProg` embeds into it. |
| [`chaitinOmega_not_computable`](InformationTheory/Shannon/Kolmogorov/OmegaNoncomputable.lean) | Chaitin's constant is not computably approximable: no computable sequence of dyadic numerators `a` satisfies `\|Ω - a n * 2 ^ (-n)\| ≤ 2 ^ (-n)` for every `n`. |
| [`prefixComplexity_le_twoPartLength`](InformationTheory/Shannon/Kolmogorov/SufficientStatistic.lean) | Every two-part description of `x` bounds its prefix complexity, up to an additive constant independent of `x` and of the model. The index part carries the machine-specific coefficient `4` built into `twoPartLength`, so this is not the textbook inequality `K(x) ≤ K(S) + log \|S\| + O(1)`; the model part does carry coefficient `1`. |
| [`mdlComplexity_sub_prefixComplexity_le`](InformationTheory/Shannon/Kolmogorov/SufficientStatistic.lean) | The shortest two-part description length agrees with the prefix complexity up to an additive constant. As with `prefixComplexity_le_twoPartLength`, the index part of `twoPartLength` carries the machine-specific coefficient `4`, so this is a statement about that quantity and not the textbook minimum description length principle. |

**Ch. 15 — Distributed source coding**

| Result | Statement |
|--------|-----------|
| [`slepian_wolf_full_rate_region_achievability`](InformationTheory/Shannon/SlepianWolf/FullRateRegion/PairBound.lean) | **Slepian–Wolf theorem** (full rate region achievability): for an i.i.d. source `(Xⁿ, Yⁿ)` with full support, any rate pair `(R_X, R_Y)` with `R_X > H(X\|Y)`, `R_Y > H(Y\|X)`, `R_X + R_Y > H(X,Y)` is achievable: there are codebook sizes `M_X, M_Y` with the required asymptotic rates and encoders/decoders whose error probability → 0. |
| [`wyner_ziv_achievability`](InformationTheory/Shannon/WynerZiv/Achievability/Headline.lean) | Wyner–Ziv operational achievability. If the information-theoretic Wyner–Ziv rate `wynerZivRate` at distortion `D` for the i.i.d. source `P_XY` (with decoder side information `Y`) is strictly below `R`, then `R` is operationally achievable at distortion `D`: there is a sequence of Wyner–Ziv block codes whose log-cardinality rate tends to `R` and whose expected block distortion is eventually within … |
| [`wyner_ziv_converse`](InformationTheory/Shannon/WynerZiv/Converse/Headline.lean) | Wyner–Ziv converse (Cover–Thomas, operational lower bound). If rate `R` is achievable at distortion `D` for the i.i.d. source `P_XY` with decoder side information, then the reshaped Wyner–Ziv rate satisfies `R_WZ(D) ≤ R`. |
| [`mac_converse`](InformationTheory/Shannon/MultipleAccess/Converse.lean) | Per-letter-sum outer bound for the MAC (Cover–Thomas): for uniform, independent messages sent over a memoryless joint channel and decoded jointly, the rate pair `(log \|M₁\|, log \|M₂\|)` lies in the capacity region whose information bounds are the per-letter single-letter channel sums (plus the Fano error slack). |
| [`mac_achievability`](InformationTheory/Shannon/MultipleAccess/Achievability/RandomCoding.lean) | Achievability of the MAC corner-point region (Cover–Thomas): for an independent product input `p₁ ⊗ p₂` with full-support marginals and a full-support MAC channel `W`, any rate pair `(R₁, R₂)` strictly inside the corner-point region `R₁ < I(X₁; (X₂, Y))`, `R₂ < I(X₂; (X₁, Y))`, `R₁ + R₂ < I((X₁, X₂); Y)` is achievable: for every target error `ε' > 0` there is `N` such that for all `n ≥ N` there is … |
| [`mac_capacity_region_reconciliation`](InformationTheory/Shannon/MultipleAccess/Reconciliation.lean) | The achievability and converse descriptions of the MAC capacity region coincide: under the independent product input `p₁ ⊗ p₂`, a rate pair lies in the achievability region (with corner informations `macInfo₁` / `macInfo₂` / `macInfoBoth`) iff it lies in the converse region (with the textbook conditional/joint informations `I(X₁; Y \| X₂)` / `I(X₂; Y \| X₁)` / `I((X₁, X₂); Y)` on … |
| [`mac_timesharing_capacity_region`](InformationTheory/Shannon/MultipleAccess/TimeSharingConverse/Assembly.lean) | Full first-quadrant characterization of the MAC capacity region (Cover–Thomas): for a channel whose transition probabilities are all strictly positive, the operational capacity region, intersected with the first quadrant, equals the closed convex hull of the union of all per-input pentagons `macPentagon p₁ p₂ W` over probability inputs `p₁`, `p₂`. |
| [`bc_degraded_converse_from_code`](InformationTheory/Shannon/BroadcastChannel/DegradedFromCode.lean) | The degraded outer bound instantiated at a bare broadcast code (Cover–Thomas): for a physically degraded Markov channel `W` and any two-receiver block code `c`, the canonical ambient measure `bcConverseAmbient c W` discharges every hypothesis of the message-level converse `bc_degraded_converse`, so the rate pair `(log M₁, log M₂)` lies in the auxiliary-variable capacity region whose information … |
| [`bc_achievability`](InformationTheory/Shannon/BroadcastChannel/Achievability/Assembly.lean) | Achievability half of the degraded broadcast channel coding theorem, in the superposition inner-bound form of Cover–Thomas *Elements of Information Theory*. |
| [`marton_achievability`](InformationTheory/Shannon/BroadcastChannel/Marton/Achievability.lean) | Achievability of Marton's inner bound for a two-receiver broadcast channel with private messages. For auxiliary variables with joint law `pV`, an input kernel `K` and a channel `W`, a rate pair obeying the three strict inequalities · `R₁ < I(V₁; Y₁)`, · `R₂ < I(V₂; Y₂)`, · `R₁ + R₂ < I(V₁; Y₁) + I(V₂; Y₂) − I(V₁; V₂)` is achievable: at every large enough block length there is a `BroadcastCode` … |
| [`martonRegionUnion_subset_capacity`](InformationTheory/Shannon/BroadcastChannel/MartonFullSupport.lean) | Marton's inner bound is achievable in full: the union over all auxiliary laws and input kernels, not only over the full-support ones, is contained in the operational capacity region. Only the channel is required to have full support. |
| [`bc_capacity_subset_coop`](InformationTheory/Shannon/BroadcastChannel/OuterBound.lean) | The operational capacity region of a general broadcast channel is contained in the cooperative outer region: letting the two receivers pool their outputs can only enlarge the set of achievable rate pairs, and the resulting single-user converses bound each rate and the sum. |
| [`bc_capacity_subset_uv`](InformationTheory/Shannon/BroadcastChannel/OuterBoundUV/Assembly.lean) | The operational capacity region of a broadcast channel is contained in the UV (Nair–El Gamal) outer region. Together with `marton_region_subset_capacity` this places the capacity region between Marton's inner bound and the UV outer bound as subsets of the plane. |
| [`bc_lessNoisy_capacity_eq_uv`](InformationTheory/Shannon/BroadcastChannel/Superposition/Assembly.lean) | The capacity region of a less noisy broadcast channel whose transition law gives every output pair positive mass is its UV outer region `bcOuterRegionUV`, a single-letter expression in the four information slots of a five-tuple law. |
| [`bc_moreCapable_capacity_eq_uv`](InformationTheory/Shannon/BroadcastChannel/Superposition/MoreCapable.lean) | The capacity region of a more capable broadcast channel whose transition law gives every output pair positive mass is its UV outer region `bcOuterRegionUV`, a single-letter expression in the four information slots of a five-tuple law. |
| [`bc_degraded_capacity_eq_uv`](InformationTheory/Shannon/BroadcastChannel/Superposition/MoreCapable.lean) | The capacity region of a physically degraded broadcast channel whose transition law gives every output pair positive mass is its UV outer region. Degradedness is the strongest of the three comparisons, so the single-letter characterization of a more capable channel applies with no further analysis. |
| [`relay_cutset_outer_bound`](InformationTheory/Shannon/RelayCutset.lean) | **Cut-set outer bound** (relay channel, Cover–Thomas): for a uniformly distributed message `W` decoded from the receiver output `Yⁿ`, the rate is bounded by the `min` of the two cut rates, each taken as its per-letter sum plus a common Fano slack: · the **broadcast cut** `∑ᵢ I(Xᵢ; Y₁ᵢ, Yᵢ \| X₁ᵢ) + h(Pe) + Pe · log(M - 1)`, and · the **MAC cut** `∑ᵢ I(Xᵢ, X₁ᵢ; Yᵢ) + h(Pe) + Pe · log(M - 1)`, where … |

**Ch. 16 — Log-optimal portfolio**

| Result | Statement |
|--------|-----------|
| [`growthRate_concaveOn`](InformationTheory/Shannon/Portfolio/Basic.lean) | (Cover–Thomas): the growth rate is concave in the portfolio. |
| [`logOptimal_of_kuhnTucker`](InformationTheory/Shannon/Portfolio/Basic.lean) | (Cover–Thomas), reverse direction: a portfolio `bs` satisfying the Kuhn–Tucker condition is log-optimal (maximizes the growth rate on the simplex). |
| [`kuhnTucker_of_logOptimal`](InformationTheory/Shannon/Portfolio/Basic.lean) | (Cover–Thomas), forward direction: a log-optimal portfolio `bs` satisfies the Kuhn–Tucker condition `∀ i, ∑ a, p a · X a i / S_bs(a) ≤ 1`. |
| [`competitive_optimality`](InformationTheory/Shannon/Portfolio/Basic.lean) | (Cover–Thomas): competitive optimality of a Kuhn–Tucker portfolio `bs`. If `bs` satisfies the Kuhn–Tucker condition `∀ i, ∑ a, p a · X a i / S_bs(a) ≤ 1`, then every portfolio `b` on the simplex has expected wealth ratio at most one, `∑ a, p a · (S_b(a) / S_bs(a)) ≤ 1`. |
| [`seqLogWealth_div_tendsto_growthRate`](InformationTheory/Shannon/Portfolio/OperationalSequences.lean) | Operational log-optimal portfolio theorem (Cover–Thomas): for an i.i.d. market sequence `As`, a fixed rebalanced portfolio `b` under price relatives `X`, the time-averaged log-wealth growth `(1/n)·log S_n` converges almost surely to the growth rate `growthRate (lawPmf μ (As 0)) X b`, where `lawPmf μ (As 0)` is the law of a single outcome. |
| [`sideInfo_growthRate_increment_le_mutualInfo`](InformationTheory/Shannon/Portfolio/SideInformation.lean) | (Cover–Thomas, portfolio with side information): the increment of the growth rate obtained from the side information `Y` is bounded above by the mutual information `I(X; Y)`. Here `bs` is a marginal log-optimal (Kuhn–Tucker) portfolio and `bcond y` an arbitrary portfolio on the simplex for each observed `y`, so `W(bcond \| Y) − W*(X) ≤ I(X; Y)`. |
| [`seqLogWealth_div_tendsto_stationary`](InformationTheory/Shannon/Portfolio/StationaryMarket.lean) | Stationary ergodic log-optimal portfolio theorem (Cover–Thomas): for a measure-preserving ergodic shift `T` on a probability space, a fixed rebalanced portfolio `b`, and an integrable per-epoch log return, the Birkhoff time average of the log-wealth growth converges almost surely to the expected log return `∫ ω, log (b · X ω) ∂μ`. |
| [`growingMemory_logWealth_tendsto_condOptGrowthInfty_concrete`](InformationTheory/Shannon/Portfolio/StationaryWinftyConcrete.lean) | Growing-memory `W_∞` AEP (Cover–Thomas) for the concrete two-sided market: the growing-memory log-wealth average converges almost surely to the infinite-past optimal growth rate `W_∞ = condOptGrowthInfty`. |
| [`universal_portfolio_regret_tendsto_zero`](InformationTheory/Shannon/Portfolio/Universal.lean) | (Cover–Thomas): the per-period regret of the universal portfolio relative to the best constant-rebalanced portfolio chosen in hindsight tends to `0`. |

**Ch. 17 — Entropy inequalities**

| Result | Statement |
|--------|-----------|
| [`han_inequality`](InformationTheory/Shannon/Han/Basic.lean) | **Han's inequality**: for a finite family `Xs : Fin n → Ω → α` of random variables, `(n − 1) · H(Xs) ≤ ∑ i, H(Xs except i)`. The degenerate cases `n = 0, 1` (both sides `0`) are included. |
| [`shearer_inequality`](InformationTheory/Shannon/Han/DShearer.lean) | **Shearer's inequality** (integer covering form): if `S : ι → Finset (Fin n)` covers each `i : Fin n` at least `k` times, then `k · H(X_{[n]}) ≤ ∑_j H(X_{S_j})`. |
| [`loomis_whitney`](InformationTheory/Shannon/LoomisWhitney.lean) | The **Loomis–Whitney inequality** (information-theoretic proof). For any non-empty `A : Finset (Fin n → α)`, `A.card ^ (n - 1) ≤ ∏ i : Fin n, (projectionExcept i A).card`. |
| [`brascamp_lieb_finset`](InformationTheory/Shannon/BrascampLieb.lean) | **Brascamp–Lieb inequality** (combinatorial form): if `S : ι → Finset (Fin n)` covers each coordinate at least `k` times, then `\|A\|^k ≤ ∏ i, \|π_{S i}(A)\|` for every nonempty `A : Finset (Fin n → α)`. Loomis–Whitney is the special case `S i := univ.filter (· ≠ i)` with `k = n - 1`. |
| [`entropy_power_inequality_of_density`](InformationTheory/Shannon/EPI/DensityForm.lean) | Entropy power inequality for absolutely continuous distributions with regular densities. All 16 hypotheses are regularity preconditions (measurability, independence, absolute continuity, finite second moment, `IsRegularDensityV2`, normalization, `IsBlachmanConvReady`, finite Fisher information, finite entropy) for `X`, `Y`, and `X + Y`; they do not encode the EPI inequality core. |
| [`minkowskiDeterminantInequality`](InformationTheory/Shannon/MinkowskiDet.lean) | **Minkowski determinant inequality**. For PosDef `A B`, `det(A+B)^(1/n) ≥ det(A)^(1/n) + det(B)^(1/n)`. |
| [`stam_inequality_smoothed_density`](InformationTheory/Shannon/EPI/Stam/Standalone.lean) | **Stam's inequality (density-level headline, Cover–Thomas / Blachman 1965).** For probability densities `pX, pY` and a Gaussian heat kernel `g_t` (`t > 0`), the Gaussian-smoothed densities `fX = pX ∗ g_t`, `fY = pY ∗ g_t` satisfy the inverse (harmonic-mean) Fisher information inequality `1 / J(fX ∗ fY) ≥ 1 / J(fX) + 1 / J(fY)`. |
| [`debruijn_identity_per_time`](InformationTheory/Shannon/FisherInfo/DeBruijnStandalone.lean) | **de Bruijn identity (per-time, density form, Cover–Thomas).** For `X ⊥ Z` with `Z ∼ 𝒩(0, 1)` and an explicit Lebesgue density `pX` of `X` (a probability density with finite second moment), the heat-flow entropy `h(X + √s·Z)` has, at every time `t > 0`, derivative `(1/2)·J(X + √t·Z)` with the V2 Fisher information of the smooth convolution density `pX ∗ g_t`. |
| [`debruijn_identity_integrated`](InformationTheory/Shannon/FisherInfo/DeBruijnStandalone.lean) | **Integrated de Bruijn identity (general a.c. Cover–Thomas).** The integrated form `h(X + √T·Z) − h(X) = ∫₀ᵀ (1/2)·J(X + √t·Z) dt` for a general absolutely-continuous `X`, obtained by applying `debruijnIntegrationIdentity_holds` to the general path-regularity producer `isDeBruijnPathRegular_of_heat_flow`. |
<!-- THEOREMS:END -->

The library root [`InformationTheory.lean`](InformationTheory.lean) imports every module.

## Use it as a dependency

Requires the Lean toolchain pinned in [`lean-toolchain`](lean-toolchain) — `leanprover/lean4:v4.31.0`,
matching Mathlib `v4.31.0`. Add to your `lakefile.toml`:

```toml
[[require]]
name = "InformationTheory"
git = "https://github.com/FujiHaruka/information-theory"
rev = "v1.0.1"
```

Each release carries a prebuilt archive of the library's oleans, and Lake downloads it instead
of compiling the ~130k lines from source. The download applies to tagged revisions only; on any
untagged revision, or if the fetch fails, Lake reports it and falls back to a source build.

Note that a matching toolchain is what makes the prebuilt artifacts usable — a project on a
different Lean or Mathlib version will rebuild from source regardless.

## Development

`lake build` and `lake env lean <file>` need no extra flags. The Mathlib search tool (loogle)
and the API documentation generator (doc-gen4) are deliberately kept out of the published
dependency set, so rebuilding either of them takes `lake -R -Kdev=on update`; see
[`CLAUDE.md`](CLAUDE.md) for the round trip that restores the committed manifest.
