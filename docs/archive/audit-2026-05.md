# 棚卸し報告 — audit-2026-05

ライブラリ `InformationTheory/` (Shannon + Fano 中心) の健全性 / 主定理 / 公理依存 / 教科書対応を
`docs/audit-2026-05-plan.md` の手順で測定した結果。**新規証明は書いていない**。

---

## §1 静的健全性指標

### 1.1 ファイル / 行数

| 計測対象 | 値 |
|---|---|
| `.lean` ファイル数 (`Exam/` 除く) | **41** |
| 総行数 (`Exam/` 除く) | **19,006** |
| 最大ファイル | `Shannon/ChannelCodingAchievability.lean` (1890 行) |

プラン記載値 (19,054 / 42) との小差は audit 開始までの commit による移動 (本体機能は変化なし)。

### 1.2 残存 `sorry` / `admit`

`rg -nw 'sorry|admit' InformationTheory/` の 9 一致を 1 件ずつ目視確認。**全て docstring 内の文章** (
"sorry-driven で並べる" 等の説明) で、**コードに sorry / admit は 0 件**。

| ファイル | 行 | コンテキスト |
|---|---|---|
| `Shannon/ChannelCodingAchievability.lean` | 20-21 | モジュール docstring (skeleton phase の説明) |
| 同 | 234, 1228 | 補助補題 docstring (歴史的経緯) |
| 同 | 1586, 1590 | 旧 `iid_jointEntropyOfChannel_eq` docstring (旧 sorry 経由解の説明) |
| `Shannon/LoomisWhitney.lean` | 10 | モジュール docstring ("sorry なしで証明する") |
| `Shannon/HanD.lean` | 9 | モジュール docstring ("sorry-driven で並べる") |
| `Shannon/MutualInfo.lean` | 15 | モジュール docstring ("sorry-driven で順次充填") |

**`Phase B` カタログ対象ファイルの sorry: 0 件。赤旗なし。**

### 1.3 `axiom` 宣言

`rg -nw 'axiom' InformationTheory/` の 3 一致は **全て `Polymatroid.lean` の docstring** (
"Polymatroid axiom (i)/(ii)/(iii)" — 数学的な polymatroid 公理の言及)。
`^(axiom|noncomputable axiom)\b` grep は 0 件。**カスタム公理宣言なし。**

### 1.4 `unsafe`

0 件。

### 1.5 `noncomputable` 主定理

`noncomputable def/theorem/abbrev` は計 66 件 (`Exam/` 除く)。内訳:

| ファイル | 件数 | コメント |
|---|---|---|
| `Shannon/ChannelCoding.lean` | 7 | code / encoder / decoder / mutualInfoOfChannel 等の定義 |
| `Shannon/AEP.lean` | 7 | jointRV / Hn_RV 等の RV 構成 |
| `Shannon/Stein.lean` | 5 | 仮説検定 / β_α etc. |
| `Shannon/ShannonCodeKraftReverse.lean` | 5 | shannonFanoCode 等 |
| `Shannon/ShannonCode.lean` | 4 | shannonCode 等 |
| `Shannon/ChannelCodingAchievability.lean` | 4 | random code / decoder 等 |
| `Shannon/Sanov.lean` | 3 | typeClass 等 |
| その他 17 ファイル | 1-2 件ずつ | 大半が積分依存の def |

`noncomputable` の出現箇所は **全て def / abbrev** (Real / ENNReal 上の積分式や `Classical.choice`
依存の構成)。**`noncomputable theorem` で意味解釈に注意を要するものは無し** (本ライブラリの
主定理は全て `Prop` を結論する命題、計算性は問題にならない)。

### 1.6 結論

| 健全性指標 | 値 | 判定 |
|---|---|---|
| 残存 sorry / admit (コード) | 0 | ✅ |
| カスタム `axiom` 宣言 | 0 | ✅ |
| `unsafe` 使用 | 0 | ✅ |
| 主定理ファイル赤旗 | なし | ✅ |

Phase A は完全クリーン。Phase B (主定理 statement カタログ) に進む。

---

## §2 主定理カタログ

49 件を 5 カテゴリで列挙。各エントリは **statement verbatim** + 仮定列挙 + 結論形式 (log 底 /
unit / prob 形式 / 形式) + namespace。

### §2.1 Fano 系 (7 件)

#### InformationTheory.fano_core (Fano/Core.lean:379)
```lean
theorem fano_core (hcard : 2 ≤ Fintype.card X) :
    P.condEntropy ≤ Real.qaryEntropy (Fintype.card X) P.errorProb := by
```
- 仮定: `{X : Type*} [Fintype X] [DecidableEq X]`, `(P : FiniteJointPMF X X)`,
  `(hcard : 2 ≤ Fintype.card X)`
- 結論: 単発不等式 `H(X|X̂) ≤ qaryEntropy |X| Pe`; nats / `Real` / PMF 形

#### InformationTheory.fano_inequality (Fano/Core.lean:424)
```lean
theorem fano_inequality (hcard : 2 ≤ Fintype.card X) :
    P.condEntropy ≤ fanoBoundRHSOfAlphabet X P.errorProb := by
```
- 仮定: 同上
- 結論: 単発不等式; nats / `Real` / PMF 形

#### InformationTheory.error_lower_bound (Fano/Core.lean:431)
```lean
theorem error_lower_bound (hcard : 2 ≤ Fintype.card X) {a : ℝ}
    (ha0 : 0 ≤ a) (ha1 : a ≤ 1 - 1 / (Fintype.card X : ℝ))
    (hPe1 : P.errorProb ≤ 1 - 1 / (Fintype.card X : ℝ))
    (haH : Real.qaryEntropy (Fintype.card X) a < P.condEntropy) :
    a < P.errorProb := by
```
- 仮定: 上記 + 4 unfolding ineq
- 結論: 厳密不等式 (Fano 逆向き); nats / `Real`

#### InformationTheory.condEntropy_le_pushforward_condEntropy (Fano/DPI.lean:184)
```lean
theorem condEntropy_le_pushforward_condEntropy
    (P : FiniteJointPMF X Y) (f : Y → X) :
    P.condEntropy ≤ (P.pushforward f).condEntropy := by
```
- 仮定: `{X Y} [Fintype X] [Fintype Y] [DecidableEq X]`
- 結論: DPI 単発不等式; nats / `Real` / PMF 形

#### InformationTheory.fano_inequality_decode (Fano/DPI.lean:355)
```lean
theorem fano_inequality_decode
    (P : FiniteJointPMF X Y) (decode : Y → X)
    (hcard : 2 ≤ Fintype.card X) :
    P.condEntropy ≤ fanoBoundRHSOfAlphabet X (P.pushforward decode).errorProb := by
```
- 仮定: 同上 + `(hcard : 2 ≤ Fintype.card X)`
- 結論: 単発不等式 (Phase 0 form via DPI); nats / `Real`

#### InformationTheory.fano_inequality_decode' (Fano/DPI.lean:365)
```lean
theorem fano_inequality_decode'
    (P : FiniteJointPMF X Y) (decode : Y → X)
    (hcard : 2 ≤ Fintype.card X) :
    P.condEntropy ≤ fanoBoundRHSOfAlphabet X
      (∑ x, ∑ y, if x = decode y then 0 else P.mass x y) := by
```
- 仮定: 同上
- 結論: 単発不等式 (decode' 明示 sum form); nats / `Real`

#### InformationTheory.MeasureFano.fano_inequality_measure_theoretic (Fano/Measure.lean:224)
```lean
theorem fano_inequality_measure_theoretic
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Ω → X) (Yo : Ω → Y) (decoder : Y → X)
    (hXs : Measurable Xs) (hYo : Measurable Yo) (hdec : Measurable decoder)
    (hcard : 2 ≤ Fintype.card X) :
    condEntropy μ Xs Yo ≤
      Real.binEntropy (errorProb μ Xs Yo decoder)
        + errorProb μ Xs Yo decoder * Real.log ((Fintype.card X : ℝ) - 1) := by
```
- 仮定: `{Ω} [MeasurableSpace Ω]`, `{X} [Fintype X] [DecidableEq X] [Nonempty X]`
  `[MeasurableSpace X] [MeasurableSingletonClass X]`, `{Y} [MeasurableSpace Y]`,
  `[IsProbabilityMeasure μ]` + 3 measurable + `hcard`
- 結論: 単発不等式 (measure-theoretic, Y 連続も許容); nats / `Real`

---

### §2.2 Shannon 基本 + 単発不等式 (12 件)

#### InformationTheory.Shannon.mutualInfo_nonneg (Shannon/MutualInfo.lean:42)
```lean
theorem mutualInfo_nonneg (μ : Measure Ω) (Xs : Ω → X) (Yo : Ω → Y) :
    0 ≤ mutualInfo μ Xs Yo := bot_le
```
- 仮定: なし (本体は `bot_le`)
- 結論: `0 ≤ MI` (型から自明) — ⚠️ tautology に近い

#### InformationTheory.Shannon.mutualInfo_comm (Shannon/MutualInfo.lean:93)
```lean
theorem mutualInfo_comm
    (μ : Measure Ω) [IsFiniteMeasure μ] (Xs : Ω → X) (Yo : Ω → Y)
    (hXs : Measurable Xs) (hYo : Measurable Yo) :
    mutualInfo μ Xs Yo = mutualInfo μ Yo Xs := by
```
- 仮定: `[IsFiniteMeasure μ]`, 2 measurable
- 結論: 等式; `ENNReal`

#### InformationTheory.Shannon.mutualInfo_eq_zero_iff_indep (Shannon/MutualInfo.lean:109)
```lean
theorem mutualInfo_eq_zero_iff_indep
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Ω → X) (Yo : Ω → Y)
    (hXs : Measurable Xs) (hYo : Measurable Yo) :
    mutualInfo μ Xs Yo = 0 ↔ IndepFun Xs Yo μ := by
```
- 仮定: `[IsProbabilityMeasure μ]`, 2 measurable
- 結論: iff 等価; `ENNReal`

#### InformationTheory.Shannon.mutualInfo_ne_top (Shannon/MutualInfo.lean:192)
```lean
theorem mutualInfo_ne_top
    [Fintype X] [MeasurableSingletonClass X]
    [Fintype Y] [MeasurableSingletonClass Y]
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Ω → X) (Yo : Ω → Y)
    (hXs : Measurable Xs) (hYo : Measurable Yo) :
    mutualInfo μ Xs Yo ≠ ∞ :=
```
- 仮定: `[Fintype X] [MSC X] [Fintype Y] [MSC Y] [IsProb μ]` + 2 measurable
- 結論: 有限性; `ENNReal`

#### InformationTheory.Shannon.klDiv_map_le (Shannon/DPI.lean:52)
```lean
theorem klDiv_map_le {α β : Type*}
    [MeasurableSpace α] [MeasurableSpace β]
    {f : α → β} (hf : Measurable f)
    (μ ν : Measure α) [IsFiniteMeasure μ] [IsFiniteMeasure ν] :
    klDiv (μ.map f) (ν.map f) ≤ klDiv μ ν := by
```
- 仮定: `[MS α] [MS β]`, `(hf : Measurable f)`, `[IsFiniteMeasure μ] [IsFiniteMeasure ν]`
- 結論: 単発不等式 DPI; `ENNReal`

#### InformationTheory.Shannon.mutualInfo_le_of_postprocess (Shannon/DPI.lean:139)
```lean
theorem mutualInfo_le_of_postprocess
    (μ : Measure Ω) [IsFiniteMeasure μ]
    (Xs : Ω → X) (Yo : Ω → Y) (hXs : Measurable Xs) (hYo : Measurable Yo)
    {f : Y → Z} (hf : Measurable f) :
    mutualInfo μ Xs (f ∘ Yo) ≤ mutualInfo μ Xs Yo := by
```
- 仮定: `[IsFiniteMeasure μ]` + 3 measurable
- 結論: 単発不等式 (postprocess DPI); `ENNReal`

#### InformationTheory.Shannon.mutualInfo_eq_entropy_sub_condEntropy (Shannon/Bridge.lean:588)
```lean
theorem mutualInfo_eq_entropy_sub_condEntropy
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Ω → X) (Yo : Ω → Y)
    (hXs : Measurable Xs) (hYo : Measurable Yo) :
    (mutualInfo μ Xs Yo).toReal
      = entropy μ Xs - InformationTheory.MeasureFano.condEntropy μ Xs Yo := by
```
- 仮定: `[IsProb μ]` + 2 measurable
- 結論: 等式 `MI = H(X) - H(X|Y)`; nats / `Real` (`.toReal`)

#### InformationTheory.Shannon.MaxEntropy.klDiv_uniformOn_univ_toReal_eq (Shannon/MaxEntropy.lean:123)
```lean
theorem klDiv_uniformOn_univ_toReal_eq
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (X : Ω → α) (hX : Measurable X) :
    (klDiv (μ.map X) (uniformOn (Set.univ : Set α))).toReal
      = Real.log (Fintype.card α) - entropy μ X := by
```
- 仮定: `[Fintype α] [MSC α] [Nonempty α]`, `[IsProb μ]`, `(hX : Measurable X)`
- 結論: 等式 (max-entropy bridge); nats / `Real`

#### InformationTheory.Shannon.MaxEntropy.entropy_le_log_card (Shannon/MaxEntropy.lean:229)
```lean
theorem entropy_le_log_card
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (X : Ω → α) (hX : Measurable X) :
    entropy μ X ≤ Real.log (Fintype.card α) := by
```
- 仮定: 同上
- 結論: 単発不等式 `H ≤ log |α|`; nats / `Real`

#### InformationTheory.Shannon.MaxEntropy.entropy_eq_log_card_iff (Shannon/MaxEntropy.lean:241)
```lean
theorem entropy_eq_log_card_iff
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (X : Ω → α) (hX : Measurable X) :
    entropy μ X = Real.log (Fintype.card α)
      ↔ μ.map X = uniformOn (Set.univ : Set α) := by
```
- 仮定: 同上
- 結論: iff 等価 (等号成立条件); nats / `Real`

#### InformationTheory.Shannon.Pinsker.tvNorm_le_sqrt_klDiv (Shannon/Pinsker.lean:118)
```lean
theorem tvNorm_le_sqrt_klDiv
    (P Q : Measure α) [IsProbabilityMeasure P] [IsProbabilityMeasure Q]
    (hPQ : P ≪ Q) :
    tvNorm P Q ≤ Real.sqrt (klDiv P Q).toReal := by
```
- 仮定: `[IsProb P] [IsProb Q]`, `(hPQ : P ≪ Q)`
- 結論: 単発不等式 (weak Pinsker, 定数 1); nats / `Real`

#### InformationTheory.Shannon.PinskerSharp.tvNorm_le_sqrt_klDiv_div_two (Shannon/PinskerSharp.lean:306)
```lean
theorem tvNorm_le_sqrt_klDiv_div_two
    (P Q : Measure α) [IsProbabilityMeasure P] [IsProbabilityMeasure Q]
    (hPQ : P ≪ Q) :
    Pinsker.tvNorm P Q ≤ Real.sqrt ((klDiv P Q).toReal / 2) := by
```
- 仮定: 同上
- 結論: 単発不等式 (sharp Pinsker, 定数 1/√2); nats / `Real`

---

### §2.3 Han / 組合せ (14 件)

#### InformationTheory.Shannon.jointEntropy_chain_rule (Shannon/Han.lean:56)
```lean
theorem jointEntropy_chain_rule
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i)) :
    jointEntropy μ Xs
      = ∑ i : Fin n,
          InformationTheory.MeasureFano.condEntropy μ (Xs i)
            (fun ω (j : Fin i.val) =>
              Xs ⟨j.val, j.isLt.trans i.isLt⟩ ω) := by
```
- 仮定: `[IsProb μ]`, `(hXs : ∀ i, Measurable (Xs i))`
- 結論: n 変数 chain rule 等式; nats / `Real`

#### InformationTheory.Shannon.han_inequality (Shannon/Han.lean:330)
```lean
theorem han_inequality
    {n : ℕ}
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i)) :
    ((n : ℝ) - 1) * jointEntropy μ Xs
      ≤ ∑ i : Fin n, jointEntropyExcept μ Xs i := by
```
- 仮定: 同上
- 結論: Han 主不等式; nats / `Real`

#### InformationTheory.Shannon.jointEntropySubset_univ (Shannon/HanD.lean:55)
```lean
theorem jointEntropySubset_univ
    (μ : Measure Ω) (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i)) :
    jointEntropySubset μ Xs Finset.univ = jointEntropy μ Xs := by
```
- 仮定: `(hXs : ∀ i, Measurable (Xs i))`
- 結論: 等式 (univ = 全変数); nats / `Real`

#### InformationTheory.Shannon.jointEntropySubset_chain_rule (Shannon/HanD.lean:191)
```lean
theorem jointEntropySubset_chain_rule
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (S : Finset (Fin n)) :
    jointEntropySubset μ Xs S
      = ∑ i ∈ S,
          InformationTheory.MeasureFano.condEntropy μ (Xs i)
            (fun ω (j : (S.filter (· < i))) => Xs j.val ω) := by
```
- 仮定: `[IsProb μ]`, `(hXs : ∀ i, ...)`
- 結論: subset 版 chain rule 等式; nats / `Real`

#### InformationTheory.Shannon.condEntropy_subset_anti (Shannon/HanD.lean:262)
```lean
theorem condEntropy_subset_anti
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (i : Fin n) {T₁ T₂ : Finset (Fin n)} (hT : T₁ ⊆ T₂) :
    InformationTheory.MeasureFano.condEntropy μ (Xs i)
        (fun ω (j : T₂) => Xs j.val ω)
      ≤ InformationTheory.MeasureFano.condEntropy μ (Xs i)
          (fun ω (j : T₁) => Xs j.val ω) := by
```
- 仮定: `[IsProb μ]`, `hXs`, `(hT : T₁ ⊆ T₂)`
- 結論: 単発不等式 (conditioning monotonicity); nats / `Real`

#### InformationTheory.Shannon.han_inequality_subset (Shannon/HanD.lean:387)
```lean
theorem han_inequality_subset
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (S : Finset (Fin n)) :
    ((S.card : ℝ) - 1) * jointEntropySubset μ Xs S
      ≤ ∑ i ∈ S, jointEntropySubset μ Xs (S.erase i) := by
```
- 仮定: `[IsProb μ]`, `hXs`
- 結論: 単発不等式 (subset 版 Han); nats / `Real`

#### InformationTheory.Shannon.subset_average_anti (Shannon/HanDAverage.lean:126)
```lean
theorem subset_average_anti
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    {k : ℕ} (hk : 1 ≤ k) (hkn : k + 1 ≤ n) :
    averageSubsetEntropy μ Xs (k+1) ≤ averageSubsetEntropy μ Xs k := by
```
- 仮定: `[IsProb μ]`, `hXs`, `(hk : 1 ≤ k)`, `(hkn : k+1 ≤ n)`
- 結論: 単発不等式 (D-1 average chain step); nats / `Real`

#### InformationTheory.Shannon.subset_average_chain (Shannon/HanDAverage.lean:171)
```lean
theorem subset_average_chain
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    {k₁ k₂ : ℕ} (h1 : 1 ≤ k₁) (h12 : k₁ ≤ k₂) (h2 : k₂ ≤ n) :
    averageSubsetEntropy μ Xs k₂ ≤ averageSubsetEntropy μ Xs k₁ := by
```
- 仮定: `[IsProb μ]`, `hXs`, 3 size constraints
- 結論: 単発不等式 (D-1 transitive chain); nats / `Real`

#### InformationTheory.Shannon.shearer_inequality (Shannon/HanDShearer.lean:41)
```lean
theorem shearer_inequality
    {ι : Type*} [Fintype ι]
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (S : ι → Finset (Fin n))
    {k : ℕ}
    (hk : ∀ i : Fin n,
      k ≤ (Finset.univ.filter (fun j : ι => i ∈ S j)).card) :
    (k : ℝ) * jointEntropy μ Xs
      ≤ ∑ j : ι, jointEntropySubset μ Xs (S j) := by
```
- 仮定: `[Fintype ι]`, `[IsProb μ]`, `hXs`, `(hk : k-cover)`
- 結論: Shearer (k-cover); nats / `Real`

#### InformationTheory.Shannon.loomis_whitney (Shannon/LoomisWhitney.lean:351)
```lean
theorem loomis_whitney
    {n : ℕ} {α : Type*} [Fintype α] [DecidableEq α] [Nonempty α]
    [MeasurableSpace α] [MeasurableSingletonClass α]
    {A : Finset (Fin n → α)} (hA : A.Nonempty) :
    A.card ^ (n - 1) ≤ ∏ i : Fin n, (projectionExcept i A).card := by
```
- 仮定: `[Fintype α] [DecidableEq α] [Nonempty α] [MS α] [MSC α]`, `(hA : A.Nonempty)`
- 結論: 単発不等式 (純組合せ); N/A / `ℕ`

#### InformationTheory.Shannon.brascamp_lieb_finset (Shannon/BrascampLieb.lean:90)
```lean
theorem brascamp_lieb_finset
    {n k : ℕ} {ι : Type*} [Fintype ι]
    {α : Type*} [Fintype α] [DecidableEq α] [Nonempty α]
    [MeasurableSpace α] [MeasurableSingletonClass α]
    {A : Finset (Fin n → α)} (hA : A.Nonempty)
    (S : ι → Finset (Fin n))
    (hk : ∀ j : Fin n,
      k ≤ (Finset.univ.filter (fun i : ι => j ∈ S i)).card) :
    A.card ^ k ≤ ∏ i : ι, (projectionSubset (S i) A).card := by
```
- 仮定: 同 Loomis-Whitney + `[Fintype ι]` + `(hk : k-cover)`
- 結論: 単発不等式 (general cover); N/A / `ℕ`

#### InformationTheory.Shannon.jointEntropySubset_empty (Shannon/Polymatroid.lean:46)
```lean
theorem jointEntropySubset_empty
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) :
    jointEntropySubset μ Xs ∅ = 0 := by
```
- 結論: polymatroid 公理 (i) empty; nats / `Real`

#### InformationTheory.Shannon.jointEntropySubset_mono (Shannon/Polymatroid.lean:73)
```lean
theorem jointEntropySubset_mono
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    {S T : Finset (Fin n)} (h : S ⊆ T) :
    jointEntropySubset μ Xs S ≤ jointEntropySubset μ Xs T := by
```
- 結論: polymatroid 公理 (ii) mono; nats / `Real`

#### InformationTheory.Shannon.jointEntropySubset_submodular (Shannon/Polymatroid.lean:192)
```lean
theorem jointEntropySubset_submodular
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (S T : Finset (Fin n)) :
    jointEntropySubset μ Xs (S ∪ T) + jointEntropySubset μ Xs (S ∩ T)
      ≤ jointEntropySubset μ Xs S + jointEntropySubset μ Xs T := by
```
- 結論: polymatroid 公理 (iii) submod; nats / `Real`

#### InformationTheory.Shannon.edgeBoundary_ge_AMGM (Shannon/HypercubeEdgeBoundary.lean:671)
```lean
theorem edgeBoundary_ge_AMGM {n : ℕ} {A : Finset (Fin n → Bool)} (hA : A.Nonempty) :
    2 * (n : ℝ) * ((A.card : ℝ) ^ (n - 1 : ℕ)) ^ ((n : ℝ)⁻¹)
      ≤ (edgeBoundaryCount A : ℝ) + n * A.card := by
```
- 仮定: `(hA : A.Nonempty)`
- 結論: 単発不等式 (AM-GM 形); N/A / `Real`

#### InformationTheory.Shannon.edgeBoundary_entropy_sharp (Shannon/HypercubeEdgeBoundarySharp.lean:627)
```lean
theorem edgeBoundary_entropy_sharp {n : ℕ} {A : Finset (Fin n → Bool)}
    (hA : A.Nonempty) :
    (A.card : ℝ) * ((n : ℝ) - Real.logb 2 A.card) ≤ (edgeBoundaryCount A : ℝ) := by
```
- 仮定: `(hA : A.Nonempty)`
- 結論: 単発不等式 (entropy-sharp); **log₂** / `Real`

---

### §2.4 AEP / Sanov / Stein (6 件)

#### InformationTheory.Shannon.typicalSet_prob_le (Shannon/AEP.lean:1279)
```lean
theorem typicalSet_prob_le
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : ℕ → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (hindep_full : iIndepFun (fun i => Xs i) μ)
    (hident : ∀ i, IdentDistrib (Xs i) (Xs 0) μ μ)
    (hpos : ∀ x : α, 0 < (μ.map (Xs 0)).real {x})
    (n : ℕ) {ε : ℝ}
    (x : Fin n → α) (hx : x ∈ typicalSet μ Xs n ε) :
    (μ.map (jointRV Xs n)).real {x}
      ≤ Real.exp (- (n : ℝ) * (entropy μ (Xs 0) - ε)) := by
```
- 仮定: `[IsProb μ]`, `hXs`, **`iIndepFun` (※ Pairwise 形でなく iIndep)** + `IdentDistrib`,
  `hpos : 0 < P.real{x}` (full support), `hx : ∈ typicalSet`
- 結論: 単発不等式 (typical 上界 only); nats / `Real`

#### InformationTheory.Shannon.typeClass_Qn_le (Shannon/Sanov.lean:172)
```lean
theorem typeClass_Qn_le
    (P Q : Measure α) [IsProbabilityMeasure P] [IsProbabilityMeasure Q]
    (hPpos : ∀ a : α, 0 < P.real {a})
    (hQpos : ∀ a : α, 0 < Q.real {a})
    (n : ℕ) :
    ((Measure.pi (fun _ : Fin n => Q)) (typeClass P n)).toReal
      ≤ Real.exp (-((n : ℝ) * klDivSumForm P Q)) := by
```
- 仮定: `[IsProb P] [IsProb Q]`, **両者 full support**
- 結論: 単発不等式 (method of types); nats / `Real`

#### InformationTheory.Shannon.sanov_ldp_upper_bound (Shannon/SanovLDP.lean:471)
```lean
theorem sanov_ldp_upper_bound
    (Q : Measure α) [IsProbabilityMeasure Q]
    (hQpos : ∀ a : α, 0 < Q.real {a})
    (E : ∀ n, Finset (TypeCountIndex α n))
    (D : ℝ)
    (hD : ∀ n, ∀ c ∈ E n, D ≤ klDivIndex (fun a => (c a : ℕ)) n Q)
    {ε : ℝ} (hε : 0 < ε) :
    ∃ N, ∀ n ≥ N, 0 < n →
      0 < ((Measure.pi (fun _ : Fin n => Q))
            (⋃ c ∈ E n, typeClassByCount (α := α) (fun a => (c a : ℕ)))).toReal →
      (1 / (n : ℝ)) * Real.log (((Measure.pi (fun _ : Fin n => Q))
        (⋃ c ∈ E n, typeClassByCount (α := α) (fun a => (c a : ℕ)))).toReal)
        ≤ -D + ε := by
```
- 仮定: `[IsProb Q]`, `Q full support`, event seq `E`, minimum rate `D`, ε margin
- 結論: 弱 LDP (eventually `≤ -D + ε`, **conditional on positive prob**); nats / `Real`

#### InformationTheory.Shannon.sanov_ldp_equality (Shannon/SanovLDPEquality.lean:1243)
```lean
theorem sanov_ldp_equality
    (Q : Measure α) [IsProbabilityMeasure Q]
    (hQpos : ∀ a : α, 0 < Q.real {a})
    (P : α → ℝ) (hP_prob : (∑ a, P a) = 1)
    (hP_full : ∀ a, 0 < P a)
    (E : ∀ n, Finset (TypeCountIndex α n))
    (h_in_E : ∀ᶠ n : ℕ in atTop, roundedTypeIndex P n ∈ E n)
    (h_minimizer : ∀ n, ∀ c ∈ E n,
      klDivSumForm_ofVec P (fun a => Q.real {a})
        ≤ klDivIndex (fun a => (c a : ℕ)) n Q) :
    Tendsto
      (fun n : ℕ => (1 / (n : ℝ)) * Real.log
        (((Measure.pi (fun _ : Fin n => Q))
          (⋃ c ∈ E n, typeClassByCount (α := α) (fun a => (c a : ℕ)))).toReal))
      atTop (𝓝 (-(klDivSumForm_ofVec P (fun a => Q.real {a})))) := by
```
- 仮定: 上記 + 入力分布 `P` (full support) + `E n` に `roundedTypeIndex P n` が eventually
  入る + `E n` 全体が `P` を minimizer に持つ
- 結論: Tendsto (強形 strict equality); nats / `Real`

#### InformationTheory.Shannon.stein_lemma (Shannon/Stein.lean:1390)
```lean
theorem stein_lemma
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (P Q : Measure α) [IsProbabilityMeasure P] [IsProbabilityMeasure Q]
    (Xs : ℕ → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (hindep : Pairwise fun i j => Xs i ⟂ᵢ[μ] Xs j)
    (hident : ∀ i, IdentDistrib (Xs i) (Xs 0) μ μ)
    (hMap : μ.map (Xs 0) = P)
    (hMapJoint : ∀ n, μ.map (jointRV Xs n) = Measure.pi (fun _ : Fin n => P))
    (hPpos : ∀ x : α, 0 < P.real {x})
    (hPQ : P ≪ Q) (hQpos : ∀ x : α, 0 < Q.real {x})
    {ε : ℝ} (hε : 0 < ε) (hε1 : ε < 1) :
    (klDiv P Q).toReal
        ≤ Filter.liminf
            (fun n : ℕ => -((1 : ℝ) / n) * Real.log (steinOptimalBeta P Q n ε))
            Filter.atTop
      ∧
    Filter.limsup
        (fun n : ℕ => -((1 : ℝ) / n) * Real.log (steinOptimalBeta P Q n ε))
        Filter.atTop
      ≤ (klDiv P Q).toReal / (1 - ε) := by
```
- 仮定: `Pairwise` 独立 + `IdentDistrib` + `μ.map = P` + `μ.map jointRV = pi P` (**冗長**:
  3 番目から導けるべき所を hypothesis 化) + `P ≪ Q` + 両者 full support + `ε ∈ (0,1)`
- 結論: 弱形 (liminf ≥ D, limsup ≤ D/(1-ε)) — ε 依存に上界が膨らむ; nats / `Real`

#### InformationTheory.Shannon.StrongStein.stein_strong_lemma (Shannon/StrongStein.lean:498)
```lean
theorem stein_strong_lemma
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (P Q : Measure α) [IsProbabilityMeasure P] [IsProbabilityMeasure Q]
    (Xs : ℕ → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (hindep : Pairwise fun i j => Xs i ⟂ᵢ[μ] Xs j)
    (hident : ∀ i, IdentDistrib (Xs i) (Xs 0) μ μ)
    (hMap : μ.map (Xs 0) = P)
    (hMapJoint : ∀ n, μ.map (jointRV Xs n) = Measure.pi (fun _ : Fin n => P))
    (hPpos : ∀ x : α, 0 < P.real {x})
    (hPQ : P ≪ Q) (hQpos : ∀ x : α, 0 < Q.real {x})
    {ε : ℝ} (hε : 0 < ε) (hε1 : ε < 1) :
    Tendsto
      (fun n : ℕ => -((1 : ℝ) / n) * Real.log (steinOptimalBeta P Q n ε))
      atTop
      (𝓝 (klDiv P Q).toReal) := by
```
- 仮定: stein_lemma と同一
- 結論: 強形 (Tendsto → D(P||Q), ε 依存除去); nats / `Real`

---

### §2.5 符号化 + その他 (10 件)

#### InformationTheory.Shannon.ShannonCode.shannonLength_kraft_le_one (Shannon/ShannonCode.lean:129)
```lean
theorem shannonLength_kraft_le_one
    {D : ℝ} (hD : 1 < D) (P : Measure α) [IsProbabilityMeasure P]
    (hP : ∀ a : α, 0 < P.real {a}) :
    kraftSum D (shannonLength D P) ≤ 1 := by
```
- 仮定: `[Fintype α] [MS α] [MSC α]`, `(hD : 1 < D)`, `[IsProb P]`, full support
- 結論: 単発不等式 (Kraft); **D-ary** / `Real`

#### InformationTheory.Shannon.ShannonCode.entropyD_le_expectedLength_of_kraft (Shannon/ShannonCode.lean:164)
```lean
theorem entropyD_le_expectedLength_of_kraft
    {D : ℝ} (hD : 1 < D) (P : Measure α) [IsProbabilityMeasure P]
    (hP : ∀ a : α, 0 < P.real {a})
    (l : α → ℕ) (h_kraft : kraftSum D l ≤ 1) :
    entropyD D P ≤ expectedLength P l := by
```
- 仮定: 同上 + 任意 `l : α → ℕ` with Kraft
- 結論: 単発不等式 (Gibbs); D-ary / `Real`

#### InformationTheory.Shannon.ShannonCode.expectedLength_shannon_lt_entropyD_add_one (Shannon/ShannonCode.lean:261)
```lean
theorem expectedLength_shannon_lt_entropyD_add_one
    {D : ℝ} (hD : 1 < D) (P : Measure α) [IsProbabilityMeasure P]
    (hP : ∀ a : α, 0 < P.real {a}) :
    expectedLength P (shannonLength D P) < entropyD D P + 1 := by
```
- 仮定: 同上
- 結論: 厳密上界; D-ary / `Real`

#### InformationTheory.Shannon.ShannonCode.shannonCode_expected_length_bounds (Shannon/ShannonCode.lean:345)
```lean
theorem shannonCode_expected_length_bounds
    {D : ℝ} (hD : 1 < D) (P : Measure α) [IsProbabilityMeasure P]
    (hP : ∀ a : α, 0 < P.real {a}) :
    entropyD D P ≤ expectedLength P (shannonLength D P) ∧
    expectedLength P (shannonLength D P) < entropyD D P + 1 := by
```
- 仮定: 同上
- 結論: サンドイッチ (`H_D ≤ L < H_D + 1`); D-ary / `Real`

#### InformationTheory.Shannon.ShannonCodeKraftReverse.exists_prefix_code_of_kraft (Shannon/ShannonCodeKraftReverse.lean:482)
```lean
theorem exists_prefix_code_of_kraft
    {D : ℕ} (hD : 2 ≤ D)
    (l : α → ℕ) (hl : ∀ a, 0 < l a)
    (hk : ∑ a : α, ((D : ℝ)) ^ (-(l a : ℤ)) ≤ 1) :
    ∃ c : α → List (Fin D),
      Function.Injective c ∧
      (∀ a, (c a).length = l a) ∧
      IsPrefixFree c := by
```
- 仮定: `[Fintype α] [DecidableEq α]`, `(hD : 2 ≤ D)`, `(hl : 0 < l a)`, Kraft
- 結論: 存在 (prefix-free + injective); D-ary

#### InformationTheory.Shannon.shannon_converse_single_shot (Shannon/Converse.lean:81)
```lean
theorem shannon_converse_single_shot
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Msg : Ω → M) (Yo : Ω → Y) (decoder : Y → M)
    (hMsg : Measurable Msg) (hYo : Measurable Yo) (hdecoder : Measurable decoder)
    (hMsg_uniform :
      μ.map Msg = (Fintype.card M : ℝ≥0∞)⁻¹ • Measure.count)
    (hcard : 2 ≤ Fintype.card M)
    (hMI_finite : mutualInfo μ Msg Yo ≠ ∞) :
    Real.log (Fintype.card M) ≤
      (mutualInfo μ Msg Yo).toReal +
        Real.binEntropy
          (InformationTheory.MeasureFano.errorProb μ Msg Yo decoder) +
        InformationTheory.MeasureFano.errorProb μ Msg Yo decoder *
          Real.log ((Fintype.card M : ℝ) - 1) := by
```
- 仮定: `[Fintype M] [DecidableEq M] [Nonempty M] [MS M] [MSC M]`, `[IsProb μ]`, 3 measurable,
  `(hMsg_uniform : μ.map Msg = uniform)`, `(hcard : 2 ≤ |M|)`, `(hMI_finite : MI ≠ ∞)`
- 結論: 単発不等式 (Fano + MI 形); nats / `Real`

#### InformationTheory.Shannon.slepian_wolf_converse_X / _Y / _sum / _single_shot (Shannon/SlepianWolf.lean:217 / 293 / 361 / 449)
4 件まとめ:
- 3 個別 (X, Y, sum) と 1 連言 (single_shot) — それぞれが marginal/joint error と log M_X / log M_Y /
  和の下界を結ぶ Fano 形不等式。
- 仮定: `[Fintype α], [Fintype β]` 各々 (DecidableEq/Nonempty/MS/MSC) + `[NeZero Mx] [NeZero My]`,
  `[IsProb μ]`, 2 measurable, 該当する `(hcard : 2 ≤ |·|)`。
- 結論: 単発不等式 3 本 (`log Mx ≥ ...`, `log My ≥ ...`, `log Mx + log My ≥ ...`); nats / `Real`

→ verbatim は subagent 出力参照 (本 doc には sample のみ):
```lean
theorem slepian_wolf_converse_X ... :
    Real.log (Mx : ℝ) ≥
      InformationTheory.MeasureFano.condEntropy μ Xs Ys
        - Real.binEntropy (errorProb ...)
        - errorProb ... * Real.log ((Fintype.card α : ℝ) - 1) := by
```

#### InformationTheory.Shannon.ChannelCoding.channel_coding_achievability (Shannon/ChannelCodingAchievability.lean:1605)
```lean
theorem channel_coding_achievability
    (W : Channel α β) [IsMarkovKernel W]
    (p : Measure α) [IsProbabilityMeasure p]
    (hp_pos : ∀ a : α, 0 < p.real {a})
    (hW_pos : ∀ a : α, ∀ b : β, 0 < (W a).real {b})
    {R : ℝ} (hR_pos : 0 < R) (hR : R < (mutualInfoOfChannel p W).toReal)
    {ε' : ℝ} (hε' : 0 < ε') :
    ∃ N : ℕ, ∀ n, N ≤ n →
      ∃ (M : ℕ) (_hM_lb : Nat.ceil (Real.exp ((n : ℝ) * R)) ≤ M)
        (c : Code M n α β),
        (c.averageErrorProb W).toReal < ε' := by
```
- 仮定: `[Fintype α] [DecidableEq α] [Nonempty α] [MS α] [MSC α]` (同様 β),
  `[IsMarkovKernel W]`, `[IsProb p]`, **両者 full support** (`hp_pos`, `hW_pos`),
  `R > 0`, `R < I(p;W)`, `ε' > 0`
- 結論: `∃ N, ∀ n ≥ N, ∃ M (≥ ⌈exp(nR)⌉), ∃ code, c.averageErrorProb W < ε'`
  → **average error**, rate `(log M)/n ≥ R`、入力分布 `p` は **固定** (max なし)
  nats / `Real` (via `Real.exp`)

#### InformationTheory.Shannon.mutualInfo_chain_rule_fin (Shannon/MIChainRule.lean:117)
```lean
theorem mutualInfo_chain_rule_fin
    {n : ℕ}
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    [StandardBorelSpace Y] [Nonempty Y]
    (Xs : Fin n → Ω → α) (hXs : ∀ i, Measurable (Xs i))
    (Yo : Ω → Y) (hYo : Measurable Yo) :
    mutualInfo μ (fun ω i => Xs i ω) Yo
      = ∑ i : Fin n,
          condMutualInfo μ (Xs i) Yo
            (fun ω (j : Fin i.val) => Xs ⟨j.val, j.isLt.trans i.isLt⟩ ω) := by
```
- 仮定: `[Fintype α] [MS α] [MSC α] [Nonempty α]`, `[IsProb μ]`,
  **`[StandardBorelSpace Y] [Nonempty Y]`**, 2 measurable
- 結論: chain rule 等式; nats / `ENNReal`

#### InformationTheory.Shannon.mutualInfo_iid_eq_nsmul (Shannon/MIChainRule.lean:392)
```lean
theorem mutualInfo_iid_eq_nsmul
    {n : ℕ} (hn : 0 < n)
    (μ : Measure Ω) [IsProbabilityMeasure μ]
    (Xs : Fin n → Ω → α) (Ys : Fin n → Ω → β)
    (hXs : ∀ i, Measurable (Xs i)) (hYs : ∀ i, Measurable (Ys i))
    (h_iid_joint : μ.map (fun ω (i : Fin n) => (Xs i ω, Ys i ω))
                      = Measure.pi (fun i => μ.map (fun ω => (Xs i ω, Ys i ω))))
    (h_iid_X : μ.map (fun ω (i : Fin n) => Xs i ω)
                  = Measure.pi (fun i => μ.map (Xs i)))
    (h_iid_Y : μ.map (fun ω (i : Fin n) => Ys i ω)
                  = Measure.pi (fun i => μ.map (Ys i)))
    (h_copy : ∀ i, μ.map (fun ω => (Xs i ω, Ys i ω))
                      = μ.map (fun ω => (Xs ⟨0, hn⟩ ω, Ys ⟨0, hn⟩ ω)))
    (h_copy_X : ∀ i, μ.map (Xs i) = μ.map (Xs ⟨0, hn⟩))
    (h_copy_Y : ∀ i, μ.map (Ys i) = μ.map (Ys ⟨0, hn⟩)) :
    mutualInfo μ (fun ω i => Xs i ω) (fun ω i => Ys i ω)
      = n • mutualInfo μ (Xs ⟨0, hn⟩) (Ys ⟨0, hn⟩) := by
```
- 仮定: `(hn : 0 < n)`, `[IsProb μ]`, 2 measurable, **6 個の生 hypothesis** (`Measure.pi` 形 +
  IdentDistrib 形 を joint / X / Y それぞれに分解)
- 結論: `MI(X^n; Y^n) = n • MI(X₀; Y₀)`; nats / `ENNReal`

注: 主定理ファイルの **ChannelCoding.lean (706 行)** は定義 + 基本 `@[simp]` lemma のみで、
独立の主定理 statement なし (`Code`, `errorProb`, `mutualInfoOfChannel` 等の definitions が
中核)。

---

## §3 公理依存表

### 手順

`InformationTheory/Audit/PrintAxioms.lean` を作成し、§2 の主定理 49 件に対して `#print axioms` を
発行。出力を 4 区分に分類:

| 区分 | 意味 | カウント |
|---|---|---|
| **clean** | `propext`, `Classical.choice`, `Quot.sound` のみ | **49 / 49** |
| **with `sorryAx`** | 🚨 主定理が sorry 経由 | 0 |
| **with custom axiom** | 🚨 カスタム `axiom` 宣言経由 | 0 |
| **suspicious** | `Lean.ofReduceBool` 等、解釈に注意を要するもの | 0 |

### 結果

**全 49 件 clean**。出力 (`/tmp/audit-axioms.txt`):

```
'InformationTheory.FiniteJointPMF.fano_core' depends on axioms: [propext, Classical.choice, Quot.sound]
'InformationTheory.FiniteJointPMF.fano_inequality' depends on axioms: [propext, Classical.choice, Quot.sound]
...
'InformationTheory.Shannon.mutualInfo_iid_eq_nsmul' depends on axioms: [propext, Classical.choice, Quot.sound]
```

49 件すべてが Lean 4 標準 3 公理のみに依存。

### 注意点

- 名前空間ネスト確認は手動: 例 `InformationTheory.FiniteJointPMF.fano_core` (Fano/Core.lean の
  `namespace InformationTheory` + `namespace FiniteJointPMF` ネスト)。subagent の 1 次抽出では
  `InformationTheory.fano_core` 等のフラット名を出してきた箇所があったが、`#print axioms` が
  "Unknown constant" を出して即座に補正できた。
- 初回 run で 19 件 "Unknown constant" を観測 → `lake build InformationTheory` で olean 再構築後に
  完全成功。CLAUDE.md 記載の "olean stale" 落とし穴をなぞった形。
- 完成済 (Phase A の sorry 0 件) と合わせて、**主定理層は健全性の最低ラインを完全クリア**。

### 引き継ぎ

- `InformationTheory/Audit/PrintAxioms.lean` は Phase E 時点で削除 (Definition of Done の判断:
  全件 clean のため audit 終了後の保持価値なし、ただし再現性のため git 履歴には残る)。

---

## §4 教科書対応 + 仮定監査

### 判定基準

| 判定 | 意味 |
|---|---|
| 🟢 **standard** | 教科書定理とほぼ同形 (微小な technical 仮定差のみ) |
| 🟡 **specialized** | 仮定が強い / 結論が弱いが、explicit scope 宣言で正当化されている |
| 🔴 **weakened** | 名前が示唆する強さに statement が届いていない / 仮定が本質的に過剰 |

### 一覧表

| カテゴリ | 主定理 | log 底 | unit | 仮定の特徴 | CT 対応 | 判定 |
|---|---|---|---|---|---|---|
| Fano | `FiniteJointPMF.fano_core` | nats | nats | `[Fintype X] [DEq X]`, hcard | CT 2.10.1 (同形) | 🟢 |
| Fano | `FiniteJointPMF.fano_inequality` | nats | nats | 同上 | CT 2.10.1 | 🟢 |
| Fano | `FiniteJointPMF.error_lower_bound` | nats | nats | 同上 + Pe ≤ 1-1/q | CT 2.10.1 逆向き | 🟢 |
| Fano | `FiniteJointPMF.condEntropy_le_pushforward_condEntropy` | nats | nats | `[Fintype X] [Fintype Y] [DEq X]` | CT 2.8.1 DPI | 🟢 |
| Fano | `FiniteJointPMF.fano_inequality_decode` | nats | nats | + decoder | CT 2.10.1 (decoder 形) | 🟢 |
| Fano | `FiniteJointPMF.fano_inequality_decode'` | nats | nats | 同上 (sum 形) | CT 2.10.1 同 | 🟢 |
| Fano | `MeasureFano.fano_inequality_measure_theoretic` | nats | nats | 測度形 (Y 連続 OK) | CT 2.10.1 (measure 拡張) | 🟢 |
| Shannon | `Shannon.mutualInfo_nonneg` | — | — | (`bot_le`, 型から自明) | CT 2.4.0 | 🟢¹ |
| Shannon | `Shannon.mutualInfo_comm` | — | — | `[IsFiniteMeasure μ]` | CT 2.4.2 | 🟢 |
| Shannon | `Shannon.mutualInfo_eq_zero_iff_indep` | — | — | `[IsProb μ]` | CT 2.6.6 | 🟢 |
| Shannon | `Shannon.mutualInfo_ne_top` | — | — | `[Fintype X][Fintype Y]` | (technical) | 🟢 |
| Shannon | `Shannon.klDiv_map_le` | — | — | `[IsFiniteMeasure μ] [IsFiniteMeasure ν]` | CT 2.8.1 DPI for KL | 🟢 |
| Shannon | `Shannon.mutualInfo_le_of_postprocess` | — | — | `[IsFiniteMeasure μ]` | CT 2.8.1 | 🟢 |
| Shannon | `Shannon.mutualInfo_eq_entropy_sub_condEntropy` | nats | nats | `[IsProb μ]` (`.toReal`) | CT 2.4.0 定義同形 | 🟢 |
| MaxEnt | `Shannon.MaxEntropy.klDiv_uniformOn_univ_toReal_eq` | nats | nats | `[Fintype α][MSC][Nonempty]` | CT 2.6.0 (bridge) | 🟢 |
| MaxEnt | `Shannon.MaxEntropy.entropy_le_log_card` | nats | nats | 同上 | CT 2.6.4 | 🟢 |
| MaxEnt | `Shannon.MaxEntropy.entropy_eq_log_card_iff` | nats | nats | 同上 | CT 2.6.4 等号 | 🟢 |
| Pinsker | `Shannon.Pinsker.tvNorm_le_sqrt_klDiv` | nats | nats | `[IsProb P][IsProb Q]`, `P ≪ Q` | CT 11.6.1 弱形 | 🟡² |
| Pinsker | `Shannon.PinskerSharp.tvNorm_le_sqrt_klDiv_div_two` | nats | nats | 同上 | CT 11.6.1 sharp | 🟢 |
| Han | `Shannon.jointEntropy_chain_rule` | nats | nats | `[IsProb μ]` | CT 2.5.1 | 🟢 |
| Han | `Shannon.han_inequality` | nats | nats | `[IsProb μ]` | CT 17.6.1 (Han) | 🟢 |
| Han | `Shannon.jointEntropySubset_univ` | nats | nats | (technical) | (helper) | 🟢 |
| Han | `Shannon.jointEntropySubset_chain_rule` | nats | nats | `[IsProb μ]` | CT 2.5.1 (subset) | 🟢 |
| Han | `Shannon.condEntropy_subset_anti` | nats | nats | `[IsProb μ]` | CT 2.6.5 | 🟢 |
| Han | `Shannon.han_inequality_subset` | nats | nats | `[IsProb μ]` | Han 一般化 | 🟢 |
| HanD | `Shannon.subset_average_anti` | nats | nats | `[IsProb μ]`, k size | CT 17.6 (avg form) | 🟢 |
| HanD | `Shannon.subset_average_chain` | nats | nats | 同上 | CT 17.6 | 🟢 |
| Shearer | `Shannon.shearer_inequality` | nats | nats | k-cover hypothesis | Shearer 1986 | 🟢 |
| LW | `Shannon.loomis_whitney` | — | ℕ | `[Fintype α] ...` | Loomis-Whitney 1949 | 🟢 |
| BL | `Shannon.brascamp_lieb_finset` | — | ℕ | + `[Fintype ι]`, k-cover | (combinatorial BL) | 🟡³ |
| Poly | `Shannon.jointEntropySubset_empty` | nats | nats | `[IsProb μ]` | polymatroid (i) | 🟢 |
| Poly | `Shannon.jointEntropySubset_mono` | nats | nats | `[IsProb μ]` | polymatroid (ii) | 🟢 |
| Poly | `Shannon.jointEntropySubset_submodular` | nats | nats | `[IsProb μ]` | polymatroid (iii) | 🟢 |
| HC | `Shannon.edgeBoundary_ge_AMGM` | — | Real | AM-GM 形 | (combinatorial) | 🟢 |
| HC | `Shannon.edgeBoundary_entropy_sharp` | **log₂** | Real | Han 経由 sharp | (combinatorial sharp) | 🟢 |
| AEP | `Shannon.typicalSet_prob_le` | nats | Real | `iIndepFun + IdentDistrib`, full支持 | CT 3.1.2 上界のみ | 🟡⁴ |
| Sanov | `Shannon.typeClass_Qn_le` | nats | Real | 両者 full支持 | CT 11.1.4 | 🟢 |
| Sanov | `Shannon.sanov_ldp_upper_bound` | nats | Real | 条件付 (0 < prob) | CT 11.4 LDP 弱形 | 🟡⁵ |
| Sanov | `Shannon.sanov_ldp_equality` | nats | Real | minimizer + roundedType | CT 11.4 強形 (専用) | 🟡⁶ |
| Stein | `Shannon.stein_lemma` | nats | Real | `Pairwise + IdentDistrib + Map`, full | CT 11.8.3 弱 (ε-loss) | 🟡⁷ |
| Stein | `Shannon.StrongStein.stein_strong_lemma` | nats | Real | 同上 | CT 11.8.3 強 | 🟢 |
| Code | `Shannon.ShannonCode.shannonLength_kraft_le_one` | D-ary | D-ary | `[Fintype α]`, 1 < D, full | CT 5.4 | 🟢 |
| Code | `Shannon.ShannonCode.entropyD_le_expectedLength_of_kraft` | D-ary | D-ary | 同上 + kraft ≤ 1 | CT 5.3.1 | 🟢 |
| Code | `Shannon.ShannonCode.expectedLength_shannon_lt_entropyD_add_one` | D-ary | D-ary | 同上 | CT 5.4.1 上界 | 🟢 |
| Code | `Shannon.ShannonCode.shannonCode_expected_length_bounds` | D-ary | D-ary | 同上 | CT 5.4.1 サンドイッチ | 🟢 |
| Code | `Shannon.ShannonCodeKraftReverse.exists_prefix_code_of_kraft` | D-ary | — | `[Fintype α][DEq α]`, D ≥ 2 | CT 5.2.1 | 🟢 |
| Conv | `Shannon.shannon_converse_single_shot` | nats | Real | uniform 入力固定 | CT 7.9 (uniform 形) | 🟡⁸ |
| SW | `Shannon.slepian_wolf_converse_X` | nats | Real | (technical, X marginal) | CT 15.4.1 | 🟢 |
| SW | `Shannon.slepian_wolf_converse_Y` | nats | Real | (technical, Y marginal) | CT 15.4.1 | 🟢 |
| SW | `Shannon.slepian_wolf_converse_sum` | nats | Real | (technical, joint) | CT 15.4.1 | 🟢 |
| SW | `Shannon.slepian_wolf_converse_single_shot` | nats | Real | (3 連言) | CT 15.4.1 | 🟢 |
| CC | `Shannon.ChannelCoding.channel_coding_achievability` | nats | Real | p / W 両者 full, average 誤り, **入力 p 固定** | CT 7.7.1 achievability part (intermediate) | 🟡⁹ |
| MIC | `Shannon.mutualInfo_chain_rule_fin` | nats | ENNReal | `[StandardBorelSpace Y]` | CT 2.5.2 | 🟢 |
| MIC | `Shannon.mutualInfo_iid_eq_nsmul` | nats | ENNReal | 6 個の生 `Measure.pi` + IdentDistrib hyp | CT 8.2 (i.i.d. 系) | 🟢¹⁰ |

**集計**: 🟢 **40 件** (81.6%), 🟡 **9 件** (18.4%), 🔴 **0 件**。

### 🟡 判定の主定理 (9 件) — scope 注記

¹ **`mutualInfo_nonneg`**: 本体は `bot_le`、`ENNReal` の type 自明性に乗っている。
  「MI が真に 0 以上である」content は型から自動。これは 🟢 とした (型適合のための boilerplate)。

² **`tvNorm_le_sqrt_klDiv` (Pinsker 弱形)**: TV 距離 `tvNorm = (1/2)‖P-Q‖₁`、係数 `1`。
  - 教科書標準 (CT 11.6.1): `‖P-Q‖_TV ≤ √(D(P||Q)/2)`、係数 `1/√2` (sharp).
  - 本ライブラリは弱形 (係数 1) + 強形 (PinskerSharp.lean、係数 1/√2) を併存。
  - **scope 宣言**: 別ファイル `PinskerSharp.lean` で sharp 形が完成しているため、本弱形は
    stepping stone として正当化されている。

³ **`brascamp_lieb_finset`**: finset / k-cover の組合せ形のみ。連続版 (Lieb-Thirring,
  Gaussian Brascamp-Lieb) は scope 外。Loomis-Whitney 上位互換として位置付け済み。

⁴ **`typicalSet_prob_le`**: `Q^n(T) ≤ exp(-n(H-ε))` 上界のみ。
  - 教科書標準 (CT 3.1.2): 上界 + 下界 + `μ(T) → 1` の 3 点セット。
  - 本ライブラリは上界のみ。下界と確率収束は提供されていない (要 follow-up)。
  - 仮定 `full support` は CT 3.1.2 不要 → 過剰仮定。
  - **要 follow-up**: `typicalSet` 完全形を別エントリで埋めるか、scope 宣言。

⁵ **`sanov_ldp_upper_bound`**: 条件付き定式化 (`0 < prob → bound`)。
  - 教科書 (CT 11.4): 無条件で `(1/n) log P^n(E) → -inf_E D(·||Q)` の上界。
  - 本形は退化ケース (prob = 0) を vacuous に扱う。**scope: 退化除外**。

⁶ **`sanov_ldp_equality`**: 単一 minimizer P + `roundedTypeIndex P n ∈ E n` 仮定付。
  - 教科書 (CT 11.4 強形): 一般の closed set E に対し `(1/n) log P^n(E) → -inf_{P*∈E} D(P*||Q)`.
  - 本形は E の minimizer P が事前指定された **single-mode** Sanov に scope を絞っている。
  - 一般 closed-set Sanov LDP は別エントリ (未着手)。

⁷ **`stein_lemma`**: `liminf ≥ D ∧ limsup ≤ D/(1-ε)`、ε 依存上界。
  - 教科書 (CT 11.8.3): ε によらず `lim = D(P||Q)` (我々の `stein_strong_lemma` がこれ)。
  - 弱形は強形への stepping stone として併存している。**scope: 弱形 + 強形のペア構造**。
  - 仮定 `hMapJoint : ∀ n, μ.map (jointRV ...) = Measure.pi (fun _ => P)` は冗長 (`hindep + hident` から
    導けるはず)。**仮定圧縮の余地あり**。

⁸ **`shannon_converse_single_shot`**: `hMsg_uniform : μ.map Msg = (1/|M|) • count`、
  入力 Msg が一様分布限定。教科書 (CT 7.9): 任意の入力分布で converse 成立。
  - **scope: uniform input only**。
  - random coding analysis の典型形に近いが、一般入力に拡張するのが筋。

⁹ **`channel_coding_achievability`** ⭐ **最重要 🟡**:
  - 仮定: `hp_pos : ∀ a, 0 < p.real {a}`, `hW_pos : ∀ a b, 0 < (W a).real {b}` — 両者 full support。
  - 結論: average error。
  - 入力分布 `p` は **固定**。`R < I(p;W)` のみ要求 (チャネル容量 `C := sup_p I(p;W)` ではない)。
  - 教科書 (CT 7.7.1 Shannon noisy channel coding):
    - `R < C` (capacity!) → ∃ block code with **max error** → 0.
    - 本ライブラリは intermediate step (固定 p, average error)。
  - **scope: achievability "half" (CT docstring 内で明示済み)**。capacity 到達 + max error 化
    (expurgation) は未着手。
  - 仮定 `full support` は random coding に必要 (typical set 議論で 0 確率 atom を排除) が、
    CT 7.7.1 本体は full support 不要。**仮定圧縮の余地あり**。

¹⁰ **`mutualInfo_iid_eq_nsmul`**: 6 個の生 `Measure.pi` + `IdentDistrib` 形 hypothesis を取る。
  内容は CT 8.2 系の純標準だが、**i.i.d. 構造を 1 件の builder で済ませる API が無い**点で
  仮定が見苦しい (`Pairwise + IdentDistrib + Map` 形に揃えるべきか)。技術的問題で 🟢 に留めた。

### 🟡 共通テーマ

- **full support 仮定の過剰使用**: AEP / Sanov / Stein / ChannelCoding 系で `0 < P.real {a}` が
  仮定に含まれるケースが多い。これらは Cover-Thomas 本体では不要。`klDiv = ∞` の場合に
  bound が自明成立する形で処理できれば外せる可能性がある。
- **stepping stone と final form のペア構造**: `Pinsker / PinskerSharp`, `Stein / StrongStein`,
  `SanovLDP / SanovLDPEquality`, `ChannelCoding achievability vs Shannon noisy channel coding`
  の 4 ペアで「弱形が先・強形が後」の階段になっている。最も上の段が教科書定理に対応。
- **入力分布固定 vs capacity**: ChannelCoding と Shannon converse 共に「与えられた p に対する
  achievability/converse」のレベル。`max_p I(p;W)` を取って capacity 形に上げる operation は
  未提供 (next moonshot 候補)。

### 🔴 判定の主定理

**なし** (0 件)。49 件全ての statement について、name と claim の致命的乖離は検出されなかった。

---

## §5 結論と次フェーズ判定

### §5.1 数値サマリ

- **静的健全性 (§1)**: コードに `sorry` / `admit` / カスタム `axiom` / `unsafe` の使用なし。
  41 ファイル / 19,006 行 (Exam/ 除く)。
- **主定理カタログ (§2)**: 49 件抽出 (Fano 7 / Shannon 基本 12 / Han 16 / AEP+Sanov+Stein 6 /
  符号化 + MIChainRule 10)。
- **公理依存 (§3)**: 49/49 件すべて `[propext, Classical.choice, Quot.sound]` のみに依存。
  `sorryAx` / カスタム axiom / suspicious axiom: **0 件**。
- **教科書対応 (§4)**: 🟢 **40 件 (81.6%)** / 🟡 9 件 (18.4%) / 🔴 **0 件**。
  🟡 は scope 明示 (弱形 / 中間 form / single-input 等) で正当化済み。

### §5.2 分岐判定

事前判定基準:

| 条件 | 該当 |
|---|---|
| §3 で 🚨 が 1 件以上 **かつ** §4 で 🔴 が 5 件以上 → 分岐 C | ❌ (§3 🚨 = 0, §4 🔴 = 0) |
| §3 はクリーン **かつ** §4 で 🟢 が 70% 以上 → 分岐 A | ✅ (§3 全件 clean, §4 🟢 81.6%) |
| 上記いずれにも該当しない → 分岐 B | — |

→ **分岐 A (再利用テスト)** に進む。

### §5.3 判定根拠

- 静的健全性 + 公理依存の両方が完全クリーンで、Cover-Thomas 比較でも 🔴 が一件も無い。
  本ライブラリは「Lean が通った」と「教科書定理を形式化した」のギャップがある状態ではなく、
  既に**主定理層は健全に教科書定理に対応**している。
- 残る 9 件の 🟡 は、いずれも (a) 弱形 / 強形ペアの弱形 (Pinsker, Stein, Sanov の各々で強形は
  別エントリ済み)、(b) intermediate form (ChannelCoding achievability 固定 p / average 形)、
  (c) 過剰 full support 仮定、のいずれかであり、**現状の API が機能不全である兆候ではない**。
- このため、次フェーズの優先度は (B 修復) ではなく (A 再利用) — 既存 API が想定外の組合せ
  ストレス下でも機能するかを試すことで、本ライブラリの実用性能を計測する。

### §5.4 次フェーズ plan

`docs/reuse-test-plan.md` を新規作成 (本コミット内)。

候補 3 案 (plan A 内で 1 つを選択):

| # | ターゲット | 主要利用 API | 難易度 |
|---|---|---|---|
| 1 | Joint source-channel separation theorem | ShannonCode + ChannelCoding | 高 |
| 2 | Rate-distortion lower bound の入口 (R(D) ≥ I(X; X̂)) | MutualInfo + DPI + MaxEntropy | 中 |
| 3 | Channel coding converse (n-variable Fano scaled) | `shannon_converse_single_shot` + `mutualInfo_chain_rule_fin` + `mutualInfo_iid_eq_nsmul` | 中 |

推奨は **#3 (channel coding converse n-variable)**:
- 既存の 4 つの API (converse single-shot, MI chain rule, MI i.i.d., Fano measure 形) を
  直接組み合わせる純粋テスト。
- 🟡 `shannon_converse_single_shot` の "uniform input" scope が n-channel 用途で本質的か否か
  を検証可能 — 検証結果は次々フェーズの statement 修復 (分岐 B) に直接フィードバック。
- 期待される追加 bridge: **0 本** (純既存 API のみで書けるなら ✅)。

詳細は `docs/reuse-test-plan.md` 参照。

### §5.5 Definition of Done チェック

- [x] `docs/audit-2026-05.md` の §1〜§5 が全て埋まっている
- [x] §5 で分岐 (A / B / C) が決定し、次フェーズ plan ファイルが新規作成されている
- [x] Phase C の `InformationTheory/Audit/PrintAxioms.lean` の保持/削除を判断 → **削除** (clean のため、
  ただし git 履歴には残る)
- [x] 主要 commit がすべて push 済み (各 Phase 完了で commit、push は user 操作)
