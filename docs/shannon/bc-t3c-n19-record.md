# N19 — T3c relay の記録 (proof-log + metrics + 収穫の書き上げ + 正直な棚卸し)

**Parent**: [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §5 の枠表 N19 行 / §5.1 (leg 冒頭宣言の規約) /
**撤退ラインの SoT** = 同 plan §6-3 (⚠ **本 md は条文を複製しない**) /
**配分の SoT** = 同 plan §7 判断ログ 13 (⚠ **カウンタの実測値も本 md は複製しない**) /
**雛形** = 前 relay の記録 leg [`../proof-logs/proof-log-bc-open-problem-t3b-relay.md`](../proof-logs/proof-log-bc-open-problem-t3b-relay.md)
(とくに §3 = 見立ての較正 (母集団の定義つき) / §6 = 運用上の障害 / §9 = ツール開発への示唆 / §10 = 採らなかった書き方) /
**判定の SoT** = facts [`bc-facts.md`](bc-facts.md) (⚠⚠ **本 md も proof-log も判定を持たない** — 判定を複製すると真実源が 2 つになる)

**leg 冒頭宣言 (N19)**: 側 = 記録 / 動かすもの = 本 plan の 20 leg (N0–N19) が §0 のゴールに対して何を動かし何を動かさなかったかを機械の裏取りつきで確定させ、§6-3 の発火 (**未達と書く**) を成果物の側で履行して、次の判断をユーザーに返せる状態にする

> ⚠⚠ **本書の §0 と §1 は着手前に凍結されており、実行 leg / 監査 / 着地は 1 文字も書き換えてはならない**
> (親 plan §4.4-2 の義務。r19 / r20 / r21 / r22 で確立した作法 = **事後に見立てを直して当てにいく経路を
> 構造的に塞ぐ**)。⚠ **見立ての当たり外れ / 反証条件の発火状況は §2 以降に別途書く** — **§0 / §1 は動かさず、
> 効きは着地側に書く**。
> **凍結の機械確認**: 起票 commit と現行の同区間の md5 一致で見る。⚠⚠ **範囲の非空 (行数) を先に確認してから
> md5 を取る** — **範囲指定を誤ると空文字列どうしの偽 PASS になる** (r20 / N17 監査の実測)。
> ⚠ **起票時点では `## 2.` が存在しない**ので、起票側の基準値は `awk '/^## 0\./,0'` (= §0 以降すべて) で取った。
> **§2 以降が書かれた後は `awk '/^## 0\./,/^## 2\./' | sed '$d'` に切り替える** ⟹ ⚠⚠ **切り替えの前後で
> 行数と md5 は必ず変わる**。**比較すべきは「切り替え後の区間と、起票 commit の同じ切り出しを当てたもの」**である。
>
> ⚠⚠ **本 leg で §6-3 が発火する** — **20 leg (N0–N19) を使い切って §0 未達ゆえ「未達」と書く**。
> ⚠⚠ **発火とは「未達と書く」ことであって、達成条件を緩めることではない** (親 §0.1-1)。
> ⚠⚠ **中間結果は記録するがゴールの代替にしない** (親 §0.1-2)。⚠ **同時に「尽きた」とも書かない**。
> ⚠ **relay の cap と plan の leg 予算は別勘定である** (親 §7 判断ログ 12-(1)) ⟹ **混ぜて書かない**。
> ⚠ **側 = `記録` ゆえ §6-4 の配分カウンタは分子・分母のどちらも動かない** (実測の SoT = 親 §7 判断ログ 13)。

---

## 0. 見立て (⚠ 着手前に凍結。実行 leg / 監査 / 着地は 1 文字も書き換えない)

⚠ **本節は proof-log を 1 行も書く前に書いた**。⚠⚠ **本節は `lake` も `scripts/session_metrics.ts` も
1 度も走らせておらず、下記の 1 行反証も 1 本も実行していない** — 反証は **実行 leg の義務**である
(⟹ **機械の判定は §2 以降に別途書く。一致しなかったものは一致しなかったと書く**)。
⚠ **見立ては当てにいくものではなく較正の材料である**。⚠⚠ **本節に壁宣言は 1 つも無い**。

### 見立て A (**締める**) — 較正は「前 relay の全件命題が再現したか」の形では書けない ⟹ 母集団を 2 欄に割る義務が立つ

理由 3 点:

1. **母集団の作り方が前 relay と違う**。前 relay の母集団 B は facts の各節から「義務を締める方向の見立て」を
   **事後に**拾ったもの (定義と抽出の SoT = 前 relay proof-log §3 冒頭 + §3.1)。本 relay の r19–r22
   (N15 / N16 / N17 / N18) は §4.4-2 の義務で **起票 md の §0 に向きタグつきで着手前に凍結**され、しかも
   §4.4-1 が締める側に **着手前の 1 行反証**を課している ⟹ ⚠⚠ **その義務そのものが交絡である**
   (親 plan §5.1 の N12 着地ブロックが既に名指し。⚠ **逐語はここへ複製しない**)。
2. **relay 内でも見立ての形式が一様でない**。起票 md を持たない leg (親 §5.1 の N12 着地ブロックが
   「**N8 には反証条件も見立ても書かれていなかった**」と名指し) と、6 本立ての leg (N10) と、
   4 本立ての凍結型 (N15–N18) が混在する ⟹ **relay 全体で 1 つの比率を出すと、分母が形式の違いを吸収する**。
3. **前 relay §10 が名指しで禁じた形にちょうど当たる** — 「母集団を書かずに数だけ書く」。
   ⚠ **前 relay は母集団が 2 種類だったが、本 relay は leg ごとに違う公算がある**。

⟹ **A が主張するのは**: 記録は較正欄を **(i) 凍結型 4 leg / (ii) それ以外**の 2 欄に割り、
**各欄に母集団の定義と抽出コマンドを併記する義務**を負う。⚠⚠ **relay 全体の単一の比率を書いてはならない**。

**実行 leg が着手前に機械へ掛ける 1 行の反証 (§4.4-1 の義務。⚠ 起票では実行していない)**:

```bash
rg -n '^#+ *見立て' docs/shannon/bc-t3c-*.md            # 見立てを持つ leg 成果物とその本数の分布
rg -no '\*\*(締める|緩める)\*\*' docs/shannon/bc-t3c-*.md  # 向きタグの付与状況
```

⟹ ⭐ **t3c の成果物で見立てが同一形式 (本数が揃い、全件に向きタグが付く) に揃っていれば、母集団は 1 つでよく
A は死ぬ**。⚠ **A は締める側ゆえ §4.4 の非対称性が効く** — 前 relay の実測では締める向きの見立ては
**例外なく機械が覆している** (SoT = 前 relay proof-log §3.1) ⟹ ⚠ **A も覆る側が既定である**。

### 見立て B (**締める**) — 層 3 の到達点は `@[entry_point]` の本数では書けない ⟹ 3 欄が要る

理由 3 点:

1. **親 plan §5「relay 終端の棚卸し」の層 3 の本数は「N16 着地時点」と明示的にラベルされている**
   ⟹ ⚠ **stale ではなく、その時点の正直な棚卸しである**。⚠⚠ **しかし relay 終端の数としてそのまま写すと
   誤りになる** — その後 N17 が `InformationTheory/Shannon/BroadcastChannel/Thm7Region.lean` を**新設**し、
   N18 が同ファイルへ追記している。⚠⚠ **正しい本数はここでは決めない — 実行 leg が機械で数える**。
2. **本 relay は層 3 に初めて `sorry` + `@residual` を残した** (N17 / N18。分類は `plan:`)。前 relay は
   `sorry` も `@residual` も置かずに終わっており (SoT = 前 relay proof-log §8「ボトルネックではなかったもの」)、
   ⟹ ⚠⚠ **`@[entry_point]` の本数は `sorry` の有無を 1 文字も表さない**ので、前 relay の層 3 欄の書き方は
   **そのままでは使えない**。
3. **対象ファイルの列挙を更新しないと新設分が落ちる** — 前 relay の再導出コマンド (proof-log §10) は
   ファイル名を直書きしている ⟹ **同じ形で写すと N17 / N18 の分が静かに消える**。

⟹ **B が主張するのは**: 記録の層 3 欄は **(i) `@[entry_point]` の再計数 (対象ファイルの列挙つき) /
(ii) `sorry` と `@residual` の本数と分類 / (iii) type-check done と proof done の区別**の
**3 欄**を持つ義務を負う、という限定である。

**実行 leg が着手前に機械へ掛ける 1 行の反証 (⚠ 起票では実行していない)**:

```bash
rg -c '@\[entry_point\]' InformationTheory/Shannon/BroadcastChannel/*.lean
scripts/sig_view.ts --sorry InformationTheory/Shannon/BroadcastChannel/Thm7Region.lean
```

⟹ ⭐ **新設ファイルが `@[entry_point]` を 1 本も持たず、かつ `sorry` を持つ宣言が既存ファイル側に 1 本も
無いなら、(i) は「N16 時点の数がそのまま relay 終端の数である」に落ちて B の半分が死ぬ**。
⚠ **その場合でも (ii)(iii) は残る** ⟹ **B は「本数だけでは書けない」の形でしか生きない**。

### 見立て C (**緩める**) — metrics は前 relay の manifest 設定をそのまま流用してよい (作り直しは要らない)

理由 = 本 relay も可変枠 14 leg は散文 + 検証器 + 一次文献で回っており、層 3 が動いたのは判定枠 第 3 段と
形式化枠だけである ⟹ **`filters.file_prefix` を広げても、拾える Lean 側の leg は増えない**。
⚠ **「metrics は relay の作業量を代表しない」は設定を変えて直す話ではなく、§0 の射程限定として書く話である**
(前 relay proof-log §0 末尾がその形)。⚠ さらに本 relay は **数値検証器が repo 側
(`docs/shannon/verifiers/`) に出ている**ので、scratchpad 固有性による取りこぼしの形は前 relay と違う
(⚠ **前 relay §7 の処方「ブリーフに前 leg の scratchpad の絶対パスを書く」は本 relay で無効化されている** —
根拠の SoT = 親 plan §4.9)。

**殺す道具 (§4.4-2 の義務どおり着手前に決めた。⚠ 記録 leg ゆえ §4.5 の道具 3 = 明示 witness の直接評価を
機械へ読み替える)** = **manifest を 2 通り走らせて出力を突き合わせる**。前 relay と同じ設定のものと、
`file_prefix` を落とす (または `docs/` に替える) ものを作り、

```bash
deno run -A scripts/session_metrics.ts docs/metrics/bc-open-problem-t3c-relay.manifest.json \
  --render-md --out docs/metrics
```

を両方に当てる。⟹ ⚠ **記録が引く数値が 1 つでも変わったら C は死ぬ** (= 設定の作り直しが要る)。
⚠ **C は緩める側ゆえ §4.4 の非対称性が効く** — 外れると **metrics を組んでから死ぬ**ので、
**leg の後半ではなく先頭で殺しにいくこと**。

### 見立て D (**緩める**) — §6-3 の履行に新しい枠組みは要らない (前 relay proof-log §0 の型 + 差し替え 2 点で足りる)

理由 = 前 relay の §0 は **(a) 未達の宣言 / (b) 動いたのは中間結果のみ / (c) 判定の SoT は facts の該当節で
あって本 proof-log は判定を持たない**の 3 点で組んであり、**本 relay でもそのまま立つ**。
⚠ ただし **本 relay 固有の差し替えが 2 点**要る:

- **(a′) 予算と cap を混ぜない** — **plan の leg 予算 (N0–N19) と relay の cap は別勘定**であり
  (条項の SoT = 親 plan §7 判断ログ 12-(1))、⚠⚠ **「cap に当たった」と「予算を使い切った」を同じ文に書けない**。
  **§6-3 が要求するのは予算側**である。
- **(b′) 「0 sorry で proof done」型の一文が使えない** — 見立て B-2 と同じ理由。

**殺す道具 (§4.5 の道具 1 = 恒等式への帰着を機械へ読み替える)** = 前 relay proof-log の §0 を
`awk '/^## 0\./,/^## 1\./'` で切り出し、**各主張行について本 relay の対応 SoT (facts の節 / plan の節 /
成果物 md / 再導出コマンド) を名指せるか**を 1 対 1 で照合する。⟹ ⚠ **(a′)(b′) 以外に写せない行が
1 行でも出たら D は死ぬ**。⚠⚠ **「写せない」の判定は prose の印象ではなく、名指した SoT が実在するか**で行う
(`ls` / `rg` で当てる)。

### ⚠ 併記する 3 点 (どれか 1 つだけを書かない)

1. ⚠⚠ **本 leg は記録であって `Thm7 ⊋ C` の材料を出す leg ではない** — **どちらに転んでも出ない。
   排除もされない**。⚠ **レート領域の包含についても 1 文字も言わない**。
2. ⚠⚠ **§6-3 が本 leg で発火する** ⟹ **未達と書く**。⚠⚠ **中間結果をゴールの代替にしない** (親 §0.1-2) /
   ⚠ **到達した中間結果は台帳に残すが、ゴール達成とは書かない**。
3. ⚠ **同時に「尽きた」とも「だから記録に意味が無い」とも書かない** — 意味は **20 leg が何を動かし
   何を動かさなかったかを機械の裏取りつきで確定させ、次の判断をユーザーに返せる状態にすること**である。

---

## 1. 反証条件 (⚠ 新規 3 本。着手前に凍結。事後に書き換えない)

### (a) 継承分 — ⚠⚠ **無い。ただし継承しない理由を書く**

- **N18 §1-(b) の 3 本** (preflight / 材料 (i)(ii) / 既存署名) = **対象が形式化債務側**であり、
  ⚠ **本 leg は `InformationTheory/` を 1 行も書かない** ⟹ **継承しない**。⚠ **「不発火」とも書かない**
  (評価しないものを不発火と書かない)。⚠⚠ **複製も書き換えもしない**。
- **N15 / N16 の反証条件** = **`(C2)` 側の判定の分**である ⟹ 同上。
- **親 plan §6 の撤退ライン (とくに 6-3)** は**反証条件ではない** ⟹ 継承ではなく**そのまま効く**。
  ⚠⚠ **6-3 は本 leg で発火する**ので、下の **条件 1** はその履行を判定する装置である。
- **子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) §6 の
  R-2 / R-3 / R-4** も同様に**そのまま効く**。⚠ とくに **R-4** は**記録の文面にも効く**
  (「従う」で埋めた記述を書かない)。

### (b) 新規 3 本 (⚠ **発火を判定する道具を条件ごとに名指す**。⚠⚠ 「うまくいかなかったら」型は不可)

⚠ **以下で `<PL>` = `docs/proof-logs/proof-log-bc-open-problem-t3c-relay.md`** とする
(⚠ **実行 leg が別 slug を採ったときは、その実パスに読み替えて同じ判定を当てる**)。

1. ⭐⭐ **§6-3 の発火 (「未達と書く」) が、成果物のどこかで中間結果によって埋め合わされている** ⟹ 発火。
   **条件文** = 次のいずれかが機械で出たとき発火する — **(i)** `rg -c '未達' <PL>` が **0**
   (= proof-log 本体に未達を述べる行が 1 行も無い)、**(ii)** 記録の 4 か所 (`<PL>` / 本 md の §2 以降 /
   facts [`bc-facts.md`](bc-facts.md) の `## N19 (T3c)` 節 / 親 plan §5.1 の N19 着地ブロック) のうち、
   **「§0 のゴールは未達」と「`Thm7 ⊋ C` の材料は 1 つも無く排除もされていない」の 2 文が
   両方立っているものが 2 か所未満**、**(iii)** ⚠⚠ **埋め合わせの機械的な tell** =
   **到達点を述べる節に数値行 (leg 数 / commit 数 / facts の節数 / `@[entry_point]` 本数 / 再現本数 /
   検証器のテスト本数のいずれか) が在り、かつ同じ節に (ii) の 2 文が無い**。
   **発火したときの処置** = **(i)(ii)** ⟹ ⚠⚠ **その場で書き直す** (未達の宣言を落として中間結果で埋めた形は
   親 §0.1-2 違反である)。**(iii)** ⟹ **数値行を「作業量の記録」欄へ移し、到達点の欄から外す**。
   ⚠ **数値を消すのではなく場所を分ける** — 数値そのものは記録の資産である。
   **判定に使う道具** = `rg -c '未達' <PL>` / `rg -n '未達|Thm7 ⊋ C' <4 か所>` の**同一節内の共起**の数え上げ /
   到達点の節を `awk` で切り出したうえでの数値行の走査。⚠ **prose の印象で判定しない**。

2. **較正の母集団が leg ごとに確定しない** ⟹ 発火。⚠⚠ **本条件は発火する公算が実際にある**
   (見立て A-2 が予告する形そのものである)。
   **条件文** = **(i)** t3c の成果物 md を走査して **向きタグ (`締める` / `緩める`) を持たない見立てが
   1 本でもある**、または **(ii)** 起票 §0 の見立てに対して **着地側の当たり外れを述べる記述が引けない
   見立てが 1 本でもある** ⟹ 発火。
   **発火したときの処置** = **較正の射程を「凍結型 4 leg (N15–N18)」に限ると明記し、relay 全体の比率は
   書かない**。⚠⚠ **母集団を書かずに数だけ書くのは前 relay §10 が名指しで禁じた形である** ⟹
   **数を書くときは必ず母集団の定義と抽出コマンドを併記する**。
   **判定に使う道具** = `rg -n '^#+ *見立て' docs/shannon/bc-t3c-*.md` + 各 leg の較正節への `rg`
   (⚠ **成果物 md の実在は `ls` で当てる** — 親 plan の記述だけを根拠にしない。親 §4.7)。

3. **metrics が再現しない** ⟹ 発火。
   **条件文** = **(i)** manifest に書くセッション ID が解決できない、**(ii)**
   `scripts/session_metrics.ts` が非 0 で終了する / 出力ファイルが生成されない、**(iii)** 出力が
   本 relay の leg 構成 (N0–N19) と対応づかない (⚠ **セッション境界と leg 境界は一致しない** —
   前 relay も少数のセッションで 20 leg を回している)。
   **発火したときの処置** = ⚠⚠ **数値を推定で埋めない**。**(i)(ii)** ⟹ **metrics の節を落として
   「測れなかった」と書き、再生成手順 (manifest の設計と実行コマンド) だけを残す**。**(iii)** ⟹
   **セッション単位の数値であることを明記し、leg 単位に割り付けない**。
   **判定に使う道具** = `deno run -A scripts/session_metrics.ts <manifest> --render-md --out docs/metrics`
   の終了コード + `ls docs/metrics/bc-open-problem-t3c-relay.metrics.md` による生成物の実在確認。

## 2. 実施

### 2.1 ⭐⭐ 到達点 (⚠ §6-3 の履行。⚠⚠ 数値はここに置かない — 作業量は §2.4)

⚠⚠ **20 leg (N0–N19) を使い切って §0 のゴールは未達である** (親 plan §6-3 の発火)。
⚠⚠ **`Thm7 ⊋ C` の材料は 1 つも出ておらず、排除もされていない**。

⚠ **発火とは「未達と書く」ことであって、達成条件を緩めることではない** (親 §0.1-1)。
⚠ **動いたのは中間結果のみであり、中間結果はゴールの代替にしない** (親 §0.1-2)。
⚠ **同時に「尽きた」とも書かない** — 残った受け皿は親 plan §5 の棚卸し表が型つきで持つ。
⚠⚠ **relay の cap (23) と plan の leg 予算 (20) は別勘定であり、混ぜて書かない** (親 §7 判断ログ 12-(1))。
**§6-3 が要求するのは予算側である**。

⚠ **判定の SoT は [`bc-facts.md`](bc-facts.md) の各 `## N* (T3c)` 節である** — ⚠⚠ **本 md も
proof-log も判定を 1 つも持たない**。

### 2.2 step 0 — ⭐ 着手前の 1 行反証 (⚠⚠ 本文を 1 行も書く前に実行した)

§4.4-1 の義務どおり、**§0 の締める側 2 本 (見立て A / B) の 1 行反証を最初に機械へ掛けた**。
⚠ **§0 が指定したコマンドをそのまま走らせている** (逐語は §3.1 / §3.2)。
続けて**緩める側 2 本 (C / D) の「殺す道具」も leg の先頭で走らせた** — ⚠ **C は §0 自身が
「外れると metrics を組んでから死ぬので leg の後半ではなく先頭で殺しにいくこと」と指定していた**。

### 2.3 成果物

1. **metrics** — `docs/metrics/bc-open-problem-t3c-relay.manifest.json` を新規に書き、
   `.metrics.json` / `.metrics.md` を生成した。⚠ **セッション同定は時刻窓ではなく、編集ファイルと
   `--turns` のターン内容で行った** (理由は §3.3)。
2. **proof-log** — `docs/proof-logs/proof-log-bc-open-problem-t3c-relay.md` を新規作成
   (前 relay の proof-log の続編)。⚠ **判定を複製せず、質的観察に集中させた**。
3. **本 md の §2–§5**。

### 2.4 ⚠ 作業量の記録 (⚠⚠ 到達点ではない。§2.1 と混ぜない)

⚠ **すべて機械で数え直した値である** — 親 plan / facts の記述を引き写していない。
⚠⚠ **ただし「側の内訳」だけは時点を分けて読む** (監査 訂正 1) — **実行 leg の時点で機械が返すのは
`記録 2`** であり、`記録 3` は **着地 leg が親 plan §5.1 へ N19 の冒頭宣言行を書いた後の値**である。

| 項目 | 実測 |
|---|---|
| leg | **20** (N0–N19) |
| 側の内訳 (⚠ **着地後**) | **(C2) 14 / 形式化債務 3 / 記録 3** (⚠⚠ **実行 leg の時点では `記録 2`** — N19 の宣言行が親 plan にまだ無かった) |
| commit (`5e3fce9a~1..7a9dc552`) | **91** (⚠ 本 leg 自身の commit を含まない) |
| うち `InformationTheory/` を触るもの | **12** |
| facts の `(T3c)` 節 | **15** (⚠ `## N19 (T3c)` は着地 leg が作る ⟹ **着地後は 16**) |
| T3c が層 3 へ追加した `@[entry_point]` | **6** (⚠ **削除は 0 本**ゆえ **追加した 6 本はすべて現存する**。⚠⚠ **`BroadcastChannel/` の現存総数ではない** — そちらは 36 本である) |
| `sorry` / `@residual` | **2 / 2** (`Thm7Region.lean` の 1 ファイル、分類は `plan:`) |
| repo 側の数値検証器 / 型検査 probe | **16 本 / 15 本** |
| サブエージェント | **92 体** (⚠ **前 relay と二重計上の `d6d1ee1c` の 11 体を含む** = 全体の約 12% / ⚠ **計測時点で進行中の値であり、監査が独立に数えると 93**) |

## 3. 機械検証

### 3.1 見立て A の 1 行反証 (⚠ 着手前に実行)

```bash
rg -n '^#+ *見立て' docs/shannon/bc-t3c-*.md
rg -no '\*\*(締める|緩める)\*\*' docs/shannon/bc-t3c-*.md
ls -1 docs/shannon/bc-t3c-*.md          # 成果物 md の実在を親 plan ではなく ls で当てる (親 §4.7)
```

**出力の要点** (逐語は再実行で引ける):

- 見出し型の `見立て` を持つ成果物は **6 本** で、本数は **6 / 3 / 4 / 4 / 4 / 4** と**揃っていない**。
  ⚠⚠ **その 6 本目は本 md 自身 (N19) である** (監査 訂正 8) ⟹ **形式の調査には自 leg を入れ、
  §4 の較正には入れていない** = **節をまたいで母集団の扱いが一致していない**。
  ⚠ **「本数が揃っていない」という結論自体は N19 を除いても成立する** (6 / 3 / 4 / 4 / 4)。
- ⭐ **向きタグを持たない見立てが 1 本ある** — `docs/shannon/bc-t3c-n10-epsilon-zero.md:69`
  = `### 見立て 6 (**予想**)`。⚠ **`締める` / `緩める` のどちらでもない**。
- 見出し型を持たない leg にも**着地時の表の中に向きタグが在る** (N6 / N7 / N9 / N12)。
  ⚠ **形式が違う**。

### 3.2 見立て B の 1 行反証 (⚠ 着手前に実行)

```bash
rg -c '@\[entry_point\]' InformationTheory/Shannon/BroadcastChannel/*.lean
scripts/sig_view.ts --sorry InformationTheory/Shannon/BroadcastChannel/Thm7Region.lean
```

- **`Thm7Region.lean` は `@[entry_point]` を 1 本持つ** (`thm7Region_nonempty`, L528)。
- **`sorry` を持つ宣言は 2 本** (`isClosed_iUnion_thm7RegionOfLaw` L256 /
  `isClosed_thm7Region` L288)、いずれも `@residual(plan:bc-computable-region-formalization)`。

**追加で当てた 4 点** (⚠ **§0 が予測した「新設は N17」を機械で確かめ直した結果、帰属が動いた**):

```bash
git diff 5e3fce9a~1..7a9dc552 -- 'InformationTheory/**' | rg '^\+' | rg -c '@\[entry_point\]'   # 6
git diff 5e3fce9a~1..7a9dc552 -- 'InformationTheory/**' | rg '^-'  | rg -c '@\[entry_point\]'   # 0
git log --oneline -S'@[entry_point]' 5e3fce9a~1..7a9dc552 -- .../Thm7Region.lean               # c4ab66a6 のみ
for c in 637dc7de c82fc351 510714ed; do git diff 5e3fce9a~1..$c -- 'InformationTheory/**' \
  | rg '^\+' | rg -c '@\[entry_point\]'; done                                                   # 5 / 5 / 5
lake env lean InformationTheory/Shannon/BroadcastChannel/Thm7Region.lean                        # 0 error / sorry 警告 2
```

⟹ ⭐ **6 本目を入れたのは N18 (`c4ab66a6`) であって N17 ではない**。
⟹ ⭐⭐ **N12 着地 / N16 着地 / N17 着地のいずれの時点でも実測は 5 である**
⟹ ⚠⚠ **親 plan §5 の「`@[entry_point]` 5 本ちょうど」も facts `## N12 (T3c)` の同記述も、
ラベルされた時点においては正しい** (⚠ **stale ではない**)。⚠ **relay 終端の数としては 6 である**。
⚠ **親 plan を直すのは着地 leg の仕事なので本 leg は直していない**。

### 3.3 見立て C の殺す道具 (⚠ leg の先頭で実行)

`filters.file_prefix` だけを替えた manifest を 3 通り作って突き合わせた。

```bash
deno run -A scripts/session_metrics.ts docs/metrics/bc-open-problem-t3c-relay.manifest.json \
  --render-md --out docs/metrics                                       # 終了コード 0、生成物 2 本
# 変種は scratchpad で走らせて出力を JSON で突き合わせた
```

| 設定 | 対象ファイル Edit 回数 (合計) | 対象ファイル Write 回数 |
|---|---|---|
| `InformationTheory/Shannon/` (前 relay と同じ) | **112** | **2** |
| `docs/` | **952** | **59** |
| `filters` キーごと削除 | **0** | **0** |

⚠ **オーケストレーター単独では `InformationTheory/Shannon/` の編集は 0 回**であり、
**112 回はすべてサブエージェント側**である。

### 3.4 見立て D の殺す道具

```bash
awk '/^## 0\./,/^## 1\./' docs/proof-logs/proof-log-bc-open-problem-t3b-relay.md   # 42 行
rg -c '^## N19 \(T3c\)' docs/shannon/bc-facts.md                                   # 0
rg -n '判定 = \*\*NO-GO' docs/shannon/bc-facts.md | wc -l                          # 2
rg -o '^\*\*leg 冒頭宣言 \(N[0-9]+\)\*\*: 側 = [^/]*' docs/shannon/bc-open-problem-t3c-plan.md
```

⚠ **名指した SoT の実在を `rg` / `ls` で当てた** (§0 が指定した判定法どおり、prose の印象で判定していない)。

### 3.5 セッションの同定

全 91 commit を UTC / JST の 9 時間差を補正したうえでセッション窓へ写像し、
**どのセッション窓にも入らない commit は 0 件**であることを確認した。
⚠ **ただし窓が重なるため時刻だけでは一意に決まらない** ⟹ **編集ファイルと `--turns` の
ターン内容で割り付け直した** (詳細は proof-log 冒頭)。

### 3.6 凍結の機械確認 (⚠⚠ **ブリーフ指定のコマンドは偽陰性を出した。中身は不変である**)

**結論 = 凍結は守られている**。⚠ **ただし r20 の監査が指摘した「凍結確認コマンドが構造的に
誤判定を出す」という現象が、向きを変えてもう 1 度出た** — **今度は偽陽性ではなく偽陰性である**。

**(a) ブリーフが指定した 2 コマンド (同一の `awk` 形を両側へ当てる) は一致しない**:

```bash
git show bd23adbf:docs/shannon/bc-t3c-n19-record.md | awk '/^## 0\./,/^## 2\./' \
  | sed '$d' | grep -v '^[[:space:]]*$' | md5     # 1d3bb77573d44368e5dcf8ec2e423224 (135 行)
awk '/^## 0\./,/^## 2\./' docs/shannon/bc-t3c-n19-record.md \
  | sed '$d' | grep -v '^[[:space:]]*$' | md5     # ef43919dcecde95fd9c7006afccff2c1 (136 行)
```

**原因 (実測)** = ⚠⚠ **起票 commit には `## 2.` が存在しない** (`rg -c '^## 2\.'` = 0) ため、
**その側では `awk` の範囲が EOF まで走り、`sed '$d'` が §1 の最終行そのものを削ってしまう**。
⟹ **差分は 1 行だけで、その 1 行は「削られてはならない §1 の行」である**:

```bash
/usr/bin/diff <起票側> <現行側>
# 135a136 >    の終了コード + `ls docs/metrics/…` による生成物の実在確認。
```

**(b) 正しい切り出しどうしは完全に一致する** — ⚠ **起票側は `## 2.` を持たないので
`awk '/^## 0\./,0'` を当てる** (⚠ **これは §0 の凍結ブロック自身が指定した形である**):

```bash
git show bd23adbf:docs/shannon/bc-t3c-n19-record.md | awk '/^## 0\./,0' \
  | grep -v '^[[:space:]]*$' | md5                # ef43919dcecde95fd9c7006afccff2c1 (136 行)
awk '/^## 0\./,/^## 2\./' docs/shannon/bc-t3c-n19-record.md \
  | sed '$d' | grep -v '^[[:space:]]*$' | md5     # ef43919dcecde95fd9c7006afccff2c1 (136 行)
```

⚠ **範囲が非空であること (136 行) を先に確認してから md5 を取った** — **範囲指定を誤ると
空文字列どうしの偽 PASS になる**。

**(c) 独立な裏取り (⚠⚠ 論法は監査 訂正 9 が差し替えた)** — 実行 leg は
`git diff bd23adbf -- <本 md>` の**削除行が 0 本**であることを md5 非依存の裏取りとして挙げたが、
⚠⚠ **削除行 0 は「書き換えられていない」を含意しない** — **凍結区間への挿入は削除行 0 のまま通る**
(監査が凍結区間の途中へ 1 行入れた版を作り、当該テストが**削除行 0 で PASS する**ことを機械で示した)。
**正しい md5 非依存の裏取りは次の 2 本**であり、⭐ **監査が両方を実行して凍結が byte 水準で
保たれていることを独立に確定させた** ⟹ ⚠ **結論は動かない。論法だけが不足していた**:

```bash
git diff bd23adbf -- docs/shannon/bc-t3c-n19-record.md | rg '^@@'
# 出力は `@@ -198,3 +198,<n> @@` 1 個だけ ← 末尾の append のみ (⚠ 凍結区間の内側に hunk が無い)
#   ⚠ `<n>` は §2 以降が伸びるたびに増える ⟹ 値を控えず「hunk が 1 個で開始行が 198」を見る
git show bd23adbf:docs/shannon/bc-t3c-n19-record.md > /tmp/frozen.md   # 200 行
head -200 docs/shannon/bc-t3c-n19-record.md > /tmp/cur200.md
cmp /tmp/frozen.md /tmp/cur200.md                                      # byte 一致
```

⚠ **本 leg は当初 `## 2.` の直前へ `---` の区切りを置いていたが、それが切り出し範囲に入って
差分をもう 1 行増やしていたので外した** — ⚠⚠ **凍結区間の中身ではなく、その直後の書式である**。

**⚠ プロセスへの申し送り** = **凍結の確認は「同一コマンドを両側へ当てる」形では書けない** —
**起票側には終端見出しが存在しないので、両側でコマンドが変わるのが正しい**。
⚠⚠ **r20 が偽陽性を潰した是正 (空行の正規化) は本 leg でも有効だが、それだけでは足りない**。

## 4. 見立ての較正

⚠⚠ **§0 / §1 は 1 文字も書き換えていない**。効きは本節にだけ書く。
⚠ **§0 の締める側 2 本 (A / B) の 1 行反証は、本文を書き始める前に実行した** (§2.2 / §3.1 / §3.2)。

### 4.1 見立て A (**締める**) = ⭐ **生存 (覆らなかった)。⚠ ただし「2 欄」は下限だった**

**殺す条件** = 「t3c の成果物で見立てが同一形式 (本数が揃い、全件に向きタグが付く) に揃っていれば A は死ぬ」。
**実測 (§3.1)** = **本数は揃わず (6 / 3 / 4 / 4 / 4 / 4)、向きタグを持たない見立ても 1 本ある**
⟹ **殺す条件は成立しない** ⟹ **A は生存**。

⚠ **ただし A が主張した「2 欄に割る」は下限である** — 欄 (ii) は
**着手前 6 本立て型 (N10) / 着地時の表型 (N6 / N7 / N9 / N12) / 見立てを持たない leg** の
**3 型に割れる**。⟹ **A は「1 欄では書けない」の形で当たり、「2 欄で足りる」の形では外れた**。

⚠⚠ **§0 が「A も覆る側が既定である」と書いた予告は外れた** — ⚠ **本 relay の締める側で
覆らなかった見立ては A が少なくとも 3 例目以降である** (監査 訂正 5。⚠ **当初は「2 例目」と書いたが、
判定語の字面だけを拾う数え方は本 md 自身が禁じた「丸め」に当たる** — **出所 leg の勘定では
N15 の見立て C も生存側であり**、結論水準の生存は **N15 C / N17 A / N17 C / N18 A の 4 本**である)。
⟹ ⚠⚠ **前 relay の「締める側は 9 件が 9 件とも覆る」は、凍結型 4 leg の欄では再現していない**
(⚠⚠ **射程は「この欄」であって relay 全体ではない** — 監査 訂正 6。§1 反証条件 2 の処置が
「較正の射程を凍結型 4 leg に限ると明記する」と定めており、relay 全体の射程で書くのはその処置に反する。
母集団の定義と内訳は proof-log §3.1。⚠ **母集団が違うので比率を並べない**)。

### 4.2 見立て B (**締める**) = ⭐ **生存 (覆らなかった)。⚠ ただし理由 1 の帰属が動いた**

**殺す条件** = 「新設ファイルが `@[entry_point]` を 1 本も持たず、かつ `sorry` を持つ宣言が
既存ファイル側に 1 本も無いなら (i) は死ぬ」。
**実測 (§3.2)** = **新設ファイルは `@[entry_point]` を 1 本持つ** ⟹ **第 1 連言肢が偽**
⟹ **殺す条件は成立しない** ⟹ **B は生存**。⚠ **(ii)(iii) も残る** (§0.2 の proof-log 参照)。

⚠⚠ **ただし B-1 の中身は 1 点外れた** — B-1 は「その後 **N17 が新設し**、N18 が追記している」と書いたが、
⭐ **`@[entry_point]` を足したのは N18 だけであり、N17 着地時点の実測は 5 のままである**。
⟹ ⚠⚠ **本 leg が 1 leg 前に評価していたら、殺す条件の第 1 連言肢は真になっていた** =
**B の生存は N18 の 1 commit に依存している**。⚠ **「B は当たった」と単記に丸めない**。

⚠ **B-1 が「stale ではなく、その時点の正直な棚卸しである」と書いた側は完全に当たり** —
機械で当てた 3 時点 (N12 / N16 / N17 着地) すべてで実測は 5 である。

### 4.3 見立て C (**緩める**) = ⚠⚠ **外れ (結論)。⚠ ただし理由は生存した**

**殺す条件** = 「manifest を 2 通り走らせて、記録が引く数値が 1 つでも変わったら C は死ぬ」。
**実測 (§3.3)** = **対象ファイル Edit 回数が 112 → 952 (約 8.5 倍) に変わる** ⟹ **C は死んだ**。
⟹ **前 relay の manifest 設定をそのまま流用してよい、とは言えない**。

⚠ **しかし C が挙げた理由 (「`file_prefix` を広げても、拾える Lean 側の leg は増えない」) は正しい** —
**層 3 が動いた leg は 4 本のままである**。
⟹ ⭐ **新しい型の外れ方である**: **理由が正しいまま結論が死ぬ**。
⚠⚠ **原因は、C の理由が「どの leg を拾うか」を見ているのに、殺す条件が「いくつ数えるか」を
見ていること**である — **同じ設定変更が、前者を動かさずに後者を 8.5 倍動かす**。

⚠ **第 3 の観測**: **§0 が変種として挙げた「`file_prefix` を落とす」は、`filters` キーごと削除すると
対象ファイル Edit 回数が 0 になる** ⟹ **「フィルタ無し = 全件」ではない**。
⚠ **§0 の指示のうちこの選択肢は実行できるが意味を持たない**。

⚠ **C は緩める側なので外れは高くつく形だが、実際の課金は小さかった** — ⭐ **§0 自身が
「leg の後半ではなく先頭で殺しにいくこと」と指定しており、そのとおり先頭で殺したので、
書き直したのは manifest の 1 行と proof-log §0.4 の射程限定だけである**。
⟹ ⭐ **§4.4 の非対称性への処方 (緩める側は先に殺す) が機能した実例である**。

### 4.4 見立て D (**緩める**) = ⚠⚠ **外れ (差し替えは 2 点では足りなかった)**

**殺す条件** = 「(a′)(b′) 以外に写せない行が 1 行でも出たら D は死ぬ。判定は名指した SoT が
実在するかで行う」。**実測 (§3.4)** = **写せない行が 3 本出た** ⟹ **D は死んだ**。

| # | 前 relay §0 の行 | 本 relay での実測 |
|---|---|---|
| 1 | 「本 relay は **NO-GO を 1 本も出しておらず**」 | ⚠ **写せない** — 本 relay は NO-GO を含む複数の型の判定を出している (逐語と型の SoT = facts) |
| 2 | 「可変枠の側は **14 本とも `(C2)`**」 | ⚠ **写せない** — 側は **(C2) 14 / 形式化債務 3 / 記録 3** の混在である |
| 3 | 「到達点と限界の SoT は facts の `## M19 (T3b)` 節」 | ⚠ **名指した SoT が実在しない** — `rg -c '^## N19 \(T3c\)'` は **0** |

⚠ **3 は時点由来であって型由来ではない** — **`## N19 (T3c)` 節は着地 leg が作る**ので、
着地後には写せるようになる。⚠⚠ **しかし D の殺し方の字義 (`rg` で当てる) では本 leg の時点で死ぬ**。
⟹ ⚠ **1 と 2 だけでも D は死ぬので、判定は 3 に依存していない**。

⚠ **D が予告した (a′)(b′) の 2 点自体はどちらも当たり** — cap と予算を混ぜられないこと、
「0 sorry で proof done」型の一文が使えないことは、実際に proof-log §0 で書き分けが要った。
⟹ **D は「型は流用できる」の形では生き、「差し替えは 2 点で足りる」の形では死んだ**。

### 4.5 反証条件 3 本の発火状況 (⚠ **3 本それぞれ書く。まとめない**)

**反証条件 1 (§6-3 の発火が中間結果で埋め合わされている)** = ⚠ **不発火**。
**判定の逐語**:
- **(i)** `rg -c '未達' <PL>` = **1** ⟹ 0 ではない ⟹ 不発火。
- **(ii)** 「§0 のゴールは未達」と「`Thm7 ⊋ C` の材料は 1 つも無く排除もされていない」の **2 文が
  両方立っている**のは、**proof-log §0.1 と本 md §2.1 の 2 か所** ⟹ **2 か所未満ではない** ⟹ 不発火。
- **(iii)** 到達点を述べる節 (proof-log §0.1 / 本 md §2.1) は**どちらも (ii) の 2 文を同じ節に持つ**
  ⟹ 条件の後半 (「同じ節に 2 文が無い」) が偽 ⟹ 不発火。⚠ **数値行は「作業量の記録」欄
  (proof-log §0.3 / 本 md §2.4) へ分けた** — ⚠ **数値を消したのではなく場所を分けた**。

⚠⚠ **無条件の不発火ではない**。理由 2 点:
1. **(ii) は 2 か所ちょうどで、下限に接している** — ⚠ **残る 2 か所 (facts `## N19 (T3c)` 節 /
   親 plan §5.1 の N19 着地ブロック) は着地 leg が書く**ので、⚠⚠ **着地 leg が 2 文のどちらかを
   落とすと (ii) は発火しうる**。⟹ **本 leg の不発火は着地 leg の書き方を保証しない**。
2. **(iii) の判定は「到達点を述べる節」の同定に依存する** — ⚠ **その同定は本 leg の自己申告である**。

**反証条件 2 (較正の母集団が leg ごとに確定しない)** = ⭐⭐ **発火した**。
**発火した条件文** = **(i)** — **向きタグ (`締める` / `緩める`) を持たない見立てが 1 本ある**:
`docs/shannon/bc-t3c-n10-epsilon-zero.md:69` の `### 見立て 6 (**予想**)`。
⚠ **1 本でも在れば発火する条件文なので、本数の多寡は判定に効かない**。
⚠ **(ii) 単独では発火していない** — 凍結型 4 leg の 15 本はいずれも着地側に当たり外れの記述が引ける
(⚠ **N18 の見立て B は「評価していない」と書かれており、これは記述が引けない場合ではない**)。

**処置 (§1 が定めたもの) を実行した**: **較正の射程を凍結型 4 leg (N15–N18) に限ると明記し、
relay 全体の比率を書かなかった**。**数を書いた箇所には母集団の定義と抽出コマンドを併記した**
(proof-log §3.1 / §3.2)。⚠ **さらに欄 (ii) では比率そのものを書いていない**。

⚠⚠ **本条件の発火は見立て A-2 が予告した形そのものである**。

**反証条件 3 (metrics が再現しない)** = ⚠ **(i)(ii) は不発火 / (iii) は部分発火**。
- **(i)** manifest に書いた **20 セッションの ID はすべて解決した** ⟹ 不発火。
- **(ii)** `session_metrics.ts` は **終了コード 0** で、`.metrics.json` / `.metrics.md` が
  **2 本とも生成された** ⟹ 不発火。
- **(iii)** ⚠ **部分発火**。⚠ **全 91 commit がセッション窓に入る (未対応 0 件)** が、
  **20 セッションのうち 2 本は N0–N19 の leg へ 1 対 1 に対応しない**:
  - `d6d1ee1c` は**前 relay の記録 leg と本 relay の plan 起票 + N0 を同一セッションで抱えており、
    前 relay の manifest にも載っている** ⟹ **二重計上である**。
  - `f5296e13` は **leg 番号を持たないユーザー対話セッション**で、**leg の枠組みそのものを変えた**。
  - ⚠ 加えて `716ce498` は **commit を 1 本も出していない** (成果物は次セッションが commit した)
    ⟹ **commit の帰属とセッションの帰属は一致しない**。

  **処置 (§1 が定めたもの) を実行した**: **セッション単位の数値であることを manifest の `problem` と
  proof-log 冒頭に明記し、leg 単位に割り付けなかった**。⚠⚠ **数値を推定で埋めていない**。

## 5. 未確認のまま残したものの名指し

⚠⚠ **以下は「おおむね妥当」ではなく、確かめていないことと弱いと自覚している判断を、
向きつきで名指したものである**。

1. ⚠⚠ **「起票と実行を分けて git で凍結する作法が効いた」を、私は測っていない** (proof-log §4)。
   測れたのは**凍結が 4 leg とも破られなかったこと**だけで、**分けなかった場合との比較は存在しない**。
   ⟹ **向き = 作法の効果を過大に読む側**。⚠ **proof-log にもそう書いたが、表現が控えめすぎる可能性がある**。
2. ⭐⭐ **⚠ 本項は監査が閉じた (監査 §4 の判定 = 本項の留保は「強すぎ」)**。実行 leg は
   「そのテキストが親へ届いたかどうかは transcript から判定していない ⟹ 診断は読み落としの
   可能性を排除していない」と書いたが、**判定はできた** — 親 transcript の受信封筒を
   `summary=` を持つ報告封筒と `idle_notification` だけの封筒に分けると、
   ⭐ **形式化枠 2 leg の 11 体のうち 6 体は報告が親へ届き、5 体は親に存在しない**
   (N17 = impl / style、N18 = exec / dedup / landing)。⟹ ⚠⚠ **その 5 体について
   「オーケストレーターが届いた報告を読み落とした」可能性は排除される** ⟹
   ⭐ **「障害は生産ではなく伝達の側にある」は機械で確定した** (留保は不要である)。
   ⚠ **同時に未達の実数は 3 ではなく 5 であり、オーケストレーター側の検出も取りこぼしていた**
   (N17 の 2 体は名指されていない)。⚠ **「4 例中 3 例」という分母 4 は再現せず**、
   N18 単独では **6 体中 3 体**である。
3. ⚠ **「前 relay の乖離事象 (報告と実態の食い違い) は再発していない」は記録の不在に基づく**
   (proof-log §5.3)。⟹ **向き = 再発の見落とし側**。⚠ **「起きなかった」の証拠ではない**。
4. ⚠⚠ **セッション → leg の割り付けは、時刻の重なりを内容で切ったものであり、
   ターン単位 (`prompt_ids`) では切っていない** ⟹ **境界セッションの二重計上は残っている**
   (とくに `d6d1ee1c` は前 relay の manifest と重複する)。
   ⟹ **向き = 本 relay の作業量を過大に読む側**。⚠ **切る手段 (`prompt_ids`) は存在するのに使っていない**。
5. ⚠⚠ **本項は監査が「弱すぎ」と判定した (監査 §4)** — 実行 leg は「3 型に割れるという再分割は
   私の判断であり独立に確かめていない ⟹ 向き = 欄が足りない**可能性**」と書いたが、
   ⭐ **欄が足りないことは可能性ではなく機械で出る事実である** — **欄 (i) (凍結型 4 leg) と
   欄 (ii) の 3 型を足しても 20 leg 中 19 leg しか覆わず、N19 自身がどちらの欄にも入らない**。
   N19 の 4 本は凍結型・向きタグつき・着手前 1 行反証つきで**欄 (i) の形式に当てはまる**ので、
   欄 (ii) の定義 (「欄 (i) の形式に当てはまらない見立て」) でも拾えない。
   ⟹ ⚠⚠ **「3 つ目の `9`」に直接効く** — **N19 の締める 2 本 (A / B) を入れると締める側は 11 本になり、
   `9` という一致は未申告の除外に依存していたことになる**。**処置は proof-log §3 が持つ**
   (母集団の定義に「N19 を自己較正ゆえ除外する」を明記した)。
   ⚠ **どちらの数でも主判定は動かない** — **A も B も生存側なので、覆らない例が増えるだけである**。
6. ⚠⚠ **見立て D の「写せない行 3 本」は、前 relay §0 の 42 行のうち何を「主張行」と数えたかの
   基準を私が明示していない** (§4.4)。⟹ **本数の精度は無い**。⚠ **D の死そのものは行 1 / 行 2 の
   どちらか 1 本で決まるので判定は動かないが、「3 本」という数は引用に耐えない**。
7. ⚠ **反証条件 1-(iii) の「到達点を述べる節」の同定は本 leg の自己申告である** (§4.5)。
   ⟹ **向き = 不発火と読む側**。⚠ **別の読み方をすれば作業量の記録欄も到達点の節に見えうる**。
8. ⚠⚠ **proof-log §2 で「道具 (2) (構造つき全探索) が主戦力になった leg は本 relay に無い」と書いたが、
   これは各 leg 成果物の記述からの判断であり、全 leg の検証器を読んで確かめてはいない**。
   ⟹ **向き = 道具 (2) の使用を見落とす側**。
9. ⭐ **⚠ 本項も監査が閉じた (監査 §4 の判定 = 本項の留保は「強すぎ」)**。実行 leg は
   「帰属はファイル別の diff 加算から出しており、同一 commit 内で他ファイルへ移送された可能性を
   排除していない」と書いたが、⭐ **per-commit で `+` と `-` を数える独立実装が排除した** —
   12 commit を走査すると **3 commit だけが `@[entry_point]` を触り、それぞれ +3 / +2 / +1、
   削除は全 commit で 0、各 commit が触るファイルは 1 本**である ⟹ **移送は起きていない**
   ⟹ ⭐ **帰属 (N8 3 / N11 2 / N18 1) は確定した** (留保は不要である)。
10. ⭐ **⚠ 本項は監査が独立に閉じた** — 先頭 200 行の `cmp` で **byte 一致**、`git diff` の hunk は
    **末尾 1 個の append** (開始行 198 = §2 の直前)、⚠⚠ **当該 `---` は git 履歴に 1 度も入っていない**
    ⟹ **「自分の判定を自分で通した」構造は残らない**。
    ⚠⚠ **ただし懸念の提起そのものは正しかった** — 実行 leg が md5 非依存の裏取りとして挙げた
    **「`git diff` の削除行 0 本」は論理的に不十分**である (**凍結区間への挿入は削除行 0 のまま通る**)。
    監査は凍結区間の途中へ 1 行入れた版を作って当該テストが **削除行 0 で PASS する**ことを機械で示した
    ⟹ **正しい裏取りは (i) hunk header が末尾 1 個の append であること / (ii) 起票側の全行と現行の
    先頭同数行の `cmp` である** (⚠ **この知見は proof-log §4 が運用への申し送りとして持つ**)。
11. ⚠⚠ **本 md と proof-log は「§0 のゴールは未達」と書いているが、その未達の内訳
    (肯定側と否定側のどちらにどれだけ近いか) を私は評価していない** — ⚠ **評価しないことは §0.1-2 に
    沿う判断だが、⚠⚠ 「評価していない」と「近くない」は別である**。⟹ **向き = 記録の空白**。

## 6. 敵対的独立監査と着地 (⚠ 本節は着地 leg が書いた)

**監査 = [`bc-t3c-n19-audit.md`](bc-t3c-n19-audit.md)** (親 plan §4.6、既定の立場 =
「被監査 leg が書いた数値・母集団・較正・『未達』の書き方はすべて誤りである」)。
**35 本試して 15 本成立 / 19 本不成立 / 1 本未決着**、⭐ **判定 = 訂正あり生存**
(⚠⚠ **訂正 15 件のうち主判定を動かすものは 0 件**)。
⚠ **監査は成果物 / proof-log / metrics / 親 plan / facts を 1 文字も書き換えていない** —
**反映は本節を書いた着地 leg の仕事である**。⚠⚠ **監査 md も事後に書き換えない**。

**主判定** (前 relay の全件命題「締める方向の見立ては 9 件中 9 件とも機械で覆った」は本 relay では
再現していない) は、**最寛容な読み (どの部分でも死ねば覆ったと数える) でも最大 7/9** ゆえ生存した。
⚠⚠ **ただし内訳は書き直しになった** — **完全に覆った 1 / 結論水準の生存 4 / 部分評価 1 /
強い形だけが死んだ 3** である (§4.1 と proof-log §3.1)。

**訂正 15 件の反映先** (⚠ **本 md 側 = 7 件 / proof-log 側 = 10 件。うち 2 件 (1 / 9) は両方**):

| 訂正 | 反映先 |
|---|---|
| 1 (側の内訳の時点) | 本 md §2.4 + proof-log §0.3 |
| 2 / 3 / 4 (較正 4 分類) | proof-log §3.1 (+ §8 の提案行へ波及) |
| 5 (「2 例目」) / 6 (射程) | 本 md §4.1 |
| 7 (母集団が 19 leg しか覆わない) | proof-log §3 冒頭 / §3.1 / §3.2 |
| 8 (形式調査に自 leg が入る) | 本 md §3.1 |
| 9 (削除行 0 の論法) | 本 md §3.6-(c) + proof-log §4-7 (⭐ **後続 relay 向けの運用知見として残した**) |
| 10 (再生成で 68 行動く) | proof-log §9 |
| 11 / 12 (subagent の伝達) | proof-log §5.1 |
| 13 (「判定を 1 つも持たない」) | proof-log §0.1 + §9 |
| 14 (現存本数) / 15 (92 体) | 本 md §2.4 |

**自己申告 11 項の検算 = 弱すぎ 1 / 妥当 8 / 強すぎ 2** ⟹ **§5 の第 2 / 第 5 / 第 9 / 第 10 項を
書き直した** (⚠ **弱く書きすぎるのも記録としては誤りである**)。

⚠⚠ **本 leg の着地をもって T3c relay は終端する**。**§6-3 の発火 (20 leg を使い切って §0 未達) は
§2.1 / proof-log §0.1 / 親 plan §5.1 の N19 着地ブロック / facts `## N19 (T3c)` の 4 か所で履行した**。
⚠ **残った受け皿は親 plan §5 の「relay 終端の棚卸し」が型つきで持つ** — ⚠⚠ **「尽きた」とは書かない**。
