# BC 未解決問題 — 候補経路 (層 1)

> 本 relay の主成果物。SoT は経路ごとの記述そのもの。昇格条件 = 親 plan §3.1 の 5 条件、
> 確信度ラベル = §3.2。親 plan: [`bc-open-problem-plan.md`](bc-open-problem-plan.md) /
> 確定事実: [`bc-facts.md`](bc-facts.md) / attack 台帳:
> [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md)
> **本文書が引く数値・記号的検算の再検証**: R1 は `python3 docs/shannon/bc-route-r1-check.py`
> (約 9s、`--quick` 0.5s。乱数種固定・局所最適化不使用ゆえ**再実行で全報告値が一致する**)、
> R2 は `python3 docs/shannon/bc-jognair-general-check.py`
> (約 160s、`--quick` 約 25s。乱数種と局所最適化の開始点を固定してあり、`--quick` 2 回の
> 出力が実行時間行を除いて完全一致することを確認済)。
> ⚠ `unprobed` の経路を「証明できた」と書かない (親 plan §3.2)。

## 経路一覧

| id | 主張 (1 文) | 軸 | attack slug | 確信度 | 満たす条件 |
|---|---|---|---|---|---|
| R1 | Gohari–Liu–Nair Conjecture 1 (双対汎関数 `F` の加法性) をそのパラメータ `(α, λ)` について一様に仮定し、かつ `F` が Marton 領域の support function の**タイトな**双対であるなら、3 補助変数 Marton 内界の support function は同一チャネルの自己テンソルに対して加法的 (`h_n^T(θ) = n·h_1^T(θ)`, ∀`n`, ∀`θ ≥ 0`) である | E+G | `support-vs-dual-additivity` | `unprobed` (含意は文献で確認済) | (1)(2)(3) / **(5) novelty gate 不合格 (L8)** |
| R2 | [GJNW13] Theorem 1 の `\|X\| ≥ 3` 版の**鋭い加法スラック** `f*(k)` は決定論的な「格子」BC で達成される (`f*(k) = D(k)`)。`D(2) = 0` なので `\|X\| = 2` では Theorem 1 そのものに退化し、`D(3) = log₂φ` (φ = 黄金比)、完全平方では `D(m²) = log₂m` | C | `jog-nair-inequality` | `probed` | (1)(2)(3)(4) / (5) は `(secondary)`。**核 step 9 は `gap` として表示** |
| R3 | 4 項損失 `L'` は入力アルファベットの 2 分割について**超加法的** (`L'(p) ≥ Σ_w p(w) L'(p_w)`、同値に `T(p) ≤ H(W) + Σ_w p(w) T(p_w)`)。`\|X\| ≤ 2` を [GJNW13] Theorem 1 で閉じると、最適化を含まない再帰 `Λ_split` が `gap` の**証明された**チャネル依存上界を与える | C | `slack-loss-lower-bound` | `probed` | (1)(2)(3)(4)(5) **すべて**。⚠ **核 step 2–4 は証明済** (係数相殺)。⚠ R2 と step 1 を共有 |
| R4 | `X` からのチャネル `W` が `Z` に **more capable で支配される**なら `K(U) := max_{p(v\|u,x)}[I(V;Z) − I(U;V)]` は `I(X;Z) − I(U;W)` で上から抑えられる ((H5))。`W = Z` で既存の `I(X;Z\|U)` に一致するので、支配チャネルの族上で最大化した `Λ_env` は**構成から `Λ_sup` 以上**の証明された下界になる | C | `dominated-channel-envelope` | `probed` | (1)(2)(3)(4) / **(5) は (H5) についてのみ通過** (問い 2 `Λ_env` は FAIL 寄り保留・問い 3 は FAIL = 既知側)。⚠ **核 (H5) は証明済**、⚠ **族の admissibility (step 5) は `gap`**。⚠ R2 / R3 と step 1 を共有し、**R3 の step 7 の強化**でもある (同じ `Λ_comb` に合流) |

**R1 は (5) novelty gate を通らなかった。経路として昇格しない。**
L8 が一次文献 3 本を逐語取得した結果、**R1 の 12 step は 1 本残らず出典が付いた** — 主要な出所は
Anantharam–Gohari–Nair (IEEE TIT 65(3), 2019、以下 [AGN19]) の式 (2) / (9)(10)(11) / (20)(21)(22) +
Lemma 2、Gohari–Liu–Nair (ISIT 2025、以下 [GLN25]) の式 (1) + Appendix、Nair (ITA 2020、以下 [N20])
の式 (1)(2) + Definition 1.4。**含意としては 1 step も落ちていない (R1 は正しい)** が、新規性はゼロで
ある。対応表と逐語は [`bc-facts.md`](bc-facts.md) §L8 の (iv) 行。
本文書は**その判定を反映した後の姿**として残す — R1 は経路ではなく、「軸 E の開いている場所が
どこか」を逐語出典つきで確定させた**地図**である (→「L8 後に残るもの」)。

---

## 合流 — 3 本が与えるもの

> **本節は新しい主張を含まない** — R1〜R4 の各節と [`bc-facts.md`](bc-facts.md)
> §L13 / §L15 / §L16 / §L17 / §L18 に既にあるものを 1 か所に集めただけである。
> ⚠ **本節の `(H1)`–`(H6)` は facts §L13–§L18 の番号**であり、R1 節が使う `(H1)(H2)(H3)`
> (Conjecture 1 / タイトさ / `M(T)` の閉凸性) とは**別物**である。
> ⚠ **run をまたいだ数値を混ぜない** — 以下の数値には「どの leg の同一 run か」を必ず添えてある。

### A. 何が得られたか

**共通の主語 (step 1)**。(H1) により

```
gap(T,p) = min{H(X|Y), H(X|Z)} − L'(T,p)
```

が**厳密に**成り立つ (L13、係数相殺による証明)。ゆえに「鋭いチャネル依存スラックを作る」と
「非負 4 項損失 `L'` を下から抑える」は**同じ問題**であり、R2 / R3 / R4 はこの step 1 を共有する。

**産物**。`L'` の証明された下界を合わせた

```
Λ_comb := max{Λ_split, Λ_cav, Λ_env}  ≤  L'
  ⟹  gap(T,p) ≤ min{H(X|Y), H(X|Z)} − Λ_comb
```

が合流点である。`Λ_comb` は **2 つの周辺 `q(y|x)`, `q(z|x)` と `p(x)` だけで計算できる**
(同時分布に触る量を含まないので P3 の ill-posed を避ける制約を満たす。facts §L13)。

**各枝の射程 (実測)** — ⚠ **ここが本節の要**:

- **`Λ_split` / `Λ_cav` (R3)** は `|X| ≤ 2` で**厳密** (base case が [GJNW13] Theorem 1) ⟹ **(N1) を
  構成から満たすのはこの枝だけ**で、証明書もある (`BSC(.1)/BEC(.5)` の `p = 0.2/0.8` で
  `Λ_split − Λ_sup = +0.013540`、L17 run) ⟹ **落とせない**。
- 同じ枝が **`|X| ≥ 3` の dense では桁で緩い** — **L17 の同一 run、dense `|X|=3` の 10 本**で
  残距離 `L'推定 − Λ` の中央が `Λ_split 0.522889` / `Λ_cav 0.363557` に対し `Λ_env 0.002955`。
- **`Λ_env` (R4)** が dense で主。**`max` を取る枝の実測分布**は、L17 の同一 run の dense 10 本が
  **10/10 で `Λ_env` 枝**、`|X| ≥ 3` の 140 本 (L17 run) で `max{Λ_split, Λ_cav} > Λ_sup推定` が **0 本**。
- ⚠ **`Λ_env` の支配が*証明された*わけではない** — 上の `0/140` は「率」ではなく**探索強度に依存する
  下界**である。決着が付くのは逆向きだけ (`Λ_split > Λ_sup推定` が 1 本出れば支配は反証される)。

**R2 だけ主語が別**である。R2 が決めにいくのは `f*(k) = D(k)`、すなわち**アルファベットサイズ `k`
だけの定数**であってチャネル依存の下界ではない (`D(2) = 0` / `D(3) = log₂φ = 0.694242…` /
`D(m²) = log₂ m`)。`Λ_comb` には合流しない。

### B. 証明済 / probed / gap の内訳

⚠ **「証明済」と書けるのは記号的な証明 (係数相殺 / 不等式の連鎖) だけ**である (親 plan §2.1 —
**掃引の成立は証拠であって証明ではない**)。

| 命題 | 主張の 1 行 | 状態 | 証明の型 | 所在 |
|---|---|---|---|---|
| **(H1)** | `gap(T,p) = min{H(X\|Y),H(X\|Z)} − L'(T,p)` (Markov を使わない 5 変数恒等式) | **証明済** | 記号的な係数相殺 (残差ちょうど 0) | facts §L13 / R2 の (H1) 節・R3 step 1・R4 step 1 |
| **(H2)** | 比較補題 `obj(f,g) − max{I(X;Y),I(X;Z)} ≤ H(f(X),g(X)) − max{H(f(X)),H(g(X))}` ⟹ P1 ⟹ P2 | **証明済** | DPI 2 回 + 場合分け | facts §L13 |
| **(H3)** | 超過の内訳: `L' − Λ_sup` は `U` の非最適性 + `D1 := I(X;Z\|U) − I(V;Z\|U)` + `D2 := I(U;V\|Z)` にちょうど分かれ、後ろ 2 項は非負 | **証明済** | 記号的な係数相殺 | facts §L15 |
| **(H4)** | `X` の任意の関数 `W = m(X)` について `L'(p) ≥ Σ_w p(w) L'(p_w)` (grouping 超加法性) | **証明済** | 記号的な係数相殺 (無仮定の線形恒等式 4 本) | facts §L15 / R3 step 2–4 |
| **(H6)** | 任意の `p(w\|x)` (非決定論的 `W`) でも同じ超加法性が**罰項を足さずに**成立 ⟹ `L'(·,q)` は `p(x)` の凹関数。⚠ **(H4) を含む拡張かつ強化** (`T` 形の罰項が `H(W)` → `I(X;W)` に下がる。決定論的 `W` では (H4) に戻る) | **証明済** | 記号的な係数相殺 + 条件付き独立性 | facts §L17 / R3 step 3' |
| **(H5)** | `W` が `Z` に more capable で支配されるなら `K(U) ≤ I(X;Z) − I(U;W)` | **証明済** | 既知不等式の記号的な連鎖 (⚠ 古典鎖ではない) | facts §L16 / R4 step 3 |
| **cell 証明書** (R4 step 5′) | admissibility を有限個の不等式へ還元する (`I_p(X;W) = A_r(p) − D(pW‖r)` + `p ↦ I_p(X;Z)` の凹性) | **補題は証明済**。個々の `W` は **`probed (per-instance certified)`** — L18 run で `Λ_env` の計算に使う全数 71 本が `CERT` (INDET 0 / REFUTE 0) | 補題は記号的。個々の判定は有限個の不等式 + 浮動小数点 (`mpmath` 50 桁で同一判定) | facts §L18 / R4 step 5 / 5′ |
| **閉包補題** | `{Z}` の閉包演算だけで作れる `W` は `𝒟_Z = {Z∘Q}` を出られず、そこでは `Λ_env = Λ_sup` (利得ちょうど 0) ⟹ **資格の穴は本質的** | **証明済** | 4 行の記号的証明 (閉包 4 演算 + DPI) | facts §L18 / R4「資格の穴は本質的である」 |
| **`Λ_split` / `Λ_cav` の下界性** | `Λ_split ≤ Λ_cav ≤ L'` | **証明済** | (H4)/(H6) の再帰 + base case = [GJNW13] Theorem 1 (`Λ_cav` は単体格子上の LP に落とすと下界性が保たれる) | R3 step 6 / 6' |
| **`Λ_env` の下界性** | `Λ_sup ≤ Λ_env ≤ L'` | **証明済** (⚠ 使う `W` の資格は上の cell 証明書の階級) | (H5) + 2 段最大化の入れ替え (R4 step 2) | R4 step 2–6 |
| **R2 step 9** | `f*(k) ≤ D(k)` (決定論的な格子 BC が極値である) | **`gap` — 証明も反例も無い** | — (`k = 3` の掃引が超えられなかったのは証拠であって証明ではない) | R2 step 9 |
| **族 `𝒲` の一般的記述定理** | 一般の `W` を族に入れる資格の*定理* (R4 step 5 (a)) | **`gap`** | — (判定可能な文献の道具はすべて `ln` を証明するが、`ln` の利得は定義から 0) | R4 step 5 (a)(c) / facts §L18 |

### C. 留保 (数える前に必ず併記する)

親 plan §3.3 / §7 の 3 つ:

- **(a)** 3 本とも **step 1 ((H1)) を共有する**。
- **(b)** **R4 は R3 の step 7 (`Λ_sup`) の強化でもあり**、産物は同じ `Λ_comb` に合流する
  (`Λ_comb := max{Λ_split, Λ_env}` へ更新) ので**完全に独立な 3 本ではない**。
- **(c)** **R4 の新規性は (H5) に局限される** (`Λ_env` という対象も構造的な読みも gate は既知側と
  判定した。facts §L16 の文献行)。

⚠ **加えて、鋭さは詰まっていない**:

- **L17 の同一 run、dense `|X|=3` の 10 本**: `Λ_comb` の残距離の中央は `0.002955`
  (old = `max{Λ_split, Λ_env}`) → `0.002955` (new = `max{Λ_cav, Λ_env}`)、`gap > 1e-3` の 7/10 本での
  `slack/gap` の中央も `1.160` → `1.160` で**1 桁も動かない**。
- **L18 の同一 run・同一インスタンス集合、dense `|X|=3` の 10 本**: 残距離の中央は
  `0.002984` (`Λ_sup`) → `0.002640` (`Λ_env`) → `0.002644` (`Λ_env^cert`)。⟹ **L18 の出口条件 (B) が
  測ったのは「証明済に制限して改善が消えないか」であって、改善そのものの大きさではない**。

⟹ **「経路が 3 本立った」を「未解決問題に近づいた」と読まないこと**。L17 / L18 で動いたのは
*証拠の階級* (R4 step 5 の `gap` → `probed (per-instance certified)`) であって、`Λ_comb` と真の `gap`
の距離ではない。

### D. 次の relay (層 3 = 形式化) へ渡すもの

⚠ **形式化そのものは本 relay のスコープ外** (親 plan §2.1)。以下は引き継ぎだけである。

- **形式化する価値があるのは B の「証明済」の側だけ**である。そしてそれが与えるのは
  **「証明された計算可能な下界の機械検証」**であって、**未解決問題の解決ではない** — `Λ_comb` が
  与えるのは `gap` の上界を計算する手続きであり、`T` の閉形式ではない。
- **着手順の推奨**:
  1. **(H1)** — **全経路の step 1** なので最初。これが無いと R2 / R3 / R4 のどれも主語を持たない。
  2. **(H6)** — 証明済の中で**最も強く、かつ (H4) を含む** (拡張かつ強化) ので 2 本が 1 本で済む。
- **在庫** (現物は R1 の「記法と前提」節): 関係する Lean 資産は
  `InformationTheory/Shannon/BroadcastChannel/MartonUnion.lean` の **`martonRegionUnion`** で、これは
  **2 補助変数版で共通補助 `U₀`/`W` を持たない**。⚠ **我々のこの `def` の凸性は未証明**である
  (module docstring 逐語 "Convexity is claimed of the quadrilaterals only, not of the union"、facts V5)。
- **形式化しても閉じない場所は 2 つ**: **R2 step 9** (`f*(k) ≤ D(k)`) と **族 `𝒲` の一般的記述定理**
  (R4 step 5 (a))。どちらも B の表で `gap` であり、Lean に載せても未証明の穴がそのまま残る。

---

## R1 — `F` は「`{a_x}` ↔ `θ` の Legendre」ではなく `min_α max_p min_a` の 3 段入れ子の中段である

> **経路ではない** — L8 の novelty gate に不合格 (死因 `known-result`) で昇格しない。本節は
> **軸 E がどこで開いているかを逐語出典つきで確定させた地図**として残す。⚠ **圧縮版** —
> 12 step の詳細表 / 仮説の性質の表 / P2 の probe 設計 / 殺され方の詳細は git 履歴にあり、
> 逐語の SoT は [`bc-facts.md`](bc-facts.md) §L8 (7 行) である。

### 主張と死因

**主張 (1 文)**: 有限アルファベットの DM-BC `T(y,z|x)` と 3 補助変数 Marton 内界 `M(T)`
(= Jog–Nair Bound 1、私信のみ、補助 `(U,V,W)`) について、**(H1)** Gohari–Liu–Nair Conjecture 1 が
`F` の内部パラメータ `(α, λ)` の**すべての値**で成り立ち、**(H2)** `F` から作る双対値 `h̄` が
`M(T)` の support function `h^T` に一致し (タイトさ)、**(H3)** `M(T)` が閉凸であるなら、
`h_n^T(θ) := sup_{R ∈ M(T^{⊗n})} θ·R` は `h_n^T(θ) = n·h_1^T(θ)` を全 `n ≥ 1` / 全 `θ ∈ ℝ²₊` で
満たし、外部ノートの同値条件 (3) により `C(T) = M(T)` が従う。
⚠ **R1 は Conjecture 1 を証明しない** — (H1) は未解決の予想そのもので、R1 は含意である。

**死因 = `known-result` (L8)**。一次文献 3 本 — Anantharam–Gohari–Nair (IEEE TIT 65(3), 2019、
[AGN19]) / Gohari–Liu–Nair (ISIT 2025、[GLN25]) / Nair (ITA 2020、[N20]) — を逐語取得した結果、
**R1 の 12 step は 1 本残らず出典が付いた**: step 1–5 = [AGN19] 式 (2) の各段 (ステップ (b) は逐語
"a max-min theorem of Terkelsen"、(c) は逐語 "upper concave envelopes" + Remark 1) と式 (9)(10)(11)
(Fenchel、逐語 `f†(d) = max_{p(x)}{f(p(x)) − Σ_x d_x p(x)}`)、**step 6 = [GLN25] 式 (1) そのもの**
(`=` であって `≤` ではなく Appendix A に証明)、step 7–9 = [AGN19] 式 (21)→(22)→**Lemma 2**、
step 10–12 = [AGN19] 式 (20) の逐語 "Marton's inner bound is optimal **if and only if** …" +
[N20] 式 (1) + Remark 1.2。**含意としては 1 step も落ちていない (R1 は正しい) が、新規性はゼロ**で
ある (対応表と逐語は facts §L8 (iv))。あわせて **(H2) は仮定ではなく既知の定理** ([GLN25] 式 (1) +
Appendix A、facts §L8 (ii))、**(H3) は [AGN19] §I-B の地の文**の主張 (逐語 "Since Marton's achievable
region is a convex set in R³₊ …"、⚠ 証明は付いていない。facts §L8 (vi)) と判明した
⟹ **残る仮定は (H1) = Conjecture 1 ただ 1 本**。

**⟹ L8 後の総括: R1 は数学的にはどこも壊れなかった。壊れたのは新規性の側である。**
最初に壊れると見た step 6 (タイトさ) も (H1) の一様な読み (step 7) も `M(T)` の閉凸性 (step 12) も、
L8 の実測ではどれも壊れなかった。**軸 E で残っている未解決は (H1) = Conjecture 1 ただ 1 本**。

### 記法と前提

- `θ` は受信者ラベルの入替で **`θ = (1, μ)`, `μ ≥ 1`** と正規化してよい。⚠ **`λ` が 3 つ衝突する**
  ので本節では 方向 `θ` / `α` (外部ノートの `λ-SRM`) / `μ` (`F` の定義中の `λ`) と呼び分ける。
  `M(T)` の 4 制約 `A₁, A₂, S₁, S₂` の逐語は facts F2、`F` のエントロピー項は逐語
  **`−αH(Y) − (λ−α)H(Z)`** (facts §L8 (ii)。facts V7 の転記 `(1−α)` は誤り)。**上凹包を取る対象**
  (step 3、`marton-3aux-probe` に要るのはこれ 1 本) は
  `G_{α,μ}(q) := max_{(U,V): 入力周辺 = q}[I(U;Y)+μI(V;Z)−I(U;V)] − α·H(Y_q) − (μ−α)·H(Z_q)`。
- ⚠ **在庫との差 (最重要の前提)**: 我々の Lean `def`
  `InformationTheory/Shannon/BroadcastChannel/MartonUnion.lean` の **`martonRegionUnion` は
  2 補助変数版で共通補助 `U₀`/`W` を持たない** ⟹ **R1 は我々の `def` の上では述べられない**
  (`W` が step 3 の上凹包を担うため)。⚠ **我々のこの `def` の凸性は未証明である** (module docstring
  逐語 "Convexity is claimed of the quadrilaterals only, not of the union"、facts V5)。
  **(H3) の閉凸性は 3 補助版 `M(T)` についての言明で、対象が違う** — facts V5 の判定は動かない。
- **記号的検算 16 項目** (step 2 / 3 / 4 の恒等式 + 4 制約まわりの関係式) は [`bc_probe.py`](bc_probe.py)
  の `prove_identity` で残差ちょうど 0。再検証は `python3 docs/shannon/bc-route-r1-check.py`。
  ⚠ **このスクリプトは R1 の主張そのものを probe しない** (振っているのは四辺形の形であって実在
  チャネルの最大化点ではない)。

### L8 後に残るもの

- **(a) facts §L2 が「どちらの文献も供給していない」とした 2 段は、両方とも供給されていた。**
  (a) `M` の閉凸性 = [AGN19] §I-B の地の文 (facts §L8 (vi))、(b) `{a_x}` と `θ` を結ぶ双対の段 =
  [AGN19] 式 (2) + (9)(10)(11) (同 (iv))。L2 が誤ったのは**二次文献 ([GLN25]) だけを読んでいたため**
  ⟹ attack `support-vs-dual-additivity` の標的は**文献の側では最初から閉じていた**。
- **(b) facts ① の「同一命題ではない」に構造的な理由がつく。** `F` の加法性は `σ = 1` と `(α,μ)` を
  固定した層の主張、support function の加法性は `min_α` を取った後の主張で、`min_α` は自己テンソル
  には透過だが `T₁ ≠ T₂` では `≥` しか出ない (step 10) ⟹ 「Conjecture 1 は `T₁ ≠ T₂` を許すのに
  異なる 2 チャネルの積で Marton 領域が Minkowski 和より真に大きい例が既知」という緊張は
  **`min_α` の段でちょうど解消される** (facts F5 の非因子分解と衝突しない)。
- **(c) 外部ノートの否定側 (11) が「どこを壊せばよいか」に翻訳される。** step 1–11 が正しければ
  自己テンソルで `h_n > n·h_1` を出すことは (H1) または (H2) の反証と同値で、step 7 が要求するのも
  **分離可能な `a` だけ** ⟹ **反例を探す場所は `(α,μ)` 平面上の Conjecture 1 の破れか、step 6 の
  タイトさの破れの 2 つに絞られる**。
- **(d) `markovity-via-dual-F` (軸 G) の park が解ける方向を指す。** L5 で M2 が偽になったのは共通
  補助 `W` を落としたからだった (facts §L5) が、R1 は `W` の効きどころを step 3 (上凹包) として
  特定するので、Conjecture 2 を領域側 `h` ではなく双対側 `F` で扱う受け皿ができる。
- **(e) 前提工事 `marton-3aux-probe` の仕様が決まる。** R1 は 3 補助版の話なので我々の Lean `def` の
  上では述べられないが、**何を作ればよいか**は指定する — probe 側に要るのは「`W` を持つ領域」では
  なく「`Δ(X)` 上の `G_{α,μ}` とその上凹包」1 本である (step 3)。

### 骨格の訂正 — `{a_x}` と `θ` の間に Legendre 変換は存在しない

L7 のブリーフが与えた骨格を検算した結果、**2 点が通り、1 点が要修正、1 点が誤り**だった。通った
2 点は step 4 の Fenchel の形 (`F(T,{a}) = max_p[G+⟨a,p⟩]`, `conc(G)(p) = min_a[F−⟨a,p⟩]`) と
「共通補助 `W` は上凹包を実現する変数である」で、後者は **[AGN19] Remark 1 逐語** "the random
variable W plays the role of mixing between various distributions of p(u, v, x), to achieve the
concave envelope." で確認された (L5 の留保は解除。⚠ 同時に既知でもある)。

- **要修正**: 上凹包を取る対象は `W` を持たない Marton 目的値**そのものではない** — エントロピー項
  `−α·H(Y_q) − (μ−α)·H(Z_q)` を**含んだ** `G_{α,μ}` を凹化しないと `F` と合わない (step 3)。
  [AGN19] の `t_λ(X)` も受信者側の情報量項を含んだ形で定義されている (`H` 形と `I` 形の差は `p(x)`
  について線形ゆえ `{a_x}` に吸収される)。
- **誤り (as framed)**: **`{a_x}` と `θ` の間に Legendre 変換は存在しない**。`θ` は `F` の**内部
  パラメータ `(α, μ)` として現れる** (step 2: `μ` が方向そのもの、`α` が総和レート制約 2 本への
  乗数の配分) のであって、`a` と共役な関係には無い。正しい絵は **`min_α max_p min_a` の 3 段入れ子**
  で、`a` は最内層、`θ` は最外層の**パラメータ**である。

**この訂正が実質的である理由**: 「2 つの双対化の合成」のままだと `θ` 側にも Fenchel 変換を探しに
いくことになり、存在しないものを探す leg を 1 つ溶かす。実際に必要なのは**線形計画双対 (step 1) →
パラメータ同定 (step 2) → 上凹包 (step 3) → Fenchel (step 4) → ミニマックス交換 (step 5)** という
別種の 5 段で、**Legendre 変換が現れるのは step 4 の 1 箇所だけ**である。⟹ attack slug
`support-vs-dual-additivity` の説明文「`{a_x}` ↔ `θ` の Legendre / 上凹包の段」は誤解を招くので、
台帳側の文言は「`F` の内部パラメータ `(α,λ)` と方向 `θ` の対応 + 上凹包の段」に直すのが正しい。

### 未達の条件と次の一手 — P3 だけが生きている

- **P1 (実施済 — L8)**。一次文献 3 本の取得と 3 点の判定 = (i) 量化子は**一様な読みが正しい**
  (決定打は [N20] が同じ対象を `F₁^{(λ,α)}` と書き "For λ ≥ 1 and α ∈ [0,1], define the following"
  と量化し Definition 1.4 "global tensorization property" と名付けていること) / (ii) `F` ↔ Marton
  領域は述べられており**等号 (タイト)** / (iii) 範囲は 3 本すべてで明示。逐語 7 行は facts §L8。
- **P2 (取り下げ — 目的が消えた)**。step 6 のタイトさの数値 probe。step 6 は [GLN25] 式 (1) として
  既知で、L7 が「残余」とした `S₁ ≥ A₂` も [AGN19] が `[·]₊` の切断で処理済 (facts §L8 (vii))。
  ⚠ **設計の規律だけは捨てない** — 「2 つの下界を比べる設計にしてはならない / 同一の `p` の上で述語
  だけを見る」は 3 補助版を数値で触るとき (`marton-3aux-probe` 等) にそのまま要る。
- **P4 (価値が下がった)**。`argmin_α Λ_{α,μ}(T₁) ≠ argmin_α Λ_{α,μ}(T₂)` となる 2 チャネルを出す
  probe。step 10 の非可換性は文献が既に踏まえている ([N20] Remark 1.3 逐語 "slightly more general
  than is needed") ので既知の再確認にしかならない (優先度は最低)。
- **P3 (生きている、しかも L8 で主語が強くなった)**。`|X| = 2` (⟹ `|X²| = 4`) で `conc(G^{T⊗T})(p)`
  を単体グリッド上の総当たり上凹包で直接計算し、**相関のある** `p` について
  `conc(G^{T⊗T})(p) ≤ conc(G^T)(p₁) + conc(G^T)(p₂)` を検定する。**破れが出れば Conjecture 1 の
  反例**。肯定コントロール = `p = q⊗q` で両辺が一致すること。
  **L8 の発見**: これは **[AGN19] 式 (22) の破れの探索そのもの**で、同論文が **Lemma 2** で双対形
  (`F(T₁,{a}) + F(T₂,{b}) < F(T₁⊗T₂,{a+b})`) に書き換えたうえで実際に走らせ、逐語 "we were unable
  to find any such instance" と報告している。**そして著者らが逐語で独立再現を求めている**: "since
  this is a rather central problem in network information theory, we would also urge other interested
  researchers to **independently confirm our numerical observations**." ⟹ **P3 は「新しい問い」では
  ないが、著者自身が公開で要請している独立再現**であり、軸 E で実際に着手できる作業として残る。
  ⚠ **探索空間も原論文が指定済** — (a) [AGN19] が逐語で "the main nontrivial cases" と名指す
  `|U| = 2, |V| = 3` と `|U| = 3, |V| = 2` を外さない、(b) 空間は同論文 Proposition 3 / 一般化
  AND・XOR パターンの排除で大幅に削れる (手順は原論文にある)。

⚠ **通っても未解決問題は解けない、かつ新規性もゼロである** — [GLN25] Conclusion が逐語 "Even if one
can prove the additivity within this class …, one would have shown that Marton's region is the
capacity region." と述べる以上、**「(H1) ⟹ Marton = 容量」という含意自体は著者らが既に持っている**
(facts V7)。R1 が残す価値は 3 点に限られる: **(1) 軸 E で新規性を主張しうるのは Conjecture 1 の証明
か反証だけ**である (他の 11 step はすべて文献にある)、(2) (c) の反例探索の絞り込みが既知の枠組みの
上で正しいと確認された、(3) 記号的検算 16 項目 = 二次文献の転記から原典の式を復元できた記録 (誤りは
1 件だけ出た — `F` の第 2 項は `−(λ−α)H(Z)` であって facts V7 の `−(1−α)H(Z)` ではない。R1 の代数は
すべて `(λ−α)` 側で組んであるので経路は無傷)。

---

## R2 — `|X| ≥ 3` の鋭いスラックは決定論的な「格子」チャネルで達成される

> 軸 C (`jog-nair-inequality`)。**前提となる一次文献の逐語は
> [`bc-facts.md`](bc-facts.md) §L10 の C1–C8 行が SoT**。
> **本節の数値・記号的検算の再検証**: `python3 docs/shannon/bc-jognair-general-check.py`
> (約 160s、`--quick` 約 25s)。乱数種と局所最適化の開始点を固定してあるので
> **再実行で報告値は一致する** (実行時間行を除く)。

### 主張 (条件 1)

有限アルファベットの 2 受信者 DM-BC `q(y,z|x)` と、`(U,V) → X → (Y,Z)` を満たす任意の
5 つ組について

```
I(U;Y) + I(V;Z) − I(U;V)  ≤  max{I(X;Y), I(X;Z)} + D(|X|)
```

が成り立つ。ここで `D(k)` は **`|X| = k` の決定論的 BC** (`Y = g(X)`, `Z = h(X)`) だけを
渡る次の量:

```
D(k) := max over (g, h) with |X| = k, max over p(x) of  [ H(Y,Z) − max{H(Y), H(Z)} ]
```

⚠ **`min{H(X|Y), H(X|Z)}` と書いてはいけない** — `(Y,Z)` が `X` を復元する場合 (格子 BC)
だけ両者は一致する。`g = h = 定数` のような復元しない `(g,h)` では `min{H(X|·)} = H(X)`
に跳ね上がるが、実際の `gap` は 0 である。上の形は復元条件を課さずに正しい値を返す。

同値な言い方 (以下ではこちらを使う)。[GNA12] の記法で `T(X) := max_{p(u,v|x)} I(U;Y) +
I(V;Z) − I(U;V)` (`p(x)` とチャネルの関数)、`gap(T,p) := T(X) − max{I(X;Y), I(X;Z)}`、
`f*(k) := sup over |X| = k のチャネルと p(x) の gap` とおくと、**R2 は `f*(k) = D(k)`**
である (`≥` は witness で確定するので、内容は `≤` の側)。

**退化条件**: `D(2) = 0` なので `|X| = 2` では R2 は [GJNW13] Theorem 1 **そのもの**に
なる (弱くならない)。`D(3) = log₂φ = 0.694242…` (φ = 黄金比)、`D(m²) = log₂ m`。

⚠ **R2 は「正しい一般化」の候補として弱い**ことが同時に分かっている — 下の step 8 が
示すとおり **完全平方 `k = m²` では `D(k)` が 3 行で出る自明上界と一致する**ので、そこでは
主張の中身が消える。中身が残るのは非平方の `k` (最小の例が `k = 3`) だけである。この
限界は「殺され方」の 2 番目に書いた。

### 記法と前提

| 記号 | 意味 | 出所 |
|---|---|---|
| `T(X)` | `max_{p(u,v\|x)} I(U;Y)+I(V;Z)−I(U;V)`。`p(x)` とチャネルの関数 | **[GNA12] §I-B 逐語**の定義 (下の novelty gate) |
| `gap(T,p)` | `T(X) − max{I(X;Y), I(X;Z)}` | 本文書 |
| `f*(k)` | `\|X\| = k` を渡る `gap` の sup | 本文書 |
| `D(k)` | 上の決定論的 BC 版 | 本文書 |
| φ | 黄金比 `(1+√5)/2` | — |
| 「格子」BC | `(Y,Z)` が `X` を復元する決定論的 BC。`X` が `\|Y\| × \|Z\|` の格子の部分集合に埋まることと同じ | 本文書 |

**前提はカーディナリティ制約 `|X| = k` と Markov 連鎖 1 本だけ** — Theorem 1 と同じで、
チャネルにも `p(x)` にも条件を置かない (facts C1)。補助変数は `|U|, |V| ≤ |X|` に制限して
よい ([GJNW13] §II-A Remark 3 逐語、facts C3)。

### ステップ (条件 2 + 3)

| # | 中間命題 | 入力 | 出力 | ラベル | 根拠 / 再検証 |
|---|---|---|---|---|---|
| 1 | `H(X) − [I(U;Y)+I(V;Z)−I(U;V)] = [I(U;X)−I(U;Y)] + [I(V;X)−I(V;Z)] + H(X\|U,V) + I(U;V\|X)` | なし (恒等式) | `LHS ≤ H(X)` (4 項が Markov 下で非負) | `new` (初等) | **係数相殺で証明済** = probe §1 の (I1)(I2)。DPI 項は `I(U;X)−I(U;Y) = I(U;X\|Y) − I(U;Y\|X)` で、Markov から第 2 項が 0。⟹ **L13 の (H1) が本 step と step 2 を等式まで閉じた** (下の (H1) 節) |
| 2 | `max{I(X;Y),I(X;Z)} = H(X) − min{H(X\|Y),H(X\|Z)}` ⟹ `gap ≤ min{H(X\|Y), H(X\|Z)}` | step 1 | 上界その 1 | `new` (初等) | probe §1 の (I4)。⟹ **(H1) 節で `gap = min{H(X\|Y),H(X\|Z)} − L'` の等式に閉じる** (本 step の不等式は `L' ≥ 0` の系) |
| 3 | `I(X;Y)+I(X;Z) − LHS = [I(X;Y)−I(U;Y)] + [I(X;Z)−I(V;Z)] + I(U;V)` ⟹ `gap ≤ min{I(X;Y), I(X;Z)}` | なし (恒等式) | 上界その 2 | `new` (初等) | probe §1 の (I3) |
| 4 | `2·gap ≤ I(X;Y) + H(X\|Y) = H(X) ≤ log₂ k` ⟹ **`f*(k) ≤ ½ log₂ k`** | step 2, 3 | 証明済の上界 | `new` (初等) | 掃引ではなく証明。以後この量を「自明上界」と呼ぶ |
| 5 | 決定論的 BC では `U = Y, V = Z` が `LHS = H(Y,Z)` を出す ⟹ `gap = H(Y,Z) − max{H(Y),H(Z)}`。とくに `(Y,Z)` が `X` を復元する格子 BC では `LHS = H(X)` かつ `gap = min{H(X\|Y),H(X\|Z)}` で step 2 の上界に**一致** ⟹ `f*(k) ≥ D(k)` | step 2 | 下界 (明示 witness) | `new` (初等) | 構成は原典の反例と同型 ([GJNW13] §I-B-2 逐語 "consider U = Y, V = Z and X ~ [1/3,1/3,1/3]"、facts C6) |
| 6 | `D(2) = 0`: `\|X\| = 2` では `g` も `h` も「定数」か「単射」のいずれか。両方定数なら `H(Y,Z) = 0 = max{H(Y),H(Z)}`、少なくとも一方が単射ならその側で `H(Y,Z) = H(X) = max{H(Y),H(Z)}` ⟹ 4 通りすべてで `gap = 0` | — | **`\|X\| = 2` で R2 = Theorem 1** | `new` (初等) | 退化条件。probe §8 の `k = 2` 行が数値でも 0 |
| 7 | `D(3) = log₂ φ`。3 セルの L 字形で `p = (q, 1−2q, q)` とおくと `min{H(X\|Y),H(X\|Z)} = −(1−2q)log((1−2q)/(1−q)) − q log(q/(1−q))`、内点条件は `5q² − 5q + 1 = 0` ⟹ `q* = (5−√5)/10`、そこで `(1−2q)/(1−q) = 1/φ`, `q/(1−q) = 1/φ²` ゆえ値は `((1−2q)+2q)·log₂φ = log₂φ` | step 5 | `f*(3) ≥ log₂φ` | `new` | 閉形式は手計算、**決定論的 `\|X\|=3` チャネル 729 通りの全列挙**で最大が `log₂φ` (差 `+4.4e-16`) であることを確認 (probe §7(b)) |
| 8 | `a × b` 格子の一様分布は `min{log₂ a, log₂ b}` を与えるので `D(m²) ≥ log₂ m = ½log₂(m²)`。step 4 と合わせて **`f*(m²) = log₂ m` が確定** | step 4, 5 | 完全平方での確定値 | `new` (初等) | probe §8 が `k = 4` と `k = 9` で下界 = 自明上界を数値でも確認。⚠ **同時にそこでは R2 の中身が消える** |
| 9 | **`f*(k) ≤ D(k)`** — 決定論的な格子 BC が極値である | step 1–8 | 主張の残り半分 | **`gap`** | **経路の核。証明も反例も無い**。`k = 3` については掃引が超えられなかった (下記 probe) が、それは証拠であって証明ではない |
| 10 | step 5 + step 9 ⟹ `f*(k) = D(k)`。`k = 2` で Theorem 1 を回復し、`k = 3` で `f*(3) = log₂φ` | step 9 | 結論 | 9 に従属 | — |

**ラベルについての注記 (§5-8)**: step 1–8 を `new` と表示したのは**新規性の主張ではない**。
どれも数行の初等的議論で、ほぼ確実に folklore だが、**逐語の出典を取れていない**ので
`known` (出典つき) を名乗る資格が無い、というだけである。実質を担うのは step 9 のみで、
そこは `gap` と表示してある。

### (H1) 厳密なスラック恒等式 — step 1 と step 2 を等式として閉じたもの (L13)

step 1 と step 2 は上界を与える不等式として書かれているが、**両者は等式まで閉じる**。L13 が
`prove_identity` で係数相殺により証明した (掃引ではない、Markov 連鎖も使わない) 5 変数の恒等式:

```
H(X) − [I(U;Y) + I(V;Z) − I(U;V)]
  = I(U;X|Y) + I(V;X|Z) + H(X|U,V) + I(U;V|X) − I(U;Y|X) − I(V;Z|X)
```

Markov 連鎖 `(U,V) → X → (Y,Z)` の下では減算 2 項が消える (機械確認、最大 `1.922e-15`)。
`I(X;Y) + H(X|Y) = H(X)` とその `Z` 版 (どちらも係数相殺で証明済) を併せ、

```
L'(T,p) := min over p(u,v|x) of [ I(U;X|Y) + I(V;X|Z) + H(X|U,V) + I(U;V|X) ]
```

と置くと `T(X) = H(X) − L'` かつ `max{I(X;Y),I(X;Z)} = H(X) − min{H(X|Y),H(X|Z)}` ゆえ

```
gap(T,p) = min{H(X|Y), H(X|Z)} − L'(T,p)
```

が**厳密に**成り立つ。`L'` は**非負 4 項の最小**なので、step 2 の上界
`gap ≤ min{H(X|Y),H(X|Z)}` は `L' ≥ 0` の系にあたる。

**この恒等式が R2 に対して持つ意味は 3 つ**:

1. **問いの言い換えが厳密である** — 「鋭いチャネル依存スラック `Φ` を作る」と「`L'` を下から
   抑える」は同じ問題。子候補 C2 の必要条件もそのまま翻訳される: **(N1)** ⟺ `|X|=2` で
   `L' = min{H(X|Y),H(X|Z)}` (= [GJNW13] Theorem 1 の言い換え)、**(N2)** ⟺ 格子 BC で `L' = 0`
   (`U = Y, V = Z` と取ると 4 項が**項別に** 0)。
2. **最適点での 4 項分解が新しい診断になる** — どの項が損失を担うかがチャネルで切り替わる
   (順は `I(U;X|Y) / I(V;X|Z) / H(X|U,V) / I(U;V|X)`)。Blackwell は 2 つの `p` のどちらでも 4 項とも
   ちょうど `0`；平滑化した dense Blackwell (`ε=0.1`) は `0.244929 / 0.270619 / 0.000003 / 0.000004`；
   BSC(0.1) 2 本は `0.000004 / 0.468996 / 0 / 0` で和がちょうど `min{H(X|Y),H(X|Z)}`。
   ⚠ **dense 行の後ろ 2 項は厳密な 0 ではなく `1e-6` スケール**。⟹ **dense チャネルでは `L'` はほぼ
   DPI 2 項で尽きる**ので、次の候補は `(U,V)` を「`X` を復元し `X` の下で条件付き独立」に絞ってよく、
   抑える対象が 4 項から 2 項に減る。
3. **step 9 の表示も変わる** — `f*(k) ≤ D(k)` は「`L'` を下から抑える」形の主張として書き直せる。
   ⚠ **ただし step 9 の状態は動かない** (依然 `gap`)。恒等式は問いを書き換えるだけで、
   決定論的極値性そのものには何も与えない。

⚠ **本節は R2 の確信度ラベルを動かさない** — (H1) は step 1/2 の**強化**であって step 9 ではない。

### 仮説の性質 (親 plan §5-8)

| 前提 | 分類 | 経路上のどこに出ているか |
|---|---|---|
| 有限アルファベット `X, Y, Z` | **precondition** | 記法の節 |
| Markov `(U,V) → X → (Y,Z)` | **precondition** (Theorem 1 と同一) | step 1 の非負性 |
| `\|U\|, \|V\| ≤ \|X\|` | **precondition** (`T(X)` の評価に十分。[GJNW13] Remark 3) | step 5、probe §7(d) が数値でも確認 |
| **決定論的極値性 (step 9)** | **核。仮説に押し込まず `gap` として表示** | step 9 |

**核を前提に押し込んでいないことの確認**: R2 は「〜を仮定すれば」の形をしていない。
未証明部分は step 9 ただ 1 つで、それは仮説ではなく `gap` ラベルの中間命題として表に出て
いる。R1 が (H1) を仮定した含意だったのに対し、**R2 は無条件の主張であり、証明の一部が
欠けている**という違いがある。

### novelty gate — 実施結果 (L10)

**判定: 標的の問い自体は open のまま。ただし「誰も攻めていない」ではなく「攻められて
いるが未達」に訂正が要る。** 根拠 4 点:

1. **問いは原典が逐語で open と宣言**: [GJNW13] §V Conclusion 逐語 "The inequality fails
   when |X| ≥ 3 so a natural question is whether there is a correct generalization for
   higher cardinality input-alphabets." (facts C5)。
2. **⚠ facts C8 の訂正 (本 leg の発見)**: C8 は著者 publications ページ通読で「`|X| ≥ 3`
   の一般化を掲げた項目は無い」としたが、**[GNA12] = Gohari–Nair–Anantharam,
   *On Marton's inner bound for broadcast channels*, arXiv:1202.0898 (ISIT 2012) が
   まさにそれを掲げている**。表題に濃度が出ないため見落とされたもの。逐語 (§I-B):
   "Beyond binary input alphabets: The inequality (1) itself fails to hold where |X| = 3,
   for instance in the Blackwell channel. Therefore, we attempt to establish properties of
   the optimizing distributions p(u,v|x) that achieve T(X), in Section III."
   ⟹ **攻め口は「極値分布 `p(u,v|x)` の必要条件」であって「スラック付き不等式」ではない**
   (§III の Theorem 2 = AND 型の一般化、Lemma 1/2)。
3. **`T(X) − max{I(X;Y),I(X;Z)}` の大きさを `|X|` の関数として抑える主張は [GNA12] に
   現れない** (読んだ範囲 = 本文 §I–§IV の通読 + Appendix A/B/C の走査。`Blackwell` /
   `golden` / 数値の全文検索も併用)。Blackwell についての数値も
   無い。⟹ **加法スラックの枝は空いている**。
4. **乗法スラックの枝は空いていない**: [GNA12] Appendix B が定数 `c_{p(u,x)}` (= 全チャネル
   で `I(U;Y) ≤ c·I(X;Y)` を満たす最小の `c`) を導入し、逐語 "If c_{p(u,x)} + c_{p(v,x)} ≤ 1
   then note: I(U;Y) + I(V;Z) ≤ c_{p(u,x)}I(X;Y) + c_{p(v,x)}I(X;Z) ≤ max(I(X;Y), I(X;Z))"、
   Theorem 3 が binary XOR で `c_u + c_v ≤ 1` を示している。⟹ 候補 G3 (乗法形) は数値でも
   死んだ (下記) が、**新規性の面でも先客がいる**。

⚠ **`(secondary)`**: 「加法スラックの枝が空いている」は 1 本の一次文献の通読 + 1 回の
WebSearch による NOT-FOUND であって、未解決であることの一次的裏づけではない。

**⟹ 副産物**: [GNA12] §III の必要条件 (極値 `p(u,v|x)` は AND 型の配置を持てない、行/列は
相異なる像を持つ) は **step 9 を攻める道具になりうる** — 決定論的極値性を示すには極値分布の
構造を絞る必要があり、それは同論文がやろうとしていることそのものである。

### 数値 probe の結果 (条件 4)

再検証: `python3 docs/shannon/bc-jognair-general-check.py`。

**コントロール 3 本 (harness を信用する前に通したもの)**:

| 種別 | 内容 | 結果 |
|---|---|---|
| 肯定 | `\|X\| = 2` のランダムチャネル 2000 本 (`\|Y\|,\|Z\|` も Dirichlet 濃度も振る、`X = f(U,V)` に限定しない) で Theorem 1 が破れないか | **違反 0** |
| 否定 | Blackwell + `U=Y, V=Z` + `X ~ [1/3,1/3,1/3]` で原典の数値を再現 | `LHS = H(Y,Z) = log₂3 = 1.584963`、`max{I,I} = h(1/3) = 0.918296` を**逐語どおり再現** |
| 最大化 | `\|X\| = 2` で `gap` の最大が **ちょうど 0** に張り付くか (Theorem 1 + tightness で真値が 0 と分かっている) | 3 チャネルすべてで `\|残差\| < 6e-12`。`local_max_certificate` に**単体の 8 頂点と 2 つの構造 witness を `directions=` で明示**して増分 `< 1e-9` |

**kill の結果**:

- **G1 (`f(k) = log₂(k/2)`) = `refuted`**。反証は 2 本、いずれも**明示 witness の直接評価
  で最適化を使わない**:
  - Blackwell + 一様: `gap = 2/3 = 0.666667 > log₂(3/2) = 0.584963`。
    ⚠ **これは原典自身の witness である** — 種が「Blackwell でちょうど等号」と見たのは、
    原典が書いた粗い上界 `1 ≥ max{I(X;Y),I(X;Z)}` を等号と読んだためで、真の
    `max` は `h(1/3) = 0.918296` なので破れ幅は `log₂3 − 1` ではなく `2/3` である。
  - Blackwell + `p = (q, 1−2q, q)`, `q = (5−√5)/10`: `gap = log₂φ = 0.694242`
    (違反幅 `+0.109279`)。
- **G1 は k ≥ 4 では空虚**: `log₂(k/2) ≥ ½log₂k ⟺ k ≥ 4` なので、`k ≥ 4` の G1 は step 4 の
  3 行の自明上界の帰結にすぎない。⟹ **G1 はどの `k` でも「偽か自明か」**。
- **G3 (乗法 `ρ(k)`) = `refuted`**。同じ 1 径数族の反対の端 `a → 0` で
  `LHS/max → 2` (`a = 10⁻⁵` で `1.999999`)。step 3 より `ρ ≤ 2` は無条件で成り立つので
  **`ρ*(k) = 2` が全 `k ≥ 3`** (`k > 3` へは `p(x)` の台を 3 点に落とせば同じ族が使える —
  同じ議論で **`f*` は `k` について単調非減少**でもあり、`D_lb(5) = 1 = f*(4)` の行はこれで
  説明がつく) ⟹ 乗法形の真の定数は自明上界と一致し中身が無い。
  (⚠ 同じ族の 2 つの端が別々の候補を殺している — `gap` が最大になる点と比が最大になる点が
  違う。)
- **G2 (sum rate ギャップ `SR_Marton − SR_RTD ≤ g(k)`) は「掃引では殺せなかった」**。
  決定論的格子 4 本 (Blackwell / 2×2 / 2×3 / 3×3) で差は `0.207 / 0.623 / 0.772 / 1.031`、
  いずれも `log₂(k/2)` の下。⚠ **証明ではない**。`SR_Marton` 側は明示の達成点
  (Bound 1 で `W` 定数, `U=Y, V=Z`) が与える**下界**、`SR_RTD` 側も最大化による**下界**
  なので差は上振れしうる (それでも違反しなかったので判定は保たれる)。
  ⚠ G2 は独立な候補ではない — G1 形のスラックから `W = w` ごとの条件付き適用で `g = f` が
  従う。ただし [GJNW13] の証明が `|W| = 2` を使うのに対し `|X| ≥ 3` では濃度限界が
  `|W| ≤ |X|` までしか落とさないので、「`|W| ≤ k` の時分割値 = `|W| = 2` の `SR_RTD`」は
  **`gap` (未検討)**。

**掃引の範囲 (`f*(3) > log₂φ` を探した範囲)**:

| 掃引 | 範囲 | 結果 |
|---|---|---|
| ランダムチャネル | `\|X\|=3`, `\|Y\|,\|Z\| ∈ {2,3,4}`, Dirichlet 濃度 3 通り、400 本。step 2+3 の**証明済上界**で screen し上位 8 本を最大化 | screen 上界の最大が既に `log₂φ` に届かない |
| 決定論的チャネル | `\|X\|=3` の `(g,h)` を **全 729 通り列挙** | 最大 = `log₂φ` (差 `4.4e-16`) |
| チャネル込み同時最大化 | `T(y,z\|x)` も変数にして `\|Y\|=\|Z\| = 2, 3, 4` の 3 通り、各 25 再スタート + Blackwell/黄金比の**構造 seed を明示** | どれも `log₂φ` を超えない |
| 補助変数の濃度 | Blackwell で `\|U\|=\|V\|=3` と `=4` を比較 | 差 `< 2e-7` (原典の還元と整合) |

⟹ **掃引では `log₂φ` を超えられなかった**。`f*(3) ∈ [log₂φ, ½log₂3] = [0.694242, 0.792481]`
(下は明示 witness、上は step 4 の証明済上界)。

⚠ **この掃引が見落としをしうる実例が本 leg 内にある**: `|X| = 4` の同時最大化では、真の最大
`1.0` を**ランダム再スタート 20 本が全部外し**、最良でも `0.694242` (= Blackwell の値) に
着地した (差 `0.305757`)。
構造 witness を seed に渡して初めて `1.0` が出る。**偽の「収束した」に見える**ので、
「掃引で出なかった」を「最大がそこにある」と読んではいけない (親 plan §2.1 の警告の実例)。

### 殺され方

**最初に壊れるのは step 9 (決定論的極値性)**。掃引が `|X|=3` で決定論的側に収束したのは
証拠だが、上の `|X| = 4` の実例が示すとおり、**同じ探索が真の最大を 0.3058 bit 外した前例が
ある**。`f*(3) ∈ (log₂φ, ½log₂3]` の witness が非決定論的チャネルに潜んでいる可能性は
排除できていない。次に叩くならここ (探索は `|X|=3` で `H(X) > 2·log₂φ = 1.388` を要求する
= `p` がほぼ一様、`I(X;Y) ∈ (0.694, 0.891)` という狭い窓に絞れる)。

**2 番目に壊れるのは価値の側**: step 8 が示すとおり **完全平方 `k = m²` では `D(k)` が自明
上界に一致する**ので、そこでは R2 に中身が無い。`k = 3` などの非平方でしか意味を持たない
主張を「`|X| ≥ 3` への正しい一般化」と呼ぶのは苦しい。⟹ **加法定数という形そのものが
弱い**というのが本 leg の主要な収穫で、これが下の子候補の起票理由になっている。

**3 番目は `D(k)` 自体の未知性**: probe が出した `D_lb(k)` (`k = 2..9` で
`0 / log₂φ / 1 / 1 / 1.167987 / 1.309641 / 1.449983 / log₂3`) は **`|Y|,|Z| ≤ 3` に制限した
探索の下界**であり、`k ≥ 5` では `D(k)` の閉形式が無い。`k = 3` の `log₂φ` と完全平方の
`log₂ m` 以外に閉形式は取れていない。

**壊れない場所**: step 1–4 は係数相殺で証明済なので偽にならない。step 5–8 は明示 witness
の直接評価 (最適化不使用) なので、偽になるとすれば転記の問題だけである。

### 死因から起票した子候補 (§5-9)

| id | 内容 | 状態 |
|---|---|---|
| **C1** | `f*(k) = D(k)` (= R2 の step 9)。最小の未決着例は `k = 3` で `f*(3) ∈ [log₂φ, ½log₂3]` | R2 の核として本節に収録 |
| **C2** | 定数ではなく**チャネル依存の汎関数** `Φ(T, p)` を作る。witness から強制される必要条件は 2 本 — **(N1)** `\|X\| = 2` で `Φ = max{I(X;Y), I(X;Z)}` ちょうど、**(N2)** 格子 BC で `Φ ≥ H(X)` | **L13 で `killed(numeric-counterexample)`** — 候補 5 本 (下記) が全部死亡 |

**C2 で死んだ候補 5 本** (次の leg が同じ穴に落ちないよう記録する。案 A / B は probe §11、
P1–P3 は L13 の [`bc-jognair-phi-check.py`](bc-jognair-phi-check.py)。逐語と数値は
[`bc-facts.md`](bc-facts.md) §L13 が SoT):

- **案 A `Φ = C[max{I(X;Y),I(X;Z)}]`** (`p(x)` 上の上凹包): 上凹包は元の関数の最大値を
  超えないが、Blackwell では `max_p max{I,I} = 1` bit に対し `T(X) = 1.544732` ⟹ **(N2) 違反**。
- **案 B `Φ = I(X;Y,Z)`** (`Y ⊥ Z | X` の代表で測る): 格子 BC では等号で (N2) を満たすが、
  `\|X\|=2` の BSC(0.1) 2 本 + 一様入力で `I(X;Y,Z) = 0.742086 > max{I,I} = 0.531004`
  ⟹ **(N1) 違反** (Theorem 1 より真に弱い)。
- **P1 `Φ_det := max_{f,g}[I(f(X);Y) + I(g(X);Z) − I(f(X);g(X))]`** (`f,g` は `X` の関数、全列挙):
  決定論的な条件付分布は `p(u,v|x)` 多面体の端点ゆえ `Φ_det ≤ T` は恒真で、主張は
  **`T = Φ_det` =「最大化子は決定論的に取れる」と同値**。**(N1)(N2) とも PASS** — しかも `|X|=2`
  では [GJNW13] Theorem 1 そのものになる。**それでも refuted** — 最大違反幅 `+0.1484` (dense な
  ランダム `|X|=3`)、明示 witness (Blackwell の 2 出力を BSC(0.1) に通したもの、一様 `p`) でも
  `Φ_det = 0.7065361326214252` に対し `0.7686115289588658` = **margin `+0.062075`**。⚠ **死因は
  機構** — 最良組の空セルへ質量を流すと目的値が `+p(x)·ε·log₂(1/ε)` で超線形に増え、傾きは
  `ε = 1e-1 … 1e-6` で `0.551 → 5.946` と発散、桁あたり増分が予測値 `1.1073` と一致する =
  **決定論的点は局所最大ですらない**。⚠ **機構は不発点も当てている** (`η=0` は相殺で、`η=0.2` は
  空セル消失で、どちらも margin ちょうど 0)。生存するのは `|X|=2` と決定論的 BC の 2 つの退化
  ケースだけ。**dense ランダム 600 本のクラス分けでは、空セルを残す class A の 99 本が全数違反**
  (class B の 501 本では `Φ_det` が `max{I,I}` に退化するので主張が `gap > 0` に潰れる。⚠ class B の
  違反数は探索強度に依存する下界であって率ではない)。
- **P2 `D(p) := max_{f,g}[H(f(X),g(X)) − max{H(f(X)),H(g(X))}]`** (主張 `gap ≤ D(p)`、
  `D(p) ≤ D(|X|)` ゆえ R2 より真に強い): **(N1)(N2) とも PASS**。**明示 witness の直接評価で
  refuted** — 部分マージ型チャネル `β=2/3, α=3/4, p=(0.45,0.45,0.10)` で
  `gap = 0.4132331253245206` に対し `D(p) = 0.3762211396014732` (全列挙ゆえ厳密) ⟹
  **`+0.037012` bit**。⚠ **孤立点ではなく族** — `p = ((1−d)/2,(1−d)/2,d)` を `d = 0.05 … 0.20` で
  振ると全域で margin `+0.022 … +0.036`。
- **P3 `L' ≥ H(X|Y,Z)`** (同値に `gap ≤ min{I(X;Y|Z), I(X;Z|Y)}`): (N2) PASS、**(N1) FAIL**
  (BSC(0.1) 2 本で `0.211081` bit 緩む = 案 B と同じ穴)。⚠ **さらに ill-posed** — 右辺は同時
  分布に依存するのに `gap` と `L'` は 2 つの周辺だけで決まる (最大差 `1.284e-15`) ⟹
  **以後の候補は 2 つの周辺だけの関数として書くこと**。
- ⚠ **教訓 (L13 で 1 段強くなった)**: 案 A / B はそれぞれ (N2) / (N1) の**片方だけ**を満たすので
  「必ず両方に当ててから probe せよ」が L10 の教訓だった。**L13 はそれが必要だが十分でないことを
  示した** — P1 と P2 は**両方を通したうえで死んだ**。(N1)(N2) の witness はどちらも退化点
  (`|X|=2` / 決定論的 BC) にあり、そこは P1 の kill 機構がちょうど相殺する場所なので、
  **必要条件を全部通っても generic には死にうる**。⟹ **次の候補は構築の前に generic な非退化
  インスタンス 1 本** (dense・非決定論的・`|X| ≥ 3`・非一様 `p`) **にも当てる** (親 plan §5-12)。
- **⟹ C2 の後継は子 attack `slack-loss-lower-bound`** — 下の (H1) により、C2 の問いは
  「非負 4 項の損失 `L'` を下から抑える」と**厳密に同じ問題**である。

### 確信度ラベル (§3.2)

**`probed`**。核ステップのうち step 1–4 は記号的検証 (係数相殺 = 証明)、step 5–8 は明示
witness の直接評価、step 9 は掃引が反証を出せなかった、という状態である。
⚠ **step 9 を「証明できた」とは書かない** — 掃引の成立は証拠であって証明ではなく、しかも
本 leg 内に同じ掃引が真の最大を外した実例がある。

---

## R3 — 入力アルファベットの 2 分割で損失 `L'` は超加法的 (base case は Theorem 1)

> 軸 C (`slack-loss-lower-bound`、L13 が C2 の死因から起票した子)。**本節の再検証**:
> `python3 docs/shannon/bc-slack-loss-lower-check.py` (`--quick` 約 40s)。**L17 が加えた分
> (段 A / (H6) / `Λ_cav` / 枝の射程)** は `python3 docs/shannon/bc-split-general-check.py`
> (full 約 211s / `--quick` 約 28s)。乱数種と開始点は固定。
> ⚠ **記号的検算 (§0 / §4d) は最適化を使わないので再実行で必ず一致する**。

### 主張 (条件 1)

有限アルファベットの 2 受信者 DM-BC `q(y,z|x)` と入力分布 `p(x)` について、`X` の任意の
関数 `W = m(X)` を取ると

```
L'(p)  ≥  Σ_w p(w) · L'(p_w)          (p_w := p(· | W = w))
```

が成り立つ (`L'` は (H1) の非負 4 項損失)。同値に `T(p) ≤ H(W) + Σ_w p(w) T(p_w)`。
`|X| ≤ 2` では [GJNW13] Theorem 1 が `L' = min{H(X|Y), H(X|Z)}` を**厳密に**与えるので、
2 分割を再帰的に取ることで**チャネル依存の下界**

```
Λ_split(p,q) := max over 2 分割 {S, Sᶜ} of [ p(S)·Λ_split(p|S) + p(Sᶜ)·Λ_split(p|Sᶜ) ]
                base case: |X| ≤ 2 で min{H(X|Y), H(X|Z)}、|X| = 1 で 0
gap(T,p)  ≤  min{H(X|Y), H(X|Z)} − Λ_split(p,q)
```

が得られる。**Λ_split は最適化を含まない有限の再帰**であり、2 つの周辺 `q(y|x)`, `q(z|x)`
と `p(x)` だけで決まる (P3 の ill-posed を避ける制約、facts §L13)。

### ステップ (条件 2 + 3)

| # | 中間命題 | 入力 | 出力 | ラベル | 根拠 / 再検証 |
|---|---|---|---|---|---|
| 1 | `gap(T,p) = min{H(X\|Y),H(X\|Z)} − L'(T,p)` | なし | 主語の翻訳 | **`known` (in-project)** | (H1)。L13 で係数相殺により**証明済** (facts §L13) |
| 2 | **(H4)** 4 項が**項別に**分解する: `I(U;X\|Y) = I(U;X\|W,Y) + I(U;W\|Y) − I(U;W\|X,Y)` / `I(V;X\|Z)` 版 / `H(X\|U,V) = H(X\|U,V,W) + H(W\|U,V) − H(W\|X,U,V)` / `I(U;V\|X) = I(U;V\|X,W) + I(U;W\|X) + I(V;W\|X) − I(U,V;W\|X)` | なし (恒等式) | 4 本の無仮定な線形恒等式 | **`new` (証明済)** | `bc_probe.prove_identity` が 4 本とも**残差ちょうど 0** で accept (probe §4d) |
| 3 | `W = m(X)` を入れると `H(W\|X) = 0` の系として減算 4 項が消える ⟹ `F(U,V;p) − Σ_w p(w) F(U,V;p_w) = I(U;W\|Y) + I(V;W\|Z) + H(W\|U,V) ≥ 0` | step 2 | 各 `(U,V)` での超加法性 | **`new` (証明済)** | 逐語照合: 直接評価 `0.072550744` と `bc_probe.Joint` 経由の 3 項和が差 `8.7e-16` (probe §4d) |
| 3' | **(H6)** `W` を任意の `p(w\|x)` (非決定論的) で作ると `W ⊥ (U,V,Y,Z) \| X` から減算 3 項 + 加算 2 項が消え `F(U,V;p) − Σ_w p(w) F(U,V;p_w) = I(U;W\|Y) + I(V;W\|Z) + I(X;W\|U,V) ≥ 0` ⟹ **step 3 の `H(W\|X) = 0` は要らない**。同値な言い換え = **`L'(·,q)` は入力分布 `p(x)` の凹関数** | step 2 | 決定論的 `W` を仮定しない超加法性 | **`new` (証明済)** | 係数相殺 + 条件付き独立性 (掃引ではない): `prove_identity` が残差ちょうど 0、消えるべき 5 項が BayesNet 150 本で最大絶対値 `2.242e-15`、逐語照合の最大偏差 `5.329e-15` (L17 probe §0 / §3)。`T` 形では罰項が `H(W)` から `I(X;W)` へ下がる ⟹ **(H6) は (H4) の拡張であり同時に強化** |
| 4 | 右辺を `p(u,v\|x)` 上で最小化して **`L'(p) ≥ Σ_w p(w) L'(p_w)`** | step 3 (または 3') | 主張の前半 | **`new` (証明済)** | 各 `(U,V)` で成り立つ不等式の最小化は不等式を保つ |
| 5 | `\|X\| ≤ 2` で `L' = min{H(X\|Y),H(X\|Z)}` | — | base case | **`known`** | [GJNW13] Theorem 1 (facts §L13 の逐語行) |
| 6 | step 4 を再帰的に適用し step 5 で閉じる ⟹ `Λ_split ≤ L'` | 4, 5 | **証明済の下界** | 4, 5 に従属 | probe §4b が `T` の下界と右辺を全ケースで比較 |
| 6' | **`Λ_cav(p) := max{Σ_w λ_w Λ_split(p_w) : p = Σ_w λ_w p_w}`** (= `Λ_split` の**上凹包**) も `L'` の下界。構成から `Λ_cav ≥ Λ_split` (分割は台が交わらない分解 = 分解の特別な場合) | 3', 6 | より鋭い下界 | 3', 6 に従属 | ⚠ **代償: `Λ_split` の売りだった「最適化を含まない有限の再帰」は失われる**。ただし単体格子上の **LP** に落とすと (a) LP は厳密に解け、(b) 格子点しか使わない値は真の上凹包の**下界** ⟹ **`L'` の下界であることは保たれ、最適化器を信用する必要が無い**。格子 `N = 16/32/48/64` で `0.841117 / 0.841246 / 0.841383 / 0.841424` と単調増加 (どれも valid な下界。L17 probe §4) |
| 7 | `Λ_sup := H(X) − min{Γ_Y, Γ_Z}` (`Γ_Y := max_U [I(U;Y)+I(X;Z\|U)]`) も `L'` の下界 | なし | 併用できる第 2 の下界 | **`known`** (Marton 内界 ⊆ Nair–El Gamal 外界の和レート) | DPI 2 本。`Γ_Y = I(X;Z) + max_U[I(U;Y)−I(U;Z)]` = **less noisy 順序からのズレ** (probe §0(c)) |
| 8 | **`Λ_comb := max{Λ_split, Λ_cav, Λ_sup}`** を採る (R4 step 6 で `Λ_sup` → `Λ_env` に更新される) | 6, 6', 7 | 結論 | 6, 6', 7 に従属 | ⚠ **L17 で「相補的な 2 本」という読みの射程が縮んだ** — 実測は **`Λ_sup`/`Λ_env` が主、`Λ_split`/`Λ_cav` は `\|X\| ≤ 2` を厳密に押さえる base** である (`\|X\| ≥ 3` の 140 本で `max{Λ_split, Λ_cav} > Λ_sup推定` が **0 本**)。⚠ **枝は落とせない** — `\|X\| = 2` では `Λ_split = beta = L'` が [GJNW13] Theorem 1 で厳密ゆえ **(N1) を構成から満たすのはこの枝だけ**で、証明書もある (`BSC(.1)/BEC(.5)` の `p = 0.2/0.8` で `Λ_split − Λ_sup = +0.013540`)。⚠ **`Λ_sup`/`Λ_env` の支配が証明されたわけではない** — 「率」ではなく**探索強度に依存する下界**であり、決着が付くのは逆向き (`Λ_split > Λ_sup推定` が 1 本出れば支配は反証される) だけ |

**ラベルの注記**: step 2–4 を `new` と表示したのは新規性の主張ではなく「逐語の出典を取れて
いない」という意味 (R2 と同じ規約、親 plan §5-8)。⚠ **step 7 は `known`** — Marton 内界の
和レートが Nair–El Gamal 外界の和レートを超えないことは既知なので、本経路の新規性は
step 2–6 の側にしか無い。

### 数値の結果 (条件 4)

**`Λ_split` が厳密 (`= L'`) になる点が 3 種類ある**:

- **格子 BC 全部** (Blackwell / L 字 / 十字 / `2x2` / `2x3` / `3x3`): 右辺が `H(X)` に一致
  ⟹ `Λ_split = 0 = L'`。
- **`|X| = 2`**: base case ゆえ構成から厳密 (= (N1) を**構成から**満たす)。
- **P2 の kill witness** (部分併合チャネル、`p = (0.45,0.45,0.10)`): margin `−1.7e-07`、
  slack `0.413233` が facts §L13 の `gap = 0.4132331253245206` と一致 ⟹ **P2 を殺した
  witness の上でちょうど鋭い**。そこでは `D(p) = 0.376221 < 0.413233` なので、`Λ_split` は
  **P2 の反例を「説明する」側に立っている**。

**殺しにいった結果**: 族の掃引 (格子 / 平滑化 Blackwell / dense ランダム `|X|=3,4`) で違反
`0/16`、**チャネルと `p(u,v|x)` を同時に振る敵対的探索**で最良 margin `−2.0e-08`
(`|X|=3` の 3 形状、直接評価) ⟹ 反例は出ず、記号的証明 (step 2–4) と整合。

**鋭さの比較 (L15 run、dense ランダム `|X|=3`)**: `slack(Λ_sup)/gap` の中央 `1.35`、自明上界
`min{I(X;Y),I(X;Z)}` / `min{H(X|Y),H(X|Z)}` より鋭い点が `12/12`。`Λ_split` はこの領域では
緩い (同 run の slack 中央 `0.577`) ので、`Λ_comb` の両輪が要る。⚠ **どちらも主語は `Λ_sup` /
`Λ_split` であって `Λ_comb` ではない** (`Λ_comb` の残距離は下の L17 run のブロック)。

**L17 の測定 (同一 run・dense `|X|=3` の同一 10 本で before/after)** — ⚠ **上の L15 / L16 の
数値は別 run・別インスタンス集合なので混ぜない。以下は本ブロック内の old/new だけを比べる**:

- 残距離 `L'推定 − Λ` の**中央**: `Λ_split` **`0.522889`** → `Λ_cav` **`0.363557`** (**−30%**)。
  同じ 10 本で `Λ_env` は **`0.002955`** ⟹ **`Λ_split`/`Λ_cav` 側は dense で桁で緩い**。
- **`Λ_comb` の残距離の中央は `0.002955` (old = `max{Λ_split, Λ_env}`) → `0.002955`
  (new = `max{Λ_cav, Λ_env}`) で 1 桁も動かない**。`gap > 1e-3` の 7/10 本での `slack/gap` の
  中央も `1.160` → `1.160` で動かない。
- **縮まない理由は確定した** — `Λ_cav` の改善幅 (`0.16` 前後) は `Λ_env` との差 (`0.36` 前後) を
  埋めず、**10 本すべてで `max` を取るのは `Λ_env` 枝**である。
- **推定不足 2/10 行** (`Λ_env` が `L'推定` を `+2.16e-06` / `+9.36e-07` 超過 = `max_U` の届き
  不足であって反例ではない)、**矛盾の証明書 (`Λ_split`/`Λ_cav` が `L'推定` を超えた行) は 0/10**。
  probe §5 が**全 10 行を印字する** (推定不足の行を黙って除外しない)。

### novelty gate (条件 5) — 本候補について単独で実施 (L15)

一次文献 5 本を逐語で当たった: [GNA12] (arXiv:1202.0898) / [GEA11] (arXiv:1006.5166) /
[GJNW13] (arXiv:1001.1468) / [GLN25] (Mar-Con.pdf) / Nair の concave envelope 手稿
(`concenve.pdf`)。**アルファベットの 2 分割に関する超加法性 / `H(W)` を伴う分解は 1 本も
持っていない**。最近傍は 2 つで、どちらも**別の対象**である:

- **[GNA12] の上凹包 `C[f] := max_{p(v\|x)} Σ p(v) f(X\|V=v)`** — 分割は任意の `p(v|x)`
  (台が交わってよい) で、`H(W)` の罰も無い ⟹ 別物。
  ⚠ **訂正 (L17)**: 当初この道具を **Equation (2)** に帰していたが誤りで、上凹包が現れるのは
  **Conjecture 1 / Conjecture 3 の側**である。Eq. (2) の逐語は
  `αI(U;Y) + I(V;Z) − I(U;V) ≤ max{αI(X;Y), I(X;Z)}` (`|X| = 2`, `α ≥ 1`) = 同論文 Theorem 1 の
  `α` 重みつき版で、入力分布の分解を含まない (偽であることは Appendix B の反例)。**結論
  (別物) は不変**だが、この誤同定が L17 の kill-first に誤った事前確率を与えた (facts §L17)。
- **concave envelope 手稿の sub-additivity** — **積チャネル**に関する加法性 (テンソル化の軸)
  であって、入力アルファベットの分割ではない。

⟹ **gate 通過**。⚠ 留保: 検索は一次文献 5 本 + web 検索 2 回であり、「文献に無い」の網羅性は
そこまでである (facts §L15 に検索語を残す)。

### 確信度ラベル (§3.2)

**`probed`**。⚠ **R2 と step 1 ((H1)) を共有する** ので完全に独立な経路ではない。ただし核は
別で、**R2 の核 step 9 (`f*(k) ≤ D(k)`、未証明) に対し R3 の核 step 2–4 は証明済**である。
⚠ **「未解決問題を解いた」と読まない** — R3 が与えるのは `gap` の**証明された計算可能な
上界**であって、`T` の閉形式ではない。`Λ_comb` に残っている距離の実測値は **L17 run の
dense `|X|=3` 10 本のうち `gap > 1e-3` の 7 本での `slack/gap` の中央 `1.160`** である
(上の「L17 の測定」ブロックと facts §L17。⚠ `1.35` は **L15 run の `Λ_sup`** の
`slack(Λ_sup)/gap` の中央であって `Λ_comb` の値ではない — run も主語も違うので混ぜない)。

---

## R4 — 支配チャネル `W` で罰項を差し替えると `K(U)` の上界が 1 パラメータ族に一般化する

> 軸 C (`dominated-channel-envelope`、L16 が R3 の step 7 (`Λ_sup`) の緩さから起票した子)。
> **本節の再検証**: `python3 docs/shannon/bc-d2-lower-check.py` (full 約 93s / `--quick` 約 16s)。
> **step 5 / 5′ (admissibility) だけは** `python3 docs/shannon/bc-admissible-check.py`
> (L18、full 約 137s / `--quick` 約 20s)。
> ⚠ **本節が引く数値はすべて full 実行のもの** (`--quick` とは食い違う。facts §L13 の教訓)。
> 乱数種と開始点は固定。⚠ **記号的検算 (§0 の恒等式) は最適化を使わないので再実行で必ず一致する**。

### 主張 (条件 1)

`(U,V) → X → (Y,Z)` の下で `K(U) := max_{p(v|u,x)} [I(V;Z) − I(U;V)]` と置くと
`L' = min_{p(u|x)} [H(X) − I(U;Y) − K(U)]` (2 段最大化の入れ替え)。`X` からのチャネル `W` が
**`Z` に more capable で支配される** (supp(p_X) 上の任意の入力 `p'` で `I_{p'}(X;W) ≤ I_{p'}(X;Z)`)
とき

```
K(U)  ≤  I(X;Z) − I(U;W)                                        … (H5)
```

`W = Z` では `I(X;Z) − I(U;Z) = I(X;Z|U)` = R3 の step 7 が使う既存の上界にちょうど一致する
⟹ (H5) はその**1 パラメータ族への一般化**であり、支配チャネルの族 `𝒲` 上で最大化した

```
Λ_env := max{ H(X|Z) − max_U [I(U;Y) − max_{W∈𝒲_Z} I(U;W)],
              H(X|Y) − max_U [I(U;Z) − max_{W∈𝒲_Y} I(U;W)] }
```

は **構成から `Λ_env ≥ Λ_sup`** を満たす `L'` の下界である。2 つの周辺 `q(y|x)`, `q(z|x)` と
`p(x)` だけの函数なので P3 の ill-posed を避ける制約 (facts §L13) を満たす。

### ステップ (条件 2 + 3)

| # | 中間命題 | 入力 | 出力 | ラベル | 根拠 / 再検証 |
|---|---|---|---|---|---|
| 1 | `gap(T,p) = min{H(X\|Y),H(X\|Z)} − L'(T,p)` | なし | 主語の翻訳 | **`known` (in-project)** | (H1)。L13 で係数相殺により**証明済** (facts §L13)。⚠ **R2 / R3 と共有する step** |
| 2 | **主張 A** `L'(p) = min_{p(u\|x)} [H(X) − I(U;Y) − K(U)]` | なし | 標的を `K(U)` の上界へ還元 | **`new` (証明済)** | 2 段最大化の入れ替えのみ。probe §0 の恒等式が `accept = True` / 残差 `0`、最適点から `U` を抜いて測り直した差は `2.05e-05` (両側とも最大化 = 下界ゆえ符号は両向き) |
| 3 | **(H5)** `W` が `Z` に more capable で支配されるなら `K(U) ≤ I(X;Z) − I(U;W)` | なし | 罰項を差し替えた上界 | **`new` (証明済)** | 既知不等式の連鎖のみ: (i) `H(U\|V) − H(U\|W,V) = I(U;W\|V) ≤ I(X;W\|V)` (`U → X → W` を `V` で条件付け) / (ii) `I(X;W\|V) = Σ_v p(v) I_{p(x\|v)}(X;W) ≤ Σ_v p(v) I_{p(x\|v)}(X;Z) = I(X;Z\|V)` (**支配条件を各 `p(x\|v)` に適用**) / (iii) `I(X;Z\|V) = H(Z\|V) − H(Z\|X)` (`Z ⊥ V \| X`)。probe §0 が (i) の DPI と (iii) を残差 `0` で accept |
| 4 | `Λ_sup := H(X) − min{Γ_Y,Γ_Z}` が `L'` の下界 (= step 3 の `W = Z` の場合) | 3 | 族の基準点 | **`known`** (Marton 内界 ⊆ Nair–El Gamal 外界の和レート) | R3 step 7 と同じ。`Γ_Y = I(X;Z) + max_U[I(U;Y)−I(U;Z)]` |
| 5 | 族 `𝒲` の各 `W` が実際に支配条件を満たす (admissibility) | — | `Λ_env` を**計算**するための入力 | **`probed (per-instance certified)`** | ⚠ **3 点を書き分ける — どれか 1 つに丸めると過大 / 過小になる** (L18、facts §L18)。**(a) 一般の `W` に対する*定理*は無い** (ここは不変)。しかし **admissibility は有限個の不等式へ還元でき (step 5′)、`Λ_env` の計算に実際に使う `W` の全数 71 本が `CERT` (INDET 0 / REFUTE 0)** ⟹ 「数値検証に留まる」という以前の但し書きは**強すぎた**。正しくは「**インスタンスごとに機械検証できる有限の証明書がある** (残るリスクは浮動小数点のみで、**探索強度には依存しない**)」。**(b) 閉形式で言える族** — `Z` が消去チャネルなら `I_p(X;BEC(ε)) = (1−ε)H(p)` から **`W ∈ 𝒲_{BEC(ε)} ⟺ η_mc(W) := sup_p I_p(X;W)/H(p) ≤ 1−ε`** が**一般の `W`・一般アルファベットで**成り立つ。さらに `W = BSC(δ)` なら **Mrs. Gerber の補題**から `η_mc = 1 − h(δ)` ⟹ **`ε ≤ h(δ)` は引用ではなく*証明*として持てる** (⚠ 以前の「文献既知は 1 族のみ」は `W` 側を不必要に狭く書いていた)。**(c) 穴が残る理由 (機構つき)** — 利得が出るのは `mc \ ln` の隙間だけだが、そこを**一般の `Z`** について押さえる道具は文献に無く、判定可能な道具 (van Dijk 凹性 / Löwner LMI / SDPI / Doeblin) はすべて `ln` を証明する。そして **`ln` の利得は定義から 0** ⟹ **資格の穴と R4 の新規性はほぼ同一の対象**である |
| 5′ | **cell 証明書** — admissibility を*有限個の不等式*へ還元する (補題は記号的に**証明済**) | 5 | 3 値判定 `CERT` / `REFUTE` / `INDET` | **`new` (証明済)** | `A_r(p) := Σ_x p_x D(W_x‖r)` と置くと **`I_p(X;W) = A_r(p) − D(pW‖r)`** (恒等式) ⟹ `A_r` は `p ↦ I_p(X;W)` の**アフィンな優関数**。一方 `p ↦ I_p(X;Z)` は**凹** ⟹ 部分単体 `S = conv{v_i}` 上で `p = Σ_i λ_i v_i` と書けば **`Δ_W(p) ≥ Σ_i λ_i [ Δ_W(v_i) − D(v_i W‖r) ]`** ⟹ **ある `r` が存在して全頂点で `Δ_W(v_i) ≥ D(v_i W‖r)` なら cell 全体で `Δ_W ≥ 0`**。最長辺 2 分割で単体を被覆して全 cell 合格なら**単体全域の証明**、逆に cell 頂点で `Δ_W(v) < 0` なら**反証** (`v` は単体の実点)。⚠ **これは証明である** — 恒等式 + 凹性 + 非負性であって掃引ではない (残るのは浮動小数点だけ。`mpmath` 50 桁の再評価で同一判定)。⚠ **射程の限界も機構つきで持つ** — `Δ_W` が**内点で 0 に接する**族 (`W = Z` 自身と、`Z` に情報同値な閉包元) は両辺がともに `O(d²)` で潰れるので**細分では閉じない** ⟹ **証明書 (十分条件の機械) と解析 (漸近的に接する族) の分業**という形で射程が確定した |
| 6 | `Λ_env ≥ Λ_sup` かつ `Λ_env ≤ L'` ⟹ **`Λ_comb := max{Λ_split, Λ_cav, Λ_env}`** | 3, 4, 5 | 結論 | 3–5 に従属 | **R3 の step 8 の `Λ_comb` はここで `Λ_sup` → `Λ_env` に更新される** (`Λ_env ≥ Λ_sup` は構成から)。⚠ **L17 で `Λ_split` 枝も `max{Λ_split, Λ_cav}` へ更新した** (R3 step 6' の上凹包)。⚠ **ただし実測ではこの枝が `\|X\| ≥ 3` で `max` を取らない** — 140 本で `max{Λ_split, Λ_cav} > Λ_sup推定` が 0 本、dense 10 本の `Λ_comb` 残距離の中央は `0.002955` → `0.002955` と動かない (facts §L17)。枝が効くのは `\|X\| ≤ 2` で、そこで (N1) を構成から満たすのがこの枝である ⟹ **落とせない** |

**ラベルの注記**: step 2–3 / 5′ の `new` は新規性の主張ではなく「逐語の出典を取れていない」の意
(R2 / R3 と同じ規約、親 plan §5-8)。⚠ **本経路の弱点は step 5 に残る** — L18 で `gap` から
`probed (per-instance certified)` へ動いたが、動いたのは**証拠の階級**であって「一般の `W` を族に
入れる資格の*定理*」が立ったわけではない。`Λ_env` を具体的に計算するぶんには使う `W` の全数が
証明書つきだが、族 `𝒲` そのものを一般に記述する定理は無い。

### 資格の穴は本質的である (L18、記号的に**証明**)

**`{Z}` の閉包演算だけで作れる `W` は、`Z` の劣化版から出られない**。`𝒟_Z := {Z∘Q : Q は確率行列}`
と置くと、4 つの閉包演算すべてで `𝒟_Z` は閉じる — (a) 劣化 `(ZQ)Q′ = Z(QQ′)` / (b) 出力の交わらない
混合 `λZQ₁ ⊕ (1−λ)ZQ₂ = Z·[λQ₁ | (1−λ)Q₂]` / (c) `Z = Z·I` / (d) 同一出力アルファベット上の凸結合
`λZQ₁ + (1−λ)ZQ₂ = Z(λQ₁+(1−λ)Q₂)` ⟹ **`{Z}` の閉包 ⊆ `𝒟_Z`**。かつ `W = Z∘Q` なら `U → X → Z → W`
と結合できるので DPI で `I(U;W) ≤ I(U;Z)` が**すべての `U`** で成り立つ ⟹ `max_{W∈𝒲} I(U;W) = I(U;Z)`
⟹ **`Λ_env = Λ_sup`、利得はちょうど 0**。

⚠ **これは 4 行の証明であって掃引ではない** (数値の裏取りは下の「閉包の裏取り」)。⟹ **資格の穴は
偶然ではなく本質的**である — 資格を証明できる範囲を閉包だけで広げても利得は恒等的に 0 なので、
**証明には*非劣化の* base point が別途要る**。それを供給するのが step 5′ の cell 証明書 (一般の `Z`)
と step 5 (b) の閉形式 (`Z` が消去チャネル) の 2 本である。

**副産物 (§L16 の結論への別経路)**: 劣化 ⊊ less noisy ⊊ more capable のうち **`ln` までは定義から
利得 ≤ 0** ⟹ **利得は more capable だが less noisy でない隙間にしか無い**という §L16 の結論は、
**van Dijk を経由せず定義だけで出る**。

### 数値の結果 (条件 4)

**以下の箇条書きはすべて L16 run の値である** (facts §L16 が SoT)。⚠ **後続の「L18 の測定」
ブロックとは run もインスタンス集合も違うので、dense の残距離を run をまたいで比べない**。

- **(H5) の裏取り**: 支配 `W` 20 本 × ランダム `p(u,x)` で最悪 margin `−8.217e-04` (正なら破れ)。
  **対照の非支配 `W`** (`Z = BEC(.5)` / `W = BSC(.05)`) では `U = X` の直接評価で違反 `+0.213603` を
  検出 ⟹ **掃引に検出力がある**。利得の恒等式
  `I(U;W) − I(U;Z) = Σ_u p(u) Δ_W(p_u) − Δ_W(p)` は最大偏差 `1.671e-15` で成立 ⟹ **利得は
  `Δ_W(p') := I_{p'}(X;Z) − I_{p'}(X;W)` の凹性の欠損そのもの**。
- **改善の実測**: `Λ_env − Λ_sup` は中央 `+0.000002` / 最良 `+0.009576` (構成から ≥ 0)。
  **dense `|X|=3` 10 本の残距離 `L' − Λ` の中央は `0.002986` (`Λ_sup`) → `0.002655` (`Λ_env`)、
  `gap > 1e-3` の 5 本での `slack/gap` 中央は `2.544` → `2.544` で動かない** ⟹ dense の残距離は
  詰まっていない。一方 **L15 が (N1) 証明書に使った `BSC(.1)/BEC(.5)` 族では deficit が実質的に縮む**
  (`p=0.1` で `+0.004539 → +0.000299`、`p=0.2` で `+0.013538 → +0.008948`。どちらも 2 値 `U` の格子 =
  最適化不使用の証明書) ⟹ **改善は「more capable だが less noisy でない」順序の隙間に局在する**。
- **(N1) 不合格 / (N2) 合格**: (N1) の最大 deficit は `+0.023313 → +0.023313` (`BSC(.15)/Z(.5) p=0.2`)
  で動かない (⚠ 想定どおり — (N1) を満たす*証明済*の `Λ` は Theorem 1 を含意する、facts §L15)。
  (N2) は格子 BC 3 本で `Λ_env` の最大 `8.81e-06` (最適化ノイズ)。**推定不足は 13 行** (すべて `1e-6` 台。
  反例ではなく `max_U` の届き不足。probe §4 に全行印字)。
- **副産物 — 消去族は死ぬ**: `W =` 確率 `γ` で `X` を明かす消去チャネルは頂点で `H` の傾きが無限大
  ゆえ**どんな `γ > 0` でも支配が破れ** (`γ* = 0`)、分割 `m_S(X)` へ落としても同じ機構で死ぬ ⟹
  **`W` は「出力を粗くする」型では作れず、`Z` と非可比な方向へずらす必要がある** (機構と実測比は
  facts §L16)。

**L18 の測定 (probe は `python3 docs/shannon/bc-admissible-check.py`、full 実行。facts §L18 が SoT)**:

- **証明書の鋭さ**: `Z = BEC(ε)` / `W = BSC(δ)` で**文献既知の閉形式境界 `h⁻¹(ε)`** を CERT 側と
  REFUTE 側が**窓幅 `2.91e-11`** で挟む (`ε = .5/.3/.7` の 3 本、真値は
  `0.110027864 / 0.053239041 / 0.189297705`) ⟹ 証明書は健全なだけでなく**実質的に完全**である。
- **出口条件 (B) = 証明済に制限しても改善は残る**。最も決定的なのは **`|X| = 2` の最適化不使用の表**
  (2 値 `U` の格子を直接評価するので最適化器を信用せずに読める): deficit の縮みの最良は `+0.004178`
  (`Λ_env`) → `+0.004176` (`Λ_env^cert`)、**残存率は中央 `1.0000` / 最小 `0.9913`**。一般
  インスタンス 32 本では**残存率 中央 `0.997` / 最小 `0.976`** (改善が `> 1e-4` の 8 本に限る)。
- **推定不足は 10 行** (すべて `1e-6`〜`1e-5` 台。`max_U` の届き不足であって反例ではない。
  probe が**全行を印字する**)。
- **閉包の裏取り** (上の記号的証明の数値側): 閉包元 150 本で後処理核の witness 残差 `‖ZQ − W‖_∞` の
  最大 `2.220e-16` / LP `min_Q ‖ZQ − W‖_1` の最大 `0.000e+00` / 利得の最大 `+1.332e-15`。対照の
  非劣化 base point (`U` の格子 = 最適化不使用) では利得が正になる (`BEC(.5)/BSC(.115)` で
  `+0.028886`、`BEC(.3)/BSC(.06)` で `+0.023484`) ⟹ **検出力がある上での 0 である**。

⚠ **注記 2 点 (これを外すと数を誤読する)**:

- **`Λ_env` 側と `Λ_env^cert` 側で探索強度が非対称である** — 証明済側は粗い篩の上位 60 本を細かい
  格子で再選別しており、L16 の 12 本より探索が強い。どちらも同じ真の `max` の下界なので、**残存率が
  1 を超える行があるのは矛盾ではない**。⟹ **(B) の決着を担うのは `|X| = 2` の最適化不使用の表**である。
- **(B) の生存は「鋭さが詰まった」ことを意味しない** — dense `|X|=3` 10 本の残距離 `L′ − Λ` の中央は
  `0.002984` (`Λ_sup`) → `0.002640` (`Λ_env`) → `0.002644` (`Λ_env^cert`) で、**§L17 の結論
  (`Λ_comb` は 1 桁も動かない) は不変**。(B) が測ったのは「証明済に制限したときに改善が消えないか」
  であって、改善そのものの大きさではない。

### novelty gate (条件 5) — 一次文献 11 本 (一次判定 + 追試の 2 報)

**3 つの問いに分けて判定した。通ったのは (H5) だけである**:

- **問い 1 ((H5) そのもの) = 保留 (PASS 寄り)** — 既知の記述は見つからず。最近傍は (i) Gohari–Nair 2022
  "Auxiliary Receiver Approach" Theorem 7 (18h) だが対象が Fano 由来の**容量 outer bound**で族が無制約
  (BC 側に more capable / less noisy は 0 ヒット)、(ii) Geng–Gohari–Nair–Yu arXiv:1105.5438 **Lemma 7**
  `I(V;Z|W) − I(U;V|W) ≤ I(V;Z|U,W) ≤ I(X;Z|U,W) ≤(a) I(X;Y|U,W)` — **罰項も第 1 項も支配する側に
  揃える**ので古典上界より**緩くなる**方向 ⟹ 第 1 項に支配する側 `I(X;Z)`・罰項に支配される側 `I(U;W)` を
  混ぜて**締める** (H5) とは逆で、含意も示唆もしない。⚠ **未走査**: 秘匿容量 / wiretap 系と
  Nair "Capacity regions of two new classes of 2-receiver BC" (ISIT'09)。
- **問い 2 (`Λ_env` という対象) = FAIL 寄り保留** — [GNA12] (arXiv:1202.0898) Theorem 1 が `|X|=2` の
  同目的の上界で、同論文が逐語 `Beyond binary input alphabets: The inequality (1) itself fails to hold
  where |X| = 3, for instance in the Blackwell channel` と書いて**一般アルファベット化を既知の未解決方向
  として明示**している ⟹ `Λ_env` は「新しい問いを立てた」のではなく**既知の目標に対する未報告の
  パラメトリゼーション**。加えて arXiv:1105.5438 Lemma 7 の閉形式は**既存の `Λ_sup` に相当**する
  ⟹ step 4 が `known` である裏づけが 1 本増えた。
- **問い 3 (構造的な読み) = FAIL (既知)** — 利得の恒等式は **van Dijk 1997** (less noisy ⟺ 相互情報量差の
  凹性) の証明そのもの (教科書級、El Gamal–Kim Ch.5)、「利得は凹性の欠損」は Nair 凹包手稿の中心概念、
  「2 順序の隙間がクラスの分かれ目」は **Gohari–Liu–Nair arXiv:2606.12839** の構成原理 ⟹ **構造的な
  読みは本経路の新規成果ではなく、既知の道具で候補の射程を*測った*もの**である。

逐語・検索語・網羅性の留保は facts §L16 の文献行 (一次文献 11 本 + web 検索 8 回)。

### 確信度ラベル (§3.2)

**`probed`**。⚠ **留保 3 つ** (経路の本数を数えるときは必ず併記する):

- **R2 / R3 と step 1 ((H1)) を共有する** ⟹ 完全に独立な経路ではない。
- **R4 は R3 の step 7 (`Λ_sup`) の強化でもある** — 産物は同じ `Λ_comb` に合流する
  (`Λ_comb := max{Λ_split, Λ_env}` へ更新) ので、R3 と R4 は独立な 2 本ではなく相補的な 2 本である。
- **新規性は (H5) に局限される** (上の gate 判定 3 つ)。`Λ_env` という対象も構造的な読みも既知側。

⚠ **成立性への赤旗とその回答** (次 leg が読み違えないため): 赤旗は「古典鎖
`K_Z(U) ≤ I(V;Z|U) ≤ I(X;Z|U)` から (H5) を出すには `I(U;W) ≤ I(U;Z)` (= less noisy) が要る。
more capable は less noisy を含意しないので (H5) は古典鎖では出ない ⟹ 偽である risk」。
**回答**: (H5) の証明は古典鎖ではない — **`V` で条件付けてから支配条件を各 `p(x|v)` に当てる**経路
(step 3) であり `I(U;W) ≤ I(U;Z)` を要求しない。数値でも**実際に `I(U;W) > I(U;Z)` (= less noisy
でない) となる `U` で利得 `+0.010590` が出た上で違反探索は最悪 margin `−8.217e-04`** ⟹ 解消。
⚠ ただし**「古典鎖では出ない」こと自体は正しく、それが (H5) の内容の源**である。
