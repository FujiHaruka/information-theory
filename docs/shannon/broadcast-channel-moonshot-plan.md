# T3-C Broadcast Channel (degraded) Capacity Region ムーンショット計画 🌙

**Status**: degraded ✅ CLOSED / 一般 BC (Marton inner bound) ✅ CLOSED — degraded BC capacity region (Cover–Thomas Thm 15.6.2) は **converse + achievability 両側 genuine closure 済**: converse headline `bc_degraded_converse` (auxiliary-variable 容量領域 membership、核 `bc_input_singleletterize` / `bc_singleletterize_bound₁`、L-BC2 再開) + achievability headline `bc_achievability` (superposition inner bound、2026-07-03 relay closure) がともに `@audit:ok`・`InformationTheory.lean` 登録済 (子 [`bc-degraded-converse-plan.md`](bc-degraded-converse-plan.md) / [`bc-achievability-plan.md`](bc-achievability-plan.md)、`#print axioms` sorryAx-free)。achievability の最終ゲート = degradedness superadditivity `bc_degraded_infoJoint_ge` (`bcInfo₁+bcInfo₂ ≤ bcInfoJoint`) を stochastic Markov `U→Y₁→Y₂` 自作 + 既存 DPI `mutualInfo_le_of_markov` で closure (撤退スロット L-BC1/L-BC3 未使用)。**L-BC5 (一般 BC + Marton) はユーザー指示で解除** → 子 [`marton-inner-bound-plan.md`](marton-inner-bound-plan.md) で追跡し、**Phase 0–8 完了 = headline `marton_achievability` (EGK Thm 8.3、private message のみ) が proof-done**。bookkeeping (README 定理表 / proof-log / WZ 再配線) まで消化して子 plan は CLOSED、生きた撤退ラインは 0。Körner–Marton は scope-out 継続 (textbook-roadmap Ch.15)。**一般 BC の外界フレームは進行中 — less noisy / more capable の 2 クラスで容量領域が等号で閉じた** (`bc_lessNoisy_capacity_eq_uv` / `bc_moreCapable_capacity_eq_uv`、下記「後続」。**一般 BC 未解決問題への 20 leg relay は完走**、未解決本体そのものは依然未解決)。**Phase 5 は完遂** (degraded 接続 = 古典形 converse の符号への着地 `bc_degraded_converse_from_code` まで)、**Phase 2 の拡張 P4–P7 も完遂したので子 plan は全 Phase 完遂**。子 §後続作業 も 9 leg relay で一括消化し、残るのは B-3 / B-4 / C-1 / C-2 / C-4 / D-2 / E-2 / F-14 / F-23 / G-1 の 10 項目のみで、いずれも到達目標の前提ではない。
**SoT**: `docs/textbook-roadmap.md` Ch.15 + 子 plan。詳細履歴は git。

**後続**: 一般 BC の足場は子 [`bc-general-region-plan.md`](bc-general-region-plan.md) で**全 Phase 完遂 ✅**。
**未解決本体への攻略は子 [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) が承継する** —
ユーザー決定 (2026-08-02) により**ゴールを T3 (未解決本体そのものを、散文の証明 + Lean の proof done
の両方で決着させる) へ置き直した**。**T3 relay は L0–L20 完了 = relay 終了 (残枠 0)** —
⚠ **§0 のゴールは未達である** (子 §6 の撤退ライン 3 本目が発火 = **未達と書いた**。⚠ **発火とは
未達と書くことであって達成条件を緩めることではない** — 完了条件は 1 文字も動いていない)。
⚠ **§1.1 の唯一の穴 (C2) = 外界側の effective compactness は未着手のまま 1 ミリも動いていない**
(状態の詳細は下記 sub-plan テーブル、判定の SoT は在庫
[`bc-t3-cardinality-inventory.md`](bc-t3-cardinality-inventory.md) §17.3)。
⟹ **ユーザー決定 (2026-08-03) により継続**。**後継の子
[`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md) が新規 20 leg (M0–M19) で承継し、
標的は (C2) 本体**である。⚠ **前 relay が未達で終わった事実は消さない** — 後継は前 relay の成果物の
上ではなく在庫 §17.4 項目 1 から出発し、**§0 のゴールと完了条件は 1 文字も動かさない**。
**後継の現況 (2026-08-03) = M0 / M1 完了 (2/20) で、⚠ 撤退ライン §6-1 が発火 ⟹ ユーザー判断待ちで
PAUSED** — M0 (在庫) は壁 0 件で着地したが詰まりは `Thm7` 側の対象の不在にあり、M1 (本命 attack
`t3-thm7-tightness` の反証試行) は **GO / NO-GO のどちらも返さず「判定不能」**だった (厳密な分離は
1 本も立たず、同時に反証も塞がっていない)。⚠ **発火は「未達と書く / 次の判断をユーザーへ返す」ことで
あって達成条件を緩めることではない**。⚠ **(C2) 本体の可変枠 M2–M15 は 14 leg すべて未消化**である
(判定の SoT は子 plan §7 判断ログ 2、逐語の SoT は [`bc-facts.md`](bc-facts.md) `## M1 (T3b)`)。
⚠ **後継の枠は前 relay の配分事故 (21 leg 中 14 leg が (C1) 側の形式化へ流れ、標的の (C2) には
実質 0 leg しか入らなかった) を構造で塞ぐ設計**である (子 §5 の予約枠 + 子 §5.1 の側の宣言規約 +
子 §6-4 の配分の撤退ライン)。⚠ **到達した中間結果は 1 行だけ残す** —
層 3 (形式化) 側で補助変数の基数境界 (a′) が L18 / L19 で **proof done** になり (7 file / `sorry` 0 /
`@residual` 0)、`closure_convexHull_martonRegionUnion_eq_bounded` 1 本を README 定理表へ登録した。
⚠ **これはゴール達成ではない** (子 §0.1-2)。下記 20 leg relay の到達点 (経路 3 本) は **T2 = 中間結果**で
あって本丸ではなく、**形式化しても未解決問題は解けない** (routes.md 合流節 §D)。
**一般 BC 未解決問題への 20 leg relay** ([`bc-open-problem-plan.md`](bc-open-problem-plan.md)) は
**20/20 leg 完走・子 plan は終了した** (未解決本体への攻略、
L0 文献 / L1 数値 probe 基盤 / L2 還元層の決着 / L3 軸 A / L4 棚卸し #1 / L5 軸 G / L6 軸 B /
L7 経路構築 #1 / L8 経路 #1 の probe + novelty gate / L9 棚卸し #2 / L10 軸 C / L12 軸 A / L13 軸 C /
L14 棚卸し #3 + 収穫判定 / L15 軸 C の子 (経路 R3) / L16 軸 C の子 (経路 R4) / L17 `Λ_comb` の鋭さ /
L18 R4 step 5 の admissibility / **L19 収穫 (経路 3 本の書き上げ) / L20 記録** まで完了 = **20/20 leg**)。
**§3.1 の 5 条件を満たす有効経路は 3 本 = §3.3 の目標 (経路 ≥ 3 本・うち ≥ 2 本 `probed`) に到達済** —
**R2** (L10、`probed`。⚠ 核 step 9 は `gap`) / **R3** (L15、`probed`、核 (H4) は証明済) /
**R4** (L16、`probed`、核 (H5) は証明済)。⚠ **留保 3 つを判定と一緒に持つ** (これを外すと本数を過大に
読む): (a) **3 本とも step 1 ((H1)) を共有する** ⟹ 主語の翻訳は 1 本しかない / (b) **R4 は R3 の step 7
の強化でもあり産物は同じ `Λ_comb` に合流する** ⟹ 独立な 2 本ではなく**相補的な 2 本** / (c) **R4 の
新規性は (H5) に局限される** (`Λ_env` という対象も構造的な読みも gate は既知側と判定)。L7 の R1 は (4) を
通ったが (5) novelty gate で落ちた (一次文献 3 本で 12 step すべてに逐語出典が付いた = 死因
`known-result`) ため経路表に載らず、軸 E の地図として
[`bc-open-problem-routes.md`](bc-open-problem-routes.md) に残る。⚠ **本数の現物は子の台帳が SoT**。
**L9 は残り枠を gate-first へ組み替えた** ([探索 + gate] → [構築 + probe] の 2 leg 1 組。L10 はこれを
1 leg で消化し、L11 が余剰枠になって leg ラベルと予算の 1 枠ずれが解消した)。
**供給源は 2 度閉じた** — **L12 で軸 A** (子 2 本とも `known-result`。Nair 2010 の essentially less noisy /
essentially more capable が同じ操作を先取りし容量領域まで確定済) と、**L13 で軸 C のスラック候補族**
(`jognair-channel-dependent-slack` は候補 3 本とも `numeric-counterexample` で `killed` ⟹ 経路は増えず、
子 attack `slack-loss-lower-bound` を起票して閉じた) ⟹ **残る本命は軸 C の子 2 本と (条件つきで)
軸 G ブロックの 3 系統** (軸 D は L18 の拾い上げ候補へ落ちた)。⚠ **どちらの leg も軸外の収穫のほうが大きい** — L12 は「BC 容量領域の計算可能な
特徴付けは未解決」と述べる一次文献 (Nair 2013) を立て (**L0 で NOT-FOUND だった F8 の出典**)、L13 は
**厳密なスラック恒等式 (H1)** と比較補題 (H2) を証明して標的を「非負 4 項損失の下界」へ言い換えた。
**L14 (棚卸し #3 + 収穫判定) の枝は消化済** — 事前登録した「1 本以下 → 軸 G ブロック一括発動」は
**L15 が経路 R3 を返したので発動せず**、軸 G は park のまま relay を終える。**L15 / L16 が R3 / R4 を
立てて目標本数に到達**し、**L17 は本数ではなく鋭さを撃った** — (H6) (非決定論的 `W` への一般化) を
記号的に証明し上凹包 `Λ_cav` が `Λ_split` の残距離を −30% 改善したが、**`Λ_comb` の dense 残距離は
1 桁も動かず** (`0.002955` → `0.002955`)、⚠ **縮まない理由が確定した** (`max` を取るのは常に `Λ_env`
枝で、`Λ_split` 枝が勝つのは `|X| = 2` だけ)。**L18 は R4 step 5 (`W` の族の admissibility) を撃ち、
ラベルを `gap` → `probed (per-instance certified)` へ動かした** — **cell 証明書**が admissibility を
有限個の不等式へ還元し、`Λ_env` の計算に実際に使う `W` の**全数 71 本が `CERT`** (INDET 0 / REFUTE 0)。
出口条件 (B) も**生存** (証明済に制限しても改善が残る)。加えて**資格の穴が本質的であることを 4 行で
*証明*** (閉包演算だけで作れる `W` は `Z` の劣化版から出られず利得はちょうど 0)。⚠ **`Λ_comb` の鋭さは
動かない** (L17 の結論は不変) ⟹ **判断ログ 30 の退路が完全に成立**。強制 park 0 / **ユーザー判断は不発**
(諮る条件は生存 0 本)。**L19 (収穫、探索なし) は routes.md に合流節を新設し** `Λ_comb :=
max{Λ_split, Λ_cav, Λ_env} ≤ L'` の各枝の射程 (`Λ_split`/`Λ_cav` は `|X| ≤ 2` で厳密、`Λ_env` は dense
`|X|=3` で 10/10 本が `max` を取る) と**証明済・probed・gap の内訳表**を 1 か所に確定して次 relay
(層 3 = 形式化) へ引き継いだ。あわせて **R1 を地図として圧縮** (routes.md 1018 → 858 行)、確信度ラベルの
**誤帰属 1 件を訂正** (`Λ_comb` の残距離比に L15 run の `Λ_sup` の値 `1.35` を誤用していたのを L17 run の
`1.160` へ)、**rng 共有の損失率を再導出** — 記録していた 2 つの率 (判断ログ 32 (c) の `44%` / facts §L18
の `40%`) は修正前のコードが失われ再現できず**両方とも不採用** (facts §L19)。⟹ 探索していないので
**経路は 3 本のまま**。**残る gap は 2 つ** — **R2 step 9** (`f*(k) ≤ D(k)`、決定論的な格子 BC が極値かは
証明も反例も無い) / **族 `𝒲` の一般的記述定理** (R4 step 5 (a)、判定可能な文献の道具はすべて `ln`
〈利得ちょうど 0〉を証明するのみ) で、どちらも `Λ_comb` の鋭さそのものとは別系統。**L20 (記録)** は
`docs/proof-logs/proof-log-bc-open-problem-relay.md` + `docs/metrics/bc-open-problem-relay.*` を新設し、
**繰り返し出た失敗形** (再導出できない数値を散文にキャッシュする / run をまたいだ数値の混同 / 最適化器の
非違反を証拠として過大に読む / 一次文献の誤同定の伝播など計 8 件) を抽出、**延長する場合の次の 20 leg の
設計**を残した。**継続の判定 (proof-log §6。⚠ これは判定であって決定ではない — 継続するかはユーザーが
決める)**: 同方向 (`Λ_comb` の鋭さ) の継続は**価値が低いと判定**した。根拠は 1 行 —— **L17 / L18 が
同一 run で「`Λ_comb` の残距離は 1 桁も動かない」を確定させ、`max` を取るのは常に `Λ_env` 枝と分かった**
ため。選択肢は **A. 層 3 (形式化) へ切り替え / B. park した軸 G を発動 / C. 同方向 (`Λ_comb` の鋭さ) を
継続 (非推奨)** の 3 つ。

**🎉 moonshot の到達 — more capable BC の容量領域が単一文字式で閉じた (Phase 5 本線)**:
`@[entry_point]` **`bc_moreCapable_capacity_eq_uv : bcCapacityRegion W = bcOuterRegionUV W`**
(`BroadcastChannel/Superposition/MoreCapable.lean`、明示仮説は `hW` = 全出力対に正の質量 + `hmc` =
`IsBCMoreCapable W` のみ)。挟み込み `内界 ⊆ bcCapacityRegion ⊆ 外界` を作るのが本計画の当初目標
だったが、**degraded より真に広い 2 クラスで挟み込みが等号に潰れた** (先行する less noisy 版
`bc_lessNoisy_capacity_eq_uv` は `IsBCLessNoisy.isBCMoreCapable` 経由で本 3 本の系になる)。等号の
心臓である逆包含 `@[entry_point]` `bc_moreCapable_uv_subset_superposition` は `hW` すら要求しない
(`hW` が要るのは内界を符号に落とす段だけ)。**クラス述語はどちらも両領域を一切参照しない
チャネルレベルの述語なので load-bearing ではない** (署名走査での判定。`sorryAx` 非依存の側は
`#print axioms` で毎回引き直す — 散文にキャッシュしない)。

そこまでの土台: Phase 1 (操作的容量領域 = 主語) / Phase 3 (協調外界) / Phase 4a (UV 単一文字化、
floating 形) / **Phase 4b** headline `bc_capacity_subset_uv` の**全平面版** (明示仮説は `W` +
`[IsMarkovKernel W]` のみ、第一象限制約なし) / Phase 5 定義段 (`BroadcastChannel/Classes.lean` =
`IsBCLessNoisy` / `IsBCMoreCapable` / `IsBCSemiDeterministic` + 包含鎖
`degraded ⊆ less noisy ⊆ more capable`) / 内外の橋 S1–S6 (`OuterBoundUV/MartonBridge.lean`、到達点は
`@[entry_point]` `marton_region_subset_uv`) / Phase 2 の最小完遂 P1–P3
(`BroadcastChannel/MartonUnion.lean`) / **less noisy の等号 S0–S8 全段** (子 plan が SoT)。実装は
`ChannelCoding/CodeToAmbient.lean` (MAC/BC 共有層) +
`BroadcastChannel/OuterBoundUV/{Bridge,Region,Assembly,Quantization}.lean` +
`BroadcastChannel/Superposition/{Region,TimeShare,FullSupport,Assembly,MoreCapable}.lean` +
`Shannon/{CondMutualInfoMixture,MutualInfoReencoding,MutualInfoFiniteRange,BoolLaw}.lean` +
`Probability/{Mixture,SingletonMass}.lean` (いずれも 1500 行ガイド内。後 4 者は後続作業 relay で
BC / MAC namespace から昇格した汎用層で、**この昇格で BC → MAC の import 辺は消滅した**)。

**🔄 経路変更 — 内界の選択がクラスごとに変わる (等号が成立した理由)**: 等号の次手としていた逆包含
`bcOuterRegionUV ⊆ martonRegionUnionFS` は**偽**と判定された (劣化 BSC 対 `q=0.1`, `p=0.25` の
数値反例。確定事実の台帳 [`bc-facts.md`](bc-facts.md)、再検証スクリプトは
`bc-marton-union-gap-check.py`)。`martonRegionUnion` は EGK Thm 8.3 の共通補助変数 `U₀` を持たない
形なので、**劣化 BSC 対ですら `martonRegionUnionFS ⊊ bcCapacityRegion`**。⟹ 等号を狙うクラスでは
内界を **superposition** に差し替え、達成側は `bc_achievability` の本体を degradedness 非依存の
共通形 `bc_achievability_of_rate_lt` へ factor out して再利用した (**`bc_achievability` の署名・
結論は逐語不変**)。Marton union 側の順包含 `martonRegionUnion_subset_uv` は一般 BC の内界として
そのまま生きている。一般 BC 容量領域の特徴づけ自体は未解決問題なので、内外一致は計画の Phase 外。

**後片付けは完了**: README 定理表への登録 (`b545cbd7` / `594887a4` / `e070dae4`)、`Superposition/`
サブディレクトリ昇格 + 重複解消 + `ℝ≥0∞` イディオムの refactor leg (`4ea35cc0`…`d0ac3aed`、
5 ファイル / **−85 行**、新しい数学 0 行、style PASS)、族としての proof-log (`3d680d3e`)、第 3 スロット
`uvInfoJoint` の def 化 + ベタ書き置換 (`bb40c820`)、汎用 2 本の `Shannon/` 直下への移設 (`730844a1`)。

**✅ degraded との接続も完遂 (子 plan §推奨実行順 #3、`bf49a91d`…`86b90af1`)** — 領域レベルの
degraded 等号は more capable の**系として 6 行**で取れた (`bc_degraded_capacity_eq_uv`) ので、
本 leg の価値はそこではなく**古典形 converse (Cover–Thomas Thm 15.6.2) の操作的着地**にある:
`@[entry_point]` `bc_degraded_converse_from_code` (`BroadcastChannel/DegradedFromCode.lean`) が
`bcConverseAmbient c W` 上で `bc_degraded_converse` の構造前提 (`h_deg_block` / `h_memo`) を両方
discharge し、**残る明示仮説を `IsBCDegraded W` と 2 本のメッセージ数だけにした** ⟹ `Converse.lean`
の docstring が散文で主張していた「両構造前提は操作的 degraded 設定では真」が機械検証された。
副産物として汎用 5 本が `ChannelCoding/CodeToAmbient.lean` へ、append 型 Markov 鎖 2 本が
`private` 解除で `Shannon/CondEntropyMemoryless.lean` へ上がり、WynerZiv の逐語重複 2 本が消えた
(−58 行)。

**✅ Phase 2 の拡張 P4–P7 も完遂 (子 plan §推奨実行順 #4、`64a2d6ab`…`93a3a6fb`) ⟹ 子 plan は
全 Phase 完遂**。P4/P5 (四角形と union の下方集合性 / 非空性 / 凸性) と P6 (補助アルファベットの
付け替え不変性 — `martonRegion_subset_union` が**任意 universe の四角形を union に吸収する**、
⟹ **L-BCO2 が「答えた」に到達**) は `MartonUnion.lean` へ、P7 (全支持の除去) は新規
`BroadcastChannel/MartonFullSupport.lean` (242 行 / 13 decl) へ。P7 の実りは `@[entry_point]`
`martonRegionUnion_subset_capacity` = 内界 union の包含から補助側の全支持仮説 (`hpV` / `hK`) を
一様測度への摂動 + 3 情報量の連続性で落とした版で、**残る明示仮説はチャネル側の `hW` のみ**。
攻略路は **MAC 側の完全な先例** (`mac_pentagon_subset_capacityRegion_allprob` + MAC の汎用混合
一族 = 後に `Probability/Mixture.lean` へ昇格し `mixLaw` 一族となる) がそのまま効き、superposition
側 S7 の混合不等式ルートは模倣不要だった (Marton 側は全アルファベットが `Fintype` ゆえ連続性
ルートが通る)。(5) semi-deterministic は L-BCO7 で「外界側だけで止める」と判断済 (**新規作業なし**)。

**✅ 子 §後続作業 も 9 leg relay で一括消化 (`aad88f5d`…`9081cef4`)** — 死んだ宣言の削除 / 命名束 /
汎用補題の `Shannon/` · `Probability/` への昇格 (**BC → MAC の import 辺が消滅**) / 重複の一般形への
畳み込み / module doc の実態同期 / 家系横断の `entropy_le_log_card` 重複統合 / MAC の `axis` 命名衝突の
解消まで。**残るのは子 §後続作業 の B-3 / B-4 / C-1 / C-2 / C-4 / D-2 / E-2 / F-14 / F-23 / G-1 のみで、
いずれも到達目標の前提ではない** (詳細と commit は子が SoT)。

> **Parent**:
> - [`textbook-roadmap.md`](../textbook-roadmap.md) §「Tier 3 — T3-C. Broadcast Channel (degraded) (Cover–Thomas Ch.15.6)」

## 要点 (再利用しうる設計判断)

- **T3-B MAC を verbatim 雛形**として domain/codomain swap + auxiliary RV `U` 圧縮で導出: `BroadcastChannel := Kernel α (β₁ × β₂)`、`BroadcastCode` は encoder un-curry (1 joint) + decoder 2 分離、`InBCCapacityRegion` は 2 inequality bundle (`R₂ ≤ I_u`, `R₁ ≤ I_xy`)。BC は 2 receiver 非対称ゆえ region `swap` は無効 (mono のみ)。
- **撤退ライン L-BC1〜L-BC5** (frozen slug、他 plan が参照): L-BC1 joint typicality multi-receiver body / L-BC2 Fano + chain rule **(再開 → genuine closure 済、子 [`bc-degraded-converse-plan.md`](bc-degraded-converse-plan.md)、headline `bc_degraded_converse`、Route B = entropy-difference / term-by-term degradedness)** / L-BC3 inner bound existence pass-through / L-BC4 outer bound `InBCCapacityRegion` pass-through / L-BC5 一般 (non-degraded) BC + Marton / Körner-Marton は完全 scope-out **(Marton 部分は解除・達成 → 子 [`marton-inner-bound-plan.md`](marton-inner-bound-plan.md) の headline `marton_achievability`。Körner–Marton は scope-out 継続)**。
- rate-bound 系の proof done closure は子 plan [`broadcast-channel-signature-rewrite-plan.md`](broadcast-channel-signature-rewrite-plan.md) 参照。

## Sub-plan 一覧 (backlink — plan_lint 双方向照合点)

| 子 plan | 担当 | 状態 |
|---|---|---|
| [`bc-degraded-converse-plan.md`](bc-degraded-converse-plan.md) | L-BC2 degraded converse single-letterization (`bc_degraded_converse` / `bc_input_singleletterize`、Route B) | CLOSED ✅ (genuine、`@audit:ok`、root 登録済) |
| [`broadcast-channel-signature-rewrite-plan.md`](broadcast-channel-signature-rewrite-plan.md) | BC rate-bound declaration の defect → genuine signature rewrite | CLOSED ✅ |
| [`bc-achievability-plan.md`](bc-achievability-plan.md) | BC (degraded) achievability = superposition inner bound (`bc_achievability`、Cover–Thomas Thm 15.6.2 達成側) | CLOSED ✅ (genuine、`bc_achievability` `@audit:ok`、`#print axioms` sorryAx-free、root+README Ch.15 登録済) — two-tier superposition random coding (E0 vanishing typicality-LLN + per-receiver Bonferroni + averaged swap + two-tier pigeonhole + ε-selection)、最終ゲート `bc_degraded_infoJoint_ge` (degradedness superadditivity `bcInfo₁+bcInfo₂ ≤ bcInfoJoint`) を stochastic Markov `U→Y₁→Y₂` 自作 + DPI で closure。撤退スロット L-BC1/L-BC3 未使用 |
| [`bc-general-region-plan.md`](bc-general-region-plan.md) | 一般 BC 容量領域フレーム (操作的容量領域の定義 → 協調外界 → UV outer bound → 一致クラス拡張) | **全 Phase 完遂 ✅** (Phase 5 = 等号 2 クラス + degraded 接続、Phase 2 = union の API 完成)。子 §後続作業 も 9 leg relay で消化し、残るのは B-3 / B-4 / C-1 / C-2 / C-4 / D-2 / E-2 / F-14 / F-23 / G-1 のみで**いずれも到達目標の前提ではない** — Phase 1 ✅ (`BCAchievable` / `bcCapacityRegion`) / Phase 3 ✅ (`bcOuterRegionCoop`、Wolfowitz strong converse の対偶で ambient 構成を迂回) / Phase 4a ✅ (`bc_uv_converse` = Nair–El Gamal 単一文字化、**degradedness 前提なし**、核は新規自作 `csiszar_sum_identity_cond`) / **Phase 4b ✅ CLOSED** (headline `bc_capacity_subset_uv` を**全平面版**で達成。union を無制約に取ると外界が平面全体に退化する反例 class は `IsUVChannelLaw` = 1 本の合成積恒等式で閉じた。**外界に符号制約を入れなかった判断が退化被覆のコストを約 15 行に決めた**) / **Phase 2 ✅ 完遂** 最小完遂 P1–P3 (`bcAuxAlphabet = ULift.{u} (Fin (k+1))` で `IsBCLessNoisy` の `Type u` 量化を 0 行で吸収) + 拡張 P4–P7 (`64a2d6ab`…`93a3a6fb`。**経路変更で「等号の前提」ではなくなっていたが API としては完成**。P6 で **L-BCO2 が「答えた」に到達**、P7 = 新規 `MartonFullSupport.lean` 242 行で内界 union の包含から補助側の全支持仮説を落とし残る明示仮説を `hW` のみにした。**MAC の `mac_pentagon_subset_capacityRegion_allprob` が同じ定理の完全な先例**で、superposition 側 S7 の混合不等式ルートは模倣不要 = **ルートが違えば行数見積りも移らない**) / **Phase 5 ✅ 完遂**: 定義段 ✅ (`Classes.lean`) / 内外の橋 S1–S6 ✅ (`MartonBridge.lean`、到達点 `marton_region_subset_uv`) / **less noisy の等号 ✅ S0–S8 全段 proof done** (`06817339`…`558b3fca`。内界を Marton union から **superposition** へ差し替え、S5 量子化 / S6 時分割 / S7 全支持摂動 802 行 / S8 組み立て + headline) / **more capable の等号 ✅ 到達** (`4a01dff8`…`594887a4`、`Superposition/MoreCapable.lean` 909 行 = 在庫の帯 820–980 の中。到達点 3 本 `bc_moreCapable_uv_subset_superposition` (逆包含、**`hW` を要求しない**) / `bc_moreCapable_capacity_eq_uv` / bare `bc_moreCapable_superposition_eq_capacity` で、`IsBCLessNoisy.isBCMoreCapable` 経由で **less noisy の 3 本を包含する** (畳み直しは意図的に未実施 = 子 §後続作業 G-1)。設計の要は 3 点 — (a) 受け皿の **新 `structure` は不要**だった (内界は `InBCCapacityRegion` を消費していない、`dep_consumers.sh` 実測)、(b) **第 3 制約を `max p.1 0 + p.2 ≤ bcInfoJoint` と書いて `bc_achievability_of_rate_lt` の `hJlt` に逐語で合わせる**判断が**内界の達成可能性からクラス仮説を完全に落とした**、(c) 唯一の新しい数学の核は **more capable の条件付き版** `IsBCMoreCapable.condMutualInfo_le` で、これが **less noisy の逆包含が捨てていた外界の 4 本目 `sumBound₁` を第 3 制約の担い手に変えた**。全 leg で **Mathlib の壁 0 件**、style PASS、honesty は新規 `sorry` 0 のため launch 条件外) / **degraded との接続 ✅ 完遂** (`bf49a91d`…`86b90af1`、新規 `BroadcastChannel/DegradedFromCode.lean` 213 行 / 4 decl。頂点は `@[entry_point]` `bc_degraded_converse_from_code` = 古典形 converse を符号に着地、加えて 6 行の系 `bc_degraded_capacity_eq_uv`。子 plan §存在しないもの (d) を `bcConverse_degradedBlock` が解消。壁は Mathlib / in-project とも 0 件 = **BC 家系 11 leg 連続**)。**子 §推奨実行順は #1 proof-log / #2 more capable / #3 degraded 接続 / #4 Phase 2 P4–P7 / #6 後続作業 relay がすべて完了、#5 semi-deterministic は L-BCO7 で判断済 = 新規作業なし ⟹ Phase としての残りゼロ**。**見積り精度の教訓 (子の判断ログ 23 / 24 / 26)**: plan の粗見積りは S7 で 6.5 倍・S8 で約 2 倍外れ、どちらも実測は**在庫 probe の帯の中**に入った ⟹ 新しい数学を含む leg は在庫 leg から始め、**「数学 (probe 実測) / 散文・section」の 2 列**で積む (2 列に切り替えた S7 / S8 / more capable の 3 leg 連続で帯内)。**degraded 接続で初めて下端を割った** (実測 355 行 / 帯 361–456、−2%) — 主因は**同じ leg の leg A が `private` 解除で上流公開した資産と主目標の結論が逐語一致した**ことで、⟹ **在庫は移設前の状態で見積もるので、資産移設は後続 step の見積りを下振れさせる**。加えて **在庫が plan の誤りを見つけたのは S6 / S7 / S8 / more capable の 4 leg 連続** ⟹ 「残るのは組み立てだけ」は入口の仮説の本数で検算し、plan が「既存の X では受けきれない」と書いたら着手前に `dep_consumers.sh` で X の消費者を確かめる。逆向きに **probe / 事前予測が外れる軸も 3 つ**判明 (個数 = 重複は 2 本でなく 3 本 / 自動性 = `inferInstance` は非 reducible な `def` で落ちない / **波及方向** = 非 reducible 化で落ちたのは下流 consumer ではなく当の宣言自身の証明本文)。**撤退ライン**: active は **L-BCO7 のみ** (semi-deterministic、判定対象は `bc_achievability_of_rate_lt` の全支持 3 本。等号を述べない判断が済んでいるので新規作業は生まない)。L-BCO1 / L-BCO4 / L-BCO5 / L-BCO6 は不発動、**L-BCO2 は P6 で「答えた」に到達**、**L-BCO3 は最後の判定対象だった more capable が閉じて retire**、**L-BCO9 は S5–S8 の 4 段連続で不発動のまま retire**、**L-BCO8 は無効化** (逆包含 `bcOuterRegionUV ⊆ martonRegionUnionFS` が偽と判定され枠組みごと失効。退避先だった「`sorry` で署名を保つ」は**偽の命題を署名に残すことになるため使用禁止**)、**L-BCO10** (degraded 接続の `bcConverse_block_append` が閉じない) **も不発動のまま retire**。**後続作業も 9 leg relay で一括消化 ✅** (`aad88f5d`…`9081cef4`。A / F-a / F-12 / F-5 / F-19 / F-21 / F-22 / F-c / F-28 / F-15 は先行して完了済) — 死んだ宣言 2 本の削除 (B-1 / B-5) / 命名束 (B-2 / F-1 / F-3 / F-4 / F-6 / G-2) / 汎用補題の `Shannon/` · `Probability/` への昇格 (F-15 / F-24 / F-b。**BC → MAC の import 辺が消滅**) / 重複の一般形への畳み込み (D-1 / G-3 / G-4) / `IsBCDegraded` の `Basic.lean` 移設 (F-29) / `CondMutualInfoMixture.lean` の分割 (E-1) / module doc と docstring の実態同期 (F-10 / F-11 / F-d / C-3) / BC 族外の `ℝ≥0∞` イディオム (F-25) / 家系横断の `entropy_le_log_card` 重複統合 / MAC の `axis` 命名衝突の解消。**残るのは B-3 / B-4 / C-1 / C-2 / C-4 / D-2 / E-2 / F-14 / F-23 / G-1 のみで Phase 5 の前提ではない** (F-9 / F-17 / C-5 / G-5 / F-26 / F-27 / F-20 / G-6 は `docs/rules/` · `scripts/` 側への起票または family 既定手の記録)。確定事実の台帳は [`bc-facts.md`](bc-facts.md) |
| [`bc-open-problem-plan.md`](bc-open-problem-plan.md) | 未解決本体 (一般 BC の計算可能な容量領域の特徴づけ) への攻略 — **進め方 (メタプラン) 策定済 + L0 (文献ランドスケープの verbatim 確定) / L1 (数値 probe 基盤) 完了。軸ごとの本格 attack は L2 から**。三層 (散文 → proof-probe → 形式化) × 階段 T0–T3 × 台帳。ブランチ `bc-computable-region`。**本 relay のゴールは散文レベルの候補経路 (5 条件つき) で、形式化はスコープ外 = 次 relay**。**L0 が攻撃軸を組み替えた** (逐語引用・出典・再検証手順は [`bc-facts.md`](bc-facts.md) が SoT): (a) inner = outer と outer = capacity は既知の負の結果で閉じている (BSSC の sum rate ギャップ / NEG outer の strict suboptimality) が、**「inner = capacity」は死んでいない** — Marton 内界の最適性は未解決の予想。(b) **UV は最良既知 outer ではない** — Gohari–Nair の auxiliary-receiver 外界 (Theorem 7) が真に改善しており、その導出法が軸 B の第一手「UV 導出で捨てた項を拾う」そのもの ⟹ 軸 B の主語を「改善項 ↔ 我々の Lean 証明ステップの対応付け」と「J-bound の最適性 (Li 曰く unknown)」へ差し替え。(c) **BC 容量領域が単一文字特徴付けを持つかは `Δ₁₈ᴴ` で肯定的に決着済** (Li 2021) ⟹ **軸 D は主語を「linear entropy hierarchy の最低レベルはどこか」へ差し替えて優先度 1 → 2 へ降格**。加えて「BC 容量領域の計算可能性が未決」と述べる一次文献は NOT-FOUND で、この標的を使うにはまず出典を立てる必要がある。(d) **軸 G (Gohari–Liu–Nair ISIT 2025 の Markovity 予想) を新設・優先度 1** — 数値証拠のみで提案された真の未解決問題で、論文に完全に明示された局所最大点インスタンスが数値 probe 基盤の検証を兼ねる。ユーザー提供の外部ノート ([`bc-external-note-tensorization.md`](bc-external-note-tensorization.md)) は**文献層 (V6–V10) が L0 の検証を通った一方、その還元 (未解決問題 = Marton 領域の自己テンソル化の可否) は既知の枠組み**と判明 (Additivity Conjecture = Anantharam–Gohari–Nair 2019、Gohari–Liu–Nair ISIT 2025 の Conjecture 1 として再掲) ⟹ **軸 E は優先度 1 を維持するが、根拠を「本丸への最短路」から「probe 基盤の歩留まり」へ差し替え**。新規性を置ける場所は還元の先 (否定側インスタンス / support function 版と双対汎関数版の同値性 / 軸 G) だけ。**L1 は探索の道具立てを完成させ、副産物として文献側の所見を 1 件生んだ**: (i) 汎用 probe 基盤 [`bc_probe.py`](bc_probe.py) が建ち、既知の答え (Gohari–Liu–Nair ISIT 2025 §III-B の目的値 4 つ) を印刷 8 桁全桁で再現して**検証済** ⟹ kill-first 規約 (反証ファースト) がここで初めて実効化した。(ii) 外部ノートは**恒等式層 (V1–V3) が 3 本とも真** (V1 は掃引ではなく係数相殺による証明) だが、**軸 E の生死を決める還元層 (V4/V5) は未試験**。(iii) 再現の副産物として §III-B の点が**論文自身の局所最大性条件 2 本を違反する**ことが判明 (独立監査 CONFIRMED、肯定コントロールつき。ただし論文が引く濃度限界 `\|U\|+\|V\| ≤ \|X\|+1` の内側では真の局所最大) ⟹ 軸 G を塞いでいた障害主張が弱まり、**軸 F が優先度 4 → 3** へ (主語も「濃度限界の改善」から「**濃度限界がどの主張の生死を分けるか**」へ差し替え)。**L2 は依然として本 relay の分岐点** (還元が我々の定義の上で生きているか — L1 が通したのは恒等式層だけで還元層の信頼度は上がっていない) だが、生きても既知の枠組みの再確認であって新規性ではない。**L2 の答え = 還元層は我々の 2 補助変数 `def` の上では死ぬ。ただし死因は Marton 内界の非加法性ではなく共通補助 `U₀` の欠落**で、否定側インスタンス (11) の数値探索は**保証された偽陽性**を出す ⟹ **軸 E は優先度 1 → 3 へ降格** (残る標的は support function 版と双対汎関数版の同値性 — L2 で「別命題」と確定し、実体のある未解決の小問になった)。分岐は選ばれず round-robin 継続。**L3 (軸 A の第一手) は FAIL** — `IsBCMoreCapable` を `IsBCMoreCapable.condMutualInfo_le` の結論に差し替えると通らず、しかも**両者は同値** (両方向 `#print axioms` sorryAx-free = 本 relay 初の `machine` 行) ⟹ 新クラス候補ゼロ。⚠ 軸 A 自体は生存 (死因から子 attack 2 本)。副産物として**実使用仮説の棚卸しは証明本文の `rg` ではなく `dep_graph.sh` の前方閉包で行い、辺の向き先が `def` 自身か名前つき補題かを区別する**という手順の訂正が出た (`exact hmc _` 形の `def` 直接展開は `rg` から構造的に漏れる)。**L4 (棚卸し #1)** は強制 park 対象ゼロを確認した上で idea 6 本を起票し、**ゼロ進捗カウンタが §5-1 と両立せず構造的に発火しえない**ことを検出、**経路構築への切り替えを L7 に固定**して L5–L18 の可変枠に slug を割り当てた。**L5 / L6 で優先度 1・2 の軸が同じ leg で 2 本とも否定的に決着した**: **L5 (軸 G) = Markovity 予想は我々の 2 補助変数 `def` の上へ「述べられるが、述べた形 (M2) は偽」** — 数値反例 `+7.147687e-04` (hardening 3 段 + 独立 4 系統 + 肯定コントロール 3 本)。⚠ **論文の Conjecture 2 の反例ではない** (`F` に残る `−αH(Y) − (λ−α)H(Z) + Σ p(x)a_x` は共通補助 `W` 由来で線形項では相殺できない) ⟹ 軸 G は前提工事待ちで**軸 E と同一の工事へ合流**。副産物は「`h_free` の最適点が非 Markov ⟺ 非 rectangular」の完全一致 (90 点中 89 点が決定論的・矩形・Markov、破れる 1 点がそのまま反例)。**L6 (軸 B) = Theorem 7 との対応付けは同定完了・帰結は否定的** — 不等号ステップは L4 の名前フィルタが言う 21 本ではなく**結論形で数えると 39 本** (⚠ conclusion-shape 検索の実例がまた出た)、対応は matched 22 / no-slack 12 / unmatched 5 で、**`unmatched` 5 本はいずれも最終領域を動かさない** (領域は全 UV 法の合併ゆえ独立な `Q` を取れば消える / 残る 1 本は `n` 文字項で単一文字化には新補助が要る = Theorem 7 がやったこと) ⟹ **我々固有の exploitable な緩みは 0 本**。軸 B の (ii) (J-bound 最適性) は開いたままだが `bcOuterRegionJ` の新設 = **層 3 でスコープ外** (`outer-region-j-def` として次 relay へ)。⟹ **否定 2 本が同じ場所を指したことが最大の産物** — 軸 G の続行にも軸 E の (ii) にも `{a_x}` ↔ `θ` の Legendre の段が要る ⟹ **L7 の経路の主語を `support-vs-dual-additivity` に据えた** (層 1–2 に収まる: `F` の実装は L1 に、領域側 `h` は L2 / L5 にあり、要るのは 2 つを繋ぐ導出だけ)。**L7 (経路構築 #1) で候補経路 R1 が立った (`unprobed`)**。最大の産物は**骨格の訂正** — **`{a_x}` と `θ` の間に Legendre 変換は存在しない**。`θ` は `F` の**内部パラメータ `(α, μ)` として現れる** (`μ` が方向そのもの、`α` が総和レート制約 2 本への乗数の配分) のであって `a` と共役ではなく、正しい絵は `min_α max_p min_a` の **3 段入れ子**で Legendre は最内層の 1 箇所だけ (旧記述のままだと `θ` 側にも Fenchel を探しにいって leg を 1 つ溶かす)。R1 が最初に壊れる場所も同定済 (① step 6 のタイトさ = 最大化点で `S₁ ≥ A₂`。半分の `S₂ ≥ A₂` は L7 が証明済 / ② (H1) の読み = Conjecture 1 で `(α,λ)` が量化されているか、逐語未確認) ⟹ 子 attack 3 本を起票。同居 probe `u0-necessity-quantified` も**肯定的に決着** — `\|X\|=3` の Blackwell チャネルで `U₀` を持たない 2 補助変数 Marton union の閉凸包が時分割領域より真に強い (閉形式 `(ln3 − ln2)/2`、最適化非依存) ⟹ **L2 が観測した「時分割への潰れ」は `\|X\|=2` に固有**。⚠ **`U₀` が不要だとは言っていない**し (比較対象は素の TDMA 三角形)、Blackwell は教科書的対象ゆえ**新規性は主張しない**。**L8 (経路 #1 の probe + novelty gate) で R1 は (4) を通り (5) で落ちた** — 一次文献 3 本 (Gohari–Liu–Nair ISIT 2025 / **原典 Anantharam–Gohari–Nair, IEEE TIT 65(3), 2019** / Nair ITA 2020) を取得した結果、(H1) の読みは文献どおりで **R1 は数学的に無傷**だが、**12 step すべてに逐語出典が付いた** (死因 `known-result`) ⟹ R1 に残る仮定は **(H1) = Conjecture 1 ただ 1 本**で、これは著者らが既に持っている含意に一致する。副産物として **L2 / L7 の 2 つの誤判定 (「(a) 閉凸性 / (b) 双対の段のどちらの文献も供給していない」/「`S₁ ≥ A₂` は未決」) がどちらも原典 1 本の取得で解消** ⟹ **教訓 = 一次文献を先に取る** (原典は検索では届かず著者の publications ページ経由でのみ取得できた)。⚠ **R1 は `refuted` ではない** — 軸 E のどこが開いているか (= Conjecture 1 ただ 1 点) を逐語出典つきで示す**地図**として残す。**L9 (棚卸し #2) は残り枠を gate-first へ組み替えた** — 実測 2 つ (経路 1 本 = 2 leg / gate は一次文献の取得だけで経路を丸ごと殺すのに構築 1 leg より安い) から **[探索 + gate] → [構築 + probe]** の 2 leg 1 組へ。あわせて §5 に 11 項目め「一次文献を先に取る」を追加、`markovity-conjecture` の park 再開条件を `marton-3aux-probe` の 1 本に絞り (「形式上満たされた」だけでは満たされない)、最低ライン (経路 ≥ 2 本 / うち `probed` ≥ 1 本) の到達可能性を**到達可能だが条件つき**と判定した (条件 3 つ・代替収穫物 4 本・ユーザー判断の論点 2 つは子 §7) **L10 (軸 C) は relay 初の有効経路を供給した** — 1 leg で [探索 + gate] と [構築 + probe] の両方が済み、**経路 R2 が `probed` で成立** (主張 = [GJNW13] Theorem 1 の `\|X\| ≥ 3` 版の鋭い加法スラック `f*(k)` は決定論的な格子 BC で達成される。確定値 `f*(2)=0` / `f*(4)=1` / `f*(9)=log₂3`、未決は `f*(3) ∈ [0.694242, 0.792481]`。⚠ 核 step 9 は `gap` で証明も反例も無い)。候補 G1 / G3 は明示 witness で `refuted`。**idea `shannon-cone-lp` (LP gate) は原典が否定した** — [GJNW13] §I-B-2 逐語で「この不等式は `Γ*₅` を知っていても導けない、原因はカーディナリティ制約」⟹ 濃度制約つき候補は LP では殺せないので **novelty gate は LP ではなく数値 probe で回す**のが以後の既定。 **L12 (軸 A) は gate が候補を殺して軸ごと閉じた** — 「クラス定義の帰結を定義に採って真に広いクラスを切り出す」という第一手は、`∀p` 型の述語に対しては**平均化で恒等的に同値へ戻る**ので無効で (L3 の機械検証はこの一般則の一例)、有効にする唯一の方法である「量化範囲を狭める」は **[N10] = C. Nair, *Capacity regions of two new classes of 2-receiver broadcast channels*, IEEE TIT 56, 4207–4214, 2010** が *sufficient class* として 2010 年に実行済 (Definition 2/3 = essentially less noisy / essentially more capable、Remark 3 が collapse を逐語、Theorem 2 が EMC の容量領域、Claim 2 (vi) が more capable ⊊ EMC の 4 文字 witness) ⟹ 子 2 本とも `killed(known-result)`。**⚠ 軸外の収穫が本体**: **[N13] = C. Nair, *Upper concave envelopes and auxiliary random variables*, Springer IJAESAM 5(1):12–20, 2013 の p.2 が「A computable characterization of the capacity region for the two-receiver broadcast channel is still unknown, and is one of the most fundamental open problems」と逐語で述べ、脚注 1 で computable を「任意の ε に対し有界時間 `T_ε` で ε 近似できる」と定義している** ⟹ **L0 で NOT-FOUND だった F8 の出典が立った** (⚠ この語義は Boche 系の Banach–Mazur 非計算可能性とは別物で、標的は**肯定側**)。副産物として **[N10] Theorem 2 は `bc_moreCapable_capacity_eq_uv` の真の一般化**ゆえ次 relay の形式化標的、子 attack 2 本 (`eln-canonical-class` / `superposition-optimality-scope`)。 **L13 (軸 C) は構築枠を使ったが候補 3 本とも死に経路は増えなかった** — 子候補 `jognair-channel-dependent-slack` (R2 の加法定数を捨ててチャネル依存汎関数へ) の 3 案は明示 witness の直接評価で全滅し `killed(numeric-counterexample)`、うち 2 案は**記録済の必要条件を全部通したうえで generic に死んだ** ⟹ 子 plan に運用規約 §5-12 (generic-instance-first = 第 3 の必要条件「非決定論的チャネルでの局所最適性」と generic な非退化インスタンスを構築の**前**に当てる) を追加。⚠ **副産物のほうが大きい** — **(H1) 厳密なスラック恒等式 `gap = min{H(X\|Y),H(X\|Z)} − L'`** (係数相殺で*証明*、`L'` は非負 4 項の損失) と **(H2) 比較補題**が、軸 C の次の問いを「非負 4 項損失 `L'` の下界」へ厳密に言い換え、子 attack `slack-loss-lower-bound` として起票された。 **L14 (棚卸し #3 + 収穫判定) は生存経路を台帳から再導出して 1 本と確定し、事前登録の「1 本以下」の枝を*順序を組み替えて*発動した** — L15 = 軸 C の子 `slack-loss-lower-bound` (1 leg 打ち切り)、軸 G ブロック (3 leg) は L15 が経路を返さなかった場合に L16–L18 で発動する条件つき。強制 park 0 / 最低ラインの数値は据え置き / ユーザー判断は不発 (諮る条件は生存 0 本)。 **L15 (軸 C の子 `slack-loss-lower-bound`) で経路 R3 が立ち、最低ライン (経路 ≥ 2 本 / うち `probed` ≥ 1 本) に到達した** — 核は **(H4) grouping 超加法性**: `X` の任意の関数 `W = m(X)` に ついて `L'(p) ≥ Σ_w p(w) L'(p_w)` (同値に `T(p) ≤ H(W) + Σ_w p(w) T(p_w)`) が **4 項の項別分解に よる係数相殺で*証明*済** (`prove_identity` が 4 本とも残差ちょうど 0)。`\|X\| ≤ 2` を [GJNW13] Theorem 1 で閉じると、**最適化を含まない再帰 `Λ_split`** が `gap` の証明された計算可能な上界を 与え、**格子 BC 全部 / `\|X\|=2` / P2 の kill witness の 3 種類で厳密**になる (敵対的探索でも 違反なし、最良 margin `−6.9e-08`)。novelty gate は一次文献 5 本で単独通過 (最近傍の [GNA12] Equation (2) は**上凸包**を使う別対象)。⚠ **周辺だけの緩和 3 本 (`Λ_sup` = Nair–El Gamal 外界の 和レート / `Λ_ib` = information bottleneck 曲線の和 / その max) は (N1) 不合格が**証明書つきで**確定** (最適化不使用の 2 値 `U` 格子、deficit `+0.031338`) ⟹ **一般化 = 「(N1) を満たす `Λ` は [GJNW13] Theorem 1 を含意するので、緩和で再現を期待せず base case として*内蔵*せよ」** (子 plan §5-13 provable-first)。副産物に **(H3) 超過分の 2 項分解** (担うのは常に `I(U;V\|Z)`) と **`Λ_sup` が鋭い ⟺ `p` で less noisy 順序がつく** (34/36 一致)。 **L16 (軸 C の子 `dominated-channel-envelope`) で経路 R4 が立ち目標本数 3 本に到達** — 核は **(H5)**: `X` からのチャネル `W` が `Z` に more capable で支配されるなら `K(U) ≤ I(X;Z) − I(U;W)` (既知不等式の連鎖のみで*証明*済。`W = Z` で既存の `I(X;Z\|U)` に一致 ⟹ 構成から `Λ_env ≥ Λ_sup`)。⚠ **新規性は (H5) に局限** — gate は `Λ_env` という*対象*を FAIL 寄り保留、構造的な読み (利得 = `Δ_W` の凹性の欠損) を **FAIL (既知、van Dijk 1997)** と判定。改善は「more capable だが less noisy でない」順序の隙間に局在し **dense の残距離は動かない**。副産物 = **消去族は死ぬ** (頂点で `H` の傾きが無限大 ⟹ `γ* = 0`) ⟹ `W` は「出力を粗くする」型では作れず **`Z` と非可比な方向へずらす**必要がある。 **L17 (`Λ_comb` の鋭さ) は本数を増やさず鋭さを撃ち、詰まらなかったが理由を確定させた** — **段 A**: 多分割 `\|W\| ≥ 3` は 2 分割再帰に**完全に含まれる** (差ちょうど `0.000e+00`) ⟹ 新規の余地ゼロ。**(H6)**: 非決定論的 `W` でも超加法性は**罰項なしで成立**し記号的に証明済 (⚠ L15 の `H(W\|X) = 0` は不要だった)、**同値な言い換え = `L'` は入力分布 `p(x)` の凹関数**、`T` 形では罰項が `H(W)` → `I(X;W)` に下がる ⟹ **(H4) の拡張かつ強化**。上凹包 **`Λ_cav`** は `Λ_split` の残距離を **−30%** 改善する (⚠ 代償 = 最適化を含む形になるが、単体格子 LP に落とせば `L'` の下界であることは保たれる)。⚠ **しかし `Λ_comb` の dense 残距離は 1 桁も動かない** (`0.002955` → `0.002955`、`slack/gap` 中央 `1.160` → `1.160`) — **理由は確定**: `max` を取るのは 10/10 本で `Λ_env` 枝、`Λ_split` 枝が勝つのは `\|X\| = 2` だけ (`\|X\| ≥ 3` の 140 本で 0 本。⚠ `Λ_sup` の支配が*証明された*わけではなく探索強度依存の下界)。⟹ **教訓 (一般化して持つ) = `max` で結合した下界を詰めるときは、まずどちらの枝が `max` を取っているか (`argmax` の分布) を測る。「A 側で動かなかった」から「B 側が詰めどころ」は導かれない** (L16 が L17 へ渡した設計指示がこの誤りだった)。副産物に **facts §L15 の [GNA12] Equation (2) の同定の訂正** (上凹包は Eq. (2) ではなく Conjecture 1 / 3 の側の道具。結論は不変だが誤った事前確率を kill-first に与えていた)。 **L18 (R4 step 5 の admissibility = 探索の最終枠) は資格を証明側へ動かしたが鋭さは動かさなかった** — 核は **cell 証明書**: `A_r(p) := Σ_x p_x D(W_x‖r)` が `I_p(X;W) = A_r(p) − D(pW‖r)` により `p ↦ I_p(X;W)` の**アフィンな優関数**であることと `p ↦ I_p(X;Z)` の凹性から、部分単体上で `Δ_W(p) >= Σ_i λ_i [Δ_W(v_i) − D(v_i W‖r)]` ⟹ **全頂点で頂点 KL 条件が通れば cell 全体で `Δ_W >= 0`** ⟹ admissibility が**有限個の不等式へ還元**され判定は 3 値 `CERT` / `REFUTE` / `INDET` になる (⚠ **これは証明** — 掃引ではない。残るリスクは浮動小数点のみで探索強度に依存せず、`mpmath` 50 桁の再評価で同一判定)。⟹ **R4 step 5 のラベルは `gap` → `probed (per-instance certified)`** (`Λ_env` の計算に実際に使う `W` の**全数 71 本が `CERT`**、INDET 0 / REFUTE 0。⚠ **一般の `W` に対する*定理*が立ったのではない**)。**出口条件 (B) = 生存** (`𝒲` を証明済に制限しても改善は残る — `\|X\|=2` の最適化不使用の表で残存率 中央 `1.0000` / 最小 `0.9913`)。⚠ **ただし `Λ_comb` の鋭さは動かない** (dense 残距離の中央 `0.002984` → `0.002644`) ⟹ **L17 の結論は不変**。**副産物 2 つ**: (i) **資格の穴が本質的であることの 4 行の証明** — `𝒟_Z := {Z∘Q}` は 4 つの閉包演算すべてで閉じるので **`{Z}` の閉包 ⊆ `𝒟_Z`**、そこでは DPI から `Λ_env = Λ_sup` で**利得はちょうど 0** ⟹ **非劣化の base point が別途要る** (⟹ 「利得は more capable だが less noisy でない隙間にしか無い」という §L16 の結論が **van Dijk を経由せず定義だけで出る**)、(ii) `Z` が消去チャネルなら membership は閉形式 `η_mc(W) := sup_p I_p(X;W)/H(p) <= 1−ε` で決まり、`W = BSC(δ)` では **Mrs. Gerber の補題**から `η_mc = 1−h(δ)` ⟹ **`ε <= h(δ)` は引用ではなく*証明*として持てる**。⟹ **教訓 (一般化して持つ) = 「安い証明」と「無価値な証明」は一致しうる** — 閉包性は 4 行で証明できるがその範囲の `W` は利得が定義から 0 ⟹ **資格を証明しにいく前に「証明できる範囲に利得があるか」を先に測る**。⟹ **判断ログ 30 の事前固定した退路が完全に成立** ((i) は L17 で、(ii) は L18 で決着) ⟹ **L19 は探索せず経路 3 本の書き上げに充てる**。 | **20 leg 完走 ✅** (relay 終了。**L0–L20 ✅ 完了 = 20/20**。**§3.1 の 5 条件を満たす経路は 3 本 = §3.3 の目標 (経路 ≥ 3 本・うち ≥ 2 本 `probed`) に到達** — **R2** (`probed`、⚠ 核 step 9 は `gap`) + **R3** (`probed`、**核 (H4) は証明済**) + **R4** (`probed`、**核 (H5) は証明済**)。⚠ **留保 3 つ**: **3 本とも step 1 ((H1)) を共有**ゆえ完全独立ではない / **R4 は R3 の step 7 の強化で産物は同じ `Λ_comb` に合流する** ⟹ 独立な 2 本ではなく**相補的な 2 本** / **R4 の新規性は (H5) に局限される**。加えて 3 本が与えるのは `gap` の上界であって `T` の閉形式ではない。R1 は (5) 不合格で経路表に載らない。⚠ 本数の現物は台帳から再導出すること。**L19 (収穫) は routes.md に合流節を新設** (`Λ_comb` の内訳表 + 引き継ぎ)、**L20 (記録) は proof-log + metrics を新設**し繰り返し失敗形を抽出。**継続するかは proof-log §6 の選択肢 A (層 3 へ切り替え) / B (軸 G 発動) / C (同方向継続・非推奨) から — ⚠ 判定であって決定ではない、ユーザーが決める**) |
| [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) | **未解決本体 (T3) への攻略 — ゴールは「一般 2 受信者 DM-BC の容量領域の計算可能な特徴付けを肯定側か否定側で決着させる」で、散文の証明と Lean での proof done の両方をもって完了とする** (ユーザー決定、2026-08-02)。前 relay ([`bc-open-problem-plan.md`](bc-open-problem-plan.md)) を承継するが**ゴールが違う** — 前 relay は T2 (中間結果・散文のみ) で完走し、その到達点は本丸ではない。⚠ **本 plan は §0.1 にゴール保護条項を持つ** (完了条件を狭める変更は `GOAL-CHANGE` として起票しユーザー承認まで適用しない。前 relay で L0 前にゴールが独断で狭められた事故の再発防止)。**標的の語義は [N13] 脚注 1 の「任意の `ε` に対し有界時間 `T_ε` で `ε` 近似できる」で、Boche 系の Banach–Mazur 非計算可能性とは別物**。**単一文字性は標的ではない** (F7 で肯定的に決着済、脚注 3 が計算容易性と分離)。**L0 の一次文献ゲートが軸を組み替えた** (逐語は [`bc-facts.md`](bc-facts.md) `## L0 (T3)` 節が SoT): 原案の 3 分解 (C1) 内界列 / (C2) 外界列 / (C3) 有効収束率 のうち、**(C1) は既に在り** (正規化 n-letter Marton の増加列が容量領域へ収束し、各 `n` の領域はカーディナリティ境界により計算可能 ⟹ 容量領域は **computably overt**)、**(C3) は独立の課題ではない** (計算可能解析の標準定義から両側が揃えば停止則は自動に従う ⟹ (C2) に吸収) ⟹ **穴は (C2) = 上から収束する計算可能な外界列の不在ただ 1 つに絞られた** (= 容量領域の **effective compactness** が未知)。**L1 が穴の位置をさらに精確にした** (逐語は [`bc-facts.md`](bc-facts.md) `## L1 (T3)` 節が SoT): **和集合形 (`ΣH₁`) の外界は多文字化しても原理的に無駄**である — 優加法性から `∩_n (1/n)·UV(W^⊗n) = UV(W)` となって列が定数化し、`UV(W) ⊋ C(W)` の witness が 3 本ある ⟹ 改善はゼロ。その構造的な理由は **多文字化は内界には効くが外界には効かない**という非対称性 (積補助変数の構成は領域を単調に大きくするので、内界には前進・外界には後退) ⟹ 穴の記述は「**外界列が無い**」ではなく「**多文字化*以外*の機構が要る**」へ動いた。**L2 で (C2) の内容そのものが読み替わった** (逐語は [`bc-facts.md`](bc-facts.md) `## L2 (T3)` 節が SoT): (C2) は「上から収束する外界**列**を作る」ことではなく **「`C` を共通部分にもつ、`k` と `W` について一様に半計算可能な外界の可算族が 1 つでも存在するか」**と同値で (**減少性も有効収束率も要らない**)、さらにそれは **「基数有界かつ閉アトムの一階特徴づけが存在するか」**と等しい。**`Thm7(W)` (= [auxrec] Theorem 7、`∀J` 形 = `ΠH₂`、[Li21] の言う「J 版 UV 外界」と同一物) が `W` について一様に effectively compact であることは証明できた** ⟹ **(C2) ⟸ (`Thm7(W) = C(W)`)** で、**残る穴は `Thm7 = C` の厳密一致 1 点**。⚠ **この還元先は一次文献が「optimality unknown」と明言する未決命題** — 「未決」であって「偽」ではない。⟹ **T3-α の次の一手は attack `t3-thm7-tightness`** (最初の一手は反証試行 = BSSC で厳密な分離が出るか。分離できれば還元先は死ぬが同時に Marton 予想が偽になる ⟹ どちらに転んでも軸が 1 本確定する)。あわせて **`∃` 形外界の多文字化は一般に無駄**であることが十分条件 (H1)–(H5) つきの定理として立ち (UV はその特別な場合)、⚠ **射程は「多文字化」1 機構のみで `∃` 形外界の別の可算族は排除されない**。**T3-β 側には証明テンプレートが見つかった** (単調列の存在を仮定 ⟹ Banach–Mazur 計算可能 ⟹ 既知の非計算可能性と矛盾) が、**否定側は先に「BC 容量領域は Banach–Mazur 計算可能でない」を立てる必要がある** ⟹ **難度評価はさらに上がった**。⟹ **軸は T3-α 肯定側 (外界列の構成) と T3-β 否定側 (effectively compact でないことの証明) の 2 本が優先度 1** で、同じ穴の表と裏である。**T3-γ (階層の最低レベル) は `killed`** — [Li21] 脚注 3 と §VIII の定義候補 3 が「階層のレベルを下げても計算容易性の証明可能な利得は無い」と明記しているため (⚠ Li 自身の open problem としては生存ゆえ収穫候補には残す)。**T3-δ (`Λ_comb` の載せ替え) は優先度 3** で、載せ替え先が (C2) に確定した。⚠ **難度の較正 (我々の演繹、未 probe)**: **否定側は Marton 最適性予想の反証を含意する** ⟹ 原案が T3-β の根拠にしていた「BC の一次文献が無いから新規性は構造的に確保されている」という読みは**撤回済**で、新規性ではなく**難度**の問題である。あわせて **F8 の NOT-FOUND は 2024 年の一次文献 (Boche–Deppe arXiv:2101.09754v3 p.2 逐語) で解消**。**20 leg、うち L16–L18 は層 3 (形式化) の集中枠として先に予約** | **CLOSED (ゴール未達) — L0–L20 完了 ✅ = relay 終了 (残枠 0)。⚠ §0 のゴールは未達 — (C2) は未着手のまま 1 ミリも動いていない。⟹ ユーザー決定 (2026-08-03) で継続が選ばれ、後継は [`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md) (下行)。⚠ この plan 自体は閉じており書き換えない** (⚠ **ここは子の状態キャッシュで、直近 3 leg + 終端状態だけを置く** — L0–L16 の到達点と逐語は子 plan §1.1 / §5 と [`bc-facts.md`](bc-facts.md) の `## L0 (T3)`–`## L7 (T3)` 節 + 在庫 [`bc-t3-cardinality-inventory.md`](bc-t3-cardinality-inventory.md) §11–§16 が SoT。⚠ **直近 4 leg (L16–L19) は層 3 の実装線 = 補助変数の基数境界**を走っており、散文線 (軸 T3-α/β = 容量領域の effective compactness、本命 attack `t3-thm7-tightness`) とは**別の線**である — 片方を読んでもう片方の現況を推測しないこと。 **L16 = gateway atom + 全重み版 (⚠ 壁 0 件)** — [GA09] の別 grouping で目的関数が**全重み域に載る**ことが確定し、**子の判断ログ 10 の射程限定は撤回された** (同 12 = **「この機構では載らない」という未検証の否定的判断も、資産の棄却と同じ強度の反証を要求する**)。逐語の SoT は在庫 §12。 L16 / L17 は規約ゲート PASS で honesty ゲートは launch 条件外により不発火、**L18 / L19 は両ゲートとも実走して PASS** / **撤退ライン 3 本とも不発火**。 ⚠ **予算は据え置かない — 「総額据え置き」は撤回した (子の判断ログ 11)**: (a′) 累計の実測は **2553 行** (7 file) で §8.2 の合計上限 920 行を大きく超過し、**超過幅は L19 でも狭まっていない** (超過幅・内訳・再導出コマンドの SoT は在庫 §16.6。⚠ **「内数の再配分」で隠さない**)。⚠ 超過は **(a′-1) 側**に局在 (測度形 ↔ ベクトル形の橋 / L17 の `CardinalityBound.lean`)。⚠ **B には予測帯が無かった** (子 §5.4 が「§14.7 の帯を B へ流用しない」と書いた結果、帯を立てないまま着手した) ⟹ **L19 の数字は予実差ではなく実測のみ**。⚠ **L15 の「鏡像で主因が倍化する」予測は不発** — L19 は鏡像 (`q₂` を動かして `V₂` 自身の台を縮める形) を実装せず、**swap 対称性 4 本で L17 の headline を再利用**した。**可変枠 L2–L15 / 集中枠 L16–L18 / L19 / L20 をすべて消化した ⟹ 残枠 0**。 **L17 = 台の部分型化、GO / ⚠ 壁 0 件・Mathlib 側の不足 0 件** (`6aebdccf` → 両ゲート `29ff3cd1` → 在庫同期 `b0d485e1`) — 新規 1 ファイル `Marton/CardinalityBound.lean` に **16 decl** が landing し、**`V₁` (外側補助変数) 側の基数上界**が `exists_bcAuxAlphabet_card_le_martonWeightedSumAllWeights` (`@[entry_point]`) として立った (`martonAuxBound` も Lean 化)。⭐ **ルートは S′ で決着し、L16 時点の申し送りは 2 点とも覆った** — (a)「台の部分型へ落とす段は**本当に無い**」は**誤落判定**で、向きを反転すれば `Subtype.val` が大域単射になり既存資産 2 本で **6 行**だった、(b) gateway atom 候補の **`Set.InjOn` 版エントロピー不変性は不適** (S′ では一度も要らず、代替のルート I は**コンパイラで落ちている**)。⚠ **訂正の所在は在庫 §13.1** (子の新規 判断ログ 13 = **「この段は無い」の根拠が*ある対象の非存在*であるときは、その対象を本当に経由しなければならないかを先に問う**)。ゲートは **honesty = OK (tier 1) / 規約 = PASS**、**撤退ライン 3 本とも不発火**。 **L18 = A (閉凸包版の領域等式) を proof done で closed、⚠ 壁 0 件** (`36d0180b` → honesty `3341783e` → style `fb59985a` → 改名 `85c35e5a` → `c5708d1d` → `a6941ed4`) — 新規 1 ファイル `Marton/RegionCardinality.lean` に **11 decl** が landing し、headline `closure_convexHull_martonRegionUnion_eq_outerBounded` (`@[entry_point]` + `@audit:ok`) が **Marton 合併の閉凸包を「外側補助変数の基数を切り詰めた合併」の閉凸包と等号で結んだ**。`@residual` は 1 本も立っていない。⚠ **命名は style ゲートが `Bounded` → `OuterBounded` へ 7 名改名した** (`Bounded` は**両方の補助変数が有界だと誤読させる** — 実際に切っているのは外側 `k₁` だけ、在庫 §15.5)。⚠ **実測の SoT は在庫 §15**。⟹ **子の新規 判断ログ 15 = gateway atom の完証が言うのは「最難関が片付いた」ことだけであって、残りの段の在庫が取れていることではない** — 在庫 §14.8 の「残るのは行数と配線だけ」に対し**記載から漏れていた前提が 3 件**あり (分解形への橋 / `0 ≤ martonInfoV₁V₂` / 下方集合の凸包の下方性)、⚠ **うち 1 件は Mathlib に無く 20 行の自作**だった (在庫 §15.1)。判断ログ 16 = **loogle の生クエリのヒット数を「無い」の根拠に使わない** (素の `IsLowerSet, convexHull` は `Found 2`、0 になるのは結論形クエリのほう)。 **L19 = B (`V₂` 側の基数) を proof done で closed、⚠ 壁 0 件 / 自作補題 0 本 / loogle 呼び出し 0 回** (`d5ed7bdc` → `14bdd2f3` → honesty `a9787043` → style `adbceb76` / `c793cedb` → 在庫 `7c2c6bbf`) — 新規 1 ファイル `Marton/Swap.lean` (swap 対称性 4 本) + 既存 2 ファイルへの追記で headline 2 本 `exists_bcAuxAlphabet_card_le_martonWeightedSumAllWeights_inner` / `closure_convexHull_martonRegionUnion_eq_bounded` (ともに `@[entry_point]` + `@audit:ok`) が立ち、3 ファイルとも **0 sorry / 0 `@residual`**。⟹ **`k₂` も `martonAuxBound α` で切れた** = 両補助アルファベットの走る基数が有限個になり、**L18 が残した「`k₂` は `ℕ` 全域のまま」は解消した**。⚠ **実測の SoT は在庫 §16**。⟹ **子の新規 判断ログ 17** = 「2 段の縮小が互いを壊さないか」という危険視の要否は変換自身ではなく***下流の消費形***で決まる (在庫 §14.5 項目 3 が `ncard` 保存則の形で危険視した段は、分離ベースの証明に現れなかった) / **判断ログ 18** = **補題の結論が、実際に証明している事実より弱く書かれていることがある** (`exists_weights_dominating` は両分岐とも `0 ≤ a` を満たしていたのに公開しておらず、署名だけ見ると「swap 配置に必要な重みの符号が足りない」= 壁に見えた ⟹ 再利用の前に**証明本体**を読む)。 ⚠ **(a′) が閉じても §0 のゴールは動いていない** — **§1.1 の唯一の穴 (C2) = 外界側の effective compactness は 1 ミリも動いておらず**、(a′) が進めたのは (C1) 側の in-project Lean 実現の 1 段である (判定の SoT は在庫 §16.7)。⚠ **残る形式化上の穴**: 基数は有限個になったが**各指標の `pV` / `K` はなお連続体を走る** ⟹「有界時間で `ε` 近似」を言うにはその上の**最適化**が要る (**未着手**)。⚠ **`Convex ℝ (bcCapacityRegion W)` の債務**と **`martonRegionUnion` 自身の凸性 (強度差の疑い)** も未決のまま残る — **L20 の収穫でこれらを落とすと嘘になる** (在庫 §16.7 / 子 §5.3 / §5.5)。 **L20 = 記録の枠、closed** (`8c20d1f3` / `deadd6f2` / `8d8a66a0`。子 §5.5): proof-log + metrics の新設 / 層 3 の 7 ファイルが与えたもの・与えていないものの確定 (在庫 §17.1) / 正直な棚卸し (同 §17.3) / **README 定理表への登録は `closure_convexHull_martonRegionUnion_eq_bounded` 1 本だけ** (`..._outerBounded` は登録していない)。⚠ **実測 1 件の訂正** — **`Swap.lean` の `@[entry_point]` は 0 本**であり、L19 の commit message と子 §5.4 の「headline 4 本」は module doc の *Main statements* 掲載の意味である (在庫 §17.1)。⟹ ⚠ **子 §6 の撤退ライン 3 本目 (「20 leg を使い切って未達 ⟹ 未達と書く」) が発火した = 未達と書いた** (判定の SoT は在庫 §17.3)。⚠ **発火とは「未達と書く」ことであって達成条件を緩めることではない** — §0 / §0.1 は本 relay で 1 文字も動いていない。⚠ **到達した中間結果 ((a′) = 閉凸包の基数有界化が proof done + README に 1 本登録) は台帳に残すがゴール達成ではない**。⟹ **次の判断 (継続 / 軸の入れ替え / 終了) はユーザーに返す** — 材料は在庫 §17.4 にあり、⚠ **どれを推すかは plan に書かない**。子の新規 判断ログ 19 = **本 relay の失敗形は 1 本の軸に畳めず 2 本**である (軸 1 = 未検証の否定的判断を機械に落とさないまま設計へ通した〈反証コストは 1〜6 行〉/ 軸 2 = 残件の員数を prose だけで見積もり**両方向に**外した) ⟹ 畳まない理由は**処方が違う**こと 1 点) |
| [`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md) | **未解決本体 (T3) への攻略 第 2 次 — 前 relay ([`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md)) の継続。ユーザー決定 (2026-08-03) で新規 20 leg (M0–M19)**。⚠ **§0 のゴールと完了条件は前 plan から 1 文字も動かさない** (肯定側 = 任意の `ε` に対し有界時間で `ε` 近似を返す手続きの構成と正当性 / 否定側 = その不存在の証明。⚠ **いずれも散文の証明と Lean の proof done の両方**)。§0.1 のゴール保護条項 (`GOAL-CHANGE` の起票規約 / 中間結果はゴールの代替にしない / 各 leg 冒頭の 1 行) も継承。**標的は (C2) 本体** = 「`C` を共通部分にもつ、指標と `W` について一様に半計算可能な外界の可算族が 1 つでも存在するか」で、⚠ **残る穴は `Thm7(W)` と `C(W)` の厳密一致 1 点**まで縮んでいる (子 §1、逐語の SoT は [`bc-facts.md`](bc-facts.md) の `## L0 (T3)`–`## L7 (T3)` 節)。**本命 attack は `t3-thm7-tightness` で、第一手は反証試行** (厳密な分離が出れば還元先は死ぬが同時に Marton 予想が偽 ⟹ どちらに転んでも軸が 1 本確定する)。⚠ **前 relay の配分事故を枠の構造で塞ぐのが本 plan の設計上の要点** — **M0–M1 = gate / M2–M15 = (C2) 本体の予約枠 (14 = 過半) / M16–M18 = 形式化枠 / M19 = 記録**で、**形式化債務 (在庫 §17.4 の項目 2 / 3 / 4) は M16–M18 の内数で高々 1 leg** かつ **M2–M15 へのはみ出しを明示的に禁止** (子 §5.2)。各 leg は冒頭に **側 ((C2) / (C1) / 形式化債務 / 記録)** を宣言し、`rg` で数えられる形にする (子 §5.1) | **M0–M5 / M7–M10 完了 (10/20)、側はすべて `(C2)` — ⚠ §6-1 は M1 で発火済 (ユーザー決定で継続)。可変枠 M2–M15 のうち 8 leg 消化・6 leg 残**。**M10 時点の標的は依然 S9 = `Thm7 ⊆ Thm8` を領域レベルで付けること (= (31a)–(31e))** で、**判定は未決** (証明も反例も立たず **NO-GO は出ていない**)。⭐ **M10 で路線の生死が決着した — 「単一の `T_J` インスタンス経由で 5 本を閉じる」路は反例つきで死に、残るのは 2 点 gluing 一本になった**: 明示チャネル (`X=(X₁,X₂)` 独立一様、`Y=X₁`, `Z=X₂`, `G=X₁`, `K=X₂`) 上で**適格性 8 本を厳密に満たす** witness が、試した `J` の**すべて**で (31a)–(31e) のどれかを **1 bit 単位で破る** (時分割の全区間には閉形の反例族 `Se = I(G;J)` / 鏡映 `Sb = I(K;J)` が在り `max(Sb,Se) ≥ 1/2`、軌跡外は完全適格な決定的 witness の全探索が `+1.000`) ⟹ **M9 行 4 の `J := G` は (31b) は無条件に閉じるが同じ `J` で (31c)/(31d)/(31e) が破れる**。⭐ **同時に 2 点版の定式化が過大だったことも判明** — Thm8 の witness は `(hat,tilde)` だけ (逐語 `auxrec.txt:1430`) で、適格性は **(20a) = チルダ + `J` / (20b) = ハット + `J` / (20c) = `(U,V,W)` のみ (`J` 非依存)** に厳密に分離する ⟹ **正しい要求は「同一の `(hat,tilde)`・`(U,V,W)` は `J` ごとに別」**で、厳密版との差はちょうど 3 本の等式 `D1=D2=D3=0`。⭐ **その緩和版には明示解が在る** (M8 の knife-edge の `(hat,tilde)`、適格性 8 本すべて厳密に 0、2 インスタンスのレート束から **(31a)–(31g) の 7 本すべてが従う**) ⟹ **残りは「2 点同時適格 witness を Thm7 の族から取り出す」1 点**。⚠ **M10 でも探索の失敗を 3 件検出 (うち 1 件は 1 bit 幅)** ⟹ 有効だったのは適格性の分離構造による全探索と閉形反例族の手構成の 2 つだけ (逐語の SoT は [`bc-facts.md`](bc-facts.md) `## M10 (T3b)`)。⚠ **M8 で残余が縮み (独立な義務 3 本 / 反例は非対角 witness に限る / 適格性 (19)/(20) は本質的)、M9 でさらに 2 本が落ちた**: ⭐ **`J := G` を採ると `(18c) ≤ (31b)` が適格性なしで無条件に成立し** (緩みは厳密に `I(Û,Ŵ;G\|Y)`)、鏡映で `J := K` の下で `(18f) ≤ (31e)` ⟹ **M8 が名指しした詰まり (「(19)/(20) は `J=(G,K)` をまとめてしか拘束しない」) の正体は拘束の強さではなく `J` の選び方だった** (`J := (G,K)` では同じ 2 本が 63/300・58/300 で破れる)。⚠ **S9 は閉じない** — 2 本の closure は別々の Thm7 インスタンスの witness を使うのに Thm8 は 7 本に共通の 1 witness を要求する ⟹ **詰まりは「1 つの witness を `J := G` と `J := K` の両方で適格にできるか」= S10 の 2 点版へ移動**し、**残る義務は (31a) と (31c) の 2 本**。⚠ **M9 は先行 6 leg が多用した「対角 witness」の適格性も監査した** — 逐語は積形を要求し文字どおり同一の確率変数を置く読みは非適格だが、**(18)/(19)/(20)/(31) のどの項も補助系統を高々 1 つしか含まない**ので値は `X` 条件付き iid コピー版と恒等的に一致し、**先行 4 行 (M4 行 2 / M4 行 3 / M7 行 5 / M8 行 3) は 1 本も覆らなかった** (逐語の SoT は [`bc-facts.md`](bc-facts.md) `## M8 (T3b)` / `## M9 (T3b)`)。 (⚠ **ここは子の状態キャッシュ** — 判断の SoT は子 plan §7 判断ログ 2、逐語の SoT は [`bc-facts.md`](bc-facts.md) `## M1 (T3b)` 節 + 在庫 [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md)。**M0 (gate、側 = (C2))** = (C2) 側の在庫を結論形で取り直し、形式化の受け皿 (平面集合版の計算可能性述語) が**既存の Mathlib 部品だけで構成できる = 壁 0 件**ことを確定。⚠ **詰まりは受け皿側ではなく `Thm7` 側の対象の不在** (既存 BC 領域 def は 2 レート版、`Thm7` は 3 レートの定理) で、**未決の設計軸が 1 本残る**。**M1 (gate、側 = (C2))** = 本命 attack `t3-thm7-tightness` の第一手 (反証試行) を実行し、判定は **判定不能** — **NO-GO は出ず** (厳密な分離は 1 本も立たず、文献にも `Thm7` / `Thm8` の劣最適性の主張は 0 件)、**同時に反証も塞がっていない** (生きているルート 2 本 + 新しい探索先 1 本)。⟹ **§6-1 が発火** (「M1 の gate が GO / NO-GO のどちらも返さない ⟹ M7 の棚卸しを待たず、T3-β 側の第一手へ振り替えるかを含めてユーザーに判断を返す」)。⚠ **発火とは次の判断をユーザーへ返すことであって達成条件を緩めることではない** — §0 / §0.1 は 1 文字も動いていない。⚠ **M0 / M1 の収穫は中間結果でありゴール到達ではない** (子 §0.1-2)。⚠ **ユーザーへ返した 4 択の逐語は子 §7 判断ログ 2 が SoT で、どれを推すかは書かない**。⚠ **前 relay が未達で終わった事実は消さない**: 上行の CLOSED (ゴール未達) はそのまま残す。⚠ **撤退ラインは 4 本で、3 本目 = 「20 leg を使い切って未達 ⟹ 未達と書く」は前 relay で発火した実績があるライン**、**4 本目 = 配分の撤退ライン** (可変枠を `N` leg 消化した時点で側が `(C2)` だった leg が `N` の半分を切ったら、その場でユーザーに判断を返す) は本 relay の新設。⚠ **運用規約の新設 2 軸** = 軸 1「無い / 載らない / 足りない / 経由できない」と書いた瞬間に着手*前*に 1 行の反証を書いてコンパイラに落とさせる (棄却対象は資産に限らず、分解の可能性 / 経路の向き / 自分たちが既に証明していた事実を含む) / 軸 2 gateway atom を通した*後*に残りの段の在庫を別途取り直す (⚠ 「残るのは行数と配線だけ」は tell として名指しで禁止)。⚠ **2 軸は 1 本に畳まない — 処方が違う**) |
| [`marton-inner-bound-plan.md`](marton-inner-bound-plan.md) | L-BC5 解除 = 一般 (non-degraded) BC の Marton inner bound (EGK Thm 8.3、private message のみ) | CLOSED ✅ (**Phase 0–8 完了**) — `marton_achievability` (`Marton/Achievability.lean`、`@[entry_point]` + `@audit:ok`、`#print axioms` sorryAx-free、root+README Ch.15 登録済) が厳密不等号 3 本 `R₁<I(V₁;Y₁)` / `R₂<I(V₂;Y₂)` / `R₁+R₂<I(V₁;Y₁)+I(V₂;Y₂)−I(V₁;V₂)` の達成可能性を無条件に与える (入力は一般カーネル `K : Kernel (V₁×V₂) α` 形。決定的 `x=f(v₁,v₂)` 版は full support 前提と両立せず直接の系にはならない)。機構: 抽象 second-moment 核 + 共分散の鋭化 (sum-rate 制約の要) + Fourier–Motzkin + mutual covering の weak/strong 両版 + 受信機ごとに分岐する 3 本入れ子の半径 + 3 段アンサンブルを 2 段 pigeonhole へ再結合。生きた撤退ラインは 0。Phase 8 bookkeeping (README 定理表 / [proof-log](../proof-logs/proof-log-marton-inner-bound.md) / WZ `Concentration.lean` を `Shannon/ConditionalAEP.lean` へ再配線) まで消化済 |
