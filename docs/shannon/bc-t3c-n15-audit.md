# N15 敵対的独立監査 — 「`C` の閉形が [probc] の外に書けるインスタンス族」の gate (commit `40c3ed23`)

**被監査対象** = [`bc-t3c-n15-instance-gate.md`](bc-t3c-n15-instance-gate.md) (主判定 = **GO**) /
**親 plan** = [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §4.6 (敵対的独立監査の義務) /
**独立検証器** = [`verifiers/n15_audit_sumbc.py`](verifiers/n15_audit_sumbc.py) (**23/23**、EXIT=0)。

> ⚠ 本監査は N15 の実装 leg に一切関与していない。⚠ **被監査 leg の検証器 `n15_instance_gate.py` は 1 行も読んでいない / 呼んでいない**
> (親 plan §4.6 の独立実装義務)。⚠ **成果物本体は 1 文字も書き換えていない** (被監査対象を監査器が書き換えない)。

---

## 0. 立場と方法

⚠⚠ **既定の立場 = 「主判定 GO は偽である」**。各系統は「確認する」のではなく **壊しにいって壊れなかった**ことの記録である。

**方法**:

1. **一次文献を独立に強制再取得**した — 被監査 leg と同じ scratchpad に既存テキストがあったため、
   **別ディレクトリへ `lit-fetch.sh` で再取得し md5 を照合**した (`glnsum` / `probc` / `auxrec` / `GK-outer` の **4/4 が一致**)。
   ⟹ 取り違え・改竄・stem 汚染の線は消えた。⚠ **抽出テキストはコミットしていない** (repo は public)。
2. 引用は**行番号ごと本文を開いて**照合した。⚠ **「正しいが射程が違う」形** (量化子・時制・単数複数・
   どの対象についての言明か) を主軸に置いた。
3. GO の骨 (`SR_C = 7/3` / `(0,5/4,5/4) ∈ UV` / `d < 2/15`) は**印字値を使わずに 1 段深いところから再導出**した
   (段 A/B、厳密有理)。
4. 台帳訂正 (§6-1) は**段落全体を独立に読んで**時制の読みを検査し、**波及の有無を機械 grep で判定**した。

**総合判定 = 訂正あり生存**。**反証試行 46 系統中 7 系統が成立**、**訂正 7 件、うち主判定 GO を動かすもの 0 件**。

---

## 1. 独立再取得と逐語照合

### 1.1 再取得 (改竄・取り違えの排除)

| stem | md5 (監査の独立取得) | 被監査 leg 側と一致 |
|---|---|---|
| `glnsum` | `58d66612108289ebb473847993776aff` | ✓ |
| `probc` | `f431053a9507fe30a3a0d1949bb99f97` | ✓ |
| `auxrec` | `e16f7335e0942bc31cd7737edf7fc27b` | ✓ |
| `GK-outer` | `42cc143a9f66f274171ddc2069430315` | ✓ |

⭐ **`glnsum` は本 relay で初めて使われた一次文献**であり、`lit-fetch.sh` の追加分 (arXiv:2606.12839) から
**監査が独立に取得しても同一の行番号系が再現する**ことを確認した。

### 1.2 逐語が実文と食い違った箇所 — **1 件**

| 所在 | 成果物の記述 | 実文 | 向き |
|---|---|---|---|
| §2.4 Lemma 3 の行 | 「`for all` **λ0 ≥ λ1 ≥ λ2 ≥ 0**」を `glnsum.txt:224` の逐語として引く | `:224` は "… **for all λ0 ≥**" で行が切れ、続きは **`:225` "max{λ1 , λ2 } ≥ 0"** | ⚠ **成果物の側が狭い** (実文の方が強い) |

⭐ **誤りの機構**: `:224` を**単独行で引いた**ため行末で切れ、続きを Definition 1 の順序条件から補ってしまっている。
⟹ **複数行にまたがる言明を単一行番号で引かない**という運用上の教訓に落ちる。
⚠ **向きは保守側** — 実際の Lemma 3 はより広い `λ` で成り立つので、GO はむしろ強まる。

### 1.3 一致を確認した引用 (主要なもの)

`auxrec.txt:1034-1036` (Theorem 7) / `:1172-1178` (Remark 12) / `:1004-1014` (Theorem 6) / `:1250` /
`:1507-1508` / `:1532-1535` / `:1558-1561` / `probc.txt:34-41` / `:128-131` / `:132-135` / `:339-344` (Claim 3) /
`:488-502` (Claim 4) / `:541-548` / `:551` / `:557` / `:558-560` / `:592` / `:610-612` / `:617-624` /
`glnsum.txt:55-59` / `:60-61` / `:64-76` / `:224-225` / `:302` / `:376-397` / `:481-493` / `:511-521` /
`:557-597` / `:598-680` / `:634-637` / `:682-698` / `GK-outer.txt:194` / `:255` / `:339-340` / `:381-383`
— **いずれも行番号ごと本文と一致**した。

---

## 2. 標的ごとの反証試行 (**46 系統中 7 系統が成立**)

⚠ **成立 = 監査の攻撃が当たった / 不成立 = N15 が耐えた**。

### A 群 — 逐語と射程 (18 系統。成立 4 / 不成立 14)

| # | 攻撃 | 結果 |
|---|---|---|
| A1 | 文献の取り違え / 改竄 (同一 scratchpad 共有) | **不成立** — md5 4/4 一致 |
| A2 | `auxrec:1034-1036` (I1) の逐語 | **不成立** |
| A3 | `auxrec:1172-1178` (I2) の逐語 | **不成立** |
| A4 | Theorem 7 の量化子 (`∃p(x) ∀T_J`) を成果物が崩していないか | **不成立** — 崩していない |
| A5 | ⭐ `glnsum:224` Lemma 3 の順序条件 | ⭐ **成立** (→ §1.2) |
| A6 | `glnsum:481-493` Corollary 1 の言明 | **不成立** |
| A7 | `glnsum:682-698` Theorem 4 の言明と条件 (a)/(b)/(c) | **不成立** |
| A8 | ⭐ `glnsum:73` の射程 — 同行が言うのは **`O_UVW ⊋ M`** であって `⊋ C` ではない | ⭐ **成立** — §3.2 表 (C) 欄が `UV ⊋ C` の根拠に `:73` を単独で当てている。`⊋ C` は **Theorem 4 (`M = C`) を 1 段挟んで初めて出る** (成果物 §5.1 資産 2 が実際に供給しているので**結論は立つ**) |
| A9 | ⭐ `glnsum:75-76` の射程 — 実文は "**this** sum-broadcast channel" (Fig. 3 の単数) | ⭐ **成立** — §3.2 表 (C) 欄が「[glnsum] の和クラス」全体を [probc] の外と書く。⚠ **§5.1 が渡すのは Fig. 3 のインスタンス 1 本**なので、渡す対象の水準では逐語どおり |
| A10 | `probc:34-41` の段落境界 (引用が段落をまたいで切り貼りされていないか) | **不成立** — 連続 1 段落で正しい |
| A11 | `probc:551/557/592/610-612` が Claim 4 = C を**普遍に**言っていると読ませていないか | **不成立** — 成果物は「[probc] の積クラス上で」と明記。⚠ `probc:510` Remark 3 は Claim 4 自身も一般には strictly sub-optimal と述べており、成果物の限定はそれと整合 |
| A12 | `probc:617-624` Remark 4 の混合クラスの引用 | **不成立** |
| A13 | `auxrec:1558-1561` を「非比較性の証明」として引いていないか | **不成立** — 成果物 §2.5 は「どちら向きも未証明」と明記 |
| A14 | `GK-outer:381-383` Proposition 2 の向き (Thm6 ⟹ Thm4) | **不成立** |
| A15 | `GK-outer:339-340` (Theorem 6 = Theorem 3 の弱化) の読み | **不成立** |
| A16 | `probc:339-344` Claim 3 の値 (`8/3` / `3 − 1/15 = 44/15`) | **不成立** — `probc:465-469` で `8/3 = SR_M = SR* < 44/15 ≤ SR_UV` を確認 |
| A17 | `glnsum:512` の "resembles" を「同じ形」と強めていないか | **不成立** — 同じセル内に原文 "This channel resembles …" を併記しており、読者が原文へ戻れる |
| A18 | ⭐ `glnsum:69` の射程 — §3.2 表 (B) 行が「[probc] の積 **3 クラス**」全体に `UV ⊋ C` の `○` を付けている | ⭐ **成立** — `:69` の厳密性の witness は **Claim 3 = 逆向き semi-deterministic 側のみ**であり、`probc:471` / `:484-487` も strictness を **Claim 3 から**導いている。⚠ **more-capable 側の厳密性は [probc] のどこにも無い** (全走査)。⚠ **成果物 §6-8 が同じことを「確かめていない」と書いており自己矛盾** |

### B 群 — 検査軸 `C ⊆ Thm7 ⊆ UV` (5 系統。成立 0)

| # | 攻撃 | 結果 |
|---|---|---|
| B1 | I1 に落とした仮説が無いか | **不成立** — "Given a broadcast channel … and any achievable rate triple" で**チャネルにもレートにも仮説が無い** |
| B2 | I2 に落とした仮説が無いか | **不成立** — "for all broadcast channels T (y, z\|x)" |
| B3 | ⭐⭐ **`UV` の版の取り違え** (facts `## M1 (T3b)` 行 4 が 1 度事故った軸) を**制約 1 本ずつ**再照合 | **不成立** — `glnsum` (2a)=`auxrec:1005` / (2b)=`:1011` / (2c)=`:1012` / **(2d)∧(2e) = `:1013` の入れ子 `min` の展開**。witness は `glnsum:61` "for some pmf p(u, v, w, x)" と `auxrec:1014` "(U, V, W ) −− X −− (Y, Z)" で**どちらも一般版**。⟹ **独立版 `p(u)p(v)p(w\|u,v)` は混ざっていない** |
| B4 | `glnsum:61` の基数境界 (`\|W\| ≤ \|X\|+5` 等) が領域を変えないか | **不成立** — "it suffices to consider" なので領域は不変 |
| B5 | 段 1 の集合論 (`UV = C ⟹ Thm7 = C` / 対偶) | **不成立** — 挟み込みから直ちに従う |

### C 群 — GO の骨 (12 系統。成立 2 / 不成立 10)

| # | 攻撃 | 結果 |
|---|---|---|
| C1 | `h_C(λ) ≤ max_C(R0+R1+R2)` の当て方 — `λ2 = 1−d < 1` なので `R ≥ 0` が要る | **不成立** — `C ⊆ {R ≥ 0}` は文献側で裏が取れる (`glnsum:43` "non-negative rate triples" / `:303-304` "any achievable **non-negative** rate triple")。⚠ 検証器 `B5` で **`R ≥ 0` を外すと上界が実際に壊れる**ことを反例 1 本で示し、この前提が load-bearing であることを明示した |
| C2 | ⭐ `SR_C = 7/3` を印字値でなく独立に再導出できるか | **不成立 (= 再導出できた)** — 成分の区分線形式は **[probc] Appendix B (`probc.txt:398-403`) に独立に印字**されており (`5/3 − (2/3)λ` on `[0,1/2]` / `4/3` on `[1/2,1]`)、これを Corollary 1 (17) に入れると **α=1/2 で 2 指数が一致 ⟹ `2^t + 2^t = 2^{t+1}` の恒等式で `7/3`** が出る (`A1`–`A4`、厳密有理)。⚠ さらに**同じ成分関数が [probc] 自身の積合成則で `4/3 + 4/3 = 8/3` を再現する** ⟹ 成分関数は 2 経路でクロスチェックされた |
| C3 | `(0,5/4,5/4) ∈ UV` を (2a)–(2e) へ直接代入して破れないか | **不成立** — 5 本すべて成立、和レートは **ちょうど `5/2` で等号** (`A5`–`A7`、厳密有理) |
| C4 | `d < 2/15` の厳密有理での再計算 | **不成立** — `2 − (7/3)/(5/4) = 2/15`、`d = 2/15` で差ちょうど 0 (`B1`–`B4`) |
| C5 | ⭐⭐ 「**`h_UV(λ) > h_C(λ)` は `d < 2/15` と同値**」(§3.4) | ⭐ **成立** — 使った 2 本はどちらも**片側**評価 (`h_UV ≥ …` / `h_C ≤ …`) なので出るのは**十分条件だけ**である。検証器 `C1`–`C3` が、同じ 2 入力と整合しつつ `d = 1/2 (> 2/15)` でも `h_UV > h_C` となる模型を構成して「同値」を反証した。⚠ **成果物 §6-12 が「錐は下界であって最大ではない」と正しく書いており、§3.4 と自己矛盾している** |
| C6 | ⭐ `C` の**支持関数の閉形**の帰属 (§3.2 表 / §5.1 資産 1) | ⭐ **成立** — **Corollary 1 が与えるのは `M(T)` の支持関数**であって `C` ではない。`C` へ渡すには Theorem 4 (`M = C`) が要る。⚠ **§4.2 (b)-1 は正しく「`M(T)` の支持関数」と書いており、成果物内で不整合** |
| C7 | Theorem 4 の条件が**和チャネル自身**に対するものなら Fig. 3 は落ちる (`:516-517` が「和チャネルはどの primary クラスにも入らない」と明記している) | **不成立** — `:682-688` の条件 (a)/(b)/(c) は**成分 `Ta, Tb`** に対するものであり、Fig. 3 の成分は semi-deterministic ⟹ Lemma 3 で全 `λ` ⟹ **`M(T) = C(T) = O_aux(T)` が領域レベルで従う**。⚠ **この攻撃は「和チャネルが primary でない」という論文自身の文言を使った最も有望な線だったが、条件の掛かる対象が違った** |
| C8 | `λ = (1,1,1−d)` が Corollary 1 / Theorem 4 の順序条件から外れないか | **不成立** — `d ≤ 1` で `λ0 ≥ λ1 ≥ λ2 ≥ 0` を満たす |
| C9 | ⭐ 順序条件が Corollary 1 / Theorem 4 / Lemma 3 で**書き分けられている**か | **不成立 (訂正は A5 に計上)** — ⭐ **論文は確かに書き分けている**: Corollary 1 は `λ0 ≥ λ1 ≥ λ2 ≥ 0` と鏡像 `λ0 ≥ λ2 ≥ λ1 ≥ 0` の **2 本立て**、Theorem 4 と Lemma 3 は `λ0 ≥ max{λ1 , λ2 } ≥ 0`。しかし**成果物が写し違えたのは Lemma 3 の 1 か所だけ**で、Corollary 1 / Theorem 4 の側は当たっている |
| C10 | `SR_Thm7 ≤ SR_Thm6 ≤ SR_Thm8` の連鎖 | **不成立** |
| C11 | Proposition 2 が `λ = (1,1,1−d)` にも当たって錐を潰さないか | **不成立** — `GK-outer:393-399` の Theorem 6 の制約は**すべて `R0+R1+R2 ≤ …` の和レート**であり、一般方向の支持関数は縛らない |
| C12 | `Thm7 ⊆ Thm8` の**領域レベル**の順序が 2026 年の 2 本のどちらかに在って本族を即死させないか | **不成立** — `GK-outer` / `glnsum` を全文検索した結果、順序の言明は **`GK-outer:283`(Thm3⟹Thm5) / `:337` / `:383`(和レートのみ) / `:518`** の 4 本だけで、⚠ **`glnsum` は auxrec Theorem 7 に一度も言及していない**。⟹ 成果物 §6-7 の限定を**この 2 本の範囲では閉じた** |

### D 群 — 台帳訂正 (§6-1) とその波及 (4 系統。成立 0)

| # | 攻撃 | 結果 |
|---|---|---|
| D1 | `probc:36` を「寄与以前の知識状態」と読むのは誤読ではないか | **不成立** — 段落全体を独立に読むと、`:36` の現在完了 → `:37-38` "Recently it has been shown … do not coincide" → `:39-41` "**In this paper we show that the UV outer bound is strictly suboptimal by establishing the capacity region for a new class**" と進む。⟹ 本論文の寄与が「**容量が既知でありながら UV と一致しないクラス**」を作る以上、`:36` を**時制なしの定理として引くことはできない**。N15 の読みは正しい |
| D2 | それでも facts 行 4 の連鎖は救えるのではないか | **不成立** — `probc` Claim 3 (`:339-344` / `:465-469`) は**容量が既知** (`SR_M = SR*`) で、しかも**私信平面 `R0 = 0` の和レート**について `8/3 < 44/15 ≤ SR_UV` を与える ⟹ **行 4 が使った `UV_2rate_general = M` は [probc] 自身の例で破れる**。⚠ **破れるのは導出であって結論の否定ではない** — 成果物もそう書いている |
| D3 | 訂正の向きの主張 (「生きている空間が広がる」) が過大でないか | **不成立** — facts `:834` の P4 は (i-1) を「塞がった 2 か所」の 1 つに数えているので、行 4 が落ちれば **(i) の穴が広がる**。⟹ M1 の主判定 **判定不能** はむしろ強まる方向で、覆らない |
| D4 | 訂正が過去 leg の判定へ波及するか (→ §2.1 で判定) | **不成立** — 波及先は 1 か所のみ |

### E 群 — 反証条件・較正・禁止事項 (7 系統。成立 1 / 不成立 6)

| # | 攻撃 | 結果 |
|---|---|---|
| E1 | §0 / §1 が事後に書き換えられていないか | **不成立** — `4a8061f7` の 1–97 行と **md5 が一致** (`055ddfff7faeec5b339a790af614f423`)。全差分は **`⏳ 以下 §2–§6 は未記入である` + 空行の 2 行削除のみ**で、他はすべて 101 行目以降への追記である |
| E2 | §4.2 (a) 1 本目の発火は本当に成立するか — §3.5 は **[probc] の 3 クラスしか見ていない** | **不成立** — 監査が (A) 群の抜け道を 1 本追加検査した: **BSC(e/2) は BEC(e) の劣化版**なので `p = e/2` なら成分が degraded BC となり **逆向き degraded 積 (El Gamal、容量既知)** で `C` が書けてしまう。しかし **`e > h(e/2)` を満たす `e` は `(0,1)` に存在しない** (0.01 刻み全走査で空、端点 `e=0.99` でも `h(0.495)=0.999928 > 0.99`) ⟹ **この抜け道は離調帯 `e > h(p)` に届かない**。発火は生き残る |
| E3 | §3.5 の 4 数値 (唯一の浮動小数使用箇所) | **不成立** — 独立再計算で `0.400000 / 0.531004 / 0.004563 / 0.002531` が**全桁一致** |
| E4 | §4.2 (b)-2 の「**部分発火**」が 2 値からの逃げになっていないか | **不成立** — 条件文の**字義** (「N9 / N10 の連鎖がそのまま再現する」) については **「再現しない」と明示的に答えている**うえ、発火が方向依存であること (`(1,1,1)` では死ぬ / 錐では死なない) とその理由も書かれている。⚠ **ラベルは保守側 (より多くの死を報告する側) に振れている**ので、GO を膨らませる働きはしない |
| E5 | §4.3 の「結論は当たり / 経路は外れ」が**外れを当たりに丸める装置**になっていないか | **不成立** — 導入の向きが逆である。同節は「**これを `当たり` に丸めると、見立て A が名指しした Theorem 2 の積クラスが生きているかのように読める**」と、**丸めを禁じるために**この語彙を立てている |
| E6 | ⭐ ただしその新語彙が**一貫して適用**されているか | ⭐ **成立** — 見立て **C** は「死因はクラス仮説の一致からではなく共有された逆定理から来る」= **結論は当たり / 経路は外れ**の型であるのに、判定欄は **`当たり (覆らなかった)` と単記**されている。⚠ **セル本文では経路の外れを開示済**なので隠蔽ではないが、⚠ **表だけを読むと締める方向の的中率が実際より高く見える** |
| E7 | 禁止事項 6 種 (`Thm7 ⊋ C` の材料 / `Thm7 ⊄ Thm8` / `R ∈ Thm7` / 「尽きた」型 / 「方法の死」を「包含の否定」と書く / `e < h(p)` を「比較の相手が無い」で捨てる) | **不成立 = 違反 0** — `⊋` の全 12 出現は**文献逐語**か **`UV ⊋ C`** (列挙軸) で、`Thm7 ⊋ C` は 3 か所ともすべて否定形 (「出ない」「出ていない」「排除もされていない」)。`e < h(p)` の帯は §0 併記 2 と §3.5 末尾の**両方**で「理由が違う」と明記 |

### 2.1 ⭐⭐ 標的 4 の波及判定 (成果物が未判定のまま残したもの)

> ## 判定 = **波及する。ただし行き先は facts `## M1 (T3b)` の P4 行 (i-1) の 1 か所だけであり、N7 / N9 / N10 / N12 の判定には波及しない。**

**根拠 (3 本)**:

1. **機械**: `M1 (T3b) 行 4` を参照する箇所は repo 全体で **N15 自身の 3 か所と `bc-facts.md:974` の計 4 か所のみ**
   (`rg -no "M1 \(T3b\)\`? 行 4[b]?" docs/shannon/*.md`)。うち `:974` は
   **「`## M1 (T3b)` 行 4 が事故った『私信平面に限る』問題は本 leg には無い」**という**非依存の言明**である。
   また `probc.txt:34-36` を引く箇所は **facts `:828` (行 4 そのもの) と N15 の議論だけ**で、
   ⚠ **T3c の leg (N7 / N9 / N10 / N12) は 1 度も引いていない**。
2. **内容**: 行 4 が供給するのは「**容量既知クラス全体**にわたる一般的な閉じ」であり、
   N7 / N9 / N10 が出したのは **[probc] という 1 インスタンス上の直接計算** (支持関数の一致 / 恒等式) である。
   ⟹ 一般的な閉じが落ちても、**個別の計算は 1 つも無効化されない**。N12 は N6–N11 の棚卸しなので同じ。
3. **向き**: 訂正は **P4 (i-1) が塞いだとする穴を開け直す**方向である。⟹ M1 の主判定 **判定不能** を**強める**側であり、
   閉じた判定を覆す働きを持たない。

⚠ **同時に名指しする副作用**: facts `:834` の P4 は「塞がったのは (i-1) と (i-2) の 2 か所だけ」と数えている。
行 4 の訂正が伝播すると **(i-1) は塞がっていない** ことになるので、⚠ **P4 の隙間の数え (現在 (i-a) / (i-b) の 2 つ) は
3 つへ増える**。⟹ **facts への伝播 leg は行 4 だけでなく P4 行の数えも同時に直す必要がある**。

---

## 3. 訂正一覧

### 3.1 ⚠⚠ 主判定 (GO) を動かすもの — **0 件**

⚠ **明示的に数えた**。下の 7 件はいずれも GO の骨 (`[glnsum]` の和チャネル族が [probc] の外で `C` の支持関数の
閉形を供給し、`d ∈ (0, 2/15)` の錐で挟み込みが潰れない) を**壊さない**。

### 3.2 主判定を動かさないもの — **7 件**

⚠ **系統との対応 (1 対 1)**: 訂正 1 = `A5` / 2 = `A8` / 3 = `A9` / 4 = `C5` / 5 = `C6` / 6 = `A18` / 7 = `E6`。
⟹ **成立した 7 系統と訂正 7 件は過不足なく対応する**。

| # | 所在 | 訂正 | なぜ GO を動かさないか |
|---|---|---|---|
| 1 | §2.4 Lemma 3 の行 | `glnsum.txt:224` の逐語は「`for all λ0 ≥`」で切れており、続きは **`:225` の `max{λ1 , λ2 } ≥ 0`**。引用を `:224-225` にし、順序条件を `λ0 ≥ max{λ1 , λ2 } ≥ 0` に直す | **実文の方が強い** (より広い `λ`)。GO は狭い方の条件でも立つ |
| 2 | §3.2 表 (C) 行「`UV ⊋ C` か」欄 | `glnsum.txt:73` が言うのは **`O_UVW ⊋ M`**。`⊋ C` にするには **Theorem 4 (`M = C`)** を 1 段挟む旨を同欄に書く | 挟む Theorem 4 は**成果物 §5.1 資産 2 が既に供給**しており、結論は立つ |
| 3 | §3.2 表 (C) 行「本 gate に対する状態」欄 | `glnsum.txt:75-76` は "**this** sum-broadcast channel" (Fig. 3 の単数) についての言明であり、「和クラス全体が [probc] の外」ではない | ⚠ **§5.1 が N16 へ渡すのは Fig. 3 のインスタンス 1 本**なので、渡す対象の水準では逐語どおり |
| 4 | §3.4 の結論行 | 「`h_UV(λ) > h_C(λ)` は `d < 2/15` と**同値**」→ **「`d < 2/15` **ならば** `h_UV(λ) > h_C(λ)`」(十分条件のみ)** に直す。⚠ **§6-12 と自己矛盾している** | GO が要るのは十分方向だけ。⚠ **真の錐はより広い可能性がある**ので、訂正の向きは GO を**広げる**側 |
| 5 | §3.2 表 (C) 行 / §5.1 資産 1 | Corollary 1 が閉形で与えるのは **`M(T)` の支持関数**。`C` の支持関数と書くには Theorem 4 を経由する旨を明記する。⚠ **§4.2 (b)-1 は正しく書いており成果物内で不整合** | 経由すべき Theorem 4 は Fig. 3 の成分 (semi-deterministic) に対し **全 `λ` で効く** ので、結論 (`C` の閉形が書ける) は立つ |
| 6 | §3.2 表 (B) 行「`UV ⊋ C` か」欄 | 「[probc] の積 3 クラス」全体に `○` を付けているが、`glnsum.txt:69` の厳密性の witness は **Claim 3 = 逆向き semi-deterministic 側のみ**である (`probc.txt:484-487` も strictness を **Claim 3 から**導いている)。⚠ **成果物 §6-8 が「現行 more-capable インスタンスで `UV ⊋ C` かは確かめていない」と書いており自己矛盾** | 族 (B) は**どちらに転んでも渡らない** — `UV = C` なら段 1 でより強く死ぬ |
| 7 | §4.3 の表 (見立て C の判定欄) | `当たり (覆らなかった)` の単記を、同節が立てた 2 軸 (**結論は当たり / 経路は外れ**) に揃える。セル本文は既に経路の外れを開示している | 較正の帳簿であって判定入力ではない (成果物自身が「報告であって判定入力ではない」と明記) |

### 3.3 orchestrator への推奨 (⚠ 監査は実行していない)

- 訂正 1–7 の**成果物への反映は監査の仕事ではない** (被監査対象を監査器が書き換えない)。伝播 leg へ回す。
- **facts への伝播は 2 行同時**である — `## M1 (T3b)` の **行 4** と **P4 行の「塞がった 2 か所」の数え** (→ §2.1 の副作用)。
- ⚠ **`bc-facts.md` の `## M1 (T3b)` 節は再検証コマンドに `$LIT` の絶対パスを埋め込んでいる**
  (`/private/tmp/claude-502/…/scratchpad/lit`)。⚠ **N15 由来ではない既存の問題**だが、scratchpad は消えるので
  `lit-fetch.sh` の呼び出しへ置き換えるのが筋である。

---

## 4. 判定

> ## ⭐ 判定 = **訂正あり生存**

- **反証試行 = 46 系統。うち成立 = 7 系統** (A 18 / B 5 / C 12 / D 4 / E 7)。
- **訂正 = 全体 7 件、うち主判定 GO を動かすもの = 0 件**。
- **標的 4 の波及 = 波及する (行き先は facts `## M1 (T3b)` P4 の (i-1) の 1 か所)。N7 / N9 / N10 / N12 には波及しない。**

⚠ **併記 (成果物の側に立って書くのではなく、監査が独立に確認した限定として書く)**:

1. **`Thm7 ⊋ C` の材料は本監査でも 1 つも出ていない** — gate ゆえ原理的に出ない。**排除もされていない**。
2. **本族の生死を握る外部依存は 1 本** — `Thm7 ⊆ Thm8` の**領域レベル**の順序。⚠ **監査は 2026 年の 2 本
   (`GK-outer` / `glnsum`) を全文検索して順序の言明が無いことを確認した**が、⚠ **それ以外の文献は掃いていない**。
3. **成果物の自己申告した限定 (§6 の 16 本) のうち、監査が独立に潰せたのは §6-7 の一部だけ**である。

---

## 5. 監査が確かめていないこと (⚠ 名指し。ここを薄くしない)

1. ⭐⭐ **[glnsum] Fig. 3 / [probc] Fig. 2 のチャネル行列を再構成していない**。図はビットマップで `pdftotext` では
   辺構造が取れない (テキストは `Xa ∈ {1,2,3,4}` / `Za ∈ {1,2}` / `Ya ∈ {1,…,6}` という**頂点集合だけ**を残す)。
   ⚠ **監査は成果物 §6-3 の限定を 1 段深くしただけである** — 成分の重みつき和レートの区分線形式が
   **[probc] Appendix B (`probc.txt:398-403`) にも独立に印字されている**ことと、それが [probc] 自身の積合成則で
   `8/3` を再現することを確かめたが、⚠ **その区分線形式そのものをチャネルから導いてはいない**。
2. **`(0,5/4,5/4)` の個別の相互情報量** (`I(Ua;Ya) = 1/3` / `I(Xb;Zb|Ub) = 2/3` 等) は**論文の印字値**である。
   監査が確かめたのは**それらを (2a)–(2e) に入れたときの算術**だけである。
3. **[glnsum] Theorem 4 / Lemma 3 の証明 (Appendix A) を読んでいない**。⟹ 「Fig. 3 の成分が全 `λ` で
   primary クラスに入る」は **Lemma 3 の言明への依存のまま**である (成果物 §6-5 と同じ限定)。
4. **[GK-outer] Proposition 2 の量化子の帳尻を再導出していない**。監査は `:384-399` を読んだ (被監査 leg の 4 行より広い)
   が、⚠ **`∃p ∀T_J` の外側の `∃p` がどう運ばれるかは著者の言明に依存させたままである**。
5. ⭐ **成果物 §6-8 の問い (現行 [probc] more-capable インスタンスで `UV = C` か) を settle できなかった**。
   ⚠ **攻めはした** — [probc] 内の strictness の言明を全走査した結果、`:471` / `:484-487` はいずれも
   **Claim 3 (semi-deterministic 側) から**導かれており、⚠ **more-capable 側の厳密性は [probc] のどこにも無い**。
   ⟹ **この問いは本当に開いている**。
6. **`glnsum:64-65` の引用リスト ([1],[4],[8],[12],[13],[21],[23]–[25]) を 1 本も開いていない** ⟹
   §3.2 の族 (A) の「`O_UVW = C = M`」は**同論文の要約への依存のまま**である (成果物 §6-4 と同じ)。
7. **`Thm7` の値を 1 つも計算していない** — 監査も成果物と同じく「挟み込みが潰れていない」までである。
8. **§3.2 の列挙の網羅性を独立に検査していない**。⚠ 監査が追加したのは **degraded 積の抜け道 1 本** (E2) だけで、
   ⚠ **「公表された容量既知クラス」の母集団そのものは数え直していない**。
9. **Lean を 1 行も書いていない / 走らせていない** — 本監査は層 1 + 層 2 のみである。
