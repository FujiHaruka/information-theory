# 一般 BC の計算可能な容量領域 — 未解決本体 (T3) への攻略 第 2 次

> **Parent**: [`broadcast-channel-moonshot-plan.md`](broadcast-channel-moonshot-plan.md)
> **Predecessor**: [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md)
> (L0–L20 完走・**§0 のゴールは未達**で終了。⚠ **その plan は閉じており書き換えない**)。
> 確定事実 = [`bc-facts.md`](bc-facts.md) / attack 台帳 =
> [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) / 前 relay の到達点と限界 =
> [`bc-t3-cardinality-inventory.md`](bc-t3-cardinality-inventory.md) §17 / 失敗形 =
> [`../proof-logs/proof-log-bc-open-problem-t3-relay.md`](../proof-logs/proof-log-bc-open-problem-t3-relay.md) §2 / §7

**Status**: **M0 未着手。20 leg (M0–M19)**。ユーザー決定 (2026-08-03) により前 relay を**継続**する
(§7-1)。⚠ **§0 のゴールと完了条件は前 plan から 1 文字も動かさない**。
⚠ **本 relay の標的は (C2) 本体である** — 前 relay は 21 leg のうち 14 leg が (C1) 側の形式化へ流れ、
標的には実質 0 leg しか入らなかった。**その配分事故を §5 の枠構造で塞ぐ**。
**Branch**: `bc-computable-region`

## 進捗

- [ ] M0 在庫 — (C2) 側の在庫を**結論形で**取り直す 📋
- [ ] M1 gate — 本命 attack `t3-thm7-tightness` の第一手 (反証試行) 📋
- [ ] M2–M15 (C2) 本体の可変枠 (⚠ **予約枠。他の用途に流用しない**) 📋
- [ ] M16–M18 形式化枠 📋
- [ ] M19 記録枠 📋

---

## 0. ゴール — 未解決本体そのもの

**一般 2 受信者 DM-BC の容量領域の計算可能な特徴付けを、肯定側か否定側のどちらかで決着させる。
散文の証明と、その Lean による機械検証の両方をもって完了とする。**

完了条件 (どちらか一方):

- **肯定側** — 任意の `ε > 0` に対し有界時間で `ε` 近似を返す手続きを構成し、その正当性を証明する。
- **否定側** — そのような手続きが存在しないことを証明する。

いずれの側も、**散文の証明が立つこと**と**Lean で proof done に到達すること** (0 sorry / 0 residual、
`#print axioms` が sorryAx-free、かつ署名走査で load-bearing hypothesis が無いこと) の両方を満たして
初めて達成とする。片側だけでは達成ではない。

### 0.1 ⚠ ゴール保護条項 — 本節は leg / planner が書き換えてはならない

**前 relay の前身で実際に起きた事故がこれである**。原案 (`487afc03`) は T2 の完了条件を
「文献に無い命題が **Lean で機械検証される**」と書き、「**ユーザー指定の二段構え
(散文で証明 → 形式化で検証)**」と明記していた。それを `28a740c5` が **L0 が走る前に**
「散文レベルの候補経路」へ書き換え、形式化をスコープ外にした。判断ログにはユーザー判断としての
記録が無い ⟹ **planner の独断でゴールが狭まり、20 leg がその狭いゴールで回った**。

したがって本 plan でも以下を規約とする:

1. **§0 のゴールと完了条件を狭める変更は、leg / planner が適用してはならない**。狭める提案は
   判断ログに **`GOAL-CHANGE`** として起票し、**ユーザーの承認を得るまで適用しない**。
   広げる方向 (より強い完了条件) は起票不要。
2. **中間結果は記録するが、ゴールの代替にはしない**。台帳 (facts / attacks / 在庫) への収穫は
   従来どおり続けるが、**それをもって「ゴールに到達した」と書かない**。かつての階段 (T0–T3) が
   持っていた「どこで止まっても成果が残る」というヘッジは、**記録の設計としては継承し、
   ゴールの設計からは外す** — ユーザーが「成果ゼロで終わる可能性を引き受ける」と明示的に選んだ。
3. **各 leg は冒頭に「この leg は §0 のゴールに対して何を動かすか」を 1 行で書く**。書けない leg は
   起票しない。⚠ **本 relay ではこの 1 行に「側」を併記させる** (§5.1)。

---

## 1. 標的の現在地 — (C2) について今わかっている形

⚠ **値と逐語はここへ複製しない**。判定の SoT は [`bc-facts.md`](bc-facts.md) の
`## L0 (T3)` 〜 `## L7 (T3)` 節、前 relay の到達点と限界の SoT は在庫
[`bc-t3-cardinality-inventory.md`](bc-t3-cardinality-inventory.md) §17 である。
標的の分解 ((C1) 内界側 / (C2) 外界側 / (C3) 有効収束率) と、(C3) が (C2) に吸収されて
**穴が (C2) 1 つに絞られた**経緯は前 plan
[`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) §1.1 が SoT。

落としてはならない 4 点:

- **(i) (C2) の同値形** — 要求は「上から収束する外界**列**を作る」ことでは**ない**。
  **容量領域 `C` を共通部分にもつ、指標と `W` について一様に半計算可能な外界の可算族が
  1 つでも存在するか**と同値である。⚠ **減少性も有効収束率も要らない**
  (facts `## L2 (T3)` 節)。さらにそれは**基数有界かつ閉アトムの一階特徴づけが存在するか**と等しい。
- **(ii) 還元先は 1 点に縮んでいる** — [auxrec] Theorem 7 の外界 `Thm7(W)` が `W` について
  **一様に effectively compact であることは前 relay で証明済**である ⟹ **(C2) ⟸ (`Thm7(W)` と
  `C(W)` の厳密一致)**。⚠ **残る穴はこの厳密一致 1 点**であり、その最適性は一次文献が
  「未解決」と明言している。⚠ **「未決」であって「偽」ではない** (facts `## L2 (T3)` 節)。
- **(iii) 穴の正体** — (C2) は **[Li21] §VIII の open problem (BC 容量領域を表せる線形エントロピー
  階層の最低レベルは何か) = BC の単文字特徴づけ問題**と**同一の穴**である
  (facts `## L3 (T3)` 節)。肯定側は「基数有界な特徴づけの存在」で、これは Marton 3 補助変数内界の
  最適性予想から**片方向にのみ**従う ⟹ 難度は Marton 予想で上から押さえられるが同値ではない。
- **(iv) 難度の較正 (⚠ 我々の演繹、未 probe)** — **否定側は Marton 最適性予想の反証を含意する**
  (facts `## L0 (T3)` 節)。⟹ 否定側の「新規性は構造的に確保されている」という読みは成立しない。
  問題は新規性ではなく**難度**である。

⚠ **前 relay が層 3 で landing させたのは (C1) 側の 1 段** (Marton 内界の閉凸包の基数有界化) で
あって、**(C2) は 1 ミリも動いていない** (判定の SoT は在庫 §17.3)。本 relay はそれを**出発点にしない** —
出発点は在庫 **§17.4** の 5 項目、とりわけ項目 1 = **(C2) 本体**である。

---

## 2. Approach

**三層を命題単位で回す設計は前 relay から有効なので引き継ぐ**。

```
層 1  散文        アイデア + 証明スケッチ
層 2  proof-probe 核 1〜2 ステップを機械検証 — 散文の自己欺瞞を最短で殺す装置
層 3  形式化      散文全体を Lean へ                            ← スコープ内
```

**⚠ 層 2 の道具を明示する**。T3 の主語は計算可能性なので、**数値掃引は原理的に効かない**
(有限の掃引は「有界時間で `ε` 近似できるか」を判定できない)。⟹ 本 relay の層 2 は次の 3 つに限る:

1. **小さな命題の厳密証明** (係数相殺・不等式の鎖など、掃引でなく計算で閉じるもの)。
2. **反例構成** — 明示 witness の直接評価。⚠ 最適化器の非違反は証拠として弱い (§4)。
3. **型検査 1 本の反証** — 「無い / 載らない / 足りない / 経由できない」と書いた瞬間に走らせる
   (§4 軸 1 の処方の実装)。⚠ **これは層 2 の主力である** — 前 relay の失敗 4 件はすべて
   反証コスト 1〜6 行だった。

**攻める順序 (relay 全体の形)**:

- **(C2) は §1 (ii) により 1 点へ還元されている**。⟹ 第一手は**その 1 点を両側から同時に測る** —
  厳密一致が成り立つか (肯定側) / 厳密な分離が出るか (否定側)。**どちらに転んでも軸が 1 本確定する**
  ので、gate として最も安い (§3)。
- 層 1 が通った命題は**その命題単位で層 3 へ載せる**。全体が揃うのを待たない。
- **⚠ 形式化の受け皿を M0 で先に測る** — (C2) の主張は「領域の計算可能性」なので、in-project に
  **平面集合版の計算可能性述語**が要る。スカラー版 `IsComputableENNReal` は Kolmogorov 家系に在る
  (facts `## L5 (T3)` 節)。⟹ **M0 でこの受け皿を結論形で数え直し**、無ければ (C2) の散文が
  立った時点で定義段を切る。⚠ 定義は教科書形の転記ではなく、**消費する Mathlib 補題の結論形から
  起こす** (CLAUDE.md「Mathlib-shape-driven Definitions」)。

---

## 3. 攻撃軸

| 軸 | 主語 | 優先度 | 第一手 |
|---|---|---|---|
| **T3-α** | **肯定側** — 容量領域の **effective compactness** を示す | 1 | **attack `t3-thm7-tightness`**。§1 (ii) より残るのは `Thm7(W)` と `C(W)` の厳密一致 1 点。⚠ **最初の一手は反証試行** — BSSC 変種で**厳密な** (数値でない) 分離が出るか。**分離できれば還元先は死ぬが、同時に Marton 予想が偽になる** ⟹ **どちらに転んでも軸が 1 本確定する** |
| **T3-β** | **否定側** — effectively compact でないことの証明 | 1 | 同じ穴の裏。**真の未着手部分は「チューリング機械 → BC チャネル行列」の還元**で、これは文献に無い (attack `t3-tm-to-bc-reduction`)。⚠ **「文献に無い」であって「不可能」ではない**。⚠ **決定不能性 ≠ `ε` 近似不可能性** — 混ぜると誤った壁宣言になる。⚠ **肯定側は選言 / 否定側は連言** (肯定側は候補 1 本が当たれば決着、否定側はすべてを潰す必要がある) |

⚠ **優先度 1 の 2 本は同じ穴の表と裏**である。前 relay の教訓 (軸を並べて round-robin すると gate を
通らない候補に構築枠を使う) から **gate-first** を採る方針は維持する。
attack の状態と死因の SoT は [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) の
「T3 attack 一覧」であり、**本 plan は状態をキャッシュしない**。

### 3.1 ⚠ 死んだ路線 — 再探索しない

**内容はここへ複製しない**。前 plan [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) **§3.1**
が列挙する路線 (内界 = 外界 / 外界 = 容量 / 停止則の独立化 / 階層のレベル下げ / UV 外界の多文字化 /
`∃` 形外界の多文字化 / [Li21] Theorem 10 経由 / `{Z}` の閉包演算だけで作る支配チャネル族) は
**すべて死因つきで確定しており、本 relay では再探索しない**。

⚠ **同時に落としてはならない射程の限定が 2 つある** (どちらも前 plan §3.1 に逐語):
死んでいるのは「多文字化」という**1 つの機構だけ**で `∃` 形外界の別の可算族は排除されない /
[Li21] §VIII の Tarski 指数関数問題の留保は **exact decision の語義**であり `ε` 近似を否定しない。
⚠ **後者を取り違えると誤った壁宣言になる**。

---

## 4. 運用規約

前 relay の運用規約 (SoT は [`bc-open-problem-plan.md`](bc-open-problem-plan.md) §5 と
前 plan §4) を**そのまま継承する**。再掲せず、本 relay で**新設する分**だけ書く。

### 4.1 失敗形 2 軸への処方 (⚠ 1 本に畳まない — 処方が違う)

材料は proof-log
[`../proof-logs/proof-log-bc-open-problem-t3-relay.md`](../proof-logs/proof-log-bc-open-problem-t3-relay.md)
§2 / §7 と前 plan 判断ログ 19。

- **軸 1 = 未検証の否定的判断を、機械に落とさないまま設計へ通した** (前 relay で 4 件、反証コストは
  実測 1〜6 行)。⟹ **規約: 「無い / 載らない / 足りない / 経由できない」と書いた瞬間に、着手*前*に
  1 行の反証 (型検査 1 本) を書いてコンパイラに落とさせる**。⚠ **棄却対象は資産に限らない** —
  分解の可能性 / 経路の向き / **自分たちが既に証明していた事実**まで含む。
  ⚠ **観察が正しくても結論は誤りうる**: 否定の根拠が*ある対象の非存在*であるときは、
  **その対象を本当に経由しなければならないか**を先に問う。
- **軸 2 = 残件の員数を prose だけで見積もり、両方向に外した** (過小 1 / 過大 1)。⟹ **規約:
  gateway atom を通した*後*に、残りの段の在庫を別途取り直す**。⚠ **「残るのは行数と配線だけ」という
  言い回しは tell として名指しで禁じる** — 見積り表に中身を分解していない 1 行が立っていたら同じ tell。
  ⚠ 過大側の tell は、危険視の根拠が変換自身の性質だけで書かれ**下流の消費形**を見ていないこと。

⚠ **軸 1 の処方をいくら徹底しても軸 2 は防げない** (軸 1 は着手*前*に 1 行、軸 2 は gateway atom
通過*後*に在庫を取り直す)。**両方を別々に走らせる**。

### 4.2 在庫と証拠の作法 (3 規約)

- **在庫の「無い」は、家系でも検索語でもなく結論形で数える** — 結論パターン (`ConvexOn` 等) を
  project 全体に当てる (CLAUDE.md「In-repo asset search」)。
- **補題の再利用前に、署名だけでなく証明本体を読む** — 「結論に書いていない」は
  「証明していない」ではない。
- **loogle の生クエリのヒット数を「無い」の根拠にしない** — **結論形クエリを併記する**。
  生クエリの `Found n` は `n > 0` なら「在る」の誤証拠に、`n = 0` なら根拠の弱い「無い」になる。

### 4.3 検証の誠実性

CLAUDE.md の Definition of Done がそのまま効く (Standard B)。

- **`sorry` + `@residual(<class>:<slug>)` が唯一の honest な退出路**である。詰まったら署名を
  証明したい形のまま残し、body を `sorry` にする。本 relay の退出路の slug は
  **`@residual(plan:bc-open-problem-t3b-plan)`** (= 本ファイルの filename stem)。
  Mathlib の不足に当たった場合のみ `@residual(wall:<name>)` を立て、**register への追記を伴う**。
- **hypothesis に証明の核を担わせて `sorry` を消すのは禁止** (load-bearing hypothesis)。
  regularity precondition は可。
- **`sorryAx`-free は必要条件であって十分条件ではない** — 完了判定には**署名走査**
  (全 hypothesis が regularity precondition であることの確認) を必ず併せる。
- ⚠ **機械で再導出できる事実 (sorry の有無 / 宣言の存在 / axiom 依存) を本 plan の散文に
  キャッシュしない**。壁は `@residual(wall:<name>)` が**コード側の SoT** で、plan はスラグを参照する。

---

## 5. leg 予算と配分

**20 leg (M0–M19)**。⚠ **前 relay の配分事故 (21 leg 中 14 leg が (C1) 側へ流れ、標的の (C2) には
実質 0 leg しか入らなかった) を、枠の構造で塞ぐ**。「予約が無ければ探索が枠を食う」は前 relay で
**2 度**実証されている。

| 枠 | leg | 側 | 内容 |
|---|---|---|---|
| **M0** | 1 | gate | **在庫**。(C2) 側の在庫を**結論形で**取り直す — 形式化の受け皿 (平面集合版の計算可能性述語) / `Thm7` 側の対象が in-project にあるか / 在庫 §17.4 項目 1 に必要な材料。⚠ **在庫は `mathlib-inventory` の担当ファイルへ書く。本 plan には書かない** |
| **M1** | 1 | gate | **本命 attack `t3-thm7-tightness` の第一手** (§3 の反証試行)。⚠ **GO/NO-GO のどちらでも軸が 1 本確定する**設計なので、ここで「判定不能」に落ちたときだけ §6-1 が発火する |
| **M2–M15** | **14** | **(C2)** | **(C2) 本体の可変枠 (⚠ 予約枠)**。[探索 + gate] → [構築 + probe] → [層 3 へ載せる] の 3 段 1 組で回す。⚠ **この枠を他の用途に流用しない** |
| **M7 / M13** | (内数) | — | 棚卸し — 軸の枯渇カウンタ、**側の配分カウンタ** (§6-4)、撤退ラインの判定 |
| **M16–M18** | 3 | 形式化 | **形式化枠**。(C2) について層 1 が通った命題を Lean へ。⚠ **在庫 §17.4 の項目 2 / 3 / 4 (有限個の基数の上での最適化 / `Convex ℝ (bcCapacityRegion W)` の債務 / `martonRegionUnion` 自身の凸性の強度差) は、この枠の内数で高々 1 leg**、かつ **(C2) の形式化を直接 unblock する場合に限る** |
| **M19** | 1 | 記録 | 記録 — proof-log + metrics + 収穫の書き上げ + 正直な棚卸し |

⟹ **(C2) 本体の枠は 14 / 20 = 過半**である。

### 5.1 ⚠ leg 冒頭の宣言規約 — 「どちらへ流れているか」を leg 単位で見えるようにする

前 relay では 14 leg が (C1) 側へ流れたことに**最後まで気づけなかった**。⟹ **各 leg は起票時
(ブリーフ) と着地時 (本節への追記) の両方に、次の 1 行を同じ形式で書く**:

```
**leg 冒頭宣言 (M<n>)**: 側 = (C2) / 動かすもの = <§0 のゴールに対して何を動かすか 1 行>
```

- **側の語彙は 4 つだけ**: `(C2)` / `(C1)` / `形式化債務` / `記録`。⚠ **`gate` は独立の側ではない** —
  gate leg も何の側の gate かを書く (M0 / M1 はどちらも `(C2)` の gate)。
- **書けない leg は起票しない** (§0.1-3)。
- **数え方**: `rg -c '側 = \(C2\)'` を本ファイルに当てる。⚠ **この数が §6-4 の入力である**。

**leg 冒頭宣言 (M0)**: 側 = (C2) / 動かすもの = (C2) の形式化受け皿は既存 Mathlib 部品だけで型検査を通ることを実測し、詰まりが受け皿側ではなく `Thm7` 側の対象の不在 (3 レート / 9 補助変数 / `min` / 量化子の入れ子) にあることを確定した — 在庫は [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md)

### 5.2 ⚠ はみ出し禁止条項 — 「(a′) と同じ流れ方をしたら止める」

前 relay の (a′) は**形式化債務が可変枠を 14 leg 連続で食った形**である。同じ流れ方を具体で禁じる:

1. **形式化債務 (在庫 §17.4 の 2 / 3 / 4) を M2–M15 の枠で消化してはならない**。それらが必要に
   なったときは **M16–M18 の内数**で高々 1 leg、それを超える分は**別 plan へ切り出す**
   (`@residual(plan:<新 plan の filename stem>)` で参照し、本 relay の枠は使わない)。
2. **「(C2) を進めるための前提工事」という理由づけで可変枠を形式化債務に充てる場合、
   その leg の冒頭宣言の側は `形式化債務` である** — `(C2)` と書いてはならない。
   ⚠ **前 relay で実際に起きたのはこの誤記の形**である ((C1) 側の基数境界を「標的の前提」と読んだ)。
3. **2 leg 連続で側が `(C2)` 以外になったら、3 leg 目を起票する前に §6-4 の配分カウンタを引く**。

---

## 6. 撤退ライン

**⚠ 撤退ラインはゴールを下げる装置ではない** (§0.1-2)。撤退とは「本 relay を終了して次の判断を
ユーザーに返す」ことであって、「達成条件を緩める」ことではない。

- **6-1**: **M1 の gate が GO / NO-GO のどちらも返さない** (厳密な一致も厳密な分離も測れず
  「判定不能」で終わる) ⟹ **M7 の棚卸しまで待たずに**、T3-β 側の第一手へ振り替えるかを含めて
  ユーザーに判断を返す。⚠ **数値で分離らしきものが出ただけでは GO/NO-GO のどちらでもない**
  (§2 のとおり掃引は原理的に効かない)。
- **6-2**: **M13 の棚卸しで、(C2) について層 3 に載せられる散文が 1 本も無い** ⟹ M16–M18 の
  形式化枠を層 1 へ戻すかをユーザーに諮る (⚠ **`GOAL-CHANGE` ではない** — 完了条件は動かさず
  配分だけ動かす)。
- **6-3**: **20 leg を使い切って未達** ⟹ **未達と書く**。到達した中間結果は台帳に残すが、
  **ゴール達成とは書かない**。⚠ **前 relay で発火した実績があるライン**である。
  ⚠ **発火とは「未達と書く」ことであって、達成条件を緩めることではない**。
- **6-4 (⚠ 新設 — 配分の撤退ライン)**: **(C2) 本体の枠が形式化側に食われていないかを機械で数える**。
  可変枠 M2–M15 を `N` leg 消化した時点で、**冒頭宣言の側が `(C2)` だった leg 数が `N` の半分を
  切ったら、その場でユーザーに判断を返す** (棚卸しの leg を待たない)。⚠ **予約枠 14 の半分 = 7 に
  届かないことが確定した時点でも同じく発火する**。⚠ **数え方は §5.1 の `rg` 1 本**であり、
  prose の印象で判定しない — 前 relay が気づけなかったのは、まさに数えていなかったからである。

---

## 7. 判断ログ

書く頻度: 方針変更 / 撤退 / 当初仮定の修正。決着済 entry は削除 (git が履歴)、active のみ残す。
プラン予算 ≤ 600 行 / active 判断ログ ≤ 10 entry。
⚠ **番号は本ファイルで独立**である (前 plan の判断ログとは別勘定。相互参照は「前 plan 判断ログ N」と書く)。

1. **継続 + 新規 20 leg (ユーザー決定、2026-08-03)**: 前 relay ([`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md))
   が L0–L20 を完走して**§0 のゴール未達**で終了し、次の判断 (継続 / 軸の入れ替え / 終了) を
   ユーザーに返した。ユーザーは **継続**を選択し、**新規 20 leg の予算**を切った ⟹ 本 plan を新設。
   ⚠ **引き受けたリスクを明示して持つ**:
   - **前 relay は 21 leg を使って未達だった**。そのうち 14 leg が (C1) 側へ流れ、標的である
     (C2) には実質 0 leg しか入らなかった ⟹ **同じ配分事故が再発すれば本 relay も未達で終わる**。
     §5 の枠構造と §6-4 はこのリスクに対する唯一の防具であり、**外したら賭けが変わる**。
   - **(C2) は一次文献が未解決と明言している穴**であり (§1 (iii))、**否定側は Marton 最適性予想の
     反証を含意する** (§1 (iv))。⟹ **20 leg 使ってゴール未達で終わる可能性は依然として高い**。
     それを「中間結果は出た」で埋め合わせない (§0.1-2)。
   - ⚠ **前 relay の中間結果 ((C1) 側の形式化) は本 relay のゴールに対して何も与えていない** —
     出発点は在庫 §17.4 項目 1 であって、前 relay の成果物の上ではない (§1 末尾)。
