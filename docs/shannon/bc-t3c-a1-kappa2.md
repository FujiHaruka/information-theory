# A1 アーク (N4) — [probc] の `SR_C` 点で被覆数 `κ = 2` は実現するか

**判定 = `κ=2 不可能`** (⚠ **より強く `κ = ∞`**。⚠ **射程は「[probc] インスタンス / `SR_C` を運ぶ
レート点 / A1 が採る witness 粒度」の 3 点に限る** — §5 が外れる範囲を書く)。

**親** [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §5.3 アーク宣言 (A1) /
**前段** [`bc-t3c-a1-novelty-gate.md`](bc-t3c-a1-novelty-gate.md) §5 (`κ = 2` は未計算 = A1 の単一の生死判定点)。
⚠ **根拠は恒等式であって掃引ではない** (掃引の非違反は本書のどの結論も支えていない、plan §4.5)。
⚠ 数値の SoT は [`bc-facts.md`](bc-facts.md)。⚠ 一次文献は行番号のみ (取得 = [`lit-fetch.sh`](lit-fetch.sh))。

**記号** `𝒯` = `T_{J|X}` の空間 = 重心 `p` の `Δ(X)` 上の測度 `μ` 全体 (`## M7 (T3b)` 行 2)。
`μ_const` = `J = const` (`= δ_p`) / `μ_X` = `J = X`。`w` = **full witness** (plain `(U,V,W)` +
tilde + hat の 3 系統。`## M11 (T3b)` 行 4 と gate §0-a の「`w` のレート束」がこの粒度)。
`E_R(w) = { μ ∈ 𝒯 : w が μ で適格 かつ w のレート束が μ で R を運ぶ }`、`κ(R) = min{|S| : ⋃_{w∈S} E_R(w) = 𝒯}`。

## 0. 主結果 (2 本)

**(K1) 有限被覆の構造定理 (インスタンス非依存)** — `κ(R) < ∞` なら、**最小被覆の各成員は (19) を
全 `μ` で満たす** (`= (19)-uniform`)。⚠ **Baire もコンパクト性も分離定理も使わない**。
`human-judgment (導出)` + `machine` (アフィン性 = T4)。
⚠ **仮説 `κ < ∞` は真に効いており外せない** — 可算本を許すと、有限台の読みで
`𝒯_fin = ⋃_n {μ : μ({q_n}) = 0}` (`{q_n}` は可算無限点列) が**真のアフィン切片による可算被覆**になり、
成員は (19) 非一様でありうる (`bc-t3c-a1-kappa2-audit.md` §1-a の手構成、`human-judgment`)。
⚠ **(K1) は `κ = 2` 一般を潰さない** — (19)-uniform な 2 本が (20) で相補う配置は残る
(§5-d。同 audit §3 末尾、`human-judgment`)。

**(K2) [probc] / `SR_C` 点での帰結** — `R*` を `SR_C = 2C + d* = 1.1007802` を運ぶレート点とすると
**`κ(R*) = ∞`**。したがって `κ(R*) = 2` は実現しない。`human-judgment (導出)` + `machine` (T1/T2/T3/T5/T5b/T5c/T7)。

**(K3、系) 障害の形** — `μ_X` を覆う witness の `E_R(w)` は**すべて `𝒯` の真のアフィン切片に含まれる**
(= `𝒯` で内点を持たない)。⟹ **薄いのは `μ_X` を覆う成員に限る**。`SR_C` を運び `μ_const` 近傍で適格な
(19)-uniform witness は**実在する** (明示例 = §1b)。⚠ 旧稿の後半節「被覆族の全成員が薄い」は**偽**であり、
`bc-t3c-a1-kappa2-audit.md` §1-b が反例を手構成して覆した (`machine` A11)。

## 1. 導出 (⚠ 各段は §4.5 の道具 1 = 恒等式への帰着)

**(D1) 制約はすべて `μ` について非狭義アフィン**。(19a)–(19c) は等式 (`auxrec.txt:1076-1078`)、
(20a)/(20b) は各 2 本の不等式 (`:1080-1081`)、**(20c)**、(18) の `J` 依存本 (18c)(18d)(18f)(18g)(18h) は
`min{アフィン, アフィン} + アフィン ≥ R` = 半空間 2 本。⟹ **`E_R(w)` は有限本の非狭義アフィン制約で
切り出される凸集合** (machine: T4 で残差 4.11e-15)。
⚠ **(20c) は旧稿の列挙から落ちていた** (`bc-t3c-a1-kappa2-audit.md` §6 訂正 6)。**`J` 非依存ゆえ
`μ` 方向の切り出しには効かず結論は不変**だが、列挙としては上が正しい形である。

**(D2) 線分補題**。`E` が有限本の非狭義アフィン制約で切られ `ν₀ ∉ E` なら、任意の `ν ∈ 𝒯` に対し
`E ∩ [ν₀,ν]` は `0` を含まない閉部分区間 ⟹ その下限は正。最小被覆 `{E_1..E_k}` (`k ≥ 2`) では
最小性より `ν₀ ∈ E_1 ∖ ⋃_{j≥2}E_j` が取れ、有限個の閉部分区間が `0` を避ける ⟹ **`[ν₀, ν₀+δ(ν−ν₀)] ⊆ E_1`**。
(19) 残差は線分上アフィンで 2 点で 0 ⟹ 直線上で恒等的に 0 ⟹ `ν` で 0。`ν` は任意 ⟹ **(K1)**。

**(D3) 場合分けの要らない鎖 (⭐ `## M12 (T3b)` 行 3 の仮説を弱めた形)**。任意の witness で
`(18i) ≤ I(W;Z) + I(V;Z|W) + I(X;Y|V,W)` (2 つの `min` を各々上から落とす。`I(W;Y) ≤ I(W;Z)` の側でも
`min = I(W;Y) ≤ I(W;Z)` なので**符号の場合分けは不要**) `= I(X;Y) + I(Ṽ,W̃;Z) − I(V̂,Ŵ;Y) − [(19a)+(19c)]@const`
(連鎖律 + Markov。machine: T5b で恒等式残差 2.44e-15、第 1 段の超過 0.00e+00)。
⟹ **`(19a),(19c)@const` と `(20b) 下側@X` だけで `(18i) ≤ I(Ṽ,W̃;Z) ≤ I(X;Z) ≤ 2C`**
(`(20b)` 下側@`J=X` は恒等的に `−H(X|V̂,Ŵ,Y)` = machine T5c 1.55e-15 ⟹ `H(X|V̂,Ŵ,Y) = 0`
⟹ 構造補題 `## M12 (T3b)` 行 2 で `H(X|V̂,Ŵ) = 0` ⟹ `I(V̂,Ŵ;Y) = I(X;Y)`)。
⚠ **`J = const` での適格性は要らない — (19) だけでよい**のが (K2) を動かす点である
(machine: T6 で weakened 仮説だけを満たし `J = const` で非適格な witness が **95 本**実在)。

**(D4) 組み立て**。`(18i)` は `J` 非依存 (`auxrec.txt:1069-1071` 逐語 / machine T3 で `J` 掃きの
振れ幅 **0.00e+00**、他 5 本は `≥ 1.10e-01`) ⟹ `E_R(w) ≠ ∅` なら `(18i) ≥ SR_C`。
`κ(R*) = k < ∞` を仮定 ⟹ (K1) で最小被覆の全成員が (19)-uniform ⟹ `μ_X` を含む成員 `w₁` は
`(19)@const` を満たし、かつ `μ_X` で適格 ⟹ (D3) で `(18i)(w₁) ≤ 2C = 1.0620088 < 1.1007802 = SR_C`。
`E_R(w₁) ∋ μ_X ≠ ∅` と矛盾 ⟹ **有限被覆は存在しない**。⟹ (K2)。(K3) は同じ対偶。

⚠ **`R*` の存在は Thm7 の量化子構造からで、矛盾ではない** — Thm7 が与えるのは `∃p ∀μ ∃w` の
点ごとの選択であり、有限化を主張していない。(K2) は**その選択が有限族に取れないこと**を言う。

## 1b. ⭐⭐ 厚い成員の明示例 — `SR_C` を運ぶ (19)-uniform witness

(K3) の旧後半節「被覆族の全成員が薄い」を覆した**反例 witness**。出所は
[`bc-t3c-a1-kappa2-audit.md`](bc-t3c-a1-kappa2-audit.md) §1-b (独立監査の手構成 + `machine` A11)。
**資産として残す** — §5-d の「(19)-uniform な 2 本の反平行条件」の**第 1 の駒**である (同 audit §1-c)。

**構成** (閉形、手構成): `p_X` 一様、`W = const`、`U = (X₁, X₂ ⊕ N₂)`、`V = (X₂, X₁ ⊕ N₁)`
(`N₁, N₂ ~ Bern(α*)` 独立)、**plain / tilde / hat の 3 系統とも同一 (対角)**。

**測定値** (`machine`、監査の独立実装 [`verifiers/kappa2_audit_probc.py`](verifiers/kappa2_audit_probc.py) A11):

- 対角ゆえ **(19) 残差は全 `J` で厳密に 0** ⟹ **(19)-uniform**。
- `(18i) = 1.100780183291 = SR_C` (差 **1.4e-12** = `α*` の桁落ち)。
- `J = const` の (20) 余裕 = `(0.20917, 0.68244, 0.20917, 0.68244)` と**すべて狭義正**。(20c) 残差 **4.4e-16**。
- `R* = (0, SR_C/2, SR_C/2)` を運ぶ。**`μ_const` の相対近傍 120/120 方向で適格かつ `R*` を運ぶ**
  ⟹ **`E_{R*}(w)` は内点を持つ** (= 厚い)。
- ⚠ **この `w` は `J = X` では非適格** (`(20b)` 下側 = **−0.1847 < 0**) ⟹ **(D3)/(D4) とは矛盾しない**。
  薄さは `μ_X` を覆う成員に固有の性質である。

**閉形の理由** (`human-judgment`): `I(U;Y) − I(U;Z) = d*` は、`f(s) := I(X;Y)_s − I(X;Z)_s` の一様点での
凹性ギャップが `max_α D(α) = d*` (`D` = 1 ブロックの more-capable 差) に一致することから出る。
⟹ `(18i) = 2C + d*` は偶然ではなく **`d*` の変分的特徴づけそのもの**である。

⚠ 鏡映 `σ` で**第 2 の駒**が出る (同 audit §1-c)。**2 本で `𝒯` を覆えるかは §5-d のまま未決**である。

## 2. (S1)–(S4) の逐語確認

- **(S1) 有限被覆は (19) 一様 witness に限られる = ⭕ 結論は成立。⚠ ただし gate §4-b の Baire 論証は
  差し替えた**。反証を 1 本当てたところ**位相依存の穴**が出た: `𝒯` を有限台の測度 (= 有限アルファベット
  `J`) と読むと `𝒯 = ⋃_n {|supp| ≤ n}` が**自分自身の中で痩せた閉集合の可算和** ⟹ **`𝒯` は Baire 空間で
  ない** ⟹ 「疎閉集合の有限和は覆えない」が使えない。閉包 (全測度) を取れば Baire は回る。
  ⟹ **どちらの読みでも成り立つ (D2) の線分補題へ置換した** (最小性 + アフィン性のみ、有限的)。
  ⚠ 「`E_R(w)` が真の閉アフィン超平面に含まれるなら疎」自体は正しい (内点を持てば アフィン包が `𝒯` の
  アフィン包と一致し超平面に入らない)。
- **(S2) `J = X` を覆う witness の適格集合は `{I(X;J_μ) ≥ d*}` に含まれる = ⭕ 逐語どおり**。
  ⚠ **T7 が再現したのは `## M12 (T3b)` 行 8 の `machine` 欄だけ** — `|I(V̂,Ŵ;J₁) − I(X;J₁)| = 1.78e-15` と
  depolarizer 較正 (`I(X;J₁) = 0.020414 / 0.078072 / 0.227782`、上限 `1.0824230 / 1.1400807`) であって、
  **頑健性の不等式 `(18i) ≤ 2C + I(X;J₁)` の本体は T7 に掛かっていない** (旧稿は「上限を machine で再現」と
  書いており主張しすぎだった)。⭐ **不等式本体を恒等式 + DPI で機械に載せたのは独立監査**である
  ([`bc-t3c-a1-kappa2-audit.md`](bc-t3c-a1-kappa2-audit.md) §5 の A8、残差 **2.66e-15**)。
  ⚠ **本書の (K2) はこの行を使っていない** — `I(X;J₁) < d*` の窓を要求しない ((19)-uniform が
  `J₁ = const` そのものを与えるため)。⟹ 行 8 は下界 `κ ≥ 2` の再現用としてのみ引いた。
- **(S3) 幾何的障害 1 本は閉じている = 再探索していない**。⚠ 併記の宿題「`I(X;J_μ)` がチャネルに
  ついて凸」は確認済 — `p_X` 固定で `I(X;J) = H(X)_p − ∫H(X)_q dμ(q)` は `μ` について**アフィン**
  (したがって凸かつ凹) であり、劣位集合 `{I < d*}` は凸。machine: T4 の `XJ` 項の残差 (4.11e-15 に含む)。
- **(S4) 決着は反平行条件の側で付く = ❌ 訂正した**。見立ての ⚠ 部分「(20) 上側は `μ` についてアフィン
  ではなく凹包条件」は**取り違えである**: `## M12 (T3b)` 行 7 の凹包は **`∀μ` を潰したときの形**であって、
  **`μ` を固定した (20) 上側はアフィン不等式**である (machine T4 が (20a)/(20b) の上側残差まで込みで
  4.11e-15)。⟹ `E_R(w)` は**上側も含めて半空間の有限交わり**。
  ⟹ **反平行条件を書き下す前に (D2) が効いてしまい、`SR_C` 点では (20) 側の代数に入る前に決着した**。
  ⚠ **反平行条件そのものが死んだのではない** — それは `κ = 2` の**一般の**判定条件として残る (§5-d)。

## 3. 機械検証 (再検証コマンド)

```
python3 docs/shannon/verifiers/kappa2_probc.py      # 11 テスト / 約 7 秒 / 終了コード 0 が PASS
```

検証器は Theorem 7 の `(18a)`–`(18i)` / `(19a)`–`(19c)` / `(20a)`–`(20c)` を `auxrec.txt:1037-1082`
から逐語実装し、[probc] Lemma 8 (`probc.txt:900-923`: `X1→Y1 = BEC(e)`, `X1→Z1 = BSC(p)`,
`X2→Y2 = BSC(p)`, `X2→Z2 = BEC(e)`, `p = 0.1`, `e = h(0.1)`) の積チャネル上で評価する。
⚠ **本 leg の 4 度の設計中の誤りは検証器が出した** (`(19a)+(19c)` の符号 / `_joint` の軸 /
`d*` の全単体最大化の未収束 / weakened 仮説の非空虚性が最初の族では 0 本)。

## 4. `bc-facts.md` の既存 claim の再現 (⭐ N5 閾値表の入力 = **7 本**、値レベル一致)

| # | 節・行 | 再現した値 | test |
|---|---|---|---|
| 1 | `## M12 (T3b)` 行 9 | `C = 0.5310044` / `2C = 1.0620088` / `d* = 0.03877137` at `α* = 0.07766967` / `SR_C = 1.1007802`、全単体最大も **同値** (相関で増えない)、`min D₁ = 0.000e+00` | T1 |
| 2 | `## M12 (T3b)` 行 2 | 構造補題 — `Y`/`Z` とも非混同対 **0 組**、決定的 `A` (集合分割 15 × 受信機 2) の反例 **0/30** | T2 |
| 3 | `## M12 (T3b)` 行 3 | 主定理の 3 段 (S2)(S3)(S4) 残差 **2.44e-15 / 8.88e-16 / 0.00e+00** + `(19a)+(19c)` = レート差 **1.22e-15** | T5 |
| 4 | `## M12 (T3b)` 行 4 | uniform 到達 witness (`W = X`, `U = V = const`) の `(18i) = 1.0620088 = 2C`、全 `J` で適格 (違反 **0.00e+00**) | T9 |
| 5 | `## M12 (T3b)` 行 8 | 行 8 の **`machine` 欄** = `I(V̂,Ŵ;J₁) = I(X;J₁)` (残差 1.78e-15) + depolarizer 較正 `I(X;J₁) = 0.020414/0.078072/0.227782`、上限 `1.0824230/1.1400807` (前者のみ `< SR_C`)。⚠ **頑健性の不等式本体は T7 に掛かっていない** (機械に載せたのは監査 A8、残差 2.66e-15 — §2 (S2)) | T7 |
| 6 | `## M11 (T3b)` 行 4 | 適格性が `μ` についてアフィン ⟹ `E(w)` 凸。残差 **4.11e-15** | T4 |
| 7 | `## M11 (T3b)` 行 5 | witness の時分割は混合重みについてアフィン (残差 **5.11e-15**) ⟹ 残差の併合は線形 | T8 |

⚠ **T3 / T5b / T5c / T6 は本 leg の新結果側の検証**であって既存 claim の再現ではない (上表に数えていない)。
⚠ **T6 は掃引であり証拠として数えていない** (整合確認。plan §4.5)。
⚠ row 1 の「全単体最大も同値」は **400 リスタート Nelder-Mead 由来 = `optimizer`** であって `machine` ではない
(plan §4.5 により最適化器由来の非違反は証拠価値が低い。監査 §5 の註記)。

## 5. 残る段 (⚠ 未決部分)

- **(a) witness 粒度を落とした被覆数 `κ'` は未決** — A1 は `w` を full witness に取る (`## M11 (T3b)` 行 4 /
  gate §0-a)。**plain `(U,V,W)` を `μ` ごとに選び直し `(hat,tilde)` だけを固定する** 読み `κ'` では、
  (19) が「`L(μ) ∈ 𝒞` (`## M11 (T3b)` 行 2 の凸体帰属)」に緩み**等式でなくなる** ⟹ (D2) が効かない
  ⟹ **`κ' = 2` は排除されていない**。⚠ `κ' ≤ κ` なので (K2) からは何も出ない。**`κ'` 読みは (D2) が
  効かない別の入口**である。⚠ **唯一の入口ではない** — (b)–(d) も同じく未決であり、なかでも
  **§5-d の方が具体的な駒を持つ** (§1b の明示 witness。監査 §1-c / §3 末尾)。
- **(b) `SR_C` 以外のレート点は未決** — (D4) は `2C < 和レート` を使う。和レートが `2C` 以下の点では
  uniform 到達 witness (再現 #4) が効いて `κ = 1` になる場合があるが、`R₁`/`R₂` 側の角は測っていない。
- **(c) 他チャネルは未決** — 構造補題 (再現 #2) と `2C < SR_C` の 2 つが [probc] 固有。(K1) だけは
  インスタンス非依存である。
- **(d) 反平行条件の代数は未着手** — (K1) により一般の `κ = 2` 判定は「**(19)-uniform な 2 本の
  witness の (20) + レート束の半空間系が `𝒯` を覆うか**」に落ちる (S4 の訂正後の形)。本 leg は
  `SR_C` 点で (D4) が先に効いたため、この代数へ入っていない。
  ⚠ **(K1) はこの一般の `κ = 2` を潰さない** (監査 §3 末尾)。⭐ **第 1 の駒は §1b で実在**する
  (`SR_C` を運ぶ (19)-uniform witness、鏡映 `σ` で第 2 の駒)。⚠ **駒が 2 つ揃うことと `𝒯` を覆うことは
  別**であり、2 本の (20) 半空間系が `𝒯` を覆うかの代数は**手つかずのまま未決**である。
- **(e) N5 への入力 (⚠ 判定は N5 の担当)** — 本インスタンスの `SR_C` 点で確定したのは **`κ = ∞`** の
  1 点だけである。⚠ **`κ ∈ {1, ∞}` への二値化 (= gate §5 の `inert` 条件) は未確立** — 測ったレート点が
  `Thm7^unif` の点と `SR_C` 点の 2 つだけだからである (監査 §6 訂正 3)。
  gate §5 の `inert` 条件に直接効くのはこの 1 点であり、(a)–(d) が開いている間は本 leg は
  「`κ` が 2 値へ潰れる」を**このレート点についてのみ**支持する。
