# N16 — 「現行 [probc] more-capable インスタンスで `UV = C` か」の gate (判定枠 第 3 組 第 2 段の**上流** gate)

**Parent**: [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §5.1 (N16 起票ブロック) /
**配分決定の SoT** = 同 plan §7 判断ログ 11 (⚠ **「第 2 段の上流に置く」根拠・引き受けたリスク・棚卸しを取らない
逸脱の条文はそちら。本書は複製しない**) /
**問いの出所** = [`bc-t3c-n15-instance-gate.md`](bc-t3c-n15-instance-gate.md) §6-8 +
[`bc-t3c-n15-audit.md`](bc-t3c-n15-audit.md) §5-5 + facts [`bc-facts.md`](bc-facts.md) `## N15 (T3c)` の **N15-h**。

**leg 冒頭宣言 (N16)** (⚠ 親 plan §5.1 の N16 起票ブロックからの逐語コピー): 側 = (C2) / 動かすもの =
N15 が名指しした開いた問い「現行 [probc] more-capable インスタンスで `UV = C` か」を gate し、
`UV = C` なら N7 / N9 / N10 の `Thm7 = C` が挟み込みで強制されていたことを確定させて判定枠 3 組が §0 に
対して持っていた情報量を較正し、`UV ⊋ C` なら [probc] 上の `Thm7 = C` は挟み込みでは説明されないことを
確定させて N15 の選別基準の射程を測る

> ⏳ **未実行 — 判定はまだ 1 つも出ていない**。
> ⚠⚠ **本書の §0 と §1 は着手前に書かれたものであり、実行 leg は 1 文字も書き換えてはならない**
> (親 plan §4.4-2 の義務)。⟹ **事後に見立てを直して当てにいく経路を構造的に塞ぐ**のが趣旨である。
> ⚠ **見立ての当たり外れ / 反証条件の発火状況は §4 に別途書く** — §0 / §1 は動かさず、**効きは着地側に書く**
> (親 plan §5.1 の N11 着地ブロックが確立した作法)。
> **凍結の機械確認** = 起票 commit と現行の同区間 (`## 0.` 行から `## 2.` の直前まで、起票時のみ在った
> プレースホルダ行を除く) の md5 一致。

---

## 0. 着手前の見立てと「締める / 緩める」の明記 (§4.4 の義務。⚠ 事後に書き換えない)

⚠ **本節は機械を 1 つも走らせる前・一次文献を 1 行も引く前に書いた**。以下は紙の上の見立てであり、
機械に掛けた結果は §3 に別途書く (一致しなかったものは一致しなかったと書く)。
⚠ **見立ては当たりにいくものではなく較正の材料である**。

### 見立て A (**緩める**) — 現行 [probc] more-capable インスタンスでも `UV ⊋ C` が立つ

理由 = [probc] の寄与そのものが「**容量が既知でありながら UV と一致しないクラス**」の構成であり、その厳密性を
生む機構は **UV がブロック間で相関した補助変数を許す**ことである (facts [`bc-facts.md`](bc-facts.md)
`## M5 (T3b)` = 争点が優加法性と同型であるという判定)。**more-capable 積でも積構造は同じ**なので同じ機構が
効く公算がある。⚠ **`C` 側が超加法性の分だけ持ち上がっていることは分かっている**が (facts `## M1 (T3b)` の
副産物行 = [probc] Lemma 8 の閉形。⚠⚠ **値はここへ写さない**)、⚠ **`UV` 側が同じ量ちょうどしか持ち上がらない
理由は無い**。

**殺す道具 (§4.4-2 の義務どおり着手前に決めた)** = **§4.5 の道具 1 (恒等式への帰着)** — 「相関補助変数の利得が
**UV の制約の側で**恒等式により潰れる」ことを示せれば A は死ぬ。
⚠⚠ **道具 2 / 道具 3 では A を殺せない** — 明示 witness の直接評価 (道具 3) は `UV ⊋ C` を**立てる**方向にしか
効かず、**「witness が見つからない」は §4.5 により証拠価値がほぼ 0** である。⟹ **A を殺すのは恒等式だけである**。

### 見立て B (**締める**) — 本 gate はどちらの側も返せず「開いている」で終わる

理由 = N15 成果物 §6-8 と N15 監査 §5-5 が**独立に攻めて settle できなかった**と明記しており、しかも監査は
[probc] 内の strictness の言明を**全走査**している (= **文献側の材料は尽きている**)。⟹ `UV` の支持関数を
自前で評価するしかなく、それは N15 §6-2 (「`Thm7` の値を 1 つも計算していない」) と同じコストの側にある。

**着手前の 1 行反証 (§4.4-1 の義務)** = ⚠ **本 gate が要るのは `UV` と `C` の比較であって `Thm7` の値ではない**。
しかも `UV ⊋ C` の側は**片側**でよい — N15 は [glnsum] 側で**明示 witness 1 本の直接評価**だけで `UV ⊋ C` を
立てている (facts `## N15 (T3c)` の **N15-c**) ⟹ **同型の片側評価は [probc] インスタンス上でも成り立ちうる**
⟹ **B は無条件には成り立たない**。
⚠ **裏取りの状態を正直に書く**: この反証が引く片側評価は **facts N15-c の再検証コマンド (検証器 `G4`) が
[glnsum] 側で機械に持っている**形であって、⚠⚠ **[probc] インスタンス上で同型の witness が実際に構成できるかは
別問題である** (それは本 leg の仕事であり、起票時点では裏が無い)。
⚠ **逆側 (`UV = C`) は全 witness にわたる主張ゆえ片側評価では出ない** ⟹ **B は「`UV = C` の側は本 leg では
出ない公算が高い」という弱い形でなら生き残る** (⚠ **その弱い形を判定するのは §1-(b) の反証条件 1 である**)。

### 見立て C (**締める**) — `UV = C` が出ても較正は [probc] の 1 点の上に限られ、N10 の半空間全体には及ばない

理由 = N15-h が名指した問いの主語は「**現行 [probc] more-capable インスタンス**」= **1 点**である。一方
N10 の決着の射程は**半空間 `e ≤ h(p)` の全体**へ広がっている (親 plan §7 判断ログ 7、逐語の SoT = facts
`## N10 (T3c)`)。⟹ **1 点で `UV = C` が出ても、挟み込みが `Thm7 = C` を強制するのはその点の上だけ**であり、
**半空間の内部では `UV` を別に評価する必要が残る**。

**着手前の 1 行反証 (§4.4-1 の義務)** = 挟み込み `C ⊆ Thm7 ⊆ UV` の 2 本は**チャネルにもレートにも仮説を
持たない** (facts `## N15 (T3c)` の **N15-b** が逐語 2 本で確認済) ⟹ **`UV = C` が半空間の全体で立つなら
`Thm7 = C` も全体で強制され、C の言う「1 点に限られる」は崩れる** ⟹ **C が成り立つのは `UV = C` が 1 点
(あるいは半空間の真部分) でしか立たない場合に限る**。
⟹ ⚠⚠ **C を生かすも殺すも、`UV` 側の判定が `(p,e)` にどう依存するかを見なければ決まらない** —
⚠⚠ **1 点の判定を半空間へ黙って延長するのが本 leg の最大の誤り方**であり、**C はそれを先に名指しするために
置いた見立てである**。

### 見立て D (**締める**) — `UV` の版の取り違えで gate が空回りする

理由 = `UV` には**一般 witness 版**と**独立版** (`p(u)p(v)p(w|u,v)`) の 2 つがあり、facts `## M1 (T3b)` は
**この取り違えで 1 度事故っている** (同節の行 4 / 行 4b。⚠ **(α) は現在も未確認である**)。⟹ 「`UV = C` か」を
版を固定せずに問うと**答が 2 通りある**。

**着手前の 1 行反証 (§4.4-1 の義務)** = 本 gate の挟み込みが使う `UV` は **N15 §2.1 が版を明示して照合した
一般 witness 版**であり (facts N15-b)、⚠ **`Thm7 ⊆ UV` が成り立つのもその版である** ⟹ **本 gate が問うべき
版は一意に決まっている** ⟹ **D は無条件には成り立たない**。
⚠ **弱い形では生き残る** — `O_indep ⊆ UV_general` ゆえ、⚠⚠ **文献が `O_indep = C` を言っていても
`UV_general = C` は従わない**。⟹ **一次文献の言明を引くときに版を確認する義務は残り、それは §1-(b) の
反証条件 3 の入力である**。

### ⚠ 併記する 3 点 (どれか 1 つだけを書かない)

1. ⚠⚠ **本 leg は gate であって `Thm7 ⊋ C` の材料を出す leg ではない** — **どちらの側に転んでも出ない**。
   **排除もされない**。
2. ⚠⚠ **`UV = C` 側は「材料が出る」ことではなく減算方向の較正である** — 判定枠 3 組 (N6 … N11) が §0 に
   対して持っていた情報量が**小さかった**という測定であって、⚠ **それらの判定が誤りだという主張ではない**
   (facts N15-h が同じ限定を明記している)。
3. ⚠ **「文献に無い」≠「偽」** — 監査の全走査で「more-capable 側の厳密性は [probc] のどこにも無い」ことは
   確定しているが、⚠⚠ **それは `UV = C` の証拠ではない** (親 plan §3 の T3-β 側の注意がここにも効く)。

---

## 1. 反証条件 (§4.4-2 の義務。⚠ 着手前に書いてある。事後に書き換えない)

### (a) 継承分 — ⚠⚠ **無い。ただし SoT を指し、複製も書き換えもしない**

⚠ **本 gate は継承した反証条件を 1 本も持たない**。近傍の 2 組は**対象が違う**ので継承しない:

- [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md) §4.2 の 3 本 = **候補 4 (`e > h(p)` 側) の分**。
  ⚠ **1 本目は N15 で発火済 / 2 本目・3 本目は「本 leg では評価していない」のまま**である (発火状況の SoT =
  [`bc-t3c-n15-instance-gate.md`](bc-t3c-n15-instance-gate.md) §4.2 (a))。⟹ ⚠⚠ **N16 もこの 2 本を評価しない**
  — 本 gate の標的は `e ≤ h(p)` 側であり、⚠ **「不発火」とも書かない**。
- [`bc-t3c-n9-cone-gate.md`](bc-t3c-n9-cone-gate.md) §4.2 の 3 本 = **N10 の分**。⚠ **決着済であり本 gate は
  消費しない**。
- ⚠⚠ **どちらも複製しない / 書き換えない** (親 plan §7 判断ログ 8 / 10-(3) の条項)。

### (b) 新規 3 本 (⚠ **発火を判定する道具を条件ごとに名指す**。⚠⚠ 「うまくいかなかったら」型は不可)

1. **`C` 側が本 gate の要求する形で手に入らない** ⟹ ⚠ **`UV = C` の側の判定を降ろす**。
   **条件文** = 選んだ方向で `h_C(λ)` が **道具 1 (恒等式) か 道具 3 (閉形 / 明示 witness の直接評価) の
   どちらでも**出ず、**最適化器の出力にしか依存しない**とき発火する。⚠ **facts `## M5 (T3b)` 行 6 が
   「[probc] の領域は境界点 1 つでも最適化が要る」と明記しており、これが 3 本の中で最も起きやすいと予測する**。
   **発火したときの処置** = ⚠⚠ **`UV = C` と書かない**。書けるのは `UV ⊋ C` (片側 witness が立つ場合) か
   **判定不能**だけである。⚠ **「掃引したが超過が見つからなかった」を `UV = C` の証拠にしない**
   (親 plan §4.5 = 前 relay で 6 度再現した失敗形)。
   **発火の判定に使う道具** = **道具 1 / 道具 3** (⚠ **道具 2 (構造で候補を有限に落としてからの全探索) は
   ここでは使えない** — 母集団が補助変数の連続空間である)。

2. **`UV ⊋ C` が立ち、しかも `Thm7 = C` が既に確定している** ⟹ ⚠ **「第 2 段の土台は妥当」という読みを降ろす**。
   **条件文** = [probc] インスタンス上で `UV ⊋ C` の**明示 witness が 1 本**立ったとき発火する (⚠ **`Thm7 = C`
   の側は facts `## N9 (T3c)` / `## N10 (T3c)` で既に確定しており、本 leg は再導出しない**)。⟹ **挟み込みは
   [probc] 上で `Thm7 = C` を強制していなかった**ことになり、⟹ ⚠⚠ **N15 の選別基準 (「生きた候補は `UV ⊋ C`
   の族に限られる」) は実例により「必要ではあっても十分ではない」ことが確定する** ⟹ **和チャネル族の選定根拠が
   1 段弱まる**。
   **発火したときの処置** = ⚠ **gate を捨てるのではなく出力の向きが変わる** — ⚠⚠ **「第 2 段の土台が強まった」
   とは書かず、弱まったと書く**。
   **発火の判定に使う道具** = **道具 3 (明示 witness の直接評価)**。

3. **問いが既に一次文献で閉じていると判明する** ⟹ ⚠ **判定を出さず `known-result` と書いて gate を捨てる**。
   **条件文** = **[probc] の外**の一次文献に、逆向き more-capable 積上の `UV` と `C` の関係を決める言明が
   在ったとき発火する。⚠ **監査が全走査したのは [probc] の内側だけ**であり (監査 §5-5)、⚠ **`Thm7 ⊆ Thm8` の
   順序を掃いたときも 2026 年の 2 本 (`GK-outer` / `glnsum`) しか見ていない** (facts `## N15 (T3c)` の
   **N15-e**) ⟹ **この条件は実際に発火しうる**。
   ⚠ **発火の判定には版の確認を伴う** — 見立て D のとおり ⚠⚠ **`O_indep = C` の言明は `UV_general = C` を
   含意しない**。
   **発火の判定に使う道具** = **逐語照合** (⚠ **取得は [`lit-fetch.sh`](lit-fetch.sh)。抽出テキストは repo が
   public ゆえコミットしない。行番号は `pdftotext -layout` 出力に対する実測値で書く**)。

### 1.1 出力型 (⚠ 着手前に固定する。⚠⚠ 判定そのものはここに書かない)

本 gate の出力は **3 値**である:

- **`UV = C`** — 現行 [probc] more-capable インスタンス上で一致する ⟹ **挟み込みにより `Thm7 = C` は
  強制されていた** ⟹ ⚠⚠ **減算方向の較正** (判定枠 3 組が §0 に対して持っていた情報量の測定)。
  ⚠ **見立て C のとおり、射程が 1 点か半空間かを必ず書き分ける**。
- **`UV ⊋ C`** — 挟み込みは強制していない ⟹ **反証条件 2 が発火し、N15 の選別基準の射程が測られる**。
- **判定不能** — ⚠ **「開いている」で終わることは失敗ではない**が、⚠⚠ **`UV = C` の側へ丸めない**
  (**`UV ⊋ C` の witness が見つからなかったことは `UV = C` の証拠ではない**)。

⚠ **否定的判定 (`UV = C` / 反例 / 壁) を出した場合は親 plan §4.6 の敵対的独立監査が必須**である
(⚠ **既定の立場を「その判定は偽である」に置いた敵対的ブリーフにする / 被監査 leg の検証器を 1 行も呼ばない
独立実装にする**)。

### 1.2 ⚠ 禁止事項 (親 plan §1 / §3.1 / §4.5 からの再掲。⚠⚠ 本 leg でも 1 つも緩めない)

- **`Thm7 ⊋ C` の材料が出たと書かない** — gate ゆえどちらの側でも原理的に出ない。⚠ **排除もされない**。
- **`Thm7 ⊄ Thm8` を主張しない** — ⚠ **領域レベルの順序はどちら向きも未証明である** (facts `## N15 (T3c)`
  の **N15-e** = 本族の生死を握る唯一の外部依存)。
- **`R ∈ Thm7` と書かない**。
- **「あと少し」「残るのは行数と配線だけ」型の言い回しを使わない** (親 plan §4.1 軸 2 が名指しで禁じる tell)。
- **「方法の死」を「包含の否定」と書かない**。
- ⚠⚠ **`e < h(p)` の帯を「比較の相手が無い」という理由で捨てない** — **その帯が的として空なのは標的が真だと
  証明されたからであって理由が違う** (SoT = [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md) §4.1 の
  訂正 3 / 訂正 4)。⚠ **「比較の相手が無い」は `e > h(p)` 側の死因である** (facts `## N15 (T3c)` の **N15-f**)。
- ⚠ **数値掃引の非違反を証拠にしない** — ⚠⚠ **許容差を締める / 格子を細かくするのは証拠にならない**
  (親 plan §4.5、前 relay で 6 度再現)。

---

⏳ **以下 §2 以降は未記入である。**

## 2. （実行 leg がここから書く）
