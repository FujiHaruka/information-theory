# N2 敵対的独立監査 — 形式化債務の員数

> **Parent plan**: [bc-open-problem-t3c-plan.md](bc-open-problem-t3c-plan.md)
> 被監査成果物 = [bc-t3c-n2-estimate.md](bc-t3c-n2-estimate.md) (⚠ 本監査は 1 バイトも編集していない)
> 既定の立場 = 「N2 の判定『収まらない』は偽である」。以下は攻撃して落ちなかったものだけである。
> 再生成した probe = `docs/shannon/probes/t3c-n2/` (4 本)。⚠ `InformationTheory/` へは 1 行も書いていない。

---

## 0. 判定

**訂正あり生存** — 判定「収まらない」は生きている。**訂正 5 件**、うち **1 件は員数そのものを 41 → 22 に
下げる**。⚠ ただし員数の減少は**中核定理の本数を 1 本も減らさない**ので、判定を覆さない。

---

## 1. 再現義務の結果 (⚠ 被監査 probe は消滅しており、コードは監査側が独立に書き直した)

被監査 scratchpad `.../d6d1ee1c-.../scratchpad/n2/` は**機械確認で消滅**
(`ls` → `no matches found`)。⟹ N2 の「41/41 が型検査を通った」「629 行」「5.2 秒」は
**当時の証拠が存在しない主張**である (親 plan §4.7)。以下は N2 の *主張* だけを読んで書き直した実測。

| # | 対象 | N2 の主張 | 監査の実測 | 判定 |
|---|---|---|---|---|
| **R1** | 領域 `Thm7(W)` を端から端まで (P9 相当) | 191 行 / 既定予算 5.2 秒 / 0 sorry / 通 | **167 行 / 3.77 秒 / 0 sorry / 通**。`⋃_p ⋂_{kJ,T_J} ⋃_{aux}` の入れ子 / `\|X\|+6`・`\|X\|+1` の基数境界 / 13 変数の周囲空間 / **25 スロット** (うち 17 が `(18)` 側) / 4 節の法則述語 / `R₀=0` スライス + `slice_comm` (証明つき) をすべて含む | ⭐ **再現した。しかも安く再現した** |
| **R2** | (β) 実効性層の言明 (P12 相当) | 133 行 / 言明のみ / 証明 0 行 / 通 | **127 行 / 2.76 秒 / 0 sorry / 通**。実効開・実効閉 (Π01)・実効コンパクト・computably overt・実効連続 + (L-∃) / (L-∀) / 一様 Π01 の ⋂ 閉包 / 制約集合の Π01 + 古典側の対照 3 本 (**この 3 本だけ証明つきで通る**) | ⭐ **再現した** |

`docs/shannon/probes/t3c-n2/r1-thm7-region.lean` / `r2-effectivity-layer.lean`。
⚠ **R1 が保証しないもの** (N2 と同じ): 17 スロットと変数の対応 / 入れ子の向き / 上限値が `Thm7` のもので
あること。⚠ **R2 が保証しないもの**: 何も証明していない (古典側 3 本を除く)。

⚠ **監査側が踏んだ落とし穴を 1 件そのまま記録する**: R2 の初版は実効コンパクト性を
`∀ S, IsCompact S → IsEffectivelyCompactN S` と書き、**これは偽の命題なのにコンパイラは黙って通した**。
⟹ N2 §7 の N18 への申し送り (「定義できたから段が軽い と読まない」) は、**独立に再現した**。

**N2 の内部算術は合う** — `144+120+191+133+41 = 629`、表 13 ファイルの行の和 `= 1017`。
⚠ **内部整合性は成果物の実在を保証しない** (親 plan §4.7 (b))。

---

## 2. 攻撃 5 軸

| 軸 | 掛けた機械 | 返ってきたもの | 判定への影響 |
|---|---|---|---|
| **1. 中核 1–7 は §0 に本当に必要か** | `r4-goal-statement-discrete.lean` を新規に書いて `lake env lean` | ⭐ **§0 の完了条件は肯定側・否定側とも、離散 `Computable` + `Primcodable` + Mathlib の `hausdorffDist` だけで言明できた** (55 行 / 2.71 秒 / 0 sorry / 実効性述語 0 個)。チャネルは有理行列、出力は有理点の有限リストで、**計算可能性述語を跨ぐ型はすべて離散**である | **訂正 (下記 C5)。判定は覆らない** |
| **2. Mathlib に計算可能解析が 0 宣言か** | loogle 10 クエリ + Mathlib ディレクトリ実査 + in-project `rg` | N2 の 5 クエリは**全件そのまま再現** (`Computable, Real` / `Primcodable, Real` / `ComputablePred, Real` / `Computable, Real.log` / `Computable, MeasureTheory.Measure` → すべて `Found 0`)。**結論形も再現** — `\|- Computable _` は `Found 71 … 43 match` で、**43 件を全部目視して全件が `ℕ` / `List` / `Vector` / `Option` / `Sum` / `Nat.Partrec.Code` 上**。有理近似方向を追加で 5 本引いても `Computable, Rat` / `Computable, CauSeq` / `Computable, Real.mk` / `Primcodable, Metric.ball` はすべて `Found 0`。`Mathlib/Computability/` を実査し、**Topology / Analysis / MeasureTheory を同時に import する Mathlib ファイルは 2 本だけ**で、どちらも `Computability/AkraBazzi/`(漸化式の漸近論) で無関係。**⚠ in-project は 0 件ではなかった** (下記 C3) | **訂正 (C3)。Mathlib 側は生存** |
| **3. 員数 41 は膨らんでいないか** | `r1` を圧縮形で書き直して `scripts/sig_view.ts --names` で数え直し | ⭐ **潰れた**。9 本の補助変数を `Fin 3 × Fin 3` 添字の**族 (Pi 型)** にすると、N2 が 13 射影・`thm7OneLaw`/`thm7OneLawP`・`IsThm7LawShape`/`IsThm7LawShapeP` 等に割った**領域層 33 def が 12 def に潰れる**。(β) 語彙は 8 → 10 (Primcodable instance と単体 1 本を明示した分)。**like-for-like で 41 → 22** | **訂正 (C1)。⚠ 中核定理は 11 本のまま ⟹ 判定は覆らない** |
| **4. 軸の決定 (3 レート新設) の逐語** | facts `## L0 (T3)` の URL から `auxrec.pdf` を `curl` + `pdftotext -layout` で**再生成**し引き直し | ⭐ **行番号まで完全に再現した** (`auxrec.txt` 2951 行)。**`auxrec.txt:1038` 逐語** = `R0 + R1 ≤ min{I(W ; Y ), I(W ; Z)} + I(U ; Y \|W )` ⟹ **`R₀=0` を代入しても `W` は消えない**。さらに `(18c)`–`(18i)` は `Ŵ`/`W̃` を、`(19a)` は 3 本すべてを含み、**基数境界 (`:1083-1084`) は `R₀` に依存しない**。⟹ 「`R₀=0` なら 3 本落ちる」は**独立に潰れた**。副産物: **17 スロット / +8 = 25 スロット / 補助 9 本 / 13 変数**を `:1037-1084` から独立に数え直し、**N2 の 4 つの数はすべて厳密に一致** | **N2 が生存。訂正なし** |
| **5. 判定の閾値そのもの (強度 diff)** | 最近接の in-tree 資産を `wc -l` + honesty 確認 | 計算可能性を実数に当てる in-project の唯一の先例 = `Shannon/Kolmogorov/OmegaNoncomputable.lean` = **601 行 / 0 sorry / 0 residual**。その中身は `IsComputableENNReal` (**点ごと**・`ℝ≥0∞`・位相を 1 語も含まない)。中核 3–7 は**集合レベル・パラメータ一様・位相つき**で、**最近接先例より厳密に強い**。⟹ 弱い版で §0 の Thm7 経路を支える道は見つからなかった | **N2 が生存。⭐ この軸が判定を支えている** |

---

## 3. 訂正一覧

| # | N2 文書の該当箇所 | 誤り | 正しい値 / 機械の出力 |
|---|---|---|---|
| **C1** | §0 「員数 = 新規 def 41 本」/ §3.1 の表 (領域層 30 + 3) | **員数が書き方に依存しており、41 は最も高い側の書き方の値である** | 9 補助を `Fin 3 × Fin 3` 添字の族にすると **領域層 12 def + 補題 1** で同じ内容が通る (`r1-thm7-region.lean`、167 行 / 3.77 秒 / 0 sorry)。like-for-like 総計 **41 → 22** |
| **C2** | §6 前提の箱「`StandardBorelSpace` は右入れ子の積で 5 因子目から既定予算では合成できない ⟹ `synthInstance.maxSize 2000` + `maxHeartbeats 4000000` か手組み `have` 11 行」 | **恒久的な注意事項として書かれているが、平坦な 13 重積という符号化の副作用である** | `r3-standardborel-pi-vs-flat.lean` が 1 ファイルで両方を出す — **Pi 型版は `set_option` 無し・既定予算で `inferInstance` が通り**、N2 の平坦 13 タプル版は同じファイルで `failed to synthesize instance of type class StandardBorelSpace (…)` と落ちる |
| **C3** | §2 の β-0 行 / N0 在庫 §3.2 の「in-project は `rg` で **0 decl**」「`ℝ` / `Set` / 測度に触れるものは 0 件」 | **偽**。名前検索が `semicomput` / `effectively compact` / `computably` に寄っていて結論形で引いていない | `InformationTheory.Kolmogorov.IsComputableENNReal` (`Shannon/Kolmogorov/OmegaNoncomputable.lean:78`) = **有理 (二進) 近似で計算可能実数を定義した現物**。`IsFloorComputableENNReal` (`:89`) と橋 `isComputableENNReal_of_floor` (`:93`) つき、**0 sorry / `@audit:ok`** |
| **C4** | §0 ⭐ 「`Primcodable ℝ` は存在しないので**有理近似の層を先に作らないと言明すらできない**」 | **強すぎる**。C3 の現物が反例であり、言明に要る層は薄い | `Primcodable ℚ` は **`import Mathlib.Data.Rat.Denumerable` の 1 行**で通る (実測: その import 前は `failed to synthesize instance of type class Primcodable ℚ`、後は 4 例すべて通る)。⚠ `import InformationTheory` の閉包には入っていないので**この 1 行は必ず要る** — N2 の前提の箱に無い |
| **C5** | §5.1 の根拠「中核 **1–7** は BC とは独立した一般命題である」/ §5.2 単位 A | **経路に対する必要性であって §0 に対する必要性ではない** | `r4-goal-statement-discrete.lean`: §0 の**肯定側・否定側の両方が実効性述語 0 個で言明できる** (55 行)。⚠ **言明は証明ではない** — 既知の散文経路 (facts `## L2 (T3)` 行 6) は実効コンパクト性を通るので、**その経路では 1–7 は要る**。⚠ 一方 **否定側 (親 plan §3 の T3-β、attack `t3-tm-to-bc-reduction`) は 1–7 を 1 本も要求しない** — 要るのは停止問題側の離散資産で、Mathlib は `ComputablePred.halting_problem_not_re` を持っている |

⚠ **C5 は「回避」ではない**。§0 の完了条件は 1 文字も動かしていない (逐語照合済: 肯定側 =
「任意の `ε > 0` に対し有界時間で `ε` 近似を返す手続きを構成し、その正当性を証明する」、
否定側 = 「そのような手続きが存在しないことを証明する」)。⚠ **1 点だけ弱い**: `Computable f`
(全域) は停止を言うが**明示的な時間上界は言わない**。facts `## L2 (T3)` が (C2) に有効収束率を
要求していないので操作的には整合するが、「有界時間」を計算量の語として読むなら R4 の言明は弱い。

---

## 4. 切り出し単位 A / B / C への影響

- **単位 A (計算可能解析の層)** — ⭐ **生存し、しかも軸 5 が最も強く支持する単位である**。
  ただし **C3 により出発点は 0 ではない** (`IsComputableENNReal` が点ごと版の先例として在り、
  0 sorry)。⚠ **強度 diff を単位 A の冒頭に書くこと** — 先例は点ごと / `ℝ≥0∞` / 位相なし、
  中核 3–7 は集合レベル / 一様 / 位相つきで**厳密に強い**。601 行はしたがって**下界ですらない**。
- **単位 B (3 レート領域層)** — ⭐ **根拠は生存し、N2 が書いたより強い**。監査は独立に
  端から端まで通しており (167 行 / 3.77 秒 / 0 sorry)、**員数は 33 def ではなく 12 def**、
  **C2 により型クラス合成の予算問題も消える**。⚠ ただし **17 スロットの転記義務は 1 ミリも縮まない** —
  圧縮したのは def の本数であって、`(18a)`–`(20c)` を正しく写す義務ではない。
- **単位 C (3 レート operational との接続)** — **員数不明のまま切り出すリスクの評価**:
  新設部分そのものの波及は 0 だが、リスクは**接続側**にある。N0 在庫は既存 2 レート資産の
  署名変更が **64 decl / 8 file** を動かすと実測しており、接続補題が既存 `bcCapacityRegion` /
  `BCAchievable` の形に合わなければ、単位 C は「新設 1 本」ではなく**既存資産の署名工事**に化ける。
  ⚠ しかも単位 C は B の後ろにあるので、**その化け方は B が着地するまで観測できない** —
  これは親 plan §4.1 軸 2 (残件の員数を prose だけで見積もる) が名指しする形そのものである。
  ⟹ N2 §5.3 の「B 着手後に N2 相当の測定をやり直す」義務は**必須**であり、
  切り出し先 plan の起票時点で**単位 C を「員数未測定」と明示的にマークすること**を勧める
  (マークしないと、後続セッションが A / B と同じ確度で見積もられた単位だと読む)。

---

## 5. 未閉のまま残したもの

- **中核 8–11 の員数** — 監査は R1 / R2 の 2 本しか再現していない (ブリーフの停止条件)。
  N2 の `isClosed_thm7RegionOfInfo` (35 行) / 合併レベルの閉性 template / 3 レート operational は
  **監査側では 1 行も測っていない**。
- **否定側 (T3-β) の形式化員数** — **誰も測っていない**。C5 の帰結として、
  §6-5 の判定「収まらない」は **Thm7 経路の員数からのみ導かれている**。
  ⚠ これは N2 の落ち度ではない (親 plan §5 の N2 行が Thm7 経路を標的として指定している) が、
  **§6-5 の判定を「§0 の Lean 側へ本 relay で到達できない」と読むなら、否定側の未測定が効く**。
- **R1 の周囲測度の束縛子** — 監査の R1 は `⋃ (hν : IsFiniteMeasure ν)` で束ねている。
  総質量は法則の第 4 節 (`ν.map X = p`) が固定するので領域は変わらないはずだが、
  **それを Lean で証明してはいない**。
- **`R₀=0` スライスの Π01 性** — N2 §1.5 が開いたままと書いた件。監査も**触れていない**。
- **N2 の probe 13 本のうち 11 本** — 再現していない。⚠ ゆえに P1a / P4–P8 / P10 / P11 / P13 の
  実測値は**いま証拠が存在しない主張のままである**。

---

**判定 1 行: 訂正あり生存 — 「収まらない」は生存する (支えているのは軸 5 の強度 diff であって
員数 41 ではない)。訂正の件数: 5 件。**
