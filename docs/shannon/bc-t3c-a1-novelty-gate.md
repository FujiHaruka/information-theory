# A1 novelty gate (N3) — 被覆数 `κ` の新規性判定

**判定 = `novel`**

**親** [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §5.3 アーク宣言 (A1) 反証条件 (1)。
判定対象は **`κ` の新規性だけ** (`E(w)` は `## M11 (T3b)` 行 4 が既に命名・凸性証明済)。既定の立場
`redundant` を成立させにいき、**成立させられなかった** — 潰れた部分は §1 / §4 に明記する。⚠ **`novel`
は「使える」ではない** — §4 は `κ` が §0 に対して仕事をしないことを 3 点で示す。再現本数 (`inert`
判定) は N5 の担当で本 gate は判定しない。⚠ 数値の SoT は `bc-facts.md`。

## 0. 定義の補修 (⚠ アーク宣言のまま採用してはならない)

**(0-a) `B(w)` は `J` 非依存ではない** — `human-judgment (primary)` / 再検証
`awk 'NR>=1034 && NR<=1084 {printf "%d| %s\n", NR, $0}' $LIT/auxrec.txt`。(18c)/(18d)/(18f)/(18g)/(18h)
の右辺に `I(W̃;J)` / `I(Ŵ;J)` / `I(X;J)` / `I(Û;J|Ŵ)` / `I(Ṽ;J|W̃)` が**逐語で入っている**
(`auxrec.txt:1041-1063`) ⟹ アーク宣言の `⋂_J ⋃_{w : J ∈ E(w)} B(w)` は**レート束の `J` 依存を
落としている**。**補修**: `E_R(w) := { μ ∈ E(w) : w のレート束が μ で R を運ぶ }` を使い
`κ(R) := min{ |S| : ⋃_{w∈S} E_R(w) = 𝒯 }` と読む。**(0-b) 補修しても凸性は生き残る** (`human-judgment`)
— `J` 依存 9 項は `μ` にアフィン (`## M11 (T3b)` 行 4、`machine`)、(18) の `min{·,·}` はアフィンの min
= 凹 ⟹ `{μ : R ≤ 凹}` は半空間の有限交 ⟹ `E_R(w)` は**閉凸**。**(0-c) 添字衝突**: `## L2 (T3)` 行 6 の
`𝒯_k` の `k` は `|J| ≤ k`、`κ ≤ k` の `k` は witness 本数。別物である。

## 1. 言明可能性テスト — `redundant` は中間段でだけ立たない (`human-judgment`)

**潰れた部分**: 台帳が現在この機構について述べている命題は**1 本残らず 2 値へ潰れる** — uniform 領域
`Thm7^unif` = `κ = 1` / `∃∀` swap が偽 (`## M12 (T3b)` 行 5) = `∃R, κ(R) ≥ 2` / `Thm7^unif ⊊ Thm7` = 同上 /
量化子の順序 `∀J ∃w` = `κ < ∞` を要求しない読み。**いずれも逆向きにも損失なく戻る**。
**潰れなかった部分**: **`κ(R) ≤ 2`** (= 中間領域 `Thm7^{≤k}`) は上記 5 語のブール結合で書けない。
⚠ 反論 1「`κ ≤ k` は witness 空間を `𝒲^k` に取り替えた `∃∀` swap にすぎない」: 成立するが**その
取り替え自体が既存語彙に無い**。⚠ 反論 2「`k` 本を混ぜて 1 本にすれば 2 値へ戻る」: **死んでいる** —
`## M11 (T3b)` 行 5 が時分割 gluing を**恒等式で**殺しており (`machine`)、混合 witness の `J := G`
残差はちょうど `(1−μ)·r(w_K,G)` で `μ = 1` 以外 0 にならない ⟹ 併合による潰しは不可。⟹ 通過。
⚠ **通過幅は中間段 1 本ぶんしかない**。

## 2. in-repo 先行資産 — 該当なし (`human-judgment (primary)`)

再検証 `rg -c "被覆|有限被覆|finite subcover|有限個の witness|witness の族|selection|選択関数|取り替え|有限族" docs/shannon/bc-*.md`
(名前でなく**概念**で当て、ヒット全行を目視)。`bc-facts.md` の `被覆` は `## L16 / L17` の**単体 cell
分割証明書**と `## L2 (T3)` の**レート領域の有限有理被覆の枚挙**の 2 用途のみ。`取り替え` の全ヒットは
`## M9 (T3b)` 行 4 の **`J` の取り替え** (witness 側ではない)。`bc-t3-cardinality-inventory.md` の
有限性はすべて **Carathéodory 型 = witness 1 本のアルファベット径** ⟹ **本数の不変量は 1 本も無い**。

## 3. 文献の先行 — 機構は名指しされているが不変量は無い (`human-judgment (primary)`)

取得 `SCRATCH=<scratchpad> docs/shannon/lit-fetch.sh <out> auxrec probc GK-outer li21` / 再検証
`grep -in "covering\|finite family\|selection\|same auxiliary\|finitely many\|Helly" $LIT/{auxrec,GK-outer,probc,li21}.txt`
⟹ **4 本とも 0 ヒット** (`Carath` の 3 ヒットは全部 witness のアルファベット径 = `auxrec.txt:198,1169,1473`)。

- ⭐ **機構そのものは一次文献が明示している** — `auxrec.txt:334-338` 逐語: "the joint distribution
  induced on (X, Xr) **does not depend on** the choice of the auxiliary receiver J; ... However, the
  conditional distribution pW|X,Xr **does depend on** our choice of the auxiliary receiver J ...
  Therefore, we conclude that **there exists some pX,Xr such that for every auxiliary receiver J there
  exists some pW|X,Xr**" ⟹ 文献の語彙は**量化子の順序ちょうど**で、「何本で足りるか」は問われていない。
- ⭐ **`κ = 1` の推論は使われているが名前が無い** — `probc.txt:1442` 逐語: "Since the choice of the
  auxiliaries **do not depend on λ**, we conclude that" ⟹ 同一不変量は**無い** (別名でも無い) が、
  `κ = 1` 側は一様性のてことして既に稼働中。

## 4. ⭐ 仕事をするかテスト — **量化子の位置は別。だが §0 への payoff は 3 点とも取れない**

**(4-a) `## M12 (T3b)` 行 7 と同じ問いか = 別の問い** (`human-judgment`、⚠ **初版は「行 8」としていたが誤り** —
行 8 は頑健性 `(18i) ≤ 2C + I(X;J₁)` の行で、M12-b の 3 分類は行 7 である)。関係 `Φ ⊆ 𝒲_R × 𝒯`
(「`w` が `J = μ` で適格かつ `R` を運ぶ」) で量化子を並べる — **M12-b は 𝒯 側の有限化** (固定 `w` で
`∀μ ∈ 𝒯 Φ(w,μ)` が**有限個の `μ_1..μ_N`** の `⋀_i Φ(w,μ_i)` に落ちるか。判定 = 落ちない: (b) は関数
恒等式、(c) は凹包)、**`κ` は 𝒲 側の有限化** (`∀μ ∃w Φ(w,μ)` が**有限個の `w_1..w_k`** の
`∃w_1..w_k ∀μ ⋁_i Φ(w_i,μ)` に落ちるか)。⟹ **「有限個の条件」と「有限個の witness」は同じでない —
同一関係の転置した両側**で、M12-b の NO は 𝒯 側の言明ゆえ 𝒲 側には何も言わない。⟹ 読みは正しい。

**(4-b) ⚠⚠ 取り消し — 下の Baire 論証は否定された**。[`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) §2 (S1) が
**`𝒯` は Baire 空間ではない**ことを認めた (有限台の読みでは `𝒯 = ⋃_n {|supp| ≤ n}` = 自分自身の中で
痩せた閉集合の可算和) ⟹ **「疎閉集合の有限和は覆えない」は使えない**。⟹ 根拠は同書 §1 の
**(D2) 線分補題へ差し替え済** (最小性 + アフィン性のみ、有限的)。**結論 (有限被覆は (19)-uniform witness に
限る = (K1)) は差し替え後も生きている**が、⚠ **以下の段落を生きた論証として拾わないこと** (記録として残す):

> **ただし両側は独立でない — 有限被覆は M12-b(b) を強制する** (`human-judgment`、導出)。
> (19a)–(19c) は**等式**制約 (`auxrec.txt:1076-1078` 逐語) で `μ` にアフィン。ある `w` の (19) 残差が
> `𝒯` 上恒等的に 0 でなければ `E_R(w)` は真の閉アフィン超平面に含まれ **`𝒯` で疎**。
> `𝒯` はコンパクト距離凸集合 ⟹ Baire ⟹ **疎閉集合の有限和は `𝒯` を覆えない** ← ⚠ **この 1 段が偽**
> ⟹ **`κ(R) < ∞` なら被覆族は (19) を全 `J` で満たす witness だけで構成できる** (1 次元版は台帳に既在 =
> `## M11 (T3b)` 行 1(iv)「(19) 適格 `λ` 集合は全区間か高々 1 点」`machine`) ⟹ **有限被覆が回避できるのは
> (a)(c) だけで (b) は強制される**。

⚠ 「`E_R(w)` が真の閉アフィン超平面に含まれるなら疎」という**部分主張自体は正しい** (同書 §2 (S1))。
落ちたのは `𝒯` の Baire 性だけである。

**(4-c) 有限パラメータ記述は出ない** ((4-b) の帰結)。`κ ≤ k` + 基数上界 (`auxrec.txt:1083-1084`) が
与えるのは **witness 側の有限パラメータ化だけ**で、残る `∀μ ∈ 𝒯` は M12-b(b) の関数恒等式 + (c) の凹包
条件を**そのまま抱える** (`k` 本の選言は 1 本より有限的にならない)。

**(4-d) `## L2 (T3)` 行 6 との強度差 = `κ` の有界性が厳密に強い** (`human-judgment`)。`κ ≤ k` ⟹
`∃(𝒲^k コンパクト) ∀(𝒯 overt)` の Π01 形 ⟹ 同じ (L-∃)/(L-∀) で effective compactness。逆は取れない —
**effective compactness は `## L2 (T3)` 行 6 で既に無条件の定理**で、そこから `κ` の有界性が出るなら
後者も定理になる (未知)。⟹ **厳密に強い** ⟹ ⚠ **計算可能性側の主語は既に無条件で埋まっており
`κ` は 1 mm も足さない**。(C2) の残りは `Thm7 = C` の厳密一致だけで、`κ` はそこに何も言わない。

**(4-e) 濾過の向きが (C2) と逆**。`Thm7^{≤1} ⊆ Thm7^{≤2} ⊆ …` は `k` について**増加**ゆえ
`⋂_k Thm7^{≤k} = Thm7^{unif}`。`## M12 (T3b)` 行 5 より後者は `SR_C` を運ばない ⟹ **`## L2 (T3)`
行 5 が要求する「`C` を共通部分にもつ可算族」にならない**。

## 5. 退化テスト — 構造の違う 2 例は取れる。**`κ = 2` の実現は未計算**

⚠ **本節は N3 時点の記録である。⭐ N4 が決着させた** — [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md):
[probc] の **`SR_C` 点では `κ = ∞`** (⟹ そこでは `κ = 2` は実現しない)。⚠ **ただし訂正 3 により
`{1,∞}` への二値化そのものは未確立** — 測ったレート点が `Thm7^unif` の点と `SR_C` 点の 2 つだけである
([`bc-t3c-a1-kappa2-audit.md`](bc-t3c-a1-kappa2-audit.md) §6 訂正 3)。**以下は書き換えず記録として残す**。

- **(a) `κ(R) = 1`**: `Thm7^unif` の点 (定義)。到達 witness の実在は `## M12 (T3b)` 行 4 (`machine`)。
- **(b) `κ(R) ≥ 2`**: [probc] インスタンスの `SR_C` の点。`## M12 (T3b)` **行 8** (頑健性) より `J₁` と
  `J = X` の 2 点で適格な witness は `(18i) ≤ 2C + I(X;J₁)` ⟹ `SR_C` を運び `X` を覆う witness は
  `E_R(w) ⊆ {I(X;J) ≥ d*}` となり `J = const` を覆えない ⟹ 別 witness が要る。⚠ **`2C` / `d*` / `SR_C`
  の値は `## M12 (T3b)` **行 9** が SoT。本文書では再計算していない**
  (⚠ **本 gate 初版は頑健性を行 9 に、値の SoT を行 6・行 10 に帰属させていた — どちらも誤り**。
  正しくは頑健性 = 行 8 / 値 = 行 9 で、`bc-facts.md` の表を機械で数え直して確定した)。**(a)(b) の構造は違う** —
  (a) は「(20) が全 `J` で緩い」型、(b) は「(19) 一様だが (20) が片側で破れる」型。
- ⚠⚠ **`κ = 2` が実現するかは未計算**。(4-b) の結論 (⚠ 根拠は取り消された Baire ではなく
  [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) §1 (D2) / (K1)) より被覆族は (19) 一様 witness に限られ、そこでの
  `E_R(w)` は有限本の半空間の交わり ⟹ `𝒯 ⊆ H₁ ∪ H₂` は「全制約対 `(j₁,j₂)` で 2 つの開半空間が `𝒯`
  上で交わらない」= **反平行条件**と同値。実例は `## M11 (T3b)` 行 4 の系 (anti-parallel law) が
  **別配置で**持つが 2 witness 版は未計算 ⟹ **`κ ∈ {1, ∞}` は排除できていない**。排除できなければ
  `κ` は 2 値の言い換えへ潰れ N5 で `inert` ⟹ **A1 の単一の生死判定点であり N4 の標的**である
  (⚠ §4.5 により数値掃引の非違反は証拠にならない — 要るのは恒等式への帰着か閉形の手構成)。
  ⚠ **N4 が決着させた** ([`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md)): **`SR_C` 点では `κ = ∞`**
  (⚠ **根拠は恒等式であって掃引ではない**) ⟹ そこでは `κ = 2` は実現しない。⚠⚠ **ただし訂正 3 により
  `{1,∞}` への二値化そのものは未確立** — 測ったレート点が `Thm7^unif` の点と `SR_C` 点の 2 つだけ
  だからである ([`bc-t3c-a1-kappa2-audit.md`](bc-t3c-a1-kappa2-audit.md) §6 訂正 3) ⟹ 上の
  「排除できていない」が解消したのは **`SR_C` 点についてのみ**で、`inert` 条件そのものは開いている。
  ⚠ **一般の `κ = 2` (= 反平行条件の代数) も未決のまま**である ((K1) はこれを潰さない)。

## 6. 再現の標的 (⚠ 「再現できた」ではない。N5 の再現本数テストの入力)

1. **`## M12 (T3b)` 行 5** (`∃∀` swap は偽、和レート差 `d*`) + **行 8** (頑健性 `(18i) ≤ 2C + I(X;J₁)`)
   → `κ(SR_C の点) ≥ 2` を値レベルで再現 (§5(b) の下界導出)。⚠ **初版は「行 9」と書いていたが誤り** —
   行 9 は `C` / `2C` / `d*` / `SR_C` の数値の逐語確認の行である。
2. **`## M11 (T3b)` 行 5** (時分割 gluing は恒等式で死亡) → 「`κ` の `⋃` は単一 witness へ併合できない」。
3. **`## M12 (T3b)` 行 7** (`∀J` の 3 分類) → (4-b) の「有限被覆は (b) を強制し (a)(c) だけ回避」で
   分類を**有限被覆側から**再導出。⚠ **初版は「行 8」としていたが誤り** — 行 8 は頑健性の行である。
4. **`## M11 (T3b)` 行 4** (`E(w)` 凸 / anti-parallel law) → `κ` の well-defined 性と反平行条件。
5. **`## M4 (T3b)`** (`∀T_J` は `∀T_{J|X}` へ両向きで潰れる) → `𝒯` の定義域確定 = `κ` の被覆対象の同定。

## 7. 併記 — アーク宣言の要修正点 2 件 (⚠ 本 gate は既存ファイルを編集していない)

- **接続 4 本目 `## M15 (T3b)` の帰属が誤り**。M15 の「点ごと witness 移送 (恒等写像)」の**点はレート点**
  で、移送先は **Thm8 の witness** (S9 = `Thm7 ⊆ Thm8` を付ける方法) ⟹ `J ↦ w` の**定数 selection の
  死ではない**。⚠ ただし M15 は `J := Y₁` / `J := Z₂` の**2 点で適格かつ `SR_C` を運ぶ明示 witness** を
  持つので `κ(SR_C)` の**上界側の入力**としては生きる。⚠ 接続は残り 3 本 (`## M4` / `## M11` / `## M12`) で §5.3 の閾値 3 本は維持される。
- **`B(w)` の `J` 依存** (§0-a) — 補修版 `E_R(w)` を plan 側へ反映すること。
