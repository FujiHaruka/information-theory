# N10 敵対的独立監査 — 判定 = **訂正あり生存** (主判定 `ε = 0` は落ちず、訂正 8 件のうち 5 件は N10 が自分を**過小**に書いていた側)

> **Parent**: [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §4.6 (敵対的独立監査) / §3.1 (禁止される書き方)。
> **対象**: [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md) (commit `490ce557`) / 被監査検証器 = `verifiers/n10_epsilon_zero_probc.py`。
> **監査側の検証器** = [`verifiers/n10_audit_probc.py`](verifiers/n10_audit_probc.py) (**34/34**、約 46 秒。テスト番号 `B1`–`B34`)。
> ⚠⚠ **被監査検証器は 1 行も実行・import・複製していない** (読んだだけである)。`shoulder_certificate_probc.py` /
> `n7_audit_probc.py` / `n9_*.py` / `capacity_probc.py` も 1 本も実行していない。チャネル行列・相互情報量・繊維の座標・
> `δ` / `ψ` / `d*` はすべてインスタンス定義からゼロで書き直した。⚠ **既定の立場を「N10 の判定は偽」に置いた**。
> ⚠ **監査者は N10 の実装に関与していない**。

---

## 0. 総合判定 — **訂正あり生存**

**主判定は生存した。** [probc] 上で

```
∀ t ∈ [0,1], ∀ (b,A0,A1) ∈ [0,1]³ :  T_t := G_t + d*_t − ψ_t(b) ≥ 0     ⟹ Ω(t) = d*_t ⟹ ε = 0
```

は、**破りにいった 39 系統のうち 29 系統が潰せなかった**。核の 2 段 (§2.2 の恒等式・§2.3 の分解) は監査側で
**生の同時分布から**独立に再導出でき (40 桁で残差 `1.5e-40`、`B2`–`B7`)、さらに**浮動小数を 1 つも使わない
記号代数** (`Q[C,t]` 上の厳密算術、`B12` / `B13`) でも恒等式として閉じた。逆向き `Ω(t) ≥ d*_t` も生の同時分布から
再現した (`B25`、残差 `8.4e-41`)。⚠ **`ε = 2.0786e-07` そのものは本監査も再導出していない** (§12-5)。

**⚠ ただし訂正 8 件のうち 5 件は N10 が自分を過小に書いていた側である**。とくに:

- **§2.4 は不等式ではなく恒等式である** — `σ_x := d*_t − ψ_t(x) ≥ 0` と書くと
  `T_t = C(1−t)h(a) + t·S_A + e·σ_b + C·E_{X1}[σ_{B}]` が**厳密に成り立つ** (`B13`、記号代数)。
  ⟹ 標的は「非負の 4 項の和」であり、§2.6 の等号集合は「4 項が同時に消える点」として即座に出る。
- **§2.5 の `δ ≥ 0` は float を 1 つも要さない** — 「符号変化はちょうど 1 回」は**全 `p` について構造的**であり
  (`B18`)、`δ(x0) > 0` は**測る量ではなく凸弧から導かれる量**である (`B19`) ⟹ **§3.4 / §5-7 が名指しした
  「丸めが効きうる唯一の箇所」は消せる**。監査は区間演算でも `[0.01,0.45]` を厳密に閉じた (`B24`)。
- **効いている条件は 1 本だけである** — `e + C = 1` は BEC の消失/非消失確率の**定義** (`C := 1−e`) であって
  条件ではない (`B15`) ⟹ 実質の入力は `δ ≥ 0`、すなわち `e ≤ h(p)` の 1 本 ⟹ **標的は `e = h(p)` の曲線ではなく
  半空間 `e ≤ h(p)` の全体で真である** (`B32` / `B33`)。これは §4.1 が「公算が高い」と書いた事柄の格上げであり、
  同時に §4.1 / §4.2-1 の「`e ≠ h(p)` では Theorem 3 が適用できない」を**片側で覆す**。

**⚠⚠ 主判定 (`ε = 0`) を動かす所見は 0 件である。**

⚠ **本監査でも `Thm7 ⊋ C` の材料は 1 つも出ていない** / ⚠ **`Thm7 ⊄ Thm8` は主張していない / `R ∈ Thm7` とも書いていない** /
⚠ **レート領域の包含について本監査は新しいことを何も言っていない** / ⚠ **決着は [probc] という 1 インスタンス
(と、監査が広げた `e ≤ h(p)` の族) の上の話である**。

---

## 1. 標的 1 — §2.3 の恒等式の 3 段 (⚠ ここが落ちれば判定ごと落ちる) — **試行 7 系統、7 系統とも落ちた**

### 破りに振った試行

1. 連鎖則 `I(X;Y) = I(X2;Y2) + I(X1;Y1|Y2)` を、閉形を一切使わず **6 変数 `(X1,X2,Y1,Y2,Z1,Z2)` の同時分布**から
   数え直す (`B2`)。
2. **BEC の厳密性** `I(X1;Y1|Y2) = C·H(X1|Y2)` を生の同時分布と突き合わせる (`B3`)。
3. `I(X1;Z1|Z2) = e·J(a) + C·E_{X2}[J(A)]` を同じく生から突き合わせる (`B3`)。
4. ⚠ **`Y2` と `Z2` の役割が入れ替わっていないか** — 入れ替えた読み `G'_t := I(X1;Y1|Z2) − t·I(X1;Z1|Y2)` を
   組んで、繊維の恒等式が**壊れる**ことを確かめる (`B4`)。
5. 繊維の恒等式 `t·I(X;Z) − I(X;Y) = ψ_t(b) − G_t` を生の同時分布から (`B5`)。
6. §2.3 の分解を、左辺を**生の相互情報量**にしたまま突き合わせる (`B7`)。
7. 3 段全体を**浮動小数なしの記号代数**で再導出する (`B12`)。

### 結果 — **3 段はすべて生存。しかも記号レベルで完全に閉じる**

生の同時分布からの残差は最大 `1.492e-40` (40 桁演算、`B2`–`B7`)。役割入替の対照 (`B4`) は最大 `0.0175` ずれる
⟹ **`Y2` / `Z2` の割り当ては load-bearing であり、N10 の割り当てが正しいものである**。

⭐ **記号代数 (`B12`)**: 原子を `h(a), h(b), I2, δ(a), δ(b), E_{X2}[δ(A)], E_{X1}[δ(B)], d*_t` に取り、使う関係を
**`J = C·h − δ` (δ の定義) と `I(X1;X2)` の 2 通りの読み方**の 2 本だけに限ると、§2.3 の分解も
`I(X1;Z1|Z2) = C·h(a) − C²·I2 − S_A` も **`Q[C,t]` の恒等式として厳密に 0** になる。
⟹ **この段に浮動小数は 1 つも要らない**。

⚠ **`I(X1;X2)` の 2 通りの読み方** (`h(a) − E_{X2}[h(A)]` と `h(b) − E_{X1}[h(B)]`) を関係として使う点は
N10 の散文が明示していないが、相互情報量の対称性そのものであり追加の仮定ではない。

---

## 2. 標的 2 — §2.2 の恒等式と `B_i` の向き — **試行 2 系統、2 系統とも落ちた**

`I(X1;Y2) − C·I(X1;X2) = E_{X1}[δ(B)] − δ(b)` を生の同時分布から確認 (残差 `6.058e-41`、`B6`)。
**向きの取り違え**を疑って `X2` で条件付けた読み (`E_{X2}[δ(A)] − δ(a)`) を対照に置くと最大 `0.0327` ずれる
⟹ **`B_i := P(X2=1|X1=i)` の向きは load-bearing であり、N10 の向きが正しい**。

⭐ **導出は 1 行である**: `h(x*p) = C·h(x) + h(p) − δ(x)` は `δ` の定義の書き換えにすぎず、これを
`x = b` と `x = B_i` に当てて引くと恒等式が出る。⟹ **強データ処理不等式の定数は本当に使っていない**
(N10 §2.2 の注記は正しい)。

---

## 3. 標的 3 — 繊維の座標 `(b,A0,A1)` の全射性と退化繊維 — **試行 3 系統、3 系統とも落ちた**

1. **全射性**: 有理数の厳密算術で 4000 個の法則を `(b,A0,A1)` へ落として復元 — **失敗 0** (`B8`)
   ⟹ 立方体 `[0,1]³` は `Δ₃` を**過不足なく**覆い、`max` の取りこぼしも取りすぎも無い。
2. **退化繊維** (`b ∈ {0,1}`、`a ∈ {0,1}`、`A0 = A1`、`X1`/`X2` 決定的の 8 隅): 閉形と生の同時分布の差は
   `6.888e-41`、どこでも `T ≥ 0` (`B8`)。⚠ **`b = 0` で `A1` が未定義になる箇所は重み `b·J(A1)` が 0 で消え、
   `a ∈ {0,1}` で `B_i` が未定義になる箇所も重み `a` / `1−a` が 0 で消える** ⟹ 連続に延びる。
3. **座標を捨てる**: `Δ₃` 上に Dirichlet で直接引いた 800 個の法則で `d*_t` を超えるものを探す — 最小余裕
   `1.512e-03` (`B9`、screen)。

⟹ **N10 §1.2 / 検証器の繊維パラメータ化に取りこぼしは無い**。⚠ **退化点で `max` が落ちる**という
ブリーフの疑いは発火しなかった。

---

## 4. 標的 4 — §2.4 の不等式の段 — **試行 6 系統、5 系統が落ち、1 系統が成立 (訂正 6 = 上方修正)**

### 破りに振った試行

1. **重みが本当に確率重みか** (`e`, `C(1−a)`, `C·a` が非負で和 1 か、`t` 依存を落としていないか) — `B15`。
2. 畳み直し `e·h(b) + C·H(X2|X1) = h(b) − C·I2` が本当に恒等式か — `B15`。
3. ⚠ **`t ∈ (0,1)` の内部で `C(1−t)h(a)` と `t·S_A` を同時に 0 へ寄せて下界を負に落とす** (ブリーフ系統 7) — `B11`。
4. **有理数の明示構成による反例** — 分母 10 以下の全繊維 × 4 個の `t`、計 5324 点を 40 桁で総当たり — `B10`。
5. float64 の局所降下 100 系統 (screen) — `B34`。
6. 記号代数で `σ` を入れた形を再導出 — `B13`。

### ⭐ **訂正 6 (成立・上方修正)** — §2.4 は不等式ではなく**恒等式**である

`σ_x := d*_t − ψ_t(x)` (`d*` の定義から `≥ 0`) を入れると、記号代数で

```
T_t = C(1−t)·h(a) + t·S_A + e·σ_b + C·[ (1−a)·σ_{B0} + a·σ_{B1} ]        (Q[C,t] の恒等式)
```

が**厳密に**出る (`B13`、浮動小数を 1 つも使わない。生の同時分布との突き合わせは `B14`、残差 `1.248e-40`)。
N10 §2.4 は「`ψ_t ≤ d*_t` を `S_B` の 2 か所へ当てて代入すると `d*_t` と `h(b) − C·I2` が相殺し `T_t ≥ …`」と
**不等式の連鎖**として書くが、実際には**捨てた分がそのまま `σ` の項として残る等式**である。効き目は 3 つ:

- **ブリーフ系統 7 の攻撃 (両項を同時に小さくする) は構造的に不可能である** — 4 項がすべて非負の**和**なので、
  どれを小さくしても負にはならない (`B11` で実測、最小 `0.00e+00`)。
- **§2.6 の等号集合が導出になる** — 零点は「4 項が同時に消える点」であり、場合分けを機械に頼らず書ける。
- **`D4` / `D5` / `D6` の格子確認は判定には要らない** — N10 §3.4 は既にそう書いているが、恒等式の形にすると
  **要らなさが式の上で見える**。

⚠ **判定は動かない** (N10 の結論と同じ)。⚠ **これは N10 が過小に書いていた側の訂正である**。

### 落ちた 5 系統

重みは非負で和 1 (`e + C = 1` による。⚠ **これは定義であって条件ではない** — §7 訂正 4)、畳み直しの残差
`1.148e-41`、有理数 5324 点の最小 `T = −3.4e-42` (40 桁のノイズ)、局所降下の最良 `−2.7e-16`。
⟹ **反例は 1 つも出なかった**。

---

## 5. 標的 5 — §2.5 の `δ ≥ 0` (唯一の解析的入力) — **試行 8 系統、5 系統が落ち、3 系統が成立 (訂正 5)**

### 破りに振った試行

1. `sign δ''(x) = sign[e(1−2p)²x(1−x) − C·p(1−p)]` を独立に導出し、40 桁の中心差分と突き合わせる (`B17`)。
2. ⚠ **「`(0,1/2)` 上で符号変化はちょうど 1 回」は float の主張か** (`B18`)。
3. **符号変化が 2 回ある `p` を探す** (`B20`、119 個の `p`)。
4. ⚠ **`x0` が区間の端に寄る `p` で論法が壊れないか** (`B20` の `N(1/2)` の最小値)。
5. `δ(0) = 0`, `δ(1/2) = 0`, `δ'(1/2) = 0`, `1/2` 対称性を独立に確認 (`B18`)。
6. **`δ(x0) > 0` は本当に必要な入力か** (`B19`)。
7. **区間演算で取り直す** (`B24`)。
8. **文献の閾値と食い違わないか** (`B21` + 一次資料検索)。

### ⚠⚠ **訂正 5 (成立・上方修正)** — この証明は浮動小数を 1 つも要さない

N10 §2.5 は「符号変化はちょうど 1 回」を機械で確かめ、§3.4 / §5-7 で
「**区間演算はしていない / 丸めが効きうるのは §2.5 の 1 変数の符号だけである**」と自己限定している。**これは過大な自己限定である**:

- **(a) 符号変化 ≤ 1 回は全 `p` について構造的である** — `N(x) := e(1−2p)²x(1−x) − C·p(1−p)` は
  `N'(x) = e(1−2p)²(1−2x) > 0` ゆえ `(0,1/2)` 上で**狭義単調増加**である ⟹ 符号変化は**高々 1 回**。
  ⟹ **2 回は原理的に起きない** (`B18` で 49 個の有理 `p` を厳密算術で、`B20` で 119 個の `p` を実測。
  最大変化回数 1)。⚠ **N10 が「float の主張」と書いた行は、実際には浮動小数に依存しない**。
- **(b) `δ(x0) > 0` は測る量ではなく導かれる量である** — 凸弧 `[x0,1/2]` では `δ' ≤ δ'(1/2) = 0` ゆえ
  `δ` は非増加 ⟹ `δ(x0) ≥ δ(1/2) = 0`。⟹ **凹弧の弦論法に必要な `δ(x0) ≥ 0` は凸弧側から無料で出る**
  (`B19`、`[x0,1/2]` 上の `δ'` の最大は `−4.74e-04 ≤ 0`)。
- **(c) 符号変化が 0 回の場合も同じ結論になる** — その場合 `δ` は `[0,1/2]` 全体で凹で両端が `≥ 0` ゆえ弦の上。
  ⟹ **`x0` が端に寄る `p` でも論法は壊れない** (ブリーフ系統 10 の疑いは発火しない)。
  ⚠ 実測では全 `p` で `N(1/2) > 0` (最小 `4.838e-06` at `p = 0.49583`) なので変化回数は常に 1 だが、
  **証明はその事実を要求しない**。

⟹ **`δ ≥ 0` は `p ∈ (0,1/2)` と `e ≤ h(p)` の全体で、浮動小数なしに閉じる**。
⭐ **補強**: 区間演算 (中心形の厳密包含) で `[0.01,0.45]` 上の下界 `+7.842e-04 > 0` を 800 箱で証明した (`B24`)。
両端の帯 `[0,0.01]` / `[0.45,0.5]` は上の構造論法で覆われるので**数値を要しない**。

⚠ **判定は動かない** — 動くのは「判定がどこまで浮動小数から切り離せるか」の帳簿である
(N10 §3.4 の最終行と §5-7 は書き換えるべきである)。

### 文献との照合 (系統 8) — **食い違いなし**

`δ ≥ 0` ⟺ **BEC(e) が BSC(p) より more capable** ⟺ `e ≤ h(p)` は既知の閾値である
(同一容量の二元入力対称チャネルの中で BSC が最も capable でなく BEC が最も capable、という標準的な順序の系)。
監査は両向きを独立に確認した (`B21`): **必要性は `δ(1/2) = h(p) − e` から**、**十分性は
`δ_e = δ_{h(p)} + (h(p)−e)·h ≥ 0` から**。⟹ N10 の「文献を引かずに自前で閉じた」は正しく、かつ既知の閾値と一致する。
⚠ **一次文献の逐語照合はしていない** (§12-1)。

---

## 6. 標的 6 — 連鎖と射程 (honesty) — **試行 7 系統、4 系統が落ち、3 系統が成立 (訂正 1・7・8)**

### ⚠⚠ **訂正 1 (成立)** — §1.4 / §3.1-2 の「N7-p の『同値』は成り立たない」は**論理として誤り**

N10 は「facts N7-p が『同値』と書いた 3 変数不等式 `G_1 ≥ ψ_1(b) − d*_1` は**必要条件どまり**」と書き、
§3.1-2 で「N7-p が書いた『同値』は成り立たない」と結論する。**しかし N10 自身が 4 変数の主張を証明している**以上、
その `t = 1` の場合である 3 変数不等式も**真**であり (監査も独立に確認: `B28`、`40³` 繊維で最小 `1.093e-06`)、
**真な命題どうしは同値である** ⟹ **「同値は成り立たない」は N10 自身の結果と矛盾する**。

⭐ **正しい言い方**: 落ちたのは**同値ではなく還元の路** — `G_t ≥ G_1` で `t` を落とす論法は
`max_t [ψ_t(b) − d*_t] − [ψ_1(b) − d*_1]` の分だけ緩く、その最大は `d*_1` に等しい (`B29` で再現、
達成は `b = 0`, `t = 0` の**厳密な点**である。⚠ N10 が「格子上は `t = 0.00125`」と書く点は格子の見え方であって
真の達成点は `t = 0`)。⟹ **書くべきは「`G_t ≥ G_1` による `t` 消去では 4 変数の主張は出ない」**であって
「同値でない」ではない。

⚠ **判定は動かない**。⚠ ただし **§0 見立て 1 の「当たった」の帳簿は半分だけ正しい** — 「真の標的は 4 変数である」
(= 還元路が使えない) は当たりで leg の設計を支えたが、「必要条件どまり」の部分は誤りである
⟹ **親 plan §4.4 の較正台帳へは「当たり、ただし表現に誤り 1 か所」として入れるべきである**。

### ⚠ **訂正 7 (成立)** — §1.3 の「閉じる恒等式」の**証拠の型**が主張と合っていない

N10 §1.3 は「`H(Y)_一様 − F*(t) − t·H(Z|X) = 2C + d*_t` を残差 `6.661e-16` (`E3`) で確認した」と書く。
被監査検証器のソースを読むと、`F*(t)` は `min_s f_t` として**計算されておらず** `(1−t)·H(Y|X) − d*_t` が
**代入されている** ⟹ その残差が測っているのは `C = 1 − h(p)` の算術だけである (同じ量を `w3` が別に測っている)。
⟹ **`E3` の後半は恒等式の確認ではなくトートロジーである**。

⭐ **監査が埋めた**: 生の `f_t = H(Y)_s − t·H(Z)_s` を 605 個の法則上で**直接最小化**し、
`min_s f_t = (1−t)·H(Y|X) − d*_t` を残差 `1.722e-41` で確認した (`B26`)。**主張自体は真である**。
⚠ **判定は動かない** (N9 監査の訂正 4 と同型の「証拠の型」の訂正である)。

### ⚠ **訂正 8 (成立)** — §3.1 の「レート領域の包含について何も言っていない」は同節の他の行と緊張する

§3.1 は `h_Thm7(0,1,t) = h_C(0,1,t) = 2C + d*_t` を出したうえで、閉じた台帳の 3 本目として
N9 の連鎖 (`ε = 0 ⟹ Thm7 = C`) を引用し、しかし末尾で「**レート領域の包含について新しいことは何も言っていない**」と書く。
**leg 冒頭宣言が「[probc] 上で `Thm7 = C` が厳密に立つのか…を確定させる」と書いていること**とも読み合わせると、
読者は矛盾を読む。⚠ **§5-1 の書き方 (「本 leg が閉じたのは `Ω(t) = d*_t` まで、そこから先は N9 の連鎖の引用」) が
正しく、§3.1 もその形に揃えるべきである**。⟹ **文言の訂正であり、判定も射程も動かない**。

### 落ちた 4 系統

- **方向の被覆** (`B16`): `R0 = 0` スライスの支持関数の優越は `λ ≥ 0` の全方向で要るが、`max(λ1,λ2) = 1` に
  正規化すると `(1,t)` か `(t,1)`、`t ∈ [0,1]` に落ちる ⟹ **N10 の量化子 `∀t ∈ [0,1]` (端点込み) で
  N9 の連鎖が要求する全方向が尽きる**。20000 方向で外れ 0。⚠ **N7-b の射程 (`0 ≤ t ≤ 1` でのみ LP の読みが立つ)
  と N10 の使い方は一致する** — ブリーフが疑った「射程のずれ」は発火しなかった。
- **片側性** (§1.3): 「`Ω ≤ d*` ⟹ `ε = 0`」しか使わないという N10 の断りは正しい。逆向きが従わないことも正しい。
- **隠れた数値入力** (`B23`): `β*_1` / `x0` / `0.0387…` を再計算したが、**連鎖のどの段にも入っていない**
  (使うのは「`d*_t` は `ψ_t` の最大値である」という定義だけ) ⟹ **N10 §2.4 の注記は正しい**。
  ⭐ **副産物**: ブリーフが渡した `h(β*_1) = 0.39331` は誤りで、N10 の値
  `h(β*_1) = 0.39391442644035` (`β*_1 = 0.07766967015180`) が正しい (`B23`、40 桁)。
- **台帳 3 本が閉じるか** (§3.1): (1) N7-g (1) は `min_s f_t = F*(t)` が厳密に立ち `argmin` も
  特徴づけられたので閉じる (`B26` / `B30`)。(2) N7-p の 3 変数不等式は真 (`B28`)。
  (3) `C ⊆ D_β` は `K_β ⊆ K ⊆ Thm7|_{R0=0} ⊆ K_β` の挟み込みで閉じるが、**両端は N6-p と N7-q (iv) の引用**である
  ⟹ **N10 が「N9 の連鎖の引用である」と断っているとおり**であり、過大主張ではない。

---

## 7. 標的 7 — class, not instance (§3.3 の負の対照) — **試行 6 系統、3 系統が落ち、3 系統が成立 (訂正 2・3・4)**

### ⚠⚠ **訂正 4 (成立・上方修正)** — 「効いているのは `e = h(p)` と `e + C = 1` の 2 本」ではなく 1 本である

§4.1 は「閉じた理由は `p = 0.1` ではなく **`e = h(p)` と `e + C = 1`** の 2 本だけであった」と書く。
しかし **`C := 1 − e` は BEC の非消失確率であり、`e + C = 1` は定義である** — `e` をどう動かしても成り立つ
(`B15` / `B32`)。⟹ **実質の入力は `δ ≥ 0`、すなわち `e ≤ h(p)` の 1 本だけ**である。

⭐ **帰結 (射程の格上げ)**: §2.2–§2.4 の連鎖は `δ ≥ 0` の下で恒等式として閉じ、`δ ≥ 0` は
**`p ∈ (0,1/2)` かつ `e ≤ h(p)` の全体で構造的に成り立つ** (§5 訂正 5) ⟹
**`Ω(t) = d*_t` は 2 パラメータの半空間 `{(p,e) : e ≤ h(p)}` の全体で成立する** — 曲線 `e = h(p)` の上だけではない。
⟹ §4.1 が「`p` を動かした族は同じ理由で空である**公算が高い**」と書いた事柄は、**公算ではなく証明**になる
(⚠ ただし `Ω(t) = d*_t` の水準まで。`C` 側の閉形の再計算は監査もしていない — §12-3)。

### ⚠ **訂正 2 (成立)** — 「`e < h(p)`: 標的は**余裕つき**で成立する」は誤り

§3.3-2 は `e < h(p)` 側について「`δ(1/2) = h(p) − e > 0` と厳密に正になり、**標的は余裕つきで成立する**」と書く。
**標的 `T_t ≥ 0` は `e ≤ h(p)` のどこでも等号を持つ** — `X1` 決定的な法則 (`A0 = A1 = 0`) で `b = β*_t` を取ると
`T_t = 0` になり、これは `e` に依らない (`B32`、4 個の離調で `T = +0.00e+00`)。
⟹ **余裕が付くのは `δ` の内点の値であって標的そのものではない**。被監査検証器の `G2` が印字する
`min_t [d*_t − Ω]` も (格子の分解能の内側で) 0 であって正の余裕ではない。

⚠ **判定は動かない** — むしろ「`e = h(p)` が境界そのものである」という §3.3-2 の結論は保たれる
(標的が**偽になり始める**のがちょうど `e = h(p)` である)。動くのは境界の**両側の描写**である:
正しくは「`e ≤ h(p)` では等号つきで成立 / `e > h(p)` では偽」であって、「下は余裕つき・上は偽」ではない。

### ⚠⚠ **訂正 3 (成立)** — 「`e ≠ h(p)` では [probc] Theorem 3 が適用できない」は片側で偽

§4.1 末尾と §4.2 の反証条件 1 は「**`e ≠ h(p)` では [probc] Theorem 3 が適用できない (more capable が両ブロックで
壊れる)**」と書く。**`e < h(p)` 側では more capable は壊れるどころか強まる** — 両ブロックとも
`δ_e = δ_{h(p)} + (h(p)−e)·h > 0` ゆえ厳密に more capable であり、Theorem 3 の仮説は a fortiori 満たされる
(`B33`)。⟹ **Theorem 3 が使えなくなるのは `e > h(p)` の側だけ**である。

⚠ **N11 への実務的な帰結は変わらない** — 標的が偽になるのは `e > h(p)` の側だけなので、分離の的を探すなら
そちら側に出る必要があり、そこでは確かに Theorem 3 の仮説が壊れる。⚠ **しかし §4.1 / §4.2-1 の逐語は
「`e ≠ h(p)`」を「`e > h(p)`」に直すべきである** — でないと N11 が `e < h(p)` の帯を「比較の相手が無い」と
誤って捨てる (⚠ その帯は**標的が真である**ことが上で証明されたので的としては空だが、**理由が違う**)。

### 落ちた 3 系統

- **`p` の族** (`B31`): `p ∈ {0.005, 0.01, 0.05, 0.2, 0.35, 0.49}` で `δ ≥ 0` と `T ≥ 0` がどちらも生存
  (最小 `−4.44e-16`)。⚠ **これは screen であり、証拠を担っているのは §5 の構造論法である**。
- **`e > h(p)` で標的が偽になること** (`B32`): `e = h(p) + 0.02 / 0.05` で最小 `T = −0.01998 / −0.04984`
  ⟹ N10 の報告値 `−0.01993 / −0.04982` と整合 (差は格子の分解能)。⚠ **これは分離ではない**。
- **退化の名指し** (`B8` / `B11`): `b ∈ {0,1}` では `S_B = 0` で §2.4 の不等式が自明化し、`X1` 決定的では
  `S_A = 0` と `h(a) = 0` で下界の 2 項が同時に消え、`A0 = A1` では `I2 = 0` で `S_A` が `δ(a)` に潰れる。
  ⟹ **どの退化でも論法は空回りせず、等号側に落ちるだけである**。

---

## 8. 訂正一覧 (逐語。⚠ 判定を動かすかを 1 件ずつ明記)

| # | 場所 | 何を何へ | `ε`/判定を動かすか |
|---|---|---|---|
| **1** | §1.4 / §3.1-2 | 「facts N7-p の『同値』は成り立たない — 3 変数不等式は**必要条件どまり**」→ **「落ちるのは同値ではなく還元の路である。`G_t ≥ G_1` で `t` を落とす論法は最大 `d*_1` だけ緩い。3 変数不等式は本 leg の結果の `t = 1` の場合として真であり、`ε = 0` と同値である」** | **動かさない** (⚠ §0 見立て 1 の較正が半分だけ正しくなる) |
| **2** | §3.3-2 | 「`e < h(p)`: 標的は**余裕つき**で成立する」→ **「`e ≤ h(p)` のどこでも標的は等号を持つ (`X1` 決定的 × `b = β*_t`)。余裕が付くのは `δ` の内点であって標的ではない」** | **動かさない** (境界の結論は保たれる) |
| **3** | §4.1 末尾 / §4.2-1 | 「**`e ≠ h(p)`** では [probc] Theorem 3 が適用できない」→ **「`e > h(p)` では適用できない。`e < h(p)` では more capable が強まるので適用できる」** | **動かさない** (⚠ N11 の探索域の記述が変わる) |
| **4** | §4.1 | 「閉じた理由は `e = h(p)` と `e + C = 1` の **2 本**だけ」→ **「`e + C = 1` は `C := 1−e` の定義であって条件ではない。実質の入力は `δ ≥ 0` (⟺ `e ≤ h(p)`) の 1 本であり、`Ω(t) = d*_t` は半空間 `e ≤ h(p)` の全体で成立する」** | **動かさない** (⭐ 射程を広げる) |
| **5** | §2.5 / §3.4 末尾 / §5-7 | 「符号変化はちょうど 1 回 (float の主張) / 区間演算はしていない / 丸めが効きうるのは §2.5 の 1 変数の符号だけ」→ **「符号変化 ≤ 1 は `N'(x) = e(1−2p)²(1−2x) > 0` から全 `p` で構造的、`δ(x0) ≥ 0` は凸弧から導出、符号変化 0 の場合も凹性だけで閉じる ⟹ `δ ≥ 0` の証明に浮動小数は 1 つも要らない。監査は区間演算で `[0.01,0.45]` も厳密に閉じた」** | **動かさない** (⭐ 浮動小数への依存が 0 になる) |
| **6** | §2.4 / §2.6 | 「`ψ_t ≤ d*_t` を当てて `T_t ≥ C(1−t)h(a) + t·S_A`」→ **「`σ_x := d*_t − ψ_t(x) ≥ 0` を入れると `T_t = C(1−t)h(a) + t·S_A + e·σ_b + C·E[σ_B]` が**恒等式**である ⟹ 標的は非負 4 項の和であり、§2.6 の等号集合はその 4 項が同時に消える点として導出される」** | **動かさない** (⭐ 上方修正) |
| **7** | §1.3 (`E3`) | 「閉じる恒等式 `H(Y)_一様 − F*(t) − t·H(Z|X) = 2C + d*_t` を残差 `6.661e-16` で確認」→ **「検証器は `F*(t)` を `(1−t)H(Y|X) − d*_t` で置き換えており `min_s f_t` を計算していない ⟹ その残差は `2C = C+1−h(p)` の算術だけを測る。監査が生の `f_t` の直接最小化で埋めた (残差 `1.7e-41`)」** | **動かさない** (証拠の型) |
| **8** | §3.1 末尾 | 「レート領域の包含について新しいことは何も言っていない」→ **「本 leg が再導出したのは支持関数の一致までであり、領域の等式は N9 §4.1 の連鎖の引用である (= §5-1 の書き方)」** | **動かさない** (文言) |

**⚠⚠ 8 件のうち `ε` を動かすものは 0 件、判定を動かすものも 0 件である。**
**⭐ うち 5 件 (2・3・4・5・6) は N10 が自分を過小に書いていた側の訂正である。**

---

## 9. 破る側に振った試行の一覧と本数

**39 系統。10 系統が成立 (= 訂正を生んだ) / 29 系統が落ちた (= 潰しにいって潰せなかった)。**
⚠ **成立した 10 系統のうち、主判定 (`ε = 0`) を動かすものは 0 本である**。

| 標的 | 系統 | 成立 | 落ちた |
|---|---|---|---|
| 1. §2.3 の恒等式 3 段 (`Y2`/`Z2` の向き込み) | 7 | 0 | 7 |
| 2. §2.2 の恒等式と `B_i` の向き | 2 | 0 | 2 |
| 3. 繊維の全射性・退化繊維 | 3 | 0 | 3 |
| 4. §2.4 の不等式の段 | 6 | 1 (訂正 6) | 5 |
| 5. §2.5 の `δ ≥ 0` | 8 | 3 (訂正 5 の 3 面) | 5 |
| 6. 連鎖と射程 (honesty) | 7 | 3 (訂正 1・7・8) | 4 |
| 7. class, not instance | 6 | 3 (訂正 2・3・4) | 3 |

⚠ **「落ちた」は追認ではない** — 各系統は**生の同時分布からの独立再導出**・**浮動小数を使わない記号代数**・
**有理数の総当たり**・**区間演算**・**明示の負の対照**のいずれかであり、**乱択の非違反を証拠に使っていない**
(乱択が出てくるのは `B9` / `B34` の破る側だけで、その非違反は本監査の判定入力ではない)。

**判定を担っている系統**: 記号代数 `B12` / `B13` (完全に厳密)、生の同時分布からの恒等式
`B2`–`B8` / `B14` / `B25` / `B26` (40 桁)、有理数の総当たり `B10`、区間演算 `B24`、構造論法 `B18` / `B19` / `B16`。

---

## 10. 独立検証器

[`verifiers/n10_audit_probc.py`](verifiers/n10_audit_probc.py) (**34/34**、約 46 秒。`--quick` で約 16 秒)。

```bash
python3 docs/shannon/verifiers/n10_audit_probc.py
```

⚠ **`n10_epsilon_zero_probc.py` / `shoulder_certificate_probc.py` / `n7_audit_probc.py` / `n9_cone_probc.py` /
`n9_audit_probc.py` / `capacity_probc.py` / `kappa2_*.py` を 1 行も実行・import・複製していない**。
テスト番号は `B1` (インスタンス) / `B2`–`B8` (生の同時分布からの恒等式と繊維の座標) / `B9`–`B11` (反例探索) /
`B12`–`B16` (記号代数・重み・方向の被覆) / `B17`–`B24` (`δ ≥ 0` と区間演算) / `B25`–`B30` (連鎖と等号集合) /
`B31`–`B34` (class, not instance と screen)。

**証拠の型は 4 段階で明示している**: 厳密な記号代数 (`B12` / `B13`、浮動小数を 1 つも使わない) >
区間演算 (`B24`) > 40 桁演算 (`B2`–`B11`, `B14`, `B17`–`B30`) > float64 の掃引 (`B28`, `B31`–`B34`、**SCREEN と明記**)。

---

## 11. ⚠ N11 へ向けて本監査が動かしたもの (⚠ gate ではない — N10 §4 が SoT)

⚠ **本監査は gate を書き換えない**。以下は §8 の訂正 2・3・4 が N10 §4.1 / §4.2 の**射程の記述**に与える影響の
名指しであり、N11 の標的の選定そのものは N10 §4 と orchestrator の判断に属する。

- **標的が真である領域は曲線ではなく半空間である** — `Ω(t) = d*_t` は `{(p,e) : 0 < p < 1/2, e ≤ h(p)}` の
  全体で成立する (訂正 4・5)。⟹ **`e < h(p)` の帯を「未探索の的」として起票する理由は無い**
  (⚠ **理由は「比較の相手が無い」ではなく「標的が真であることが証明された」である** — 訂正 3)。
- **残るのは `e > h(p)` の側だけである** — そこでは `δ` が `x = 1/2` で `h(p) − e` だけ負に落ち、標的が偽になる。
  ⚠⚠ **これは分離ではない** (N10 §4.1 が既にそう書いている) — 超えたのは上界と下界であって領域ではない。
  ⚠ **そちら側では両ブロックの more capable が壊れるので Theorem 3 の閉形が消える** (= N10 §4.2 の反証条件 1)。
- ⚠ **本監査は `C` 側を 1 mm も動かしていない** — 離調側で測ったのは `Ω` と `δ` だけであり、
  N10 §5-11 が名指しした「片側だけの観測」という限定はそのまま残る。

---

## 12. ⚠ 本監査が確かめられなかったことの名指し

1. **[probc] Theorem 3 / [auxrec] Theorem 7 の逐語** — 一次文献を取得していない (`lit-fetch.sh` を実行していない)。
   N10 §1.1 が引く `probc.txt:900-901 / 906 / 916 / 917` の行番号も**未確認**である。⟹ **インスタンス定義の転記に
   誤りがあれば本監査も同じ誤りを共有する** (N9 監査 §12-2 / N10 §5-3 と同じ穴)。言えるのは、転記されたインスタンス
   (`p = 0.1`, `e = h(p)`, 交差した BEC/BSC) が**自己整合であり、`d* ≈ 0.03877` という文献値を再現する**ことだけである。
2. **`h_Thm7(0,1,t) ≤ 2C + Ω(t)` (N7-b)** — **再導出していない**。`(18b)` / `(18i)` / 同時分布の逐語照合も
   していない。本監査が確かめたのは `Ω(t)` の中身だけであり、**この上界が正しくなければ `ε = 0` の結論は立たない**。
   ⟹ N10 §5-2 の限定はそのまま残る。
3. **`C` 側の下界 `h_C(0,1,t) ≥ 2C + d*_t`** — `R1(β) = C + 1 − h(β*p)` / `R2(β) = C·h(β)` は**台帳からの引用**であり、
   `β`-split が Theorem 3 の箱の頂点であること (N7-q (iv)) は再導出していない。`B27` が示したのは
   「その 2 本を与えられれば橋は算術である」ことだけである。
4. **N9 §4.1 の連鎖** (`ε = 0 ⟹ Thm7|_{R0=0} = K_β = K ⟹ D′ = D_β ⟹ Thm7 = C`) — **引用**である。
   `D_β ⊆ C` も `C ⊆ D` も本監査は再検証していない (N9 監査が別途 43 系統で扱っている)。
5. **`ε = 2.0786e-07` そのもの** — N7 の DC 分枝限定も N7 監査のアフィン緩和も**再実行していない**。
   本監査が独立に立てたのは「厳密な差は 0 である」ことであって、「N7 の上界が妥当だった」ことではない。
6. **被監査検証器 `n10_epsilon_zero_probc.py` の実行結果 (`22/22`・所要時間)** — **禁止事項として 1 行も
   実行していない** ⟹ 未確認である。⚠ **訂正 7 はソースの読解に基づく**。
7. **区間演算は `δ` の `[0.01,0.45]` の 1 か所だけ**である。`B2`–`B11` / `B14` / `B17`–`B30` の残差は 40 桁の
   浮動小数であって保証された誤差上界ではない (⚠ ただし `B12` / `B13` / `B8` の全射性 / `B18` の有理 `p` は
   **完全に厳密**であり、判定の骨格はそこに乗っている)。
8. **§1.4 (H2) の 2 次形式の分解** — 数値 3 件 (最小固有値 `0.069344`、`(1−2p)² − C = 0.108996`、
   `(b*p)(1−b*p) − b(1−b) = 0.064211`) は再現したが、N10 が書いた
   `(x+y)²[(1−2p)² − C] + C[(1−2p)²·S_in − S_out]` という**分解の形そのものは再導出していない**。
   ⚠ **これは局所の話であり判定には入らない** (§4 の恒等式が大域で閉じているため)。
9. **§2.7 の「捨てた経路」(見立て 5)** — N10 自身が「検証器にも入れていない」と書いており、本監査も追っていない。
10. **一般 BC / BEC-BSC 以外のチャネル** — 触れていない。監査が広げたのは `(p,e)` の 2 パラメータ族までであり、
    交差した BEC/BSC という構造そのものは外していない。⟹ N10 §5-4 の限定は残る。
11. **基数** — 監査は `X = {0,1}²` の 4 点法則しか掃いていない。N10 の連鎖に補助変数の基数は現れないので
    影響しないが、**N7-b の上界側は別**である (そちらは未検証、§12-2)。
12. **`Thm7 ⊋ C` について本監査は何も産んでいない** — 材料は 1 つも出ていない。幅 `ε` の内側も、
    `e > h(p)` 側の `C` の挙動も測っていない。
