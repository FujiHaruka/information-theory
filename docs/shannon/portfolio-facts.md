# Portfolio (log-optimal) settled-facts ledger

> family `portfolio` の確定事実の**単一の真実源**。フォーマット規約 → `CLAUDE.md`「Plan / docs hygiene」。
> 再導出が高コストな事実のみ記録。sorry 数 / axiom status / decl 存在は **キャッシュしない**
> (`#print axioms` / `scripts/sig_view.ts --sorry` / `rg` で都度)。
> confidence: `machine` (axiom/sorry 機械検証、再検証コマンド必須) / `loogle-neg` (Found 0、query 併記) /
> `human-judgment` (解析的壁判断、低信頼)。

| claim | confidence | 再検証コマンド | last-verified (commit) | notes |
|---|---|---|---|---|
| 有限和版 `ConcaveOn.sum`（凹関数の有限和は凹）は Mathlib 不在 | loogle-neg | `loogle "ConcaveOn.sum"` → Found 0 | `cea0b16e` | 唯一の Mathlib gap。`Basic.lean` 内で自作 `concaveOn_finset_sum`（`Finset.cons_induction` + base `concaveOn_const` + step `ConcaveOn.add`、~10 行）で充足。Mathlib PR 余地あり |
| portfolio 4 headline すべて proof-done sorryAx-free（`competitive_optimality` / `growthRate_concaveOn` / `logOptimal_of_kuhnTucker` / `kuhnTucker_of_logOptimal`、`Shannon/Portfolio/Basic.lean`） | machine | `#print axioms InformationTheory.Shannon.Portfolio.competitive_optimality`（他 3 も同様）= `[propext, Classical.choice, Quot.sound]` + `lake env lean InformationTheory/Shannon/Portfolio/Basic.lean`（silent） | `cea0b16e` | 独立 honesty-auditor PASS で 4 定理とも `@audit:ok`。壁ゼロで在庫予測どおり着地、撤退ライン不発 |
| forward KT (`kuhnTucker_of_logOptimal`) は price-relative 非負性 `X a i ≥ 0` を要さない | machine | `Basic.lean` の当該証明が `hXnn` を参照しないこと（FDeriv/tangent-cone 論法は X の符号に非依存）。skeleton の `hXnn` は削除済 | `cea0b16e` | honesty-auditor が「theorem-hypothesis 削除は monotone-safe、falseness risk なし」と検証。`competitive_optimality` は skeleton どおり `hXnn`/`hpos` を保持 |
| Leg A operational AO-iid 2 headline（`seqLogWealth_div_tendsto_growthRate` / `seqLogWealth_asymptotically_optimal`、`Portfolio/OperationalSequences.lean`）proof-done sorryAx-free | machine | `#print axioms InformationTheory.Shannon.Portfolio.seqLogWealth_div_tendsto_growthRate`（+ `_asymptotically_optimal`）= `[propext, Classical.choice, Quot.sound]` + `lake env lean InformationTheory/Shannon/Portfolio/OperationalSequences.lean`（silent） | `187d0f82` | gambling SLLN core (`strong_law_ae_real`) の 1:1 clone、想定どおり壁ゼロ。独立 honesty-auditor PASS で `@audit:ok` |
| Leg C side-info 増分不等式（`sideInfo_growthRate_increment_le_mutualInfo`、`Portfolio/SideInformation.lean`）proof-done sorryAx-free、署名 honesty 修正済 | machine | `#print axioms InformationTheory.Shannon.Portfolio.sideInfo_growthRate_increment_le_mutualInfo` = `[propext, Classical.choice, Quot.sound]` + `lake env lean …/SideInformation.lean`（silent）。署名は `hbs`/`hbcond`（simplex 帰属）を持ち `hKTcond` を持たない | `187d0f82` | ΔW ≤ I(X;Y) の不等号（gambling 等号と別機構、KT + Gibbs 上界）。KT 条件は simplex 帰属を強制しない（反例 m=1, bcond=100）ため simplex 帰属は regularity として必須。@audit:ok |
| Leg B stationary fixed-b 2 headline（`seqLogWealth_div_tendsto_stationary` / `stationaryLogReturn_integral_le_of_kuhnTucker`、`Portfolio/StationaryMarket.lean`）proof-done sorryAx-free | machine | `#print axioms InformationTheory.Shannon.Portfolio.seqLogWealth_div_tendsto_stationary`（+ `stationaryLogReturn_integral_le_of_kuhnTucker`）= `[propext, Classical.choice, Quot.sound]` + `lake env lean …/StationaryMarket.lean`（silent） | `187d0f82` | fixed-b 収束は `birkhoff_ergodic_ae` の 1:1 適用。dominance は **新規 measure-theoretic 証明**（有限 `logOptimal_of_kuhnTucker` は general-measure に効かず、`Real.log_le_sub_one_of_pos` 経由の積分版 KT）。W_∞ AEP 完全形は `portfolio-stationary-woo-plan` へ deferral。@audit:ok |
| Leg D Cover universal portfolio regret（`universal_portfolio_regret_tendsto_zero`、`Portfolio/Universal.lean`）proof-done sorryAx-free | machine | `#print axioms InformationTheory.Shannon.Portfolio.universal_portfolio_regret_tendsto_zero` = `[propext, Classical.choice, Quot.sound]` + `lake env lean …/Universal.lean`（silent） | `187d0f82` | @audit:ok |
| simplex 上の Dirichlet 積分（exact-constant regret）は Cover asymptotic regret には不要 = `wall:simplex-dirichlet-integral` は **not-a-wall** | human-judgment | Leg D 実装が Dirichlet 積分を回避したこと（`Universal.lean` の `bestConstantWealth_le_mul_universalWealth` が `Measure.addHaar_image_homothety` + `exp_neg_one_le_shrink` で shrink 論法、積分核の閉形式評価なし） | `187d0f82` | 起草 M0 probe は `stdSimplex` 測度 / 多変量 Dirichlet の Mathlib 不在から壁公算大と見たが、asymptotic regret は Cover shrink（simplex を頂点へ相似縮小した部分体積比較）で閉じ、正確な定数のみが Dirichlet 積分を要すと判明。`wall:simplex-dirichlet-integral` は audit-tags.md register に**未登録のまま**（登録不要） |
| W_∞ AEP サブ計画 R1 gateway `exists_measurable_argmax_on_stdSimplex` sorryAx-free（simplex 上の可測 argmax、`Portfolio/StationaryWinfty.lean`） | machine | `#print axioms InformationTheory.Shannon.Portfolio.exists_measurable_argmax_on_stdSimplex` = `[propext, Classical.choice, Quot.sound]` + `lake env lean InformationTheory/Shannon/Portfolio/StationaryWinfty.lean`（silent） | `3544a8ae` | Tikhonov（狭義凹正則化）ルート、self-build ~290 行。追加仮説 `hF_conc`（ConcaveOn）+ `[Nonempty (Fin m)]` は regularity（m=0 で simplex 空 ⟹ 選択不能、非 load-bearing）。独立 honesty-auditor + style-auditor PASS で @audit:ok。Mathlib 一般 measurable-selection（KRN）不在（`Mathlib/Probability/Decision/BayesEstimator.lean:46` TODO） |
