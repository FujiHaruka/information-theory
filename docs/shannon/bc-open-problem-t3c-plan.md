# 一般 BC の計算可能な容量領域 — 未解決本体 (T3) への攻略 第 3 次

> **Parent**: [`broadcast-channel-moonshot-plan.md`](broadcast-channel-moonshot-plan.md)
> **Predecessor**: [`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md)
> (M0–M19 完走・**§0 のゴールは未達**で終了。⚠ **その plan は閉じており書き換えない**)。
> 確定事実 = [`bc-facts.md`](bc-facts.md) / attack 台帳 = [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) / (C2) 側の在庫 = [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md) / 前 relay の失敗形と運用の実測 = [`../proof-logs/proof-log-bc-open-problem-t3b-relay.md`](../proof-logs/proof-log-bc-open-problem-t3b-relay.md)

**Status**: 起票 (2026-08-04)。ユーザー決定により**継続**し、**新規 20 leg (N0–N19)** の予算を切った
(§7 判断ログ 1)。⚠ **§0 のゴールと完了条件は前 plan から 1 文字も動かしていない** (§0.1-1)。
⚠⚠ **前 2 次は合わせて 41 leg を使って未達で終わっている** — その実績の上での第 3 次である。
**入口は 1 点**: `C` の和レート面の**端**を `Thm7` が超えられるか (§1)。
⚠ **前 relay は NO-GO を 1 本も出しておらず、`Thm7 ⊄ Thm8` は 1 度も主張されていない。`R ∈ Thm7` も言えておらず、`Thm7 ⊋ C` の材料は 1 つも無い**。
**Branch**: `bc-computable-region`

## 進捗

- [x] N0 / N1 / N2 gate 3 本 (資産と在庫 / 入口 1 点の第一手 / ⭐ early gate) ✅ ⟹ **N2 で §6-5 発火**
- [x] **A1 (N3–N5) ⭐ 探索アーク 1 本目** — 決着を要求しない枠 (§5.3) ⟹ **終端判定 = `続行`** (§6-6 不発火。⚠ **昇格条件は満たさない / §0 への payoff は 3 点とも不成立**)
- [x] **N6 / N7 / N8 判定枠 第 1 組 ([探索 + gate] → [構築 + probe] → [層 3 へ載せる])** — 肩が **NO-GO (許容差つき)** で決着し、その還元のうち **`(R1)` 1 本だけ**が Lean へ載った (⚠ **監査は 2 段とも訂正あり生存 / `h_Thm7(0,1,t)` の上界そのものは Lean に無い**)
- [x] **N9 / N10 判定枠 第 2 組 第 1–2 段** — 錐が恒等式で閉じ (N9)、**`ε` は厳密に 0** になった (N10。⚠ **監査は 2 段とも訂正あり生存 / 決着は [probc] の族の上に留まる / `Thm7 ⊋ C` の材料は 1 つも無い**)
- [x] **N11 判定枠 第 2 組 第 3 段 [層 3 へ載せる]** — N10 が立てた `δ ≥ 0` が **0 sorry / sorryAx-free** で Lean に載った (⚠ **監査は訂正あり生存 / 載ったのは `ℝ` 上の 1 変数不等式 1 本であり、レート領域の包含についても [probc] の判定そのものについても 1 文字も言っていない**)
- [x] **N12 判定枠の棚卸し** — 判定枠 6 leg (N6 … N11) の確定 / 未確定を 2 欄に分けて確定させた (⚠ **監査は訂正あり生存 / 起票の反証条件 (2) は監査 訂正 4 で発火 → 不発火へ反転 / `Thm7 ⊋ C` の材料は依然 1 つも無く排除もされていない**)
- [x] **A2 (N13–N14) ⭐ 探索アーク 2 本目 — 1 leg 目 (N13 = novelty gate) で `redundant` 死し、N14 が閉じた** (⚠ **終端 N15 には届かず、facts に `## A2 (T3c)` 節は無い** / ⚠ **監査は訂正 19 件つきで主判定を支持・主判定を動かす訂正は 0 件** / ⚠⚠ **§0 への payoff は 3 点とも不成立**) ⟹ **§6-6 の精神により 3 本目のアークは起票しない** (§7 判断ログ 10)
- [x] **N15 判定枠 第 3 組 第 1 段 `[探索 + gate]`** — **判定 = GO** ([glnsum] の和 (⊕) チャネル族が [probc] の外で `C` の支持関数の閉形を供給する) ⟹ **N16 へ渡す族は 1 本** (⚠ **監査は訂正 7 件つきで主判定を支持・主判定を動かす訂正は 0 件** / ⚠⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無く排除もされていない** / ⚠ **Lean には 1 つも載っていない**)
- [x] **N16 = 第 2 段の上流 gate**「現行 [probc] more-capable インスタンスで `UV = C` か」 — **判定 = `UV = C`** ⟹ **N7 / N9 / N10 の `Thm7 = C` は挟み込みで強制されていた** = ⚠⚠ **減算方向の較正** (⚠ **監査は訂正 14 件つきで主判定を支持・主判定を動かす訂正は 0 件** / ⚠ **射程は 1 点 + 条件つき半空間で「半空間全体で `UV = C`」とは書かない** / ⚠⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無く排除もされていない** / ⚠ **Lean には 1 つも載せていない**)
- [ ] **N17 / N18 形式化枠 (子 plan の単位 B) / N19 記録** 🚧 — ⭐ **ユーザー決定 (2026-08-09) で relay の cap を 20 → 23 へ延長し、残枠 3 を走らせる** (§7 判断ログ 12。⚠⚠ **`GOAL-CHANGE` ではない** / ⚠ **relay の cap と plan の leg 予算は別勘定という条項は維持される** — 幅が一致したのは偶然である) ⟹ **N17 = 起票済** ([`bc-t3c-n17-unit-b.md`](bc-t3c-n17-unit-b.md))。⚠⚠ **§6-5 は既に「収まらない」と判定済ゆえ N17 / N18 は §0 の Lean 側には届かない**
- ⚠⚠ **第 3 段 `[層 3 へ載せる]` は枠が無く 3 段そろわない** — §7 判断ログ 10-(5) の明示的な逸脱

---

## 0. ゴール — 未解決本体そのもの

**一般 2 受信者 DM-BC の容量領域の計算可能な特徴付けを、肯定側か否定側のどちらかで決着させる。
散文の証明と、その Lean による機械検証の両方をもって完了とする。**

完了条件 (どちらか一方):

- **肯定側** — 任意の `ε > 0` に対し有界時間で `ε` 近似を返す手続きを構成し、その正当性を証明する。
- **否定側** — そのような手続きが存在しないことを証明する。

いずれの側も、**散文の証明が立つこと**と**Lean で proof done に到達すること** (0 sorry / 0 residual、
`#print axioms` が sorryAx-free、かつ署名走査で load-bearing hypothesis が無いこと) の両方を満たして
初めて達成とする。片側だけでは達成ではない。

### 0.1 ⚠ ゴール保護条項 — 本節は leg / planner が書き換えてはならない

**前 relay の前身で実際に起きた事故がこれである**。原案 (`487afc03`) は T2 の完了条件を
「文献に無い命題が **Lean で機械検証される**」と書き、「**ユーザー指定の二段構え
(散文で証明 → 形式化で検証)**」と明記していた。それを `28a740c5` が **L0 が走る前に**
「散文レベルの候補経路」へ書き換え、形式化をスコープ外にした。判断ログにはユーザー判断としての
記録が無い ⟹ **planner の独断でゴールが狭まり、20 leg がその狭いゴールで回った**。

したがって本 plan でも以下を規約とする:

1. **§0 のゴールと完了条件を狭める変更は、leg / planner が適用してはならない**。狭める提案は
   判断ログに **`GOAL-CHANGE`** として起票し、**ユーザーの承認を得るまで適用しない**。
   広げる方向 (より強い完了条件) は起票不要。
2. **中間結果は記録するが、ゴールの代替にはしない**。台帳 (facts / attacks / 在庫) への収穫は
   従来どおり続けるが、**それをもって「ゴールに到達した」と書かない**。かつての階段 (T0–T3) が
   持っていた「どこで止まっても成果が残る」というヘッジは、**記録の設計としては継承し、
   ゴールの設計からは外す** — ユーザーが「成果ゼロで終わる可能性を引き受ける」と明示的に選んだ。
3. **各 leg は冒頭に「この leg は §0 のゴールに対して何を動かすか」を 1 行で書く**。書けない leg は
   起票しない。⚠ **本 relay ではこの 1 行に「側」を併記させる** (§5.1)。

---

## 1. 標的の現在地 — 入口は 1 点

⚠ **値と逐語はここへ複製しない**。判定の SoT = [`bc-facts.md`](bc-facts.md) / 状態の SoT =
[`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) / 標的の分解 ((C1) 内界側 / (C2) 外界側 / (C3) 有効収束率) と穴が (C2) 1 つに絞られた経緯の SoT = [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) §1.1。

**入口 (前 relay が park した唯一の的。SoT = facts `## M19 (T3b)` M19-4 行 = `## M6 (T3b)` 最終行)**:

> **`C` の和レート面の端 `R^end` を `Thm7` が超えられるか** — すなわち `R₀+R₁` が `C` 側の閉形の天井を超え、かつ和レートが `SR_C` に留まる点を運ぶ `Thm7` witness (facts が数え上げた全 `T_J` = 11 種すべてで適格) が在るか。

**入口がこの 1 点である根拠 (⚠ 3 値はすべて facts が SoT。ここには写さない)**: `C` 側の天井は
`## M6 (T3b)` 行 4 で**閉形と恒等式で確定**しており (最適化器が立てたものではない)、`Thm7` 側は `## M14 (T3b)` の対角 witness が**ちょうどその天井に接している** (同節行 5)。接点で拘束しているのは
**`(18b),(18c),(18d)` の 3 本ちょうど**で、⭐ **その 3 本は自分自身の天井には触れていない**
(対角 witness は `W` が自明なので `(18b) = I(U,W;Y)` に対し `I(X;Y) = 2C` まで余地がある)。

⟹ **問いは「`(18b),(18c),(18d)` を同時に天井より上げたまま、全 `T_J` で適格性を保ち `(18i) = SR_C` を維持できるか」**であり、押し上げられれば **`Thm7 ⊋ C`** が出る。

⚠⚠ **これは「あと少し」ではない**。**余地があること**と**適格性を保ったまま押し上げられること**は
別命題であり、**後者は 1 度も試されていない**。⚠ **「残るのは行数と配線だけ」型の言い回しは
tell として名指しで禁じる** (§4.1 軸 2)。

### 1.1 落としてはならない 4 点 (前 plan §1 から継承。⚠ 逐語は複製しない)

- **(i) (C2) の同値形** — 要求は「上から収束する外界**列**」では**ない**。`C` を共通部分にもつ、指標と `W` について一様に半計算可能な外界の**可算族が 1 つでも存在するか**と同値である (減少性も有効収束率も要らない。facts `## L2 (T3)` 節)。
- **(ii) 還元先は 1 点に縮んでいる** — `Thm7(W)` の一様 effective compactness は証明済ゆえ
  **(C2) ⟸ (`Thm7(W)` と `C(W)` の厳密一致)**。⚠ **「未決」であって「偽」ではない**。
- **(iii) 穴の正体** — (C2) は [Li21] §VIII の open problem (= BC の単文字特徴づけ問題) と **同一の穴**である (facts `## L3 (T3)` 節)。
- **(iv) 難度の較正** — **否定側は Marton 最適性予想の反証を含意する** (facts `## L0 (T3)` 節) ⟹ 問題は新規性ではなく**難度**である。

⚠ **前 relay の中間結果は出発点を与えない** — 出発点は上の入口 1 点であって、M2–M18 の成果物の
上ではない。⚠ **前 relay の形式化 3 leg (M16–M18) が載せたのは「制約右辺どうしの関係」**であって
Theorem 7 / Theorem 8 そのものではない (facts `## M19 (T3b)` の M19-5 行)。

---

## 2. Approach

**§0 の完了条件は「散文の証明」と「Lean の proof done」の両方**である。前 2 次はどちらにも届かず、**未達の形が 2 次で違う** (逐語は §7 判断ログ 1) ⟹ 本 relay は「配分を守る枠構造」をそのまま継承し、**その上に「Lean 側の最も重い段の員数を先に測る早期 gate」を 1 本足す**。20 leg の形は次のとおり。

- **入口 1 点を両側から測る系統 (判定枠、側 = `(C2)`)** — §1 の問いを [探索 + gate] → [構築 + probe] → [層 3 へ載せる] の 3 段 1 組で回す。**肯定側 (押し上げられる = `Thm7 ⊋ C`) と否定側 (押し上げは不可能 = 天井が `Thm7` 側にも立つ) のどちらに転んでも軸が 1 本確定する**。
  ⚠ **N2 着地後の改定で、可変枠 14 のうち 7 leg がこの系統、6 leg が下の探索アークになった**
  (§5 / §7 判断ログ 4)。
- **⭐ 決着を返さない探索アーク系統 (可変枠の内数 6 leg = 3 leg × 2 本、側 = `(C2)`)** —
  **決着圧力を落とすのではなく、成果物の型を 4 つに増やして圧力の当たり先を変える** (§5.3。
  起票の診断と引き受けたリスクは §7 判断ログ 4)。⚠ **道具立て (§4.5 の 3 つ) は変えない。
  変えるのは何を検証するかだけである**。
- **⭐ Lean 側の員数を先に測る系統 (early gate 1 leg、形式化枠の内数、側 = `形式化債務`)** —
  ⚠ **この leg は形式化枠 3 の内数**であり、**可変枠からは 1 leg も出さない** (§5.2)。
- **層 1 が通った命題はその命題単位で層 3 へ載せる** — 全体が揃うのを待たない (前 relay で有効)。
- **層 2 の道具は 3 つに限る** (§4.5)。⚠ **数値掃引は原理的に効かない** — T3 の主語は計算可能性で
  あり、**最適化器の非違反は証拠価値がほぼ 0** (前 relay で 6 度再現)。

**層 1** 散文 (アイデア + 証明スケッチ) / **層 2** proof-probe (核 1〜2 ステップを機械検証 — 散文の自己欺瞞を最短で殺す装置) / **層 3** 形式化 (散文全体を Lean へ ← スコープ内)

---

## 3. 攻撃軸

| 軸 | 主語 | 優先度 | 第一手 |
|---|---|---|---|
| **T3-α** | **肯定側 / 分離側** — §1 の入口 1 点 | 1 | **N1 = 反証試行**。`(18b),(18c),(18d)` を同時に天井より上げる witness を、全 `T_J` の適格性を保ったまま取りにいく。⚠ **手構成か恒等式で決める** (§4.5)。**押し上げられれば `Thm7 ⊋ C`、天井が `Thm7` 側にも恒等式で立てば還元先が 1 段強くなる** ⟹ どちらに転んでも軸が 1 本確定する |
| **T3-β** | **否定側** — effectively compact でないことの証明 | 2 | 同じ穴の裏。真の未着手部分は「チューリング機械 → BC チャネル行列」の還元 (attack `t3-tm-to-bc-reduction`)。⚠ **「文献に無い」≠「不可能」** / ⚠ **決定不能性 ≠ `ε` 近似不可能性** — 混ぜると誤った壁宣言になる。⚠ **肯定側は選言 / 否定側は連言** (肯定側は候補 1 本が当たれば決着、否定側はすべてを潰す必要がある) |

⚠ **優先度を 1 本に絞ったのは前 relay との差である** — 入口が 1 点に確定した以上 **gate-first は
T3-α 側**であり、T3-β へ振り替えるかは §6-1 の判断に載る。attack の状態と死因の SoT は [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) であり、**本 plan は状態をキャッシュしない**。

### 3.1 ⚠ 死んだ路線 — 再探索しない

**内容はここへ複製しない**。前々 plan [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) §3.1 が列挙する 8 路線 (内界 = 外界 / 外界 = 容量 / 停止則の独立化 / 階層のレベル下げ / UV 外界の多文字化 / `∃` 形外界の多文字化 / [Li21] Theorem 10 経由 / `{Z}` の閉包演算だけで作る支配チャネル族) は
**すべて死因つきで確定しており、本 relay でも再探索しない**。⚠ **同時に落としてはならない射程の
限定 2 つ**も前 plan §3.1 の逐語のまま継承する (死んでいるのは「多文字化」という 1 機構だけ / [Li21] の Tarski 指数関数問題の留保は **exact decision の語義**であり `ε` 近似を否定しない)。

**前 relay で新たに死んだもの (⚠ SoT は facts の各節。⚠ どれも「包含の否定」ではない)**:

- **S9 = 点ごと witness 移送 (恒等写像)** — `## M15 (T3b)` で**反例つきで 偽**。独立監査済。
  ⚠ **死んだのは「移送」という方法である** / ⚠ **反例 witness が運ぶ角は `C` の内点** (`## M6 (T3b)`)
  ⟹ **`Thm7 ⊋ C` の材料ではない**。
- **単一 `T_J` インスタンス経由の証明路** = `## M10 (T3b)` で反例つきで死亡 / **時分割 gluing** = `## M11 (T3b)` で**恒等式により**死亡 (探索の失敗ではない) / **`∃∀` swap (領域版)** = `## M12 (T3b)` で偽 (3 段の恒等式)。

⚠ **禁止する書き方**: `Thm7 ⊄ Thm8` を主張する / 「方法の死」を「包含の否定」と書く /
`R ∈ Thm7` と書く / 「あと少し」「残るのは行数と配線だけ」型の言い回し。

---

## 4. 運用規約

### 4.1–4.3 継承 (⚠ 再掲しない。逐語の SoT は前 plan)

- **§4.1 失敗形 2 軸** (3 本とも逐語の SoT = 前 plan [`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md) §4.1–§4.3) — 軸 1 (未検証の否定的判断を機械に落とさないまま設計へ通す) / 軸 2 (残件の員数を prose だけで見積もる)。**処方は 2 本立てで 1 本に畳まない** — 軸 1 は着手*前*に 1 行の反証、
  軸 2 は gateway atom 通過*後*に在庫を取り直す。⚠ **軸 1 は本 relay で §4.4 が強化する**。
- **§4.2 在庫と証拠の作法 3 規約** — 結論形で数える / 署名でなく証明本体を読む / loogle の生クエリの
  ヒット数を「無い」の根拠にしない。⚠ **3 本目は在庫 [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md)
  §6 で実測 2 回発火** (`Primcodable ℚ` と Hausdorff instance がどちらも生クエリ `Found 0` なのに実在)。
- **§4.3 検証の誠実性** — CLAUDE.md の Definition of Done (Standard B) がそのまま効く。**`sorry` + `@residual(<class>:<slug>)` が唯一の honest な退出路**で、本 relay の slug は **`@residual(plan:bc-open-problem-t3c-plan)`** (Mathlib の不足に当たった場合のみ `@residual(wall:<name>)` を立て、**register への追記を伴う**)。
  ⚠ **hypothesis に証明の核を担わせて `sorry` を消すのは禁止** / ⚠ **`sorryAx`-free は必要条件で
  あって十分条件ではない** (**署名走査**を必ず併せる) / ⚠ **機械で再導出できる事実 (sorry の有無 /
  宣言の存在 / axiom 依存) を本 plan の散文にキャッシュしない**。

### 4.4 ⭐ 見立ての較正 — ⚠ 向きによって外れ方が非対称である (新設)

前 relay の実測 (SoT = proof-log §3、母集団の定義つき。⚠ **母集団を先に固定する — 台帳には `9` が
2 つあり別勘定である**)。

- **義務を締める方向 = 9 件、9 件とも機械で覆った** (例外 0)。
- **義務を緩める方向 = 当たり 3 / 外れ 5 で、⚠ 外れの方が高くついた** — 締める側の外れは
  「要らない義務を用意した」で済むが、緩める側の外れは**その見立ての上に leg を組んでから死ぬ**。
- ⚠⚠ **「緩める提案は当たりやすい」とは書かない** — 成立しない。**非対称なのは
  「締める方向が全件覆る」という一方だけ**である。

⟹ **処方 (2 本、別々に走らせる)**: (1) **締める方向の見立ては着手前に 1 行の反証を機械に掛ける**
(= §4.1 軸 1 の強化。⚠ **ブリーフの各見立てに「締める / 緩める」を明記させる**) / (2) **緩める方向の
見立ては、着手前に §4.5 の 3 つのどれで殺せるかを先に決める** — 決められないなら主戦力に据えない。

### 4.5 ⭐ 層 2 の方法論 — 効く道具は 3 つだけ (新設)

⚠⚠ **最適化器の非違反は証拠価値がほぼ 0 である** (前 relay で **6 度**再現。典型は許容差を
`1e-2 → 1e-4 → 1e-6` と締めると超過が**単調に潰れる**形で、超過の正体は境界の緩みだった) ⟹ **掃引の非違反は「まだ見つかっていない」以上のことを言わない**。

**効いたのは次の 3 つだけである**:

1. **恒等式への帰着** — 係数が記号的に相殺する形まで持っていく。
2. **適格性 / 最適性の分離構造を使った全探索** — 空間全体を掃くのではなく、構造で候補を
   有限個に落としてから尽くす。⚠ **決定的 witness の全探索は整数 bit しか実現できない**
   ⟹ **確率的 witness を併せる** (前 relay は決定的全探索が完全適格な反例を取りこぼす事故を 3 度検出している)。
3. **閉形の族 / 反例の手構成** — 明示 witness の直接評価。

⚠ **1 と 3 は「値は機械・導出は我々」という混合の confidence を持つ** — facts へ書くときは
`machine` と `human-judgment` を**分けて書く**。⚠ **探索アーク (§5.3) でもこの 3 つを使う — 道具は
変えない**。変わるのは**何を検証するか**だけで、判定枠では「命題が真か」、探索アークでは「新しい対象が facts の既存 claim を再現するか」を同じ 3 道具に掛ける ⟹ **アークの成果物にも殺す基準が付く** (§5.3 / §6-6)。

### 4.6 ⭐ 否定的判定には敵対的な独立監査を掛ける (新設)

**反例 / 壁 / FALSE を出した leg には独立監査を掛ける**。⚠ **既定の立場を「反例は偽」に置いた
敵対的ブリーフ**にすること (前 relay で実証済 — M15 の監査は反例を壊す 7 本を実行して 7 本とも不成立を得たうえで、**副産物として `R1SET` の取りこぼしを塞ぎ文言の誤り 1 件を検出**した)。
⚠ **立場を書かずに「確認せよ」とだけ書いた監査は追認に潰れる**。⚠ 監査器は**被監査 leg の
検証器を 1 行も呼ばない独立実装**にする。

### 4.7 ⚠⚠ 報告を成果物の証拠にしない (新設)

**`git log` / `ls` / ファイル本体で必ず裏を取る**。前 relay の実測は 2 種類あり ⚠ **別の事象として扱う
(対処が違う)** — **(a) subagent が最終報告を送らず idle 化した事故が 6 例** (⚠ **`SendMessage` での
個別の問い合わせは有効**) / **(b) 報告と実態が乖離した事象が 1 件** (具体的な完了報告が届いたのに
検証時点でそのいずれも存在しなかった。⚠⚠ **出所は未特定であり、原因も責任も書かない**)。
⟹ **報告の内部整合性は成果物の実在を一切保証しない**。⚠ **idle かどうかは信号にならない** —
**完了報告を返した agent にも等しく機械検証を当てる**。

### 4.8 ⭐ 再発射の前に前任の生存を確認する (新設)

前 relay で、検証が「成果物なし」と出たのを受けて再発射した結果 **2 体が同じ task に付き衝突寸前になった**
⟹ **再発射の前に前任が生存していないかを確認する** (⚠ **手順の穴であって agent の失敗ではない**)。

### 4.9 ⭐ 資産は repo 側に置く — scratchpad は消える (⚠ 実際に消えた)

- **置き場所**: 数値検証器 = `docs/shannon/verifiers/` / 型検査 probe = `docs/shannon/probes/<leg>/` /
  一次文献の取得コマンド = [`lit-fetch.sh`](lit-fetch.sh) (⚠ 抽出テキストは repo が public なのでコミットしない)。⚠ **判定基準の SoT は CLAUDE.md `## Scratchpad`** — ここには複製しない。
- **前 relay / 本 relay の scratchpad 資産 (一次文献 16 本 + 検証器 5 系統 + N2 の probe 13 本) は 2026-08-05 の再起動で消滅済**。仕様と依存先の SoT = [`bc-facts.md`](bc-facts.md) `## 資産 (T3c 以降)` (⚠ **決定 = 再生成しない。必要になった leg が仕様から作る**)。
- ⚠⚠ **旧対策「ブリーフに絶対パスを書く」は無効** (パスごと消える) ⟹ **再発明しない**。

---

## 5. leg 予算と配分

**20 leg (N0–N19)**。⚠ **前 relay の枠構造は配分事故を防ぐことに成功した** (可変枠 14 leg が 14 本
とも `(C2)`、§6-4 不発火) ⟹ **枠構造そのものを継承し、変更は 1 点だけ** — 形式化枠 3 のうち 1 leg を **early gate として先頭近くへ移す** (§2、採否の根拠は §7 判断ログ 2)。

| 枠 | leg | 側 | 内容 |
|---|---|---|---|
| **N0** | 1 | gate | **資産の生存確認 + 在庫の差分取り直し**。§4.9 の scratchpad 資産が生きているかを機械で確認し、欠けていれば再生成する。あわせて (C2) 在庫 [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md) のうち **N1 / N2 が消費する節だけ**を結論形で取り直す。⚠ **在庫は `mathlib-inventory` の担当ファイルへ書く。本 plan には書かない**。⚠ **消滅後の現況は §4.9** |
| **N1** | 1 | gate | **入口 1 点の第一手** (§3 の反証試行)。⚠ **GO / NO-GO のどちらでも軸が 1 本確定する**設計なので、ここで「判定不能」に落ちたときだけ §6-1 が発火する |
| **N2** | 1 | **形式化債務** (⚠ **形式化枠 3 の内数 1/3**) | ⭐ **early gate = 量化子の段の員数を測る**。標的は facts `## M19 (T3b)` M19-5 の (a)(b) — 領域としての `Thm7(W)` / UV 外界の def と `∃p ∀T_J ∃aux` の量化子。⭐ **設計軸 (在庫 §2.5) は N2 が決着させた** — **3 レートで受け皿を新設し、`R₀ = 0` スライスは派生 def として同時に置く** (SoT = [`bc-t3c-n2-estimate.md`](bc-t3c-n2-estimate.md) §1.1–§1.2。監査 軸 4 が一次文献を再取得し行番号まで再現、訂正なしで生存)。⚠ **スライスの実効コンパクト性が 3 レート版から従うかは未決** (子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) §4 が SoT)。⚠ **成果物は「員数と段のリスト」であって実装ではない** (実装が始まったら §6-5) |
| **⭐ A1 (N3–N5)** | **3** | **(C2)** | ⚠ **新設 = 探索アーク 1 本目**。**アーク内の個別 leg に決着を要求しない** (§5.3)。⚠ **先に置く** — 後半に回すと「残 leg が少ないので決着可能な小問へ」の圧力が必ず戻る (前 relay で実測された形) |
| **N6–N12** | **7** (⚠ **7 leg とも消化済**) | **(C2)** | **判定枠 (⚠ 予約枠)**。[探索 + gate] → [構築 + probe] → [層 3 へ載せる] の 3 段 1 組で回す。⚠ **この枠を他の用途に流用しない**。⚠ **A1 と A2 の間に挟む** — A1 の産物が判定枠で使えるかを見るため、**アーク 2 本を連続させない** |
| **⭐ A2 (N13–N15)** | **3** | **(C2)** | ⚠⚠ **N13 (1 leg 目 = novelty gate) で `redundant` 死 ⟹ 枠としては消滅した**。**N14 = アークを閉じる記録 leg** (側 = `記録`) / **N15 は再配分** (下記) ⟹ ⚠ **3 本目のアークへは横滑りしない** (§6-6、§7 判断ログ 10) |
| **N15** | 1 (⚠ **消化済**) | **(C2)** | ⭐ **再配分 = 判定枠 第 3 組 第 1 段 `[探索 + gate]`**。標的 = 「**`C` の閉形 (= 比較の相手) が [probc] の外に書けるインスタンスが在るか**」1 点 (⚠ **N12 §4 の候補 2 (他インスタンス) と候補 4 (`e > h(p)` 側) を 1 本の gate に束ねる** — 2 候補は同一のボトルネックの裏表。§7 判断ログ 10) ⟹ ⭐ **着地 = GO** (§5.1) |
| **N16** | 1 (⚠ **消化済**) | **(C2)** | ⭐ **第 2 段の上流 gate = 「現行 [probc] more-capable インスタンスで `UV = C` か」** (N15 成果物 §6-8 / N15 監査 §5-5 が名指しした開いた問い)。⚠⚠ **第 2 段 `[構築 + probe]` は N16 では取らない** — ⚠ **§7 判断ログ 10-(4) の条文からの逸脱ではなく、第 2 段が立つ土台の妥当性検査を先に置くものである** (根拠と引き受けたリスクの SoT = §7 判断ログ 11)。⚠⚠ **本格的な棚卸し leg も取らない** (下行の明示的な逸脱)。**成果物 = [`bc-t3c-n16-uv-gate.md`](bc-t3c-n16-uv-gate.md)** (⚠ **§0 / §1 は起票時に凍結され事後に 1 文字も動いていない**)。⭐ **着地 = 判定 `UV = C`** (逐語の SoT = §5.1 の N16 着地ブロック + facts `## N16 (T3c)`) |
| **N9 / N15 → 棚卸し** | (内数) | — | ⚠ **改定で移設** — 棚卸しは **N12 と N16** に置く。⚠ **アーク終端 (N5 / N15) と同じ leg に置かない** (自己採点になる)。⚠⚠ **N16 の分は取らない = 明示的な逸脱** — 代替として **N16 着地時に「relay 終端の正直な棚卸し」を最小形で残す義務**を負う (§7 判断ログ 11) |
| **N17 / N18** | 2 | 形式化 | **形式化枠の残り (2/3・3/3)**。⚠ **N2 で §6-5 が発火済** ⟹ **子 plan** [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) **へ切り出し済**で、その 3 単位のうち **B (3 レート `Thm7(W)` の領域層)** が type-check done まで最短で、**探索アークが置く新しい def の受け皿にもなる** (§5.3 R4) ⟹ **アークの産物と抱き合わせて使う**。⚠ **A / C は本 relay の枠外**である |
| **N19** | 1 | 記録 | 記録 — proof-log + metrics + 収穫の書き上げ + 正直な棚卸し |

**消化数 (N16 着地時点)**: **20 leg 中 17 消化 (N0 … N16) / 残枠 3** = 形式化 2 (N17 / N18) + 記録 1 (N19)。**内訳**: 可変枠の消化 = **14** (N3 / N4 / N5 = A1、N6 … N11 = 判定枠、N12 = 判定枠の棚卸し、N13 = A2、N14 = A2 を閉じる記録、N15 = 判定枠 第 3 組 第 1 段、N16 = 第 2 段の上流 gate) / gate 2 (N0 / N1) / 形式化枠の内数 1 (N2) ⟹ **可変枠 14 は使い切った**。

**⭐ §6-4 の配分カウンタ (N16 着地時点で実際に引いた)** — §5.1 の数え方 (⚠ **N3–N16 の行だけ**を数える):

```
rg -c '^\*\*leg 冒頭宣言 \(N([3-9]|1[0-6])\)\*\*: 側 = \(C2\)' docs/shannon/bc-open-problem-t3c-plan.md
```

⟹ **実測値 `12` / 可変枠の消化 = 14** ⟹ **`12 ≥ 14/2` ゆえ §6-4 は不発火** (⚠ **予約枠 14 の半分 = 7 にも既に届いている**ので、その条項でも発火しない)。⚠ **N12 / N14 は側 = `記録` ゆえ起票行を増やしていない** (§7 判断ログ 9 / 10)。⚠ **gate 2 本 (N0 / N1) と N2 は可変枠ではないので数に入らない**。⚠⚠ **カウンタは起票行を数える** ⟹ **必ず消化数と対にして読む** (N16 の起票で `11 → 12` になり、**その着地で対にして読む分母が `13 → 14` になった**)。⚠ **§5.2-3 (2 leg 連続で側が `(C2)` 以外) は N14 = `記録` → N15 = `(C2)` → N16 = `(C2)` ゆえ不発火**である。

**⭐ 次に来るもの (⚠ cap 延長後の状態、2026-08-09)**: ⭐ **ユーザー決定で relay の cap は 20 → 23 になり、残枠 3 (N17 / N18 = 形式化枠 = 子 plan の単位 B / N19 = 記録) を走らせる** (SoT = §7 判断ログ 12。⚠⚠ **`GOAL-CHANGE` ではない** / ⚠ **relay の cap と plan の leg 予算は別勘定という判断ログ 11-(7) の条項は維持される** / ⚠⚠ **§6-3 の発火ではない**)。⚠⚠ **§6-5 は既に「収まらない」と判定済ゆえ N17 / N18 は §0 の Lean 側には届かない** (§7 判断ログ 12-(4))。⚠⚠ **第 3 段 `[層 3 へ載せる]` は枠が無く 3 段そろわない** (§7 判断ログ 10-(5) の明示的な逸脱)。⚠ **探索アーク枠 6 leg は 2 本とも終わっており 3 本目は起票しない** (⚠ **逐語の SoT = facts `## N13 (T3c)` の N13-j**)。⚠⚠ **残った受け皿の型つき一覧は下の「relay 終端の棚卸し」が持つ** — ⚠ **どれに leg を配るかは未反映である** (⚠ **着地済の成果物 md に「後続 leg が何をするか」を埋め込まない**)。

- **(α) / (β)** — ⭐ **N9 / N10 が消化した。`ε` は厳密に 0 である** (⚠ **N10 が再導出したのは支持関数の一致まで**で領域の等式は N9 の連鎖の引用。射程 = §7 判断ログ 7)。⭐ **その解析的入力 `δ ≥ 0` は N11 が層 3 へ載せた** — ⚠⚠ **載ったのは `ℝ` 上の 1 変数不等式 1 本であって [probc] の判定そのものではない**。
- **(γ) `(R2)` / `(R3)` を層 3 へ載せて `h_Thm7(0,1,t)` の上界そのものを Lean に立てる** — ⚠ **候補として残る (N16 の NO-GO 分岐の受け皿)**。⚠ **§7 判断ログ 5 のテスト可能な線 (載せてよいのは本 relay の `(C2)` leg が立てた命題だけ / 領域 def に手を出したら子 plan へ回す) を先に引くこと**。
- **他インスタンス / `e > h(p)` 側** — ⭐ **N15 が 1 本の gate に束ねて取った (判定 = GO)**。⚠ **`e > h(p)` 側 (候補 4) は死んだ** (死因 = `probe-failed` = **比較の相手が無い**。⚠⚠ **「その帯に分離が無い」ではない**) / ⚠ **他インスタンス (候補 2) は生きているが起票時の中身とは別物になった** (生きたのは積ですらない和 (⊕) チャネル側)。**逐語の SoT = facts `## N15 (T3c)` + 成果物 §5.2**。⚠⚠ **`e > h(p)` 側の反証条件 3 本は [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md) §4.2 が SoT** ⟹ **ここへ複製しない / 書き換えない**。

⚠⚠ **「尽きた」と書かない** — 残るのは **一般 BC / 他インスタンス / (γ) / `e > h(p)` 側**である。
⚠ **`Thm7|_{R0=0} = C|_{R0=0}` は `Thm7 = C` を含意しない** (N6-n の反例 2 本) / ⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無く、排除もされていない**。

### ⭐⭐ relay 終端の棚卸し (最小形。⚠ **§7 判断ログ 11-(4) が課した義務の履行**。⚠⚠ **本格的な棚卸し leg の代替ではない**)

⚠⚠ **relay の cap と plan の leg 予算は別勘定であり、混ぜて書いてはならない** — **N16 は cap 20 の下では
最後の leg だった**が、⭐ **ユーザー決定 (2026-08-09) で cap は 23 へ延長され N17 / N18 / N19 が走る**
(§7 判断ログ 12)。⚠ **条項そのものは維持される** (幅が一致したのは偶然である)。⚠⚠ **これは §6-3
(20 leg を使い切って未達) の発火ではない**。⚠⚠ **以下の到達点は cap 延長で 1 文字も変わらない** —
**N16 着地時点の正直な棚卸しである**。

**本 relay が §0 のゴールに対して到達したところ (⚠ §0.1-2 により中間結果をゴールの代替にしない)**:
⚠⚠ **§0 のゴール (肯定側か否定側の決着 + Lean の proof done) には到達していない**。可変枠 14 leg が動かしたのは
**[probc] という 1 インスタンス族の上での `Thm7` と `C` の比較**であり、⚠⚠ **`Thm7 ⊋ C` の材料は 1 つも出ておらず、
排除もされていない**。⚠ **層 3 に載ったのは `@[entry_point]` 5 本ちょうど** (N8 の 3 本 + N11 の 2 本) で、
⚠⚠ **レート領域の包含については Lean は 1 文字も言っていない** (載ったのは制約右辺どうしの不等式と
`ℝ` 上の 1 変数不等式である)。⚠ **探索アーク 2 本はいずれも §0 に向いた claim を 1 本も動かしていない**
(⚠ **これは §7 判断ログ 4 が引き受けたリスクの実現であって改定の失敗ではない**)。

**⭐ N16 が §0 に対して持つ意味 (正直に)**: **減算方向の較正**である。⚠⚠ **判定枠 3 組 (N6 … N11) が §0 に対して
持っていた情報量が小さかったという測定**であって、⚠ **それらの判定が誤りだという主張ではない**
(⚠ **N16 でも監査でも 1 つも覆っていない**)。

**残った受け皿 (⚠⚠ 「尽きた」と書かない。⚠ どれに leg を配るかは未反映である)**:

| 受け皿 | 型 | 状態 |
|---|---|---|
| 第 2 段 `[構築 + probe]` = [glnsum] の和 (⊕) チャネル族 | `[構築 + probe]` | ⚠⚠ **N16 は 1 mm も触っていない** (facts `## N16 (T3c)` N16-f / 監査 §6-12) ⟹ **土台の妥当性検査だけが済んだ状態**である |
| (γ) `(R2)` / `(R3)` の層 3 化 | `[層 3 へ載せる]` | ⚠ **消していない** (§7 判断ログ 10-(6))。⚠ **判断ログ 5 のテスト可能な線を先に引く** |
| 一般 BC への持ち上げ | `障害の構造` | ⚠ **`restatement` 公算が最も高い** (facts `## N6 (T3c)` N6-j。⚠ **消滅ではない**) |
| `e > h(p)` 側 | 死因 `probe-failed` | ⚠⚠ **「その帯に分離が無い」ではなく「比較の相手が無い = 測れない」** (facts N15-f)。⚠⚠ **`e < h(p)` の帯を同じ理由で捨てない** — 理由が違う |
| 形式化枠 N17 / N18 | `形式化債務` | 子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) の**単位 B** に割当済 (⚠ **中核は 0 本しか書かれていない**) ⟹ ⭐ **N17 で着手 (2026-08-09、起票 = [`bc-t3c-n17-unit-b.md`](bc-t3c-n17-unit-b.md))**。⚠⚠ **§0 の Lean 側には届かない** |

### 5.1 ⚠ leg 冒頭の宣言規約 — 「どちらへ流れているか」を leg 単位で見えるようにする

**各 leg は起票時 (ブリーフ) と着地時 (本節への追記) の両方に、次の 1 行を同じ形式で書く**:

```
**leg 冒頭宣言 (N<n>)**: 側 = <(C2) | (C1) | 形式化債務 | 記録> / 動かすもの = <§0 のゴールに対して何を動かすか 1 行>
```

- **側の語彙は 4 つだけ**: `(C2)` / `(C1)` / `形式化債務` / `記録`。⚠ **`gate` は独立の側ではない** —
  gate leg も何の側の gate かを書く (N0 / N1 は `(C2)` の gate、**N2 は `形式化債務`**)。
- **書けない leg は起票しない** (§0.1-3)。⚠ **探索アークの leg (N3–N5 / N13–N15) はこの条項の
  適用対象外** — アークは**アーク単位で** §5.3 の宣言を書き、**個別 leg には決着を要求しない**
  (§7 判断ログ 4 でユーザー承認済)。⚠ **ただし冒頭宣言の 1 行は書く** (側 = `(C2)`。「動かすもの」
  の欄には**そのアークの対象**を書き、判定を書かない) ⟹ **§6-4 のカウントは無傷**。
- **数え方**: 本ファイルに `rg -c '^\*\*leg 冒頭宣言 \(N[0-9]+\)\*\*: 側 = \(C2\)'` を当てる。
  ⚠ **この数が §6-4 の入力である**。⚠ **上の雛形は `<…>` 形なのでカウントに混じらない**
  (前 relay は雛形行と条項が混じって数え直しが要った)。⚠ **gate 2 本 (N0 / N1) は可変枠ではない**
  ので、§6-4 を引くときは **N3–N16 の行だけ**を数える。

**leg 冒頭宣言 (N0)**: 側 = (C2) / 動かすもの = 前 relay の資産 (検証器・一次文献) の生存を機械で確認し、N1 / N2 が消費する在庫だけを結論形で取り直して、両 leg が在庫の空振りで溶けないようにする
> 着地 = [`bc-t3c-c2-inventory.md`](bc-t3c-c2-inventory.md)。資産は**全件生存・実行再現** (⚠ その後 §4.9 のとおり消滅) / **(α) は `iCondIndepFun` が受け皿 / (β) だけが本物の空白 / (γ) は領域 def 11 本がいずれも実効コンパクト性の証拠を運ばない** / 一次文献 diff = **ゴールの主語は 2 レート**、`Thm7` / `Thm8` / GK-Bound / UVW 外界は 3 レート。⚠ **軸は決着させていない** (決めるのは N2)。

**leg 冒頭宣言 (N1)**: 側 = (C2) / 動かすもの = `C` の和レート面の端を `Thm7` が超えられるかを決着させ、GO なら `Thm7 ⊋ C` (= (C2) の還元先が死ぬ)、NO-GO なら「`C` 側の天井が `Thm7` 側にも立つ」を得て還元先を 1 段強くする
> 着地 = facts [`bc-facts.md`](bc-facts.md) `## N1 (T3c)`。**判定 = NO-GO** — `(18i) ≥ SR_C` を満たす任意の Thm7 witness で `(18b) ≤ C + S(μ*)` が **単一の `T_J` だけで、適格性 (19)/(20a)/(20b) を 1 本も消費せずに**立つ ⟹ **`Thm7` の和レート面は `C` の和レート面に含まれる** (⚠ **面 1 枚 / 1 インスタンス** / ⚠ **`Thm7 = C` でも `Thm7 ⊋ C` の材料でもない** / ⚠ 機械が閉じた余裕は N1audit 訂正後で `5.211e-7`、**厳密 0 ではない** — N1-i / N1-j)。⚠ **§6-1 は不発火**。

**leg 冒頭宣言 (N2)**: 側 = 形式化債務 / 動かすもの = §0 が要求する Lean proof done 側で最も重い段 (領域としての `Thm7(W)` / UV 外界の def と `∃p ∀T_J ∃aux` の量化子の段) の**員数を初めて測り**、残る形式化枠 2 leg (N17/N18) に収まるかを §6-5 の判定として返す
> 着地 = [`bc-t3c-n2-estimate.md`](bc-t3c-n2-estimate.md) / 敵対的独立監査 = [`bc-t3c-n2-audit.md`](bc-t3c-n2-audit.md) / facts `## N2 (T3c)`。**判定 = 収まらない** ⟹ **§6-5 が発火した** (⚠ **`GOAL-CHANGE` ではない** — §0 は不動で、届かないという事実を書くだけである)。**監査で判定は生存・訂正 5 件**。⚠⚠ **判定を支えているのは員数ではなく強度 diff である** / ⚠⚠ **否定側 (§3 の T3-β) の形式化員数は誰も測っていない** / ⚠ **ⓓ (壁) は 0 件** — 逐語と射程の限定は §7 判断ログ 3。

**leg 冒頭宣言 (N3)**: 側 = (C2) / 動かすもの = A1 (§5.3) の対象 — witness ごとの適格集合 `E(w)` と、レート点を運ぶ族の被覆数 `κ(R)` (⚠ **アーク条項により判定は書かない**)
> 着地 = [`bc-t3c-a1-novelty-gate.md`](bc-t3c-a1-novelty-gate.md)。**判定 = `novel`** (⚠ 3 語語彙の 1 語であって「使える」ではない。⚠ **novel は狭い** — 台帳の当該命題は逆向きにも損失なく翻訳され、潰れなかったのは `κ(R) ≤ 2` だけである)。⚠⚠ **§0 への payoff は 3 点とも取れなかった** — とくに **`κ` の有界性は `## L2 (T3)` 行 6 の実効コンパクト性より厳密に強く、後者は既に無条件の定理**ゆえ計算可能性側に何も足さない (逐語 3 点は成果物が SoT)。

**leg 冒頭宣言 (N4)**: 側 = (C2) / 動かすもの = A1 (§5.3) の対象 — `SR_C` を運ぶレート点で被覆数 `κ = 2` が実現するか (⚠ **アーク条項により判定は書かない**)
> 着地 = [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) + 検証器 `verifiers/kappa2_probc.py` (11/11) / 敵対的独立監査 = [`bc-t3c-a1-kappa2-audit.md`](bc-t3c-a1-kappa2-audit.md) + 独立検証器 (12/12)。**監査 = 訂正あり生存** (訂正 7 件、⭐ うち 1 件は (K3) 後半を反例で覆し、その反例 witness が §5-d の駒として資産化された)。**産物 2 本** = **(K1) 有限被覆の構造定理 (⚠ インスタンス非依存)** = `κ < ∞` なら最小被覆の各成員は (19) を全 `μ` で満たす / **(K2)** [probc] の `SR_C` 点で有限被覆は矛盾 ⟹ **`κ = ∞`**。⚠⚠ **恒等式による判定であって数値掃引ではない** (§4.5)。⚠ **測ったのは 1 レート点・1 インスタンス・full witness 粒度の 3 点に限る** / ⚠ **(K1) は `κ = 2` 一般を潰さない** (§5-d は未決)。

**leg 冒頭宣言 (N5)**: 側 = (C2) / 動かすもの = A1 (§5.3) の終端 — 監査の訂正を成果物へ反映し、再現本数を `bc-facts.md` へ台帳化して §5.3 の閾値表でアークの継続可否を出し、閾値そのものを較正する
> 着地 = facts `## A1 (T3c)` (claim 6 行) + 本 plan §5.3 の**終端判定**と**閾値の較正** (逐語はそちらが SoT)。**アーク A1 の判定 = `続行`** (再現本数 7 本、文言基準でも 6 本) ⟹ ⚠ **§6-6 は不発火。昇格条件は満たさない**。⚠⚠ **`続行` を「§0 に近づいた」と読んではならない** — N3 の novelty gate は §0 への payoff を **3 点とも不成立**と判定している。⭐ 一方 **(K1) はインスタンス非依存の構造定理**であり、**41+5 leg で初めて出た「新しい証明機構」型の産物**である。

**leg 冒頭宣言 (N6)**: 側 = (C2) / 動かすもの = N1 が閉じた和レート面**以外**の `C` の境界を棚卸しし、N1-b の無条件部分がどの面をすでに無料で閉じているかを恒等式レベルで確定させて、`Thm7 ⊋ C` を狙える面が残っているかを gate する
> 着地 = [`bc-t3c-n6-boundary-gate.md`](bc-t3c-n6-boundary-gate.md) + facts [`bc-facts.md`](bc-facts.md) `## N6 (T3c)` (逐語の SoT) + 検証器 `verifiers/capacity_probc.py` (**18/18**) / 敵対的独立監査 = [`bc-t3c-n6-audit.md`](bc-t3c-n6-audit.md) + 独立検証器 `verifiers/n6_audit_probc.py` (**20/20**)。⚠ **判定 leg ではない** (GO / NO-GO は出していない)。
> **監査 = 一部覆った / 訂正 6 件** — 覆ったのは「未着手の面は `R0=0` の肩 1 枚に落ちた」で、**「`R0 > 0` は独立の面ではない」は導かれない** (有理数の明示多面体による反例 2 本。N6-n) ⟹ **未着手の面は 2 枚**であり (`R0=0` の肩 = N7 / 錐 = N9 で決着)、1 枚に見えたのは**順序を固定した結果**である。**棚卸しの結果**: **5 枚が無料で閉じ** (N1-b を 1 本も使わない)、**F-SUM 値の 1 枚は条件つき**。
> ⚠⚠ **「無料で閉じた」を「`Thm7 ⊆ C` に近づいた」と読んではならない** — いずれも支持値が 1 点で一致するだけである。⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無い**。**§4.4-1 の着手前反証 = 成功、ただし (γ) は生き返らない** (N6-j)。

**leg 冒頭宣言 (N7)**: 側 = (C2) / 動かすもの = N6 の gate が渡した面 F-SH1 (`R0 = 0` スライスの肩) を、`conv(f_t)(一様)` の証明書で決着させ、`Thm7` の `R0=0` スライスが `C` のそれに含まれるか (NO-GO) / 緩和が `h_C` を厳密に超えるか (GO 候補) を判定する
> 着地 = [`bc-t3c-n7-shoulder-certificate.md`](bc-t3c-n7-shoulder-certificate.md) + facts [`bc-facts.md`](bc-facts.md) `## N7 (T3c)` + 検証器 `verifiers/shoulder_certificate_probc.py` (**18/18**) / 敵対的独立監査 = [`bc-t3c-n7-audit.md`](bc-t3c-n7-audit.md) + 独立検証器 `verifiers/n7_audit_probc.py` (**20/20**、破る側に振った試行 35 系統)。
> **判定 = NO-GO (許容差 `ε = 2.0786e-07` つき)** — `Thm7|_{R0=0} ⊆ C|_{R0=0} + [0,ε]²` (逆向きは N6-p から無条件)。**監査 = 訂正あり生存** (訂正 4 件はどれも `ε` を動かさない)。⭐ **収穫** = 面が `t` を含まない 3 変数の不等式 1 本へ潰れた (= N10 の標的そのもの)。
> ⚠⚠ **`ε` は N10 が厳密 0 に置き換えた** — **N7 の値は上界として妥当だが緩い**のであって誤りではない ⟹ **本ブロックの `ε` 依存の記述は N10 着地ブロックが上書きする**。⚠ **反証条件 3 本の結果・還元の可動部・射程の限定・監査が未確認のまま残した項の逐語は上の 2 成果物と facts が SoT — ここへ複製しない**。
> ⚠ **スライスが閉じても `R0>0` 方向は従属しない** (N6-n) ⟹ 当時は錐 =「`D = C` か」の 1 枚が残った (N9 で決着)。⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無い** / ⚠ **これは「近づいた」ではなく候補が 1 枚減っただけ**である。

**leg 冒頭宣言 (N8)**: 側 = (C2) / 動かすもの = N7 が (18b)/(18i) の 2 本だけで立てた方向 (0,1,t) の上界の連鎖を、マルコフ性と情報量だけで述べられる形のまま Lean の層 3 へ載せ、散文側の主張が機械で通るかを確定させる
> 着地 = `InformationTheory/Shannon/BroadcastChannel/OuterBoundTransport.lean` の**新規 6 宣言** (def 3 + `@[entry_point]` 定理 3) + facts [`bc-facts.md`](bc-facts.md) `## N8 (T3c)`。commit = `51d1ccb1` (実装) / `72aaef9a` (監査) / `d9fa6558` (style gate)。⚠⚠ **載ったのは N7 §2.1 の `(R1)` 1 本だけである** — ⚠ **冒頭宣言と commit 件名が書く「上界の連鎖」は過大であり、以後そう書かない**。`(R2)` (`U' := (U,W)` の事後分解) と `(R3)` (2 つの緩和) は**載っていない** ⟹ **`h_Thm7(0,1,t)` の上界そのものは Lean に無い**。
> ⚠⚠ **載ったのは制約の右辺どうしの不等式**であって、**レート領域の包含については 1 文字も言っていない** (⚠ **N11 も同型の限定を負う**)。⭐ **散文より強い形で載った** — 主定理はマルコフ仮説を 1 本も持たず `t ≤ 1` も要らない。⚠⚠ **ただしそれは不等式の話であって、方向 `(0,1,t)` の支持値の上界として読む LP の読みは `0 ≤ t ≤ 1` でしか成り立たない** (⚠ **混ぜて書かない**)。⚠ **署名に load-bearing hypothesis は無い** (監査が `t = −1` / `hmarkov` 落としの明示反例で `0 ≤ t` と `hmarkov` の必要性を確認)。
> ⭐ **§4.6 の敵対的独立監査 = [`bc-t3c-n8-audit.md`](bc-t3c-n8-audit.md)。判定 = 訂正あり生存** — 反証 5 本中 2 本が成立し、**訂正 4 件はすべて docstring の読ませ方**で ⚠ **Lean の命題・署名・証明は 1 文字も変わっていない**。**style gate = PASS**。⚠ **受け皿 def の由来 (M17/M18 であって M16 ではない) と監査の射程の限定 (照合した逐語は N7 証明書経由であって `auxrec.txt` そのものではない) は同成果物が SoT**。⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無い**。

**leg 冒頭宣言 (N9)**: 側 = (C2) / 動かすもの = 未着手のまま残った最後の面 (`0 < λ0 < λ1+λ2` の錐) を「`D = C` か」の形で §4.5 の道具に掛け、[probc] 上の入口 1 点について `Thm7` 側と `C` 側の比較がこの錐でも閉じるか / 射影ルートでは錐に届かないかを確定させる
> **判定 = 錐 `0 < λ0 < λ1+λ2` は恒等式により閉じた** (⚠ 当時は `ε = 2.0786e-07` つき ⟹ **N10 が厳密 0 に置き換えた**)。**起票の反証条件 3 本はいずれも不発火** (⚠ **条件文と結果 = [`bc-t3c-n9-cone-gate.md`](bc-t3c-n9-cone-gate.md) §3.2 / 見立ては同 §0。⚠ 起票ブロックの逐語は git 履歴が持つ**) / **§4.6 の敵対的独立監査 = 訂正あり生存** (⚠ **主判定を動かすもの 0 本**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N9 (T3c)` + 成果物**。
> ⚠⚠ **本 leg で `Thm7 ⊋ C` の材料は原理的に出ない** (起票の片側性) — 出ていないし、⚠ **排除もされていない**。⚠ **決着は [probc] 1 インスタンスの上の話**であって `Thm7 = C` 一般ではない。⚠ **本 leg は層 1 + 層 2 のみで Lean には 1 つも載せていない**。⚠ **「尽きた」ではない** — **gate の出力 = (β) 1 本を N10 へ渡した**。

**leg 冒頭宣言 (N10)**: 側 = (C2) / 動かすもの = N9 の gate が 1 本に絞って渡した (β)「`ε` を厳密 0 にする」を [構築 + probe] で攻め、[probc] 上で `Thm7 = C` が厳密に立つのか、立たないならどの構造が阻むのかを確定させる
> **判定 = 閉じた (恒等式による)** — [probc] 上で `Ω(t) = d*_t` が全 `t ∈ [0,1]` で厳密に成立 ⟹ ⚠⚠ **`ε` は厳密に 0 である** (⚠ **N7 の `2.0786e-07` は上界として妥当だが緩いのであって誤りではない**)。⚠⚠ **本 leg が再導出したのは支持関数の一致まで**であり、**領域の等式は N9 の連鎖の引用**である。**起票の見立て 6 本と較正の SoT = [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md) §0 / §3.5、反証条件 3 本の SoT = [`bc-t3c-n9-cone-gate.md`](bc-t3c-n9-cone-gate.md) §4.2** (⚠ **複製も書き換えもしない**) / **監査 = 訂正あり生存** (⚠⚠ **主判定 `ε = 0` を動かすもの 0 件**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N10 (T3c)` + 成果物**。
> ⭐ **決着の射程は監査 訂正 4 で広がった** — 効いている条件は `δ ≥ 0` (⟺ `e ≤ h(p)`) の **1 本だけ**で、**半空間 `{(p,e) : 0<p<1/2, e ≤ h(p)}` の全体で成立する** (⚠ **`Ω` の水準まで** / ⚠⚠ **決着は依然 [probc] の族の上に留まり、一般 BC へは持ち上がらない**)。
> ⚠⚠ **`Thm7 ⊋ C` の材料は本 leg でも 1 つも出ていない** — 出たのは**一致**の側である。⚠ **本 leg は層 1 + 層 2 のみで Lean には 1 つも載せていない**。⚠⚠ **「尽きた」ではない**。**gate の出力 = 「more capable を外したところに的があるか」(`e > h(p)` 側) 1 本を後続へ渡した** (⚠ **その反証条件 3 本は成果物 §4.2 が SoT。複製も書き換えもしない**。§7 判断ログ 8)。

**leg 冒頭宣言 (N11)**: 側 = (C2) / 動かすもの = §0 が要求する Lean 側 (層 3) を、N10 の判定を支えている唯一の解析的入力 `δ ≥ 0` の分だけ動かし、判定枠 第 2 組が散文で閉じた解析核が機械検証された状態にする
> **判定 = 閉じた** — `@[entry_point]` 定理 **2 本** (`log_two_mul_binEntropy_binConv_sub_binEntropy_le` = 起票の逐語形 / `binEntropy_binConv_sub_binEntropy_le` = 半空間版) が `InformationTheory/Shannon/BroadcastChannel/MoreCapableBinary.lean` に **0 sorry / 0 `@residual`** で載り `@audit:ok` (tier 1)、**style gate = PASS**。**起票の反証条件 3 本はいずれも不発火** (⚠ **発火状況と見立ての当たり外れ = [`bc-t3c-n12-stocktake.md`](bc-t3c-n12-stocktake.md) §5 (b)/(c)、結果の逐語は [`bc-t3c-n11-more-capable-lean.md`](bc-t3c-n11-more-capable-lean.md)。⚠ 起票ブロックの逐語は git 履歴が持つ**) / **監査 = 訂正あり生存** (⚠⚠ **主判定を動かすもの 0 件**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N11 (T3c)` + 成果物**。
> ⚠⚠ **落としてはならない限定 (逐語の SoT = 成果物 §4)**: **レート領域の包含については 1 文字も言っていない** (載ったのは `ℝ` 上の **1 変数不等式 1 本**で、Lean 側に `Thm7` も `C` も `Ω` も `d*_t` も現れない) / **[probc] の判定そのものは Lean に無い** ⟹ ⚠⚠ **「`ε = 0` が機械検証された」とは言えない** / ⚠ **`Thm7 ⊋ C` の材料ではなく、排除もされていない**。
> ⭐ **監査 訂正 6 の効き (⚠ 起票側は 1 文字も書き換えず効きを着地側に書く作法)** — 冒頭宣言の「**唯一の**解析的入力」という枠付けは **N10 §2.4 が併用する `d*_t` の達成性**を勘定に入れていない ⟹ ⚠ **「唯一の」を無条件には書かない** (⚠ **N10 からの継承であって N11 が作った誤りではない**)。

**leg 冒頭宣言 (N12)**: 側 = 記録 / 動かすもの = 判定枠 6 leg (N6 … N11) が §0 のゴールに対して何を確定させ何を確定させていないかを、SoT への裏取りと Lean 側の機械再導出つきで 2 欄に分けて確定させ、A2 / N16 の投入先を型つきで選べる状態にする
> **棚卸しは閉じた** — 判定枠 6 leg (N6 … N11) の確定 / 未確定を 2 欄に分けて確定させた。⚠ **判定 leg ではない** (新しい GO / NO-GO を 1 本も出していない)。**起票の反証条件 3 本** (⚠ **発火状況と見立ての当たり外れ = [`bc-t3c-n12-stocktake.md`](bc-t3c-n12-stocktake.md) §5 (b)/(c)。⚠ 起票ブロックの逐語は git 履歴が持つ**) — **(1)(3) 不発火 / ⚠⚠ (2) は一度発火したが監査 訂正 4 で不発火へ反転** (候補 1 = 一般 BC に `障害の構造` の型が付いた) / **監査 = 訂正あり生存** (⚠⚠ **主判定を動かすもの 1 件**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N12 (T3c)` + 成果物** (⭐ **同 §4 = 残件の型つき候補表 4 本。A2 / N16 の配分はこの表を入力にした**)。
> ⚠⚠ **落としてはならない限定**: **「尽きた」ではない** / **`Thm7 ⊋ C` の材料は依然 1 つも無く、排除もされていない** / **レート領域の包含については Lean は 1 文字も言っていない** / **N10 §2.1–§2.4 の恒等式群は Lean に 1 本も載っていない**。
> ⭐ **本 leg が新たに名指しした運用上の穴 2 点** — **N8 には反証条件も見立ても書かれていなかった** (⚠ **実害の有無は判定していない**) / **§4.4 の「締める方向は全件覆る」は判定枠では再現していない** (⚠⚠ **ただし §4.4-1 の着手前反証の義務そのものが交絡であり、前 relay の母集団とは比べられない**)。

**leg 冒頭宣言 (N13)**: 側 = (C2) / 動かすもの = A2 (§5.3) の対象 — 計算可能解析の (β) 語彙を BC 側へ持ち込む集合水準の def 群 (⚠ **アーク条項により判定は書かない**)
> **判定 = `redundant`** — 台帳がこの機構について述べている命題は **7 本中 7 本**が (β) 語彙なしの既存語彙 (`REPred` + 有理球 + 古典 `IsCompact`) へ逆向きにも損失なく潰れ、**潰れなかった部分は 0** である ⟹ ⚠⚠ **起票の反証条件 (1) が発火した** = **アーク A2 は 1 leg 目で死んだ** (⚠ **起票の選定理由・反証条件 2 段・見立ての逐語 SoT = §5.3 の「アーク宣言 (A2)」+ [`bc-t3c-a2-novelty-gate.md`](bc-t3c-a2-novelty-gate.md)**) / **§4.6 の敵対的独立監査 = 訂正あり生存** (⚠ **主判定を動かすもの 0 件**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N13 (T3c)` (節内タグ `N13-a` … `N13-k`) + 成果物**。
> ⚠ **判定の射程は「新しい対象としての地位」だけ**である — ⚠ **「この層は要らない」ではない** (既知の散文経路 `## L2 (T3)` 行 6 を Lean で閉じるなら中核 1–7 は要る)。⚠⚠ **§0 への payoff は 3 点とも不成立** — 証明義務は **0/7** のまま / (C2) の残りは `Thm7 = C` の厳密一致 1 点で (β) 語彙はそこに 1 文字も言わない。⚠ **文献 diff は 0**。
> ⚠⚠ **`Thm7 ⊋ C` の材料は本 leg でも 1 つも出ておらず、排除もされていない** / ⚠ **レート領域の包含についても 1 文字も言っていない**。

**leg 冒頭宣言 (N14)**: 側 = 記録 / 動かすもの = A2 の敵対的独立監査の訂正を台帳と成果物へ伝播し、アークの死を `## N13 (T3c)` として台帳化して、空いた N14 / N15 / N16 の配分を根拠つきで決められる状態にする
> **アーク A2 を閉じた (記録 leg)** — 監査の訂正 **20 件** (監査 19 件 + 本 leg が見つけた 1 件 = N13-k) を facts [`bc-facts.md`](bc-facts.md) と成果物 [`bc-t3c-a2-novelty-gate.md`](bc-t3c-a2-novelty-gate.md) へ伝播し、facts に **`## N13 (T3c)` 節 (claim 11 行 = N13-a … N13-k)** を新設した。⚠⚠ **`## A2 (T3c)` 節は存在しない** — **A2 は終端 (N15) に届かず 1 leg 目で死んだので、アーク終端の台帳節は立たない**。commit = `5ae2fbf8`。
> ⭐ **最重要の訂正は主判定の外側である** — facts `## N2 (T3c)` の **N2-i (「§0 の完了条件は実効性述語 0 個で言明できた」) が空虚**であることを監査が機械で示した (⚠ **N2 からの継承であって N13 が作った誤りではない**)。⚠ **本 leg が見つけた 20 件目 (N13-k)** = 監査 md が引く一次文献 stem `ah.txt` は [`lit-fetch.sh`](lit-fetch.sh) に無く正しくは **`sct`** (⚠ **行番号は独立再取得で全件再現した — 誤りは stem 名の表記だけ**)。
> ⚠ **本 leg は判定 leg ではない** — **新しい GO / NO-GO を 1 本も出していない**。⚠⚠ **`Thm7 ⊋ C` の材料は依然 1 つも無く、排除もされていない** / ⚠ **配分の決定は §7 判断ログ 10 が SoT**。

**leg 冒頭宣言 (N15)**: 側 = (C2) / 動かすもの = 「`C` の閉形 (= 比較の相手) が [probc] の外に書けるインスタンス族が在るか」を §4.5 の 3 道具で gate し、GO なら N16 の第 2 段 [構築 + probe] へ渡す族を 1 つに絞り、NO-GO なら候補 2 と候補 4 が同時に死ぬことを確定させて候補 1 の `restatement` 公算の裏づけを得る
> **判定 = GO** — **[glnsum] の和 (⊕) broadcast channel クラス** (成分が semi-deterministic、具体 = Fig. 3) が **[probc] の外**にあり (積分解を持たない)、**`C` の支持関数の閉形**を供給する (⚠⚠ **Corollary 1 が直接与えるのは `M(T)` で、`C` へは Theorem 4 を経由する**) ⟹ **N16 へ渡す族は 1 本**。**起票の反証条件の結果 (継承 3 / 新規 3) と見立ての較正の SoT = [`bc-t3c-n15-instance-gate.md`](bc-t3c-n15-instance-gate.md) §4.2 / §4.3、見立てと条件文そのものは同 §0 / §1** (⚠⚠ **複製も書き換えもしない** — §0 / §1 は起票 commit `4a8061f7` から不変で、監査 E1 が md5 で確認済) / **§4.6 の敵対的独立監査 = [`bc-t3c-n15-audit.md`](bc-t3c-n15-audit.md)、判定 = 訂正あり生存** (46 系統中 7 成立 / 訂正 7 件 / ⚠⚠ **主判定 GO を動かすもの 0 件**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N15 (T3c)` (節内タグ `N15-a` … `N15-j`) + 成果物**。
> ⚠⚠ **落としてはならない限定**: **方向 `(1,1,1)` は使えない** (そこでは `SR_Thm7 = SR_C` が文献だけで従う。⚠ **本 leg が新たに導いた否定的事実であり GO の射程を狭める**) / **生きた方向の錐 `d ∈ (0, 2/15)` は十分条件であって同値ではない** (監査 訂正 4。⚠ **真の錐はより広い可能性がある**) / **族の生死を握る外部依存 1 本 = `Thm7 ⊆ Thm8` の領域レベルの順序 (現在どちら向きも未証明)** / ⚠⚠ **`Thm7 ⊋ C` の材料は本 leg でも 1 つも出ておらず、排除もされていない** (gate ゆえ原理的に出ない) / ⚠ **Lean には 1 つも載せていない** (層 1 + 層 2 のみ)。
> ⭐ **本 leg が名指しした開いた問い** = **現行 [probc] more-capable インスタンスで `UV = C` か** (⚠⚠ **もし `UV = C` なら N7 / N9 / N10 の `Thm7 = C` は挟み込みで強制されていたことになる**。⚠ **肯定も否定もされていない**)。⭐ **波及 1 か所** = facts `## M1 (T3b)` の**行 4 と P4 行** (⚠ **向きは「生きている空間が広がる」側で主判定 判定不能を強める** / ⚠ **N7 / N9 / N10 / N12 には波及しない** — 根拠の SoT = 監査 §2.1)。⚠ **NO-GO ではないので「候補 2 と候補 4 が同時に死んだ」とは書かない** (死んだのは候補 4 のみ。死因 = `probe-failed`)。

**leg 冒頭宣言 (N16)**: 側 = (C2) / 動かすもの = N15 が名指しした開いた問い「現行 [probc] more-capable インスタンスで `UV = C` か」を gate し、`UV = C` なら N7 / N9 / N10 の `Thm7 = C` が挟み込みで強制されていたことを確定させて判定枠 3 組が §0 に対して持っていた情報量を較正し、`UV ⊋ C` なら [probc] 上の `Thm7 = C` は挟み込みでは説明されないことを確定させて N15 の選別基準の射程を測る
> **判定 = `UV = C`** — 現行 [probc] more-capable インスタンス上で **UV 外界 (一般 witness 版) と容量領域は領域として一致する** ⟹ ⚠⚠ **挟み込み `C ⊆ Thm7 ⊆ UV` はこのインスタンス上で `Thm7 = C` を強制していた** = **減算方向の較正**である。**起票の見立て 4 本と反証条件 3 本の SoT = [`bc-t3c-n16-uv-gate.md`](bc-t3c-n16-uv-gate.md) §0 / §1** (⚠⚠ **複製も書き換えもしない** — §0 / §1 は起票 commit `d9398597` から不変で、監査が空行正規化つきの md5 一致と削除行走査の 2 通りで確認済) / **較正と発火状況は同 §4、訂正の反映先の索引は同 §8** / **§4.6 の敵対的独立監査 = [`bc-t3c-n16-audit.md`](bc-t3c-n16-audit.md)、判定 = 訂正あり生存** (**60 系統中 15 成立 / 45 不成立・訂正 14 件**、⚠⚠ **主判定を動かすもの 0 件**)。**逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N16 (T3c)` (節内タグ `N16-a` … `N16-i`) + 成果物 + 監査 md** — ⚠ **値と逐語をここへ複製しない**。
> ⚠⚠ **落としてはならない限定**: **射程は 1 点 (`p = 0.1`, `e = H(0.1)`)** で、**半空間への拡張は条件つきの二次的主張**である (⚠⚠ **「半空間全体で `UV = C`」とは書かない** — 監査が埋めたのは 74 点 × 2 個の `t`) / **`Ω(t) = d*_t` は監査が独立に再導出したが `1e-9` の許容差つき / 区間演算ではない / 全 `t` ではない** / **`C ⊆ UV` は引用のままで本 leg も監査も逆定理を再導出していない** / ⚠⚠ **`Thm7 ⊋ C` の材料は本 leg でも監査でも 1 つも出ておらず、排除もされていない** (gate ゆえ原理的に出ない) / ⚠ **`Thm7 ⊄ Thm8` は主張していない / `R ∈ Thm7` とも書いていない** / ⚠ **Lean には 1 つも載せていない** (層 1 + 層 2 のみ)。
> ⭐ **監査の最大の収穫は成果物が自ら「⭐⭐ 最弱点」と名指しした 2 か所に当たった** — **§6-1 が名指しした崩れ方の向きが逆だった** (訂正 1。開いていたのは `Ω(t) > d*_t` の側で、そこには screen しか当たっていなかった) / **監査がその向きを独立に閉じた** (訂正 2 = 上方修正) / **§3.5 / §6-5 が「未解消」とした一次文献の緊張は掃引済 `probc` の内側で解消する** (訂正 3)。⚠⚠ **facts への波及は `## M1 (T3b)` 行 5 の訂正 1 か所** (⚠ **監査 §7.1 の形 = 根拠を差し替え射程を狭める。「more-capable クラス全体で `○` が付かない」とは書かない**) + **既存 6 節への追記** (⚠ **判定は 1 つも覆っていない**)。
> ⚠ **N15 の主判定 `GO` と選別基準には波及しない** — ⚠⚠ **「第 2 段の土台が弱まった」とも「強まった」とも書かない** (本 leg は [glnsum] の和チャネル族に 1 mm も触れていない)。⚠ **本 leg で可変枠 14 は使い切った** — **relay 終端の棚卸しは §5 の該当ブロック**が持つ。

**leg 冒頭宣言 (N17)**: 側 = 形式化債務 / 動かすもの = 子 plan の単位 B (3 レート `Thm7(W)` の領域層) の受け皿を層 3 に実在させ、gateway atom (中核 8 = 合併レベルの閉性を `closure` 無しで出す) の実際の難度を初めて機械で測る
> **起票の凍結先 = [`bc-t3c-n17-unit-b.md`](bc-t3c-n17-unit-b.md) の §0 (見立て 4 本 = 締める 2 / 緩める 2) / §1 (反証条件 新規 3 本)** (⚠ **実行 leg / 監査は 1 文字も書き換えない**。⚠ **不変性の確認は空行を落として正規化してから md5 を取る**)。**配分決定の SoT = §7 判断ログ 12** / **単位の SoT = 子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) §3.2**。⚠ **側 = `形式化債務` ゆえ §6-4 のカウンタ (起票行) は増えない**。
> ⚠⚠ **§6-5 は既に「収まらない」と判定済ゆえ本 leg は §0 の Lean 側には届かない** — ⚠ **成果を「§0 に近づいた」と書かない** (§0.1-2)。⚠ **同時に「だから走らせる意味が無い」とも書かない** (§7 判断ログ 12-(4))。

### 5.2 ⚠ はみ出し禁止条項 — 「形式化債務が可変枠を食う」形を具体で禁じる

前々 relay の事故は**形式化債務が可変枠を 14 leg 連続で食った形**である (⚠ **early gate を置く分、
本 relay ではこの条項の重要度が上がる**)。

1. **形式化債務を N3–N16 の枠で消化してはならない**。必要になったときは **N2 / N17 / N18 の
   内数**であり、それを超える分は**別 plan へ切り出す** (`@residual(plan:<新 plan の filename stem>)`
   で参照し、本 relay の枠は使わない)。
2. **「(C2) を進めるための前提工事」という理由づけで可変枠を形式化債務に充てる場合、
   その leg の冒頭宣言の側は `形式化債務` である** — `(C2)` と書いてはならない。
3. **2 leg 連続で側が `(C2)` 以外になったら、3 leg 目を起票する前に §6-4 の配分カウンタを引く**。
4. ⭐ **N2 は測るだけの leg である** (⟹ §6-5 で消化済) — 同型の「測る leg」を後に置く場合も
   **その場で実装になだれ込まない**。

### 5.3 ⭐⭐ 探索アークの規約 (新設。⚠ 起票の根拠は §7 判断ログ 4)

**成果物の型を 4 つに増やし、型ごとに合格条件と死因を与える**。⚠ **決着圧力を落とすのではなく、
当たり先を変える**のが趣旨である (⚠ 判定枠の圧力はそのまま。外すと前 relay の失敗形へ戻る)。

| 型 | 成果物 | 合格条件 | 死因 |
|---|---|---|---|
| **判定** (現行) | GO / NO-GO / 未決 | §4.5 の 3 道具のどれかで機械に掛かっている | 現行 5 語 |
| **⭐ 定義** | 新しい対象の def 1 本 + 健全性テスト | **facts の既存 claim を N 本再現する** (下表) | `non-reproducing` / `inert` |
| **⭐ 言い換え** | 既存の問いを新語彙で書き直したもの + 元の問いとの同値 | 同値が §4.5 の道具で機械に掛かっている | `redundant` |
| **⭐ 障害の構造** | 「なぜ既存の道具では届かないか」を対象化したもの | 障害が**対象として名指しされ**、回避条件が書かれている | `restatement` |

⚠ **「障害の構造」は「未決」と別物である** — 「未決」は判定の不在にすぎないが、この型は**届かなさ
そのものを対象化する**ことを要求する (M12 の「`T_J` ごとに witness を取り替える機構」が今なお
「量化子の順序」としか呼ばれていないのが、埋めるべき空席の典型)。

**アーク宣言** (⚠ **アーク単位で 1 回。個別 leg には決着を要求しない**):

```
**アーク宣言 (A<n>)**: 型 = <定義 | 言い換え | 障害の構造> / 対象 = <何を定義・言い換え・対象化するか> /
  接続 = <facts のどの節に接続するつもりか、3 本以上> / 反証条件 = <どうなったらこのアークを捨てるか>
```

⚠ **`接続` を 3 本以上書けないアークは起票しない** — これが §0.1-3 の精神を保つ点であり、
**同時に決着の要求を外す点**である。⚠ **`反証条件` を先に書く**のは §4.4-2 の継承。

**⭐ 進捗指標 = `bc-facts.md` の既存 claim の再現本数** (台帳は claim 行 + 再検証コマンド +
confidence を持つのでそのままテストスイートになる)。⚠ **再現は逐語一致でなく値レベル一致でよい**
(M9 の基準)。

| アーク終端の再現本数 | 判定 |
|---|---|
| 0 本 | `kill` (`inert`) |
| 1–2 本 | `park` (再開条件 = 接続先を 3 本に増やせたら) |
| 3 本以上 | `続行` (⚠ 連続 2 アークまで) |
| ⭐ **独立な** claim 2 本以上を**同一の機構で**説明した | `続行` + 判定枠への昇格を検討 |

⚠ **閾値は暫定である** (アーク運用の実測が 0 なので) — **A1 の終端で較正し直す**。

**R4 — 層 3 の使い方**: アークでは**定理ではなく def を Lean へ載せる**。def は証明が要らないので
散文が決着していなくても載り、**型が付くこと自体が健全性テストになる** (⚠ **形式化債務と同じ
方向を向く**)。⚠ **`sorry` + `@residual(plan:bc-open-problem-t3c-plan)` の退出路はそのまま**。

**投入先候補** (⚠ どれも「空席」として名指しできる場所であり思いつきではない):

1. ⭐ **`T_J` ごとに witness を取り替える機構の対象化** (型 = 障害の構造 → 定義) ⟹ **A1 として起票済・終端済。逐語は下の「アーク宣言 (A1)」が SoT**。
2. **`Thm7` の領域を変分問題として定式化し直す** (型 = 言い換え)。接続は `## M3` / `## M11` (T3b) / `## N1` (T3c)。⚠ **`redundant` で死ぬ危険が最も高い** ⟹ **novelty gate をアーク 1 leg 目に当てる**。
3. **計算可能解析の語彙を BC 側へ持ち込む** (型 = 定義)。⚠ **接続先が facts に少ない** (`## L2 (T3)` 程度) ので閾値の観点では最も弱いが、§0 の Lean 側はここを通らないと閉じない ⟹ **N17 / N18 と抱き合わせる**のが筋 (§6-5 の切り出し単位 A と同じ対象)。

**アーク宣言 (A1)** (= 投入先候補 1。⚠ **終端済** — 逐語の SoT = [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) + [`bc-t3c-a1-novelty-gate.md`](bc-t3c-a1-novelty-gate.md) + facts `## A1 (T3c)`。⚠⚠ **起票時の宣言は 2 点誤っており N3 の gate が訂正した**):

- **型** = 障害の構造 → 定義 / **対象** = `## M12 (T3b)` が `Thm7` の UV 超えの**唯一の源**と確定させた「`T_J` ごとに witness を取り替える機構」を、**witness ごとの適格集合** `E_R(w)` の**被覆数** `κ(R)` として対象化する (⭐ **訂正 1 (N3 §0-a)** = レート束の `J` 依存を落としていた `B(w)` 版からの補修。**`κ(R) = 1` が uniform 領域に対応**し、⚠ **凸性は補修後も生き残る**) / **接続 3 本** = **`## M4 (T3b)`** (指数集合が `∀T_{J|X}` へ両向きで潰れ `𝒯` の定義域が確定) / **`## M11 (T3b)` 行 4** (適格残差が `J` の測度についてアフィン ⟹ `E(w)` は凸) / **`## M12 (T3b)`** ([probc] で `κ ≥ 2` の実例 + 広がりの下界) — ⭐ **訂正 2 (N3 §7)** = 起票時の 4 本目 `## M15 (T3b)` は**帰属が誤り**ゆえ接続から外した (⚠ **3 本で §5.3 の閾値は維持**)。
- **反証条件 2 段** — **(1) novelty gate (N3) = 消化済** (結果は §5.1 の N3 着地ブロック。⚠⚠ **`## M11 (T3b)` (D) の「抽象構造だけからは結論が出ない」明示トイは生きており `inert` で死ぬ公算は残る**)。**(2) アーク終端 (N5)**: facts の既存 claim の**値レベルでの再現本数が 3 本未満** ⟹ §5.3 の閾値表と §6-6 に従って `park` / `kill`。
  ⟹ ⭐ **(2) の決着 (N5)**: **再現本数は 3 本未満ではなかった** (7 本 / 文言基準 6 本) ⟹ **`park` / `kill` に落ちなかった** = **§6-6 は不発火**。⚠ **(1)(2) の本文は決着後も消さない** (反証条件は事後に書き換えない)。

**⭐ アーク A1 の終端判定 (N5)** — ⚠ **§6-6 の規定により、判定の入力は再現本数 1 本のみである。prose の印象で判定しない**。**再現本数 = 7 本 / 文言基準 6 本** (内訳と 2 基準の差の逐語は [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) §4 の表 + facts `## A1 (T3c)` が SoT。⭐ **監査が独立実装で 7 本すべて抜き取り検証し、水増しなしと判定**) ⟹ **どちらの基準でも閾値 3 本を満たす** ⟹ **判定 = `続行`** (上の閾値表「3 本以上」) / ⚠ **§6-6 は不発火**。**昇格条件 (⭐ 独立な claim 2 本以上を同一の機構で説明した) の判定 = 満たさない** — A1 の機構 ((D2) 線分補題) は `## M11 (T3b)` 行 4 (適格残差の `μ` アフィン性) を**入力として消費**し `## M12 (T3b)` 行 5 (`κ ≥ 2`) を**有限本へ拡張**したのであって、**独立な 2 本を説明したのではない** (⚠ **再現は「再現」であって「説明」ではない**)。⚠ **昇格を取らないことは A1 の失敗ではない** (§6-6: **`続行` かつ昇格なしは正常な帰結**)。

⚠⚠ **同時に必ず併記する 2 点 (どちらか一方だけを書かない)** — **`続行` を「§0 に近づいた」と書いてはならない / N3 の novelty gate は §0 への payoff を 3 点とも不成立と判定している** (とくに **`κ` の有界性は `## L2 (T3)` 行 6 の実効コンパクト性より厳密に強く、後者は既に無条件の定理**ゆえ **計算可能性側に 1 mm も足さない**) ⭐ **/ 一方 (K1) はインスタンス非依存の構造定理**であり **41+5 leg で初めて出た「新しい証明機構」型の産物**である。

**⭐⭐ 閾値の較正 (N5 の義務)** — 上の「閾値は暫定である — A1 の終端で較正し直す」への回答。⚠ **実測 4 点の逐語は [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) + facts `## A1 (T3c)` が SoT** (要点は **再現本数は「上乗せの安さ」を測るが「§0 への payoff」は測らず 2 軸が直交する** / 再現の粒度が未定義だと本数は静かに膨らみ、敵対的独立監査そのものも本数を増やしうる / **閾値 3 本は事実上の gate になっていない** ⟹ ⭐ **識別子として働いたのは昇格条件の方**で、A1 はそれを満たさなかった)。**較正の結論 (3 点)**: **(a) 3 本ラインは維持する**。⚠ **判定入力を増やさない** — §6-6 が「入力は再現本数 1 本のみ / prose の印象で判定しない」と定めており、**軸を増やすと前 relay の失敗形 (印象での判定) へ戻る** / **(b) 再現の定義を締める** — 各再現について「**facts 行のどの欄を再現したか**」を明記することを**再現の条件に加える**。明記できないものは数えない。⚠ **監査由来の再現は数える** (独立性はむしろ高い) / **(c) アーク終端では第 2 軸を必ず「併記」する** — 「**そのアークの産物が §0 に向いた claim を 1 本でも動かしたか**」。⚠⚠ **これは判定入力ではなく報告義務である** (`kill` / `park` / `続行` の判定は従来どおり再現本数のみで決める)。**A1 ではこの欄は「動かしていない」である**。

**⭐ アーク運用の実測 2 本目 (N14 で確定。⚠ 逐語の SoT = facts [`bc-facts.md`](bc-facts.md) `## N13 (T3c)` の N13-j)** — **A1 = 終端 `続行` (再現本数 7 本) だが §0 への payoff は 3 点とも不成立・昇格条件も満たさず / A2 = 1 leg 目で `redundant` 死** ⟹ ⚠⚠ **アーク 2 本のいずれも §0 に向いた claim を 1 本も動かしていない**。⚠⚠ **これは §7 判断ログ 4 が「引き受けたリスク」と明記した形の実現であって、leg 運用改定そのものの失敗ではない** (⭐ **「novelty gate をアーク終端ではなく 1 leg 目に当てる」処方は設計どおり働き、A2 は 3 leg ではなく 1 leg で死んだ**)。⚠⚠ **同時に「アークは当たる」とも書かない** — 母集団 2 本では期待値を測れない。⚠ **「何も出なかった」とも書かない** — A1 の (K1) (インスタンス非依存の構造定理) は産物として残る。

**アーク宣言 (A2)** (⚠⚠ **注記 (N13 着地 / N14 で閉じた。⚠ 下の反証条件の本文は 1 文字も書き換えていない)** — **終端 (N15) には届かず、1 leg 目 (N13 = novelty gate) で `redundant` 判定により死んだ** ⟹ **反証条件 (1) が発火し、(2) (アーク終端の再現本数) は評価されずに終わった**。⚠ **したがって facts に `## A2 (T3c)` 節は存在しない** — 台帳は `## N13 (T3c)` が持つ):
  型 = 定義 / 対象 = 計算可能解析の (β) 語彙 (実効コンパクト / computably overt / 実効連続 / 一様 Π01 と可算 ⋂ 閉包 / (L-∃)・(L-∀)) を BC 側の集合水準の def 群として層 3 へ置く (§5.3 R4 = def のみ。⚠ 子 plan 単位 A の中核 1–7 の証明は載せない) /
  接続 = 5 節 — `## L2 (T3)` (行 5・6・10 = P1 / P2 / P4)、`## L0 (T3)` (行 3・5 = computably overt と Definition 2.4)、`## L5 (T3)` (行 4・5 = `IsComputableENNReal` と in-project 0 件確認)、`## L3 (T3)` (行 3 = exact decision と `ε` 近似の語義分離)、`## N2 (T3c)` (N2-b / N2-d / N2-g / N2-j) / 反証条件 = 2 段 — **(1) novelty gate (N13 = アーク 1 leg 目)**: 既定の立場 = `redundant`。def 群が台帳の既存 claim へ逆向きにも損失なく翻訳されるなら捨てる / **(2) アーク終端 (N15)**: facts の既存 claim の値レベル再現本数が 3 本未満なら §5.3 の閾値表と §6-6 に従って `park` / `kill`。⚠ **N5 較正 (b) を最初から (2) に織り込む — facts 行のどの欄を再現したかを明記できない再現は数えない**

- **選ばなかった候補 2 (`Thm7` の変分問題化、型 = 言い換え)**: 接続 3 本 (`## M3 (T3b)` / `## M11 (T3b)` / `## N1 (T3c)`) は書けるが **`redundant` の公算が 3 本中最も高い** — 対象の変分構造は台帳が既に恒等式で保持しており (M11 (A) の凸集合帰属 1 本への畳み込み / N1 (C) の Klein 4 群による点ごとの極値の潰れ)、言い換え型の合格条件 (元の問いとの同値を機械に掛ける) が逆向き無損失翻訳の確認と同じ操作になる。
- **選ばなかった候補 1 (一般 BC への持ち上げ、型 = 障害の構造)**: 接続 3 本 (`## N6 (T3c)` N6-j / `## N10 (T3c)` N10-e / `## L2 (T3)` 行 6) は書けるが **`restatement` の公算が最も高い** — N6-j 自身が回避条件と同じ行で「§0 への payoff は無い」を併記しており (`Thm7(W)` の一様実効コンパクト性は `## L2 (T3)` 行 6 で既に無条件の定理。N12-d も同旨)、障害の対象化が既存 claim の言い直しに落ちる。

---

## 6. 撤退ライン

**⚠ 撤退ラインはゴールを下げる装置ではない** (§0.1-2)。撤退とは「本 relay を終了して次の判断を
ユーザーに返す」ことであって、「達成条件を緩める」ことではない。

- **6-1**: **N1 の gate が GO / NO-GO のどちらも返さない** (押し上げも天井の恒等式も測れず
  「判定不能」で終わる) ⟹ **N9 の棚卸しまで待たずに**、T3-β 側の第一手へ振り替えるかを含めて
  ユーザーに判断を返す。⚠ **数値で分離らしきものが出ただけでは GO/NO-GO のどちらでもない**
  (§4.5)。
- **6-2**: **N15 の棚卸しで、(C2) について層 3 に載せられる散文が 1 本も無い** ⟹ N17 / N18 の
  形式化枠を層 1 へ戻すかをユーザーに諮る (⚠ **`GOAL-CHANGE` ではない** — 完了条件は動かさず
  配分だけ動かす)。⚠ **前 relay ではこのラインが不発火で、しかも申し送りの予測を覆した** (§4.1 軸 1)。
- **6-3**: **20 leg を使い切って未達** ⟹ **未達と書く**。到達した中間結果は台帳に残すが、
  **ゴール達成とは書かない**。⚠ **前 2 次とも発火した実績があるライン**である。
  ⚠ **発火とは「未達と書く」ことであって、達成条件を緩めることではない**。
- **6-4 (配分の撤退ライン)**: **(C2) 本体の枠が形式化側に食われていないかを機械で数える**。
  可変枠 N3–N16 を `N` leg 消化した時点で、**冒頭宣言の側が `(C2)` だった leg 数が `N` の半分を
  切ったら、その場でユーザーに判断を返す** (棚卸しの leg を待たない)。⚠ **予約枠 14 の半分 = 7 に
  届かないことが確定した時点でも同じく発火する**。⚠ **数え方は §5.1 の `rg` 1 本**であり、
  prose の印象で判定しない。
- **6-5 (⚠ 新設 — early gate の撤退ライン)**: **N2 が返した員数が、残る形式化枠 2 leg
  (N17 / N18) に収まらない** ⟹ **可変枠に吸わせない**。**別 plan へ切り出して
  `@residual(plan:<新 plan の filename stem>)` で参照し、「本 relay の中では §0 の Lean 側へ
  到達できない」ことを明記したうえでユーザーに判断を返す**。⚠ **これは `GOAL-CHANGE` ではない** —
  §0 は動かさず、**届かないという事実を書く**だけである。⚠ **N2 が「収まる」と返したときも、
  §4.1 軸 2 により N17 着手*後*に残りの段の在庫を取り直す**。
- **6-6 (⚠ 新設 — 探索アークの撤退ライン)**: **アーク終端の再現本数が §5.3 の閾値表で `kill` /
  `park` に落ちる** ⟹ そのアークを閉じ、**次のアークへ横滑りしない** (⚠ 2 本目のアークは
  §5 の枠にあるが、**1 本目が `kill` なら 2 本目の起票前にユーザーに判断を返す**)。
  ⚠ **`park` / `kill` はアークの失敗であって本改定の失敗ではない** — 探索枠が何も返さない可能性は
  §7 判断ログ 4 で**引き受けたリスク**である。⚠ **prose の印象で判定しない** — 入力は
  §5.3 の再現本数 1 本のみ。⚠ **アーク終端の判定を棚卸し leg と同じ leg に置かない**
  (自己採点になる)。⚠ **アーク内では 6-4 / 「3 leg ゼロ進捗で park」/ `no-progress×2` を引かない**
  (アークをまたいでは引く)。

---

## 7. 判断ログ

書く頻度: 方針変更 / 撤退 / 当初仮定の修正。決着済 entry は削除 (git が履歴)、active のみ残す。
プラン予算 ≤ 600 行 / active 判断ログ ≤ 10 entry。⚠ **番号は本ファイルで独立**である
(前 plan の判断ログとは別勘定。相互参照は「前 plan 判断ログ N」と書く)。

⚠ **active の数え方 (誤読防止)**: **決着済で番号のみ残る 1 行スタブ (現在 `3` / `6` / `7` / `8`) は active に数えない** —
番号を残すのは**他文書が参照しうる**ためであり、逐語の SoT は各スタブが指す先が持つ。
⟹ **現在の active は `1` / `2` / `4` / `5` / `9` / `10` / `11` / `12` の 8 entry** である。
⚠ **entry 10 が書く「10 entry = 上限ちょうど」は当時の数え** (`3` / `7` がまだ full entry だった時点) であり、
⚠ **判断ログは追記のみ (過去 entry の書き換えはしない) ゆえその文言はそのまま残してある**。

1. **継続 + 新規 20 leg (ユーザー決定、2026-08-04)**: 前 relay ([`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md)) の §0 未達終了を受け、ユーザーが**継続**を選択して新規 20 leg の予算を切った ⟹ 本 plan を新設。
   ⚠ **引き受けたリスク**: **前 2 次は合わせて 41 leg を使って未達**で、⚠ **第 2 次は配分事故を起こしていない** (可変枠 14 本とも `(C2)`) ⟹ **未達リスクは配分では説明できない**。**20 leg 使ってゴール未達で終わる可能性は依然として高く / それを「中間結果は出た」で埋め合わせない** (§0.1-2)。⚠ **入口が 1 点に絞れたことは「近づいた」を意味しない**。

2. **early gate (N2) を採用 (2026-08-04、⚠ 実行済 = 判断ログ 3。本 entry は囲いの条項として残す)**:
   ⚠ **可変枠は 1 leg も削っていない** (形式化枠 3 の内数) — **この囲いを外すなら `GOAL-CHANGE` ではないがユーザーに諮る** (§5.2)。

3. **§6-5 の発火は決着済 (2026-08-07 発火 / 2026-08-08 起票)**: ⚠ **決着済 = 番号のみ残す (1 行スタブ)** — 子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) へ切り出し済 (参照 = `@residual(plan:bc-computable-region-formalization)`、切り出しの根拠と単位 A / B / C の SoT = 同子 plan §1.2)。⚠⚠ **`GOAL-CHANGE` ではない** (§0 不動)。⚠ **落としてはならない 3 点の逐語 SoT = facts `## N2 (T3c)` の N2-a / N2-b / N2-l** (⚠⚠ **ここへ複製しない**)。

4. **⭐⭐ leg 運用の改定を適用 (ユーザー決定、2026-08-07)**: 提案の SoT = [`bc-leg-operation-revision-plan.md`](bc-leg-operation-revision-plan.md)。**診断** = **41 + 3 leg を通じて新しい不変量・領域概念・証明機構を導入した leg は 0 本**で、原因は能力ではなく**本 plan の規約 5 つが理論構築 leg を起票不能にしていたこと**。**適用** = 成果物の型 4 つ + 探索アーク (3 leg 1 単位) + 進捗指標を facts の再現本数へ + 層 3 に def を載せる (§5.3) / 予算の割り直し (§5) / 撤退ライン **6-6** の新設 / attacks 台帳の語彙拡張。⚠ **ユーザー承認が要ったのは §0.1-3 に「アーク宣言という代替形式」の例外を作る 1 点** (`GOAL-CHANGE` ではない)。⚠ **決着圧力は判定枠でそのまま維持する** — 外すのではなく適用範囲を分けた。
   - ⚠ **引き受けたリスクを明示して持つ**: **探索アーク 6 leg が何も返さない可能性は実在する**。⚠ **それは失敗ではなく引き受けた賭け**である。⚠ **期待値が良いと断言する根拠は無い** (アーク運用の実測が 0 だから) / **`redundant` で死ぬ公算が高い** (L8 が R1 に対して実測した形) ⟹ **novelty gate はアーク終端ではなく 1 leg 目に当てる** / **アークが「決着しない言い訳」に劣化する危険** ⟹ 処方は §5.3 の再現本数 1 本のみ。
   - ⚠⚠ **本改定は §0 への到達確率を上げると主張していない**。主張しているのは**「思いつく場所に leg が入っていない」という状態を解消する**ことだけである。

5. **⭐ 判定枠 第 3 段 (N8) の側 = `(C2)` に決着 (2026-08-08)**: §5「次に来るもの」の**論点 1** の決着。⚠⚠ **`GOAL-CHANGE` ではない** (§0 は不動)。**根拠** = §5.2-2 が禁じるのは**「(C2) を進めるための前提工事」という理由づけによる可変枠の流用**であって、**判定枠が設計として持つ第 3 段はそれに当たらない** — §2 の「**層 1 が通った命題はその命題単位で層 3 へ載せる**」が**判定枠の構成要素として**第 3 段を定めている。
   - ⚠⚠ **§5.2 を空文化させないためのテスト可能な線 (本決着と必ずセットで持つ — これが無いと本 entry が §5.2 の抜け穴になる)**: **載せてよいのは本 relay の `(C2)` leg が立てた命題だけ** / **§6-5 が切り出した受け皿 (領域 def・計算可能性層 = 子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) の単位 B) に手を出し始めたら、その時点で停止して子 plan へ回す**。

6. **(β) の射程が変わった (N9 着地、2026-08-08)**: ⚠ **決着済 = 番号のみ残す** — 内容は下の entry 7 が上書きする (⚠⚠ **`GOAL-CHANGE` ではない**)。

7. **(β) が閉じ、射程が「曲線 `e = h(p)`」から「半空間 `e ≤ h(p)`」へ広がった (N10 着地 + 監査 訂正 4、2026-08-08)**: ⚠ **決着済 = 番号のみ残す (1 行スタブ。entry 6 が本 entry を指している)** — ⚠⚠ **`GOAL-CHANGE` ではない** (§0 不動。動いたのは**決着の射程**だけ)。⚠ **逐語と落としてはならない限定 (`Thm7 ⊋ C` の材料は 1 つも無く排除もされていない / 決着は [probc] の族の上に留まり一般 BC へは持ち上がらない / `e > h(p)` で標的が偽になるのは分離ではない) の SoT = facts `## N10 (T3c)` + [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md)** (⚠⚠ **ここへ複製しない**)。⚠ **半空間の射程は N16 の見立て C が消費する** (§7 判断ログ 11)。

8. **N11 = 第 3 段を「`δ ≥ 0` の層 3 化」として取る (N11 着地、2026-08-08)**: ⚠ **決着済 = 番号のみ残す (1 行スタブ)** — 落としてはならない 2 点 (**載ったのは `ℝ` 上の 1 変数不等式でレート領域の包含については 1 文字も言っていない** / **`e > h(p)` 側の探索の引き継ぎと反証条件 3 本の SoT = N10 成果物 §4.2**) は **§5.1 の N11 着地ブロック**と**下の「relay 終端の棚卸し」表**が持つ (⚠⚠ **`GOAL-CHANGE` ではない**)。

9. **⭐ N12 の側を `記録` に取る (2026-08-08)**: §5 の枠表は N6–N12 を `(C2)` の予約枠として括るが、**棚卸しは (C2) の命題を 1 本も動かさない** ⟹ §5.2-2 の精神 (「(C2) を進めるための前提工事」を `(C2)` と書かない) を棚卸しにも適用し、**§6-4 のカウンタを増やさない**。⚠⚠ **`GOAL-CHANGE` ではない** (§0 は 1 文字も動かない) / ⚠ **枠表の「N6–N12 = 判定枠 `(C2)`」という括り自体は変更しない** — 変えたのは**この 1 leg の冒頭宣言の側**だけである。
   - ⚠ **引き受けたリスク**: 側を `記録` にすると **`(C2)` の起票行が増えない**ため、**§6-4 の分母 (可変枠の消化) だけが増える** ⟹ **残枠でこの選択を繰り返すとカウンタは発火しうる**。⚠⚠ **その発火は異常ではなく設計どおりの検知である**。

10. **⭐⭐ A2 の死を受けた残枠 (N14 / N15 / N16) の配分 (2026-08-08)**: ⚠⚠ **`GOAL-CHANGE` ではない** — **§0 は 1 文字も動かない。動いたのは残枠の配分だけ**である。⚠ **本 entry で active 判断ログは 10 entry = 上限ちょうど**になる (⚠ **決着済で番号のみ残っている entry 6 の扱いは変えない** — 他文書が参照しうる)。
    - **(1) N14 = 探索アーク A2 を閉じる leg。側 = `記録`**。動かすもの = 監査の訂正 20 件を facts / 成果物へ伝播し、A2 の死を `## N13 (T3c)` として台帳化して、空いた枠の配分を決められる状態にする。**側を `記録` に取る根拠 = 判断ログ 9 と同型** (棚卸し / 記録は (C2) の命題を 1 本も動かさないので §5.2-2 の精神により `(C2)` と書かない)。**「アークを閉じる」が leg の仕事である根拠 = §6-6 の「そのアークを閉じ、次のアークへ横滑りしない」**。
    - **(2) 3 本目の探索アークは起票しない**。根拠 = §6-6 の「**次のアークへ横滑りしない**」。⚠⚠ **ただし条文の文言は「アーク終端の再現本数」を対象にしており、novelty gate 死は文言上の対象外である** ⟹ **本決定は条文を精神で読んだものである** (⚠ **文言を勝手に広げたのではない**)。**実績を隠さずに併記する** — **A1 は終端 `続行` だが §0 payoff は 3 点とも不成立・昇格条件も満たさず / A2 は 1 leg 目で `redundant` 死** (⚠ **逐語の SoT = facts `## N13 (T3c)` の N13-j**)。⚠⚠ **これは §7 判断ログ 4 が「引き受けたリスク」と明記した形の実現であって改定の失敗ではない** / ⚠⚠ **同時に「アークは当たる」とも書かない**。
    - **(3) N15 = 判定枠 第 3 組 第 1 段 `[探索 + gate]`。側 = `(C2)`**。**標的 = 「`C` の閉形 (= 比較の相手) が [probc] の外に書けるインスタンスが在るか」の 1 点**。⭐ **決定の核 = [`bc-t3c-n12-stocktake.md`](bc-t3c-n12-stocktake.md) §4 の候補 2 (他インスタンス) と候補 4 (`e > h(p)` 側) を 1 本の gate に束ねる** — **候補 4 の反証条件 1 本目** (SoT = [`bc-t3c-n10-epsilon-zero.md`](bc-t3c-n10-epsilon-zero.md) §4.2。⚠⚠ **複製も書き換えもしない**) は「`e > h(p)` では [probc] Theorem 3 が壊れ `C` の閉形が消える ⟹ **比較の相手が無いので超過を評価できない**」で、しかも「**これが最も起きやすい**」と明記されており、**候補 2 の死因欄は `probe-failed` (`C` の閉形が書けるインスタンスが無い)** ⟹ ⭐ **2 候補は同一のボトルネックの裏表である**。
      - **GO / NO-GO のどちらでも軸が 1 本確定する** (§5.1 の N1 と同じ設計): **GO** = 閉形の相手が立つ族が在る ⟹ 第 2 段 `[構築 + probe]` が回せる / **NO-GO** = **候補 2 と候補 4 が同時に死ぬ** + **候補 1 (一般 BC への持ち上げ) の `restatement` 公算の裏づけになる**。
      - **接続先 (facts の節)**: **`## M5 (T3b)`** (⭐ **[probc] 積クラス = 容量領域が既知の別インスタンス族。領域レベルは未決で終わっており死んだ路線ではない** — §3.1 の死んだ路線一覧に積クラスは無い) / **`## N10 (T3c)` N10-e** / **`## N6 (T3c)` N6-j** / **`## N7 (T3c)` N7-p**。
      - ⚠ **N15 の反証条件は着手前に書く義務がある** (§4.4-2) が、⚠⚠ **候補 4 側の 3 本は N10 成果物 §4.2 が SoT ゆえ複製しない** ⟹ **その 3 本を継承し、候補 2 側 (別インスタンス族) の分だけ N15 の起票時に新たに書く** (⚠ **起票は N15 の仕事であり本 leg では書かない**)。
    - **(4) N16 = 分岐 + 棚卸し**。**N15 が GO なら 第 2 段 `[構築 + probe]` / NO-GO なら 棚卸し** (⚠ **§5 の枠表が「棚卸しは N12 と N16 に置く」と定めており矛盾しない**)。**NO-GO 側で残る受け皿 = 候補 3 ((γ) `(R2)` / `(R3)` の層 3 化、型 = `[層3へ載せる]`)** ⟹ ⚠ **判断ログ 5 のテスト可能な線 (載せてよいのは本 relay の `(C2)` leg が立てた命題だけ / 領域 def・計算可能性層に手を出したら停止して子 plan へ回す) を先に引く**。
    - **(5) ⚠⚠ 第 3 段 `[層 3 へ載せる]` は置けない — 枠が無い**。前 2 組 (N6–N8 / N9–N11) はいずれも 3 段そろっていた ⟹ **「判定枠 第 3 組」と呼ぶが 3 段はそろわない**。⚠ **これは明示的な逸脱であって黙って落としたのではない**。
    - **(6) 消した / 消さなかった選択肢**: **候補 1 (一般 BC への持ち上げ、型 = `障害の構造`) = 採らない** — `restatement` 公算が最も高く、N6-j 自身が同じ行で「§0 への payoff は無い」と併記している。⚠ **ただし消滅ではない** — **N15 の NO-GO はこの公算の裏づけになる** / **候補 3 ((γ) の層 3 化) = 消さない** — N16 の NO-GO 分岐の受け皿として残す / **3 本目のアーク = 起票しない** (上の (2))。
    - **(7) §6-4 の配分カウンタ (⚠ 本 leg で実測した)**: **実測値 `10` / 可変枠の消化 = 12** (N14 の着地で 11 → 12。⚠ **N14 は側 = `記録` ゆえ起票行を増やさない**) ⟹ **`10 ≥ 12/2` で不発火**。⚠ **§5.2-3 (2 leg 連続で側が `(C2)` 以外) も N13 = `(C2)` → N14 = `記録` の 1 leg のみゆえ不発火**である。

11. **⭐⭐ N16 = 「現行 [probc] more-capable インスタンスで `UV = C` か」の gate に取る。第 2 段 `[構築 + probe]` は N16 では取らない (2026-08-09、起票)**: ⚠⚠ **`GOAL-CHANGE` ではない** — **§0 / §0.1 は 1 文字も動かない。動いたのは残枠の配分だけ**である。標的 = **N15 成果物 §6-8 / N15 監査 §5-5 が名指しした開いた問い 1 点**。**起票の凍結先 = [`bc-t3c-n16-uv-gate.md`](bc-t3c-n16-uv-gate.md) の §0 (見立て 4 本) / §1 (反証条件 新規 3 本)** で、⚠ **実行 leg はそこを 1 文字も書き換えない** (r19 の作法 = **事後に見立てを直して当てにいく経路を構造的に塞ぐ**)。
    - **(1) ⚠⚠ 条文からの逸脱ではなく、第 2 段の上流に置く gate である**: 判断ログ 10-(4) の条文は「N15 が GO なら 第 2 段」であり、本決定はそれを取り消さない。**根拠** = ⭐ **N15 の判定の骨そのものが「`C ⊆ Thm7 ⊆ UV` はどちらも無条件ゆえ、`UV = C` の族では `Thm7 = C` が挟み込みで強制され比較は情報を持たない ⟹ 生きた候補は `UV ⊋ C` の族に限られる」という選別基準**である (逐語の SoT = facts `## N15 (T3c)` の **N15-b**)。N15 は**この基準で**和チャネル族を選んだ ⟹ **同じ基準を現行 [probc] インスタンスに当てたときに何が出るかは、第 2 段が立つ土台の妥当性検査そのものである**。
    - **(2) ⭐ どちらに転んでも軸が 1 本確定する** (§5.1 の N1 / N15 と同じ設計): **`UV = C`** ⟹ **N7 / N9 / N10 の `Thm7 = C` は挟み込みで強制されていたことになり、判定枠 3 組が §0 に対して持っていた情報量が測られる**。⚠⚠ **これは減算方向の較正であって「材料が出る」ことではない** / **`UV ⊋ C`** ⟹ **[probc] 上の `Thm7 = C` は挟み込みでは説明されない** ⟹ **N15 の選別基準は「`UV ⊋ C` だけでは足りない」ことになり、和チャネル族の選定根拠が 1 段弱まる**。
    - **(3) ⚠ 本 leg は N15 成果物の自己矛盾を閉じる leg でもある**: N15 成果物 §3.2 表 **(B) 行**が [probc] 積 3 クラス全体に `UV ⊋ C` の `○` を付けている一方、同 **§6-8** は「現行 more-capable インスタンスで `UV ⊋ C` かは確かめていない」と書いており**成果物内で矛盾している** (⚠ **監査 訂正 6 が名指し済**。⚠ **監査は全走査のうえ「厳密性の witness は Claim 3 = 逆向き semi-deterministic 側のみ」と確認している** — 逐語の SoT = facts `## N15 (T3c)` の **N15-h** + 監査 §5-5)。
    - **(4) ⚠⚠ 棚卸しを N16 で取らないことは明示的な逸脱である (黙って落としたのではない)**: §5 の枠表は「棚卸しは N12 と N16 に置く」と定めているが、**本 leg では本格的な棚卸し leg を取らない**。⚠ **代わりに義務を条文化する** — **N16 の着地時に「relay 終端の正直な棚卸し」を最小形で残す** (⚠ **実際に書くのは着地 leg の仕事であり、本 entry はその義務を立てるだけである**)。⚠ **「尽きた」型の書き方を最小形の棚卸しでも使わない**。
    - **(5) ⚠ 引き受けたリスクを明示して持つ**: **`UV = C` が出た場合、判定枠 3 組 (N6 … N11) の判定は「情報を持たなかった」側へ較正され、§0 に対する本 relay の収穫はさらに小さくなる**。⚠⚠ **それは本 gate の失敗ではなく、測ったことの結果である** — ⚠ **同時に「だから測らない方がよい」とも書かない** (測らなければ第 2 段は妥当性未検査の土台の上に立つ)。⚠ **`Thm7 ⊋ C` の材料は本 gate でも原理的に出ない** (gate ゆえ)。
    - **(6) §6-4 の配分カウンタ (⚠ 本 leg で実測した。数え方 = §5.1 の `rg` 1 本)**: **実測値 `12` / 可変枠の消化 = 13** ⟹ **`12 ≥ 13/2` で不発火** (⚠ **予約枠 14 の半分 = 7 にも既に届いている**)。⚠⚠ **カウンタは起票行を数える** ⟹ **必ず消化数と対にして読む** — **N16 の起票で `11 → 12` になったが N16 は未消化ゆえ分母 13 は動いていない**。⚠ **§5.2-3 (2 leg 連続で側が `(C2)` 以外) は N14 = `記録` → N15 = `(C2)` → N16 = `(C2)` ゆえ不発火**である。
    - **(7) ⚠⚠ relay の cap と plan の leg 予算は別勘定である (混ぜて書かない)**: **N16 は本 relay の leg 上限 (cap 20) に当たる最後の leg**であり ⟹ **N17 / N18 / N19 は本 relay では走らない**。⚠⚠ **これは §6-3 (20 leg を使い切って未達) の発火ではない** — **plan 側の leg 予算は N16 消化後も 3 leg (N17 / N18 / N19) 未消化のまま残る**。
    - **(8) ⭐ 着地時の追記 (2026-08-09)**: **判定 = `UV = C`** で着地し、**(4) が課した「relay 終端の正直な棚卸しを最小形で残す」義務は §5 の「relay 終端の棚卸し」ブロックで履行した**。**(6) のカウンタは着地時に引き直した** (実測 `12` / 可変枠の消化 `14` ⟹ 不発火。§5 が SoT) / **(5) の引き受けたリスク (収穫がさらに小さくなる側) は実現した** — ⚠⚠ **それは本 gate の失敗ではなく測ったことの結果である**。⚠ **本 entry の (1)–(7) は書き換えていない**。

12. **⭐⭐ relay の cap を 20 → 23 へ延長し、残る 3 leg (N17 / N18 / N19) を走らせる。N17 = 単位 B の受け皿昇格 + gateway atom (中核 8) に取る (ユーザー決定、2026-08-09)**: ⚠⚠ **`GOAL-CHANGE` ではない** — **§0 / §0.1 は 1 文字も動かない。動いたのは relay の幅だけ**である。
    - **(1) ⚠⚠ 判断ログ 11-(7) の条項「relay の cap と plan の leg 予算は別勘定である」は維持される** — **今回はたまたま両者が一致する幅 (cap 23 ⟺ 残枠 3) にしただけ**であって、⚠ **条項が消えたのではない** ⟹ **以後も「cap に当たった」と「plan の予算を使い切った」を混ぜて書かない**。⚠⚠ **§6-3 の発火でもない**。
    - **(2) N17 = 子 plan の単位 B (3 レート `Thm7(W)` の領域層) に取る**。根拠 = **§5 の枠表の N17 / N18 行が既に単位 B へ割り当てている** + 子 plan [`bc-computable-region-formalization-plan.md`](bc-computable-region-formalization-plan.md) **§3.2 (ii)** (「A に依存しない / 親の N17 / N18 はここに充てられる」) と **同 (iv)** (gateway atom = 中核 8 = **合併レベルの閉性を `closure` 無しで出す**)。**起票の凍結先 = [`bc-t3c-n17-unit-b.md`](bc-t3c-n17-unit-b.md) の §0 (見立て 4 本) / §1 (反証条件 新規 3 本)** で、⚠ **実行 leg / 監査は 1 文字も書き換えない** (r19 / r20 で確立した作法)。
    - **(3) ⚠⚠ 判断ログ 5 のテスト可能な線との関係を先に処理する (⚠ 書かずに着手すると後から「線を越えた」と読める)**: 判断ログ 5 は「**§6-5 が切り出した受け皿 (領域 def・計算可能性層 = 子 plan の単位 B) に手を出し始めたら停止して子 plan へ回す**」と書くが、⚠⚠ **それは判定枠の第 3 段 (`(C2)` leg) に対する条項**である。**N17 / N18 は形式化枠として最初から単位 B へ割り当てられている** (§5 の枠表) ⟹ ⚠ **本 leg は条項違反ではない**。⚠⚠ **線そのものは 1 mm も緩めない** — **`(C2)` leg が単位 B に手を出したら従来どおり停止する**。
    - **(4) ⚠⚠ 引き受けたリスク**: **§6-5 は既に「収まらない」と判定している** (判断ログ 3 / facts `## N2 (T3c)` の N2-a) ⟹ ⚠⚠ **N17 / N18 が §0 の Lean 側へ到達する見込みは無い**。⚠⚠ **本 leg の成果を「§0 に近づいた」と書かない** (§0.1-2)。⚠ **同時に「だから走らせる意味が無い」とも書かない** — 走らせる意味は **単位 B の受け皿を層 3 に実在させ、中核 8 の実際の難度を初めて機械で測ること**である (⚠ **中核 8–10 は 0/3 で、その行数は 1 行も測れていない**)。
    - **(5) §6-4 の配分カウンタ (⚠ 本 leg で実測した。数え方 = §5.1 の `rg` 1 本)**: **実測値 `12` / 可変枠の消化 = 14** ⟹ **`12 ≥ 14/2` で不発火** (⚠ **予約枠 14 の半分 = 7 にも既に届いている**)。⚠ **N17 は側 = `形式化債務` ゆえ起票行 (分子) を増やさない** / ⚠⚠ **分母 (可変枠の消化) も増えない — 形式化枠は可変枠ではない** (§5.2-1) ⟹ **本 leg では分子・分母のどちらも動かない**。
    - **(6) §5.2-3 の発火判定 (⚠ 実測)**: **N16 = `(C2)` → N17 = `形式化債務`** ゆえ **1 leg 目**で**不発火**。⚠ **N18 も `形式化債務` / N19 は `記録` なので、N19 の起票前に「2 leg 連続で側が `(C2)` 以外」が必ず立ち、§5.2-3 の義務 (カウンタを引く) が発火する**。⚠⚠ **その発火は異常ではなく設計どおりの検知である** (判断ログ 9 の引き受けたリスクと同型 — **側を `(C2)` 以外に取ると分子が増えないまま分母だけが動きうる**。⚠ **ただし本 relay の残枠は形式化枠 2 + 記録 1 で可変枠を 1 leg も含まない**ので、⚠ **分母は 14 のまま動かない**)。⚠ **先に書いておくのは、発火時に「想定外」と読まれないためである**。
