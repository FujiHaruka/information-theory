# 一般 BC の計算可能な容量領域 — 未解決本体 (T3) への攻略 第 3 次

> **Parent**: [`broadcast-channel-moonshot-plan.md`](broadcast-channel-moonshot-plan.md)
> **Predecessor**: [`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md)
> (M0–M19 完走・**§0 のゴールは未達**で終了。⚠ **その plan は閉じており書き換えない**)。
> 確定事実 = [`bc-facts.md`](bc-facts.md) / attack 台帳 =
> [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) / (C2) 側の在庫 =
> [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md) / 前 relay の失敗形と運用の実測 =
> [`../proof-logs/proof-log-bc-open-problem-t3b-relay.md`](../proof-logs/proof-log-bc-open-problem-t3b-relay.md)

**Status**: 起票 (2026-08-04)。ユーザー決定により**継続**し、**新規 20 leg (N0–N19)** の予算を切った
(§7 判断ログ 1)。⚠ **§0 のゴールと完了条件は前 plan から 1 文字も動かしていない** (§0.1-1)。
⚠⚠ **前 2 次は合わせて 41 leg を使って未達で終わっている** — その実績の上での第 3 次である。
**入口は 1 点**: `C` の和レート面の**端**を `Thm7` が超えられるか (§1)。
⚠ **前 relay は NO-GO を 1 本も出しておらず、`Thm7 ⊄ Thm8` は 1 度も主張されていない。
`R ∈ Thm7` も言えておらず、`Thm7 ⊋ C` の材料は 1 つも無い**。
**Branch**: `bc-computable-region`

## 進捗

- [ ] N0 gate — 資産の生存確認 + 在庫の差分取り直し 📋
- [ ] N1 gate — 入口 1 点の第一手 (反証試行) 📋
- [ ] N2 ⭐ early gate — Lean 側の量化子の段の**員数を測る** (形式化枠の内数) 📋
- [ ] N3–N16 (C2) 本体の可変枠 14 📋
- [ ] N17 / N18 形式化枠 (残り 2) 📋
- [ ] N19 記録 📋

---

## 0. ゴール — 未解決本体そのもの

**一般 2 受信者 DM-BC の容量領域の計算可能な特徴付けを、肯定側か否定側のどちらかで決着させる。
散文の証明と、その Lean による機械検証の両方をもって完了とする。**

完了条件 (どちらか一方):

- **肯定側** — 任意の `ε > 0` に対し有界時間で `ε` 近似を返す手続きを構成し、その正当性を証明する。
- **否定側** — そのような手続きが存在しないことを証明する。

いずれの側も、**散文の証明が立つこと**と**Lean で proof done に到達すること** (0 sorry / 0 residual、
`#print axioms` が sorryAx-free、かつ署名走査で load-bearing hypothesis が無いこと) の両方を満たして
初めて達成とする。片側だけでは達成ではない。

### 0.1 ⚠ ゴール保護条項 — 本節は leg / planner が書き換えてはならない

**前 relay の前身で実際に起きた事故がこれである**。原案 (`487afc03`) は T2 の完了条件を
「文献に無い命題が **Lean で機械検証される**」と書き、「**ユーザー指定の二段構え
(散文で証明 → 形式化で検証)**」と明記していた。それを `28a740c5` が **L0 が走る前に**
「散文レベルの候補経路」へ書き換え、形式化をスコープ外にした。判断ログにはユーザー判断としての
記録が無い ⟹ **planner の独断でゴールが狭まり、20 leg がその狭いゴールで回った**。

したがって本 plan でも以下を規約とする:

1. **§0 のゴールと完了条件を狭める変更は、leg / planner が適用してはならない**。狭める提案は
   判断ログに **`GOAL-CHANGE`** として起票し、**ユーザーの承認を得るまで適用しない**。
   広げる方向 (より強い完了条件) は起票不要。
2. **中間結果は記録するが、ゴールの代替にはしない**。台帳 (facts / attacks / 在庫) への収穫は
   従来どおり続けるが、**それをもって「ゴールに到達した」と書かない**。かつての階段 (T0–T3) が
   持っていた「どこで止まっても成果が残る」というヘッジは、**記録の設計としては継承し、
   ゴールの設計からは外す** — ユーザーが「成果ゼロで終わる可能性を引き受ける」と明示的に選んだ。
3. **各 leg は冒頭に「この leg は §0 のゴールに対して何を動かすか」を 1 行で書く**。書けない leg は
   起票しない。⚠ **本 relay ではこの 1 行に「側」を併記させる** (§5.1)。

---

## 1. 標的の現在地 — 入口は 1 点

⚠ **値と逐語はここへ複製しない**。判定の SoT は [`bc-facts.md`](bc-facts.md)、状態の SoT は
[`bc-open-problem-attacks.md`](bc-open-problem-attacks.md)、標的の分解 ((C1) 内界側 / (C2) 外界側 /
(C3) 有効収束率) と穴が (C2) 1 つに絞られた経緯は
[`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) §1.1 が SoT である。

**入口 (前 relay が park した唯一の的、SoT = facts `## M19 (T3b)` の M19-4 行 =
`## M6 (T3b)` 最終行)**:

> **`C` の和レート面の端 `R^end` を `Thm7` が超えられるか** — すなわち `R₀+R₁` が `C` 側の閉形の
> 天井を超え、かつ和レートが `SR_C` に留まる点を運ぶ `Thm7` witness (facts が数え上げた
> 全 `T_J` = 11 種すべてで適格) が在るか。

**入口がこの 1 点である根拠 (⚠ 3 値はすべて facts が SoT。ここには写さない)**:

- `C` 側の天井は `## M6 (T3b)` 行 4 で**閉形と恒等式で確定**している (最適化器が立てたものではない)。
- `Thm7` 側は `## M14 (T3b)` の対角 witness が**ちょうどその天井に接している** (`## M6 (T3b)` 行 5)。
- 接点で拘束しているのは **`(18b),(18c),(18d)` の 3 本ちょうど**で、⭐ **その 3 本は自分自身の天井には
  触れていない** (対角 witness は `W` が自明なので `(18b) = I(U,W;Y)` に対し `I(X;Y) = 2C` まで余地がある)。

⟹ **問いは「`(18b),(18c),(18d)` を同時に天井より上げたまま、全 `T_J` で適格性を保ち
`(18i) = SR_C` を維持できるか」**であり、押し上げられれば **`Thm7 ⊋ C`** が出る。

⚠⚠ **これは「あと少し」ではない**。**余地があること**と**適格性を保ったまま押し上げられること**は
別命題であり、**後者は 1 度も試されていない**。⚠ **「残るのは行数と配線だけ」型の言い回しは
tell として名指しで禁じる** (§4.1 軸 2)。

### 1.1 落としてはならない 4 点 (前 plan §1 から継承。⚠ 逐語は複製しない)

- **(i) (C2) の同値形** — 要求は「上から収束する外界**列**」では**ない**。`C` を共通部分にもつ、
  指標と `W` について一様に半計算可能な外界の**可算族が 1 つでも存在するか**と同値である
  (減少性も有効収束率も要らない。facts `## L2 (T3)` 節)。
- **(ii) 還元先は 1 点に縮んでいる** — `Thm7(W)` の一様 effective compactness は証明済ゆえ
  **(C2) ⟸ (`Thm7(W)` と `C(W)` の厳密一致)**。⚠ **「未決」であって「偽」ではない**。
- **(iii) 穴の正体** — (C2) は [Li21] §VIII の open problem (= BC の単文字特徴づけ問題) と
  **同一の穴**である (facts `## L3 (T3)` 節)。
- **(iv) 難度の較正** — **否定側は Marton 最適性予想の反証を含意する** (facts `## L0 (T3)` 節)
  ⟹ 問題は新規性ではなく**難度**である。

⚠ **前 relay の中間結果は出発点を与えない** — 出発点は上の入口 1 点であって、M2–M18 の成果物の
上ではない。⚠ **前 relay の形式化 3 leg (M16–M18) が載せたのは「制約右辺どうしの関係」**であって
Theorem 7 / Theorem 8 そのものではない (facts `## M19 (T3b)` の M19-5 行)。

---

## 2. Approach

**§0 の完了条件は「散文の証明」と「Lean の proof done」の両方**である。前 2 次はどちらの側にも
届かなかったが、**未達の形が 2 次で違う** — 第 1 次は配分事故で標的に leg が入らず、第 2 次は
標的に 14 leg を入れたうえで**入口の手前で止まった**。⟹ 本 relay は「配分を守る枠構造」は
そのまま継承し、**その上に「Lean 側の最も重い段の員数を先に測る早期 gate」を 1 本足す**。
20 leg の形は次のとおり。

- **入口 1 点を両側から測る系統 (可変枠 14 leg、側 = `(C2)`)** — §1 の問いを [探索 + gate] →
  [構築 + probe] → [層 3 へ載せる] の 3 段 1 組で回す。**肯定側 (押し上げられる = `Thm7 ⊋ C`) と
  否定側 (押し上げは不可能 = 天井が `Thm7` 側にも立つ) のどちらに転んでも軸が 1 本確定する**ので、
  第一手 (N1) は gate として最も安い。
- **⭐ Lean 側の員数を先に測る系統 (early gate 1 leg、形式化枠の内数、側 = `形式化債務`)** —
  §0 は Lean の proof done を含むのに、**領域としての `Thm7(W)` / UV 外界の def と
  `∃p ∀T_J ∃aux` の量化子の段は前 relay のどの leg でも試されておらず見積りが存在しない**
  (facts `## M19 (T3b)` の M19-5 行)。⟹ **散文が立ってからその段で詰まると §0 は満たせない**。
  leg 19 で不可能と判明するより **leg 2 で員数を知る方が安い** (CLAUDE.md「gateway-atom-first」)。
  ⚠ **この leg は形式化枠 3 の内数**であり、**可変枠からは 1 leg も出さない** (§5.2)。
- **層 1 が通った命題はその命題単位で層 3 へ載せる** — 全体が揃うのを待たない (前 relay で有効)。
- **層 2 の道具は 3 つに限る** (§4.5)。⚠ **数値掃引は原理的に効かない** — T3 の主語は計算可能性で
  あり、**最適化器の非違反は証拠価値がほぼ 0** (前 relay で 6 度再現)。

```
層 1  散文        アイデア + 証明スケッチ
層 2  proof-probe 核 1〜2 ステップを機械検証 — 散文の自己欺瞞を最短で殺す装置
層 3  形式化      散文全体を Lean へ                            ← スコープ内
```

---

## 3. 攻撃軸

| 軸 | 主語 | 優先度 | 第一手 |
|---|---|---|---|
| **T3-α** | **肯定側 / 分離側** — §1 の入口 1 点 | 1 | **N1 = 反証試行**。`(18b),(18c),(18d)` を同時に天井より上げる witness を、全 `T_J` の適格性を保ったまま取りにいく。⚠ **手構成か恒等式で決める** (§4.5)。**押し上げられれば `Thm7 ⊋ C`、天井が `Thm7` 側にも恒等式で立てば還元先が 1 段強くなる** ⟹ どちらに転んでも軸が 1 本確定する |
| **T3-β** | **否定側** — effectively compact でないことの証明 | 2 | 同じ穴の裏。真の未着手部分は「チューリング機械 → BC チャネル行列」の還元 (attack `t3-tm-to-bc-reduction`)。⚠ **「文献に無い」≠「不可能」** / ⚠ **決定不能性 ≠ `ε` 近似不可能性** — 混ぜると誤った壁宣言になる。⚠ **肯定側は選言 / 否定側は連言** (肯定側は候補 1 本が当たれば決着、否定側はすべてを潰す必要がある) |

⚠ **優先度を 1 本に絞ったのは前 relay との差である** — 前 relay は T3-α / T3-β を同順位に置いたが、
入口が 1 点に確定した以上 **gate-first は T3-α 側**である。T3-β へ振り替えるかは §6-1 の判断に載る。
attack の状態と死因の SoT は [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) であり、
**本 plan は状態をキャッシュしない**。

### 3.1 ⚠ 死んだ路線 — 再探索しない

**内容はここへ複製しない**。前々 plan [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md) §3.1 が
列挙する 8 路線 (内界 = 外界 / 外界 = 容量 / 停止則の独立化 / 階層のレベル下げ / UV 外界の多文字化 /
`∃` 形外界の多文字化 / [Li21] Theorem 10 経由 / `{Z}` の閉包演算だけで作る支配チャネル族) は
**すべて死因つきで確定しており、本 relay でも再探索しない**。⚠ **同時に落としてはならない射程の
限定 2 つ**も前 plan §3.1 の逐語のまま継承する (死んでいるのは「多文字化」という 1 機構だけ /
[Li21] の Tarski 指数関数問題の留保は **exact decision の語義**であり `ε` 近似を否定しない)。

**前 relay で新たに死んだもの (⚠ SoT は facts の各節。⚠ どれも「包含の否定」ではない)**:

- **S9 = 点ごと witness 移送 (恒等写像)** — `## M15 (T3b)` で**反例つきで 偽**。独立監査済。
  ⚠ **死んだのは「移送」という方法であって `Thm7 ⊄ Thm8` ではない**。
  ⚠ **反例 witness が運ぶ角は `C` の内点である** (`## M6 (T3b)`) ⟹ **`Thm7 ⊋ C` の材料ではない**。
- **単一 `T_J` インスタンス経由の証明路** — `## M10 (T3b)` で反例つきで死亡。
- **時分割 gluing** — `## M11 (T3b)` で**恒等式により**死亡 (探索の失敗ではない)。
- **`∃∀` swap (領域版)** — `## M12 (T3b)` で偽 (3 段の恒等式)。

⚠ **禁止する書き方**: `Thm7 ⊄ Thm8` を主張する / 「方法の死」を「包含の否定」と書く /
`R ∈ Thm7` と書く / 「あと少し」「残るのは行数と配線だけ」型の言い回し。

---

## 4. 運用規約

### 4.1–4.3 継承 (⚠ 再掲しない。逐語の SoT は前 plan)

- **§4.1 失敗形 2 軸** — 軸 1 (未検証の否定的判断を機械に落とさないまま設計へ通す) /
  軸 2 (残件の員数を prose だけで見積もる)。**処方は 2 本立てで 1 本に畳まない** —
  軸 1 は着手*前*に 1 行の反証、軸 2 は gateway atom 通過*後*に在庫を取り直す。
  逐語は前 plan [`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md) §4.1。
  ⚠ **軸 1 は本 relay で §4.4 が強化する**。
- **§4.2 在庫と証拠の作法 3 規約** — 結論形で数える / 署名でなく証明本体を読む /
  loogle の生クエリのヒット数を「無い」の根拠にしない。逐語は前 plan §4.2。
  ⚠ **在庫 [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md) §6 は、この 3 規約のうち 3 本目が
  実測で 2 回発火した記録を持つ** (`Primcodable ℚ` と Hausdorff instance がどちらも生クエリ `Found 0`
  なのに実在した)。
- **§4.3 検証の誠実性** — CLAUDE.md の Definition of Done (Standard B) がそのまま効く。
  **`sorry` + `@residual(<class>:<slug>)` が唯一の honest な退出路**であり、本 relay の slug は
  **`@residual(plan:bc-open-problem-t3c-plan)`** (= 本ファイルの filename stem)。
  Mathlib の不足に当たった場合のみ `@residual(wall:<name>)` を立て、**register への追記を伴う**。
  ⚠ **hypothesis に証明の核を担わせて `sorry` を消すのは禁止**。
  ⚠ **`sorryAx`-free は必要条件であって十分条件ではない** — **署名走査**を必ず併せる。
  ⚠ **機械で再導出できる事実 (sorry の有無 / 宣言の存在 / axiom 依存) を本 plan の散文に
  キャッシュしない**。

### 4.4 ⭐ 見立ての較正 — ⚠ 向きによって外れ方が非対称である (新設)

前 relay の実測 (SoT = proof-log §3、母集団の定義つき)。⚠ **母集団を先に固定する。台帳には
`9` が 2 つあり別勘定である** — **母集団 A** = `## M14 (T3b)` / `## M15 (T3b)` / `## M6 (T3b)` の
3 節が持つ勘定行で合計 9 (覆った 5 + 当たった 4) / **母集団 B** = 全節から「義務を追加する /
締める方向」の見立てを拾ったもので 9 件。

- **義務を締める方向 = 母集団 B で 9 件、9 件とも機械で覆った** (例外 0)。
- **義務を緩める方向 = 当たり 3 / 外れ 5 で、⚠ 外れの方が高くついた** — 締める側の外れは
  「要らない義務を用意した」で済むが、緩める側の外れは**その見立ての上に leg を組んでから死ぬ**。
- ⚠⚠ **「緩める提案は当たりやすい」とは書かない** — 成立しない。**非対称なのは
  「締める方向が全件覆る」という一方だけ**である。

⟹ **処方 (2 本、別々に走らせる)**:

1. **締める方向の見立ては着手前に 1 行の反証を機械に掛ける** (= §4.1 軸 1 の強化)。
   ⚠ **ブリーフの各見立てに「締める / 緩める」を明記させる**。
2. **緩める方向の見立ては、着手前に §4.5 の 3 つのどれで殺せるかを先に決める**。
   決められないなら leg の主戦力に据えない。

### 4.5 ⭐ 層 2 の方法論 — 効く道具は 3 つだけ (新設)

⚠⚠ **最適化器の非違反は証拠価値がほぼ 0 である** (前 relay で **6 度**再現)。典型は許容差を
`1e-2 → 1e-4 → 1e-6` と締めると超過が**単調に潰れる**形で、「最適化器が見つけた超過」は
境界の緩みそのものであって命題の反証ではなかった。⟹ **掃引の非違反は「まだ見つかっていない」
以上のことを言わない**。

**効いたのは次の 3 つだけである**:

1. **恒等式への帰着** — 係数が記号的に相殺する形まで持っていく。
2. **適格性 / 最適性の分離構造を使った全探索** — 空間全体を掃くのではなく、構造で候補を
   有限個に落としてから尽くす。⚠ **決定的 witness の全探索は整数 bit しか実現できない**
   ⟹ **確率的 witness を併せる** (前 relay は決定的全探索が完全適格な反例を取りこぼす事故を
   3 度検出している)。
3. **閉形の族 / 反例の手構成** — 明示 witness の直接評価。

⚠ **1 と 3 は「値は機械・導出は我々」という混合の confidence を持つ** — facts へ書くときは
`machine` と `human-judgment` を**分けて書く**。

### 4.6 ⭐ 否定的判定には敵対的な独立監査を掛ける (新設)

**反例 / 壁 / FALSE を出した leg には独立監査を掛ける**。⚠ **既定の立場を「反例は偽」に置いた
敵対的ブリーフ**にすること。前 relay で有効性が実証されている (M15 の監査は反例を壊す 7 本を
実行して 7 本とも不成立を得たうえで、**副産物として `R1SET` の取りこぼしを LP で塞ぎ、逐語を
引き直し、文言の誤り 1 件を検出**した)。⚠ **立場を書かずに「確認せよ」とだけ書いた監査は
追認に潰れる**。⚠ 監査器は**被監査 leg の検証器を 1 行も呼ばない独立実装**にする。

### 4.7 ⚠⚠ 報告を成果物の証拠にしない (新設)

**`git log` / `ls` / ファイル本体で必ず裏を取る**。前 relay の実測は 2 種類ある
(⚠ **別の事象として扱う — 対処が違う**):

- **(a) subagent が最終報告を送らず idle 化した事故が 6 例**。⚠ **`SendMessage` での個別の
  問い合わせは有効**である。
- **(b) 報告と実態が乖離した事象が 1 件** — commit hash・変更ファイル数・行数まで具体的な完了報告が
  届いたのに、検証時点でそのいずれも存在しなかった。⚠⚠ **出所は未特定であり、原因も責任も書かない**。

⟹ **報告の内部整合性は成果物の実在を一切保証しない**。⚠ **idle かどうかは信号にならない** —
**完了報告を返した agent にも等しく機械検証を当てる**。

### 4.8 ⭐ 再発射の前に前任の生存を確認する (新設)

前 relay で、検証が「成果物が存在しない」と出たことを受けて再発射した結果 **2 体が同じ task に付き、
共有ファイルの衝突寸前になった**。⟹ **検証で「成果物なし」と出たときも、再発射の前に前任が
生存していないかを確認する**。⚠ **これは手順の穴であって agent の失敗ではない**。

### 4.9 ⭐ scratchpad はセッション固有 — 資産の絶対パスをブリーフに書く (新設)

一次文献 (`pdftotext -layout` 済) と数値検証器は**セッション固有の scratchpad** に在り、
⚠ **検証器を作り直すと 1 leg 溶ける**。⟹ **各 leg のブリーフに前 leg の scratchpad の
絶対パスを定型項目として書く**。

**前 relay の資産 (⚠ 起票時点で生存を機械確認済。⚠ セッション固有ゆえ消えうる — 消えていたら
facts `## L0 (T3)` 節の URL から `pdftotext -layout` で再生成する)**:

```bash
# ベース (生存確認: ls "$SP" / ls "$SP"/{lit,m6,m13,m14,m15,m15audit})
SP=/private/tmp/claude-502/-Users-haruka-dev-lean-projects/d6d1ee1c-b8f7-4b1b-9f17-e7eb0b9e0627/scratchpad
#   $SP/lit/            一次文献 16 本 (auxrec / probc / GK-outer / li21 / n13 …)
#   $SP/m6/m6_verify.py  `R ∈ C` を 1 行で判定 (3 経路一括) / $SP/m6/lib6.py  `C` の閉形評価器
#   $SP/m13/m10_fast.py  Thm7 / Thm8 の高速評価器
#   $SP/m14/lib14.py     `(G,K) = (Y₁,Z₂)` の評価器 / $SP/m14/step0.py  較正 4 本
#   $SP/m15/m15_fast.py + $SP/m15/viol*.npy   反例 witness
#   $SP/m15audit/aud.py  独立評価器 (⚠ `(31a)`–`(31e)` しか持たない)
```

⚠ **`aud.py` の射程限定を落とさない** — `(31f)/(31g)` は持たないので、領域判定に使うときは
`auxrec.txt` から独立転記した側と併用する (`## M6 (T3b)` の M6-b 行が SoT)。

---

## 5. leg 予算と配分

**20 leg (N0–N19)**。⚠ **前 relay の枠構造は配分事故を防ぐことに成功した** (可変枠 14 leg が
14 本とも `(C2)`、§6-4 不発火) ⟹ **枠構造そのものを継承する**。⚠ **変更は 1 点だけ** —
形式化枠 3 のうち 1 leg を **early gate として先頭近くへ移す** (§2、採否の根拠は §7 判断ログ 2)。

| 枠 | leg | 側 | 内容 |
|---|---|---|---|
| **N0** | 1 | gate | **資産の生存確認 + 在庫の差分取り直し**。§4.9 の scratchpad 資産が生きているかを機械で確認し、欠けていれば再生成する。あわせて (C2) 在庫 [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md) のうち **N1 / N2 が消費する節だけ**を結論形で取り直す。⚠ **在庫は `mathlib-inventory` の担当ファイルへ書く。本 plan には書かない** |
| **N1** | 1 | gate | **入口 1 点の第一手** (§3 の反証試行)。⚠ **GO / NO-GO のどちらでも軸が 1 本確定する**設計なので、ここで「判定不能」に落ちたときだけ §6-1 が発火する |
| **N2** | 1 | **形式化債務** (⚠ **形式化枠 3 の内数 1/3**) | ⭐ **early gate = 量化子の段の員数を測る**。標的は facts `## M19 (T3b)` M19-5 の (a)(b) — 領域としての `Thm7(W)` / UV 外界の def と `∃p ∀T_J ∃aux` の量化子。⚠ **先に決めるべき未決の設計軸が 1 本ある** (在庫 §2.5 = 3 レートで受け皿を新設するか `R₀ = 0` スライスで既存 2 レート資産に載せるか) — **一次文献 diff 抜きに選ぶのは CLAUDE.md「textbook-object strength diff」が名指しする事故形**なので、diff を先に取る。⚠ **成果物は「員数と段のリスト」であって実装ではない** (実装が始まったら §6-5) |
| **N3–N16** | **14** | **(C2)** | **(C2) 本体の可変枠 (⚠ 予約枠)**。[探索 + gate] → [構築 + probe] → [層 3 へ載せる] の 3 段 1 組で回す。⚠ **この枠を他の用途に流用しない** |
| **N9 / N15** | (内数) | — | 棚卸し — 軸の枯渇カウンタ、**側の配分カウンタ** (§6-4)、撤退ラインの判定 |
| **N17 / N18** | 2 | 形式化 | **形式化枠の残り (2/3・3/3)**。(C2) について層 1 が通った命題を Lean へ。⚠ **N2 が返した段のうち、(C2) の形式化を直接 unblock するものに限る** |
| **N19** | 1 | 記録 | 記録 — proof-log + metrics + 収穫の書き上げ + 正直な棚卸し |

⟹ **(C2) 本体の枠は 14 / 20 = 過半**であり、**前 relay と同数である** (early gate は形式化枠の
内数なので可変枠を削っていない)。

### 5.1 ⚠ leg 冒頭の宣言規約 — 「どちらへ流れているか」を leg 単位で見えるようにする

**各 leg は起票時 (ブリーフ) と着地時 (本節への追記) の両方に、次の 1 行を同じ形式で書く**:

```
**leg 冒頭宣言 (N<n>)**: 側 = <(C2) | (C1) | 形式化債務 | 記録> / 動かすもの = <§0 のゴールに対して何を動かすか 1 行>
```

- **側の語彙は 4 つだけ**: `(C2)` / `(C1)` / `形式化債務` / `記録`。⚠ **`gate` は独立の側ではない** —
  gate leg も何の側の gate かを書く (N0 / N1 は `(C2)` の gate、**N2 は `形式化債務`**)。
- **書けない leg は起票しない** (§0.1-3)。
- **数え方**: 本ファイルに `rg -c '^\*\*leg 冒頭宣言 \(N[0-9]+\)\*\*: 側 = \(C2\)'` を当てる。
  ⚠ **この数が §6-4 の入力である**。⚠ **上の雛形は `<…>` 形なのでカウントに混じらない**
  (前 relay は雛形行と条項が混じって数え直しが要った)。⚠ **gate 2 本 (N0 / N1) は可変枠ではない**
  ので、§6-4 を引くときは **N3–N16 の行だけ**を数える。

### 5.2 ⚠ はみ出し禁止条項 — 「形式化債務が可変枠を食う」形を具体で禁じる

前々 relay の事故は**形式化債務が可変枠を 14 leg 連続で食った形**である。⚠ **本 relay は
early gate を置く分、この条項の重要度が上がる**。

1. **形式化債務を N3–N16 の枠で消化してはならない**。必要になったときは **N2 / N17 / N18 の
   内数**であり、それを超える分は**別 plan へ切り出す** (`@residual(plan:<新 plan の filename stem>)`
   で参照し、本 relay の枠は使わない)。
2. **「(C2) を進めるための前提工事」という理由づけで可変枠を形式化債務に充てる場合、
   その leg の冒頭宣言の側は `形式化債務` である** — `(C2)` と書いてはならない。
3. **2 leg 連続で側が `(C2)` 以外になったら、3 leg 目を起票する前に §6-4 の配分カウンタを引く**。
4. ⭐ **N2 は測るだけの leg である** — **員数と段のリストを返して終わる**。⚠ **その場で実装に
   なだれ込まない** (なだれ込んだ時点で形式化枠 3 を 1 leg で使い切る形になる)。⟹ §6-5。

---

## 6. 撤退ライン

**⚠ 撤退ラインはゴールを下げる装置ではない** (§0.1-2)。撤退とは「本 relay を終了して次の判断を
ユーザーに返す」ことであって、「達成条件を緩める」ことではない。

- **6-1**: **N1 の gate が GO / NO-GO のどちらも返さない** (押し上げも天井の恒等式も測れず
  「判定不能」で終わる) ⟹ **N9 の棚卸しまで待たずに**、T3-β 側の第一手へ振り替えるかを含めて
  ユーザーに判断を返す。⚠ **数値で分離らしきものが出ただけでは GO/NO-GO のどちらでもない**
  (§4.5)。
- **6-2**: **N15 の棚卸しで、(C2) について層 3 に載せられる散文が 1 本も無い** ⟹ N17 / N18 の
  形式化枠を層 1 へ戻すかをユーザーに諮る (⚠ **`GOAL-CHANGE` ではない** — 完了条件は動かさず
  配分だけ動かす)。⚠ **前 relay ではこのラインが不発火で、しかも申し送りの予測を覆した**
  (「載らない」の根拠に置いた対象を実際には経由しなかった = §4.1 軸 1 の 5 件目)。
- **6-3**: **20 leg を使い切って未達** ⟹ **未達と書く**。到達した中間結果は台帳に残すが、
  **ゴール達成とは書かない**。⚠ **前 2 次とも発火した実績があるライン**である。
  ⚠ **発火とは「未達と書く」ことであって、達成条件を緩めることではない**。
- **6-4 (配分の撤退ライン)**: **(C2) 本体の枠が形式化側に食われていないかを機械で数える**。
  可変枠 N3–N16 を `N` leg 消化した時点で、**冒頭宣言の側が `(C2)` だった leg 数が `N` の半分を
  切ったら、その場でユーザーに判断を返す** (棚卸しの leg を待たない)。⚠ **予約枠 14 の半分 = 7 に
  届かないことが確定した時点でも同じく発火する**。⚠ **数え方は §5.1 の `rg` 1 本**であり、
  prose の印象で判定しない。
- **6-5 (⚠ 新設 — early gate の撤退ライン)**: **N2 が返した員数が、残る形式化枠 2 leg
  (N17 / N18) に収まらない** ⟹ **可変枠に吸わせない**。**別 plan へ切り出して
  `@residual(plan:<新 plan の filename stem>)` で参照し、「本 relay の中では §0 の Lean 側へ
  到達できない」ことを明記したうえでユーザーに判断を返す**。⚠ **これは `GOAL-CHANGE` ではない** —
  §0 は動かさず、**届かないという事実を書く**だけである。⚠ **N2 が「収まる」と返したときも、
  §4.1 軸 2 により N17 着手*後*に残りの段の在庫を取り直す**。

---

## 7. 判断ログ

書く頻度: 方針変更 / 撤退 / 当初仮定の修正。決着済 entry は削除 (git が履歴)、active のみ残す。
プラン予算 ≤ 600 行 / active 判断ログ ≤ 10 entry。
⚠ **番号は本ファイルで独立**である (前 plan の判断ログとは別勘定。相互参照は「前 plan 判断ログ N」と書く)。

1. **継続 + 新規 20 leg (ユーザー決定、2026-08-04)**: 前 relay
   ([`bc-open-problem-t3b-plan.md`](bc-open-problem-t3b-plan.md)) が M0–M19 を完走して
   **§0 のゴール未達**で終了し、次の判断 (第 3 次 relay / 否定側への振替 / 形式化債務 / 終了) を
   ユーザーに返した。ユーザーは **継続**を選択し、**新規 20 leg の予算**を切った ⟹ 本 plan を新設。
   ⚠ **引き受けたリスクを明示して持つ**:
   - **前 2 次は合わせて 41 leg を使って未達だった**。⚠ **第 2 次は配分事故を起こしていない**
     (可変枠 14 本とも `(C2)`) ⟹ **本 relay の未達リスクは配分では説明できない**。
     枠構造を継承しても賭けの中身は変わらない。
   - **(C2) は一次文献が未解決と明言している穴**であり (§1.1 (iii))、**否定側は Marton 最適性予想の
     反証を含意する** (§1.1 (iv))。⟹ **20 leg 使ってゴール未達で終わる可能性は依然として高い**。
     それを「中間結果は出た」で埋め合わせない (§0.1-2)。
   - ⚠ **入口が 1 点に絞れたことは「近づいた」を意味しない** — §1 の余地は**余地があること**を
     言うだけで、**適格性を保ったまま押し上げられるかは 1 度も試されていない**。
   - ⚠ **§0 は Lean の proof done を含み、その側の最も重い段は員数すら測られていない**
     (facts `## M19 (T3b)` M19-5) ⟹ **散文側が決着しても §0 は自動では満たされない**。

2. **early gate (N2) を採用する。⚠ 採用にあたって前提を 1 点訂正した (2026-08-04)**:
   - **採否 = 採用**。根拠は 2 つ — (a) §0 の完了条件が Lean の proof done を含むのに、その側で
     最も重い段の**見積りが存在しない** (facts `## M19 (T3b)` M19-5 が「本 relay で 1 度も試していない」
     と明記)、(b) **leg 19 で不可能と判明するより leg 2 で員数を知る方が安い**
     (CLAUDE.md「gateway-atom-first」)。
   - **⚠ 訂正 — 「その段は完全に未計測」は正確ではない**。在庫
     [`bc-t3b-c2-inventory.md`](bc-t3b-c2-inventory.md) §9 の型検査試験は、**`min` 入りの制約束 (T3) /
     `⋃_p ⋂_{T_J} ⋃_{aux}` の入れ子 (T4、両向きとも) / 3 レートの周囲空間 (T5) が
     いずれも通ることを実測している**。⟹ **未計測なのは「入れ子が書けるか」ではなく、
     (α) 9 本の補助変数を持つ法則述語 (在庫 §3 (g))、(β) effective compactness を述べる段が要求する
     量化子補題 (L-∃)/(L-∀) (在庫 §3 (j)、Mathlib にも in-project にも不在 = 在庫 §6 W2)、
     (γ) 領域としての def そのもの**である。⚠ **T4 の実測は「型検査は入れ子の取り違えを守らない」
     という注意つき**なので、通ったことを「段が軽い」と読んではならない。
   - **⚠ N2 は測る前に決めなければならない設計軸を 1 本抱えている** — 在庫 §2.5 (3 レートで
     受け皿を新設するか `R₀ = 0` スライスで既存 2 レート資産に載せるか) は**前 relay が意図的に
     未決のまま残した**もので、**(g)〜(j) の受け皿の型はこの分岐が決まるまで決まらない**。
     ⚠ **一次文献 diff (在庫 §2.5 が挙げる 3 本の grep) を取らずに選ぶのは事故形**である。
     ⚠ **`R₀ = 0` スライス側を採ると「スライスの effective compactness が 3 レート版から従うか」が
     別途債務になる — 「従う」と書いてはならない** (在庫 §2.5 末尾)。
   - **⚠ 可変枠は 1 leg も削っていない** — early gate は**形式化枠 3 の内数**であり、
     (C2) 本体の枠は前 relay と同じ 14 leg である (§5)。⚠ **この囲いを外したら §5.2 の事故形に
     戻る** ⟹ 外すなら `GOAL-CHANGE` ではないが**ユーザーに諮る**。
