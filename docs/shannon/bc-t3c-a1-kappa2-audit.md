# A1 (N4) 敵対的独立監査 — `κ(R*) = ∞` の判定

**判定 = `判定は生存`** (⚠ ただし **(K3) の後半節 1 本は覆った** — §1-b。(K1)/(K2) は無傷)

**対象** [`bc-t3c-a1-kappa2.md`](bc-t3c-a1-kappa2.md) / **親** [`bc-open-problem-t3c-plan.md`](bc-open-problem-t3c-plan.md) §4.6。既定の立場 =
「否定判定は偽 (`κ < ∞`)」/「(K1) は成り立たない」を**成立させにいって、成立させられなかった**。⚠ **独立性**: 被監査検証器
`kappa2_probc.py` は**読んだが 1 行も呼んでいない / import もコピーもしていない**。数値は自前実装
[`verifiers/kappa2_audit_probc.py`](verifiers/kappa2_audit_probc.py) (12 checks / 約 63 秒)。(18a)–(18i) / (19a)–(19c) / (20a)–(20c) は
`auxrec.txt:1037-1082` から、チャネルは `probc.txt:900-923` から**逐語で書き直した** (取得 = [`lit-fetch.sh`](lit-fetch.sh))。

## 1. ⭐ 攻撃 1 — 線分補題 (D2) / (K1)

**(1-a) 不成立 (= (K1) は生存)**。反例被覆の構成を 3 通り試みた。

- **2 本**: `μ_X` を覆う成員 `E₁` は (19) 非一様 ⟹ 真のアフィン切片。`E₂ ⊇ 𝒯 ∖ E₁` は非狭義アフィン制約で切られている ⟹ 線分 `[ν,ν']`
  (`ν ∈ E₁`, `ν' ∉ E₁`) との交わりが `(0,1]` を含む閉区間 ⟹ `ν` も含む ⟹ `E₂ = 𝒯` ⟹ `κ = 1` で `## M12 (T3b)` 行 5 に矛盾。
  ⚠ **2 本では最小性を使わずに死ぬ** — N4 より強い (`human-judgment`)。
- **3 本**: (D2) がそのまま効く。機械側でも単体を非狭義アフィン制約で切った 2–3 本の族 20 万回のうち被覆 3384 件を調べ、**真の切片を
  成員に持つ最小被覆 0 件** (A10、`machine`。⚠ **掃引なので plan §4.5 により証拠価値は低い** — 上の演繹が本体)。
- **可算本**: **(D2) は本当に落ちる**。有限台の読みでは `𝒯_fin = ⋃_n {μ : μ({q_n}) = 0}` (`{q_n}` は可算無限点列) が**1 本の等式で切られた
  真の切片による可算被覆**になる (有限台 `μ` は高々有限個の `q_n` しか担がない) ⟹ 成員は (19) 非一様でありうる ⟹ **`κ < ∞` は (K1) で
  真に効いており外せない** (`human-judgment`、手構成)。
- **端点・余次元**: (19)/(20)/(18) が**すべて非狭義**であることを逐語で確認 (`:1076-1078` は `=`、`:1080-1081` は `0 ≤ · ≤ ·`、(18) は
  `R ≤ ·`) ⟹ 線分との交わりは**閉**区間 ⟹ 下限が正。⚠ **1 本でも狭義が混ざれば `δ` が取れず (D2) は落ちる** (N4 が書いていない前提だが
  逐語上は満たされている)。(19) 残差は `μ` アフィン ⟹ 三分岐 (恒等 0 / 定数 ≠ 0 で `E = ∅` / 真の切片) で尽き、余次元の議論は不要。

**(1-b) ⭐⭐ ただし (K3) の後半節は覆った**。第 1 文 (「`μ_X` を覆う witness の `E_R(w)` はすべて真のアフィン切片」) は正しいが、
続く **「被覆族の全成員が薄い」は偽**。反例を**手構成**した (A11、`machine` で厳密確認):

> `p_X` 一様、`W = const`、`U = (X₁, X₂ ⊕ N₂)`、`V = (X₂, X₁ ⊕ N₁)` (`N₁,N₂ ~ Bern(α*)` 独立)、**3 系統とも同一** (対角)。

対角なので **(19) 残差は全 `J` で厳密に 0 (= (19)-uniform)**。`(18i) = 1.100780183291 = SR_C` (差 1.4e-12 = `α*` の桁落ち)、`J = const` の
(20) 余裕は `(0.20917, 0.68244, 0.20917, 0.68244)` と**すべて狭義正**、(20c) 残差 4.4e-16。`R* = (0, SR_C/2, SR_C/2)` を運び、
**`μ_const` の相対近傍 120/120 方向で適格かつ `R*` を運ぶ** ⟹ **`E_{R*}(w)` は内点を持つ**。⚠ **この `w` は `J = X` では非適格**
(`(20b)` 下側 = −0.1847 < 0) ⟹ **(D3)/(D4) とは矛盾しない**。⟹ 薄いのは **`μ_X` を覆う成員だけ**で、**厚い成員が実在する**。
**閉形**: `I(U;Y) − I(U;Z) = d*` は `f(s) := I(X;Y)_s − I(X;Z)_s` の一様点での凹性ギャップが `max_α D(α) = d*` (`D` = 1 ブロックの
more-capable 差) に一致することから出る ⟹ `(18i) = 2C + d*` は偶然でなく **`d*` の変分的特徴づけそのもの**である。
**(1-c) 副産物**: この `w` は **`SR_C` を運ぶ (19)-uniform witness の明示例**で、§5-d が未着手とした「(19)-uniform な 2 本の (20) 半空間系が
`𝒯` を覆うか」の**第 1 の駒**になる (鏡映 `σ` で第 2 の駒も出る)。

## 2. ⭐⭐ 攻撃 2 — `## M12 (T3b)` 行 3 の弱化

**不成立 (= 弱化は正しい)**。4 点を独立に潰した。

- **場合分けの除去**: `min{a,b} ≤ b` は無条件なので `(18i) ≤ I(W;Z)+I(V;Z|W)+I(X;Y|V,W)` に**場合分けは要らない**。機械: 乱択 60 本で
  超過 **0.00e+00**、うち **27/60 が行 3 が場合分けする「逆枝」** (A3、`machine`) ⟹ 取りこぼしは無い。恒等式
  `mid = I(X;Y) + I(Ṽ,W̃;Z) − I(V̂,Ŵ;Y) − [(19a)+(19c)]@const` 残差 **2.89e-15**。
- **蝶番**: `(20b)` 下側 @ `J = X` が恒等的に `−H(X|V̂,Ŵ,Y)` (残差 **1.33e-15**、A4) ⟹ 下側 ≥ 0 ⟺ `H(X|V̂,Ŵ,Y) = 0`。
- **非空虚性**: N4 の 95 本を**自前の族で数え直した**。3 系統を同一に取ると (19)@const が恒等成立するので族は豊富で、
  **375/2625 が弱化仮説を満たし、うち 88 本が `J = const` で非適格** (A9、`machine`) ⟹ 空虚でない。⚠ **本数は族の取り方で変わるので
  95 という数値自体は再現対象ではない**。⚠ **(D4) は背理法なので仮に空虚でも (K2) は壊れない** — 非空虚性は「(D3) が行 3 の真の強化」
  の根拠にすぎない。
- **弱化が load-bearing か**: (K1) が渡すのは (19)-uniform だけで `J = const` の `(20a)/(20b)` 上側は出ない (`(20a)` 上側@const =
  `H(X|Ũ,W̃) ≤ I(Ṽ;X|W̃)` は `J = X` の適格性から従わない) ⟹ **弱化なしに (K2) は動かない**。N4 の自認は正しい。

## 3. 攻撃 3 — (K2) は `## M12 (T3b)` の言い換えか

**不成立 (= 言い換えではない)**。**(K2) が M12 に足すものは 1 文で言える**:
> M12 は「単一 witness では届かない」(`κ ≥ 2`) を言う。**(K2) が足すのは「有限本にしても問題が単一 witness の問題へ潰れる」という還元** —
> (K1) が最小被覆の各成員を (19)-uniform へ落とし、そこで M12 の弱化版がもう一度効く。

⟹ `redundant` では死なない。⚠ ただし **(K1) は `κ = 2` 一般を潰さない** — (19)-uniform な 2 本が (20) で相補う配置は残る (§5-d)。
**§1-b の反例はまさにその配置の片割れ**なので、A1 の残り火はここにしかない (§5-a の `κ'` 路より具体的である)。

## 4. 攻撃 4 — 射程

plan §3.1 の禁止語彙 (`Thm7 ⊄ Thm8` /「方法の死」を「包含の否定」と書く / `R ∈ Thm7` /「あと少し」型) は**逐語で 0 ヒット**。`R*` は
`C ⊆ Thm7` から取るだけで `Thm7 ⊋ C` の材料にしていない (§1 末尾の量化子の註記も正しい)。⚠ ただし**射程を超えた言い回しが 3 件**
ある (§6 の訂正 1 / 2 / 3)。

## 5. 攻撃 5 — 再現 7 本の水増しチェック

**独立実装で 7 本すべて抜き取り検証した** (要求 3 本)。すべて値レベルで一致 (`machine`)。トートロジーかは**逐語から書き直した別実装が同じ値を出すか**で判定。

| # | facts の行 | 私の check | 判定 |
|---|---|---|---|
| 1 | `## M12` 行 9 | A1 `C=0.5310044 / 2C=1.0620088 / d*=0.03877137 / SR_C=1.1007802`、`min D₁ = 0.000e+00` | ⭕ (⚠ `α*` は訂正 5) |
| 2 | `## M12` 行 2 | A2 非混同対 0 組 + **決定的 15 本 + 確率的 4000 本**で反例 0/4015 | ⭕ (N4 は決定的のみ = §4.5 の「確率的を併せる」不履行。結論は不変) |
| 3 | `## M12` 行 3 | A3/A4 恒等式残差 2.89e-15 / 蝶番 1.33e-15 | ⭕ |
| 4 | `## M12` 行 4 | A7 `(18i) = 1.0620088 = 2C`、全 `J` 適格 違反 4.44e-16 | ⭕ |
| 5 | `## M12` 行 8 | A8 **不等式本体**を恒等式 + DPI で機械に載せた (残差 2.66e-15、`I(X;J₁)=0.020414/0.078072/0.227782`) | ⭕ (⚠ 訂正 4) |
| 6 | `## M11` 行 4 | A5 アフィン性残差 5.65e-15 | ⭕ |
| 7 | `## M11` 行 5 | A6 時分割アフィン性残差 5.64e-15 | ⭕ |

**私の数え直し = 7 本** (facts 各行の `machine` 欄が再現対象、という基準)。⚠ **文言基準なら 6 本** — #5 の本文は「上限を machine で再現」と
書くが、T7 が測るのは `I(V̂,Ŵ;J₁) = I(X;J₁)` と depolarizer 較正だけで**不等式本体は掛かっていない**。⚠ **#1 の「全単体でも `d*` は増え
ない」は 400 リスタート Nelder-Mead 由来** = §4.5 が「証拠価値ほぼ 0」と定めた最適化器 ⟹ 本数に数えてよいが `machine` でなく `optimizer`
と註記すべき (私も独立には確認していない)。⟹ **N5 の閾値表 (3 本以上 = 続行) は 6 本でも 7 本でも満たされ、水増しで判定は歪んでいない**。

## 6. ⭐ 訂正リスト (⚠ 本書は `bc-t3c-a1-kappa2.md` を編集していない)

1. **⭐⭐ §0 (K3) 27 行目「被覆族の全成員が薄い」は偽** (§1-b、`machine` A11)。第 1 文だけ残し「⟹ **薄いのは `μ_X` を覆う成員に限る**。
   `SR_C` を運び `μ_const` 近傍で適格な (19)-uniform witness が実在する (明示)」へ差し替える。
2. **§5-a「A1 を延命させるならここが唯一の入口である」は §5-b/c/d と矛盾** (それらも未決)。「`κ'` 読みは (D2) が効かない**別の**入口」へ弱める。§1-c より **§5-d の方が具体的な駒を持つ**。
3. **§5-e「`κ ∈ {1, ∞}` が確定した」は不正確** — このレート点で確定したのは **`κ = ∞`**。`{1,∞}` への二値化 (= gate §5 の `inert` 条件) は
   測ったレート点が 2 つだけで未確立。但し書き「このレート点でのみ支持する」は正しいので前半を直せば整合する。
4. **§2 (S2) と §4 表 row 5 の「頑健性 `(18i) ≤ 2C + I(X;J₁)` を machine で再現 (T7)」は上限を主張しすぎ** (§5)。「行 8 の `machine` 欄
   (`I(V̂,Ŵ;J₁) = I(X;J₁)` + 較正値) を再現」が正確。⭐ **不等式本体は本監査 A8 が機械に載せた**ので facts 行 8 は `machine` へ格上げ可能。
5. **`α*` の数値**: §4 表 row 1 の `α = 0.0776695` は誤り。**`α* = 0.07766967`** (`mpmath` 40 桁で停留点を求解)。`d* = 0.0387713704696` /
   `C = 0.531004406411` / `SR_C = 1.10078018329` は正しい。出所の `## M12 (T3b)` 行 9 も `0.077670` と `0.0776695` を併記して**元から
   揺れている** ⟹ facts 側も直すこと。
6. **§1 (D1) の制約列挙が (20c) を落としている** (`J` 非依存なので切り出しには効かず結論は不変だが「尽くした」と読める)。
7. **親子 drift**: [`bc-t3c-a1-novelty-gate.md`](bc-t3c-a1-novelty-gate.md) §4-b の **Baire 論証が無印のまま残っている**。N4 §2 (S1) が
   「`𝒯` は Baire でない」を認めて (D2) へ差し替えたのだから、gate 側にも印を入れないと cold な次セッションが §4-b を生きた論証として拾う。

## 7. 再検証

`python3 docs/shannon/verifiers/kappa2_audit_probc.py` (12 checks / 約 63 秒 / 終了コード 0 が PASS) / `docs/shannon/lit-fetch.sh <out>
auxrec probc` (一次文献。逐語の行番号は本文中)。`machine` = A0–A11。`human-judgment` = §1-a の 2 本 / 可算本の演繹、§2 末尾の
load-bearing 判定、§3 の 1 文、§4。
