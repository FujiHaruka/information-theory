# Parallel Gaussian: ② headline load-bearing-hyp → sorry+@residual honest restructure サブ計画

**Status**: CLOSED ✅ — headline (`parallel_gaussian_capacity_formula_minimal`) と regularity constructor から load-bearing hypothesis を除去し、genuine reduction chain 経由で正直化。converse 核は子 plan で genuine closure 済。

**SoT**: `docs/textbook-roadmap.md` Ch.9 (parallel-gaussian) + `docs/shannon/awgn-facts.md`。詳細履歴は git。

> **Parent**: [`parallel-gaussian-l-pg1-discharge-plan.md`](parallel-gaussian-l-pg1-discharge-plan.md)

## 要点 (再利用可能)
- 「genuine な所まで進めて、本当の壁でだけ sorry」が方針。load-bearing hyp を drop して全 body sorry にするのではなく、file 内既存の genuine reduction chain を本当に呼ぶ。
- achiever 側の MI 加法性は product (independent) 入力上で closed。converse は correlated 入力上の別不等式なので、同種 slug 下でも achiever closure では片付かない (子 plan で別途 closure)。
- per-coord AWGN closed form は single-channel の固定値特性 = regularity-shaped (load-bearing でない)。
