# Cramér family — settled-facts ledger

> 列 = claim / confidence / 再検証コマンド / last-verified (commit) / notes。
> confidence: `machine` (axiom/sorry 機械検証) / `loogle-neg` (Found 0) / `human-judgment` (低信頼、独立 pivot で再確認)。
> プラン散文に settled fact をキャッシュせず、ここにリンクする (re-derive > cache)。

| claim | confidence | 再検証コマンド | last-verified | notes |
|---|---|---|---|---|
| `CramerCltBoundaryClosure.lean` の headline 群 (`cramer_lower_boundary_unconditional`, `cramer_lower_boundary`, `boundary_liminf_lower_of_eps`, `tiltedWindow_eventually_large_of_boundary`, `tilted_halfline_tendsto_gaussian`, `tilted_halfline_tendsto_half`, `gaussianReal_Ici_eq_half` 他、計 10 decl) は sorryAx-free | machine | `lake env lean InformationTheory/Shannon/CramerCltBoundaryClosure.lean` (silent) + `#print axioms cramer_lower_boundary_unconditional` (= `[propext, Classical.choice, Quot.sound]`) | `9cdc485` | CLT-boundary closure moonshot 達成 (Phase 1-6, 独立監査 `@audit:ok` 全 10)。roadmap 判断ログ #25 / 子 plan `cramer-chernoff-clt-closure-moonshot-plan.md` 判断ログ #4 |
| `gaussianReal_Ici_eq_half` (Gaussian median 1/2) は Mathlib 不在 | loogle-neg | `loogle "ProbabilityTheory.gaussianReal, Set.Ici"` (Found 0)、median パターンも 0 | `1a19915` | symmetry-by-map (`gaussianReal_map_neg` + `noAtoms_gaussianReal`) で自作、~62 行。Mathlib PR 候補 |
| 両 root (`cramer_lower_phaseC_partial_discharge` @ infinitePi 版 / `cramer_lower` @ 一般 iid) は **配線完了で sorryAx-free** (closed-but-unwired 解消) | machine | `#print axioms InformationTheory.Shannon.Cramer.Discharge.cramer_lower_phaseC_partial_discharge` + `#print axioms InformationTheory.Shannon.Cramer.cramer_lower` (両 `[propext, Classical.choice, Quot.sound]`) | `8a8a001` | **配線方式** (子 plan `cramer-root-wiring-plan.md`、独立監査 `@audit:ok` 12 件): (A) `IsMeasureInfinitePiTiltedEq` + tilted 機構 8 decl を新上流 `CramerBoundaryUpstream.lean` へ hoist し一直線 DAG 化で import cycle 解消 → root A を `exact cramer_lower_boundary_unconditional` で discharge。(B) root B chain 3 decl (`cramer_lower`/`_legendre`/`cramer_tendsto`) を新下流 `CramerGeneralLower.lean` へ移設 (headline は Cramer.lean を transitive import するので最上流では discharge 不可) → iid joint law = `infinitePi (μ.map (X 0))` の double-pullback transport (`iIndepFun_iff_map_fun_eq_infinitePi_map`) + `Y:=X 0` で `h_bdd` 供給 → headline へ落とす。両 root に `hVar` (非退化分散) を regularity precondition 追加 (load-bearing でない、監査確認)。project 実 sorry 17→15 |
| iid 任意族 → canonical `infinitePi` copy への law 移送 transport (root B 手法) は `Y:=id` が ℝ 上 unbounded で `h_bdd` 不成立、`Y:=X 0, μ₀:=μ` への pivot が必須 | machine | `InformationTheory/Shannon/CramerGeneralLower.lean` body (`cramer_lower`) + `#print axioms` | `184affa` | 設計教訓: textbook 的 `Y:=id` は headline の `h_bdd` 前提で死ぬ。`X 0` を Y に取ると i=0 instance が有界性を供給し `cgf_eval_eq_cgf_base` 直接適用 (cgf 橋 self-build 0 行)。他 iid 命題を headline に落とす際の汎用パターン (shared helper 化候補) |
