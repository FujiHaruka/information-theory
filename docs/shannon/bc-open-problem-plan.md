# 一般 BC の計算可能な容量領域 — 攻略の進め方 (メタプラン)

> **Parent**: [`broadcast-channel-moonshot-plan.md`](broadcast-channel-moonshot-plan.md)

**Status**: L9 (棚卸し #2) 完了 = leg 10 / 20。**§3.1 の 5 条件を満たす経路は依然 0 本** (R1 は (4) 通過 /
(5) 不合格で、経路ではなく**軸 E の地図**として [`bc-open-problem-routes.md`](bc-open-problem-routes.md) に
存続)。**L9 は残り枠を gate-first へ組み替えた** — 実測「経路 1 本 = 2 leg」に L8 の実測「novelty gate は
一次文献の取得だけで経路を丸ごと殺す」を織り込み、**gate を経路構築の前に置く** (§5-11 / §6.2)。副作用と
して**生き残った経路は構築 leg の中で probe まで済む** ⟹「うち 1 本が `probed`」が別コストでなくなる。
最低ラインの到達可能性は **到達可能だが条件つき** (判定・条件・代替収穫物 → §7)。⟹ **次は L10 = 軸 C**。
**Branch**: `bc-computable-region`
**ゴール**: **散文レベルの候補経路** (§3.1 の 5 条件を満たすもの) を数本。**形式化はスコープ外**で次 relay
に回す — 散文の証明がまだ存在しない以上、形式化は前提を欠く (§2.1)。経路が誤っている可能性は織り込むが、
誤りを誤りと表示しないことは許さない (§3.2)。
**この文書の射程**: 「未解決問題に長期 relay で挑むとき、途中で諦めず、かつ嘘をつかずに、何をどの順で
回すか」という**運用の設計**。個々の attack は [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md)
が、確定事実は [`bc-facts.md`](bc-facts.md) が SoT。本文書はこの 2 つに何をどう積むかを決める。

---

## 1. 現在地

### 1.1 文献側のランドスケープ (L0 で verbatim 確定済、2026-08-01)

**逐語引用・出典 URL・再検証手順は [`bc-facts.md`](bc-facts.md) が SoT**。ここには attack を選ぶのに要る
一行だけを置く。

| # | L0 の判定 | 我々への含意 |
|---|---|---|
| F1 | CONFIRMED — 一般 2 受信者 DM-BC の容量領域は未解決 | 本丸。ただし「単一文字特徴付けが存在しない」という意味ではない (→ F7) |
| F2 | PARTIAL — 最良既知 inner = 3 補助変数 Marton (Marton 1979 単独帰属は誤り。Gel'fand–Pinsker 1980 / Liang–Kramer 2007 を含む) | **「inner = capacity」路線は死んでいない** — 未解決の予想。正しければ最低レベルは `Σ₁ᴴ` (F7 と直結) |
| F3 | **REFUTED — UV は最良既知 outer ではない**。Gohari–Nair の auxiliary-receiver 外界 (Theorem 7 = "the J version of the UV outer bound"、IEEE TIT 68(2):701–736, 2022) が真に改善し、その導出法は **UV 導出で捨てられる項を拾い直す**もの | **軸 B の第一手が文献に先取りされている**。生き残る標的は「J-bound は最適か」(Li 曰く optimality unknown) と「我々の Lean 証明のどのステップが Theorem 7 の改善項に対応するか」 |
| F4 | PARTIAL — BSSC で inner (Marton, `0.36164288`) と outer (UV, `0.3725562`) の sum rate にギャップ ≈ `0.0109`。⚠ この情報不等式は inner の明示評価を**可能にする**道具であって、明示評価を回避する道具ではない。`0.37111` は原論文が訂正した誤値 | 「inner = outer」路線は死んでいる |
| F5 | CONFIRMED — 逆向き semi-deterministic の積 BC で UV は strictly suboptimal (ギャップ ≥ `4/15`)、共通メッセージ版 UVW も同時に死ぬ。**そのチャネルでは Marton = 容量**で、緩いのは外界の側 | 「outer = capacity」路線も死んでいる。数値 probe には **arXiv:1105.5438 v2 の Fig.2** を使う (IEEE 掲載版の図は誤り) |
| F6 | PARTIAL — binary input BC で randomized time-division が Marton の sum rate を達成。出典は **Nair–Wang–Geng 2010** (arXiv:1001.1468。旧記載の著者・年はいずれも誤りで、F5 の論文との取り違え) | inner の**限界**を測る道具 |
| F7 | CONFIRMED (本 plan の想定より強い) — Li の Theorem 10 + Proposition 15 により **BC 容量領域は `Δ₁₈ᴴ` に入る**。未解決なのは「linear entropy hierarchy の**最低レベルはどこか**」(Li 自身が p.20 に open problem として明記)。脚注 3 より**単一文字性と計算容易性は別軸** | **軸 D の主語をこの形に差し替える**。「単一文字特徴付けを持つか」は肯定的に決着済で、それを標的にすると空振りする |
| F8 | PARTIAL — FSC 容量 / DMC の最適入力分布の非計算可能性は CONFIRMED。だが **「BC 容量領域の計算可能性は未決」と明示する一次文献は NOT-FOUND** (決着させた論文も見つからないので「未決」と両立はする) | 否定的解決を標的にするなら、**まず未決性の出典を立てる**のが最初の一手になる。無いなら「無い」を確定事実にする |

### 1.3 外部ノート — 自己テンソル化への縮約 (L0–L2 で決着)

[`bc-external-note-tensorization.md`](bc-external-note-tensorization.md) の骨子は**「未解決問題は Marton
領域の support function が同一チャネルのテンソル冪に対して自己加法的か、に等価変形される」**。
**逐語・判定・再検証手順の本体は [`bc-facts.md`](bc-facts.md)** (§外部ノートの文献主張 / §数値 probe の
判定 (L1) / §定義照合と数値 probe の判定 (L2))。ここには攻撃軸の選択に効く 3 点だけを置く。

1. **文献層 (V6–V10) は生存、数学層は恒等式層 (V1–V3) だけ通過**。ノートは一次文献を正確に引いた上で
   その先へ**出典なく外挿する**書き方なので、外挿部分の判定は**数値 probe と我々の `def` との照合だけ**で
   下す (L1 / L2 が実施)。
2. **還元そのものも、還元の全段も既知** — L2 が「どちらの文献も供給していない」とした 2 段 (閉凸性 /
   双対) は**両方とも原典 [AGN19] にあった** (L8)。`h_n^T(θ)` 加法性と `F(T,{a_x})` 加法性が別命題で
   あることは変わらないが、**両者を繋ぐ段も含め 12 step すべてに逐語出典が付く** ⟹ **軸 E の新規性は
   還元の先にしか置けず、その先も尽きた** (§4 軸 E、attack `support-vs-dual-additivity`)。
3. **還元層は我々の 2 補助変数 `def` の上では死ぬ。死因は非加法性ではなく共通補助 `U₀` の欠落**
   (V5-b: 反例条件 (11) が自明に充足 ⟹ 数値探索は**保証された偽陽性**) ⟹ 前提工事 `marton-3aux-probe`
   が挟まる。関連: 還元 (3) は*閉凸包*の命題 (V5)、定理1 は逆側だけ生きる (V4)。**⚠ 潰れは `U₀` の
   欠落から一般に従うのではなくチャネル依存** (L7、§4 軸 E の収穫ライン)。

### 1.2 在庫側 — 手元にあるもの

未解決問題の**両端が機械検証された形で 1 つのライブラリに並んでいる**のは、おそらく世界的にも他に例が
ない。ここが我々の唯一の構造的優位で、攻め口の設計はすべてこれを軸にする。

- 主語: 操作的容量領域 `bcCapacityRegion` (`BroadcastChannel/Operational.lean`) / 橋:
  `marton_region_subset_uv` / 外界: `bc_capacity_subset_uv` (全平面版) / 内界: `marton_achievability`
  (無条件、private only) と `bc_achievability_of_rate_lt` (superposition)
- 等号クラス: degraded / less noisy / more capable の 3 本 (`Superposition/MoreCapable.lean` ほか)
- 反証基盤: `bc-marton-union-gap-check.py` — **Lean の `def` と逐語照合した数値 sim で「逆包含は偽」を
  実際に判定した実績がある**

---

## 2. Approach

**三層 × 階段 × 台帳**。

### 2.1 三層 — ただし**この relay のゴールは層 1〜2 まで**

```
層 1  散文        アイデア + 証明スケッチ (自然言語 + 数式)        ← 本 relay のゴール
層 2  proof-probe 核 1〜2 ステップだけを機械検証 (数値 sim or Lean)  ← 本 relay の検証手段
層 3  形式化      散文全体を Lean へ                              ← ⚠ 本 relay のスコープ外
```

**層 3 は次の relay に回す**。**散文レベルの証明がまだ存在しない**以上、形式化は前提を欠く。本 relay の
成果は「**probe を通過した候補経路が数本**」で、それらが**誤っている可能性は織り込む** (誤りの排除は
層 3 の仕事)。**層 2 の役割もそれに合わせて変わる** — 形式化への入口ではなく、**散文の自己欺瞞を最短で
殺す装置**。「散文で証明できた気がする」を 1 leg 以内に潰せるかが探索速度をそのまま決める。

層 2 の 2 形態と使い分け:

- **数値 probe (主力)** — 命題を有限アルファベットの数値最適化に落として反例を探す。**基盤は L1 で
  完成した [`bc_probe.py`](bc_probe.py)** (自己検証 = `python3 docs/shannon/bc_probe.py`、API と使用例は
  同ファイルの docstring が SoT)。載せ方の要点 3 つだけ置く:
  - **線形情報量式** → まず `prove_identity` で**係数相殺 = 証明**を試み、落ちたら `test_relation` で掃引。
    **掃引の成立は証拠、係数相殺は証明** — 台帳にはこの区別を書き分ける。
  - **構造制約** → `BayesNet` に DAG として置く。⚠ 一般 BC は `Y ⊥ Z ∣ X` を仮定できないので `(Y,Z)` は
    **結合 1 ノード**にする (積 BC で置くと射程が狭まる)。
  - **補助変数上の最大化** → `maximize` が返すのは最良値であって最大値の証明ではない。⚠ **ランダム方向の
    摂動だけで「局所最大だ」と結論しない** — L1 で改善方向が単体の**境界**にありランダム 20,000 方向が
    全滅した実例が出た。`directions=` に境界点を明示的に渡す。

  実行可能な probe は `bc-*-check.py` に置き本 module を import する。**sim は必ず Lean の `def` と
  逐語照合してから信用する** (CLAUDE.md 検証の誠実性) — 照合義務は**その `def` を扱う consumer 側の
  probe が負う** (基盤側は entropy の規約 1 点のみで、これは済)。
- **Lean probe (安いときだけ)** — **既存資産への問い合わせで済む場合に限る** (典型は軸 A の仮説差し替え
  再コンパイル)。新しい補題を積み始めたらそれは層 3 なので**打ち切って散文に戻る**。

### 2.2 階段 / 2.3 台帳

- **階段** — 未解決本体だけを目標にすると「解けない = 成果ゼロ」になり、それが諦めの最大の原因になる。
  **どこで止まっても成果が残る**よう独立に価値を持つ 4 段に割る (§3)。
- **台帳** — 試した attack は死んだものも全部残す。**死因の分類**まで書くのが要点で、「数値反例で死んだ」
  「既知だった」「単に難しい」は次の一手が全く違う (§5-4)。

---

## 3. ターゲットの階段

**完了条件はすべて散文ベース** (層 3 はスコープ外、§2.1)。

| 段 | 内容 | 完了条件 | 価値 |
|---|---|---|---|
| **T0** | ギャップの正確な固定 | 内界・外界・容量領域が同じ言語で並び、「何が未解決か」が曖昧語なしで書ける | 在庫でほぼ達成済 |
| **T1** | **既知の負の結果**の再現 | F4 (BSSC のギャップ) / F5 (NEG の suboptimality) の証明の**筋が追えていて、どの道具が効いたかが言える** | 道具立てがここで揃う。新規性は無いが、以後の全 attack の入力 |
| **T2** | **人類未知の中間結果の候補経路** ← 本命 | §4 の軸のいずれかで、§3.1 の 5 条件を満たす経路が立つ | 次 relay の形式化対象。ここが現実的な到達目標 |
| **T3** | 未解決本体の候補経路 | 一般 BC の計算可能な特徴付け (肯定 or 否定) について同じく経路が立つ | 30 年級。狙うが期待値は置かない |

### 3.1 「候補経路」と呼べるための 5 条件

**これを満たすものだけを経路 (route) として台帳に載せる**。満たさないものは `idea` 枠に置く (量産して
よい — 制約は昇格時だけ)。基準が無いと「経路が 5 本出た」が空虚な数字になる。

1. **主張が確定している** — 何を証明する経路なのかが 1 文で書け、その命題に曖昧語 (「適切な」
   「よい性質の」) が無い
2. **ステップが列挙されている** — 中間命題の列があり、各ステップの入力と出力が書かれている
3. **各ステップにラベルが付いている** — `known` (既知の道具で行ける、出典つき) / `new` (新規に証明が
   要る) / `gap` (未検討)。**`gap` が残っていてもよい** — 隠さずに書いてあることが条件
4. **殺されていない** — 主張自体の小ケースを数値 probe に 1 回は通した
5. **既知でない** — 文献確認を 1 回通した (§5-7)

### 3.2 経路の確信度ラベル

| ラベル | 意味 |
|---|---|
| `probed` | 核ステップが数値 or Lean で検証を通過した |
| `unprobed` | 散文のみ。probe 未実施 |
| `refuted` | 殺された (死因を台帳へ) |

**`unprobed` のまま「証明できた」と書かない**。誤りを織り込むこと (§2.1) と、誤りを誤りと表示しない
ことは別。層 3 を捨てた代償はこのラベルで払う。

### 3.3 本 relay の到達目標 (L14 の収穫判定で使う)

- **最低ライン**: 5 条件を満たす経路 ≥ **2 本**、うち ≥ **1 本**が `probed`
- **目標**: 経路 ≥ **3 本**、うち ≥ **2 本**が `probed`
- 経路の本数は多いほど良いわけではない。**次 relay で形式化に着手できる粒度**まで書けているかが基準。
- **現在地 (L9 完了時点) = 有効な経路 0 本**。R1 は (4) を通ったが **(5) で不合格**。⚠ **本数の現物は
  台帳から再導出する** (5 条件を満たすまで経路表に載らない。plan にキャッシュしない)。
- **⚠ 最低ラインの数値は L9 でも動かしていない**。L9 が出したのは**到達可能性の判定**であって基準の
  改定ではない — 判定は「**到達可能だが条件つき**」で、条件と代替収穫物は §7 (ユーザー判断の論点)。

---

## 4. 攻撃軸 — 未知の中間結果の候補

各軸に (a) 狙い / (b) 形式化プロジェクトゆえの優位 / (c) 最初の一手 / (d) 収穫ライン (死んだときに残るもの)
を書く。**単一の軸に賭けない** — §5-5 のラウンドロビン規約で強制的に回す。

### 軸 A — 在庫駆動の最弱仮説抽出 (優先度 1)

- **狙い**: 既に証明済の等号定理が**実際に使っている仮説**を最小化し、既知クラスより真に広いクラスを
  切り出す。
- **優位**: 「証明が本当は何を使ったか」は散文の論文からは読めない。**Lean は仮説を差し替えて
  `lake env lean` に問える** — 形式化プロジェクトだけが持つ発見手段。⚠ **手順 (L3 の訂正)**: 実使用の
  棚卸しは `rg` ではなく **`dep_graph.sh` の前方閉包**で行い、**辺の向き先が `def` 自身か名前つき補題か**を
  区別する (`rg` では `exact hmc _` 形の `def` 直接展開が構造的に見えない)。
- **最初の一手 — L3 で実行、判定 FAIL**: `IsBCMoreCapable` を `condMutualInfo_le` の結論に差し替えても
  **通らず**、しかも**両方向を機械検証すると差し替え先は元クラスと同値** ⟹ **新クラス候補ゼロ**
  (逐語 → [`bc-facts.md`](bc-facts.md) §機械検証 probe の判定 (L3))。**収穫ラインはこの同値そのものとして
  達成済**。⚠ 平均化による古典的同値で**新規性はない** (新しいのは我々の `def` の上で機械検証されたこと)。
- **⚠ 軸自体は生存 (優先度 1 据え置き)**: 死因から子 attack 2 本 — `weakest-hyp-degraded-lessnoisy`
  (⚠ 同じ同値で潰れる公算も高い) と **`class-def-vs-consequence-gap`** (**条件つき版と非条件版が同値に
  ならないクラスはあるか** = 軸 A の第一手が生きる場所の探索。**L12 の主語**)。近距離の開き口として
  `sum-bc-classes` (成分の和 BC。⚠ **積ではなく和**で、軸 E の合成演算とは別物) も残る。

### 軸 B — 外界の緩みの局所化 (優先度 2、主語を差し替え。**本 relay での実働は終了**)

- **狙い**: 旧狙い「UV の証明で捨てた項を拾う」は **Gohari–Nair Theorem 7 が既に実行済** (F3) ⟹ 標的を
  (i) **我々の Lean 証明のどのステップが Theorem 7 の改善項に対応するか**の同定、(ii) **J-bound 自体が
  最適か** (Li が逐語で "optimality unknown") に差し替えた。
- **優位**: 証明が Lean にあるので「不等式を緩めた箇所」が構文的に列挙できる。**論文側は Lean 証明を
  持たないので、改善項とステップの対応付けは我々にしか作れない**。
- **最初の一手 — L6 で実行、判定は「同定完了・帰結は否定的」**: ステップ数は 21 ではなく **39 本**、
  対応付けは matched 22 / no-slack 12 / unmatched 5 で `unmatched` 5 本は**いずれも最終領域を動かさない**
  ⟹ **我々固有の exploitable な緩みは 0 本** (全文 → [`bc-outer-slack-inventory.md`](bc-outer-slack-inventory.md))。
- **収穫ライン達成済** = 対応表 + 捨て項の 4 種分類。**(ii) は開いたままだが `bcOuterRegionJ` の新設が要り
  層 3 = 本 relay スコープ外** ⟹ **次 relay の最有力候補** (`outer-region-j-def` / `jbound-optimality`)。

### 軸 C — 情報不等式 (優先度 3 → **1**、L9 で昇格)

- **狙い**: F4 の Jog–Nair 不等式の形式化 (T1) を踏み台に、新しい情報不等式を探す。
- **優位**: 情報不等式は有限の変数と有限の項からなるので、**Lean での検証と数値での反証の両方が効く**
  数少ない領域。当てずっぽうの不等式を大量に数値で殺してから、生き残ったものだけ証明に投資できる。
- **⚠ 昇格の根拠は 2 つで、どちらも L9 の再配分の中心**: (a) **9 leg 回って一度も leg の主語に
  なっていない唯一の軸** (台帳のカウント表。「進まなかった」ではなく「回ってきていない」)、
  (b) idea `shannon-cone-lp` が **novelty gate を機械化する** — elemental Shannon 不等式を制約とした
  LP (ITIP 相当) は「その不等式は Shannon 型か = 既知か」をその場で返す。L8 の実測 (gate は経路を
  丸ごと殺すが、コストは gate 側にしかない) と噛み合い、**候補を 1 leg で複数殺せる唯一の軸**になる。
- **最初の一手 (L10)**: LP gate を先に建て、その上で Jog–Nair 不等式を述べる (§6.2)。
- **収穫ライン**: 不等式ライブラリ + **機械化された novelty gate 自体** (BC 以外の多端子問題 MAC /
  Han にも効く再利用資産。§7 の代替収穫物 2)。

### 軸 D — 特徴付けの複雑さ階層 (優先度 2、主語を差し替え)

- **狙い**: 旧狙い「単一文字特徴付けを持つ、を形式的に述べる」は **F7 で肯定的に決着済** ⟹ 主語を Li
  自身の open problem に合わせる: **linear entropy hierarchy `Δᵢᴴ / Σᵢᴴ / Πᵢᴴ` の最低レベルはどこか**。
  加えて F7 脚注 3 が**単一文字性と計算容易性を分離**する ⟹ 2 つの問いとして分けて持つ。
- **優位**: 我々は**操作的定義から出発している** — `bcCapacityRegion` は符号と誤り確率で定義され bound で
  定義されていない ⟹ 「どのレベルの論理式で書けるか」を問う主語として正しい形を既に持つ (文献側は多くが
  bound を主語にするため、この問いを formal に書けない)。
- **最初の一手 (L15)**: (1) multi-letter 表現の定義を確定させ `bcCapacityRegion` と一致する筋を散文で書く
  (既知。**証明は書かない**)。(2) Li の Table I の実例に**我々の在庫のどの定理が対応するか**を対応付ける
  (degraded は Table I にあるが、我々が持つ less noisy / more capable の等号は無い)。
- **収穫ライン**: 対応表そのもの + **F2 + F7 の連結** (Marton 内界が最適なら最低レベルは `Σ₁ᴴ`)。⚠ 旧収穫
  ライン「否定的解決が T3 への橋」は **F8 の NOT-FOUND で根拠が弱まった** — 使う前に出典を立てる
  (`bc-computability-openness`)。

### 軸 E — 自己テンソル化 (優先度 1 → 3 (L2) → **経路の供給源としては閉じた** (L9))

- **狙い**: 未解決問題を「**Marton 領域の support function が同一チャネルのテンソル冪に対して
  自己加法的か**」という単一の命題に等価変形する (外部ノート
  [`bc-external-note-tensorization.md`](bc-external-note-tensorization.md) の同値条件 (3))。
- **⚠ 還元も、還元の全段も我々の新規性ではない**。Additivity Conjecture (Anantharam–Gohari–Nair 2019
  = [AGN19]、Gohari–Liu–Nair ISIT 2025 = [GLN25] の Conjecture 1 として再掲) が同じ還元を持つ。
  新規性を置ける場所は 3 つだった: (i) 否定側インスタンス (11) → **L2 で失効** (保証された偽陽性、
  前提工事 `marton-3aux-probe` が挟まる = 優先度 1 → 3 の根拠)、(ii) support function 版と双対汎関数版の
  同値性 → **L8 で消えた** ([GLN25] 式 (1) は `≤` ではなく **`=`**、出所は [AGN19] の convex duality
  ⟹ L7 が R1 に書き下した 3 段入れ子そのものが文献の式 (2) + (9)(10)(11))、(iii) → 独立軸の軸 G。
- **⟹ 残る口は 1 つだけで、それは経路にならない (L9 の判定)**: [AGN19] が Lemma 2 のインスタンス探索に
  ついて**逐語で独立再現を公開要請**しており探索空間 (`|U|=2,|V|=3` / `|U|=3,|V|=2` = "the main
  nontrivial cases") も指定済 (routes.md P3)。⚠ **これは新しい問いではない** ⟹ §3.1 の条件 (5) を
  **構造的に満たしえない** ⟹ **軸 E からは経路が出ない**。着手する価値は残るが、それは経路ではなく
  **代替収穫物** (§7 の 1) としてである。
- **前提工事 `marton-3aux-probe` は消えないが見積りが下がった** (`Δ(X)` 上の `G_{α,μ}` と上凹包 1 本、
  `F` ↔ 領域の繋ぎは文献にあるので自作不要)。**枠の割り当ては L9 で「条件つき」に決した** (判断ログ 16)
  — 工事の価値は**軸 G に全額移った** (軸 E 側の消費先はどちらも経路を生まないため)。
- **収穫ラインは達成済**: multi-letter 表現の正確な形 (逆側だけが生きる、共通メッセージ層は不要) +
  負の判定行の射程が閉凸包まで拡大 + **`U₀` 無しでの時分割への潰れは `|X| = 2` に固有** (`|X| = 3` の
  Blackwell では閉凸包が時分割領域より真に強い。⚠ **`U₀` が不要だとは言っていない**し、比較対象は素の
  TDMA 三角形で、Blackwell は教科書的対象ゆえ**新規性は主張しない**)。probe 2 本
  (`bc-marton-convexhull-check.py` / `bc-u0-necessity-check.py`) が残る。

### 軸 F — 補助変数の濃度境界 (優先度 4 → **3**、L1 で主語を差し替え)

- **狙い (L1 で差し替え)**: 旧狙い「cardinality bound の改善」は**層 3 のコスト論拠**で本 relay には効か
  ない ⟹ **濃度限界がどの主張の生死を分けるか**の仕分けへ。L1 (b) で `|U|+|V| ≤ |X|+1` を課すか否かが
  **同一の点について「局所最大である / でない」を反転させる**ことが分かった (改善摂動はちょうど限界の外へ
  出る)。`F(T,{a_x})` の定義自体には濃度制約が無いので、**制約つきと制約なしは同じ最適化問題ではない** —
  文献の主張を引くたびにどちらの空間の話かを確認する。
- **最初の一手**: `cardinality-localmax-boundary` — Marton 領域まわりの主張を「濃度制約の内側でしか
  成り立たないもの / 外でも成り立つもの」に仕分ける。
- **⚠ 昇格の根拠は観測 1 件**なので優先度 1 相当の歩留まりを主張する材料ではない。L1 の進捗は**軸 G に
  計上し本軸には計上しない** (台帳の二重計上の注意)。

### 軸 G — Marton 領域の最適化子の構造 = Markovity 予想 (優先度 1、`parked`)

**独立軸にする理由**: 標的が単一文字・単一チャネル上の**最適化子の構造**で、軸 E (テンソル冪の加法性) とも
軸 A (等号定理の最弱仮説) とも主語が違う。**残る軸の中で §3.1 の条件 (5) を満たしうる標的を持つのはここ
だけ** — 本丸より遥かに小さく、かつ真に未解決だから。

- **狙い**: Gohari–Liu–Nair (ISIT 2025) の **Conjecture 2 (Markovity Conjecture)** — 「Marton の達成領域の
  極点を評価するときは Markov 連鎖 `U → (W,X) → V` を持つ組だけを考えれば十分」。**未解決**で、しかも
  提案の根拠は**数値証拠のみ** (`|X| = 3,4,5` で各 10,000 チャネル超)。
- **優位**: (a) Marton 領域が機械検証された `def` で在庫にある、(b) 数値 probe 基盤が L1 で建った、
  (c) 同論文 §III-B に**完全に明示された数値インスタンス**があり **既知の答えがある再現課題**として
  harness の検証に使える (L1 で実施済)。
- **⚠ 「潰れている路線」は L1 で弱まった**。局所最適性条件だけで予想を証明する路線を殺していた
  §III-B のインスタンスは、**論文が §III で自ら定義する意味では局所最大ではない** (条件 (7)/(8) を違反)。
  ただし**「路線が開いている」は確立事実ではなく推論**で、濃度限界の内側では報告点は真の局所最大 ⟹
  **開きうるのは濃度非制約の空間に限る** (軸 F と連動)。攻めるなら attack `markovity-localopt-route` の
  順序 (診断が先、証明は後) を守る。
- **これまでの一手**: (1) ✅ L1 — §III-B のインスタンスを数値再現 (8 桁全桁一致)。(2) ✅ **L5 = 「述べ
  られるが偽」** — Conjecture 2 の「Equivalently」側を我々の support function へ移した命題 **M2 は数値
  反例で FALSE**。⚠ **Conjecture 2 の反例ではない** (`F` に残る `W` 由来の項は線形項で相殺できない)。
- **⚠ 続行には前提工事が要る (L5)**: 2 補助変数の在庫の上では行き止まりで `marton-3aux-probe` が挟まる。
  ただし L1 の harness が既に `F` を逐語実装しており、L8 が繋ぎの段を文献の定理と確定させたので、残るのは
  **文献の段を我々の `def` の上で使える形に写すこと**だけ (`markovity-via-dual-F`)。
- **⚠ L9 の判定 — 本軸は「経路を生みうる唯一の park 対象」**: 工事 1 + 構築/probe 2 = **3 leg** なので
  発動は**条件つき** (L14 で生存経路が 1 本以下のとき、判断ログ 16 / §6.2)。**発動しなければ park のまま
  relay を終える**。
- **⚠ 知見の横流しはできない (L7 の走査)**: L5 の反例チャネル `CEX-1` は L7 のギャップも出すが、
  **§III-B のチャネルは L7 では潰れる** ⟹ 「非矩形最適点が立つ構造」と「時分割を破る構造」は**一致
  しない** (候補チャネル選定に `markovity-localopt-route` の統計を使えない)。
- **収穫ラインは達成済**: L1 の数値再現 / **M2 の反例インスタンス**と「最適点が非 Markov ⟺ 非
  rectangular」の対応 / 大域最適化 harness 2 本。

---

## 5. 運用規約 — 「諦めない」を仕組みで担保する

諦めが起きるのは (i) 進捗の実感が無い (ii) 何を試したか忘れる (iii) 同じ失敗を繰り返す、の 3 つ。以下は
それぞれへの構造的対処で、**すべての leg のブリーフに本節への参照を入れる**。

1. **no empty leg — 進捗の単位は「証明の完成」ではなく「確定事実 1 行」**
   各 leg は attack ledger または `bc-facts.md` に**最低 1 行の新しい確定事実を追加して終わる**。
   反証も、死因の特定も、「この route は既知だった」も、すべて確定事実。証明の完成だけを進捗と
   定義すると、未解決問題では 10 leg 連続でゼロ進捗になり、そこで必ず諦めが起きる。

2. **kill-first — 反証ファースト**
   候補命題を立てたら、**まず数値 probe で殺しにいく**。証明に投資するのは殺せなかった命題だけ。
   `bc-marton-union-gap-check.py` が先例で、実際に 1 本の逆包含を殺して路線変更を導いた。**道具は
   L1 で `bc_probe.py` として汎用化済** (載せ方は §2.1) なので、本規約はもう空文ではない。
   これは時間の最大の節約であると同時に、自己欺瞞に対する最大の防波堤。

3. **proof-probe — 核だけ先に機械検証** (§2.1 層 2): 散文が完成する前に核 1〜2 ステップを検証する。

4. **attack ledger — 探索木の永続化**
   1 attack = 1 行。列 = slug / 軸 / 状態 (`live` / `killed` / `parked` / `harvested`) / **死因**
   (`numeric-counterexample` / `known-result` / `probe-failed` / `too-hard` / `mathlib-wall`) /
   残った副産物 / 最終更新 leg。`killed` の行は**削除しない** (plan hygiene の「決着済は削除」の例外 —
   ここでは死因そのものが資産)。

5. **round-robin — 軸の強制切替**
   1 つの軸で **3 leg 連続で確定事実がゼロ**なら、その軸を `parked` にして別の軸へ移る。同じ思考
   パターンの周回を構造的に止める。park した軸は 3 leg 後に再検討可 (新しい確定事実が入っていれば)。

6. **harvest line — 撤退ラインではなく収穫ライン**
   通常の撤退ラインは「詰んだら諦める」だが、未解決問題では**詰んだときに何を収穫して撤退するか**を
   事前に書く (§4 の各軸 (d))。これがあると「失敗」が「別の成果」に変わり、諦めではなく移動になる。

7. **novelty gate — 新規性主張の前に文献確認**
   「未知の結果を得た」と書く前に、必ず文献確認を 1 手挟む (WebSearch + 該当論文の verbatim 確認)。
   既知結果の再発見を新規と誤認するのは、長期セッションで最も起きやすく最も高くつく事故。
   ⚠ 逆向きの事故もある — **「既知だろう」で切り捨てる**。in-repo 資産の見落としと同じ構造 (CLAUDE.md
   「Search in-project before concluding 絶対」)。既知判定にも出典を要求する。

8. **anti-self-deception — 散文にも honesty doctrine**
   CLAUDE.md の Lean 側 doctrine を散文へ移植する:
   - 散文の各補題に「その仮説は precondition か load-bearing か」のラベルを付ける。
     **核を仮説に押し込んだ散文証明は、Lean の hypothesis bundling と同じ defect**。
   - 「〜は明らかに成り立つ」「同様にして」は**その場で層 2 の probe 対象**として台帳に起票する。
     散文における `sorry` はこの 2 つの言い回しの形で現れる。
   - 数値実験で FALSE を判定するときは、sim を実 `def` と逐語照合してから。

9. **死因は必ず子 attack を 1 本生む — ネタ切れ防止の主機構**
   attack が `killed` になったら、**その死因から新しい attack を 1 本起票してから** leg を閉じる
   (「この不等式が緩くて死んだ」→「では緩みの発生源はどのステップか」)。長期 relay で諦めが起きる
   実体は意志の問題ではなく**次に試すことのリストが空になること**なので、台帳が枯れない構造を
   規約で担保する。子を思いつかない死因だけが `harvested` に落ちる。

10. **判定は機械に寄せる — 自己評価を信用しない**
    「進捗があった / この道は生きている」を主観で判定しない。判定を次の 4 つの機械的シグナルに
    寄せる: Lean が通ったか / 数値 probe が反例を出したか / 文献に該当が有ったか / 台帳に新しい行が
    増えたか。どれにも該当しない leg は**ゼロ進捗として記録する** (§5-5 のカウントが進む)。
    ゼロ進捗を認める勇気がラウンドロビンを機能させる。

11. **primary-source-first — 一次文献を先に取る** (L9 で追加。§5-7 の重複ではなく**補完**)
    §5-7 が定めるのは「新規性主張の前に文献確認を 1 手挟む」という**タイミング**で、**どの文書を読むか**
    を定めていない。L2 と L7 の 2 つの誤判定は**どちらも §5-7 の字面を守った上で発生した** (二次文献を
    逐語で読んでいた) ので、タイミングの規約だけでは塞がらない。
    - **発動条件**: ある主張に `known` / `new` のラベルを付ける前に、**その主張の出所として引かれている
      参考文献のうち一次文献を 1 本は取得する**。⚠ 二次文献が「[n] による」と書いている段を**二次文献の
      逐語で判定しない** (実例 2 件はどちらも原典 1 本で解消し、**二次文献の側が式を粗く書いていた**)。
    - **取得経路**: 一般検索より先に**著者の publications ページ**を当たる (L8 の 3 本は検索では届かず
      著者ページからのみ取得できた。⚠ ディレクトリ一覧が 403 でも個別 PDF は取れる)。
    - **取得できないとき**: 「二次文献のみで判定した」と**明示的にラベルする** ([`bc-facts.md`](bc-facts.md)
      の confidence は `human-judgment` のまま `(primary)` / `(secondary)` の但し書き。§6.6)。

---

## 6. イテレーションの設計

**予算 = 20 leg** (超過時に延長するかは 20 leg 完了時点で判断)。

### 6.1 1 leg の内部構造

leg は毎回この形。中身の attack が何であれ枠は変えない。

| 段 | やること | 所要 |
|---|---|---|
| **入口** | handoff から 3 点を読む: 現在の軸 / その軸の連続ゼロ進捗カウント / 次の 1 手。**台帳のうち読むのは対象 attack の行と `bc-facts.md` だけ** (全文は読まない — 20 leg 分の履歴を毎回読むと context が本体に届かない) | 短 |
| **本体** | **1 attack の 1 ステップだけ**進める。複数 attack を詰めない (台帳の粒度が壊れ、死因が混ざる) | 大部分 |
| **出口** | ①確定事実を 1 行 (`bc-facts.md` or 台帳) ②`killed` なら子 attack を 1 本起票 (§5-9) ③ゼロ進捗カウント更新 ④handoff | 短 |

**出口の 4 つは leg が失敗しても必ず実行する**。失敗した leg こそ記録の価値が高い。

### 6.2 20 leg の配分 — 固定枠と可変枠

| leg | 用途 | 枠 |
|---|---|---|
| L0–L2 ✅ | **完了** (すべて 2026-08-01) — L0 文献 13 行 (F3 REFUTED / F7 強化) / L1 probe 基盤 `bc_probe.py` + facts 4 行 (V1–V3 TRUE、軸 G の (7)/(8) 違反) / L2 定義照合 + 閉凸包 probe で facts 4 行 (V4 条件つき生存 / V5 は閉凸包の命題 / **V5-b = (11) は偽陽性保証** / 新項目は別命題)。反映先は §1.1 / §1.3 / §4 | **済** |
| L3 / L4 ✅ | **完了** — L3 = 軸 A の第一手が **FAIL** (差し替え先は元クラスと同値、新クラス候補ゼロ。facts 2 行 + 子 attack 2 本) / L4 = 棚卸し #1 (park 対象ゼロ、idea 6 本起票、**ゼロ進捗カウンタが構造的に発火しないことを検出** = 判断ログ 14) | **済** |
| L5 / L6 ✅ | **完了** — L5 = 軸 G の第一手が **「述べられるが偽」** (M2 は数値反例で FALSE ⟹ 軸 E と同じ前提工事へ合流、判断ログ 16) / L6 = 軸 B は**同定完了・帰結は否定的** (ステップ数は 21 でなく **39**、我々固有の exploitable な緩みは **0 本**。inventory 1 本) | **済** |
| L7 ✅ | **完了** (`c58850c8`) — 経路構築 #1 = **R1** (12 ステップ、`unprobed`)。産物は**骨格の訂正** (`{a_x}` ↔ `θ` の Legendre は存在せず `min_α max_p min_a` の 3 段入れ子) + 壊れる場所の同定 + 同居 probe `u0-necessity-quantified` の肯定的決着。facts 2 行 + routes 1 本 + 子 attack 3 本 | **済** |
| L8 ✅ | **完了** (`81548e12`) — 経路 #1 の probe + novelty gate。**(4) は通過 / (5) は不合格** (一次文献 3 本の取得で 12 step すべてに逐語出典、死因 `known-result`)。P2 は目的が消えて取り下げ、P3 は [AGN19] の公開要請と重なり生存、P4 は価値低下。facts §L8 の 7 行 + routes.md 全面改訂 + 子 attack 2 本を `harvested` へ | **済** |
| **L9** ✅ | **完了 — 棚卸し #2**。L7 / L8 の実測で残り枠を **gate-first** に組み替え (下記 L10–L18)。判断ログ 16 を**条件つき**で決着 / **park の再開条件を訂正** (§6.6) / **§5-11 (一次文献を先に取る) を追加** / **最低ラインの到達可能性 = 到達可能だが条件つき** (§7、ユーザー判断の論点つき) | **済** |
| L10 ← 次 | **探索 + gate — 軸 C** (`jog-nair-inequality`)。**LP gate `shannon-cone-lp` を先に建てる** (elemental Shannon 不等式を制約とした LP = 「その不等式は Shannon 型か = 既知か」の機械判定)。⚠ §5-11 に従い**冒頭で Jog–Nair の一次文献を取る**。出口 = 主張 1 文 + step 列 + **gate 通過**まで | 可変 |
| L11 | **経路構築 #2 + probe (同一 leg)** — L10 の gate 生存候補を **R2** として書き下し、同じ leg で (4) probe まで済ませる。gate (5) は L10 で済んでいるので、**通れば `probed` の経路が 1 本立つ** | 可変 |
| L12 | **探索 + gate — 軸 A** `class-def-vs-consequence-gap` (idea `conditioning-collapse-schema` の schema を先に書く。昇格条件 = 条件つき版と非条件版が**同値にならない** `(F,G)` の実例 1 つ) | 可変 |
| L13 | **経路構築 #3 + probe (同一 leg)** — L11 と同じ形 | 可変 |
| **L14** | **棚卸し #3 + 収穫判定 (固定)** — **生存経路の本数でここが分岐する**: **≥ 2 本** → L15–L18 は 3 本目と `probed` の強化 / **1 本以下** → **軸 G ブロックを L15–L17 に一括発動** (工事 `marton-3aux-probe` 1 + 構築/probe 2 = 3 leg、判断ログ 16 の条件) / **0 本** → §7 の代替収穫物への切替をユーザーに諮る | **固定** |
| L15 | **探索 + gate — 軸 D** `multiletter-subject` (idea `entropic-vector-as-hierarchy-atom` を入力に)。⚠ L14 の分岐で**軸 G ブロックに差し替わりうる** | 可変 |
| L16 | **経路構築 #4 + probe (同一 leg)** | 可変 |
| L17 | **予備** — 生存経路の probe 強化 (`unprobed` → `probed`) / 落ちた枠の埋め合わせ / 軸 G ブロックの 3 枠目 | 可変 |
| L18 | **探索の最終枠 (打ち切り)** — 軸 E の P3 (§7 代替収穫物 1) / 軸 F `cardinality-localmax-boundary` / idea `product-outer-claim4` の拾い上げ、または経路の書き上げ | 可変 |
| **L19** | **収穫 leg** — 出た成果を成果物の形に確定する (散文 draft / Lean / facts) | **固定・予約** |
| **L20** | **記録 leg** — proof-log + 次の 20 leg の設計 (延長する場合の入力) | **固定・予約** |

**L19 / L20 を先に予約するのが要点**。予約しないと 20 leg 目まで探索に使い切り、何も残らないまま
予算が尽きる。**探索は L18 で必ず打ち切る**。

**gate-first — L9 の組み替えの中身**。旧配分は「探索 → 構築 → probe → gate」で **gate が最後**だった。
L8 の実測はこの順序が高くつくことを示す — gate のコスト (一次文献の取得) は構築 1 leg より安いのに、
**落ちるときは経路を丸ごと落とす**。⟹ 新配分は **[探索 + gate] → [構築 + probe]** の 2 leg 1 組。効果は
3 つ: (a) 死ぬ候補は**構築の前に**死ぬので損失が 1 leg で止まる、(b) 構築 leg を通過した経路は**その leg
内で probe まで済む** ⟹ §3.3 の「うち ≥ 1 本が `probed`」が別コストでなくなる、(c) 1 組が 2 leg のまま
なので **L10–L18 の 8 枠 (L14 を除く) で経路 4 本を試行できる**。

**出口条件**: 探索 + gate 枠 (L10 / L12 / L15 / L18) = §6.7 の「probe 可能な命題を 1 本」+ §3.1 の
**(1) 主張 1 文 + (2) ステップの列挙 + (5) novelty gate** (最後の 1 つが L9 の追加。満たせない理由は
idea 行へ)。構築 + probe 枠 (L11 / L13 / L16) = **(3) 各ステップのラベル + (4) 小ケースの probe 1 回**
⟹ **通過した時点で 5 条件が揃う**。

⚠ **leg ラベルと予算の 1 枠ずれ**: ラベルは L0–L20 の 21 枠だが予算は 20 leg。L0–L9 消費後の残りは
**11 leg / 12 ラベル**で、上の配分は 11 leg で最低ラインに届く形にしてある。**超過 1 枠の解消 (L19 と
L20 の同居 or 予算の 1 枠延長) は L19 で判断する** — 探索枠を削って辻褄を合わせない。

### 6.3 進捗と死亡のシグナル (事前定義)

途中で気分に応じて判定しないよう、先に書いておく。

**live** (1 つでもあれば軸は生きている): Lean の仮説差し替えが通った / 複数の異なる設定で数値 probe が
反例を出せなかった / 文献に該当が無いことが確認できた / 新しい部分命題が 1 本立った。

**killed** (死因を台帳へ): 数値反例が出た (sim を実 `def` と照合済) = `numeric-counterexample` /
文献に既出 = `known-result` / probe が通らず原因が命題の偽性に帰着した = `probe-failed` /
Mathlib 側の資産が無く自作行数が見積れる = `mathlib-wall`。**⚠ 「難しい」は死因ではない** — `too-hard` は
**必要な補題を名指しでき自作行数を見積れたときだけ**。名指せないなら `parked` (§5-5) であって死ではない。

### 6.4 詰まったときの診断 — 順序を固定する

**この順で潰す**。多くの時間は「4 から始める」ことで溶ける。

1. **命題が偽か?** → 小ケースを数値 probe で叩く (**未解決問題では偽の確率が遥かに高い**ので最優先)。
2. **定義がずれているか?** → Lean `def` と文献の定義を verbatim 照合 (補助変数の量化範囲 / 共通
   メッセージの有無 / 閉包の取り方)。⚠ **文献側は一次文献で照合する** (§5-11)。
3. **補題が足りないか?** → loogle (Mathlib) → `rg` + `dep_consumers.sh` (in-repo)。**in-repo を飛ばさない**。
4. **証明が見つからないだけか?** → 別戦略を 1 つ試し、駄目なら `parked`。

### 6.5 両方向の義務 — 悪い諦めと自己欺瞞に非対称に効かせる

- **「この道は死んだ」と書く前に、生きている証拠を 1 つ探す** (CLAUDE.md の壁反証義務の適用)。
- **「証明できた」「これは新しい」と書く前に、反例を 1 つと文献を 1 つ探す** (§5-2 / §5-7)。

前者が悪い諦めを止め、後者が自己欺瞞を止める。**片方だけでは必ずもう片方に倒れる**。

### 6.6 棚卸し leg (L4 / L9 / L14) にやること

探索が局所に沈むのを防ぐ定点観測。所要は 1 leg 未満でよい。

1. 台帳の全行を俯瞰し、`live` / `parked` / `killed` の分布を見る
2. **ゼロ進捗カウントが 3 に達した軸を強制 park** し、park 済みの軸に再開条件が揃ったか見る
3. **アイデアの供給源を 1 周する** — 死因からの派生 (§5-9) / 類似問題からの移植 (**MAC / Han /
   Slepian–Wolf / Wyner–Ziv が同じライブラリにある。⚠ interference channel は在庫に無い**、L4 で確認) /
   既存証明の読み直し / 数値実験のパターン / 文献の未使用の道具
4. 残り leg 数と §6.2 の配分を突き合わせ、必要なら可変枠の割り当てを変える

**L9 の点検結果** (配分は §6.2、根拠は判断ログ 20–23):

- **強制 park 対象は無い** (全軸 0)。⚠ カウンタは判断ログ 14 のとおり発火しえないので、**L9 は枯渇
  (最後に leg の主語になってからの leg 数) で判定した** — 軸 C が 9 leg 未着手で最大 ⟹ L10 を割り当てた。
- **park は `markovity-conjecture` の 1 本のみ。再開条件の文言を訂正する** — 旧文言は
  「`marton-3aux-probe` **または** `support-vs-dual-additivity` のどちらか」で、後者は L8 で `harvested`
  ゆえ**形式上は満たされている**。だが満たし方が「工事が済んだ」ではなく**「経路が既知で死んだ」**で、
  **probe 側に 3 補助版が無い状態は変わっていない** ⟹ **再開条件は `marton-3aux-probe` の 1 本に絞る**
  (L8 が効いたのは再開条件ではなく**工事の見積り**の側)。⚠ **台帳の書き換えは L10 の出口**で実施する
  (状態の SoT は台帳、本 leg の書き込み先は plan のみ)。
- **「前提工事待ち」の `todo` 4 本**: `selftensor-counterexample` / `u0-gap-2aux-vs-3aux` は工事が済んでも
  **経路にならない** (軸 E の条件 (5) が構造的に閉じた) ので発動しても収穫物どまり。`jbound-optimality` /
  `outer-region-j-def` は層 3 だが**Li が逐語で "optimality unknown" と書く唯一の開いた標的**ゆえ
  **次 relay の最有力候補**として引き継ぐ。
- **confidence 語彙の穴 (facts 側)**: `human-judgment` が一次/二次文献の逐語を区別しない (覆った 2 件は
  どちらも後者)。**分ける。ただし値は増やさない** (CLAUDE.md が 3 値を固定) ⟹ `(primary)` / `(secondary)`
  の**但し書き**にする。**実施 (凡例の改定 + 既存行の分類) は L10 の出口**。

### 6.7 その他の運用

- **サブエージェント同時 1 体**を厳守 (CLAUDE.md)。
- **Lean を触るのは既存資産への問い合わせまで** (§2.1)。**新しい補題を積み始めたら層 3 なので打ち切って
  散文に戻る** — 本 relay の予算を溶かす最大の穴がここ。
- 散文 leg の出口条件は「散文が 1 ステップ進んだ」**ではなく**「**probe 可能な命題を 1 本立てた**」。
- **経路への昇格は §3.1 の 5 条件を満たしたときだけ**。満たさないうちは `idea` 枠に置く。

---

## 7. 最低ラインの到達可能性 (L9 の判定) と次の 2 leg

**L0–L9 は完了** (要約は §6.2)。**実測 (見積りではない)**: L7 = 1 leg で経路 1 本 (12 ステップ) + 同居
probe 1 本 / L8 = 1 leg で (4)(5) 両方 ⟹ **経路 1 本 = 2 leg**、ただし (5) で落ちたので**歩留まり 0/1**。

### 判定 — **到達可能だが条件つき**

残り 11 leg のうち探索/構築に使えるのは 8 枠 (L10–L18 から L14 を除く) = **経路 4 本の試行**。最低ライン
に届くには **4 本中 2 本が gate を通ればよい** (`probed` は gate-first で自動的に付く) ⟹ **到達不能では
ない**。ただし次の 3 条件に依存する:

1. **軸 C の LP gate が実際に建つこと** (`shannon-cone-lp` の昇格条件 = Jog–Nair 不等式に「Shannon 型で
   ない」を返し肯定コントロールが通ること)。建てば**候補を 1 leg で複数殺せる**ので歩留まりが跳ねる。
2. **軸 A の schema に境界が付くこと** (同値にならない `(F,G)` の実例 1 つ)。付かなければ L3 と同じ
   潰れ方をして軸 A からは経路が 0 本。
3. **歩留まり 0/1 が構造要因ではなかったこと**。L8 の死因は**手順の欠陥** (二次文献のみで判定) に帰着し
   §5-11 がそれを構築の前へ移した ⟹ 同じ死に方は繰り返さない、が本判定の前提。⚠ **これは推論であって
   確立事実ではない** (§6.5) — 反証は L11 / L13 の gate で出る。

⚠ **供給源は 4 つに限られる** — 軸 E からは経路が出ず (P3 は条件 (5) を構造的に満たせない、§4 軸 E)、
軸 B の (ii) と軸 D の一部は層 3 ⟹ 残るのは **軸 C / 軸 A / 軸 D / (条件つきで) 軸 G**。

### 届かなかったときの代替収穫物 (⚠ 最低ラインを下げるのではない)

**L14 で生存 0 本ならここへ切り替えるかをユーザーに諮る** (判断ログ 23)。定義は先に置く:

1. **[AGN19] Lemma 2 の独立再現** (routes.md P3) — 著者が逐語で公開要請した数値再現で探索空間まで指定
   済。新しい問いではないが**公開された未達成の要請に対する確定した回答**になる。
2. **機械化された novelty gate** (`shannon-cone-lp`) — BC 以外 (MAC / Han) に再利用できる。
3. **形式化プロジェクト固有の診断 3 件** — 軸 B の 39 ステップ対応表 (L6) / 軸 A の同値 (L3、`machine`) /
   `U₀` 無しの潰れが `|X|=2` 固有であること (L7)。いずれも**機械検証された連鎖の上でしか出せない診断**。
4. **relay 運用の設計そのもの** — §5 の 11 項 + 実測 (経路 1 本 = 2 leg、gate-first、歩留まり)。本文書の
   射程 (§冒頭) がこれを主成果物の 1 つとして最初から数えている。

**次の 2 leg**: **L10 = 探索 + gate (軸 C)** → 出口は「主張 1 文 + step 列 + gate 通過」。**L11 = 構築 +
probe** → 通れば経路 1 本が `probed` で立つ (詳細は §6.2)。⚠ **L10 の出口で台帳側の 2 件も実施する** —
`markovity-conjecture` の再開条件の文言修正と facts の confidence 但し書き (§6.6)。

---

## 8. 成果物と置き場

| 成果物 | 置き場 | SoT |
|---|---|---|
| 進め方 (本文書) | `docs/shannon/bc-open-problem-plan.md` | 運用設計 |
| attack 台帳 | `docs/shannon/bc-open-problem-attacks.md` | 個々の attack の状態・死因 |
| 確定事実 | `docs/shannon/bc-facts.md` (既存に追記) | 文献事実・数値判定・機械検証 |
| **数値 probe 基盤** | `docs/shannon/bc_probe.py` (L1 で完成) | 情報量式の記号展開 / 構造制約つき掃引 / 補助変数上の最大化。載せ方は §2.1 |
| 数値 probe (実行可能) | `docs/shannon/bc-*-check.py` | 反証実験。基盤を import する |
| Lean probe (使い捨て) | `docs/shannon/bc-probe-*.lean.txt` | 仮説差し替えの機械検証。**`InformationTheory/` へは置かない** — ヘッダの手順で一時ファイルとして再実行し、確認後に消す |
| **散文の候補経路 (本 relay の主成果物)** | `docs/shannon/bc-open-problem-routes.md` (L7 で作成、R1 を収録) | 層 1 — 経路ごとに §3.1 の 5 条件と §3.2 の確信度ラベル |
| Lean | `InformationTheory/Shannon/BroadcastChannel/` 配下 | 層 2 の probe のみ。**新規モジュールは作らない** |

---

## 9. 判断ログ

leg が進むごとに追記。plan budget = 600 行 / active 決定 10 件を超えたら `/compact-plan`。

1–13. **L0–L3 — 軸の組み替え / 射程限定 / 優先度改定 / 手順の訂正 (決着済、反映先のみ残す)**: 反映先は
   §1.1 / §1.3 / §4。**引き継ぐ運用上の義務は 2 つだけ**: (a) **台帳の負の判定を別目的に流用するときは
   元の測定が何を測ったかを確認する** (還元 (3) は閉凸包の命題)、(b) 実使用仮説の棚卸しは `rg` ではなく
   **`dep_graph.sh` の前方閉包**で行い**辺の向き先が `def` か名前つき補題か**を区別する。
14. **L4 — §5-5 のゼロ進捗カウンタは構造的に発火しえない (規約本文は書き換えない)**: 増えるのは「その軸の
   leg なのに確定事実がゼロ」のときだけで、それは §5-1 (no empty leg) 違反 ⟹ **§5-1 を守る限り 3 に到達
   できない**。しかも「回して進まなかった」と「一度も回ってきていない」が**同じ 0 で表示される**。
   **提案** = (i) カウンタは「詰まりの検出」専用、(ii) 別に**枯渇カウンタ** (最後に主語になってからの
   leg 数)、(iii) 棚卸し leg で閾値超過の軸は枠を割くか優先度を下げる理由を書くかの二択。⟹ **(iii) は
   L9 で初適用** (軸 C = 9 leg 未着手が最大 → L10 を割当、§6.6)。
15, 17. **L4 / L6 — 経路構築の開始を L7 に固定 (決着済)**: 配分の現物は §6.2、結果は 19 / 20。
16. **L5 / L9 — 前提工事 `marton-3aux-probe` に枠を割くか = 条件つきで割く (L9 で決着)**: L5 では軸 E と
   軸 G が同じ工事を共有していたが、**L8 で軸 E 側の消費先が 2 つとも経路を生まなくなった** (§4 軸 E)
   ⟹ **工事の価値は全額軸 G に移った**。見積りは下がった (`Δ(X)` 上の `G_{α,μ}` と上凹包 1 本で足り、
   繋ぎは文献にある) が、**工事は単独では経路を 1 本も生まない** — 工事 1 + 構築/probe 2 = **3 leg で
   経路 1 本**は gate-first の **2 leg で 1 本**より高い。⟹ **既定は割かない。L14 で生存経路が 1 本以下の
   ときだけ L15–L17 に一括発動**。⚠ **割かない場合、軸 G は park のまま relay を終える**。
18. **L7 — 骨格の訂正**: `{a_x}` と `θ` の間に **Legendre 変換は存在しない** (`θ` は `F` の内部パラメータ
   `(α, μ)`、正しい絵は `min_α max_p min_a` の 3 段入れ子)。L8 でこの 3 段自体が [AGN19] 式 (2)+(9)(10)(11)
   と確定。⚠ **射程の限定 (§6.5)**: 同居 probe が示したのは「`U₀` 無しでも `|X|=3` には潰れないチャネルが
   ある」までで、**`U₀` が不要だとは言っていない**。
19. **L8 — 判定の書き方を固定する**: R1 は「証明できた」でも「反証された」でもなく、**(4) は通った
   (数学的に無傷) / (5) で不合格 (既知)**。誤判定 2 件の原因 (二次文献のみで判定) は §5-11 に規約化した。
20. **L9 — 配分を gate-first へ組み替えた**: 実測は 2 つ — 経路 1 本 = 2 leg / gate は文献取得だけで経路を
   丸ごと殺す。旧配分は gate が最後だったので**最も安い判定を最も高い作業の後に置いていた** ⟹
   **[探索 + gate] → [構築 + probe]** の 2 leg 1 組へ。⚠ **副次効果が本命の 1 つ** — 構築 leg 内で probe まで
   済むので **§3.3 の「うち ≥ 1 本が `probed`」が別コストでなくなる**。現物は §6.2。
21. **L9 — §5 に 11 項目め「一次文献を先に取る」を足した**: §5-7 (novelty gate) との重複ではない —
   §5-7 は**タイミング**を定めるが**どの文書を読むか**を定めておらず、覆った 2 件は**どちらも §5-7 の
   字面を守った上で**発生した (二次文献を逐語で読んでいた)。⟹ 発動条件・取得経路 (著者ページを検索より
   先に) ・取得できないときのラベルを §5-11 に明記した。
22. **L9 — park の再開条件は「形式上満たされた」だけでは満たされない**: `markovity-conjecture` の旧文言は
   `support-vs-dual-additivity` の完了でも再開可としていたが、L8 のそれは **「工事が済んだ」ではなく
   「経路が既知で死んだ」**という満たし方で、**probe 側に 3 補助版が無い状態は変わっていない** ⟹ 再開
   条件を `marton-3aux-probe` の 1 本に絞る (§6.6、台帳への反映は L10 の出口)。⚠ **一般化して持つ**:
   再開条件は「どの attack が終わったか」ではなく**「何が使えるようになったか」**で書く (前者は死亡でも
   満たされてしまう)。
23. **L9 — 最低ラインは到達可能だが条件つき。⚠ ユーザー判断の論点 2 つ**: 判定・条件 3 つ・代替収穫物
   4 本は §7、**最低ラインの数値は動かしていない** (§3.3)。ユーザーに諮るのは (a) **L14 で生存経路 0 本
   だった場合に代替収穫物へ切り替えるか** (最低ライン未達を確定させる判断なので planner が独断しない)、
   (b) **leg ラベル 21 枠 vs 予算 20 leg の 1 枠ずれの解消** (L19/L20 同居 or 1 枠延長、§6.2)。
