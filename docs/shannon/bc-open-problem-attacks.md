# BC 未解決問題 — attack 台帳

> 個々の attack の**状態と死因**の単一の真実源。運用規約 (no empty leg / kill-first / round-robin /
> harvest line) は親 [`bc-open-problem-plan.md`](bc-open-problem-plan.md) §5 が SoT。
> 文献事実・数値判定・機械検証の確定は [`bc-facts.md`](bc-facts.md) 側。
>
> **状態**: `live` (進行中) / `killed` (死亡、死因必須) / `parked` (3 leg ゼロ進捗で退避) /
> `harvested` (収穫して終了) / `todo` (未着手)
> ⚠ **語彙の穴 (L5 で発覚、改定は提案のみ)**: 「進捗はあったが**前提工事が入るまで進めない**」に当たる状態が
> 無い。`parked` はゼロ進捗による退避の意味なので、流用すると「回しても進まなかった」と
> 「別の工事待ち」が同じ語で表示される (L4 が §5-5 のカウンタで見つけたのと同じ形の欠陥)。
> **提案**: `blocked(<前提の slug>)` を足す。当面は `parked` + 現況欄に前提と再開条件を明記して運用する。
> **死因の語彙**: `numeric-counterexample` / `known-result` / `probe-failed` / `too-hard` /
> `mathlib-wall`
>
> ⚠ `killed` の行は**削除しない**。plan hygiene の「決着済は削除」の例外 — ここでは死因そのものが
> 資産で、削除すると長期 relay が同じ壁に再び当たる。

## Leg 予算

**20 leg** (L0–L20 の枠割りは親 plan §6.2 が SoT)。延長判断は L20 完了時点。
**固定枠**: L0 文献 / L1 probe 基盤 / L4・L9・L14 棚卸し / **L19 収穫・L20 記録 (予約済、探索に流用しない)**。
**現在**: leg 13 / 20 (**L13 = 軸 C の子 C2 を kill**)。**有効な経路は 1 本のまま** —
[`bc-open-problem-routes.md`](bc-open-problem-routes.md) の **R2** (L10、確信度 `probed`。⚠ 核 step 9 は
`gap` 表示で掃引が殺せなかっただけ)。**L13 は経路を 1 本も増やさず、候補 3 本を全部殺した**
(`killed` leg であって route leg ではない) — ただし副産物として **(H1) 厳密なスラック恒等式**が
記号的な係数相殺で立ち、C2 の問い (「鋭いチャネル依存スラック」) が**非負 4 項の損失 `L'` の下界**へ
厳密に言い換わった ([`bc-facts.md`](bc-facts.md) §L13)。
⚠ **記録済の必要条件を全部通しても generic に死ぬ**という実例が出た (P1) — 運用規約の追加は
親 plan §5-12 (構築の前に非退化インスタンス 1 本に当てる)。
**次は L14 = 棚卸し #3 + 収穫判定 (固定枠)** (親 plan §6.2 / §7)。**生存経路が 1 本以下**なので、
記録済の分岐は **軸 G ブロック (`marton-3aux-probe` + 構築/probe 2) を L15–L17 に一括発動**。

## Attack 一覧

| slug | 軸 | 状態 | 直近 leg | 死因 / 現況 | 残った副産物 |
|---|---|---|---|---|---|
| `lit-landscape` | — | `harvested` | **L0** | **完了**。F1–F8 + 外部ノート V6–V10 を原論文で verbatim 確認し `bc-facts.md` に 13 行。うち **F3 REFUTED** (UV は最良既知 outer でない) / **F7 は plan より強い** (`Δ₁₈ᴴ`) / **F4・F6・F8 は PARTIAL** (帰属・内容・出典の誤り) / 外部ノートの文献層は生存 (V9 は日付まで一致) | 訂正済ランドスケープ (親 plan §1.1 / §1.3) + 下記の子 attack 6 本 |
| `probe-harness` | — | `harvested` | **L1** | **完了**。基盤 `bc_probe.py` (情報量式の記号展開 / 構造制約つき掃引 / 補助変数上の最大化、selftest 18 項目) + 実行可能 probe 2 本。使い方は親 plan §2.1。テストケース (a) 外部ノート V1–V3 は**3 本とも TRUE**、うち V1 は係数相殺による**証明** (掃引ではない)、V2 は**強化つき** (無仮定の (5) の残差が正確に `I(A;B) − I(Y₁;Z₂∣A,B)` ⟹ ノートの 2 仮定はちょうど必要十分)。⚠ **ノートへの訂正 1 件**: V3 の数値例は**ノート自身の結論には不要** — 残差が相互情報量 2 本の和ゆえ設定下で `Φ ≥ Ψ` は恒真で、例が示すのは「残差が恒等的に 0 ではない」ことだけ。逐語 → [`bc-facts.md`](bc-facts.md) §数値 probe の判定 (L1) | 基盤そのもの (以後の全 attack の入力) + idea `residual-as-slack-diagnostic` の裏づけ (残差の**閉じた式**が確定) |
| `selftensor-reduction` | E+D | `harvested` | **L2** | **完了**。4 判定。**V4 = 条件つき生存** — 定理1 の逆側は我々の `bcCapacityRegion` の上で成立し、しかも**共通メッセージ層は不要** (私信のみの定義ゆえ)。ただし**達成側は在庫では閉じない** (`marton_region_subset_capacity` の full-support 仮説を決定論的符号器が満たさない)。**V5 = 還元 (3) は support function 経由ゆえ*閉凸包*の命題**。`martonRegionUnion` は既に `closure` 済で未証明なのは凸性だけ ⟹ 「closure を取れば入る」退路は最初から無く、劣化 BSC 対で測ったギャップは凸性ギャップではなく**本物の support function ギャップ** (`λ* = 0.2622134001` で `+2.23e-03`)。**V5-b = (11) は我々の 2 補助変数 `def` の上では自明に充足** — 原因は非加法性ではなく共通補助 `U₀` の欠落なので**保証された偽陽性**。**新項目 = `h_n^T(θ)` 加法性と `F(T,{a_x})` 加法性は別命題** (定義域・汎関数の型・共通補助の扱いの 3 点で食い違い、どちらも他方を構文的に含まない)。逐語 → [`bc-facts.md`](bc-facts.md) §定義照合と数値 probe の判定 (L2) | `bc-marton-convexhull-check.py` + 下記の子 attack 3 本 (`marton-3aux-probe` / `support-vs-dual-additivity` / `u0-necessity-quantified`) + **既存の負の判定行の射程拡大** (union までの距離 → 閉凸包までの距離) |
| `selftensor-counterexample` | E | `todo` | — | ⚠ **ブロッキング前提**: **我々の `martonRegionUnion` (2 補助変数) の上で走らせてはならない** (V5-b: 保証された偽陽性)。**3 補助変数版 `M(T)` を probe 側に用意すること = `marton-3aux-probe` が前提**。その上での中身 = 同一チャネルの自己テンソルで `h_2 > 2 h_1` を数値探索 (外部ノート (11))。これが 1 つ出れば `C(T) ⊋ M(T)` が直ちに従う。⚠ 還元自体は既知ゆえ、軸 E で新規性を主張できるのはここ | — |
| `marton-3aux-probe` | **G** (L10 で `E` から訂正) | `todo` | L7 (間接) | **V5-b から起票** (§5-9)。3 補助変数 Marton 領域 (共通補助 `U₀` つき、[`bc-facts.md`](bc-facts.md) F2 の Jog–Nair Bound 1 の形) を**数値 probe 側に**用意する。`selftensor-counterexample` / `u0-gap-2aux-vs-3aux` のブロッキング前提。⚠ **Lean 側の新規実装は層 3 でスコープ外** (親 plan §2.1) — `bc_probe.py` の消費者として probe 側に閉じること。**⚠ L7 の R1 が中身を 1 本に絞り込んだ**: 要るのは「`W` を持つ領域」そのものではなく **`Δ(X)` 上の `G_{α,μ}(q) := max_{(U,V): 入力周辺 = q}[I(U;Y)+μI(V;Z)−I(U;V)] − α·H(Y_q) − (μ−α)·H(Z_q)` とその上凹包 1 本**で足りる (`W` は上凹包を実現する変数として消える — routes.md R1 step 3 / (e))。⚠ **上凹包を取る対象は素の Marton 目的値ではない** — エントロピー項を含んだ `G_{α,μ}` を凹化しないと `F` と合わない (同 R1 節内「骨格の訂正」の「要修正」項)。**⚠ L9 の枠決定 — 本工事は既定では発動しない (条件つき枠)**: L8 で軸 E 側の消費先が 2 つとも経路を生まなくなったので**工事の価値は全額軸 G に移った**が、工事は単独では経路を 1 本も生まない — 工事 1 + 構築/probe 2 = **3 leg で経路 1 本**は gate-first の **2 leg で 1 本**より高い ⟹ **既定は枠を割かず、L14 で生存経路が 1 本以下のときだけ L15–L17 に一括発動する** (親 plan 判断ログ 16 / §6.2)。**発動しない場合、軸 G (`markovity-conjecture`) は park のまま relay を終える**。**⚠ L10 で軸欄を `E` → `G` に訂正**: L9 が「工事の価値は全額軸 G へ移った」と判定したのに軸欄が `E` のままだったので、**L14 で枯渇カウント (最後にその軸が leg の主語になってからの leg 数) を取ると本工事が軸 E 側に計上され、軸 G の「回ってきていない」度合いを過小に見せる** — 判定を歪める向きなので軸欄を実態へ合わせる | — |
| `support-vs-dual-additivity` | E+G | **`harvested`** | **L8** | **L7 で経路 R1 として書き下しに成功し、L8 が (4)(5) を通した ⟹ 判定は「(4) は通り、(5) で落ちた」**。**(4) probe = 通過** — R1 は数学的に無傷で、(H1) の読みも文献どおり (step 9→10 は死なない)。**死因は `known-result`** — 一次文献 3 本 ([GLN25] / **原典 [AGN19]** / [N20]) を取得した結果 **R1 の 12 step すべてに逐語出典が付き**、R1 に残る仮定は **(H1) = Conjecture 1 ただ 1 本**になった ⟹ 「未解決の予想を仮定すれば未解決問題が解ける」= [GLN25] Conclusion が逐語で述べる**著者らが既に持っている含意**に収束した。**本 attack が L2 で起票された根拠は消滅した** — L2 は「(a) `M` の閉凸性 / (b) `{a_x}` と `θ` を結ぶ双対の段の**どちらの文献も供給していない**」と判定していたが、**L8 が両方とも [AGN19] にあると確定させた** ((a) = §I-B の地の文 / (b) = 式 (2) + (9)(10)(11))。誤判定の原因は **L2 の時点で二次文献 ([GLN25]) しか読んでいなかったこと**。⚠ **R1 を `refuted` には落とさない** — 含意としては 1 step も落ちていないので、routes.md には「軸 E のどこが開いているかを逐語出典つきで確定させた地図」として残す。逐語 → [`bc-facts.md`](bc-facts.md) §L8 | 経路 [`bc-open-problem-routes.md`](bc-open-problem-routes.md) の **R1** (地図として存続。5 条件は (1)(2)(3)(4) 充足 / (5) 不合格ゆえ経路表には載らない) + 検算 [`bc-route-r1-check.py`](bc-route-r1-check.py) (約 9s、`--quick` 0.5s、局所最適化不使用ゆえ再実行で全報告値一致。16 項目は**文献の等式の独立再導出**だったことが判明) + **骨格の訂正** (同文書 R1 節内) + [`bc-facts.md`](bc-facts.md) §L8 の 7 行 + **軸 E に残る唯一の着手可能な口** = [AGN19] が逐語で公開要請した Lemma 2 のインスタンス独立再現 (routes.md P3) |
| `r1-step6-tightness` | E | **`harvested`** | **L8** | **開いた小問ではなかった (起票の前提が消滅)**。L7 は「R1 step 6 の残余 `S₁ ≥ A₂` を最大化点で示すことに証明も反例も無い」としたが、**[AGN19] は rate triple の `R₂` を `[·]₊` で切断して処理済** — 達成側は切断つきの頂点で押さえ、上界側は切断なしの線形計画緩和で押さえる 2 方向の議論になっている (逐語 → [`bc-facts.md`](bc-facts.md) §L8 (vii))。**L7 の誤検出の原因は [GLN25] が `[·]₊` を落として書いていること**で、原典にその粗さは無い ⟹ 「二次文献だけを読んで残余に見えたものが、原典を当たると処理済だった」形の誤検出 (`support-vs-dual-additivity` 行の L2 と**同じ原因の 2 件目**)。⚠ **P2 (数値 probe) は実施せず取り下げ** — 確かめるべき未決の命題が無い。**設計そのものは記録として routes.md §未達の条件に残してある** (「2 つの下界を比べる設計にしてはならない / 同一の `p` の上で述語だけを見る」という誤差の向きの規律は、`marton-3aux-probe` / `selftensor-counterexample` で 3 補助版を数値で触るときにそのまま要る) | L7 が証明した半分 (`S₂ ≥ A₂` は最大化点で必ず成立) + 取り下げた P2 の設計 (routes.md) |
| `conj1-parameter-quantification` | E | `harvested` | **L8** | **完了。3 点とも決着し、副作用として novelty gate が R1 を落とした**。**(i) 量化子は「一様」でよい** — R1 の (H1) の読みは生きている。ただし量化子は Conjecture 1 の文には無く (記法 `F(T,{a_x})` が `(α,λ)` を落としている)、根拠は (a) Mar-Con §I の "For any λ ≥ 1" と式 (1) の `min_{α∈[0,1]}`、(b) 論文自身が `min_{α∈[0,1]}` の**内側で** Conjecture 1 を適用していること、(c) 決定打として **Nair, ITA 2020 (Mar-Con 参考文献 [17]) が同じ対象を `F₁^{(λ,α)}` とパラメータを上付きで書き、"For λ ≥ 1 and α ∈ [0,1], define the following" と量化したうえで加法性を `∀η, ζ` として述べ Definition 1.4 "global tensorization property" と名付けている**こと。**(ii) `F` と Marton 領域の対応は文献にあり、上界ではなく等号** — Mar-Con 式 (1) `Γ^λ_M(T) = min_α max_p [αH(Y) + (λ−α)H(Z) + min_a(F − Σ p a)]` は `=` で、逐語 "We provide a proof of the above equation in Appendix A for completeness" + "which is derived via convex duality in [1]"。左辺 `Γ^λ_M(T) = max_{(R₁,R₂)∈R_M} R₁ + λR₂` は support function そのもの ⟹ **R1 step 6 = 仮説 (H2) は既知**。**(iii) 範囲は 3 本すべてで明示**。**⟹ R1 の 12 step は 1 本残らず逐語出典が付く** ([`bc-facts.md`](bc-facts.md) §L8 (iv) の対応表) ので、L7 が「新規性は step 6 + step 10 に限定される」と見立てた残り 2 点も既知で、**R1 は正しいが既知の再構成**。副産物として **`r1-step6-tightness` の残余は原典が `[·]₊` の切断で処理済** (§L8 (vii))、**(H3) 閉凸性は原典が地の文で主張** (§L8 (vi))、**Mar-Con 式 (1) 前の `F` の転記誤り (`(1−α)` → `(λ−α)`) を原文で確定** (§L8 (ii))。旧起票文にあった「原典 [1] = Anantharam–Gohari–Nair, *On the evaluation of Marton's inner bound for two-receiver broadcast channels*, IEEE TIT 65(3):1361–1371, 2019」は**取得できた** — IEEE Xplore は有料だが著者ページに全文がある (`https://chandra.ie.cuhk.edu.hk/pub/papers/BC/Marton-update.pdf`、8 頁)。⚠ **§5-7 の両方向の義務は両向きとも守った** — step 6 は [1] を読了した**上で** `known` に落としており、逐語出典の取れなかった項目 (下記の novelty gate 3) は `new` のまま残していない (打ち切りとして明示) | 一次文献 3 本の入手経路 + [`bc-facts.md`](bc-facts.md) §L8 の 7 行 (逐語つき) + **経路 R1 の novelty 判定** ([`bc-open-problem-routes.md`](bc-open-problem-routes.md)) |
| `u0-gap-2aux-vs-3aux` | A+E | `todo` (前提工事待ち) | **L7** | **`u0-necessity-quantified` の収穫から起票** (§5-9)。潰れが `U₀` の欠落からではなく**チャネル依存**で決まると分かった以上、次の問いは主語が動く — 「`U₀` 無しの 2 補助 union と **3 補助 Marton 領域**のギャップが `\|X\|=3` でどれだけあるか」= `U₀` の必要性の**所在**の定量。⚠ **前提工事 `marton-3aux-probe` が要る** (probe 側に 3 補助版が無い)。**ただし R1 が仕様を絞った** — 要るのは「`W` を持つ領域」ではなく **`Δ(X)` 上の `G_{α,μ}` とその上凹包 1 本**である (routes.md R1 step 3 /「L8 後に残るもの」(e))。**⚠ 軸 G の知見をこちらへ横流しできない (L7 の走査が示した)**: L5 の反例チャネル `CEX-1` は L7 のギャップも出す (`+1.549702e-03 @ λ*=0.7612`) 一方、**Gohari–Liu–Nair §III-B のチャネル対は L7 では潰れる** (`−1.388e-16`) — 非矩形局所最大点の例として使われる非自明なチャネルであるにもかかわらず時分割包絡と一致する ⟹ **「非矩形最適点が立つ構造」と「時分割を破る構造」は一致しない**。したがって `markovity-localopt-route` が集めた統計 (非矩形が残るチャネルの頻度) を `U₀` 側の候補チャネル選定にそのまま流用してはならない | — |
| `u0-necessity-quantified` | A+E | `harvested` | **L7** | **完了、判定は肯定的**。起票時の問い (「潰れないチャネルを 1 つ見つける」) に対し、`\|X\|=3` の **Blackwell チャネル**で `U₀` を持たない 2 補助変数 Marton union の閉凸包が**時分割領域より真に強い**ことを確定 — `λ=0.5` でギャップ `+2.027325541e-01`。**⚠ この instance では判定が閉形式まで落ち、最適化に一切依存しない**: `X ~ U{0,1,2}`, `V₁ := 1{X=2}`, `V₂ := 1{X≠0}` で `S(1/2) ≥ (1/2)ln3`、`\|Y\|=\|Z\|=2` ゆえ初等的に `C₁,C₂ ≤ ln2` ⟹ ギャップ `≥ (ln3 − ln2)/2 = 0.202732554054` が手計算で出る (`S` 側は明示 witness の下界 / `env` 側は Blahut–Arimoto の証明つき上界なので**符号は探索不足では説明できない**)。⟹ **L2 が劣化 BSC 対で観測した「時分割への潰れ」は `\|X\|=2` に固有**であって `U₀` の欠落の一般的な帰結ではない。⚠ **射程の限定 (過大評価の防止)**: **`U₀` が不要だとは言っていない** — L2 の劣化 BSC 対では `U₀` 無しの領域は本当に潰れ、容量領域 (superposition) に遠く及ばない。また比較対象は**素の時分割 (TDMA) 三角形**であって randomized time-division でも superposition 領域でもない (F6 は sum rate について前者を述べる別対象)。⚠ **新規性は主張しない** — Blackwell チャネルは教科書的対象で、確定事実は**我々の `def` の support function の上で L2 の副産物の射程が `\|X\|=2` に限られること**である。逐語 → [`bc-facts.md`](bc-facts.md) §数値 probe の判定 (L7 — u0 の必要性) | probe [`bc-u0-necessity-check.py`](bc-u0-necessity-check.py) (194s / `--quick` 21s。肯定コントロール 6 本 + hardening 5 段 + 検出器の健全性・構造的な負のコントロール) + 走査 39 本の統計 (ギャップ 23 本、ランダム `\|X\|=3` は 17/32、Blackwell に一様分布を混ぜても `ε=0.30` で `+5.585e-02` と連続減衰するだけで消えない = knife-edge ではない) + 下記の子 attack `u0-gap-2aux-vs-3aux` |
| `weakest-hyp-morecapable` | A | **`killed`** | **L3** | **死因 `probe-failed`** — `IsBCMoreCapable` を `IsBCMoreCapable.condMutualInfo_le` の結論そのものに差し替えると**通らない**。足りなかったのは `IsBCMoreCapable` の `def` 本体 (非条件つき比較) **1 本のみ** (chain の残り 6 本は差し替え仮説だけで通る)。さらに**両方向を機械検証したところ差し替え先は元クラスと同値** (`#print axioms` sorryAx-free) ⟹ **新クラス候補は 1 本も立たない**。⚠ 親 plan の「核は `condMutualInfo_le` 1 本」は `mutualInfo_out₂_le_out₁_of_moreCapable` 最終行の `exact hmc _` = **`def` の直接展開**を見落としていた。逐語 → [`bc-facts.md`](bc-facts.md) §機械検証 probe の判定 (L3) | 同値 2 本 (どちらも sorryAx-free) + probe 本体 [`bc-probe-l3-weakest-hyp.lean.txt`](bc-probe-l3-weakest-hyp.lean.txt) + 下記の子 attack 2 本 |
| `weakest-hyp-degraded-lessnoisy` | A | **`killed`** | **L12** | **死因 `known-result`、着手前に決着** (行の ⚠ が予告していたとおり)。less noisy 側は [N10] **Remark 2** が逐語 "Setting `P` to be the entire set of distributions `p(x)` shows that a less noisy receiver is in particular an essentially less noisy receiver" と述べており、**条件つき化は more capable とまったく同じ形で潰れる** (`P` = 全分布での ELN = less noisy)。degraded 側はそもそも `IsBCDegraded` が `∃ Q : Kernel β₁ β₂, ...` で**入力分布を量化していない** (`Basic.lean:50-52` 逐語) ので条件つき化の軸が無い。⟹ **横展開しても新クラスは 1 本も出ない**。逐語 → [`bc-facts.md`](bc-facts.md) §L12 (A1)。⚠ **起票文が要求していた手順の訂正 (`dep_graph.sh` の前方閉包で実使用を数える) は無効化されていない** — 「等号定理が実際に使う仮説を数える」こと自体は有効な手続きで、死んだのは**その結果として新クラスを得る**という出口のほう。以下は旧起票文: **`weakest-hyp-morecapable` の死因から起票** (§5-9)。同じ最弱仮説抽出を degraded / less noisy の等号定理へ横展開する。⚠ **手順を訂正して適用すること** — 実使用の棚卸しは証明本文の `rg` ではなく `dep_graph.sh` の前方閉包で行い、**辺の向き先が `def` 自身か名前つき補題か**を区別する (more capable ではこの区別を落として「核は 1 本」と誤った)。⚠ more capable が同値で潰れた以上、**同じ形の同値で潰れる公算も高い** — 先に「条件つき版と非条件版が同値になる構造」がそのクラスにもあるかを確認してから probe を組む | — |
| `class-def-vs-consequence-gap` | A | **`killed`** | **L12** | **死因 `known-result`**。問い (「条件つき版と非条件版が同値にならないクラスはあるか、あればそこが軸 A の第一手が生きる場所」) には**答えがあり、その答えごと文献に占有されている**。**(a) 同値になる範囲は構造として確定**: 述語が `∀p ∈ S, F(p) ≤ G(p)` の形で `S` が分解成分について閉じているなら、⟹ は平均・⟸ は `U` を Dirac に取るだけで**必ず同値** ⟹ `S` = 全分布では常に潰れる。**(b) 唯一の破り方は `S` を真に狭めること**で、それは [N10] Definition 2/3 の *sufficient class* `P` そのもの。**(c) そこで得られるクラス (essentially less noisy / essentially more capable) は 2010 年に定義され、容量領域が [N10] Theorem 1 / Theorem 2 で確定済**。しかも **collapse の向き (more capable ⟹ EMC) は [N10] Remark 3 が逐語**で述べており、**L3 が機械検証した同値は既知**だった。逐語 → [`bc-facts.md`](bc-facts.md) §L12 (A1)(A2)(A3)。⚠ **`probe-failed` ではない** — 命題は偽ではなく真で、しかも既出。⚠ **軸 A の収穫ラインはここで形が変わる** — 「新クラスを切り出す」は達成不能だが、**[N10] Theorem 2 は我々の `bc_moreCapable_capacity_eq_uv` の真の一般化**なので、形式化標的として残る (層 3 = 次 relay、親 plan §4 軸 A (d)) | [`bc-facts.md`](bc-facts.md) §L12 の A1–A3 (逐語) + 一次文献 [N10] + 下記の子 attack 2 本 (`eln-canonical-class` / `superposition-optimality-scope`) + **形式化標的** ([N10] Theorem 2) |
| `eln-canonical-class` | A+F | `todo` | **L12** | **`class-def-vs-consequence-gap` の死因から起票** (§5-9)。[N10] Definition 2/3 は `∃ sufficient class P` という**クラスを走る存在量化**を含み、そのままでは membership が判定できない。**観察 (facts A5)**: sufficiency は包含について単調ゆえ **`∃P` は消せる** — ELN ⟺ 標準集合 `C := {p : ∀U, I(U;Y₂) ≤ I(U;Y₁)}` が sufficient class、EMC ⟺ `D := {p : š(p) ≥ 0}` が sufficient class (`š` = `I(X;Y₁) − I(X;Y₂)` の下凸包)。`C` は `g := H(Y₁) − H(Y₂)` が上凹包に接する点の集合として書ける。**問い**: この標準化のあと残る `∀(U,V,X) ∃q` (Definition 1) に**濃度上界**が付くか。付けば ELN / EMC の membership が有限次元最適化に落ち、[N13] 脚注 1 の意味で ε 判定可能になる。⚠ **novelty 未 gate** — 単調性は Definition 1 から自明ゆえ folklore の公算が高い。**gate は「ELN membership の判定可能性」を主語にして当てること** (言い換えそのものを主語にしない)。⚠ **濃度の段は L10 が LP gate を殺した場所と同じ** ([`bc-facts.md`](bc-facts.md) C4) — 濃度制約は Shannon 型不等式の外に出るので、LP では判定できない | — |
| `superposition-optimality-scope` | A+D | `todo` | **L12** | **同じ死因から起票、主語は逆向き** (§5-9)。[N10] は「ELN または EMC ⟹ superposition coding が最適」を与えるが、**逆 (superposition が最適なチャネルはすべて ELN か EMC か) は未確定**らしい。⚠ **現時点の根拠は二次文献 1 本のみ** (WebSearch 経由の要約文 "It is known that superposition coding is optimal when the channels are either essentially more capable comparable or essentially less noisy comparable, but a converse statement is still unknown") ⟹ **§5-11 により、着手する leg の冒頭で一次文献を 1 本取ること** (候補: arXiv:1401.6219 が ELN を定義して引いている / [N10] 以降の 2 受信者 BC の総説)。**逆が偽なら witness チャネルが 1 本**、真なら superposition 最適性の特徴付けが 1 本 — どちらでも確定事実。⚠ この問いは本 relay の標的 (計算可能な特徴付け) の**部分問題**である (superposition 領域は計算可能、ゆえに「superposition が最適なクラス」の特徴付けはそのクラスに対する計算可能な特徴付けを与える) | — |
| `outer-slack-localize` | B | **`killed`** | **L0** | **死因 `known-result`** — 「UV の証明で捨てた項を拾って新しい outer を作る」は Gohari–Nair Theorem 7 (auxiliary-receiver approach、IEEE TIT 68(2):701–736, 2022) が既に実行済で、その導出法が逐語で「UV 導出で捨てられる項を最小化する」もの (F3)。着手前に文献で死亡 | 子 attack 2 本 (`outer-slack-vs-thm7` / `jbound-optimality`) |
| `outer-slack-vs-thm7` | B | **`harvested`** | **L6** | **完了、帰結は否定的**。順序どおり文献 → 我々の証明で実施。**ステップ数は 21 ではなく 39** (L4 の名前フィルタが 18 本取りこぼしていた)。対応付けは **matched 22 / no-slack 12 / unmatched 5 / 未判定 0**、`unmatched` 5 本は **(i) 我々固有 1 本 (`I(W₁;W₂∣Y₂ⁿ)`) / (ii) 構成差 4 本 (`I(Q;Y·)`) / (iii) 0 本**で、**5 本とも最終領域を動かさない** (領域は全 UV 法の合併ゆえ独立な `Q` を取れば消える。(i) は `n` 文字項で単一文字化には新補助が要る = Theorem 7 がやったこと)。⟹ **我々固有の exploitable な緩みは 0 本**。逐語 → [`bc-facts.md`](bc-facts.md) §外界の緩みの棚卸し (L6)、全文 → [`bc-outer-slack-inventory.md`](bc-outer-slack-inventory.md) | **収穫ライン達成** = Theorem 7 の改善項 ↔ 我々の 39 ステップの対応表 (機械検証済の連鎖に対する診断で文献に無い) + 捨て項の 4 種分類 + 再現時の落とし穴 2 件 + 子 attack `outer-region-j-def` |
| `jbound-optimality` | B | `todo` (前提工事待ち) | **L6** | 同上の子。J version of UV outer bound の最適性は Li が逐語で "optimality unknown" ⟹ ここは開いている。J-bound が緩む例を探す。**⚠ L6 が前提を露出させた**: 我々の `bcOuterRegionUV` は `J` を持たないので**現在の在庫では 1 行も書けない** ⟹ `outer-region-j-def` が前提 | — |
| `outer-region-j-def` | B | `todo` (**層 3 = 本 relay スコープ外**) | **L6** | **`outer-slack-vs-thm7` の収穫から起票** (§5-9)。Theorem 7 の (18a)–(18i) + (19)(20) を満たす法の合併として `bcOuterRegionJ` を定義する (`InBCOuterRegionUV` が 4 フィールドの `structure` なのに対し 9 制約 + 3 等式 + 3 挟み込み)。**形式化タスクなので本 relay では着手しない** (親 plan §2.1) — **次 relay への引き継ぎ項目**。⚠ Remark 16 が逐語で「UV の各不等式に `M0 → (M0,J^n)`, `Z^n → Ĵ^n` を代入するだけで J-bound が出る」と述べているので、**在庫の `bc_capacity_subset_uv` の証明が再利用できる公算が高い** (見積りの入力) | — |
| `jog-nair-inequality` | C | **`live`** | **L10** | **1 leg で探索 + gate + 構築 + probe まで到達し、relay 初の有効な経路 R2 が立った**。⚠ 旧起票文「BSSC の情報不等式を Lean で述べる (T1)」は主語が古い — 一次文献 3 本 ([GJNW13] = Geng–Jog–Nair–Wang, IEEE TIT 59(7):4095–4105, 2013 / [NWG10] = arXiv:1001.1468 / [NKE16] = ITW 2016) を取得した結果、**BSSC 版 (F4) と binary-input 版 (F6) は 1 本のジャーナル論文に合流済**で、`\|X\|=2` 方向は原論文が閉じている ⟹ **標的は原典 §V が逐語で open と宣言する「`\|X\| ≥ 3` への正しい一般化」に確定**した (facts C1/C5/C7)。**候補 2 本を数値で kill**: **G1** (加法定数 `f(k)=log₂(k/2)`) は `k=3` で反例 (`p=(q,1−2q,q)`, `q=(5−√5)/10` の Blackwell で `gap = log₂φ = 0.694242 > log₂(3/2)`) かつ `k ≥ 4` では自明上界 `½log₂k` の帰結で空虚 ⟹ **どの `k` でも「偽か自明か」**、**G3** (乗法形) は `ρ*(k)=2` が全 `k≥3` で自明上界と一致し中身が無い (加えて新規性でも先客 = [GNA12] Appendix B の `c_{p(u,x)}`)。**生き残った形が R2** = 「`\|X\| ≥ 3` の鋭い加法スラック `f*(k)` は決定論的な格子 BC で達成される (`f*(k) = D(k)`)」。確定した値は `f*(2)=0` (Theorem 1 に退化) / `f*(4)=1` / `f*(9)=log₂3` / 完全平方で `f*(m²)=log₂m`、**未決は `f*(3) ∈ [log₂φ, ½log₂3] = [0.694242, 0.792481]`**。⚠ **核 step 9 (決定論的極値性) は `gap`** — 掃引が超えられなかっただけで証明ではなく、**同じ掃引が `\|X\|=4` で真の最大を 0.3058 bit 外した前例が同 leg 内にある** | 経路 [`bc-open-problem-routes.md`](bc-open-problem-routes.md) の **R2** (`probed`、5 条件は (1)(2)(3)(4) 充足 + (5) は `(secondary)`) + probe [`bc-jognair-general-check.py`](bc-jognair-general-check.py) (約 160s、`--quick` 約 25s、種と開始点固定ゆえ再実行で一致) + [`bc-facts.md`](bc-facts.md) §L10 の C1–C8 (一次文献 3 本の逐語) + 下記の子 attack 2 本 |
| `jognair-deterministic-extremal` | C | `todo` | **L10** | **R2 の核 (step 9) から起票** (§5-9)。主張 = **`f*(k) ≤ D(k)`**、すなわち `\|X\| = k` で加法スラックを最大化するのは決定論的な格子 BC である。**最小の未決着例は `k = 3`** (`f*(3) ∈ [log₂φ, ½log₂3]`)。⚠ **探索窓は狭く絞れている** — `f*(3) > log₂φ` を出す witness は `H(X) > 2log₂φ = 1.388` (= `p` がほぼ一様) と `I(X;Y) ∈ (0.694, 0.891)` を要求する。⚠ **決定論的側の掃引結果を「収束した」と読まない** (`\|X\|=4` の同時最大化はランダム再スタート 20 本が全部真の最大 `1.0` を外し `0.694242` に着地した)。**攻める道具の当て**: [GNA12] (arXiv:1202.0898) §III の極値分布 `p(u,v\|x)` の必要条件 (AND 型配置の排除 / 行・列が相異なる像を持つ) は、決定論的極値性を示すのに要る構造の絞り込みそのもの。**⚠ L13 が本行に効く 3 点**: **(i)** (H1) の恒等式 `gap = min{H(X\|Y),H(X\|Z)} − L'` により、step 9 (`f*(k) ≤ D(k)`) は**非負 4 項の損失 `L'` を下から抑える**主張と等価な形に書き換わる (`bc-facts.md` §L13)。**(ii) 兄弟候補 P1 の死を本行の否定と読まない (混同注意)** — P1 は**補助変数** `(U,V)` を `X` の関数に取れるかの話、本行は**チャネル**が決定論的な格子 BC のとき極値になるかの話で主語が違う。むしろ [GNA12] §III 冒頭の逐語 WLOG は **`X` が `(U,V)` の関数**という**逆向き**の還元なので、P1 の形は文献の還元と向きが逆だった。**(iii) 開いている安価な確認が 1 件 (⚠ 確定事実として使わない)** — [GEA11] Theorem 1 の `p(u,v) > 0` は**使用部分アルファベット上の格子**についての主張なので、観測された台の大きさ (最小 `3` / 中央値 `4` / 最大 `8`) のうち `4 = 2×2` と `6 = 2×3` は定理と**整合する** (`8` は積の形に載らないので、その点は真の最大化子でないか積集合でないか)。**残る確認は「台が本当に積集合か」の 1 点**で、probe への安価な 1 追加で済む。肯定なら dense な `\|X\|=3` の最大化子は `2×2` / `2×3` 格子上に住み **[GLN25] Proposition 1 の rectangular mapping と一致する** ⟹ 本行と子 attack `slack-loss-lower-bound` の共通の構造的入力になる (`bc-facts.md` §L13 の `open check` 行) | — |
| `jognair-channel-dependent-slack` | C | **`killed`** | **L13** | **死因 `numeric-counterexample`**。起票時の主語 = R2 の加法定数を捨て、**チャネル依存の汎関数 `Φ(T,p)`** で `T(X) ≤ Φ(T,p)` を狙う (完全平方 `k=m²` で `D(k)` が自明上界と一致し R2 の中身が消えるため)。**候補 3 本を作って 3 本とも死亡** ⟹ C2 は閉じる。**P1 = 決定論的最適性** `Φ_det(q,p) := max_{f,g: X 上の関数} [I(f(X);Y) + I(g(X);Z) − I(f(X);g(X))]` (全列挙で計算)。決定論的な条件付分布は `p(u,v\|x)` 多面体の端点ゆえ `Φ_det ≤ T` は恒真で、主張 `T ≤ Φ_det` は **`T = Φ_det`「最大化子は決定論的に取れる」と同値**。**(N1) PASS、しかも最強の意味で** — `\|X\|=2` では `X` の関数は定数か単射しかなく 4 通りの値は `0` / `I(X;Y)` / `I(X;Z)` / `I(X;Y)+I(X;Z)−H(X)`、`min{I,I} ≤ H(X)` ゆえ最後が max を超えない ⟹ **`\|X\|=2` では P1 は [GJNW13] Theorem 1 そのもの**。**(N2) PASS (等号)** — `f,g` に 2 本のチャネル写像を取ると `H(Y,Z) = H(X)`。**それでも refuted** (最大違反幅 `+0.1484`、**dense なランダム `\|X\|=3` チャネル**)。**明示 witness も在る** (最適化不使用): Blackwell の 2 出力をそれぞれ BSC(0.1) に通したもの + 一様 `p(x)` + `\|U\|=\|V\|=3` で、最良組 `(f,g) = ({0,1\|2}, {0\|1,2})` の空セル `(u,v)=(1,0)` へ `p(u,v\|x=0) = 0.8·δ_{(0,0)} + 0.2·δ_{(1,0)}` と流すと `Φ_det = 0.7065361326214252` (729 通りの全列挙と差 `0.0`) に対し `0.7686115289588658` ⟹ **margin `+0.062075` bit**。⚠ **死因は 1 個の反例ではなく機構**: 最良の決定論的組の**空セル** (2 座標がどちらも既使用のセル) へ質量を流すと目的値が `+p(x)·ε·log₂(1/ε)` で**超線形に増える** ⟹ 傾き `(obj(ε)−Φ_det)/ε` は `ε = 1e-1 … 1e-6` で `0.551 / 1.535 / 2.626 / 3.732 / 4.839 / 5.946` と発散し、桁あたり増分 `+1.0914 / +1.1057 / +1.1071 / +1.1073` が予測値 `p(x=0)·log₂10 = 1.1073` と一致する = **決定論的点は局所最大ですらない** (数値のアーティファクトではない)。⚠ **機構は自分の不発点も予測して当てている (kill より強い証拠)** — `η=0` (決定論的 Blackwell) は `I(V;Z)` 側が同率で損をして相殺、`η=0.2` は最良組の `f` が定数化して空セルが消え、**どちらも margin がちょうど 0**。dense 平滑化族では予測どおり全 `ε` で正かつ `ε` にほぼ比例して潰れる (一様 `p`: `+2.535e-02 → +1.547e-05`、黄金比 `p`: `+1.334e-02 → +9.603e-06`)。**クラス分け (dense ランダム `\|X\|=3` 600 本)**: 空セルを残す **class A は 99 本で 99 本すべて違反** (直接評価ゆえ取りこぼし無し、中央値 `0.0201` / 最大 `0.1484`)、**class B は 501 本で `Φ_det` が `max{I,I}` に退化する** (差 `≤ 4.4e-16`) ので主張は決定論性と無関係な `gap > 0` になる。⚠ **class B の 40 本標本は screening `0/40` → 探索強化で `18/40` に変わったので、これは率ではなく*探索強度に依存する下界*** (率として引用しない)。⟹ **生存するのは `\|X\|=2` (Theorem 1) と決定論的 BC の 2 つの退化ケースだけ**。**P2 = `p` で精密化した加法スラック** `D(p) := max_{f,g} [H(f(X),g(X)) − max{H(f(X)),H(g(X))}]` (`p(x)` のみに依存)、主張 `gap(T,p) ≤ D(p)` は `D(p) ≤ D(\|X\|)` ゆえ **R2 より真に強い**。**(N1)(N2) とも PASS** (`D(p)=0` for 全 binary `p` / 格子で等号)。**refuted、しかも最適化器を使わない明示 witness の直接評価で** — 部分マージ型チャネル (`Z` は決定論的 `{0,1\|2}`、`Y` は確率 `β` でのみ `x=1` を明かす、`U` は `x=2` でのみランダム化) の `β=2/3, α=3/4, p=(0.45,0.45,0.10)` で `T(X) ≥ 0.8822287189138015` / `max{I,I} = 0.46899559358928117` / `gap = 0.4132331253245206` に対し `D(p) = 0.3762211396014732` (25 通りの分割対の全列挙ゆえ厳密) ⟹ **違反幅 `+0.037012` bit** (第 2 witness `p=(0.440,0.440,0.120)` で `+0.034563`)。⚠ **孤立点ではなく族** — `p = ((1−d)/2,(1−d)/2,d)` を `d = 0.05 … 0.20` で振ると全域で margin `+0.022 … +0.036`。**P3 = `L' ≥ H(X\|Y,Z)`** (同値に `gap ≤ min{I(X;Y\|Z), I(X;Z\|Y)}`): (N2) PASS (等号)、**(N1) FAIL** — BSC(0.1) 2 本で Theorem 1 を `0.211081` bit 緩める (**死んだ案 B と同じ穴**)。⚠ **加えて ill-posed** — 右辺は**同時分布** `q(y,z\|x)` に依存するのに `gap` と `L'` は 2 つの周辺だけで決まる (100 チャネルで機械確認、最大差 `1.284e-15`)。⚠ **P1 と P2 の死は独立** — (H2) より `P1 ⟹ P2` だが、P1 を殺す witness の多くは `gap ≤ D(p)` を保ち、P2 は `D(p) → 0` と `gap → 0` が同率で起きる knife-edge な `p` で別に死ぬ。⚠ **旧起票文の運用注意 ((N1)(N2) の両方に当ててから probe せよ) は必要だが十分でなかった** — 3 候補のうち 2 本は両方を通したうえで死んだ (§5-12 の追加根拠)。逐語と再検証 → [`bc-facts.md`](bc-facts.md) §L13 (probe は全体 `131.4s` / `--quick` `25.2s`。**数値は全体実行のもので `--quick` とは食い違う**) | **副産物のほうが大きい (本 leg の真の収穫)**。**(H1) 厳密なスラック恒等式** (記号的な係数相殺で**証明済**、Markov 連鎖を使わない 5 変数の線形情報量恒等式): `gap(T,p) = min{H(X\|Y),H(X\|Z)} − L'(T,p)`、`L'(T,p) := min_{p(u,v\|x)} [I(U;X\|Y) + I(V;X\|Z) + H(X\|U,V) + I(U;V\|X)]` は**非負 4 項の最小** ⟹ **「鋭いチャネル依存スラックを作る」は「`L'` を下から抑える」と厳密に同じ問題**で、必要条件も **(N1) ⟺ `L' = min{H(X\|Y),H(X\|Z)}` at `\|X\|=2` / (N2) ⟺ 格子 BC で `L' = 0`** へ翻訳される + **最適点での 4 項分解**という新しい診断 + **(H2) 比較補題** (決定論的組ごとに `obj(f,g) − max{I,I} ≤ H(f,g) − max{H(f),H(g)}`、DPI 2 回 + 場合分けで証明) ⟹ `Φ_det − max{I,I} ≤ D(p)` = **P1 ⟹ P2** かつ margin が順序づく + 子 attack `slack-loss-lower-bound` + probe [`bc-jognair-phi-check.py`](bc-jognair-phi-check.py) (全体 `131.4s` / `--quick` `25.2s`、全体実行 2 回が経過時間行を除きバイト一致) |
| `slack-loss-lower-bound` | C | **`route`** (R3) | **L13 起票 / L15 決着** | **`jognair-channel-dependent-slack` の死因から起票** (§5-9)。主語 = **非負 4 項の損失 `L'(T,p) := min_{p(u,v\|x)} [I(U;X\|Y) + I(V;X\|Z) + H(X\|U,V) + I(U;V\|X)]` を下から抑える**。(H1) の恒等式 `gap = min{H(X\|Y),H(X\|Z)} − L'` により、これは C2 が狙っていた「鋭いチャネル依存スラック」と**厳密に同じ問題**で、必要条件も翻訳済 — **(N1)** `\|X\|=2` で `L' = min{H(X\|Y),H(X\|Z)}` (= Theorem 1 の言い換え) / **(N2)** 格子 BC で `L' = 0` (`U=Y, V=Z` で 4 項が項別に 0)。**最初の道具 = 最適点での 4 項分解** (順は `I(U;X\|Y) / I(V;X\|Z) / H(X\|U,V) / I(U;V\|X)`) — Blackwell は両 `p` とも 4 項すべてちょうど 0、平滑化した dense Blackwell (`ε=0.1`) は `0.244929 / 0.270619 / 0.000003 / 0.000004`、BSC(0.1) 2 本は `0.000004 / 0.468996 / 0 / 0` で和がちょうど `min{H(X\|Y),H(X\|Z)}` ⟹ **どの項が損失を担うかがチャネルで切り替わる**ので、下界の形はそこから当てる。**⟹ 探索空間の絞り込み (probe 側の構造的読み)**: dense チャネルでは後ろ 2 項が `1e-6` スケールまで潰れ **`L'` はほぼ DPI 2 項で尽きる** ⟹ **次の候補は `(U,V)` を「`X` を復元し、かつ `X` を与えたとき条件付き独立」なものに絞ってよく、抑える対象が 4 項から 2 項に減る**。⚠ **着手手順 (L13 の運用教訓、親 plan §5-12)**: 次の候補は**構築の前に、記録済の必要条件 2 本に加えて 3 本目 = 「非決定論的チャネルでの局所最適性」に当てる** — **死んだ 2 案はちょうどこの検査で落ちる** (P1 は決定論的点が局所最大ですらなかった)。あわせて **generic な非退化インスタンス 1 本** (dense・非決定論的・`\|X\| ≥ 3`・非一様 `p`) に当てること。(N1)(N2) の witness はどちらも退化点 (`\|X\|=2` / 決定論的 BC) にあり、そこは P1 の kill 機構がちょうど相殺する場所なので、**必要条件を全部通っても generic に死にうる**。⚠ **最適化器の非違反行は弱い証拠** — L13 の 1 回の run だけで偽陰性が 3 件出ている (facts §L13 の専用行) ⟹ **決着は明示 witness 族の直接評価で付け、統計は「率」ではなく「探索強度に依存する下界」と書く**。⚠ **開いている安価な確認 1 件** — dense チャネルの最適点の台が積集合かどうか (肯定なら最大化子は `2×2` / `2×3` 格子上に住み [GLN25] Proposition 1 の rectangular mapping と一致する = 本 attack の構造的入力。facts §L13 の `open check` 行) | — | **⟹ L15 の決着 (本行の主語は解けた)**: 下界は **`Λ_split`** = **(H4) grouping 超加法性** `L'(p) >= sum_w p(w) L'(p_w)` (`W = m(X)` は `X` の任意の関数) を再帰的に使い、`\|X\| <= 2` を [GJNW13] Theorem 1 で閉じたもの。**(H4) は係数相殺で*証明済*** (4 項が項別に分解、`bc-facts.md` §L15) ⟹ 本行は `killed` でも `todo` でもなく **経路 R3 に昇格** ([`bc-open-problem-routes.md`](bc-open-problem-routes.md) §R3、5 条件すべて充足)。⚠ **起票時の 3 つの警告に対する実際の結末**: (i) 第 3 の必要条件 (非決定論的点での局所最適性) は、候補を**証明済の下界**にしたので**自動で満たされた** (証明済の不等式は摂動で破れない) ⟹ L13 の死因は本候補に効かない。(ii) 4 項分解から読んだ「dense では DPI 2 項が支配」は正しかったが、**効いたのは別の道具** — (H3) が超過分を `D1 = I(X;Z\|U) - I(V;Z\|U)` と `D2 = I(U;V\|Z)` に分け、実測で**担うのは常に `D2`** と分かった。(iii) 開いていた安価な確認 (最適点の台が積集合か) は**本 leg では不要だった** — `Λ_split` は台の構造を使わない。⚠ **周辺だけの緩和 3 本 (`Λ_sup` / `Λ_ib` / max) は (N1) 不合格が証明書つきで確定** (`bc-facts.md` §L15) ⟹ 「(N1) を満たす `Λ` は Theorem 1 を内蔵する」が本 leg の一般化。
| `dominated-channel-envelope` | C | **`route`** (R4) | **L16 起票 / L16 決着** | **`slack-loss-lower-bound` (R3) の step 7 (`Λ_sup`) の緩さから起票** (§5-9)。主語 = **`K(U) := max_{p(v\|u,x)}[I(V;Z) − I(U;V)]` の証明できる上界を `I(X;Z\|U)` より鋭くする** (主張 A `L' = min_{p(u\|x)}[H(X) − I(U;Y) − K(U)]` により、これは 4 項損失 `L'` を下から抑える問題と等価)。**⟹ 決着 = 経路 R4 に昇格** ([`bc-open-problem-routes.md`](bc-open-problem-routes.md) §R4、5 条件すべて充足)。**(H5) を記号的な連鎖で*証明*した**: `X` からのチャネル `W` が `Z` に **more capable で支配される** (supp(p_X) 上の任意の `p'` で `I_{p'}(X;W) ≤ I_{p'}(X;Z)`) なら `K(U) ≤ I(X;Z) − I(U;W)`。`W = Z` で既存の `I(X;Z\|U)` にちょうど一致するので**1 パラメータ族への一般化**であり、族上で最大化した `Λ_env` は**構成から `Λ_env ≥ Λ_sup`**。⚠ **`Λ_comb` は `max{Λ_split, Λ_sup}` → `max{Λ_split, Λ_env}` へ更新される** (R3 と R4 の産物は同じ `Λ_comb` に合流する = 独立な 2 本ではなく**相補的な 2 本**)。**⚠ 死因ではないが射程を切る 3 点 (次 leg が過大に読まないため)**: **(i) 新規性は (H5) に局限** — novelty gate を 2 報 (一次文献 11 本) で実施し、(H5) 自体は**保留 (PASS 寄り)** (最近傍の arXiv:1105.5438 Lemma 7 は罰項も第 1 項も*支配する側*に揃えるので古典上界より**緩くなる**方向 ⟹ (H5) と逆で含意も示唆もしない) だが、**`Λ_env` という*対象*は FAIL 寄り保留** ([GNA12] arXiv:1202.0898 が逐語 `Beyond binary input alphabets: The inequality (1) itself fails to hold where \|X\| = 3` と書いて一般アルファベット化を**既知の未解決方向として明示**している ⟹ 未報告のパラメトリゼーションであって新しい問いではない)、**構造的な読みは FAIL (既知)** (利得の恒等式 = van Dijk 1997 の証明そのもの / 「利得は凹性の欠損」= Nair 凹包手稿 / 「2 順序の隙間がクラスの分かれ目」= Gohari–Liu–Nair arXiv:2606.12839)。**(ii) 鋭さは限定的** — 改善 `Λ_env − Λ_sup` は中央 `+0.000002` / 最良 `+0.009576` だが **dense の残距離は動かない** (`gap > 1e-3` の 5 本で `slack/gap` 中央 `2.544` → `2.544`)。縮むのは **L15 が (N1) 証明書に使った `BSC(.1)/BEC(.5)` 族** (`p=0.1` で `+0.004539 → +0.000299`、`p=0.2` で `+0.013538 → +0.008948`、どちらも 2 値 `U` の格子 = 最適化不使用) ⟹ **改善は「more capable だが less noisy でない」順序の隙間に局在する** (利得の恒等式 `I(U;W) − I(U;Z) = Σ_u p(u)Δ_W(p_u) − Δ_W(p)` が予言する場所とちょうど一致)。**(iii) 族の admissibility は数値検証** — 文献既知は `Z = BEC(ε)` / `W = BSC(δ)` の `ε ≤ h(δ)` (El Gamal–Kim Ch.5) だけで、一般の `W` は単体格子 + 頂点 KL の検査 (採用した `W` の支配余裕の最小値 `+0.00e+00`) ⟹ **R4 step 5 に `gap` ラベルが残る**。⚠ **(N1) は不合格のまま** (最大 deficit `+0.023313 → +0.023313` で動かない) — ⚠ **想定どおりであって死因ではない** ((N1) を満たす*証明済*の `Λ` は [GJNW13] Theorem 1 を含意する、[`bc-facts.md`](bc-facts.md) §L15)。**(N2) は合格** (格子 BC 3 本で `Λ_env` の最大 `8.81e-06` = 最適化ノイズ)。⚠ **推定不足 13 行** (すべて `1e-6` 台、反例ではない。§5-13 の規約どおり分けて数え全行印字)。逐語と再検証 → [`bc-facts.md`](bc-facts.md) §L16 (probe は [`bc-d2-lower-check.py`](bc-d2-lower-check.py)、full `92.6s` / `--quick` 約 `16s`。**数値は全体実行のもので `--quick` とは食い違う**) | **副産物 = 支配チャネル `W` の設計指針 (次 leg への入力)**。**消去族は機構つきで死ぬ**: `W =` 確率 `γ` で `X` を明かす消去チャネルは `I_{p'}(X;W) = γ H(p')` だが `H` の頂点での傾きが無限大 (`D(R_i\|\|R_j) = ∞`) なのに `I_{p'}(X;Z)` の頂点傾きは `D(Z_i\|\|Z_j) < ∞` ⟹ **どんな `γ > 0` でも頂点近傍で支配が破れる** (`γ* := inf_{p'} I_{p'}(X;Z)/H(p') = 0`、実測比 `0.1191 0.0763 0.0545 0.0423 0.0345`)。**分割 `m_S(X)` へ落としても同じ機構で死ぬ** (`H(m_S)` の傾きも頂点で無限大) ⟹ **`W` は「出力を粗くする」型では作れず、`Z` と非可比な方向へずらす必要がある** (degraded な `W` では利得が恒等的に 0 という van Dijk 由来の結論と、別経路で一致する) |
| `multiletter-subject` | D | `todo` | — | multi-letter 表現の Lean 構成 + **Li の Table I のレベル (`Σ₁ᴴ` / `Π₂ᴴ` / `Δ₁₈ᴴ`) と我々の在庫の等号定理の対応付け** (L2)。⚠ 旧「単一文字特徴付けを持つ、の定義候補」は F7 で肯定決着済ゆえ主語を差し替え | — |
| `bc-computability-openness` | D | `todo` | — | **F8 の NOT-FOUND から起票**。「BC 容量領域の計算可能性は未決」と明示する一次文献が見つかっていない。否定的解決を標的にする前に出典を立てるか、**無いことを確定事実にする** (⚠ Fawzi–Fermé arXiv:2310.05515 は一発符号化の近似困難性で別物) | — |
| `markovity-conjecture` | G | **`parked`** (前提工事待ち) | **L5** | Gohari–Liu–Nair (ISIT 2025) **Conjecture 2 (Markovity Conjecture)** = 「Marton 領域の極点評価では `U → (W,X) → V` を持つ組だけで十分」。**真に未解決、根拠は数値証拠のみ** (`\|X\|=3,4,5` で各 10,000 チャネル超)。⚠ 「局所最適性条件だけで証明する路線は論文自身の反例が潰している」は **L1 で弱まった** (下記 `markovity-localopt-route`)。**L5 の第一手 = 「述べられるが偽」**: 第 1 形 ((U,V,W,X) 版) は共通補助が無いので在庫に主語が無く、第 2 形 (F 側) は `V₁ ⊥ V₂ ∣ X` として我々の support function へ移せる (命題 **M2**) が、**M2 は数値反例で FALSE** (`+7.147687e-04`、hardening 3 段 + 独立 4 系統 + 肯定コントロール 3 本)。⚠ **Conjecture 2 の反例ではない** — 論文の `F` に残る `−αH(Y) − (λ−α)H(Z) + Σ p(x)a_x` は線形項では相殺できないので M2 は部分族ではなく**移植**。逐語 → [`bc-facts.md`](bc-facts.md) §数値 probe の判定 (L5)。**park の理由はゼロ進捗ではなく前提工事**: 2 補助変数の在庫の上では行き止まりで、**軸 E に L2 が課したのと同一の `marton-3aux-probe` が挟まる**。**再開条件 (L9 で訂正) = `marton-3aux-probe` の 1 本のみ**。旧文言は `support-vs-dual-additivity` の完了でも再開可としており、L8 でそれが `harvested` になったので**形式上は満たされている**。だが満たし方は「工事が済んだ」ではなく**「経路が既知で死んだ」**であり、**probe 側に 3 補助変数版が無い状態は何も変わっていない** (L8 が効いたのは再開条件ではなく**工事の見積り**の側) ⟹ 後者を条件から外す。⚠ **一般化して持つ**: 再開条件は「どの attack が終わったか」ではなく**「何が使えるようになったか」**で書く (前者は死亡でも満たされてしまう。親 plan 判断ログ 22)。**⚠ その工事自体が条件つき枠**: `marton-3aux-probe` は既定では発動せず、**L14 で生存経路が 1 本以下のときだけ L15–L17 に一括発動する** (工事 1 + 構築/probe 2 = 3 leg、親 plan 判断ログ 16 / §6.2)。**発動しなければ本 attack は park のまま relay を終える** | probe [`bc-markovity-conjecture-check.py`](bc-markovity-conjecture-check.py) (反例インスタンスを逐語ハードコード) + 子 attack `markovity-via-dual-F` + 下記 `markovity-localopt-route` への入力 |
| `markovity-local-max-repro` | G | `harvested` | **L1** | **完了、ただし想定外の結果**。目的値 4 つは**8 桁全桁再現** (harness は既知の答えに対して検証された)。一方、論文が §III-B で逐語 "satisfies (4), (7) and (8)" と書く 3 条件のうち**成立するのは (4) だけで、(7)/(8) は `+1.08e-03` / `+2.79e-03` 違反する** — 改善の witness は明示 (種類 2/3 = 補助アルファベットを 1 増やす摂動、改善方向は決定論的境界)。**実装に関与していない独立エージェントの敵対的監査 (自前再実装・コード非共有) が CONFIRMED**、加えて**肯定コントロール** (同一インスタンスの大域最大点では論文の条件が全て成立) で「コードの性質ではない」を分離した。⚠ **射程の限定 (監査が追加)**: 論文が §I で引く濃度限界 `\|U\|+\|V\| ≤ \|X\|+1 = 4` を課した空間の内側では報告点は**真の局所最大** (改善摂動は `\|U\|+\|V\|=5` へ出る)。**論文への影響範囲**: Conjecture 1 / Conjecture 2 / 目的値 4 つ / §IV Theorem 2 はいずれも**無傷**。逐語と再検証コマンド → [`bc-facts.md`](bc-facts.md) §数値 probe の判定 (L1) の (b) 行 | 検証済 harness + 子 attack 2 本 (`markovity-localopt-route` / `cardinality-localmax-boundary`) |
| `markovity-localopt-route` | G | `todo` | — | **`markovity-local-max-repro` の結果から起票** (親 plan §5-9)。**確立している事実**: §III-B の報告点は、論文が §III で自ら定義する 3 種の摂動の意味では (7)/(8) を満たさない (独立監査 + 肯定コントロール、上行)。**ここから先は推論であって確立していない**: ⟹ §II-B が「局所最適性条件による証明路線の障害」として提示したインスタンスは、**主張されたほどには路線を塞いでいない**かもしれない。⚠ **自動的には従わない** — (i) 論文の記述は逐語 "we failed to do this" であって「この点が唯一の障害だ」とは書いていない、(ii) 濃度限界の内側では報告点は真の局所最大 (下記 `cardinality-localmax-boundary`) ⟹ **路線が開きうるのは濃度非制約の空間に限る**。**最初の一手 = 診断であって証明ではない**: (7)–(10) が非矩形パターン `A B / C A` を排除するのはどの濃度領域か、を harness で走査する。証明に投資するのは診断が生き残ってから (§5-2 kill-first)。**L5 が入力を 1 本足した**: 我々の 2 補助変数の support function の最適点 90 点で **決定論性 90/90 / rectangular 89/90 / Markov 89/90**、しかも**破れる 1 点がそのまま M2 の反例で、その写像は §III-B の非矩形パターンと同型** ⟹ 「ギャップが立つ ⟺ 最適点が非 Markov ⟺ 非 rectangular」が完全一致した。**診断の主語をここへ寄せられる** — 濃度領域の走査に加えて「非矩形が残るチャネルはどれだけ稀か」を定量できる (L5 の走査では 3/10、`λ` の窓は幅 0.05 程度) | 上記の統計 (facts §L5 の 3 行目) |
| `markovity-via-dual-F` | G+E | `todo` | L7 (間接) | **`markovity-conjecture` の L5 の park から起票** (§5-9)。M2 が偽だったのは共通補助 `W` を落としたためなので、**Conjecture 2 は領域側 `h` ではなく双対側 `F` の言葉で扱う**。⚠ **`F` の逐語実装は L1 の [`bc-markovity-localmax-check.py`](bc-markovity-localmax-check.py) に既にある** (§III-B の目的値 4 つを 8 桁再現済) ⟹ 工事の実体は「我々の領域 `def` と `F` を繋ぐ段」であって `F` の実装ではない。**`support-vs-dual-additivity` と大きく重なる** — 先にそちらを済ませるのが安い。⟹ **軸 E と軸 G が 1 つの前提工事を共有する**ことが L5 の最大の産物 (親 plan 判断ログ 16)。**⚠ L7 が繋ぎの段の中身を 2 点で更新した**: (i) 旧記述の「`{a_x}` ↔ `θ` の Legendre」は誤りで、正しくは `min_α max_p min_a` の 3 段入れ子 (`support-vs-dual-additivity` 行の訂正)、(ii) **R1 の step 3 が `W` の効きどころを特定した** — `W` は `Δ(X)` 上の上凹包を実現する変数で、`α` は総和レート制約 2 本への乗数配分 (L5 が「我々の再構成であって逐語ではない」と留保した `min{I(W;Y), I(W;Z)}` の凸結合の重み、という読みがここから導出される) ⟹ **Conjecture 2 を双対側で扱う受け皿がここに出来た** (routes.md (d))。**⚠ L8 が工事の実体を変えた**: `F` ↔ Marton 領域の対応を [GLN25] 式 (1) (等号) + [AGN19] の convex duality で逐語確定させたので、**「我々の領域 `def` と `F` を繋ぐ段」は文献に存在する = 自作不要**である ([`bc-facts.md`](bc-facts.md) §L8 (ii)/(iv))。⟹ 残る工事は「文献の段を我々の `def` の上で使える形に写す」ことであって導出の発明ではない | — |
| `cardinality-localmax-boundary` | F | `todo` | — | **同じく `markovity-local-max-repro` から起票、ただし軸が違う**。濃度限界 `\|U\|+\|V\| ≤ \|X\|+1` を課すか否かが、**同一の点について「局所最大である / でない」を反転させる** (改善摂動はちょうど限界の外へ出る)。`F(T,{a_x})` の定義自体には濃度制約が無い (逐語 "where the maximum is taken over all p(u, v, x) defined on U × V × X") ので、**この 2 つは同じ最適化問題ではない**。最初の一手 = Marton 領域まわりの主張を「濃度制約の内側でしか成り立たないもの / 外でも成り立つもの」に仕分ける。⚠ 軸 F の旧コスト論拠 (Fenchel–Eggleston が Mathlib 不在) は**層 3 の話**で、本 relay (層 1–2) には効かない | — |
| `sum-bc-classes` | A | `todo` | — | arXiv:2606.12839 (Gohari–Liu–Nair, 2026-06-11) が degraded / less noisy / more capable / deterministic / semi-deterministic **成分の和 (sum) BC** で auxiliary-receiver 外界 = Marton を示した。我々は成分クラス 3 本の等号定理を Lean で持つ ⟹ 軸 A の最弱仮説抽出がそのまま乗る。⚠ **積ではなく和** — 軸 E のテンソル冪とは別演算 | — |
| `product-bc-tensor` | E | `todo` | — | 積 BC の構成と非加法性の例 | — |
| `cardinality-bound` | F | `todo` | — | 旧主語「cardinality bound の改善」は Fenchel–Eggleston が Mathlib 不在ゆえ高コスト = 予備枠だった。**L1 で軸 F の主語が「濃度限界がどの主張の生死を分けるか」へ変わった** (親 plan §4 軸 F、優先度 4 → 3) ⟹ 本行は改善路線として保持し、当面の実働は `cardinality-localmax-boundary` | — |

## 候補経路 (本 relay の主成果物)

親 plan §3.1 の 5 条件を**すべて**満たしたものだけをここに載せる。満たさないものは下の `idea` 枠。
確信度ラベル (`probed` / `unprobed` / `refuted`) は §3.2。中身の散文は `bc-open-problem-routes.md`。

| route | 軸 | 主張 (1 文) | 5 条件 | 確信度 | 最終更新 leg |
|---|---|---|---|---|---|
| **R2** | C | `\|X\| ≥ 3` の鋭い加法スラック `f*(k) = T(X) − max{I(X;Y),I(X;Z)}` の上限は**決定論的な格子 BC** で達成される (`f*(k) = D(k)`)。`D(2)=0` ゆえ `\|X\|=2` では [GJNW13] Theorem 1 に退化し、`D(3)=log₂φ`、完全平方で `D(m²)=log₂m` | (1)(2)(3)(4) 充足 / (5) は `(secondary)` | **`probed`** (⚠ **核 step 9 は `gap`**) | **L10** |

⚠ **R2 の `probed` は「証明できた」ではない** — step 1–4 は係数相殺で証明済、step 5–8 は明示 witness の
直接評価だが、**核の step 9 (決定論的極値性) は掃引が反証を出せなかっただけ**で、同じ leg 内に同じ掃引が
真の最大を `0.3058` bit 外した実例がある (`\|X\|=4`)。⚠ (5) が `(secondary)` なのは「加法スラックの枝が
空いている」が一次文献 1 本の通読 + WebSearch 1 回の NOT-FOUND に留まるため。

⚠ **R1 は本表に載らない**。L7 が **R1** を
[`bc-open-problem-routes.md`](bc-open-problem-routes.md) に起票し、**L8 が (4)(5) を通した結果
(4) は通ったが (5) novelty gate で不合格**になった (12 step すべてに逐語出典、死因 `known-result`)。
本表の規約 (「5 条件を**すべて**満たしたものだけ」) により載せない。⚠ **`refuted` ではない** —
R1 は数学的に無傷で、routes.md に**軸 E の地図**として残る (`support-vs-dual-additivity` 行)。

**到達目標** (L14 の収穫判定): 最低ライン = 経路 ≥ 2 本 / うち `probed` ≥ 1 本。目標 = ≥ 3 本 / ≥ 2 本。

## idea 枠 (経路の 5 条件を満たす前のもの)

量産してよい。制約をかけるのは経路への昇格時だけ。

| idea | 軸 | 中身 | 起票 leg |
|---|---|---|---|
| `residual-as-slack-diagnostic` | B | 外部ノートの残差恒等式 (5) は「一文字化がどこで壊れるか」を明示する。これを我々の `bc_capacity_subset_uv` の緩みの局所化に転用できないか。**L1 で前提が強くなった** — 残差は散文の見込みではなく**閉じた式** `I(A;B) − I(Y₁;Z₂∣A,B)` として確定した (記号的に証明、`probe-harness` 行) ので、「どの項が緩みか」を式のレベルで名指せる | L(-1) |
| `absorption-as-class-def` | A+E | 外部ノートの吸収条件 (9) `Δ₁₂ ≤ N₁₂ + G₁₂` を**満たすチャネルのクラス**として定義すると、既知クラス (degraded / less noisy / more capable) を含む新クラスになるか。軸 A の最弱仮説抽出と合流しうる | L(-1) |
| `collider-conditioning` | C | 残差の正体が collider conditioning (独立な `A,B` が共通の子 `Y₁` を条件付けて従属化) なら、その現象を直接測る情報量が新しい情報不等式の候補になる | L(-1) |
| `markovity-as-class-def` | G+A | Markovity 予想が**成り立つチャネルのクラス**を定義すると、我々が等号を持つ 3 クラス (degraded / less noisy / more capable) を含むか。含むなら軸 A の最弱仮説抽出と合流し、含まないならクラスの境界が新しい標的になる | L0 |
| `conditioning-collapse-schema` | A | **L3 の死因からの派生**。条件つき版と非条件版が同値になったのは more capable 固有の事情ではなく、`∀ p, F(p) ≤ G(p)` という**周辺分布で量化されたチャネル述語**一般に効く平均化の帰結かもしれない。そうならば degraded / less noisy でも同じ形で潰れ、`weakest-hyp-degraded-lessnoisy` は着手前に結末が読める。先にやるのは schema の書き下し — どの汎関数の組 `(F,G)` について「全周辺分布での不等式」と「全 `(pU,K)` での条件つき不等式」が同値になるか。**昇格条件**: schema に**境界**を付けること (同値にならない `(F,G)` の実例を 1 つ出す)。出れば `class-def-vs-consequence-gap` に主張 1 文と step 列が入り、軸 A の第一手が生きる場所が特定される。**⟹ L12 で決着 (昇格条件は満たされたが、行き先が既知だった)**: schema は「`∀p ∈ S` で `S` が分解成分について閉じているなら必ず同値」で、**境界は `(F,G)` の側ではなく `S` の側**にある (量化範囲を狭めると破れる)。境界の実例は [N10] Claim 2 (vi) の 4 文字チャネル (more capable ⊊ essentially more capable) で、**そこで得られるクラスは 2010 年に容量領域まで確定済** ⟹ 昇格先の `class-def-vs-consequence-gap` は `killed(known-result)`。逐語 → [`bc-facts.md`](bc-facts.md) §L12 (A1)(A2)(A3) | L4 → **L12 決着** |
| `region-convexity-transplant` | E+D | **類似問題からの移植**。領域の凸性を時分割で示す機構が**別家系に 2 系統ある** — MAC は `macConcatCode` (`MultipleAccess/TimeSharing.lean:163`) → `mac_timesharing_strict` (:369) → `mac_capacityRegion_convex` (:565)、WynerZiv は補助変数上の値集合について `wzRateValueSet_timeShare_mem` (`WynerZiv/FactorizableRate.lean:948`) / `mutualInfoPmf_mixture_affine` (:783) / `wzRateValueSet_avg_mem` (:1230) / `wynerZivRate_convex_in_D` (:1122)。一方 **BC 側には `Convex ℝ (bcCapacityRegion W)` も union の凸性も無い** (`rg 'Convex ℝ' InformationTheory/Shannon/BroadcastChannel/` のヒットは四辺形版 `martonRegion_convex` の 1 本のみ)。L2 の V5 が還元 (3) を**閉凸包の命題**と確定させた以上、「support function が領域を決める」という言明自体が両端の凸性に依存する。**昇格条件**: 時分割変数を補助アルファベットへ吸収する標準手が我々の `def` の上で step 列として書けること。書ければ (3) の主張が「閉凸包」から「領域そのもの」へ降り、V5 の測定の意味が 1 段強くなる | L4 |
| `entropic-vector-as-hierarchy-atom` | D | **同じく移植**。Li の `Δ^H_atom` は逐語 `aᵀh(X₁,…,X_n) ≥ 0` で `h` は entropic vector ([`bc-facts.md`](bc-facts.md) F7)。その entropic vector が**在庫にそのまま在る** — `jointEntropySubset` (`Shannon/Han/D.lean:36` = `S ↦ H(X_S)`) と `entropyPolymatroid` (`Shannon/Polymatroid.lean:281`、`@[entry_point]`、axiom 3 本 `jointEntropySubset_empty` / `_mono` / `_submodular` (:47 / :76 / :202) を `Combinatorics.Polymatroid` に束ねたもの)。⟹ `multiletter-subject` が要求する「Li の階層のレベルを我々の型で書く」は、少なくとも `Δ₀ᴴ` 層については**在庫の消費だけで書ける**。**昇格条件**: `Δ^H_atom` / `Δ₀ᴴ` を `entropyPolymatroid` 上の述語として 1 文で書き、Table I の `Σ₁ᴴ` (degraded BC) が我々の `bc_degraded_capacity_eq_uv` の形と照合できること | L4 |
| `uv-slack-step-enumeration` | B | **既存証明の読み直しから**。`dep_rank.sh` の実測で BC family の重い entry point は `bc_degraded_capacity_eq_uv` 2425 / `bc_moreCapable_capacity_eq_uv` 2402 / `bc_lessNoisy_capacity_eq_uv` 2373 / `bc_capacity_subset_uv` 1687 / `martonRegionUnion_subset_capacity` 1661 (推移依存数)。軸 B が列挙したい対象 = `bc_capacity_subset_uv` の前方閉包で、実測は**内部 266 decl / 外部 1412**、うち名前が不等号形のものは 21 本 (⚠ **L6 で 39 本へ訂正** — 名前フィルタが 18 本取りこぼしていた。結論形で数え直した手順と落とし穴 2 件は [`bc-facts.md`](bc-facts.md) §外界の緩みの棚卸し (L6))。緩みを入れる本体は `mutualInfo_le_of_markov` (DPI) / `mutualInfo_le_of_postprocess` / `condMutualInfo_le_of_markov_joint` / `condMutualInfo_le_add_condMutualInfo` / `mutualInfo_le_condMutualInfo_of_indep_markov` / `bcUVTimeShare_uvInfo₁_ge` / `bcUVTimeShare_uvInfoSum₁_ge` / `bcConverseFanoSlack₁_le` / `klDiv_map_le`。⟹ `outer-slack-vs-thm7` の「不等号ステップを全列挙」は散文の読解ではなく **`dep_graph.sh` + 名前フィルタで機械的に得られ、規模は 21 本**。**昇格条件**: 21 本それぞれに等号成立条件を書き、Gohari–Nair Theorem 7 の「捨てられる項」と対応が付くか / 付かないものが残るかを判定できること | L4 |
| `shannon-cone-lp` | C | **数値実験のパターンの空き口から**。`bc_probe.py` は情報量式を**同時エントロピー `h(S)` 上の有理係数ベクトル**として保持する (`LinInfo.terms`) が、その表現を使っているのは恒等式の係数相殺 `prove_identity` / `is_zero` **だけ**。不等式側は掃引 `test_relation` (証拠) しかなく、**証明側の口が空いている** — 同じベクトルに elemental Shannon 不等式を制約とした LP を当てれば「その不等式は Shannon 型か」を機械判定できる (ITIP 相当。`scipy.optimize.linprog` は既に依存)。軸 C にとっての意味は大きい: F4 の Jog–Nair 不等式は Shannon 型でないからこそ定理なので、**LP が通る候補はその場で既知として殺せる = §5-7 novelty gate の機械化**。**昇格条件**: LP が Jog–Nair 不等式に「Shannon 型でない」を返し (肯定コントロール = Shannon 型不等式が通ること)、その上で生き残る新候補が 1 本でも出ること。**⚠ L10 — 昇格条件の前半が原典に否定された。ただし idea は殺さず射程を狭めて残す**: [GJNW13] §I-B-2 逐語 "this is an example of an inequality that cannot be deduced by even the knowledge of `Γ*₅`. This is primarily due to the **cardinality constraint** imposed on one of the random variables" (facts C4、原典 PDF で機械確認済) ⟹ 「LP が Jog–Nair に『Shannon 型でない』を返す」は**真だが空虚に真**で、`\|X\|` 制約を持つ候補は**全部生き残る** = **gate の kill 枝が構造的に沈黙する**。⚠ **逆向きも成り立たない** — 「Shannon 型でない」は「新しい」を意味しない (この不等式自体が「Shannon 型でない、かつ 2010 年から既知」の実例)。⟹ **`killed` にはしない** — **カーディナリティ制約を持たない候補に対しては依然 sound な killer** なので、その射程限定を明記したうえで idea 枠に残す (軸 C の本命クラスには効かない、というだけ) | L4 |
| `product-outer-claim4` | E+B | **文献の未使用の道具から**。F5 / V10 の逐語 "In Section III we establish a new outer bound (Claim 4) for product broadcast channels and use it to determine the capacity region of some new classes (Theorems 2 and 3). This outer bound is strictly better than the UV outer bound as it is optimal for the example where the UV outer bound is loose." — **Claim 4 はどの attack にも使われていない**。我々は `bc_capacity_subset_uv` を `T^{⊗n}` に当てて `h_n` の上界を引けるが、それは UV 由来で緩いことが F5 で判明済。Claim 4 は**積 BC 専用でかつ UV より真に強い**外界なので、軸 E の自己テンソル化 (`h_n` の評価) と軸 B の「UV はどこで緩むか」の**両方に同時に効く唯一の未使用資産**。**昇格条件**: Claim 4 を逐語取得し、(i) 自己テンソル `T ⊗ T` に特化した形が書けるか、(ii) それが `h_2` に UV より真に良い上界を与えるか。与えるなら `selftensor-counterexample` のブロッキング前提 (`marton-3aux-probe`) を迂回する別ルートが立つ | L4 |

## 軸ごとの連続ゼロ進捗カウント (round-robin 判定用)

| 軸 | 連続ゼロ進捗 leg 数 | 判定 |
|---|---|---|
| A | 0 | L3 で第一手が `probe-failed`、**L12 で子 2 本とも `killed(known-result)`**。⚠ **ゼロ進捗ではない** — 死因の特定は確定事実 (親 plan §5-1) で、しかも L12 は軸外の収穫 (F8 の出典) と子 attack 2 本 (`eln-canonical-class` / `superposition-optimality-scope`) を残した。**ただし軸 A は「クラスを広げて等号定理を延ばす」という当初の主語では閉じた** — 残った 2 本はどちらも主語が違う (前者は membership の判定可能性、後者は superposition 最適性の逆向き) |
| B | 0 | L0 は軸に紐づかない文献 leg。**死亡も確定事実**なのでゼロ進捗ではない (親 plan §5-1) |
| C | 0 | **L9 まで 9 leg 一度も leg の主語にならず、枯渇カウントが最大だったので L10 を割り当てた** (親 plan §6.6)。**L10 で解消** — 経路 R2 + facts C1–C8 + 子 attack 2 本 ⟹ 「回ってきていない」状態は終わり、カウントは進捗ゼロではない 0。**L13 も軸 C** — 経路は増えなかったが候補 3 本の kill + 副産物 2 本 ((H1)(H2)) + 子 attack 1 本で確定事実は増えているので**ゼロ進捗ではない** (親 plan §5-1) |
| D | 0 | L2 で multi-letter 表現が我々の定義で**逆側だけ生きる**と確定 (`multiletter-subject` の入力)。⚠ ただし L2 は軸 E の leg なので**本軸のカウントはリセットしない** (下記の二重計上の注意) |
| E | 0 | L2 で V4 / V5 / 新項目が決着。⚠ ただし**決着の中身は軸 E 内の路線 1 本 (`selftensor-counterexample`) を条件付きで塞ぐもの** — 前提工事 `marton-3aux-probe` を挟むまで動かせない。**L7 で経路 R1 + `u0-necessity-quantified` の決着** ⟹ 引き続き 0 |
| F | 0 | L1 で主語が変わったが、**leg は消費していない** (下記の二重計上の注意) |
| G | 0 | L0 で新設 (親 plan §4 軸 G)。L1 (b) で確定事実 |

3 に達した軸は `parked` にして別軸へ移る (親 plan §5-5)。**L0 / L1 はどちらも軸に紐づかない固定枠
(文献 / 基盤) なので、その leg で触れなかった軸のカウントは進めない**。

⚠ **二重計上の注意**: L1 の (b) は軸 G の leg で得た事実だが、副産物として軸 F の主語を変えた。
**進捗として数えるのは軸 G の側だけ**にする — 同じ 1 事実で 2 つの軸のカウントをリセットすると、
round-robin (§5-5) が「回っているように見えて回っていない」状態を検出できなくなる。
**L2 も同じ扱い**: multi-letter 表現の確定は軸 D の入力になるが、L2 は軸 E の leg なので
**数えるのは軸 E の側だけ**にする。**L3 も同じ**: 棚卸し手順の訂正 (`dep_graph.sh` + 辺の向き先の区別) は
他軸の最弱仮説抽出にも効くが、L3 は軸 A の leg なので**他軸のカウントは動かさない**。
**L7 も同じ**: 経路 R1 の軸ラベルは `E+G` (routes.md の (d) が `markovity-via-dual-F` の park に方向を与える) で、
同居 probe `u0-necessity-quantified` は `A+E` だが、**L7 の主語は軸 E** なので**軸 G / 軸 A のカウントは動かさない**。
**L10 も同じ**: facts C6 / C7 は L7 (軸 E) の Blackwell witness に逐語出典を与え L2 (軸 E) の副産物の射程を
`\|X\|=2` について文献側から閉じるが、**L10 の主語は軸 C** なので**軸 E のカウントは動かさない**。

### L4 の点検 — 強制 park 対象は無い。ただしカウンタは構造的に発火しえない

**park 対象**: **無い** (全軸 0、3 に達した軸はゼロ本)。park 済みの軸も無いので再開条件の点検も空。

**カウンタが実態を反映しているかの点検 — 反映していない**。免除規定 (L0 / L1 は軸に紐づかない
固定枠なので触れなかった軸のカウントを進めない) 自体は妥当で、これを外して「その leg で触れなかった
全軸のカウントを進める」に変えると、**回ってきていない軸まで park される** = round-robin の趣旨
(詰まった軸から強制的に離す) と逆向きになる。過剰なのは免除ではなく、**3 つの規約の合成**の側:

- 親 plan §5-1 (no empty leg) が**全 leg に確定事実 1 行を義務づける**
- 親 plan §6.1 が **1 leg = 1 attack の 1 ステップ**に粒度を固定する
- 上の二重計上の注意が、**leg の主語になった軸以外はカウントを動かさない**と定める

⟹ カウンタが増えるのは「その軸の leg なのに確定事実がゼロ」のときだけで、それは §5-1 違反。
**§5-1 を守る限りカウンタは 3 に到達できない** — L0–L3 で全軸 0 なのは順調さの表示ではなく、
**機構が一度も動きうる状態になっていないこと**の表示である。加えて軸 C のように「一度も回ってきて
いない」状態と「回したが進まなかった」状態が**同じ 0 で表示される**ため、区別も付かない。

**⚠ 規約の改定は提案までにとどめる** (本 leg では規約本文を書き換えない)。提案の中身は親 plan
§9 判断ログ 14 に 1 件として起票した — 要点は (i) §5-5 のカウンタを「詰まりの検出」専用と明記し、
(ii) 別に**枯渇カウンタ** (その軸が最後に leg の主語になってからの leg 数) を持ち、
(iii) 棚卸し leg で枯渇カウンタが閾値を超えた軸は**次の可変枠を割り当てるか、優先度を下げる理由を
書くかの二択を強制する**、の 3 点。判定は棚卸し leg にだけ発生するので運用コストは増えない。

## T3 attack 一覧 (親 plan = [`bc-open-problem-t3-plan.md`](bc-open-problem-t3-plan.md))

**leg 予算 20**。枠割りの SoT は T3 plan §5。状態語彙と死因の語彙、および **`killed` の行を消さない**
規約は冒頭の節と共通 (再掲しない)。上の一覧とは親 plan が違うので表を分ける。

| slug | 軸 | 状態 | 直近 leg | 死因 / 現況 | 残った副産物 |
|---|---|---|---|---|---|
| `t3-lit-gate` | — | `harvested` | **L0(T3)** | **完了**。[`bc-facts.md`](bc-facts.md) §L0(T3) に 10 行。§1.1 の分解を **(C2) 1 本**へ訂正し、軸 **T3-γ を kill** | 下記 5 本 |
| `t3-outer-computable-sequence` | T3-α | **`live`** | **L2(T3)** | **本丸の枠**。L1 で主語が「外界列を構成する」から「多文字化以外の機構」へ動き、**L2 でさらに「`C` を共通部分にもつ一様半計算可能な族が 1 つでもあるか」= 「基数有界・閉アトムの一階特徴づけが存在するか」へ読み替わった** (facts §L2(T3) 行 5・行 10)。⟹ **実体は子 `t3-thm7-tightness` に移った** — 本行は枠として残す | 子 `t3-thm7-tightness` |
| `t3-not-effectively-compact` | T3-β | **`live`** | **L3(T3)** | 否定側。「下からは半計算可能だが上からは半計算可能でない」の移植。⚠ **難度は L0 起票時の「Marton 最適性予想の反証を含意する」から 2 段階上がっている** — **(1) L2**: それに加えて **基数有界・閉アトムの一階特徴づけが linear entropy hierarchy の *どの階層にも* 存在しない**ことを含意する (facts §L2(T3) 行 10。`ΣH₁` も `ΠH₂` も `ΠH₁₇` も一律に排除する必要がある)。⚠ **肯定側は選言 / 否定側は連言**。**(2) L3**: 既存の決定不能性の結果は**移植不能**で ([Li21b] は補助変数の本数 `l` を動かすので `\|X\|,\|Y\|,\|Z\|` 固定の BC 族へ写らない / [KKNS20] は上界のみ / [FF24] は one-shot の別問題)、[BD24] が 2024-05 時点で「BC 容量領域が算法的に計算可能かは不明」と明記 (facts §L3(T3) 行 6) ⟹ **真の未着手部分は「TM → BC チャネル行列の還元」で、これは文献に無い** (facts §L3(T3) 行 7)。⟹ 子 `t3-tm-to-bc-reduction` へ切り出した。⚠ **「文献に無い」であって「不可能」ではない**。⚠ **決定不能性 ≠ `ε` 近似不可能性** — この 2 つを混ぜると誤った壁宣言になる | 子 `t3-tm-to-bc-reduction` |
| `t3-hierarchy-level` | T3-γ | **`killed`** | **L0(T3)** | 死因 `known-result` — [Li21] 脚注 3 / §VIII 定義候補 3) が計算容易性との直結を**明示的に否定**している | 子 `t3-eps-vs-exact-decidability` |
| `t3-eps-vs-exact-decidability` | T3-γ の子 | `todo` | — | facts §L0(T3) 行 9 の probe。**`ε` 近似可能性と厳密決定可能性 (Tarski 指数関数問題) の分離**を確定させる。⚠ 行 10 の難度較正がこれに依存している | — |
| `t3-fano-converse-not-effective` | T3-α の子 | `todo` | — | facts §L0(T3) の演繹の probe。Fano 由来の converse が**固定 `n` では外界を与えない**ことと、有効化が `SRM` の加法性 (= Marton 最適性) とどう関係するか | — |
| `t3-multiletter-marton-region-version` | T3-α/β 共通の前提 | **`harvested`** | **L1(T3)** | **完了**。[auxrec] p.21 の逐語で**領域版**の出典が取れた ("the multi-letter Marton's inner bound (whose limit is the capacity region)") ⟹ facts §L0(T3) 行 2 の留保は解消 (facts §L1(T3) 行 4) | — |
| `t3-multiletter-uv-outer` | T3-α の子 | **`killed`** | **L1(T3)** | 死因 `numeric-counterexample` (⚠ **反例は数値ではなく解析的** — 優加法性 + 文献の witness 3 本)。`∩_n (1/n)UV(W^⊗n) = UV(W) ⊋ C(W)` で改善ゼロ (facts §L1(T3) 行 1) | 子 2 本 (`t3-auxrec-multiletter` / `t3-uv-superadditivity-check`) + 「多文字化の非対称性」(facts §L1(T3) 行 3) |
| `t3-auxrec-multiletter` | T3-α の子 | **`live`** | **M2(T3b)** | 積 `J = J₁⊗J₂` の族の上では**優加法性が復活する** (facts §L2(T3) 行 1) ⟹ 改善しうるのは `I(X₁;X₂\|J) > 0` の**絡んだ `J`** のみ。⚠ **UNDECIDED のまま** (M2 でも kill されず ALIVE にもならなかった)。**M2 が動かしたのは「積 witness の貼り合わせ構成が閉じるか」の 1 点だけ**: **(★) は等式制約 (19a)–(19c) / (20a)–(20c) と両立して破れる** — 明示 witness の直接評価、破れ幅はちょうど `I(X₁;X₂\|J)` で、これが理論上の最大破れである (facts §M2(T3b) 行 1・行 4) ⟹ **積 witness を「任意の valid なブロック witness を貼る」形で走らせる構成は閉じない**。⚠ **これは優加法性の反証ではない** — 当該違反は (18h) の欠損に落ちるが (18i) に吸収されて領域としては損失ゼロで、しかも当該 witness 族からは構造的に損失を出せない (同 行 5)。⚠ **残余の員数が増えた** — 絡んだ `J` の下では等式制約 (19a)–(19c) 自身が加法的でなく、残余は (★) 1 本ではなく **(E1)–(E3) + (★) の 4 本** (同 行 2、機械確認つき反例あり)。⭐ **構造的な理由が判明**: `Thm7` の量化子形は `∃p ∀T_J ∃aux` なので **facts §L2(T3) 行 9 (P3) の (H1) (純 `∃` 形) を満たさない** ⟹ P3 の一般定理は `Thm7` を覆わない (同 行 6)。⚠ facts §M1(T3b) 行 7 (閉形式ギャップ `d* ≈ 0.03877`) は依然 **KILL 側の弱い証拠**。**次の分岐点**は「(★) を満たす witness を毎回**選べる**か」(選べれば KILL) と **非積 witness / 非積入力の段** (どちらも未着手。段の一覧は facts §M2(T3b) 行 8) | 新規 3 本 (`t3-thm7-tightness` / `t3-gk-bound-cardinality` / `t3-closed-atom-characterization`) + 絡んだ `J` の片ブロック還元が死ぬ理由 (facts §L2(T3) 行 2) + **交差項の上界 `I(A₁;A₂\|J) ≤ I(X₁;X₂\|J)` の 3 行証明** (facts §M2(T3b) 行 4) + **(20a)/(20b) の交差項補正** (同 行 7) |
| `t3-nonmultiletter-outer-mechanism` | T3-α の子 | **`live`** | **L2(T3)** | 探索空間が facts §L2(T3) 行 10 により**「基数有界・閉アトムの一階特徴づけ」**へ精確化された。⚠ **P3 が殺すのは「多文字化」1 機構のみ**で、`∃` 形外界の**別の可算族**は排除されない — ここを落とすと探索空間を誤って狭める。旧起票文の 2 路線 (新しい単文字外界 / 支持関数 `h_C(θ)` を上から計算可能に評価) はどちらもこの精確化の下で読み直すこと | — |
| `t3-uv-superadditivity-check` | T3-α の子 | **`harvested`** | **L2(T3)** | **完了**。L1 の 5 行の導出は**正しかった**。P3 として十分条件 (H1)–(H5) 付きの一般定理へ格上げ済 (facts §L2(T3) 行 9)。⚠ **反例 2 種** (`max` を含む形 / 基数境界を領域の**定義**に書き込む形) が適用範囲の境界を画定している ⟹ 「優加法的 ⟹ 多文字化が無駄」は**外界に限った主張**で、内界では逆に働く | 一般定理 P3 (facts §L2(T3) 行 9) + 射程の限定 (上の `t3-nonmultiletter-outer-mechanism` 行) |
| `t3-thm7-tightness` | T3-α | **`live`** | **M2(T3b)** | **本丸**。`Thm7(W) = C(W)` か。**成立すれば (C2) が解け T3 肯定側が決着する** (facts §L2(T3) 行 6–7)。**M1 = 第一手の反証試行、判定は「判定不能」** (plan §6-1 の撤退ラインが発火。逐語の SoT は facts §M1(T3b) の 9 行)。⚠ **NO-GO は出なかった** — 厳密な分離は 1 本も立たず、文献にも `Thm7` / `Thm8` の劣最適性の主張は 0 件 (3 本の全文 grep を目視)。**部分的に塞いだ道**: **(a)** `Thm7(W) ⊆ UV(W)` が全 `W` で成り立つ (2 行、facts §M1 行 3) ⟹ **容量既知クラスすべてで `Thm7` と `C` は私信平面 `R0 = 0` の上で一致** (同 行 4) / **(b)** `C` が既知で `UV ⊋ C` の唯一の公表された族 ([probc] の積 BC 2 クラス) では **`Thm8 = C`** が一次文献だけで従い、`Thm7` の**和レート**もタイト (同 行 5)。⚠ **これは「容量既知チャネルからは反例が出ない」を意味しない** — (a) は `R0 = 0` に限られ ((α) `UV_general = O_indep` が未確認。同 行 4b、**原典 [NE07] 自身が一般版を "possibly weaker" と明記**)、(b) は和レート面に限られる ⟹ **`R0 > 0` の部分**と**[probc] 積クラスの領域レベル**という**既知チャネル上の有限問題としての隙間が 2 つ残る**。⚠ **反証は塞がっていない — 生きているルートは 3 本** (同 行 9): **(i)** 上記 2 つの隙間 / **(ii)** `Thm7` と領域レベルで**非比較**な外界 (auxrec Thm 8 / GK-Bound) がある `W` で `Thm7` を真に下回れば、**`C` を一切知らずに即 NO-GO** (GK-Bound 側は前提工事 `t3-gk-bound-cardinality` が要る) / **(iii)** `∩_n (1/n)Thm7(W^⊗n) ⊊ Thm7(W)` も同様に即 NO-GO ⟹ ⭐ **これは `t3-auxrec-multiletter` の (★) 計算と同一** (下記行を参照)。⚠ **P3 (erasure Blackwell の corner) は構造的に NO-GO を出せない** — `0.4265` は `Thm7` の弱化 (Corollary 3) の上界、`0.4205` は Marton = `C` の**下界**で、`C` の上界がどちらからも供給されない (同 行 8)。⭐ **M2(T3b) でルート (iii) の第一手が入った — 判定は「NO-GO は出ていない」**: `∩_n (1/n)Thm7(W^⊗n) ⊊ Thm7(W)` を潰す側の証明 (積 witness の貼り合わせによる優加法性) は**閉じない**ことが決着した (残余不等式 (★) が等式制約と両立して破れる。facts §M2(T3b) 行 1) が、**破れは (18i) に吸収されて領域には現れず、分離は 1 本も立っていない** (同 行 5) ⟹ **ルート (iii) は生きたまま、両方向とも未決**。⚠ **P3 の一般定理が `Thm7` を覆わないことも確定した** (`Thm7` は (H1) 純 `∃` 形でない。同 行 6) ⟹ ルート (iii) を「P3 で死んでいる」と読んではならない。残る段の一覧は同 行 8 (**非積 witness / 非積入力の 2 段は未着手**) | facts §M1(T3b) 9 行 (うち ⭐ 5 本) + facts §M2(T3b) 8 行 (うち ⭐ 2 本) + **`SR_J` / `SR_GK` の正体の同定** (どちらも弱化の値。facts §M1 行 1 が §L2(T3) 行 8 の緊張を解消) + **`Thm7` の和レートの真の優加法性** (閉形式ギャップ `d* ≈ 0.03877`、同 行 7) |
| `t3-marton-thm7-exclusivity` | T3-α/T3-β 共通 | **`harvested`** | **L4(T3)** | **gate 判定: 排他性は effort 配分の道具にならない**。`Marton ⊆ C ⊆ Thm7` を挟むと **(A)∧(B) ⟺ `Marton = Thm7`** なので、**排他性 ⟺ `Marton ≠ Thm7`** であり**数値は一切要らない** (facts §L4(T3) 行 1)。⟹ 排他性の証明は **(A) か (B) の少なくとも一方の反証**と同値 = **証明コストが §0 のゴールと同等**で、effort 配分の判定材料にはならない。⚠ **逆向き (排他性が偽 = `Marton = Thm7`) は gate ではなくゴールそのもの**。**正味の収穫は非対称性の確定** — **(a)** (`SR_Marton(BSSC)` の上界) は**文献ですでに閉じている** (jn09 が閉形式で証明済、二元入力なら `SR_M = SR_RTD` で探索空間が `[0,1]^3` に潰れる。facts §L4(T3) 行 2・行 6)、**(b)** (`SR_Thm7(BSSC)` の下界) は**難しいのではなく (C2) と同型の穴**で、`J` の Carathéodory 型基数境界という**同じ 1 点**に依存する ([auxrec] Future work (ii) が 我々の (b) と逐語一致。facts §L4(T3) 行 7・行 8)。⚠ **起票時の数値チェーンは前提 2 本が崩れた** — `SR_UV ≈ 0.3711` は**著者が撤回した値** (正しくは `0.3725562…`。行 4)、`SR_GK ≤ SR_Thm7` の**向きは出典が無い** (GK-Bound と auxrec Thm 7 はどちらも Theorem 6 を上から支配するのみで非比較。行 5)。⚠ **ただし演繹の構造 ((A)∧(B) ⟹ `Marton = Thm7`) は数値に依らないので生きている** | `facts §L4(T3) 9 行` + **BSSC 変種 (crossover 0.9) の標的** (4 和レートが `1e-3` オーダーで分離し、二元入力なので Marton 側は 3 パラメータ最適化に潰れる。facts §L4(T3) 行 9) |
| `t3-gk-bound-cardinality` | T3-α の子 | `todo` | — | [GK-outer] Theorem 7 / Theorem 9 の補助変数に **Carathéodory 型の基数境界**を付ける。付けば **GK-Bound も effectively compact** になり、`t3-thm7-tightness` の還元先を最強の外界へ差し替えられる (facts §L2(T3) 行 8)。⚠ 量化子形は auxrec Theorem 7 と同一なので、境界さえ付けば行 6 の 6 段がそのまま適用できる | — |
| `t3-closed-atom-characterization` | T3-α/T3-β 共通 | `todo` | **L3(T3)** | ⚠ **L3 で接続条件が 1 つ判明した** — [Li21] Definition 13 の**第 2 種原子 `∼^r`** (`li21.txt:1024`、意味は `li21.txt:637-638` の「`pX(x) > 0 ⟹ pZ(x) > 0`」) は**閉集合ではない**ので、P4 を Li の枠へ適用するときは**支持集合を固定するか `∼^r` を除外する**必要がある (facts §L3(T3) 行 5。⚠ **L2 の P4 そのものは無効化されない** — P4 は自前の (iii) で閉アトムを要求しており `∼^r` はもともと仮定の外側)。以下は起票時の内容: facts §L2(T3) 行 10 の読み替えの probe。「基数有界 + `¬` なし + 非狭義不等式」の一階特徴づけの存在が (C2) と**同値**であることを厳密に固める (**現状は片方向 ⟸ のみ証明済**)。⚠ **[Li21] Theorem 10 (`ΔH₁₈`) との非矛盾の根拠** (Li の階層は基数境界を課さず `¬` を許す) **がこの attack の中心** — ここが崩れると肯定側の構造定理と文献が正面衝突する | — |
| `t3-li21-theorem10-route` | T3-α | **`killed`** | **L3(T3)** | **死因 `known-result`**。問い (「[Li21] Theorem 10 の論理式から `¬` と非有界基数を除去して P4 の仮定を満たす形にできるか」) には答えがあり、**除去は不可能**だが、**その不可能性は T3 否定側の材料にならない**。**(a)** 非有界基数は除去できない — バー付き変数は「長さ `n` の系列を Gödel 符号で 1 本に潰した」もので基数が `n` に依存し、変数本数を固定したまま `n → ∞` を表す唯一の手段が**アルファベットを膨らませること**である ⟹ **基数境界を課すこと = BC の単文字特徴づけを作ること そのもの** (facts §L3(T3) 行 1)。**(b)** ところが Theorem 10 は**多文字 operational 定義の逐語転写**であって (著者自身が `li21.txt:870-873` でそう書いている)、そこに壁があっても `C(W)` の effective compactness については**何も言えない** (facts §L3(T3) 行 2)。⚠ **`Γ̄*` (概エントロピー領域) を符号化しているのではない** — `Γ*`/`Γ̄*` は Theorem 10 の構成に一度も現れず、BC → `Γ̄*` の還元は [Li21] にも [Li21b] にも無い。⚠ `¬` **も除去できない**が、除去の優先順位としては**非有界基数の下流** | 子 2 本 (`t3-marton-uv-coincidence-class` / `t3-tm-to-bc-reduction`) + **facts §L3(T3) 行 3** (⚠ **[Li21] §VIII 項目 4 の Tarski-exp 留保は P4 を反証しない** — 語義が違う。これを落とすと次 leg が誤った壁宣言をする) + **facts §L3(T3) 行 4** ((C2) = Li21 Open problem との同一性) |
| `t3-marton-uv-coincidence-class` | T3-α の子 | **`live`** | **L5(T3)** | **層 3 (Lean) の最初の一手として順序が確定**。主張は「**Marton 内界と UV 外界が一致するチャネル族に限れば P4 が適用でき `C(W)` は effectively compact**」(facts §L3(T3) 前書き / 行 4)。**L5 の Lean 在庫棚卸しで 2 層に分離した** (facts §L5(T3) 行 6) — **(i)** 「`Marton(W) = UV(W)` ⟹ `C(W) = Marton(W)`」は既存のサンドイッチ 2 本 (`Marton.martonRegionUnion_subset_capacity` / `bc_capacity_subset_uv`、**どちらも一般 BC 版・sorryAx-free・`@[entry_point]`**) から `Set.Subset.antisymm` で**約 10 行** (述語 1–4 行 + 結論 5–8 行、手本は `bc_lessNoisy_capacity_eq_uv`)。**(ii)** ⚠ **しかし attack の内容は effective compactness** であり、それには**補助変数の基数境界** (在庫に無い — `martonRegionUnion` は `⋃ (k₁ k₂ : ℕ)`、UV は `ℕ × ℕ` 固定、Carathéodory 型補題 0 件。facts §L5(T3) 行 3) と**平面集合版の計算可能性述語** (スカラー版 `IsComputableENNReal` は Kolmogorov 家系に在る。facts §L5(T3) 行 4) が要る。⟹ **(i) を (ii) の達成と書かない** (T3 plan §0.1-2)。⚠ **全 DM-BC ではない部分結果**であり §0 のゴールの代替にはしない。⚠ 一致するチャネル族の**認識**自体が計算可能かは別問題 (族を仮定として与える形なら回避できる) | [`bc-t3-lean-inventory.md`](bc-t3-lean-inventory.md) + facts §L5(T3) 7 行 |
| `t3-tm-to-bc-reduction` | T3-β の子 | `todo` | — | 否定側の**真の未着手部分**。計算可能なチャネル列 `W_k` で `C(W_k)` が `k` について**一様に上半計算可能でない**ものを構成するには「**チューリング機械 (または periodic tiling / 停止問題) → BC チャネル行列**」の還元が要る (facts §L3(T3) 行 7)。⚠ **この還元は文献に無い** — [Li21b] の還元は periodic tiling → conditional affine information inequality で、**補助変数の本数 `l` を動かす**ので `\|X\|,\|Y\|,\|Z\|` 固定の BC 族へは写らない。⚠ **「無い」であって「不可能」ではない**。⚠ [bsp] Theorem 36 の否定側テンプレート (facts §L1(T3) 行 7) が要求する前段「BC 容量領域は Banach–Mazur 計算可能でない」も、この還元が無ければ立たない | — |
