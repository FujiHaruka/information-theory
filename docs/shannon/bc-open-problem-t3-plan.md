# 一般 BC の計算可能な容量領域 — 未解決本体 (T3) への攻略

> **Parent**: [`broadcast-channel-moonshot-plan.md`](broadcast-channel-moonshot-plan.md)
> **Predecessor**: [`bc-open-problem-plan.md`](bc-open-problem-plan.md) (20 leg 完走・終了)。
> 前 relay の成果物 = [`bc-open-problem-routes.md`](bc-open-problem-routes.md) の合流節 /
> 確定事実 = [`bc-facts.md`](bc-facts.md) / attack 台帳 =
> [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) / 失敗形 =
> [`../proof-logs/proof-log-bc-open-problem-relay.md`](../proof-logs/proof-log-bc-open-problem-relay.md) §3

**Status**: **L0–L16 完了 = 17/20 leg**。直近の 11 leg (L6–L16) は**層 3 の実装線** (補助変数の
基数境界) を走っており、**L16 の gateway atom は GO** — 目的関数は `hμ₂ : 0 ≤ μ₂` +
`hμ₃ : 0 ≤ μ₃` の 2 本だけで**全重み域に載った** (`μ₁` は無条件)。⟹ **次の一手 (L17) = 残件 (i)
「台の部分型へ落とす段」で、設計分岐 (ルート S / ルート V) は未決のまま** (§5.2)。
⚠ **可変枠 L2–L15 は使い切り、層 3 の集中枠も L16 を消化した** — 残るのは
**L17 / L18 (集中枠 2 本) + L19 (収穫) + L20 (記録)** だけである (§5.1)。
**Branch**: `bc-computable-region`

---

## 0. ゴール — 未解決本体そのもの

**一般 2 受信者 DM-BC の容量領域の計算可能な特徴付けを、肯定側か否定側のどちらかで決着させる。
散文の証明と、その Lean による機械検証の両方をもって完了とする。**

完了条件 (どちらか一方):

- **肯定側** — 任意の `ε > 0` に対し有界時間で `ε` 近似を返す手続きを構成し、その正当性を証明する。
- **否定側** — そのような手続きが存在しないことを証明する。

いずれの側も、**散文の証明が立つこと**と**Lean で proof done に到達すること** (0 sorry / 0 residual、
`#print axioms` が sorryAx-free、かつ署名走査で load-bearing hypothesis が無いこと) の両方を満たして
初めて達成とする。片側だけでは達成ではない。

### 0.1 ⚠ ゴール保護条項 — 本節は leg / planner が書き換えてはならない

**前 relay で実際に起きた事故がこれである**。原案 (`487afc03`) は T2 の完了条件を
「文献に無い命題が **Lean で機械検証される**」と書き、§2.1 に「**ユーザー指定の二段構え
(散文で証明 → 形式化で検証)**」と明記していた。それを `28a740c5` が **L0 が走る前に**
「散文レベルの候補経路」へ書き換え、形式化をスコープ外にした。判断ログにはユーザー判断としての
記録が無い ⟹ **planner の独断でゴールが狭まり、20 leg がその狭いゴールで回った**。

したがって本 plan では以下を規約とする:

1. **§0 のゴールと完了条件を狭める変更は、leg / planner が適用してはならない**。狭める提案は
   判断ログに **`GOAL-CHANGE`** として起票し、**ユーザーの承認を得るまで適用しない**。
   広げる方向 (より強い完了条件) は起票不要。
2. **中間結果は記録するが、ゴールの代替にはしない**。台帳 (facts / attacks / routes) への収穫は
   従来どおり続けるが、**それをもって「ゴールに到達した」と書かない**。前 relay の階段
   (T0–T3) が持っていた「どこで止まっても成果が残る」というヘッジは、**記録の設計としては継承し、
   ゴールの設計からは外す** — ユーザーが「成果ゼロで終わる可能性を引き受ける」と明示的に選んだ。
3. **各 leg は冒頭に「この leg は §0 のゴールに対して何を動かすか」を 1 行で書く**。書けない leg は
   起票しない。

---

## 1. 標的の正確な定義 — ここを間違えると全部ずれる

**「計算可能」の語義は 2 つあり、混ぜてはならない** (facts §L12 (A4) が SoT)。

- **[N13] の語義 (本 plan の標的)** — 脚注 1 逐語: "A rate region is said to be computable if for
  every ǫ > 0, one can find a bounded time T_ǫ such that the region can be approximated to within
  ǫ in time T_ǫ." ⟹ **`ε` 近似が有界時間で可能か**。
- **Boche 系の語義 (本 plan の標的ではない)** — Banach–Mazur computability の否定。**FSC 容量 /
  DMC の最適入力分布**について確定しているが、**BC には移植されていない** (facts F8)。

**単一文字性は標的ではない**。F7 (Li 2021, Theorem 10 + Proposition 15) により BC 容量領域は
`Δ₁₈ᴴ` に入ることが確定しており、単一文字特徴付けを持つかは**肯定的に決着済**。F7 脚注 3 が
**単一文字性と計算容易性を別軸として分離**している。⟹ **前 relay が追っていた加法性 / 鋭いスラックは
T3 への必要条件ではない**。

### 1.1 標的が要求しているものの分解 (L0 で gate 済 — 穴は 1 つに縮んだ)

原案の 3 分解 (C1)(C2)(C3) は L0 の一次文献 gate で**訂正された**。判定の SoT は
[`bc-facts.md`](bc-facts.md) の `## L0 (T3)` 節 (逐語と再検証コマンドはそちらにある。ここへ複製しない)。

- **(C1) 計算可能な内界の列 — 既に在る**。正規化 n-letter Marton 内界の増加列が容量領域へ収束する
  ことは**証明済**であって予想ではなく、各 `n` の Marton 領域はカーディナリティ境界により計算可能。
  ⟹ 容量領域は **computably overt** (下から半計算可能) である。
  **領域版の逐語出典も取れた** (facts §L1(T3) 行 4)。
- **(C2) 外界側 — ここが唯一の穴。L2 で問いの形そのものが変わった**。要求は「上から収束する計算可能な
  外界**列**を作る」ことでは**ない** — **`C` を共通部分にもつ、`k` と `W` について一様に半計算可能な
  外界の可算族が 1 つでも存在するか**と同値である。**減少性も有効収束率も要らない**
  (facts §L2(T3) 行 5)。さらにそれは **基数有界かつ閉アトムの一階特徴づけが存在するか**と等しい
  (同 行 10)。⟹ 一文で言えば容量領域の **effective compactness** が未知、という記述は変わらない。
  **`Thm7(W)` (= [auxrec] Theorem 7、`∀J` 形) が `W` について一様に effectively compact であることは
  L2 で証明できた** ⟹ **(C2) ⟸ (`Thm7(W) = C(W)`)**。この還元先の最適性は一次文献が「未解決」と
  明言している (同 行 6–7)。⚠ **「未決」であって「偽」ではない**。
  **L3 で (C2) の正体が確定した** — (C2) は **[Li21] §VIII の Open problem (BC 容量領域を表せる
  線形エントロピー階層の最低レベルは何か) = BC の単文字特徴づけ問題そのもの**と**同一の穴**である。
  肯定側は「基数有界 `ΣH₁` 特徴づけの存在」で、これは **Marton 3 補助変数内界の最適性予想から従う**
  (⚠ **片方向のみ** — 逆は言えない) ⟹ (C2) の難度は Marton 予想で*上から*押さえられるが同値ではない
  (facts §L3(T3) 行 4)。
- **(C3) 有効収束率 — 独立の課題ではない**。計算可能解析の標準定義
  (`computable = effectively compact ∧ computably overt`) より、両側が揃えば停止則は自動的に従う。
  ⟹ **(C3) は (C2) に吸収される**。**L2 でこれはさらに強くなった** — (C2) の同値形の証明に
  収束率も減少性も一切現れない (facts §L2(T3) 行 5) ので、収束率を独立の課題として立てる余地は無い。

⟹ **本 plan の標的は 1 文で書ける** — 容量領域の **effective compactness** を示す (肯定側) か、
否定する (否定側) か。

**訂正 1 件**: [N13] の "there are computable inner bounds and outer bounds" は**固定領域**を指しており、
原案がこれを「収束する列」と読んだのは誤りである。

**難度の較正** (⚠ **我々の演繹、未 probe**): 否定側は **Marton 最適性予想の反証を含意する**
(facts §L0(T3) 行 9–10)。⟹ 原案 §3 が T3-β の根拠にしていた「BC についての一次文献が無いから
新規性は構造的に確保されている」という読みは**成立しない**。新規性ではなく**難度**の問題である。

---

## 2. Approach

**三層は前 relay と同じだが、層 3 がスコープ内である**。

```
層 1  散文        アイデア + 証明スケッチ
層 2  proof-probe 核 1〜2 ステップを機械検証 (数値 sim or Lean) — 散文の自己欺瞞を最短で殺す装置
層 3  形式化      散文全体を Lean へ                            ← ⚠ 本 relay ではスコープ内
```

層 2 の役割は前 relay と同じ (自己欺瞞の除去) だが、**層 3 が控えているので層 2 で止めない**。
層 1 が通ったものは層 3 に載せ、載らなければ載らない理由が層 1 の欠陥である。

**順序**: 層 1 → 層 2 → 層 3 を命題単位で回す。全体が揃うのを待って形式化を始めない
(前 relay の「散文が無いから形式化は前提を欠く」は正しいが、それは**命題ごとの順序**であって
**relay 全体の順序ではない**。この取り違えがゴール縮小の理由づけに使われた)。

**⚠ 層 2 の道具が前 relay から変わる**。前 relay の層 2 は数値掃引 (`bc_probe.py` 系) だったが、
T3 の主語は計算可能性なので**数値掃引は原理的に効かない** (有限の掃引は「有界時間で `ε` 近似できるか」を
判定できない)。⟹ 層 2 は**小さな命題の厳密証明と反例構成**が中心になり、層 1 と層 3 の距離が縮む =
**命題単位で層 3 へ載せる運用が前 relay より早く回る**。

---

## 3. 攻撃軸

| 軸 | 主語 | 優先度 | 根拠 |
|---|---|---|---|
| **T3-α** | **肯定側** — 容量領域の **effective compactness** を示す (§1.1 の読み替え後の形) | 1 | L0 で穴がここ 1 つに絞られた (§1.1)。⚠ Fano 由来の converse は符号長 `N` に依存し、**固定 `n` では外界を与えない** (facts §L0(T3)、未 probe)。**次の一手 = attack `t3-thm7-tightness`** — `Thm7(W)` の一様 effective compactness は L2 で証明済ゆえ**残るのは `Thm7(W) = C(W)` の厳密一致 1 点**で、成立すれば (C2) が解ける (facts §L2(T3) 行 6–7)。⚠ その最適性は一次文献が「未解決」と明言 = **「未決」であって「偽」ではない**。⚠ 最初の一手は反証試行 (BSSC で厳密な分離が出るか) |
| **T3-β** | **否定側** — 容量領域が effectively compact でないことの証明 | 1 | 手法の前例がある (Boche 系の「下からは半計算可能だが上からは半計算可能でない」という形)。**先に「BC 容量領域は Banach–Mazur 計算可能でない」を立てる必要がある** — 証明テンプレートは facts §L1(T3) 行 7。⚠ **難度は L2 でさらに上がった** — Marton 最適性予想の反証に加え、**基数有界・閉アトムの一階特徴づけが linear entropy hierarchy の *どの階層にも* 存在しない**ことを含意する (facts §L2(T3) 行 10)。⚠ **肯定側は選言 / 否定側は連言** — 肯定側は候補 1 本が当たれば決着するが、否定側はそのすべてを潰す必要がある。⚠ **これは探索コストの見積もりであって難度の証明ではない**。⚠ **L3 で否定側の残りが精確になった** — 既存の決定不能性の結果は BC 族へ**移植不能**で ([Li21b] は補助変数の本数を動かす / [KKNS20] は上界のみ / [FF24] は one-shot の別問題)、[BD24] が 2024-05 時点で「BC 容量領域が算法的に計算可能かは不明」と明記している ⟹ **真の未着手部分は「TM → BC チャネル行列の還元」で、これは文献に無い** (facts §L3(T3) 行 6–7、attack `t3-tm-to-bc-reduction`)。⚠ **「無い」であって「不可能」ではない**。⚠ **決定不能性 ≠ `ε` 近似不可能性** |
| **T3-γ** | **階層の最低レベル** (前 plan 軸 D の継承) — `Δ₁₈ᴴ` をどこまで下げられるか | **killed** | [Li21] 脚注 3 / §VIII 定義候補 3) が「レベルを下げても計算容易性の証明可能な利得は無い」と**明記** ⟹ T3 への経路としては死ぬ。⚠ Li 自身の open problem としては生きているので**収穫候補には残す** |
| **T3-δ** | **前 relay の `Λ_comb` を T3 の言葉へ載せ替える** | 3 | 載せ替え先が **(C2) = 外界列**に確定した。⚠ **現状の `Λ_comb` では届かない**という L17 / L18 の判定は変わらない — 載せ替えが意味を持つのは新しい枝が入ったときだけ |

**優先度 1 は T3-α と T3-β の 2 本** — 肯定側と否定側で、**同じ穴 (C2) の表と裏**である。前 relay の
教訓 (proof-log §3) が「軸を並べて round-robin すると gate を通らない候補に構築枠を使う」なので、
gate-first を採る方針そのものは維持する。

⚠ **この表の「次の一手」は散文線 (層 1–2) の話であって、直近の leg が走っている線ではない** — L6–L15 は
**層 3 の実装線** (補助変数の基数境界) を連続で消化しており、**次の leg もそちら (在庫 §11.6 の
残件 2 件のうち (ii) の分岐)** である (§5.2)。両者は並行する 2 本の線で、片方を読んでもう片方の現況を
推測しないこと。

### 3.1 ⚠ 使えないと分かっているもの (前 relay の確定事実。再探索しない)

- **内界 = 外界の路線は死んでいる** (F4: BSSC で sum rate にギャップ `≈ 0.0109`)。
- **外界 = 容量の路線も死んでいる** (F5: 逆向き semi-deterministic の積 BC で UV は strictly
  suboptimal、ギャップ `≥ 4/15`。共通メッセージ版 UVW も同時に死ぬ)。
- **内界 = 容量は死んでいない** (F2: 未解決の予想)。正しければ最低レベルは `Σ₁ᴴ` (T3-γ と直結)。
- **軸 A / 軸 B / 軸 E は経路の供給源として閉じている** (前 plan §4)。
- **`{Z}` の閉包演算だけで作る支配チャネル族は利得が定義から 0** (facts §L18、4 行の証明)。
- **停止則 (有効収束率) を内界列・外界列と独立した課題として立てる路線は死んでいる** — 計算可能解析の
  標準定義から自動的に従うため (facts §L0(T3) 行 5)。
- **[Li21] の階層のレベルを下げる路線は T3 に効かない** — Li 自身が計算容易性との無関係を明記している
  (facts §L0(T3) 行 6)。
- **UV 外界を多文字化して縮める路線は死んでいる** — `∩_n (1/n)UV(W^⊗n) = UV(W)` で改善はゼロ
  (facts §L1(T3) 行 1)。
- **`∃` 形外界を多文字化して縮める路線は一般に死んでいる** — 十分条件 (H1)–(H5) の下で
  `∩_n (1/n)O(W^⊗n) = O(W)` (facts §L2(T3) 行 9)。UV は**その特別な場合**であり、反例 2 種
  (`max` を含む形 / 基数境界を領域の定義に書き込む形) が適用範囲の境界を画定している。
  ⚠ **射程の限定 (これを落とすと後の leg が誤って探索空間を狭める)**: 死んでいるのは「多文字化」という
  **1 つの機構だけ**であり、`∃` 形外界の**別の可算族**が上から `C` に収束する可能性は一切排除されない。
  ⚠ 符号に注意 — 同じ優加法性は**内界では逆に働く** (多文字 Marton が単文字より真に大きいことと整合)。
- **[Li21] Theorem 10 経由で基数有界・閉アトムの特徴づけを作る路線は死んでいる** — Theorem 10 の
  論理式から**非有界基数は除去できず**、しかもその除去は **BC の単文字特徴づけを作ること そのもの**
  である (facts §L3(T3) 行 1)。加えて Theorem 10 は**多文字 operational 定義の逐語転写**なので
  (著者自身が明記)、そこに壁があっても `C(W)` の effective compactness については**何も言えない**
  (同 行 2)。⟹ attack `t3-li21-theorem10-route` は `killed` (死因 `known-result`)。
  ⚠ **同時に落としてはならない注意** — **[Li21] §VIII 項目 4 の「Tarski の指数関数問題」留保を
  根拠に「基数境界を入れても計算可能にならない」と読んではならない**。Li の "computable" は
  **exact decision** (membership の厳密判定 = `(R,+,∗,exp)` の量化子除去) の語義であり、P4 が
  主張する **effective compactness** (`ε` 近似) を否定していない (facts §L3(T3) 行 3)。
  計算可能コンパクト集合でも点の membership は一般に決定不能 (`d(x,K)=0` は Π01) なので両者は
  **両立する**。**ここを潰すと Tarski-exp 未解決を根拠にした誤った壁宣言になる**。

---

## 4. 運用規約

前 plan §5 の 13 項目を**そのまま継承する** (再掲しない。SoT は
[`bc-open-problem-plan.md`](bc-open-problem-plan.md) §5)。特に効くものを 3 つだけ挙げる:

- **一次文献を先に取る** — `curl` + `pdftotext -layout`。`WebFetch` は当該サーバの PDF を読めない。
  前 relay の経路の死因 2 件がどちらも「二次文献だけで `new` と読んだ」。
- **最適化器の非違反は証拠として弱い** (偽陰性が 4 件)。決着は全列挙 / 明示 witness の直接評価 /
  最適化不使用の格子で付ける。統計を出すときは「率」ではなく「探索強度に依存する下界」と書く。
- **再導出できない数値を散文にキャッシュしない**。値を引くときは「どの leg の同一 run か」を必ず添える。

新設は 1 項目:

- **§0.1 のゴール保護条項** — ゴールを狭める変更は `GOAL-CHANGE` として起票し、ユーザー承認まで
  適用しない。

### 4.1 検証の誠実性 (層 3 がスコープ内になったので明示する)

層 3 に入る以上、CLAUDE.md の Definition of Done がそのまま効く。**`sorry` は honest な
incompleteness marker として自由に使ってよい**が、**hypothesis に核を担わせて `sorry` を消すのは
禁止**。`sorryAx`-free は必要条件であって十分条件ではない — 完了判定には**署名走査**
(全 hypothesis が regularity precondition であることの確認) を必ず併せる。

---

## 5. leg 予算と配分

**20 leg**。固定枠を先に予約する (前 relay の教訓 = 予約が無ければ探索が最終 leg まで枠を食い、
成果物が残らない)。

| 枠 | leg | 内容 |
|---|---|---|
| **L0** | 1 | **§1.1 の分解を一次文献で gate にかける**。標的の語義 / 既知の到達点 / (C1)(C2)(C3) の各々が文献のどこにあるか。⚠ **ここで「分解そのものが既知の標準的な読み」と出ても軸 T3-α は死なない** — 死ぬのは「その先も埋まっている」と出たときだけ |
| **L1** | 1 | (C1)(C2) の現物を確定させる — 我々の Lean 在庫 (`bcCapacityRegion` / `bcOuterRegionUV` / Marton 内界) がどの列に対応するか。**操作的定義から出発している優位が効くのはここ** |
| **L2–L15** | 14 | 可変枠。**gate-first** で [探索 + gate] → [構築 + probe] → [層 3 へ載せる] の 3 段 1 組 |
| **L8 / L14** | (内数) | 棚卸し — 軸の枯渇カウンタと撤退ラインの判定 |
| **L16–L18** | 3 | 層 3 の集中枠。**散文が立ったものを Lean へ** |
| **L19** | 1 | 収穫 — 到達点の書き上げ |
| **L20** | 1 | 記録 — proof-log + metrics |

⚠ **L16–L18 を探索に流用しない**。前 relay がゴールから形式化を落とした構造的な理由が
「形式化に割く枠を予約していなかった」ことなので、枠として先に切る。

**L1 枠に書かれていた棚卸し (我々の Lean 在庫がどの列に対応するか) は L5 で消化済** — 成果物は
[`bc-t3-lean-inventory.md`](bc-t3-lean-inventory.md)、判定は facts §L5(T3) の 7 行。土台 (定義の一般性・
サンドイッチ両方向の sorryAx-free) は健全で層 3 で作り直す定義は無い一方、⚠ **層 3 の残コストは
補助変数の基数境界** (在庫に Carathéodory 型の境界補題が 0 件、facts §L5(T3) 行 3) である。

**L6–L12 の 7 leg は、その基数境界 1 本に可変枠を連続で充てた** — 成果物は在庫
[`bc-t3-cardinality-inventory.md`](bc-t3-cardinality-inventory.md) (§5 / §6.4 / §7 / §8 / §9 / §10 が
SoT) と facts [`bc-facts.md`](bc-facts.md) `## L7 (T3)`、および新規 Lean ファイル 3 本。

- **L6 (在庫 gate) = COSTLY、⚠ 壁ではない** (`d6d964ff`)。Carathéodory 一式と単体のコンパクト性は
  Mathlib に在り、⚠ **Fenchel–Eggleston は本件に不要** (改善するのは定数だけ、在庫 §2.4) ⟹
  `wall:` slug は起票しない。⚠ **副産物の訂正 1 件** — 前在庫の「基数を落とす機構は 0 件」は
  **検索語 ("Carathéodory") による見落とし**で、UV 側に完遂済の実物
  `OuterBoundUV/Quantization.lean` が在る (在庫 §4.4 / §6.2、CLAUDE.md `cause:loogle-blind` と同型)。
  ⚠ ただし**補題としては再利用できない** — Quantization は裾を畳む切り捨てで損失が `m → ∞` で 0 に
  行くことに依存しており、Marton 側は厳密保存でなければならない ⟹ 効くのは**ファイルの形**だけ。
- **L7 (一次文献の決着) = 在庫 §6.4 の懸念は的中、ただし迂回路が在る** (`21be06aa`)。
  ⚠ **標準の support lemma では Marton の基数境界は出ない** — 素朴な Carathéodory が返すのは
  `|U| ≤ |V||X|+1` と `|V| ≤ |U||X|+1` という相互再帰で発散し、3 本の一次文献が逐語でそう述べている。
  教科書の答は摂動法だが、**[GA09] §V-B の初等証明 (凸性 + 多面体の端点) で我々の 2 補助変数版は
  閉じる** ⟹ Lean 化に Fisher 情報もエントロピーの二階微分も要らない。定数は
  **`martonAuxBound α = Fintype.card α`** (⚠ **`+1` は付かない** — `+1`/`+4` が付くのは共通メッセージ版の
  第 3 変数だけ。`bcAuxAlphabet k = ULift (Fin (k+1))` との off-by-one 一致まで逐語確認済、在庫 §7.7)。
  ⚠ **代わりに重い所見が 1 つ出た** — 在庫 §5.2 の署名が要求する「**3 汎関数の同時改善**」に対し、
  文献が保証するのは「**1 本の重み付き和**」だけで、標的の言明が文献より強い (領域は一般に非凸)。
- **L8 (標的言明の選択) = (a′) に決定** (`959cba2e`)。内容と却下 2 案の死因は判断ログ 8。
- **L9 (gateway atom) = GO** (`c5e326df` / `5fd4b38d`)。在庫 §7.5 / §8.2 が「唯一の未調査項目」と
  していた部品 **(3b) 多面体 `{q ≥ 0, Aq = b}` の端点の台が `rank A` 以下**を、新規
  `InformationTheory/Shannon/BroadcastChannel/Marton/SupportReduction.lean` の
  `exists_support_card_le_of_convexOn` として **proof done** で実装 (Mathlib の壁 0 件、
  `InformationTheory.lean` 登録済、規約ゲート PASS)。⚠ **我々の演繹** (署名を読んだ判断): 同署名は
  凸関数 `f` を仮定に取って「制約を保ちつつ `f` を減らさない台縮小」を返す形なので、**(3c)
  「凸関数は端点で最大」も同じ 1 本に畳まれている** ⟹ 残る部品は (3a) 凸性そのもの。
  ⚠ 到達状態 (sorry / axioms) の現物は `rg` / `#print axioms` で毎回引き直す (散文にキャッシュしない)。
- **L10 (部品 (3a) の凸性 gate) = GO、⚠ 壁 0 件** (`e2c2dee4` / `8a2dd0d2`)。凸核 `−H(V|Z)` の凸性は
  **自作不要**だった — 実物が 2 家系隣の
  `InformationTheory/Shannon/WynerZiv/ConditionalEntropyConvexity.lean:75`
  (`negMulLog_marginal_gap_le_joint_gap`) に proof done で在り、在庫が用意していたルート A
  (perspective 自作) / ルート B (Gibbs 変分) はどちらも不要になった (判定と loogle 逐語の SoT は
  在庫 §9。ここへ複製しない)。実装は新規
  `InformationTheory/Shannon/BroadcastChannel/Marton/ObjectiveConvexity.lean` で、目的関数を
  `auxWeightObjective k w c q` として名前つきで切り出し (線型項は抽象 `LinearMap` ではなく係数
  ベクトル `w : U → ℝ` の明示形)、headline `exists_support_card_le_auxWeightObjective`
  (`@[entry_point]`) が `A` の行和 1・`k` と `q` の非負だけを仮定に、`Fintype.card X` 本の線型制約を
  保ちつつ目的関数を減らさず台を `Fintype.card X` 以下へ落とす ⟹ **[GA09] Lemma 1 の初等証明が
  ベクトル水準で閉じた**。型クラス前提は `Fintype` 4 つのみで**測度空間を要求しない** (在庫 §9.1.1 が
  警告した `MeasurableSpace` の伝播は `letI := ⊤` で回避)。⚠ **撤退ライン 3 本とも不発火**、
  `@residual(wall:…)` は 1 本も立っていない。
- **L11 (測度形 ↔ ベクトル形の降ろしの gateway atom) = GO、⚠ 壁 0 件**
  (`31bee0b3`…`2eea49ef`)。新規 `Marton/ObjectiveVectorForm.lean` が `(V₂,Y₂)`-周辺の singleton 質量を
  外側添字 `u : V₁` についての `q` 重み付き和へ開き、支える private の master 補題が**任意射影について
  完全に一般**であることを示した。あわせて受け口の slot 不足 2 件も充填 (`SupportReduction.lean` の
  `exists_support_card_le_of_convexOn_add_aggregate` = **集約のみを読む任意の項 `g`** への台縮小の拡張。
  ⚠ `g` に仮定ゼロ / `auxWeightObjective` への非負係数 `t : ℝ` slot)。⚠ **重い所見 1 件** — 本ルートの
  射程は `μ₂ = 0` 系列と対称の `μ₁ = 0` 系列に限られる (判断ログ 10)。
- **L12 (残り 6 射影の同型反復) = 在庫 §10 の残件 4 つのうち 3 つが閉じた、⚠ 壁 0 件**
  (`bb2ee4f0` / `85028aa8` / `69e1ed21`)。`ObjectiveVectorForm.lean` (95 → 362 行) で
  **3 汎関数が要る 7 射影の周辺分布がすべて `@[entry_point]` でベクトル形に降りた** —
  `martonAuxRow` (= 保存すべき行) とその**行和 1**、および `X` / `V₁` / `V₂` / `(V₁,V₂)` /
  `(V₁,Y₁)` / `Y₁` / `Y₂` の周辺 (逐語の署名と型クラス前提は在庫 §10.1 が SoT。ここへ複製しない)。
  ⚠ **要点は `Y₁` / `Y₂` の 2 本** — 右辺が `q` を**集約を通してのみ**読む形になっており、
  `exists_support_card_le_of_convexOn_add_aggregate` の `g` に載る根拠が**構文で立った**
  (= L11 で出た「凹な `H(Y₁)` は凸 slot に載らない」を集約 slot で受ける経路が実物として通った)。
  規約ゲート PASS、**honesty ゲートは launch 条件外で不発火** (新規 `sorry` / `@residual` の導入も、
  honesty 上意味の変わる署名変更も無い)。⚠ **撤退ライン 3 本とも不発火**。
  ⚠ **想定と違った点 1 件** — 見積り 120–200 行に対し**実測 +267 行**。増分は master 補題が返す
  5 重和の `if` を潰す**定型句**であって新規の数学ではない (在庫 §10.7)。

- **L13 (経路差し替え) = 在庫 §10.3 の残件は外れていた、⚠ 壁 0 件** (`7c8ce2ab` / `36a7cafb`)。
  `pV = q ⊗ₘ κ` の disintegration は **新規宣言 0 本**で済んだ — `Measure.disintegrate` 1 term と
  消費側 1 行の `conv_lhs => rw` で既存 7 射影がそのまま一般の `pV` に効き、型クラス連鎖 3 段は
  機械検証で繋がって **import 追加 0 / 署名に増える前提 0** ⟹ 見積り 20–40 行に対し**実測 ~2 行**。
  ⚠ **代わりに在庫の残件リストが落としていた橋が 2 件あった** — 目的関数は `entropy` =
  `negMulLog` の和なので、singleton 質量の**積形**を与える 7 射影だけでは繋がらない:
  (GAP A) chain-rule 恒等式 / (GAP B) 凸 slot が内部生成する出力周辺と既在の集約形の一致。
  実装は `Marton/ObjectiveVectorForm.lean` (362 → 489 行、新規 7 本)。詳細の SoT は在庫 §11.1 / §11.2。
  ⟹ **教訓**: 残件の洗い出しを「測度側で何が要るか」だけで作ると**目的関数側の関数形
  (`negMulLog` の中に何が入るか) で要る橋を落とす** — 射影表と目的関数の定義展開を両側から突き合わせる。
- **L14 (目的関数の組み立て) = GO、⚠ 壁 0 件** (`6fe8c110`)。新規 `Marton/ObjectiveAssembly.lean`。
  ⭐ `S := I₁ + I₂ − I_{V₁V₂}` を展開すると **`H(V₂)` が相殺**し、GAP A の 2 本を当てると
  **`H(V₁)` も相殺**して、`μ₁·I₁ + μ₃·S` が**集約 slot / 凸 slot / 線型 slot にちょうど 3 分割**される
  (`c := 0`、`t := μ₃`) ⟹ **[GA09] Lemma 1 の機構が標的の重み付き和に実際に噛み合うことが
  恒等式として立った** (`martonWeightedSum_eq_auxWeightObjective_add_aggregate`)。
  ⚠ **`hμ₁ : 0 ≤ μ₁` は不要**と判定 — 恒等式側は `ring` で閉じ、台縮小が要求するのは `0 ≤ μ₃` 1 本
  だけ。退化境界 2 つ (`μ₃ = 0` / `μ₁ = −μ₃`) でも結論が vacuous でないことを確認済 (在庫 §11.4)。
- **L15 (測度形への回収) = GO、⚠ 壁 0 件 / 自作 0 件** (`0ebabf96` / `925e28a4`)。台縮小後のベクトルを
  測度へ戻し、標的言明を**測度形**で立てた (`exists_support_card_le_martonWeightedSum_measure` =
  両辺が同じ型の `Measure V₁` 上の量 = (a′-1) に一番近い landing 点)。既存資産だけで閉じ
  (`pmfToMeasure` 系 3 本 + `sum_measureReal_singleton_univ_eq_one`)、ブリーフが立てた
  「汎用の橋を 1 本置く」案は不要だった。⚠ **決め手は名前でなく式の部分項での `rg`** —
  本 relay で**4 度目**の同型 (名前検索では届かず結論形／部分項で届く) ⟹ 判断ログ 9 の規約が再確認された。
- **L16 (gateway atom + 全重み版) = GO、⚠ 壁 0 件** (`ea49d859` / `e3c28171` / `160aae37`)。
  ⭐ 在庫 §11.6 (ii) が**訂正候補**として登録した読み ——「§8.2 の射程限定は**分解の取り方の産物**」——
  が**確定した**。[GA09] `ga09.txt:587-589` の grouping で `martonInfo₂ = H(V₂) + H(Y₂) − H(V₂,Y₂)` を
  切ると、`H(Y₂)` は**集約 slot**、`H(V₂) − H(V₂,Y₂) = −H(Y₂|V₂)` は **転置した凸 slot** に載る
  (⚠ **凸性の数学は新規 0** — `convexOn_negCondEntropy_mixture` に `V` `Z` を入れ替えて渡すだけ)
  ⟹ 目的関数 `μ₁·I₁ + μ₂·I₂ + μ₃·(I₁+I₂−I_{V₁V₂})` が**全重み域に載る**。
  新規 13 本 (凸 slot を 2 本持つ `auxWeightObjectiveTwoSlot` + 全重み版の台縮小 3 本ほか)、
  ⚠ **既存 3 本は無改変 = 署名変更 0 ⟹ 波及 0**。逐語の署名の SoT は在庫 §12.2。
  ⚠ **符号仮定は `hμ₂ : 0 ≤ μ₂` + `hμ₃ : 0 ≤ μ₃` の 2 本だけ** (`μ₁` は無条件) ⟹ 判断ログ 10 の
  「2 系列に絞らないと under-hypothesis になる」は**撤回**された (判断ログ 10 の追記 2 / 判断ログ 12)。
- 4 leg とも規約ゲート **PASS**、honesty ゲートは launch 条件外で不発火 (新規 `sorry` / `@residual` の
  導入 0、既存署名の変更 0)。**撤退ライン 3 本とも不発火** (逐語の照合は在庫 §11.7 / §12.5)。

### 5.1 ⚠ 予算の実測 — (a′-1) は上限を +63% 超えた (超過幅は L16 で広がった)

(a′-1) 関連 4 ファイルの `wc -l` 実測は **1305 行** (内訳と再導出コマンドの SoT は在庫 §12.3。
L15 時点は 1070 行)。**見積り 480–800 行に対し +505 行 (+63%) の超過**であり、しかも
**(a′-1) はまだ成立していない** (縛れたのは `V₁` の台の大きさだけ)。

⚠ **「内数の再配分」という言い回しで超過を隠さない** (§10.7 / §7.4 の言い回しをここへ流用しない)。
「総額を据え置く」は L12 までの方針だったが、**実測が上限を超えた以上その方針は事実に合わない**
(判断ログ 11)。⚠ **超過の所在は 1 箇所に局在している** — 「測度形 ↔ ベクトル形の橋」
(`ObjectiveVectorForm.lean`: 見積り 120–200 → 実測 520) で、凸性・端点・台縮小の数学側は見積り通り。
⚠ **L15 が立てた「`V₂` 側の鏡像で主因が倍化する」という予測は、L16 では実現しなかった** — L16 が
採ったのは鏡像ではなく**転置**で橋を通り直さないため、同ファイルの増分は +31 行に留まった。
⚠ ただし**倍化予測が誤りだったことにはならない** — 予測の対象 (`q₂` を動かして `V₂` 自身の台を縮める
鏡像) は未実装のままで、倍化するかは**未検証**である (在庫 §12.3 / §12.4 (ii))。

⚠ **枠の位置も正直に書く** — §5 の可変枠 **L2–L15 は L15 で使い切り**、層 3 の集中枠も **L16 を
消化した**。残るのは **L17 / L18 (集中枠 2 本) + L19 (収穫) + L20 (記録)** だけであり、在庫 §12.4 の
残件 (i)(ii) を **2 leg で入れる**のが次 leg の前提条件になる。⚠ 在庫 §6.5 の予算警告は有効 (層 3
集中枠に他の形式化を同居させない)。これは**配分**の話であって `GOAL-CHANGE` ではない。

### 5.2 L16 の結果と次の一手 (L17)

**L16 = GO**。gateway atom (`μ₂·martonInfo₂ = 集約 slot + 転置した凸 slot`) は通り、目的関数は
**全重み域**に載った (`hμ₂` + `hμ₃` の 2 本のみ、`μ₁` は無条件)。⟹ 判断ログ 10 の射程限定は撤回され、
在庫 §8.2 の (a′-1) が量化する `μ₁ μ₂ μ₃` の範囲を**署名の水準で絞る必要は無くなった**
(逐語の署名と機構は在庫 §12.1 / §12.2 が SoT。ここへ複製しない)。

⟹ **残っているのは在庫 §12.4 の 2 件で、どちらも `V₂` ではなく「型の付け替え」側にある**。

**(i) 台の小ささ → 基数の小さい型への付け替え = 手つかず (L17 の主目標)**

- ⚠ **本当に無い 1 段** = **「台の部分型へ落とす段」**。`|V₁| > |α|` では**大域単射が存在しない**ので、
  `wz_entropy_map_injective`
  (`InformationTheory/Shannon/WynerZiv/Achievability/Covering.lean:1189`、**大域単射を要求**) を
  直接は当てられない。3 部品のうち残り 2 部品は既存資産で埋まる公算が高い (在庫 §11.6 (i))。
- ⚠ **`martonAuxBound` はコードに存在しない** (再導出 `rg -n 'martonAuxBound' InformationTheory/`)。

**⚠ L17 の設計分岐は未決である (ここを盛らないこと)**

在庫 §11.6 (i) の「⚠ 分からないこと (a)」= **ルート S** (台への制限を測度の水準でやる = 部分型へ
落とす) と **ルート V** (L11–L15 のベクトル層を部分型の上でもう一度走らせる) のどちらが短いか、という
分岐に `proof-pivot-advisor` を 1 体投じたが、オーケストレーターがバックグラウンド待機の完了を待たずに
経過時間を取り違えたため**起動から約 5 分で打ち切った** ⟹ ⚠ **報告が 0 行ゆえ分岐は依然 未決**で
推奨ルートは無い。⚠ **advisor ルートは潰れていない (試されていないだけ)** ので次 leg はこれも選べる。

⚠ **次 leg への申し送り (⚠ 提案であって検証済の設計ではない)**: **gateway atom を 1 本直接実装させる**案。
⚠ 根拠は CLAUDE.md「gateway-atom-first」**のみ**で、「advisor が不発」は根拠にならない。候補 atom =
**`Set.InjOn` 版のエントロピー不変性 1 本** (`Finset.sum_nbij'` / `Finset.sum_bij_ne_zero` +
`Real.negMulLog_zero` で閉じるかを試す。⚠ **3 本とも存在は確認済だが、この組み合わせで閉じるかは未検証** — 在庫 §12.4 (i))。

**(ii) `V₂` 側の基数 = L16 の GO で性質が変わった**

目的関数側は全重みで載ったので、**在庫 §11.6 (ii) の表が予告していた「鏡像側は凸 slot が 2 本要る」は
実装済**である。⟹ 残るのは `martonInfo₁/₂` の**座標順が固定**であることに起因する swap 対称性補題
(`pV.map Prod.swap` + `BCChannel` の出力入れ替え) で、**in-project 0 件** (在庫 §11.6 (ii) 項目 1)。
⚠ **書き下ろしか型引数の入れ替えで済むかは未検証**。

**`V₂` 側の基数そのものは [GA09] 逐語で決着している** — `ga09.txt:292-294` / Lemma 1
(`:295-296, 319-320`) は `|Û| < Su` **かつ** `|V̂| ≤ Sv` の形で、**片側だけ減らし他方を触らない**
⟹ §L7 (Q4) が記録する**発散する相互再帰は起きない** (あれは 3 汎関数の**値**を保存する素朴
Carathéodory の話で、ここで保存するのは `p₀(x)` だけ)。

⚠ **`@residual(wall:…)` は切らない** — 文献側では 2009–2011 に閉じており (facts §L7 行 7)、残るのは
手間であって壁ではない。⚠ 逆に「文献に在るのだから配線するだけ」と読むのも誤り (在庫 §7.4)。

### 5.3 残件の記録 (L16 以降が読む)

- **(i) アルファベットの付け替え** — 3 部品のうち **2 部品は既存資産で埋まる**: 単射 (非全射) 版の
  エントロピー不変性 `wz_entropy_map_injective`
  (`InformationTheory/Shannon/WynerZiv/Achievability/Covering.lean:1189`、`Function.Injective g` しか
  要求しない) と、同時分布の輸送 `compProd_comap_map_prodMap`
  (`InformationTheory/Shannon/ChannelCoding/CodeToAmbient.lean:401`、`Measurable` のみ要求)。
  ⚠ **本当に無い 1 段** = 「台 `ncard ≤ card α` の測度を、基数 `≤ card α` の型の上に書き直す」=
  **台の部分型へ落とす段** (`|V₁| > |α|` では大域単射が存在しないので上の単射版を直接は当てられない)。
- ⚠ **在るが使えない**: `MartonUnion.lean:249` / `:288` / `:329` / `:372` の再ラベル不変性 4 本は
  `≃ᵐ` (全単射) 版のみで**基数を落とせない**。`bcAuxMeasurableEquiv` (同 `:402`) は `private` のまま
  (再導出は 5 行)。
- ⚠ **`martonAuxBound` はコードに存在しない** (`rg` 0 hit) — 在庫 §7.7 が off-by-one まで逐語確認した
  定数 (`= Fintype.card α`、`+1` は付かない) はまだ Lean 化されていない。
- ⚠ **swap 対称性補題 (`pV.map Prod.swap` + `BCChannel` の出力入れ替え) は in-project 0 件** —
  鏡像を「型引数の入れ替え」で得られるか書き下ろしかは**未決** (`martonInfo₁/₂` は 5 つ組の
  座標順が固定された定義)。

---

## 6. 撤退ライン

**⚠ 撤退ラインはゴールを下げる装置ではない** (§0.1-2)。撤退とは「本 relay を終了して次の判断を
ユーザーに返す」ことであって、「達成条件を緩める」ことではない。

- **L8 の棚卸しで、優先度 1 の軸 T3-α が gate を通らず、かつ T3-β / T3-γ のどちらも
  第一手を返していない** ⟹ ユーザーに判断を返す (継続 / 軸の入れ替え / 終了)。
- **L14 の棚卸しで、層 3 に載せられる散文が 1 本も無い** ⟹ L16–L18 の集中枠を層 1 へ戻すかを
  ユーザーに諮る (`GOAL-CHANGE` ではない — 完了条件は動かさず配分だけ動かす)。
- **20 leg を使い切って未達** ⟹ **未達と書く**。到達した中間結果は台帳に残すが、
  **ゴール達成とは書かない**。

---

## 7. 判断ログ

書く頻度: 方針変更 / 撤退 / 当初仮定の修正。決着済 entry は削除 (git が履歴)、active のみ残す。
プラン予算 ≤ 600 行 / active 判断ログ ≤ 10 entry。

1. **ゴールを T3 (未解決本体、形式化込み) に置き直した (ユーザー決定、2026-08-02)**: 前 relay が
   T2 (中間結果、散文のみ) で完走した後、ゴール設定の経緯を遡ったところ **`28a740c5` が L0 前に
   ゴールを独断で狭めていた**ことが判明した。ユーザーは 3 択 (A. 層 3 へ切り替え / B. 軸 G 発動 /
   C. 同方向継続) に対し **「未解決問題そのものを正面から狙う」を選択** ⟹ 本 plan を新設。
   ⚠ **引き受けたリスクを明示して持つ**: 階段によるヘッジが外れたので、**20 leg 使って
   ゴール未達で終わる可能性が高い**。それを「中間結果は出た」で埋め合わせない (§0.1-2)。
2. **軸 G (Markovity 予想) を本 plan の軸に入れなかった (要再判定)**: 前 relay で park されたまま
   一度も回らなかった枠だが、主語は Marton 領域の最適化子の構造であって **T3 の完了条件に
   直結しない** ⟹ 優先度表から外した。⚠ **T3-γ 経由で F2 (Marton = 容量) に効く可能性はある**ので、
   L8 の棚卸しで拾い上げ候補として再判定する。
4. **軸 T3-γ を kill、軸 T3-β の根拠を差し替えた (2026-08-02)**: γ の死因は `known-result`
   (一次文献が計算容易性との直結を明示的に否定している)。β については「一次文献が無い ⟹ 新規性が
   構造的に確保されている」という原案の根拠を**撤回**し、**Marton 最適性予想の反証を含意する難度**
   として登録し直した。前 relay 規約 §5-9 (死因は必ず子 attack を 1 本生む) に従い、γ の死因から
   子 attack `t3-eps-vs-exact-decidability` を
   [`bc-open-problem-attacks.md`](bc-open-problem-attacks.md) に起票した。
6. **L2: (C2) の内容が読み替わり、T3-α の標的が `Thm7 = C` へ収束した (2026-08-02)**: 根拠は
   [`bc-facts.md`](bc-facts.md) §L2(T3) 行 5–7 / 行 10 (逐語と再検証コマンドはそちら。ここへ複製しない)。
   (C2) は「上から収束する外界**列**を作る」ではなく「**`C` を共通部分にもつ一様半計算可能な族が
   1 つでも存在するか**」= 「**基数有界・閉アトムの一階特徴づけが存在するか**」であり、
   `Thm7(W)` の一様 effective compactness は証明できたので、**残る穴は `Thm7 = C` の 1 点**である。
   ⚠ **これは §0.1-1 の `GOAL-CHANGE` に当たらない** — §0 の完了条件は 1 文字も動いておらず、動いたのは
   §1.1 の**未検証の読み**であって、§1.1 自身が gate にかけると宣言していたとおりの更新である。
   ⚠ **穴が 1 点に縮んだのは標的が易しくなったからではない** — 還元先は一次文献が「未解決」と明言する
   命題で、否定側 (T3-β) の難度は行 10 でさらに上がっている。
7. **L3: [Li21] Theorem 10 経由を kill、(C2) が Li21 の Open problem と同一だと確定した (2026-08-02)**:
   根拠は [`bc-facts.md`](bc-facts.md) §L3(T3) (逐語と再検証コマンドはそちら。ここへ複製しない)。
   Theorem 10 は**多文字 operational 定義の逐語転写**なので、その論理式から基数を有界化できないことは
   「BC の単文字特徴づけが未解決である」の**言い換え**にすぎず、否定側の材料にはならない ⟹ attack
   `t3-li21-theorem10-route` を `killed` (死因 `known-result`)。⟹ **T3 の未解決本体は BC 単文字
   特徴づけ問題そのもの**であり、肯定側は「基数有界 `ΣH₁` 特徴づけの存在」(Marton 最適性予想から
   従う。⚠ 片方向)、否定側は「TM → BC チャネル行列の還元」(⚠ **文献に無い**、真の未着手部分) へ
   精確化された。
   ⚠ **これは §0.1-1 の `GOAL-CHANGE` に当たらない** — §0 の完了条件は 1 文字も動いておらず、
   動いたのは §1.1 の**未検証の読み**であって、§1.1 自身が gate にかけると宣言していたとおりの更新である。
   ⚠ **副産物の部分結果 (`t3-marton-uv-coincidence-class` = 「Marton 内界と UV 外界が一致する
   チャネル族に限れば `C(W)` は effectively compact」) を §0 のゴールの代替にしない** (§0.1-2)。
   台帳には記録するが、**それをもって「ゴールに到達した」とは書かない**。
8. **L8: 層 3 の標的言明を (a′) = 「スカラー版を閉凸包の等式として述べる」形に決定した (2026-08-02)**:
   根拠は在庫 [`bc-t3-cardinality-inventory.md`](bc-t3-cardinality-inventory.md) §8 (逐語・反例の構成・
   `dep_consumers` の出力はそちら。ここへ複製しない)。⚠ **却下した 2 案は死因が違う** — 第 3 案 (c)
   (標的の右辺を有界合併にする形) は**反例で否定**した (端点で 2 本同時が残るので文献のスカラー保証から
   導けない。定数を `+1` 緩めても `V₂` 側で反復しない) 一方、(b) (領域定義にタイムシェア変数 `Q` を
   足して凸性を回復する) は**波及が「7 decl」では済まない**と判明した — `dep_consumers.sh` の逐語出力で
   `martonRegionUnion_subset_capacity` と `martonRegionUnion_subset_uv` (**どちらも `@[entry_point]`**)
   の証明が通らなくなり、必要なのは署名修正ではなく**新しい数学** (time-sharing の operational 証明 /
   `bcOuterRegionUV` の凸性) ⟹ 却下。⟹ **(a′) は定義変更ゼロで (b) と同じ対象を買う**
   (⚠ **我々の演繹**: `Q` 付き領域の合併は `conv(⋃ martonRegion)` に一致する、在庫 §8.2-3)。
   ⚠ **これは §0.1-1 の `GOAL-CHANGE` に当たらない** — §0 の完了条件は 1 文字も動いておらず、動いたのは
   層 3 で最初に立てる**補題の形**である。⚠ **同時に持つ限定**: (a′) が与える切り詰めは**閉凸包の水準**
   であって、非凸のままの合併の切り詰めは出ない (それは (c) = 文献の射程外)。⚠ **残債務 1 本**:
   凸化した対象を内界として消費する段で `Convex ℝ (bcCapacityRegion W)` が要るが in-project に無い
   (⚠ (b) を採っても同じだけ立つ債務であり、(a′) 固有の追加コストではない)。
9. **L10: 在庫を「家系」の単位で数えると隣の家系の完成品を見落とす (2026-08-02)**: 部品 (3a) の
   凸核 `−H(V|Z)` の凸性を、前在庫は「BC 家系の凸性資産は `martonRegion_convex` 1 本だけ」と
   **家系を単位に数えた**結果 in-project に無いものとして扱い、自作ルート 2 本 (A = perspective /
   B = Gibbs 変分) を用意していた。実物は 2 家系隣の
   `Shannon/WynerZiv/ConditionalEntropyConvexity.lean:75` に proof done で在り、**どちらのルートも
   不要**だった (根拠は在庫 §9。ここへ複製しない)。⚠ **CLAUDE.md が記録する Shannon–Hartley Leg B
   Leaf 2 (`cause:loogle-blind`) と同型の再発**であり、L6 の副産物の訂正 (検索語 "Carathéodory" に
   よる見落とし) に続いて**本 relay で 2 度目**である ⟹ **規約として持つ: 在庫の「無い」は家系でも
   検索語でもなく結論形で数える** (`ConvexOn` などの結論パターンを project 全体に当てる。CLAUDE.md
   「In-repo asset search」)。⚠ **見落としが埋まったことを「全体が軽くなった」と読まない** — 下がった
   のは (3a) の内数だけで、予算は据え置き (在庫 §7.4 / §9.8 / §9.10)。
10. **L11: 本ルート (C) が届くのは `μ₂ = 0` 系列 + 対称の `μ₁ = 0` 系列だけ ⟹ (a′-1) の署名を絞る
    (2026-08-02)**: 目的関数 `μ₁·I₁ + μ₂·I₂ + μ₃·(I₁+I₂−I_V₁V₂)` を `U := V₁` / `pV = q ⊗ₘ κ` と置いて
    `q = p(v₁)` だけを動かす台縮小に載せると、一般の `μ₂ > 0` では `μ₂·H(V₂)` が残る。
    ⚠ **我々の演繹**: `H(V₂)` は `q` について**凹**であり、**かつ集約 `p(x)` の関数でもない**
    (`p(v₂)` は `p(x)` から決まらない) ⟹ `auxWeightObjective` の凸 slot にも線型 slot にも、
    `…_add_aggregate` の集約 slot にも載らない。`p(v₂)` を集約に足す回避は基数定数を `|α|+|V₂|` に
    膨らませる (= §L7 が記録した発散する相互再帰) ので回避になっていない。⟹ **在庫 §8.2 の (a′-1) は
    `μ₁ μ₂ μ₃ ≥ 0` の全域を量化しているが、2 系列に絞らないと under-hypothesis になる**
    (絞り方の 2 案と、絞っても (a′-2) が閉じる理由 = 分離汎関数が要求する重みがまさにその 2 系列だけ
    であることは、在庫 §8.2 の ⚠ ボックス + §10 が SoT。ここへ複製しない)。
    ⚠ **これは L7 が記録した「標的言明が文献より強い」の再演で本 relay 2 度目** ⟹ **規約として持つ:
    文献の保証と標的の署名は、結論形だけでなく量化子の範囲まで含めて突き合わせる**。
    ⚠ **これは §0.1-1 の `GOAL-CHANGE` に当たらない** — §0 の完了条件は 1 文字も動いておらず、
    動いたのは層 3 で立てる**補題の署名**である。
    ⚠ **追記 (L15 時点、2026-08-03。本 entry は決着前なので書き換えない)**: 在庫 §11.6 (ii) が
    **この射程限定への反証の可能性**を出した — [GA09] の別 grouping (`γI(V;Z) = γH(Z) − γH(Z|V)`) を
    採って**凸 slot を 2 本**使えば一般の `μ₂ > 0` も載りうる (凸性の数学は新規 0、型検査までは
    機械確認済で恒等式は未検証)。⟹ **L16 の gateway atom (`μ₂·martonInfo₂ = 集約 + 転置 bracket`)
    で GO/NO-GO を付ける** (§5.2)。GO なら本 entry の射程限定を修正、NO-GO なら本 entry のとおり
    標的を `V₁` 側 1 変数へ**署名の水準で**絞る。
    ⚠ **追記 2 (L16 時点、2026-08-03)**: **本 entry の結論 (射程限定 + 署名を絞る) は撤回された** —
    gateway atom は **GO** で、上の追記が予告した grouping がそのまま通った。**実際の署名が持つ符号
    仮定は `hμ₂ : 0 ≤ μ₂` + `hμ₃ : 0 ≤ μ₃` の 2 本だけ**で `μ₁` は無条件 ⟹ **「2 系列に絞らないと
    under-hypothesis になる」は成立しない** (`exists_support_card_le_martonWeightedSumAllWeights`
    /`…_measure`。逐語の SoT は在庫 §12.2)。⚠ **本 entry の「`μ₂·H(V₂)` が残る」という展開自体は
    誤りではない** — 誤っていたのは**その展開しか無いと読んだこと**である。⚠ **番号は動かさない**
    (親プランの状態キャッシュが本 entry を名指すため)。教訓は判断ログ 12。
11. **L15: (a′-1) の行数見積りが上限を +34% 超過した ⟹ 「総額は据え置く」を撤回する (2026-08-03)**:
    実測 **1070 行** (4 ファイル、SoT は在庫 §11.5) に対し §8.2 の見積りは **480–800 行**で、
    しかも **(a′-1) はまだ成立していない** (縛れたのは `V₁` の台の大きさだけ)。⟹ L12 まで採っていた
    「超過は部品の**内数の再配分**として記録し総額は据え置く」(在庫 §10.7 / §7.4) は**事実に合わなく
    なったので撤回**する。⚠ **超過は 1 箇所に局在** (「測度形 ↔ ベクトル形の橋」= 見積り 120–200 に
    対し実測 489) で凸性・端点・台縮小の数学側は見積り通り ⟹ **`V₂` 側の鏡像は同じ橋をもう一度
    通すので主因が倍化する**。次 leg の見積りは**この実測から起こす** (在庫の帯を再利用しない)。
    ⚠ **これは §0.1-1 の `GOAL-CHANGE` に当たらない** — §0 の完了条件は 1 文字も動いておらず、
    動いたのは**残枠の見立て**である。可変枠 L2–L15 を使い切ったので、残る L16–L18 に在庫 §11.6 の
    残件 2 件が入るかが次の判断点になる (§5.1 / §5.2)。
12. **L16: 「本ルートでは載らない」という未検証の否定的判断が、分解の取り方を変えるだけで覆った
    (2026-08-03)**: 判断ログ 10 は目的関数を 1 通りに展開した結果から「一般の `μ₂ > 0` は本機構の
    射程外」と結論し、**その否定を一度も機械にかけずに署名を絞る方針**まで進めていた。実際には
    [GA09] `ga09.txt:587-589` の grouping に切り替えるだけで全重み域に載り、**凸性の数学は新規 0**
    (既存の多相な補題に型引数を入れ替えて渡すだけ) だった。
    ⚠ **CLAUDE.md「A dismissed asset must be dismissed by the compiler, not by prose」の同型再演**
    である — ただし今回 prose で棄却されたのは**資産**ではなく**分解の可能性**であった ⟹ **規約として
    持つ: 「この機構では載らない」という否定的判断も、資産の棄却と同じ強度の反証 (1 行を書いて
    コンパイラに落とさせる) を要求する**。⚠ 実際に効いた反証は在庫 §11.6 (ii) の**型検査 1 本**
    (転置インスタンスが `ConvexOn` の受け口を通ることの確認) で、コストは 1 行だった。
    ⚠ **これは §0.1-1 の `GOAL-CHANGE` に当たらない** — §0 の完了条件は 1 文字も動いておらず、
    動いたのは層 3 で立てる**補題の署名の射程**である (絞る方向ではなく**広がる**方向)。
    ⚠ **残件は減っていない** — 全重み化しても (a′-1) は成立せず、縛れたのは依然 `V₁` の台の
    大きさだけである (§5.2 / 在庫 §12.4)。
