# Verified Information Theory Textbook ロードマップ 📚

> Cover & Thomas *Elements of Information Theory* を骨格に Lean 4 + Mathlib で **標準 B (proof done = 0 sorry ∧ 0 @residual)** を狙う上位 index。
> 章状態 + 残壁 + 戦略遷移のみ。各 seed の statement / 規模 / publish 履歴は `docs/<family>/<topic>-moonshot-plan.md` が SoT、検証 doctrine は CLAUDE.md「検証の誠実性」+ `docs/audit/audit-tags.md`、wave 別実装履歴は `git log` が SoT。

## ゴール

Cover & Thomas (2nd ed.) **Ch.2–12, 15, 17** を Lean 形式化された定理から生成される教科書として publish。成果物 3 層 = (1) Verified library (`InformationTheory/`) + (2) Typed RV API (`H(X)` / `I(X;Y)` 等の書き味) + (3) markdown / LaTeX 原稿。

## scope-out

**章単位** (元から外す):
- Ch.6 Gambling / Ch.14 Kolmogorov Complexity / Ch.16 Portfolio Theory
- Ch.13 LZ complexity 詳細 (LZ78 漸近最適性は in)

**章内部分 scope-out** (2026-05-26 judgment #10):
- **Ch.9 Shannon-Hartley Nyquist 2W-DOF**: (c) 数学的深さ の真の壁、C&T 自身が厳密証明せず。`IsTwoWDegreesOfFreedom` degenerate-def の凍結を確定
- **Ch.9 AWGN operational 符号化定理 (n-letter achievability + converse)** — **2026-06-12 に両側とも genuine closure 済、もはや scope-out ではない (converse = judgment #27、achievability = judgment #29)**。`情報容量 = 操作的容量` の等価性 (Shannon 雑音通信路符号化定理の Gaussian 版)。**converse `awgn_converse` + achievability `awgn_achievability` ともに完全 sorryAx-free** (`#print axioms` = `[propext, Classical.choice, Quot.sound]`、2026-06-27 再確認)。converse 3 壁 (mi-bridge / multivariate-mi / continuous-mi-chain-rule) はすべて false-wall overturn で closed、achievability は continuous AEP / sphere packing / Gaussian random codebook / continuous joint typicality を `isAwgnTypicalityHypothesis` (580 行 genuine assembly、load-bearing predicate なし・regularity precondition のみ) で discharge — **当初「achievability 側は (a) 量の壁 = continuous AEP / random coding / sphere packing」と査定していた `AWGN/Walls.lean` 3 shared sorry 補題 + X-input kernel-measurability gap も commit `c44be725` で AWGN family 実 sorry 全閉鎖**。settled-facts → [`shannon/awgn-facts.md`](shannon/awgn-facts.md)。Ch.9 ③ は単一文字容量に加え **operational 符号化定理 (achievability + converse) として publish 可**。残る AWGN scope-out は Shannon-Hartley Nyquist 2W-DOF (下記、(c) 真の壁) のみ
- **Ch.10 Rate Distortion**: pmf-form ✅ で C&T 10.5 acceptable。measure-form 凸性 + n-letter 規定歪み converse は genuine closure 済 (判断ログ #20)、operational achievability 等の残りは別途
- **Ch.11 Chernoff Information converse / Hoeffding interior body**: C&T §11.9 自身が Sanov `≐` heuristic (textbook rigor で converse は不要)、MacKay/Yeung/El Gamal-Kim は触れもしない。**Chernoff converse は achievability `chernoff_lemma_achievability` ✅ の片割れ。**proof-done 達成** (`Chernoff/Converse.lean`、root wire 済): headline `chernoff_converse` sorryAx-free (`[propext, Classical.choice, Quot.sound]`) + 独立 honesty 監査 PASS (tier-1)、commit `b7595c43`/`3c609c47`。Phase A (pmf 変分恒等式) + Phase B (Sanov 下界 `sanov_ldp_lower_bound_pointwise` 配線 + P₂-摂動) 完了。詳細 `docs/shannon/chernoff-converse-plan.md`**。(**Cramér Phase C は #25/#26 で genuine closure 済 = `cramer_lower_boundary_unconditional` sorryAx-free、もはや scope-out ではない**)
- **Ch.13 LZ78 M3 (achievability) / M4 (converse)**: 当初は research-level scope-out と見ていたが両壁とも genuine closure 済 (M3 = Q_k grafting `c22f2d5`、M4 = fiber-card brick `bd28e0e`)、headline `lz78_asymptotic_optimality_with_greedy` は proof done = sorryAx-free
- **Ch.15 MAC / Broadcast / Relay / Wyner-Ziv main**: Slepian-Wolf + Wyner-Ziv convexity body で「Distributed Source Coding」mini-chapter として publish (El Gamal-Kim も SW を独立章扱い)
- **Ch.17 EPI 一般版 / Brunn-Minkowski body / Fisher chain legacy**: `_gaussian_saturation` は実質 Gaussian variance additivity (3 行 corollary) で EPI と呼ぶのは name laundering → `entropyPower_gaussian_additivity` リネーム。**一般 EPI は #18 で完全無条件 closure 済 (`entropyPowerExt_add_ge_unconditional` sorryAx-free)、もはや scope-out ではない**。**CT 17.9 Minkowski determinant も完成済 = `minkowskiDeterminantInequality` (`MinkowskiDet.lean`、sorryAx-free・全 `@audit:ok`、`#print axioms` 標準 3 公理)** — Gaussian additivity から導出した新規 ✅ (promote 完了)

## 完成判定 (DoD 2 段階 = 標準 B)

- **type-check done** (commit OK): `lake env lean <file>` 0 errors、`sorry` warning は `@residual(<class>:<slug>)` 付き
- **proof done = 標準 B** (完成): 上記 + 0 `sorry` + 0 `@residual`

**仮説の区別**: regularity-hyp (full-support / `IsFiniteMeasure` / `Var > 0` / measurability) は precondition で proof done と両立。**load-bearing-hyp (証明の核心を抱える `*Hypothesis` predicate) は禁止** — 該当箇所は `sorry` + `@residual` で表現する (詳細: CLAUDE.md「検証の誠実性」、移行: [[sorry-based-migration]])。

## 章対応進捗 (scope-out 反映後)

状態凡例: ✅ = 主定理 proof done / 🟢 = scope 内全完成 (scope-out 後の残 frontier 込み判定) / 🟢ʰ = honest hyps 付き / 🟡 = 部分達成 / ✖ = scope-out。

| Ch. | 章 | 状態 | 代表 (proof done) | 残 frontier (scope 内) |
|---|---|---|---|---|
| 2 | Entropy / MI / DPI | ✅ | `entropy`, `mutualInfo`, `mutualInfo_chain_rule`, `condMutualInfo`, `mutualInfo_le_of_postprocess` (DPI), `condEntropy_pi_chain_rule` (memoryless), `fano_inequality_measure_theoretic` | — |
| 3 | AEP | ✅ | aep_ae, aep_inProbability, typicalSet, stronglyTypicalSet | — |
| 4 | Entropy Rate / SMB | ✅ | entropyRate, shannon_mcmillan_breiman, birkhoff_ergodic_ae, BackwardMartingale | — |
| 5 | Data Compression | ✅ | `shannonCode_expected_length_bounds` / `kraftSum_le_one_of_uniquelyDecodable` (McMillan) ✅、`arithmeticCode_expected_length_bounds` / `arithmeticCode_prefix_free` / `arithmeticCode_unique_decodable` ✅、Huffman weak `huffmanLength_kraft_*` ✅、Hyp1 (SwapNormalization) 無条件 genuine ✅、**Huffman 強形最適性 `huffmanLength_optimal` genuine ✅ (2026-05-30、Cover-Thomas 5.8.1、無条件・hypothesis なし、`#print axioms` = `[propext, Classical.choice, Quot.sound]` sorryAx 非依存、独立監査 `@audit:ok`)** | — (cost-level pivot で closure) |
| 6 | Gambling | ✖ scope-out | — | — |
| 7 | Channel Capacity | ✅ | `shannon_noisy_channel_coding_theorem_general_full`, `channel_coding_feedback_converse`, `shannon_converse_single_shot` (Verdú-Han 単発) | strong converse asymptotic (`Pe → 1`, R > C) は未着手 deferred plan (sorry 不在の future work); Draft `ConverseGeneralComplete` 2 sorry は **置換確認済→retract-candidate 化 (2026-06-10)**: production `_memoryless_pure`/`_strong` が結論一致 + sorryAx-free、Draft 側 consumer-0 dead |
| 8 | Differential Entropy | ✅ (1-var) / ✅ (n-var subadd) | `differentialEntropy_gaussianReal`, `_le_gaussian_of_variance_le`, KLDivContinuous, **n-var subadditivity `jointDifferentialEntropyPi_le_sum` genuine ✅** (2026-05-29、0/0 sorryAx 非依存、監査 OK。`withDensity_map_equiv` helper + `pi_withDensity_fin` を self-build、前回 withdrawal は誤診) | — |
| 9 | Gaussian Channel | 🟢 (① ✅ / ② achiever+converse+per-coord+L-WF1+L-WF2 全 genuine ✅ / ③ operational achievability+converse 両側 genuine ✅) | **① 単一文字情報容量 `awgn_capacity_closed_form_genuine` = (1/2)log(1+P/N) genuine ✅** (0/0、sorryAx 非依存、監査 OK) + hypothesis-free MI 後継 ✅ + ParallelGaussian 電力制約 lintegral pivot ✅ + F-1 / L-PG0 / L-WF1 / L-WF2 全 discharge。**② genuine ✅ (2026-05-29 achiever+converse、2026-06-13 L-WF2)**: achiever (judgment #13) + converse closure。converse residual #5 `parallelOutput_joint_logDensity_integrable` (旧 multivariate-mi 壁) は **3 者独立再裁定で self-buildable と確定 → 多変量 mixture density lift で genuine closure** (全 `@audit:ok`、n=0 退化 honest)。**L-WF2 (water-filling 最適性) も genuine closure 済**: `KKT.isWaterFillingOptimal_of_kkt` (共通 KKT 乗数 `λ=1/(2ν)` tangent 上界 `Real.log_le_sub_one_of_pos` 直接 + 相補スラックネス、sorryAx-free、独立監査 `@audit:ok`、judgment #28)。`#print axioms parallel_gaussian_capacity_formula_minimal` = `[propext, Classical.choice, Quot.sound]` (**sorryAx-free = proof done**、唯一の入力は L-WF1 `h_kkt` precondition で IVT-dischargeable)。教訓: loogle 0 件は壁の必要条件であって十分条件でない (`feedback_independent_wall_recheck`) | **② Parallel Gaussian water-filling: 全要素 genuine = proof done ✅** (achiever + converse + per-coord 閉形式 + L-WF1 + L-WF2、headline sorryAx-free)。**③ operational 符号化定理 (achievability `awgn_achievability` + converse `awgn_converse`) も両側 genuine ✅** (2026-06-12、ともに sorryAx-free、judgment #27/#29)。Shannon-Hartley Nyquist 2W-DOF のみ scope-out ((c) 真の壁) |
| 10 | Rate Distortion | ✅ (pmf-form scope) + measure-form 凸性 + n-letter 規定歪み converse genuine | `rate_distortion_achievability` (Shannon), `rate_distortion_converse_single_shot/_specified`, `rateDistortionFunction_convexOn` (measure 形 genuine, `RateDistortionConvexity.lean:390`), `rate_distortion_converse_n_letter_singleLetter` (n-letter genuine, `RateDistortionConverseNLetter.lean:673`、proof done) | n-letter single-letterization の MI superadditivity は self-build で genuine closure 済 (判断ログ #20) |
| 11 | Statistics | 🟢 (scope 内) | `stein_converse_finite_n` / `stein_achievability` / `stein_strong_law` / `sanov_ldp_upper_bound` / `sanov_ldp_equality` / `tvNorm_le_sqrt_klDiv` (+`_div_two` sharp Pinsker) / `conditionalStronglyTypicalSlice_mass_ge` (Csiszár cond. method of types) ✅、Chernoff **achievability** `chernoff_lemma_achievability` + **converse** `chernoff_converse` ✅ (sorryAx-free、tier-1 監査)、Hoeffding sandwich Tendsto + `hoeffding_tradeoff_exp` ✅、Cramér 大偏差下界 `cramer_lower_boundary_unconditional` ✅ (sorryAx-free、#25/#26) | `hoeffding_tradeoff_with_hypothesis` tier-4 marker retro-tag。Chernoff **converse** `chernoff_converse` proof-done (sorryAx-free + 独立監査 PASS、commit `b7595c43`/`3c609c47`)。Hoeffding interior は **scope-out** (dead/一部 false-as-stated)。**Cramér Phase C は closure 済で scope-out 解除** |
| 12 | Maximum Entropy | ✅ | `entropy_le_log_card`, `entropy_eq_log_card_iff`, `entropy_le_gibbs_of_constraints`, `expFamily_maximizes_entropy_of_KKT` | — |
| 13 | Universal Coding | ✅ (scope 内、漸近最適性 headline は **proof done = sorryAx-free** — M3/M4 壁 2本とも closed (`bd28e0e`)、`h_bdd_above` は内製 discharge 済 (`a1ae108`)) | Arithmetic ✅ (expected_length_bounds / prefix_free / unique_decodable)、LZ78 M1 converse `lz78TokenCode_entropyD_le_expectedLength` ✅ (UD-object 定長 + McMillan 期待値)、漸近最適性 entry_point `lz78_asymptotic_optimality_with_greedy` は **proof done = sorryAx-free** (`#print axioms = [propext, Classical.choice, Quot.sound]`): 符号長 `lz78GreedyEncodingLength` を genuine longest-prefix parse (`lz78PhraseStrings`、`c·bitLength c |α|`) に def-fix (commit `5d08566`) → M3 achievability `lz78Greedy_achievability_ae` (Q_k grafting、commit `c22f2d5`) + M4 converse `lz78Greedy_converse_ae` (fiber-card brick、commit `bd28e0e`) の壁 2本とも closed、base 汎用 combine 補題 (`lz78_asymptotic_optimality` 系) も sorryAx-free。`h_bdd_above` 上界は内製 discharge して headline signature から除去 (定数 rate 上界、commit `a1ae108`)。FINAL completion 監査 `@audit:ok` (commit `bd28e0e`、独立 subagent) | LZ78 M3 (achievability) / M4 (converse) は **closed** (当初は research-level scope-out と見ていたが、M3 = Q_k grafting `c22f2d5`、M4 = fiber-card brick `bd28e0e` で genuine closure)。旧 FALSE per-block `IsLZ78ZivCombinatorialCore` 撤回済 |
| 14 | Kolmogorov | ✖ scope-out | — | — |
| 15 | Network IT (DSC mini-chapter) | 🟢 | Slepian-Wolf 完全 ✅ (achievability corner + 4 converse、0/0/0)、**Slepian-Wolf Full Rate Region achievability `slepian_wolf_full_rate_region_achievability` ✅ (2026-05-30、proof done、`#print axioms` sorryAx-free、honesty audit 全 `@audit:ok`)** — 3-bound rate region 全域 (`H(X|Y)<R_X`,`H(Y|X)<R_Y`,`H(X,Y)<R_X+R_Y`) を random binning + joint typicality decoder で達成、Wyner-Ziv convexity body ✅ (4 file 0/0/0)、judgment #3 circular 7 本 (WZ converse cluster) は scope-out で物理削除済 (commit `f67ec8a`、retro-tag moot) | — (judgment #3 circular 7 本は decl ごと削除済で retro-tag 不要。MAC / BC / Relay / Wyner-Ziv main は **scope-out** 済) |
| 16 | Portfolio | ✖ scope-out | — | — |
| 17 | Inequalities | 🟢 | `han_inequality` / `shearer_inequality` / `loomis_whitney` / `brascamp_lieb_finset` / `jointEntropySubset_submodular` (polymatroid) / `tvNorm_le_sqrt_klDiv` (+`_div_two` sharp Pinsker) / `edgeBoundary_entropy_sharp` (hypercube) / `stam_convex_fisher_bound_gaussian` / `heatFlowEntropyPower_continuousWithinAt_zero` (heat-flow path) ✅、Stam Step 1 ✅、`entropyPower_gaussian_additivity` 系 ✅、`minkowskiDeterminantInequality` (CT 17.9、Gaussian additivity 由来) ✅、**一般 EPI 完成 ✅ (2026-06-08、`epi-unconditional-moonshot-plan` 完了)**: 完全無条件 `entropyPowerExt_add_ge_unconditional` (ℝ≥0∞、precondition 0、sorryAx-free、監査 OK) + 実数 a.c.+有限版 `entropy_power_inequality_of_ac` (sorryAx-free) + 密度版 `entropy_power_inequality_of_density` (sorryAx-free)。3-case dispatch + route T (truncation+Gibbs+DCT) で genuine closure | — (legacy Cover-Thomas Stam-bridge route は **2026-06-11 物理削除済**: `stamToEPIBridge_holds` sorry + 露出 28 decl を削除、**EPI family の実 sorry = 0**。route-T 後継が textbook goal を sorryAx-free 達成、削除後 full build clean 3480 jobs)。**詳細 SoT → `docs/shannon/ch17-inequalities-status.md`** |

**規模見積** (再導出: `rg "^\s*sorry\s*$|:= by sorry$|by sorry$|:= sorry$" InformationTheory/ | wc -l`、`\bsorry\b` の数百は docstring 散文込みの過大値): **2026-06-13 時点で実 `sorry` タクティク = 0** (canonical regex + broadened 検索の両方で 0、残る `sorry`/`@residual` テキストは全て docstring 散文)。最後まで残っていた scope-out の dead 残置 8 件 (Huffman 偽述語 2 = GitHub issue #4 + RD witness/discharge・Hoeffding interior×2・Channel per-summand/converse 6、いずれも consumer-0 で production が sorryAx-free supersede) を 2026-06-13 に削除して達成。**2026-06-20 再確認: full build success (3471 jobs) + sorry warning ゼロで実 sorry = 0 を再裏取り**。**Ch.13 LZ78 headline (`lz78_asymptotic_optimality_with_greedy`) は 2026-06-21 に proof done = 完全 sorryAx-free を達成** (`#print axioms = [propext, Classical.choice, Quot.sound]`、機械再確認): 符号長 def-fix (commit `5d08566`) 後、M3 achievability (`lz78Greedy_achievability_ae`、Q_k grafting、`c22f2d5`) + M4 converse (`lz78Greedy_converse_ae`、fiber-card brick、`bd28e0e`) の壁 2本とも closed、`h_bdd_above` 上界も内製 discharge して signature から除去 (commit `a1ae108`)。FINAL completion 監査 `@audit:ok` (commit `bd28e0e`)。「sorryAx-free」≠「無条件 proof done」の区別が残るのは Ch.9 Nyquist `IsTwoWDegreesOfFreedom` 等の既タグ済 load-bearing のみ (scope-out で `@residual` 開示済)。scope 内 headline は全て無条件 proof done (一般 EPI 含む)。

## 「Mathlib 壁」5 分類 (scope 内 frontier に残る型)

- **(a) 量の壁** (低、未構築): well-understood、Mathlib に補題不在で一から数百行。Slepian-Wolf Phase E error bound (Ch.15)。(Ch.9 AWGN operational 符号化定理の continuous AEP / sphere packing も本型で judgment #11 で scope-out していたが、2026-06-12 に achievability+converse 両側 genuine closure = judgment #27/#29、もはや壁ではない)
- **(b) 解析の壁** (中〜高): 計算体系自体を建てる型。EPI Stam Step 2 (Ch.17) — Rioul 2011 §II-C より score-cond-mean identity + total variance decomposition で ~100 行 density-level computation (従来 ~300 行 PR 級見積りより小)、再見積もり後 scope 内に戻す余地あり
- **(c) 数学的深さ** (高、真の壁): Nyquist 2W-DOF (Ch.9、scope-out 確定)
- **(d) 実は選択** (de-circularize 済): "ROI 無し" の婉曲表現で「壁」と呼ばれていたもの、現在は honest 開示のみ
- **(e) scaffolding-was-false** (中〜大): 既存縮約 predicate / 定義が偽 / 循環 / 不健全で discharge 不能、genuine 構成を一から組み直す。Huffman mergedMeasure card-2 redesign (Ch.5)

## frontier (scope 内 closure 必要)

- **Ch.5 Huffman 強形**: **完了 ✅ (2026-05-30)** — `huffmanLength_optimal` (Cover-Thomas 5.8.1、無条件) を **cost-level pivot** で genuine closure。FALSE な per-symbol depth identity (`MergedHuffmanAuxIdentHypothesis` 等) を経由せず、`huffmanCost_eq_of_prob_multiset` (cost は確率多重集合のみで決まる = label/carrier 非依存、`huffmanCost_step` + `s.card` 強帰納で genuine、最適性を前提しない非循環証明) で weak-form 帰納核の `h_L'_link` を置換。`#print axioms` sorryAx 非依存、独立監査 `@audit:ok`。FALSE wall 5 件は `@audit:closed-by-successor(huffman-cost-level-optimality)` 化 (collapse 補題は consumer 0 で削除済)、旧 weak-form wrapper 3 件は `@audit:superseded-by`。**もはや scope 内 frontier ではない**。plan: `docs/shannon/huffman-cost-level-optimality-plan.md`。判断ログ #16
- **Ch.8 n-var DiffEnt subadditivity**: genuine ✅ (2026-05-29 closure、もはや壁ではない)
- **Ch.9 ② Parallel Gaussian water-filling**: **全要素 genuine = proof done ✅** (achiever + converse + per-coord 閉形式 + L-WF1 + L-WF2)。converse residual #5 joint log-density integrability は多変量 mixture density lift で genuine closure 済 (3 者独立再裁定で wall→plan reclassify 後)。**L-WF2 (water-filling 最適性) も genuine closure 済** — `KKT.isWaterFillingOptimal_of_kkt` (sorryAx-free、独立監査 `@audit:ok`、judgment #28; 2026-06-13 に「open/sorry-routed と一旦訂正 → 同日 genuine 着地」)。headline `parallel_gaussian_capacity_formula_minimal` は sorryAx-free、唯一の入力は L-WF1 `h_kkt` precondition (IVT-dischargeable)。③ operational 符号化定理も両側 genuine closure 済 (achievability `awgn_achievability` + converse `awgn_converse`、2026-06-12、judgment #27/#29)。**もはや scope 内 frontier ではない**
- **Ch.15 Slepian-Wolf Full Rate Region**: **完了 ✅ (2026-05-30)** — Phase D/E (decomposition + 4 expectation bound) に加え Phase F (E.5 squeeze + F.3 headline assembly) を genuine closure。`slepian_wolf_full_rate_region_achievability` proof done、sorryAx-free。**もはや scope 内 frontier ではない** (旧「Phase E 確率上界収束のみ残」は誤り — 実際は headline assembly が未実装だった、2026-05-30 実コード確認で訂正)
- **Ch.17 一般 EPI headline**: **完了 ✅ (2026-06-08、`epi-unconditional-moonshot-plan`)** — 完全無条件 EPI `entropyPowerExt_add_ge_unconditional` (ℝ≥0∞、precondition 0) を **3-case dispatch** (両 a.c.=case-1 split / 混合=gateway / 両特異=RHS 0) + **route T** (無限分散枝 = truncation+Gibbs+DCT、2026-06-07 「無限分散 a.c. 壁」を FALSE WALL と判定して closure) で sorryAx-free 化。実数 a.c.+有限版 `entropy_power_inequality_of_ac` も sorryAx-free (実数は型壁で完全無条件化不可、a.c.+有限が honest 限界)。legacy Cover-Thomas Stam-bridge route (`entropy_power_inequality` / `stamToEPIBridge_holds` sorry + 露出 28 decl) は **2026-06-11 物理削除済** — route-T 後継が完全 supersede。**EPI family の実 sorry = 0、scope 内 frontier ではない**。**詳細 SoT → `docs/shannon/ch17-inequalities-status.md`**。判断ログ #18
- **legacy migration は完了済 (frontier ではない)**: InformationTheory に active な `@audit:suspect(`/`@audit:staged(`/`@audit:defer(` declaration タグは **0 件** (paren-pattern 確認、bareword ヒットは全て過去 migration の docstring 内文字列参照)。旧 roadmap「32 件」は stale claim
- **未追跡 marker retro-tag (残)**: 2026-06-20 再確認で大半解消。Ch.9 `IsTwoWDegreesOfFreedom` は既に code タグ済 (`@residual(plan:whittaker-shannon-partial-moonshot-plan) @audit:retract-candidate(load-bearing-predicate)`、load-bearing predicate を honest 開示)、Ch.11 `hoeffding_tradeoff_with_hypothesis` は declaration 自体が実在せず (削除済)。**Ch.15 judgment #3 circular 7 本 (WZ converse cluster) も 2026-06-22 再確認で全 decl 削除済を確定** — scope-out commit `f67ec8a` (MAC+BC+Relay+WZ-main 60 file 削除) で除去済、`🟢ʰ` marker もコード全体ゼロ、active circular `:= h` defect ゼロゆえ retro-tag は moot (`hoeffding_tradeoff_with_hypothesis` と同パターン)。Ch.5 `EqualizingPerm*` 削除済、Ch.17 `IsStamToEPIBridge` は 2026-05-30 sweep 処理済。**未追跡 marker retro-tag は全件解消済**

## 教科書原稿 (層 3) / 次の一手

- **Ch.2 パイロット起動済 ✅ (2026-05-30)**: `docs/textbook/ch02-entropy.md` — 検証済 33 declaration (Entropy/condEntropy/MI/condMI/chain rule/DPI/Fano) を §2.1-2.10 の prose に紐付け。パイロットで判明した原稿生成の課題: (i) 値の型不一致 (`ℝ≥0∞` vs `ℝ`、橋渡し定理に `.toReal`)、(ii) Markov 定義の表層差 (compProd 分解形)、(iii) n 変数 chain rule の右辺長大、(iv) **章↔file が 1:1 でない** (Ch.2 中核 `condEntropy` が `InformationTheory.MeasureFano` namespace、roadmap 代表名 `Fano` は実在せず実体は `fano_inequality_measure_theoretic`)。✅ **代表名照合済 (2026-06-22)**: 章対応表「代表」列の全 backtick 識別子を実 declaration と機械照合し不一致を修正。Ch.2 概念ラベル (`Entropy`/`Fano` 等 7 件) → 実 decl 名 (`entropy` / `fano_inequality_measure_theoretic` 等)、Ch.7 stale 名 2 件 (`_feedback_complete` → `channel_coding_feedback_converse`、`strong_converse_singleShot` → `shannon_converse_single_shot`) + `...` 省略を実名 `shannon_noisy_channel_coding_theorem_general_full` に展開、Ch.8 helper 名 `withDensity_map` → `withDensity_map_equiv`。残る他章の代表 theorem 名は全て実在確認済 (`...` 省略 / namespace / Mathlib 名 / hypothesis 名 / memory slug `feedback_independent_wall_recheck` は照合対象外)
- genuine ✅ で原稿起動可能: **Ch.2 (✅ pilot) / 3 / 4 / 5 (Huffman weak + 強形最適性) / 7 / 8 (1-var) / 9 (単一文字 AWGN 容量公式 + max-ent converse) / 10 (pmf-form) / 12 / 15 (DSC) / 17 inequality 群**
- **次の一手候補**: (i) Ch.2 パイロット課題を踏まえた原稿生成テンプレ確立 (型注釈 / 代表名照合) → 他完成章 (Ch.3/4/7/12) 横展開、(ii) Ch.5 Huffman 強形最適性 genuine closure を原稿に反映 (新たに publish 可能化)

## 判断ログ

戦略遷移のみ記録。wave 別 publish 集計 / 各 seed の publish 履歴 / sub-predicate 詳細は `git log` + plan / inventory が SoT。

1. **2026-05-18〜05-20 並列実装 11 wave → 累計 ~50k 行 publish**: 18 seed の pass-through 主定理 + sub-predicate body discharge を 5〜22 並列で駆動
2. **2026-05-20 Cramér CLT closure** ⚠️ **虚偽と判明 (→ #19 で訂正、2026-06-10)**: 記載の `cramer_lower_at_cgfDeriv_unconditional` / `CramerCLTClosure.lean` は実在せず Cramér CLT closure は未達。当時は Mathlib CLT を tilted ambient に適用して下界達成と信じられていた
3. **2026-05-21 標準 B 確定 + Mathlib 壁 4 分類**: 「完成 = 定理そのものの無条件機械検証」を doctrine 化。Mathlib 壁を (a)/(b)/(c)/(d) 4 種に taxonomy 化、MAC/BC/Relay の circular DEFECT 7 本を発見し全 honest-terminal に修復
4. **2026-05-21 A 群着手 — 「壁なし 3 件」分類が誤りと判明 + (e) scaffolding-was-false 追加**: 既存縮約 predicate が 0-sorry で偽 / 循環 / 不健全命題を隠していたことを実装で発見 (Huffman SwapNorm 偽鎖 / Chernoff `IsBayesErrorPerTiltLowerBound` 偽 / LZ78 converse 計画路 不健全)
5. **2026-05-21 Chernoff 標準 B 完成 (主張) — judgment #9 で撤回**: `chernoff_lemma_tendsto_holds` は当時 publish と記載したが、当該名 declaration は不在で `chernoff_lemma_achievability` (one-sided liminf) のみ完成と確認
6. **2026-05-21 Huffman / LZ78 完遂 frontier 確定 (両者 research-level)**: ~35 エージェントの徹底 localization の末、両者の残核を研究レベルと確定 (Huffman: `mergedMeasure` card-2 redesign / LZ78: per-block Ziv core FALSE + Barron a.s. lift research-level)
7. **2026-05-25 DoD 2 段階確定 + load-bearing hyp 禁止** (CLAUDE.md 改訂): proof done = 0 sorry ∧ 0 @residual を新バーに、撤退口は `sorry` + `@residual(<class>:<slug>)` のみ、`*Hypothesis` predicate は honesty defect として禁止
8. **2026-05-26 ロードマップ整理 (上位 index 化)**: 旧 #1–#15 + 旧 #17–#26 (Huffman/LZ78 grind 詳細) + seed カード詳細 + FLAW-VACUOUS 詳細を削除し、本ファイルを「章状態 + frontier + 戦略遷移」の上位 index に縮約 (994→~85 行)
9. **2026-05-26 9 並列 chapter audit → 章別実態棚卸**: roadmap の章状態 / 代表定理 / frontier を実コードと突合 (Ch.12 ✅ 昇格、Ch.7/10/11 表記補正、Ch.9 tier-5 + Nyquist degenerate-def 列挙、未追跡 markers 5 件特定)
10. **2026-05-26 scope 縮小決定 (主流教科書調査 + 規模測定)**: 原稿パイロット (Ch.2/7/12) 起動可能規模に縮小するため scope-out 拡張。**根拠**:
    - **Ch.15 部分 scope-out** — Slepian-Wolf + WZ convexity body (~2.3k 行 ✅) で「Distributed Source Coding」mini-chapter として publish 可 (El Gamal-Kim も SW 独立章扱い)、MAC/BC/Relay/WZ main (Draft 11.6k 行) は捨てる
    - **Ch.11 Chernoff Information converse scope-out** — C&T Thm 11.9.1 の "proof" 自身が Sanov `≐` heuristic、MacKay/Yeung/El Gamal-Kim は触れもしない。Sanov LDP ✅ から将来 1 段落 corollary
    - **Ch.17 EPI 一般版 scope-out + リネーム** — 現 `_gaussian_saturation` は実質 Gaussian variance additivity (3 行 corollary)、EPI と呼ぶのは name laundering → `entropyPower_gaussian_additivity` リネーム必須、一般 EPI は sorry+@residual。**bonus**: CT 17.9 Minkowski determinant が Gaussian additivity から直接導出可能 (新規 ✅ 候補)。**留保**: Rioul 2011 §II-C で Stam Step 2 が ~100 行 density 計算 (従来見積り ~300 行 PR 級より小) と判明、再見積もり後 keep 余地
    - **Ch.13 LZ78 M3/M4 → closed** — 当初 research-level scope-out と見ていたが両壁とも genuine closure (M3 Q_k grafting `c22f2d5` / M4 fiber-card brick `bd28e0e`)、headline proof done = sorryAx-free
    - **Ch.10** — pmf-form ✅ で C&T 10.5 acceptable。measure-form 凸性・n-letter converse は後に genuine closure (判断ログ #20)、operational achievability 等の残りは別途
    - **Ch.9 Shannon-Hartley Nyquist scope-out** — (c) 数学的深さ の真の壁、C&T 自身が厳密証明せず
    - scope-out 総量 ~22k 行 Draft + ~250 residual 解放、scope 内 frontier は数百 sorry オーダー
11. **2026-05-29 Ch.9 AWGN 単一文字容量 genuine closure + ③ operational 符号化定理 scope-out**:
    - **① 単一文字情報容量 genuine 完成** — `awgn_capacity_closed_form_genuine = (1/2)log(1+P/N)` (0 sorry / 0 residual、`#print axioms` sorryAx 非依存、独立監査 OK)。max-entropy converse 込み。Ch.9 を 🟡 → 「① ✅ / ② frontier」に更新
    - **ParallelGaussian 多次元 false-statement defect 修正** — 電力制約集合が Bochner `∑ᵢ ∫(xᵢ)²∂p ≤ P` で非可積分入力 (Cauchy 等) が `integral_undef→0` で紛れ込み converse を偽命題化。AWGN と同型の lintegral pivot (`parallelGaussianPowerConstraintSet` + bridge) で修正、3 file 8 site 全置換、独立監査 OK (3 decl `@audit:ok`)
    - **load-bearing wrapper `mutualInfoOfChannel_gaussianInput_closed_form` retire** — base `AWGN.lean` の `h_bridge`-form load-bearing wrapper を削除、唯一の genuine consumer (`AWGNMIBridge.awgn_mi_gaussian_closed_form_of_primitives`、bridge を primitives から genuine 構築済) に log 代数を inline。後継 `_closed_form'` も併存。sorry 化は genuine 証明上の fake wall になるため**不可**、削除が正しい retirement
    - **③ scope-out 決定** — operational 符号化定理 (n-letter achievability + converse) の continuous AEP / random coding / sphere packing / 連続 MI chain rule / Fano は (a) 量の壁が大量。原稿パイロット優先方針 (judgment #10) で Nyquist と同じく future work に隔離。Ch.9 は単一文字容量公式の章として publish 可。honest sorry+@residual は marker として保持
    - **次セッション = AWGN 継続 (② Parallel Gaussian water-filling)** — `IsParallelGaussianPerCoordRegularity` の load-bearing hyp 解消。依存先は Ch.8 共有壁 `jointDifferentialEntropyPi_le_sum` (多変量 diffent subadditivity、sorry-routed) + channel↔RV MI decomp。着手は `docs/shannon/multivariate-diffentropy-subadditivity-plan.md` + L-PG1 discharge plan の tractability 評価から
12. **2026-05-29 (続) Ch.8 n-var subadditivity + Ch.9 ② multivariate-mi 壁 2 連続 genuine closure** (orchestrator pattern、複数 wave):
    - **Ch.8 n-var subadditivity genuine** — `jointDifferentialEntropyPi_le_sum` を 0/0 sorryAx 非依存で closure。前回 (2026-05-25 Wave 3) の honest withdrawal は **gap 誤診** (「generic `withDensity_map` Mathlib 不在」を自作不能と扱ったが、`MeasurableEmbedding.map_withDensity_rnDeriv` テンプレで ~13 行 self-build 可能だった)。`withDensity_map_equiv` + `pi_withDensity_fin` 新設、bridge は 2 変数版と同型 structural、独立監査 OK (6 decl `@audit:ok`)。**教訓: Mathlib「壁」(loogle Found 0) は鵜呑みにせず独立再判定 — 「不在 ≠ 証明不能」**
    - **Ch.9 ② `wall:multivariate-mi` genuine closure** — achiever MI per-channel 加法性 `MI(product input × parallel channel) = ∑ per-coord MI` を closure。core = `gaussianProductInput_compProd_parallelGaussianChannel_eq_pi` (compProd-of-`Measure.pi` factorization、`Measure.pi_eq` box 普遍性) + `lintegral_fin_nat_prod_eq_prod` (lintegral n-variate Fubini 自作、`Kernel.pi`/`lintegral_pi` Mathlib 不在を induction で迂回)。これも独立 wall 再検証で「self-buildable (~120-200 行)」と判定 → closure。監査 OK (3 decl `@audit:ok` sorryAx 非依存)
    - **② frontier 縮小** — headline `parallel_gaussian_capacity_formula_minimal` の bundled `h_bridge_per_coord` を per-coord `h_perCoordMI` に分解 (strictly more honest)。残 residual は AWGN single-channel MI finiteness `awgn_mutualInfoOfChannel_ne_top` (`@residual(plan:l-pg1-discharge)`、① の `.toReal` 経路から closeable でない真の residual) + per-coord MI 値 + max_ent の channel↔RV decomp。**② の bottleneck = AWGN single-channel MI bridge family (`IsAwgnOutputGaussian`/`IsAwgnMIDecomp` load-bearing) の closure**
    - **次セッション target** — AWGN single-channel MI bridge family closure (② 残 residual の共通 bottleneck)。finiteness `awgn_mutualInfoOfChannel_ne_top` (llr integrability、~moderate) と per-coord MI 値の genuine 化。`max_ent` の `h_decomp` は新設 factorization machinery で mechanical lift の可能性 (再評価)
13. **2026-05-29 (続々) Ch.9 ② achiever side 完全 genuine + headline honesty 是正** (orchestrator pattern、複数 wave):
    - **AWGN single-channel MI finiteness genuine + false-statement edge 修正** — `awgn_mutualInfoOfChannel_ne_top` を `klDiv_ne_top` (AC + llr integrability、analytic core は `ContChannelMIDecomp.lean` に既存) で closure。独立 wall 再検証で「self-buildable ~75 行」判定 (judgment #12 教訓の適用、loogle Found 0 鵜呑み回避)。**実装中に command-line で発見: 元 signature は `N≠0` 仮定欠落で `N=0`(決定論 channel)+連続入力(`P≠0`) のとき joint が対角グラフ上 = `p.prod q`-null → `klDiv=⊤` で命題が genuinely 偽**。`N≠0` を追加 (regularity precondition、headline は既に `∀i,(Nᵢ:ℝ)≠0` 保持) → residual 完全消滅・genuine。在庫 §E の「N=0 で finite」予測は誤りだった (退化境界の verbatim 確認義務の実例)
    - **headline load-bearing hyp の tier-5 honesty 検出 → 是正** — `parallel_gaussian_capacity_formula_minimal` / `isParallelGaussianPerCoordRegularity_of_pieces` が `@audit:ok` 付きのまま load-bearing open hyp (`h_bdd_global`/`h_multivar_decomp` = 相関入力 max-entropy converse、`h_perCoordMI` = per-coord achiever value) を保持。独立監査で **tier-5 (load-bearing + name laundering) 確定**、旧 `@audit:ok` は誤付与。`h_bdd_global`/`h_multivar_decomp` を drop → `sorry+@residual(wall:multivariate-mi)`、`h_perCoordMI` は **wall ではなく既存 genuine closed form `mutualInfoOfChannel_gaussianInput_closed_form'` を in-body 適用で discharge** (Q=0 退化も `klDiv_self` で genuine)。achiever_mi 完全 genuine 化、残 residual は converse 2 件のみに収斂。**教訓: 「load-bearing hyp を per-coord に分解 = strictly more honest」(judgment #12) は誤り — 分解しても load-bearing なら tier-4/5、honest なのは sorry or genuine closure**
    - **converse self-buildable 判定** — 残 `wall:multivariate-mi` (bddAbove/max_ent) を独立 wall 再検証 → self-buildable (~285-430 行、1-D template の `Fin n` lift、真の Mathlib 壁ではない)。closure plan `docs/shannon/parallel-gaussian-converse-closure-plan.md` (7 Phase、core = `h_decomp` の `Fin n→ℝ` lift)。⚠ 退化ガード: constructor が `P:ℝ` 無制約 → `P<0` で trivially 成立する退化定義悪用 (tier-5) 注意
    - **次セッション target** — converse closure (Phase 0 = `CountableOrCountablyGenerated (Fin n→ℝ)` instance 確認 → Phase 2 `h_decomp` lift core)
14. **2026-05-30 honesty 是正 sweep (orchestrator pattern、複数 wave)** — 2 frontier (Ch.5 Huffman collapse / Ch.17 EPI Stam Step 2) を attack した結果、両者とも proof done 前進ではなく **honesty 是正** に帰結 (frontier が偽 or 真の壁だった)。さらに副産物で EPI family の tier-1 タグ系統的誤付与を発見:
    - **Ch.5 collapse 補題は FALSE statement** — `collapseLabel_huffmanLengthAux` を tie-order 独立 invariant (~150-250 行) で閉じる想定で着手したが、着手前 small-case シミュレーションで **statement 自体が偽**と判明 (a=1,b=6,p=4 の反例、`{a}→{a,b}` の colex 変化が同確率 group の tie-break を反転 → 他 leaf depth が動く)。独立監査が反例を step-by-step 検算 + `Colex.toColex_lt_toColex_iff_max'_mem` で confirm。`@audit:defect(false-statement)`、standalone (伝播ゼロ)。旧判断ログ #3 の「true-but-hard 壁」診断は誤り。**教訓: 撤退ライン設計時に「閉じない」と「偽」を区別する verify step (small-case sim) を必須化** — 偽命題の証明に 200 行費やす前に検出
    - **Ch.17 EPI Stam Step 2 は load-bearing defect → scope-out** — read-only advisor が `entropy_power_inequality_via_body` の `IsStamCauchySchwarzOptimal` 仮説引数 = tier-5 load-bearing defect を発見 (`@audit:ok` の裏)。shared sorry `stam_step2_density_wall` に降格して signature から除去 (tier 5→2)。Rioul「~100 行」見積りは Fisher/score/density 計算 Mathlib 全不在で誤り、scope-out 確定
    - **EPI family @audit:ok 12 件降格** — `#print axioms` sweep で `entropy_power_inequality` (headline) 含む 12 declaration が transitive wall 経由で sorryAx 依存 = proof done でないのに `@audit:ok` 保持と判明、降格。**教訓: file-local `rg sorry` は transitive sorry を捕捉できず、conditional wrapper (sorryAx-free) と headline-routing wrapper (sorryAx 依存) が同型 signature でも proof-done 状態が逆になる。tier-1 タグ付与は `#print axioms` 必須**。load-bearing hyp → shared sorry migration では rewrite 後 wrapper の `@audit:ok` 繰越が defect を温存する経路 (migration brief に「rewrite 後 `#print axioms` 再判定」を入れる)
    - **stale claim 訂正** — roadmap「legacy migration 32 件」は実際 0 件 (paren-pattern 確認)、「EqualizingPerm 4 件 retro-tag」は declaration 削除済で対象外、と verbatim 確認で訂正
    - **次セッション target** — Ch.5 は `MergedHuffmanAuxIdentHypothesis` discharge を leaf-merge / length-multiset reduction へ pivot する新規設計 (per-symbol collapse は dead-end 確定)。Ch.17 は `EPIStamStep3Body` の `IsStamTotalExpectation` load-bearing path を `stam_step2_density_wall` へ統合 (wall 集約) が候補
15. **2026-05-30 (続) Ch.17 wall 集約 closure + Ch.5 Huffman merged-identity 経路 FALSE 確定** (orchestrator pattern、Wave 1 read-only 評価 → Wave 2 並列実装 → Wave 3 並列独立監査):
    - **Ch.17 wall 集約 = honesty 是正完了** — judgment #14 の候補だった `IsStamTotalExpectation` load-bearing path を実装。planner が Wave 1 で「`h_conv : IsStamScoreConvolution` は body 未使用 (dead hyp)、Step3Body chain は end-to-end から孤立 (cross-file consumer 0)、honest 経路は既に `entropy_power_inequality_via_body` に wired 済」を verbatim 確認 → load-bearing predicate **全廃可能**と判定。implementer が孤立 island (predicate 2 + structure + theorem ~12) を削除、残 sorry を単一 `stam_step2_density_wall` に収束。独立監査が `@audit:ok` 4 件 (仮説入力 arithmetic bridge) を `#print axioms` sorryAx-free 確認 + wall を呼ぶ wrapper 3 件を `@residual` 継承確認 → **judgment #14 の「wall 経由 sorryAx 依存なのに `@audit:ok`」前科の正しい修正版**と sign-off
    - **Ch.5 Huffman merged-identity 本体も FALSE 確定** — Wave 1 で proof-pivot-advisor が「collapse FALSE は parent predicate FALSE を**含意しない** (collapse は a-global-min も `_h_sibling` も課さない真に弱い前提)、`MergedHuffmanAuxIdentHypothesis` 本体の真偽は OPEN、small-case verify が前段未了」と正しく指摘 (judgment #14 の「hard と false を区別」原則を逆向きに適用 = 「hard を false と誤診」リスクの警告)。orchestrator が実 Lean 定義 (`groupKey`/`huffmanStep`/`huffmanLengthAux`) 忠実な simulator で網羅検証 → **本体も FALSE** (反例 `[1,2,1,1], a=0, b=2`、全強前提 + **a,b が実際に first-merged** でも x=0 で恒等式失敗)。旧 docstring の「strong precondition (first-merged) で救った」主張自体が誤りと判明。独立 auditor が手計算で反例再現 + simulator↔Lean 定義忠実性を照合し confirm。`HuffmanMergedIdentificationHypothesis` (measure-level) も同一 statement で FALSE。4 wall を `@audit:defect(false-statement) @audit:retract-candidate(false-hypothesis)` 化 (新 reason `deterministic-colex-merge-instability` は auditor が `false-hypothesis` 正規 vocab に統一、機構は docstring 散文へ)。**EPI と違い code consumer 0 で伝播ゼロ・降格 0** (merged-identity 経路は宣言のみで未 wire-in だった)
    - **pivot 方向を機械検証で grounded 化** — depth-level identity は 2532/61254 precond case で失敗、しかし **cost-level merge identity は全 case 成立 (失敗 0)**。最クリーン形 = multiset-level 漸化式 `cost(huffmanLengthAux s) = cost(huffmanLengthAux s'') + (x1.p+x2.p)` (huffmanStep の actually-merged pair、(a,b) global-min framing 不要、near-definitional / 失敗 0)。Huffman 強形 optimality は per-symbol depth identity ではなく **tie-invariant cost 漸化式 + `s.card` 帰納**で genuine 化すべき。証拠 = `docs/shannon/verify/merged_huffman_aux_ident_counterexample.py`
    - **教訓** — (1) 「hard と false を区別」原則は**両方向**に適用せよ: false を hard と誤診 (judgment #14 collapse) だけでなく、hard/OPEN を false と早断 (今回 advisor が警告した parent predicate) も同じ誤り。新 frontier は着手前に small-case verify。(2) 偽の per-symbol/structural identity でも、その上位の **tie-invariant 不変量 (cost)** は真でありうる — dead-end の正体を切り分けると pivot が grounded になる。(3) simulator で statement 真偽を判定するときは **simulator semantics と実 Lean 定義の verbatim 照合**が反例の有効性の前提 (auditor が独立照合)
    - **次セッション target** — Ch.5 は cost-level merge 漸化式の skeleton 起草 (planner) → multiset-level `huffmanStep` cost 不変量を立て `s.card` 帰納で `huffmanLength_optimal_*` を genuine discharge。Ch.17 は cleanup (`isStamScoreConvolution_intro` orphan 確認 → retract) のみで frontier 解消、教科書原稿 (層 3) パイロット着手可
16. **2026-05-30 (続々) Ch.5 Huffman cost-level pivot genuine closure + Ch.2 教科書パイロット + Ch.17 orphan 解決** (orchestrator pattern、Wave 1 plan+inventory → Wave 2 並列実装 → Wave 3 独立監査 → Wave 4 Phase R retract):
    - **Ch.5 Huffman 強形最適性 genuine closure ✅ (proof done)** — judgment #15 の cost-level pivot を実装。`huffmanLength_optimal` (Cover-Thomas 5.8.1、無条件・hypothesis なし) を `#print axioms` sorryAx 非依存で closure、独立監査 8 declaration 全 `@audit:ok`。**鍵 = orchestrator が plan 最大リスク (C3 carrier-crossing) を解消する洞察を brief に注入**: planner は C3 を「first-merge 同定が前任 dead-end と同根」と懸念したが、orchestrator が「`huffmanCost は確率多重集合のみで決まる (label/carrier 非依存)`を `huffmanCost_step` + `s.card` 強帰納で証明 — huffmanStep の確率最小2値選択で tie でも値の和は多重集合で確定、s'' の確率多重集合も確定 → 最適性を前提しない非循環証明」と指摘し、implementer がそのまま closure。per-symbol depth identity の FALSE とも `relabel_det` の carrier-crossing 限界とも独立。**教訓: 偽の構造的 identity の dead-end は、上位の tie-invariant 不変量 (cost) を「多重集合のみで決まる」形に持ち上げると非循環で閉じる (judgment #15 (2) の具体的実現)**
    - **Phase R retract** — genuine 後継が存在するので FALSE wall 5 件を `@audit:closed-by-successor(huffman-cost-level-optimality)` 化 (collapse 補題 `collapseLabel_huffmanLengthAux` は term-consumer 0 で削除)、旧 weak-form wrapper 3 件 (`huffmanLength_optimal_with_hypotheses` / `_aux_with_hypotheses` / `_modulo_aux_ident`) を `@audit:superseded-by` 化。retract 後 `#print axioms huffmanLength_optimal` 再確認で sorryAx 非依存維持を gate。残: combined wrapper 2 件 (`huffman_combined_hypothesis_holds` / `huffman_chain_combined_hypothesis_holds`) が `@audit:defect` のみで retract-candidate 未付与 = dead 残置 (軽微 follow-up)
    - **Ch.2 教科書パイロット ✅** — `docs/textbook/ch02-entropy.md` (§教科書原稿)。roadmap 代表名の stale 発見 (`Fano` 等)
    - **Ch.17 orphan 解決** — `isStamScoreConvolution_intro` は orphan ではなく genuine `@audit:ok` helper (`isStamScoreConvolution_symm`:406 / `epi_via_stam_main`:459 で live use)。retract 不要、Ch.17 wall 集約 frontier は整合状態で closure
    - **次セッション target** — (i) 教科書原稿テンプレ確立 + 他完成章 (Ch.3/4/7/12) 横展開、(ii) roadmap 代表定理名の実コード照合、(iii) Ch.17 一般 EPI / `stam_step2_density_wall` は long-term (Mathlib 壁)、現状 honest sorry 据置

17. **2026-05-30 (続々々) Ch.17 一般 EPI 「本気で攻める」再 attack — scope-out 診断の部分是正 + 壁誤分類確定** (orchestrator pattern、Wave 1 独立再評価+在庫 → Wave 2 plan → Wave 3 gateway 実装 → Wave 4 独立監査 → Wave 5 Phase 3 → Wave 6 defect 監査):
    - **契機**: ユーザーが「Ch.17 を本気で攻める」を選択。doctrine (`feedback_independent_wall_recheck`、Ch.8/Ch.9 で「壁→self-buildable」前科 2 回) に従い盲目的 300 行ではなく Wave 1 を独立 tractability 再判定に充当。
    - **Wave 1 (proof-pivot-advisor + mathlib-inventory 独立収束)**: 2 壁 (`wall:stam-step2-density` / `wall:debruijn-integration`) の共通根 = 「独立和畳み込み密度の点ごと微分可能性 + score logDeriv 表現」。両者「全体 scope-out 維持だが gateway atom が決定的判定」で一致。+ 過去 inventory の `condExp_indep` 不在 claim が loogle bare-query false-negative と判明 (cross-term self-buildable)。
    - **Wave 3 GO**: gateway atom `convDensity_add_differentiable` (`EPIConvDensity.lean`) + cross-term `score_cross_term_eq_zero` (`EPIScoreCrossTermOrth.lean`) を **genuine ✅** (0 sorry)。決め手 = `hasDerivAt_integral_of_dominated_loc_of_deriv_le` (parametric integral) で `HasCompactSupport` 不適合を回避、撤退ライン L-EPIW-1-β (truncation 壁) は**空振り**。**scope-out 根拠「density 微分可能性 Mathlib 全不在」は誤り**。Wave 4 独立監査が `#print axioms` sorryAx-free + 7 仮説 regularity (`h_diff` は per-x integrand、循環なし) を確認し 4× `@audit:ok`。
    - **Wave 5/6 = honesty 是正 (Phase 3 NO-GO の正体)**: 壁1 本体着手で「`stam_step2_density_wall` の真の blocker は Mathlib 壁ではなく **`IsStamCauchySchwarzOptimal` の signature defect (false-statement)**」と判明。密度 `fXY` 無制約全称量化 + `fisherInfoOfMeasureV2` の measure 引数無視 → `fXY=fX⋆fY` 制約欠落で命題が **FALSE** (反例 fX=fY=𝒩(0,1)/J_X=J_Y=1, fXY=𝒩(0,1/100)/J_sum=100; `100≤1/2` 偽)。Wave 6 独立監査が closed-form `fisherInfoOfDensity(𝒩 m v).toReal=1/v` で verbatim 再検算 + consumer chain 追跡 (genuine 詐称 0、FALSE 述語は sorry 経由で honest 隔離、headline 非経由) + `@residual(wall:stam-step2-density)` → `@audit:defect(false-statement) @audit:closed-by-successor(epi-wall-reattack-plan)` 是正。同型 defect `IsStamCondExpCSHyp`/`IsStamInequalityResidual` 追加検出 (計 3 述語)。
    - **判断 #14/#15 の診断是正**: 「Ch.17 EPI Stam Step 2 = 真の (b) 解析壁 / scope-out 確定」は **誤診断**。実態は (e) scaffolding-was-false (predicate signature drift)。Phase 2.B で predicate から regularity を剥いだ際に `IsStamCauchySchwarzOptimal` が「Fisher info 値の純算術 predicate」に退化し convolution 関係を喪失したのが根。**もはや research-level Mathlib 解析壁ではなく tractable な述語 redesign**。
    - **教訓**: (1) 「Mathlib 壁」claim は signature honesty を着手前に verbatim 照合すれば計画段階で defect 検出可能だった (`fisherInfoOfMeasureV2` が measure 無視する定義が drift の根)。(2) gateway atom 起点の「決定的判定 dispatch」は有効 — 1 atom を実際に建てに行くことで「density 微分可能性 全不在」の誤診断を覆した (Ch.8/Ch.9 と同型)。(3) 真壁と思っていたものが defect だった = scope-out の見積りベース判断は signature 照合で覆りうる。
    - **次セッション target** — owner-task: 3 述語一括 signature pivot (`IsStamCauchySchwarzOptimal` + `IsStamCondExpCSHyp` + `IsStamInequalityResidual` に `fXY =ᵐ convDensityAdd fX fY` + 密度 regularity 注入) + ripple (`entropy_power_inequality_via_body` / `stam_inequality_via_predicate_optimal` / `stam_convex_fisher_bound_gaussian`) → 既存 gateway を consumer として Blachman attach (Phase 3) → Phase 4 (壁2 per-time de Bruijn、heat-eq IBP は別 atom で要 tractability 判定)。plan: `epi-wall-reattack-plan.md` (Phase 3-pre として signature pivot 追記要)

18. **2026-06-08 Ch.17 一般 EPI 無条件化完成 ✅ + 2026-06-10 検証 & destale** (judgment #17 の signature pivot から無条件 dispatch route へ pivot した最終局面):
    - **完成の実体** — judgment #17 の Stam-bridge signature pivot を直接続けるのではなく、**EPI を別ルートで無条件化**して frontier を閉じた。`entropyPowerExt` (ℝ≥0∞ 値) への再型付け + **3-case dispatch** (両 a.c. / 混合 / 両特異) で `entropyPowerExt_add_ge_unconditional` (precondition = 可測+独立のみ) を sorryAx-free 化 (`EPI/Unconditional/DispatchFull.lean`、5 decl 独立監査 all-OK)。case-1 a.c. 枝の **無限分散部は 2026-06-07 に「無限分散 a.c. 壁 (dispatch 路)」を FALSE WALL と判定 → route T (truncation+Gibbs+DCT) で closure**、有限分散部は smoothing closure。実数版は型壁 (`EReal.exp_coe` が a.c.+有限を要する) で完全無条件化不可、`entropy_power_inequality_of_ac` (a.c.+有限) が honest 限界。台帳 SoT = `epi-facts.md` 行 25/27/28/29 (再検証レシピ、f767105 まで current)
    - **roadmap/ch17-status は stale だった** — 2026-06-10 に `#print axioms` で 6 headline を一括検証: `entropyPowerExt_add_ge_unconditional` / `entropy_power_inequality_of_ac` / `_of_density` / `_add_ge_of_finite_variance` / `isStamInequalityHyp_of_primitives` / `parallel_gaussian_capacity_formula_minimal` すべて sorryAx-free 確認 (注: `parallel_gaussian_capacity_formula_minimal` は 2026-06-13 に L-WF2 の load-bearing 仮説発覚で一旦 sorryAx 依存になったが、同日 L-WF2 genuine closure で再び sorryAx-free — judgment #28)。一方 roadmap Ch.17 行 + ch17-status は「一般 EPI = 唯一の残 frontier」のまま残存 (epi-facts.md のみ current だった)。本 #18 + ch17-status destale で整合化
    - **A: legacy Stam-bridge route の supersession 明示** — `stamToEPIBridge_holds` (`EntropyPower/Inequality.lean`、唯一の bridge sorry) + legacy headline `entropy_power_inequality` に `@audit:superseded-by(epi-unconditional-moonshot-plan)` を付与 (tier-3 bookkeeping、`@residual` は honest marker として保持)。`@audit:closed-by-successor` は audit-tags で tier-4 legacy ゆえ不採用。実 sorry は legacy route 全体で `stamToEPIBridge_holds` 1 個のみ (他 23 decl は 0-local-sorry の transitive consumer、Cover-Thomas 露出として温存)
    - **実 sorry 棚卸の訂正** — `rg "\bsorry\b"` は docstring 散文を含み過大 (497 行)。**実 `sorry` タクティクは 26 個**で、内訳 = Draft scope-out (~16: Cramér Phase C / Hoeffding interior / Huffman FALSE walls / RateDistortion measure-form / ChannelCoding strong converse asymptotic) + AWGN operational ③ scope-out (8: Walls×5 + Achievability/Converse/ConverseDischarge) + EPI 2 (`stamToEPIBridge_holds` legacy + `differentialEntropyExt_eq_condEntExt_add_klDiv` = finiteness-free 版で docstring 自身が「ℝ≥0∞ 減算不在で証明不能確定、有限 fibre 版が後継」と明記)。**scope 内の真の未完成 headline は 0 件**

19. **2026-06-10 (B) scope-out 低リスク 4 家系の独立壁再判定 (read-only Explore 3 並列 + #print axioms 機械検証)** — judgment #18 の (B) メニューを「壁を額面で受けない」doctrine で再判定。**結果 (Cramér gateway 反証後の最終): 14 sorry のうち 10 が dead/完成済 (壁ではない)、2 が genuine wall (Cramér、false-statement)、2 のみ re-attackable (Ch.10 RD pmf 形、未着手)**:
    - **Ch.7 converse 2 sorry = dead** (`ChannelCodingConverseGeneralComplete.lean:386,449`)。production `channel_coding_converse_general_memoryless_pure`/`_strong` が **結論完全一致 + sorryAx-free**、Draft 側 consumer-0。retract-candidate 化済 (commit `006d776`)。
    - **Ch.10 RD achievability 2 sorry = dead** (`RateDistortionAchievabilityPhaseE.lean:123` / `...Discharge.lean:311`)。production headline `rate_distortion_achievability` (sorryAx-free 機械確認) + `_witness_form_strong`/`_partial_discharge_strong` が supersede、consumer-0。
    - **Ch.11 Hoeffding interior 5 sorry = 全 dead** (`HoeffdingInteriorBody.lean:232,310` / `HoeffdingInteriorGradientBody.lean:264,316,337`)。production `hoeffding_tradeoff_exp` (sorryAx-free 機械確認) が IVT+exp-family Pythagorean bypass で完成済、interior body を一切経由せず。「research-level」は過大評価。S3/S4/S5 は `{alpha lam : ℝ}` 無条件で statement-too-strong (偽の疑い)。
    - ⚠️ **Ch.11 Cramér 2 = re-attack 失敗、genuine wall + false-statement と判明 (2026-06-10 gateway-atom 反証)**: Explore の「~30-50 行配線」verdict は **single-route 過大評価**だった。lean-implementer が着手→ブリーフ前提を反証: (1) **一般 `a` で statement が偽** (退化境界反例 Bernoulli(1/2), lam=0, a=0.9 → `0 ≤ -D(0.9‖0.5)<0`、真は最適 tilt `a=deriv(cgf)lam` のみ)、(2) tight 境界は CLT 必須 (interior producer は非 tight `-(λa-cgf+λε)` のみ、CLT boundary producer 不在)、(3) **import cycle** (`InfinitePiTiltedChangeOfMeasure`→`CramerLC2PhaseC` 依存で producer 配線方向が逆)。→ Cramér Phase C は **scope-out 妥当 (genuine wall)**。root 2 定理 (`CramerLC2PhaseC.lean:167` / `Cramer.lean:468`) は **under-hypothesized = false-statement**、独立 honesty-auditor が `@audit:defect(false-statement)` をマーク (signature 制限は consumer 2 + tendsto entry_point に ripple、provisional)。**教訓: scope-out 再判定の「not-a-wall」verdict を docs に記録する前に gateway-atom (実装着手) で確認すべきだった (退化境界 1 点代入で偽が出た)**。
    - **genuine re-attackable (壁が低い、未着手)**: **Ch.10 RD 2** — `RateDistortionConvexity.lean:161` 凸性は一般 Measure 形だと Mathlib 真ギャップ (`klDiv` joint convexity loogle Found 0) だが pmf 形限定で `convexOn_klFun` 配線 ~350-500 行、`RateDistortionConverseNLetter.lean:298` は凸性後 ~150 行。**注意: この見積も Explore 由来、着手時に gateway-atom + 退化境界 verify 必須** (Cramér の前例)。`CramerLC2PhaseC.lean:132 tilted_lower_from_predicate` は consumer-0 dead。
    - **#2 (2026-05-20) の虚偽訂正**: judgment #2 が「`cramer_lower_at_cgfDeriv_unconditional` で Cramér 下界達成」と記載するが、当該 decl も `CramerCLTClosure.lean` も **実在しない** (`find`/`grep` 0-hit)。Cramér CLT closure は **未達**。`cramer-chernoff-clt-closure-moonshot-plan` + 親 `infinitepi-tilted-rn-discharge` の false-DONE banner も訂正済 (commit `7d90ad2`)。
    - **未調査** (→ **判断ログ #21 で実施済、2026-06-11**): Ch.9 AWGN operational ③ 8 sorry。棚卸し結果 = 6 genuine 壁確認 + per-letter-integrability 壁覆し (genuine closure) + #7 据置。
    - **dead 掃除候補 (retract-tag or 削除)**: Ch.10 ach 2 + Hoeffding 5 (+ Ch.7 完了)。EPI dead-island 物理削除 (前 handoff Follow-up) も pending。
    - **教訓**: 「壁→self-buildable / 壁→既完成」前科 (Ch.8/9/17) に Ch.7/10/11 が追加。scope-out menu は鵜呑みにせず着手前に独立再判定 + 機械検証必須を再確認。

20. **2026-06-10 Ch.10 RD 「本気で攻める」— 凸性 genuine closure + converse under-hypothesized 修正** (judgment #19 の唯一の re-attackable を着手、orchestrator pattern):
    - **契機**: #19「re-attackable = Ch.10 RD のみ」をユーザー選択。handoff 警告「着手時 gateway-atom + 退化境界 verify 必須 (Cramér 前例)」に従い Wave 1 を独立 tractability 再判定に充当。
    - **sorry #1 凸性 genuine closure ✅** — `rateDistortionFunction_convexOn` (`RateDistortionConvexity.lean:390`、commit `96fd6a2`)。handoff の「pmf 形限定 ~350-500 行」見積りは過大、**measure 形のまま**既存 `klDiv_map_le` (一般 pushforward DPI) を selector-forget 経路で再利用して closure。gateway `klDiv_joint_convex` (`:283`、純 KL joint convexity) + 自作 disjoint-slice 加法性 `klDiv_add_of_mutuallySingular` (`:181`)。`#print axioms` sorryAx 非依存、独立監査 7 decl `@audit:ok`。**「壁→既存資産で配線」前科 (Ch.8/9/17) に Ch.10 凸性追加**。`cause:single-route` (pmf/log-sum のみ想定で DPI 再配線を未探索)。
    - **sorry #2 converse = under-hypothesized 修正 → genuine closure ✅** — `rate_distortion_converse_n_letter_singleLetter` (`RateDistortionConverseNLetter.lean:673`、commit `e73513c` + 監査 `cc18726`)。migration が load-bearing `h_jensen_antitone` を除去した際に regularity precondition を落とし偽化していた (反例 n=1/M=2/|α|=4/D=0 → `R=log4>log2`; 独立性欠落 n=2/X₁=X₂/D=0 → `log2>(1/2)log2`) のを `hD`+`hindep` (iIndepFun source) で true-as-framed 化し、**同セッションでそのまま genuine closure**。残壁とされた MI superadditivity を self-build。**proof done** (0 sorry / 0 residual、`#print axioms` sorryAx 非依存、独立監査 10 decl `@audit:ok`)。自作核 = gateway `condEntropy_pi_le_sum_condEntropy_per_letter` (`:447`、`H(X^n|X̂^n) ≤ ∑H(Xᵢ|X̂ᵢ)`、初回で通った) + core `mutualInfo_superadditive_of_indep` (`:570`) + n-way Jensen `rateDistortionFunction_jensen_uniform` (`:277`、binary 凸性から)。`hD`/`hindep`/`[Nonempty β]` (0 consumer) は plain precondition。`cause:signature-drops-constraint` (修正) + `cause:single-route` (壁覆し)。
    - **MI superadditivity 壁の覆し** — 残壁とされた `∑I(Xᵢ;X̂ᵢ) ≤ I(X^n;X̂^n)` (独立 source) は gateway 評価で **self-buildable** と判明。「`ParallelGaussianPerCoord.lean` の "D-1 wall" family と同根」は analogy による誤継承 (over-estimation): 独立 source の superadditivity は entropy ブロックが全て in-project に実在 (`entropy_pi_eq_sum_of_indep` + gateway + MI↔entropy bridge `mutualInfo_eq_entropy_sub_condEntropy`)。逆向き lemma `mutualInfo_le_sum_per_letter_of_memoryless_strong` (memoryless subadditivity) と direction が違うだけで同ブロックで閉じた。**「壁→既存資産で配線」前科に Ch.10 converse も追加** (凸性に続き 2 件目、しかも converse は faith 継承された sibling wall tag を gateway 評価で覆した)。
    - **教訓**: (1)「壁→既存資産で配線」前科に Ch.10 (凸性 + converse) を追加。(2) **migration の load-bearing hyp 除去は bundle 内 regularity precondition を分離残置しないと under-hypothesized を生む** (退化境界 verify が検出口)。(3) **sibling の wall tag を継承する前に、in-project entropy machinery で transposed direction を grep せよ** — 逆向きが既に証明済なら同ブロックで閉じうる。plan: `docs/shannon/rate-distortion-convexity-plan.md` + `rate-distortion-converse-plan.md`。
    - **Ch.10 RD frontier 完了** — #19 の唯一の re-attackable (Ch.10 RD pmf 形 2 sorry) は **両 sorry 共に genuine closure** で完了 (#19 の re-attackable backlog 消化済)。

21. **2026-06-11 Ch.9 AWGN operational ③ 8 sorry 棚卸し — 6 genuine 壁確認 + per-letter-integrability 壁覆し** (judgment #19「未調査」item をユーザー選択、read-only Explore 4 並列仕分け → solo lean-implementer closure → 独立監査):
    - **契機**: #19 の「Ch.9 AWGN ③ = 独立再判定未実施」をユーザー選択。judgment #11 で scope-out 済 (「未調査」ではなく査定済)。Ch.10 RD doctrine (壁を額面で受けない・transposed grep・gateway atom + LOC 見積・dead 判定) を全 agent に強制。
    - **仕分け結果 (8 sorry)**: **6 が genuine 壁を確認** — `awgn-continuous-aep-gaussian` (連続 SMB)・`awgn-random-coding-bound` (random coding AEP)・`awgn-power-constraint-honest` (χ² SLLN concentration)・`awgn-continuous-mi-chain-rule` (non-iid mixture chain rule)・`wall:multivariate-mi` (連続-Y MI 有限性 + 循環依存)・`awgn-mi-bridge-plan` (mixture→compProd factorization ~50-100 行)。各 missing piece を名指し、transposed grep でも in-project genuine machinery で埋まらず壁ラベル正当。**Ch.10 RD と対照的に over-estimation guard が大半 TRUE 壁を確認** — AWGN は連続設定で Mathlib に differential entropy / 連続 AEP/typicality が丸ごと不在ゆえ scope-out (judgment #11) は概ね妥当。
    - **per-letter-integrability 壁覆し ✅ (genuine closure)** — `awgnPerLetterIntegrability_holds` (`AwgnWalls.lean`、commit `98f2643`)。docstring が「n-dim SMB/differentialEntropy 不在=壁」と過大主張だったが、実体は **1 次元の有限 Gaussian 混合 log-density 可積分性** (volume on ℝ)。import cycle で gateway (`integrable_log_gaussianPDFReal_gaussianReal`) 再利用不可ゆえ inline self-build (pushforward→混合密度等式 + sup bound + log-abs quadratic 包絡 + `Integrable.mono'`、M=0/N=0 退化は singular→rnDeriv=0 で honest)。`#print axioms` sorryAx 非依存、独立監査 7 decl `@audit:ok`。**これは ② Parallel Gaussian の最後の residual `parallelOutput_joint_logDensity_integrable` (旧 `wall:multivariate-mi`、judgment #13) を「多変量 mixture density lift」で覆した手法の sibling** — 未再検査だった。**「Gaussian 混合 log-density 可積分性」型の壁は self-buildable パターンとして確立 (2 例目)**。
    - **#7 `awgn_achievability` rewire は据置** — Explore の「~5-10 行 delegate」見積りは誤り。`AchievabilityDischarge`(完成済 `isAwgnTypicalityHypothesis`) が `Achievability`(headline) を import する**逆方向依存**で、wire には 580 行 assembly の下層移設 or headline の上層移設 (Main.lean ripple) が必要。sorry は既に honest tag 済。**教訓: Explore の「自明 rewire」verdict も import 方向を verbatim 確認すべき (CLAUDE.md 依存方向確認義務の実例)**。
    - **AWGN ③ frontier 更新** — 8 → 7 sorry (per-letter closed)、残 7 は全 WALL (scope-out 維持)。stale wall ref (`ConverseDischarge.lean` の per-letter/markov を壁として列挙する docstring 表) を同 commit で訂正。memory `project-scope-out-recheck` 更新。**(2026-06-12 追記: この「残 7 全 WALL」判定は後続 #27 で converse 側 3 壁が全 false-wall overturn → `awgn_converse` 完全 sorryAx-free となり STALE。残壁は achievability 側のみ)**。

22. **2026-06-11 (B) dead 掃除 — 8 dead sorry retract-tag、doctrine 検証で「8 dead」を「4 dead + 4 false-as-stated」に精緻化** (handoff (B) メニュー item 1 をユーザー選択、solo):
    - **契機**: handoff「次の一手」の dead 掃除 (低リスク・確実) をユーザー選択。#19 で dead 認定された Ch.10 RD ach 2 + Ch.11 Hoeffding interior 5 + Cramér 1 (`tilted_lower_from_predicate`) を retract-tag。doctrine「dead を額面で受けない」で各 decl を独立再検証 (`dep_consumers --transitive` + `#print axioms` 機械確認)。
    - **検証結果 = 8 sorry を 2 群に分離** (#19 の一律「dead」を訂正)。load-bearing fact は機械再導出: 全 8 が consumer-0 (一部 dead-island)、production successor 2 件 (`rate_distortion_achievability` / `hoeffding_tradeoff_exp`) は `#print axioms` で標準 3 公理のみ (propext/Classical.choice/Quot.sound)。
    - **(a) TRUE-but-dead 4 件 → `@audit:retract-candidate(superseded-by-full-discharge)`** (`@residual` 残置): RD `rate_distortion_achievability_witness_form`/`_partial_discharge` (production headline + `_partial_discharge_strong` が supersede)、Hoeffding `isHoeffdingInteriorMinimizer_of_gradient`/`hoeffdingE2_interior_minimizer_via_predicates` (`0 ≤ alpha` 付き TRUE existence、`hoeffding_tradeoff_exp` が IVT+Pythagorean bypass で完成済)。
    - **(b) FALSE-as-stated 4 件 → `@audit:defect(false-statement) @audit:retract-candidate(false-hypothesis)`** (`@residual` 撤去、over-promise 訂正): Hoeffding Gradient 3 (`isHoeffdingInteriorMinimizer_of_lagrange`/`hoeffdingE2_interior_minimizer_via_lagrange`/`csiszar_pythagoras_at_lagrange` — alpha-lam リンク制約欠落で statement-too-strong、退化境界反証: alpha=0→K={P₁}/alpha<0→K=∅/lam非整合→Pythagoras破れ) + **Cramér `tilted_lower_from_predicate`** (#19「dead」を訂正 — dead **かつ** false: `a=M+1` で事象集合空→RHS=0<LHS=C·exp、sibling `cramer_lower_phaseC_partial_discharge` と同 defect family)。#19「Hoeffding S3/S4/S5 偽の疑い」を degenerate-boundary + 構造的証拠 (genuine wrapper `_of_ivt`/`_of_constraint_eq` が欠落制約 `klDivPmf (tilt lam) P₁ = alpha` を仮説に補う) で確証。
    - **dead island 発見** — `isHoeffdingInteriorMinimizer_of_lagrange` (FALSE anchor) は consumer-0 でなく **dead 4-decl island** を anchor: 3 consumer (`_exists_of_lagrange` 同 file + `_of_ivt` (`HoeffdingLagrangeIVTBody.lean`) + `_of_constraint_eq` (`HoeffdingMinimizerAttainment.lean`))。後 2 者は **TRUE signature (欠落制約を仮説に持つ) だが body は false anchor に委譲し仮説を無視** = false 補題の上に build。`hoeffding_tradeoff_exp` が bypass で全 island が dead。物理削除時は 4 decl co-delete + 2 wrapper を honest 直 sorry へ rewire (follow-up)。
    - **教訓** — (1) doctrine「dead を額面で受けない」が #19 の「8 dead」を「4 dead + 4 false」に精緻化、特に Cramér 132 の falseness は #19 で見落とし。(2) consumer-0 verdict も `--transitive` で確認すべき (Hoeffding anchor は 3 consumer の dead-island)。(3) 5 file 全 compile 0 error (docstring 編集のみ、sorry warning 維持)。残: EPI dead-island 物理削除 + Hoeffding false-island 物理削除 (共に follow-up)。

23. **2026-06-11 dead 物理削除 — #22 の retract-tag dead を実削除 (Hoeffding false-island 6 decl + Cramér orphan 1 + EPI route α-ii dead-island 2 file)** (handoff (B) item 1 続き、ユーザー選択、solo):
    - **契機**: #22 が retract-tag した dead を物理削除して frontier を縮小。削除前に各 island を `dep_consumers --transitive` で consumer-0 / dead-island-閉 を再確認 (doctrine「dead を額面で受けない」を削除前検証にも適用)。
    - **Hoeffding false-island 6 decl 削除** (`d129f8c`): GradientBody `isHoeffdingInteriorMinimizer_of_lagrange` (FALSE anchor) + `_exists_of_lagrange` + `hoeffdingE2_interior_minimizer_via_lagrange` + `csiszar_pythagoras_at_lagrange`、cross-file `_of_ivt` (`HoeffdingLagrangeIVTBody.lean`) / `_of_constraint_eq` (`HoeffdingMinimizerAttainment.lean`)。**live helper 温存** (`isHoeffdingTiltMinimal_of_constraint_eq` / `isHoeffdingLagrangeHyp_of_constraint_eq` = production `hoeffding_tradeoff_exp` に繋がる; dep で 4 consumer 確認)、`isHoeffdingMinimizerFullSupport_of_lagrange` (TRUE 構成的) も温存。stale docstring (GradientBody/InteriorBody/CramerLC2PhaseC header) 訂正。`hoeffding_tradeoff_exp` 標準 3 公理維持。
    - **Cramér orphan 削除** (同 commit): `tilted_lower_from_predicate` (consumer-0、false-as-stated)。`IsMeasureInfinitePiTiltedEq` def は別物で温存。
    - **EPI route α-ii dead-island 2 file 削除** (`110b514`): `Monotone.lean` (4 decl: crux i-a `differentialEntropyExt_indep_add_eq_add_klDiv` sorry + mono/top/entropyPower delegate) + `CondEntropyExt.lean` (5 decl: chain-rule sorry + `_of_finite` genuine orphan + helper 3)。両 file の全 decl が consumers を島内に閉じた dead orphan (machine 確認)、route β' (`TruncationLimit/Limit.lean` `_unconditional` 群、@audit:ok sorryAx-free、`entropyPowerExt_add_ge_unconditional` headline に live) が完全 supersede。import 除去 (root + route β' 3 file の vestigial CondEntropyExt/Monotone)、`Core.lean` に transitive-only だった `Truncation.Construction` 直 import を補填。`entropyPowerExt_add_ge_unconditional` 標準 3 公理維持、full build clean (3433 jobs)。
    - **frontier 効果**: 実 sorry タクティク 6 件除去 (Hoeffding 3 + Cramér 1 + EPI 2)、現在 **17 件** (`rg "^\s*sorry\s*$|:= by sorry$|by sorry$|:= sorry$" InformationTheory/ | wc -l`)。#22 follow-up (EPI + Hoeffding 物理削除) 完了。
    - **doctrine 確認**: (1) 削除前 consumer-0 再検証で「false 補題上に build した TRUE-signature wrapper」(`_of_ivt`/`_of_constraint_eq`) を co-delete (anchor-only 削除=compile 破壊 / wrapper 残置=false-premised proof 温存 の両罠を回避)。(2) EPI は genuine @residual (crux i-a) を含むため、route β' 代替性を epi-facts.md 行27/28 (machine) + `Limit.lean` source で独立確認してから削除 (memory「方針 Y park」は smoothing headline の話で、gateway 自体は route β' で proof-done = parked-but-alive でなく superseded を確証)。(3) import 除去後の transitive-import 破損 (`Core.lean:250` `differentialEntropy_le_cross_entropy` unknown) は compile が検出口、直接 import 補填で解消。

24. **2026-06-11 Cramér def-fix — false-statement 2 root に最適 tilt 仮説 `h_deriv` 追加で honest 化 (tier-5 defect → tier-2 sorry+@residual)** (handoff 残メニュー Cramér をユーザー選択、solo、独立監査 PASS、`f62be91`):
    - **契機**: #19 で false-statement defect (`@audit:defect(false-statement)`) と認定された Cramér 下界 2 root の **first-choice def-fix** (handoff「次の一手」item 1)。結論 `-(lam·a - cgf lam) ≤ liminf (1/n) log P[a·n ≤ S_n]` は一般 `a` で偽 (反例 Bernoulli(1/2), lam=0, a=0.9 → LHS=0 / liminf=-D(0.9‖0.5)<0)、最適 tilt `a = deriv(cgf) lam` でのみ真。
    - **修正**: `(h_deriv : deriv (cgf (X 0) μ) lam = a)` を signature に追加し true-as-stated 化。`@audit:defect(false-statement)` 撤去 → `@residual(plan:cramer-chernoff-clt-closure-moonshot-plan)` (root の `sorry` body 残置 = genuine CLT-boundary 壁、honest tier-2)。`dep_consumers` で確定した ripple **7 decl** に `h_deriv` を threading: root #2 `cramer_lower` chain (`cramer_lower_legendre`/`cramer_tendsto` entry_point, `Cramer.lean`) + root #1 `cramer_lower_phaseC_partial_discharge` chain (`_legendre`/`_tendsto` entry_point, `CramerLC2PhaseC.lean`) + `cramer_lower_phaseC_residual_discharge` entry_point (`InfinitePiTiltedChangeOfMeasure.lean`)。wrapper docstring を FALSE-INHERITING → honest-inheriting 書換。3 file `lake env lean` clean (root 2 sorry warning のみ、net -17 行)。
    - **独立監査 PASS** (honesty-auditor): `h_deriv` の **十分性**を 3 退化境界 (lam=0→a=mean で 0≤0 生存 / 最適内点で `lam·a-cgf = D = cramerRate` 一致 / 定数 X) で反証試行も全生存、**deriv-非微分可能トラップ閉** (Y bounded ⟹ cgf は ℝ 上 analytic で `deriv` は真の導関数、空虚充足なし)。`h_deriv` は precondition (最適 tilt 指定) であり load-bearing でない (証明の核 = CLT-boundary 窓質量は `sorry` 残置、`h_deriv` を与えても liminf 下界は出ない)。residual `plan` class は plan 実在 + `a = deriv cgf lam` を狙うので over-promise なし。書込なし (既存 tag 正しい、proof-done でないので `@audit:ok` も付けず)。
    - **frontier 効果**: 実 sorry タクティク数は不変 (root 2 sorry は honest 化のみ、削除でない)。Cramér family の **tier-5 false-statement defect が 0 に** (honesty hierarchy 改善)。次段 = CLT-closure moonshot (`cramer-chernoff-clt-closure-moonshot-plan`、新 file ~135-250 行、Phase 1 median + Phase 3 CLT witness が二大難所、genuine 壁公算、要 gateway 評価)。

25. **2026-06-11 Cramér CLT-boundary closure moonshot 達成 — #24 の「genuine 壁公算」予測を gateway 反証 → Phase 1-6 full closure + 独立監査 PASS** (handoff「次の一手」gateway 評価をユーザー選択、solo lean-implementer ×2 + honesty-auditor、`05ed225`/`9cdc485`):
    - **gateway 反証**: #24 が「genuine 壁公算」とした CLT-closure を、gateway-atom-first で Phase 1 `gaussianReal_Ici_eq_half` (Gaussian median, Mathlib 不在で「一から」) を単独 dispatch → ~62 行・sorryAx-free で**無痛閉**。二大難所の片方が割れ壁予測が覆る。続けて Phase 2-6 を 1 セッション full closure。
    - **誤報の訂正**: 実装 agent が「predecessor 3 file 不在」と 気づき を上げたが**誤報** — `fdd68a3` の昇格 refactor でパス移動しただけ (`Shannon/CramerLC2Discharge.lean` → `Shannon/Cramer/LC2Discharge.lean` 等)。plan 依存 11 シンボルを補題名 grep で全実在確認 (plumbing 健在)。plan の stale パスを訂正。
    - **着地**: `InformationTheory/Shannon/CramerCltBoundaryClosure.lean` (586 行, 0 sorry / 0 @residual, sorryAx-free, `InformationTheory.lean` import 済, full build 2972 jobs clean)。headline `cramer_lower_boundary_unconditional` = 内部最適 tilt `a = deriv (cgf (Y∘eval 0) (infinitePi μ₀)) lam` で residual largeness hyp を**除去**した Cramér 下界。CLT (`tendstoInDistribution_inv_sqrt_mul_sum_sub`) + portmanteau half-line + Gaussian median + `ε→0⁺` collapse。
    - **honesty 設計**: plan 想定の `∀a∀ε` 緩和 predicate `IsTiltedWindowEventuallyLargeC` は **false-as-framed** と判明 → 「証明済」とせず reduction tool に留め、Phase 6 は per-`(a,ε)` core を `a=m` で直接呼んで bypass (false predicate は headline 経路に乗らず)。撤退ライン L-CLT* の「hypothesis pass-through」も CLAUDE.md load-bearing 禁止と抵触するため不採用方針をブリーフ明記 (結果使わず full closure)。
    - **独立監査 PASS** (honesty-auditor, `@audit:ok` 全 10): under-hyp / load-bearing / false-predicate 隔離 / degenerate 濫用 / consumer 字面一致 / sorryAx-free を機械裏取り。`h_coboundedBelow` の**非 vacuous 性** (`P≤1 ⇒ log≤0` 上有界 → `b=0` 充足) を別ファイル検証。
    - **frontier 効果**: 実 sorry タクティク数は**不変 (17)** — consumer root `cramer_lower_phaseC_partial_discharge` (`Shannon/Cramer/LC2PhaseC.lean:165`) は **import cycle** で in-place 書換不可。root の sorry は「閉じた closure asset を指す live residual」に性質変化 (genuine 未達 → closed-but-unwired)。差し替え経路 (cycle 解消 or 別 entry_point) は別 plan。**教訓: 「genuine 壁公算」の human-judgment 予測は gateway-atom-first で必ず反証試行する** (#24 の壁予測は実際は ~62 行 median + 既存 plumbing で割れた)。`cramer-facts.md` 新設 (machine fact + 再検証コマンド)。

26. **2026-06-11 Cramér root 配線 — #25 の closed-but-unwired 2 root を解消し sorryAx-free 化 (実 sorry 17→15)** (子 plan `cramer-root-wiring-plan.md`、inventory→plan→solo lean-implementer ×2→honesty-auditor、`7e4f05a`/`184affa`/`8a8a001`):
    - **構造**: headline は両 root の下流かつ Cramer.lean を transitive import → in-place `exact` は cycle。2 方式で解消。
    - **root A (cycle-break)**: `IsMeasureInfinitePiTiltedEq` def + tilted 機構 7 theorem の計 8 decl を新上流 `InformationTheory/Shannon/CramerBoundaryUpstream.lean` へ verbatim hoist し `Cramer → CramerBoundaryUpstream → CramerCltBoundaryClosure → CramerLC2PhaseC → InfinitePiTiltedChangeOfMeasure` の一直線 DAG 化 → cycle 消滅 → `cramer_lower_phaseC_partial_discharge` を `exact cramer_lower_boundary_unconditional` で discharge。
    - **root B (移設 + transport)**: 一般 iid `cramer_lower` は最上流 Cramer.lean にあり headline を import できない → chain 3 decl (`cramer_lower`/`_legendre`/`cramer_tendsto` entry_point、外部 consumer 0 で純移設可) を新下流 `InformationTheory/Shannon/CramerGeneralLower.lean` へ移設 → iid joint law = `infinitePi (μ.map (X 0))` の double-pullback transport (`iIndepFun_iff_map_fun_eq_infinitePi_map` + `IdentDistrib.map_eq`) で headline へ落とす。**設計 pivot**: inventory の `Y:=id` は ℝ 上 unbounded で headline `h_bdd` 不成立 → `Y:=X 0, μ₀:=μ` で i=0 instance が有界性供給 + `cgf_eval_eq_cgf_base` 直接適用 (cgf 橋 self-build 0 行)。
    - **honesty**: 両 root に `hVar` (非退化分散) を regularity precondition 追加 (`Var=0` 退化のみ除外、窓質量の核は headline 内部 CLT 供給 = load-bearing でない)。独立監査 PASS (`@audit:ok` 12 件: root A/B chain 6 + hoist 3 + legacy `cramer_lower_phaseC_residual_discharge` の `_h_res` を死引数と確認し `closed-by-successor`→`ok` migrate)。transport は `h_indep`/`h_ident` を実体消費する genuine reduction (false implication なし)。
    - **教訓**: 「閉じた asset があるのに sorry が残る」= レイヤ反転 (証明が定理より下流) のサイン。解消は (a) 上流へ hoist (cycle-break) か (b) 定理を下流へ移設、の 2 択。orchestrator は dispatch 前に **transitive import 方向を実測**せよ (plan の in-place 前提は headline が Cramer を transitive import する事実で覆った)。cosmetic follow-up `_h_res` 死引数削除 = **DONE** (2026-06-11、`dep_consumers` で 0 consumer 確認後 signature 除去、sorryAx-free `[propext, Classical.choice, Quot.sound]` 維持)。

27. **2026-06-12 Ch.9 AWGN converse 3 壁すべて genuine closure — `awgn_converse` 完全 sorryAx-free** (judgment #21 の converse 側 frontier を継続、複数セッション):
    - **結果**: converse 経路 `awgn_converse` (`AWGN/Converse.lean`) は **完全 transitively sorryAx-free** (`#print axioms InformationTheory.Shannon.AWGN.awgn_converse` = `[propext, Classical.choice, Quot.sound]`)。converse の 3 Mathlib 壁すべて false-wall overturn で genuine closure。settled-facts → [`shannon/awgn-facts.md`](shannon/awgn-facts.md)。
    - **wall1 mi-bridge** (`awgn-mi-bridge`、per-letter MI = h(Y_i)−h(noise)): `awgn_per_letter_mi_bridge_genuine` (mixture→compProd 因子分解 + generic continuous-channel MI chain rule asset 上流 relocate + 混合 log-density 可積分性 + fibre entropy 平行移動不変)。**判断 #21 の「mixture→compProd factorization ~50-100 行 壁」を覆した**。
    - **wall2 multivariate-mi** (`multivariate-mi`、I(W;Y^n)/I(X^n;Y^n) finiteness): `awgnConverseJoint_pair_mi_ne_top`。**ENNReal chain rule の循環は Real-form 経由時の話で、直接 `klDiv_ne_top` (discrete-input compProd chain rule) で回避**。
    - **wall3 continuous-mi-chain-rule** (`awgn-continuous-mi-chain-rule`、I(X^n;Y^n) ≤ ∑ I(X_i;Y_i)): `awgnContinuousMIChainRule_holds`。gateway atom = generic n-D MI 分解 `mutualInfoOfChannel_toReal_eq_log_density_sub` (`ChannelCoding/MIDecomp.lean`)。**chain-rule は X-input 連続 kernel measurability (`IsParallelGaussianKernelMeasurable`、`ParallelGaussian/Basic.lean` で deferred、真の Mathlib gap) が壁だが、W-input route (離散 Fin M で kernel measurability 自由化 + 決定的 DPI) で回避**。判断 #21 が「non-iid mixture chain rule = genuine 壁」と確認した item を覆した。
    - **判断 #21 frontier の訂正**: #21 line 176「残 7 は全 WALL」は converse closure 後 STALE。converse 側の壁 3 件 (mi-bridge / multivariate-mi / continuous-mi-chain-rule) はすべて closed。残る AWGN 壁は achievability 側 3 件 (`awgn-continuous-aep-gaussian` / `awgn-random-coding-bound` / `awgn-power-constraint-honest`、`AWGN/Walls.lean`) + X-input kernel-measurability gap のみ。**(2026-06-27 追記: この「achievability 側のみ残る」も STALE — achievability も同日 2026-06-12 commit `c44be725` で closure 済、`awgn_achievability` sorryAx-free。`AWGN/Walls.lean` は削除済。詳細 judgment #29)**。
    - **教訓**: 判断 #21 で「TRUE 壁を確認」とした 6 件のうち converse 側 3 件はすべて false wall だった。連続設定でも「Gaussian 混合 log-density 可積分性」「discrete-input compProd chain rule」「W-input 離散 route での measurability 回避」が効くため、連続 = Mathlib 不在 = 壁という大括りの査定は per-wall の overturn 試行で覆りうる (CLAUDE.md「壁を額面で受けない」doctrine の再確認)。
    - **将来 cleanup (low-priority、act せず)**: `ParallelGaussian/Converse/Core.lean` が `ChannelCoding/MIDecomp.lean` の generic 分解 (`*_gen` 系: `log_rnDeriv_split_gen` / `llr_compProd_prod_split_gen` 他) と同一内容の private downstream copy を持つ (両者 genuine、honesty 問題なし)。DRY cleanup 機会。
28. **2026-06-13 Ch.9 ② L-WF2 honesty 訂正 — 「完了」記録は誤り、water-filling 最適性は未証明だった**:
    - **発見**: orphan/retract cleanup の follow-up で `WFStationarityBody.lean` (宣言ゼロの空スケルトン + 5 補題を「genuinely discharged」と謳う全虚偽 docstring) と `WFCertBody.lean` (genuine な汎用 tangent 補題 `ConcaveOn.le_tangent_of_hasDerivAt` 1 本のみ + 4 補題を「fully discharged」と謳う部分虚偽 docstring) を精査。**L-WF2 (`IsWaterFillingOptimal`) を産出する定理がプロジェクト内に皆無**と確定 (産出定理0・hypothesis 5箇所のみ、`isWaterFillingOptimal_of_certificate` 等の discharge 補題全 MISSING)。capacity formula family は `h_opt` を **load-bearing 仮説**として運んでおり、moonshot/chain-rule/roadmap は「L-WF2 genuine discharge 完了」と虚偽記録していた。
    - **対応 (ユーザー判断: sorry-routed)**: `KKT.isWaterFillingOptimal_of_kkt` を新設 (`IsWaterFillingKKT → IsWaterFillingOptimal`、body = `sorry` + `@residual(plan:parallel-gaussian-wf2-optimality-plan)`)。capacity formula family (`parallel_gaussian_capacity_formula` / `_minimal` / `_discharged` / `isParallelGaussianPerCoordReduction_discharged`) から `h_opt` 仮説を drop し本補題で内部供給 → headline は L-WF2 について無条件形だが tier-2 (`#print axioms` に sorryAx)。空スケルトン `WFStationarityBody.lean` は削除。子 plan [`parallel-gaussian-wf2-optimality-plan.md`](shannon/parallel-gaussian-wf2-optimality-plan.md) 起票 (凹 tangent + 共通 KKT 乗数 Lagrange、Phase A 済 B–D 未)。build green (3472 jobs)。
    - **同日 genuine closure (sorry-routed → proof done)**: `isWaterFillingOptimal_of_kkt` の sorry を genuine 証明に。当初 plan は凹 tangent 補題 (`ConcaveOn.le_tangent_of_hasDerivAt`、orphan な `WFCertBody.lean` 在) の移設を想定したが、**`Real.log_le_sub_one_of_pos` を `u=(N_i+P'_i)/(N_i+P*_i)` に適用すると tangent 上界 `1/(2(N_i+P*_i))` が ConcaveOn/HasDerivAt なしで直接出る**ことを発見 → 移設不要、`WFCertBody.lean` は dead orphan として削除。active/inactive 場合分け (active で導関数 `=λ=1/(2ν)`、inactive で `1/(2N_i)≤λ` を `P'_i≥0` が吸収) + 相補スラックネス。helper = `noise_pos` / `waterFillingKKT_level_pos` (`ν>0` を h_kkt+hP の退化矛盾から) / `waterFillingCost_tangent_le`。`#print axioms` sorryAx-free、独立 honesty 監査 4 decl 全 PASS (`@audit:ok`、退化境界 3 点反証)。`parallel_gaussian_capacity_formula_minimal` も sorryAx-free = **proof done**。build green (3471 jobs)。
    - **教訓**: judgment #13 の「② achiever side 完全 genuine + headline honesty 是正」で `h_bdd_global`/`h_multivar_decomp`/`h_perCoordMI` は tier-5 検出・是正したが、**同じ headline の `h_kkt`/`h_opt` (L-WF1/L-WF2) は見落とした**。converse の sorryAx-free を「② 完了」と短絡し、`h_opt` が load-bearing optimization core である点を看過。sorryAx-free ≠ unconditional — 明示仮説に optimization core を積んだ tier-4 legacy。docstring が「discharged by concavity」と過大主張していたのも一因 (実体は不在)。**「完了」記録は #print axioms (sorryAx) だけでなく署名の load-bearing 仮説有無まで照合してから**。なお #print axioms 自体は誤りでなく、load-bearing 仮説 (`h_opt`) は sorry でなく**明示引数**だったため sorryAx に出なかった ← これが「sorryAx-free を完了と短絡」の罠の本質。

29. **2026-06-27 Ch.9 AWGN ③ achievability closure の roadmap destale — converse だけでなく achievability も既に sorryAx-free だった (judgment #27 と同日 2026-06-12 closure を未記録だった)**:
    - **発見**: scope-out 再判定の問い合わせで Ch.9 ③ operational 符号化定理を機械再確認 → **`awgn_achievability` も `awgn_converse` も `#print axioms` = `[propext, Classical.choice, Quot.sound]` (両側完全 sorryAx-free)**。roadmap §scope-out は judgment #27 (2026-06-12 converse closure) は記録していたが、**同日 commit `c44be725`「AWGN family 実 sorry 全閉鎖」での achievability closure を取りこぼし**、「残る scope-out は achievability 側 (continuous AEP / sphere packing、`AWGN/Walls.lean` 3 shared sorry)」と stale 主張を残していた。`AWGN/Walls.lean` は既に削除済 (現存せず)。
    - **achievability の実体**: headline `awgn_achievability` は `isAwgnTypicalityHypothesis P hP N hN h_meas hR_pos hR hε` への直接 call。callee は 580 行 genuine assembly (continuous joint typical set on ℝⁿ×ℝⁿ / Gaussian random codebook / 3 continuous-AEP bounds / sphere volume / union bound)、仮説は regularity precondition のみ (`0<P` / `(N:ℝ)≠0` / `IsAwgnChannelMeasurable` measurability) で load-bearing `*Hypothesis` 述語なし (名前 `isAwgnTypicalityHypothesis` は歴史的 artefact)。独立監査 sign-off 済 (commit `cb1af3ce`、`@audit:ok`)。
    - **訂正点**: §scope-out AWGN 項 + Ch.9 章表 (状態 `③ ✅` 追加) + frontier 行 + Mathlib 壁 (a) 量の壁 の parenthetical を destale。**Ch.9 ③ は単一文字容量に加え operational 符号化定理 (achievability+converse) として publish 可**。残る AWGN scope-out は Shannon-Hartley Nyquist 2W-DOF ((c) 真の壁、`IsTwoWDegreesOfFreedom` load-bearing で honest 開示) のみ。
    - **教訓**: judgment #27 が「converse 3 壁 closed」を記録した同日に achievability も閉じていたが、roadmap は converse 側だけ更新して achievability scope-out 文を据え置いた = **片側 closure の partial-destale 漏れ**。「continuous = Mathlib 不在 = 壁」の大括り査定 (judgment #11/#21) は per-wall overturn で覆りうる doctrine の更なる実例 (連続 AEP / sphere packing も self-build された)。scope-out の「残り○○側」記述は片方を閉じた時点で**両側を #print axioms で再確認**してから残置文を書く。
