// 原稿で使う語の採否。`build.mjs` の lintTerminology が `avoid` の語を検査し、
// 見つけたら warn を出す。
//
// 網羅は目的ではない。実際に表記が割れた語だけを 1 行ずつ足していく
// （全語を列挙する用語集は、足し忘れたところから腐るため作らない）。
// どの候補を採るかの規則は執筆原則 `.claude/rules/textbook-writing.md` §6（日本語で定着した
// 訳語 → 原語 → 人名はラテン文字 → 略語は導入しない）。なぜその語かは `why` に書く。
// 判断の記録がないと、次に迷ったとき同じ議論をやり直す。
//
// フィールド:
//   use    採用する語
//   avoid  書かない言い換え。原稿に現れたら warn（行内 code と数式の中は検査しない）
//   except 検査を免除するファイル（パスの部分一致）。免除する理由も併記すること
//   why    その語を採った理由

export const TERMS = [
  {
    use: '十分統計量',
    // 裸の「充足」も禁じる（「充足統計量」「充足性」を包含する）。第1章 1.9 節が
    // 「この非対称さが『充足』の内容である」と 1 度だけ書いており、3 語の表では拾えなかった。
    avoid: ['充足'],
    why: '日本語の統計では十分統計量が標準訳で、読者の通りがよい。sufficient の語感よりそちらを採る。',
  },
  {
    use: 'Jensen',
    avoid: ['ジェンセン'],
    why: '人名はラテン文字で統一する（Cauchy / Gibbs / Shannon / Neyman–Fisher に揃える）。',
  },
  {
    use: '像測度',
    avoid: ['push-forward', 'pushforward'],
    why: '訳語のある術語は訳語で書く（執筆原則 §6）。第1章が像測度を使っている。',
  },
  {
    use: 'エントロピー表現',
    avoid: ['entropy 表現'],
    why: '同上。1.3 節「エントロピーとの関係」・1.4 節「エントロピー表現」の見出しに揃える。',
  },
  {
    use: 'データ処理不等式',
    avoid: ['DPI'],
    why: '略語を導入せずに済む長さ。ファイルパス DPI.lean は行内 code なので検査に当たらない。',
  },
  {
    use: 'ナット',
    avoid: ['nat である', 'nat（'],
    why: '対になるビットがカタカナなので揃える。単位名以外の nat に当たらないよう文脈ごと指定する。',
  },
  {
    use: 'ファノの不等式',
    avoid: ['ファノ不等式'],
    why: '第1章 1.10 節の見出しの形。第4章の草稿だけが「の」を落としていた。',
  },
  {
    use: 'i.i.d.',
    avoid: ['独立同分布'],
    // 初出の併記「独立同分布 (i.i.d.)」を置く節だけ免除する。第1章 1.5 節は形式化上の
    // 注記で 1 度だけ和語形を使っており、そこも同じ扱い。
    except: ['ch02/01-aep.md', 'ch01/05-chain-rules.md'],
    why: '執筆原則 §6 の「原語の略語そのものが普及の単位になっている場合」にあたる。第1章が地の文で i.i.d. を走らせているので揃える。初出でだけ和語を併記する。',
  },
  {
    use: '相対エントロピー',
    // 「KL ダイバージェンス」だけを禁じると、裸の「KL の」「KL は」が抜ける。第1章の
    // 注記が 3 箇所でそう走っていた。初出の併記は 2 語の形でしか現れないので、
    // 単独名詞としての用法だけを個別に禁じる。
    avoid: ['KL ダイバージェンス', 'KL の', 'KL は', 'KL として', 'KL に'],
    // 1.3 節の記法先取りと 1.6 節の導入では併記が要る。第5章は草稿で、第1章と割れている
    // 軸の一つ（執筆原則 §11）。触るときに合わせる。
    except: ['ch01/03-mutual-information.md', 'ch01/06-information-inequality.md', 'ch05-max-entropy.md'],
    why: '情報理論の日本語文献では相対エントロピーが定着している（KL ダイバージェンスが優勢なのは機械学習）。初出の併記だけに使う。',
  },
  {
    use: '情報源符号',
    // 「源符号」は「情報源符号」の部分文字列なので、正しく書いた側が当たらないよう
    // lintTerminology が採用語を先に伏せる。定理名（情報源符号化定理）も語幹で拾える。
    avoid: ['源符号'],
    why: '情報源符号化が学会用語で、日本語の教科書もこちらが標準。地の文が「情報源」と書き続けているのに複合語だけ縮めると、第4章の通信路符号化定理と対にならない。',
  },
];
